---
phase: 02-two-rulebooks-one-vessel
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/gptrpg/rules_core/entities.py
  - src/gptrpg/rulebooks/openquest_creatures.py
  - src/gptrpg/rulebooks/dungeonworld_like.py
  - LICENSES.md
  - tests/test_entities.py
autonomous: true
requirements: [RIG-08]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "적/NPC 상태값 하나의 모양이 {이름, 현재값, 최대값(선택), 바닥날 때 효과 참조} 네 칸뿐이다 — `StatEntry`의 필드 목록이 정확히 이 넷이고 그 이상도 이하도 아니다 (D-20)"
    - "적/NPC 하나가 가질 수 있는 상태값 개수에 코드가 상한을 두지 않는다 — 상태값 하나짜리 적과 열 개짜리 적이 같은 `Entity` 그릇에 코드 수정 없이 들어간다 (D-21 / ROADMAP 성공조건 4 / RIG-08d)"
    - "플랫폼 코드에 체력·피해·태그처럼 룰북 고유의 개념을 가리키는 칸이 하나도 없다 — `Entity`와 `StatEntry`의 필드 이름 전부가 룰북 중립이고, 무엇이 체력인지는 룰북이 선언한 `name` 문자열만으로 정해진다 (성공조건 4 / D32)"
    - "OpenQuest 크리처 두 종의 상태값 수치가 SRD 원문 그대로다 — 이 단계가 지어낸 숫자가 하나도 없다 (D-18)"
    - "바닥날 때 무슨 일이 일어나는지는 플랫폼이 아니라 룰북이 정한다 — 플랫폼은 참조 문자열 자리만 열어 두고 그 내용을 해석하지 않는다 (D32 원문, 효과 실제 연산은 M1의 D7)"
    - statement: "잘못된 상태값 선언(빈 이름, 음수 최대값, 한 적 안에서 이름 중복)은 만들어지는 순간 예외로 실패한다 — 조용히 통과해 판정·집계를 왜곡하는 경로가 없다"
      verification: backstop
    - statement: "값이 0인 상태값(예: 마법점 0)은 정상이며 거부되지 않는다"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT 원문에서 확인하지 않은 크리처 수치를 OpenQuest SRD에서 옮긴 것처럼 표기하면 안 된다 — SRD 페이지가 아닌 메인 룰북·3차 편찬물 출처를 섞는 것 포함 (RESEARCH.md Pitfall 5)"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT 실제 룰북에서 오지 않은 예시 데이터를 룰북 콘텐츠인 것처럼 두면 안 된다 — 그릇의 모양을 보이기 위한 자체 작성 수치는 그렇다고 명시해야 한다 (D-18이 자체 창작 미니 룰북을 배제한 이유)"
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - src/gptrpg/rules_core/entities.py
    - src/gptrpg/rulebooks/openquest_creatures.py
    - tests/test_entities.py
  key_links:
    - "rules_core/entities.py(그릇의 모양) ↔ rulebooks/openquest_creatures.py(그 그릇에 담긴 실제 내용) — .importlinter layers 계약이 역방향 import를 막는다"
    - "StatEntry.depleted_effect_ref(참조 문자열) → M1의 D7 효과 DSL(이 단계는 자리만 연다)"
---

<objective>
판정 방식이 다른 두 룰북의 적과 NPC가 같은 그릇에 들어가는지 확인한다. 상태값이 하나뿐인
적과 열 개인 적이 같은 자료구조에 코드 수정 없이 담기고, 플랫폼 코드에는 체력·피해·태그
같은 룰북 고유 개념이 한 칸도 없어야 한다 (D-20, D-21, ROADMAP 성공조건 4).

Purpose: D32 원문이 정한 경계 — "그릇은 플랫폼, 내용은 룰북" — 을 자료구조로 직접
만족시킨다. 던전월드 계열 적(체력 하나)과 OpenQuest 크리처(체력·마법점 등 여럿)를
같은 튜플 길이만 다르게 넣을 수 있어야, 개수를 코드가 제한하지 않는다는 것이 증명된다.

Output: `rules_core/entities.py`(`Entity`/`StatEntry`와 그 검증), OpenQuest SRD에서 그대로
옮긴 실제 크리처 두 종, 그리고 상태값 하나짜리 적이 같은 그릇을 통과함을 보이는 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-two-rulebooks-one-vessel/02-CONTEXT.md
@.planning/phases/02-two-rulebooks-one-vessel/02-RESEARCH.md
@.planning/phases/02-two-rulebooks-one-vessel/02-PATTERNS.md
@.planning/phases/02-two-rulebooks-one-vessel/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**새 파일**
- `src/gptrpg/rules_core/entities.py`
- `src/gptrpg/rulebooks/openquest_creatures.py`
- `tests/test_entities.py`

**새 심볼**
- `rules_core/entities.py`: `StatEntry`(frozen dataclass), `Entity`(frozen dataclass),
  `InvalidStatEntry`(Exception), `InvalidEntity`(Exception)
- `rulebooks/openquest_creatures.py`: `OPENQUEST_GOBLIN`, `OPENQUEST_SKELETON`,
  `OPENQUEST_CREATURES`(tuple)
- `rulebooks/dungeonworld_like.py`: `EXAMPLE_SINGLE_STAT_FOE`

**새 CLI 플래그:** 없음
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 상태값 그릇의 모양과 그 검증 (D-20, D-21)</name>
  <reversibility rating="reversible">새 모듈 하나. 아직 사건 스키마에도 명령에도 배선되지
  않으므로(엔티티의 상태 변경을 사건으로 남기는 것은 M1) 되돌리면 파일 삭제로 끝난다.</reversibility>
  <files>src/gptrpg/rules_core/entities.py, tests/test_entities.py</files>
  <read_first>
    - `src/gptrpg/rules_core/resolution.py` — `Modifier`(29-35행)의 "값 + 출처/의미" 패턴과 `@dataclass(frozen=True)` 관례, `CheckOutcome`(38-47행)이 `tuple[...]`을 쓰는 이유
    - `src/gptrpg/rules_core/rulebook.py` — 02-01이 세운 `GradeBand`/`Rulebook` 선언 모양과 모듈 docstring 관례("모양만 정의한다")
    - `src/gptrpg/rules_core/reducer.py` — `UnknownEventType`(36-46행)이 "모르면 예외" 규율을 구현한 방식
    - `src/gptrpg/event_log/schema.py` — 42행의 `ConfigDict(extra="forbid", frozen=True)` 엄격 검증 관례(RESEARCH.md Security Domain V5가 새 모델에도 같게 적용하라고 지목한 것)
    - `.importlinter` — rules_core forbidden 계약(이 모듈도 시간·무작위·파일·네트워크를 못 쓴다)
    - `.planning/phases/02-two-rulebooks-one-vessel/02-RESEARCH.md` — Pattern 4(272-292행)의 제안 모양
  </read_first>
  <behavior>
    `tests/test_entities.py`가 먼저 실패한 뒤 통과해야 할 것들:
    - `StatEntry`의 필드 이름 집합이 정확히 `{name, current, max, depleted_effect_ref}` 넷이다
      (`dataclasses.fields`로 단언 — 체력·피해·태그 같은 칸이 하나도 없다는 구조적 증거)
    - `Entity`의 필드 이름 집합이 정확히 `{entity_id, display_name, rulebook_id, stats}` 넷이다
    - 상태값 하나짜리 `Entity`와 열 개짜리 `Entity`가 같은 클래스로 만들어진다 — 개수를
      막는 검사가 없다
    - `stats`가 빈 튜플인 `Entity`도 만들어진다 (상태값이 없는 NPC)
    - `max`가 None인 상태값(상한이 없는 값)이 정상이다
    - `current`가 0인 상태값이 정상이다 (마법점 0인 언데드)
    - `current`가 음수인 상태값이 정상이다 (0 아래로 깎인 값 — 그 뜻은 룰북이 정한다)
    - `max`가 음수면 `InvalidStatEntry`가 난다
    - `name`이 비었거나 공백뿐이면 `InvalidStatEntry`가 난다
    - `depleted_effect_ref`가 빈 문자열이면 `InvalidStatEntry`가 난다 (None은 정상)
    - 한 `Entity` 안에 같은 `name`의 상태값이 둘이면 `InvalidEntity`가 난다
    - `entity_id`/`display_name`/`rulebook_id`가 비면 `InvalidEntity`가 난다
    - `Entity`와 `StatEntry`가 얼어 있다 — 필드에 대입하면 예외가 난다
  </behavior>
  <action>
  `src/gptrpg/rules_core/entities.py`를 새로 만든다. 모듈 docstring은 `rulebook.py`·`dice.py`가
  세운 짝 문장을 따른다 — "적과 NPC의 숫자를 담는 **그릇**만 정의한다. 그 값이 무슨 뜻인지,
  바닥나면 무슨 일이 일어나는지는 룰북이 정한다."

  `StatEntry`(frozen dataclass) 필드는 D-20이 확정한 네 칸뿐이다: `name: str`,
  `current: int`, `max: int | None = None`, `depleted_effect_ref: str | None = None`.
  칸을 더 늘리지 않는다 — "체력"이라는 개념은 `name`에 담긴 문자열이지 코드의 칸이 아니다.
  `depleted_effect_ref`는 참조 문자열일 뿐이고 플랫폼은 그 내용을 해석하지 않는다(효과의 실제
  연산은 D7, M1 범위). 이 두 문장을 필드 옆 주석으로 남긴다.

  `Entity`(frozen dataclass) 필드는 `entity_id: str`, `display_name: str`, `rulebook_id: str`,
  `stats: tuple[StatEntry, ...] = ()`. `stats`는 `list`가 아니라 `tuple`이다 —
  `CheckOutcome.modifiers`가 세운 "규칙 코어 안의 순서 있는 불변 묶음은 튜플" 관례를 따른다.
  D-21이 요구한 "제한 없음"은 튜플 길이에 어떤 검사도 두지 않는 것으로 만족된다.

  검증은 `__post_init__`에서 하고, 실패는 반드시 예외로 드러낸다(`InvalidStatEntry`,
  `InvalidEntity`) — `UnsupportedModifier`/`UnknownEventType`이 세운 "조용히 건너뛰지 않는다"
  규율과 같은 이유다. 두 예외는 무엇이 왜 거부됐는지 메시지에 담고, 관련 값을 속성으로
  노출한다(`UnsupportedModifier`가 `modifier_type`/`source`를 노출하는 방식). 검사 항목은 위
  `<behavior>`가 정한 그대로이며, **음수 `current`는 거부하지 않는다**(0 아래로 깎인 값의
  뜻은 룰북이 정한다) — 이 판단을 docstring에 한 줄로 남긴다.

  이 모듈은 `rules_core` 안이므로 시간·무작위·파일·네트워크를 못 쓴다. pydantic도 쓰지
  않는다(Phase 1이 `rules_core` 모든 모듈을 typing/dataclasses/collections.abc만으로 유지해
  왔고 `test_boundaries.py`가 그것을 검사한다) — 대신 `extra="forbid", frozen=True`가 노리던
  엄격함을 frozen dataclass + `__post_init__` 검증으로 같게 얻는다.

  `tests/test_entities.py`를 새로 쓰고 위 `<behavior>` 항목을 전부 덮는다. 필드 목록 단언은
  `dataclasses.fields`로 이름 집합을 만들어 정확히 비교한다(부분집합이 아니라 완전 일치) —
  나중에 누가 `hp` 같은 칸을 하나 더하면 즉시 빨간불이 켜져야 하기 때문이다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_entities.py -x -q &amp;&amp; uv run pytest -q &amp;&amp; uv run lint-imports &amp;&amp; uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_entities.py -q`가 13개 이상 통과한다
    - 필드 목록 완전 일치 단언이 존재한다: `grep -c "dataclasses.fields\|fields(" tests/test_entities.py`가 2 이상이다
    - `src/gptrpg/rules_core/entities.py`가 pydantic을 import하지 않는다: `grep -c "pydantic" src/gptrpg/rules_core/entities.py`가 0이다
    - 두 예외가 정의·사용된다: `grep -c "InvalidStatEntry" src/gptrpg/rules_core/entities.py`가 3 이상, `grep -c "InvalidEntity" src/gptrpg/rules_core/entities.py`가 3 이상이다
    - `uv run lint-imports`가 전 계약 kept를 보고한다 (entities.py가 rules_core 순수성 계약을 어기지 않는다)
    - `uv run pytest tests/test_boundaries.py -q`가 통과한다 (경계 검사가 새 모듈에도 그대로 적용된다)
  </acceptance_criteria>
  <done>
  D-20의 네 칸과 D-21의 무제한 목록이 자료구조가 되었고, 잘못된 선언은 만들어지는 순간
  예외로 실패한다. 플랫폼 코드에는 룰북 고유 개념을 가리키는 칸이 하나도 없다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 두 룰북의 적을 실제 SRD 수치로 같은 그릇에 담는다 (D-18, 성공조건 4)</name>
  <reversibility rating="reversible">데이터 선언 모듈 하나와 예시 하나. 플랫폼 코드는 안 바뀐다.</reversibility>
  <files>src/gptrpg/rulebooks/openquest_creatures.py, src/gptrpg/rulebooks/dungeonworld_like.py, LICENSES.md, tests/test_entities.py</files>
  <read_first>
    - `src/gptrpg/rules_core/entities.py` — Task 1이 만든 `Entity`/`StatEntry`와 검증 규칙
    - `src/gptrpg/rulebooks/openquest.py` — 02-01이 세운 SRD 출처 docstring 형식과 `OPENQUEST_ID`
    - `src/gptrpg/rulebooks/dungeonworld_like.py` — 02-01이 세운 등급 밴드 선언과 `DUNGEONWORLD_LIKE_ID`
    - `LICENSES.md` — 02-01이 옮겨 담은 CC BY 4.0 첨부 문구와 "어떤 파일이 이 라이선스 콘텐츠를 담는가" 목록
    - `.planning/phases/02-two-rulebooks-one-vessel/02-RESEARCH.md` — Pitfall 5(SRD 본문과 메인 룰북을 혼동하지 말 것), Open Question 3(크리처 수치 미확인 → 이 태스크가 답이다)
  </read_first>
  <behavior>
    - OpenQuest 고블린과 스켈레톤이 `Entity`로 만들어지고, 각각 상태값 열 개를 갖는다
      (능력치 일곱 + 체력 + 마법점 + 방어점)
    - 두 크리처의 체력·마법점 수치가 아래 `<action>`에 적힌 SRD 값과 정확히 같다
    - 스켈레톤의 마법점·지력·의지·매력이 0이고, 그 0이 거부되지 않는다
    - 던전월드 계열 예시 적은 상태값이 **하나뿐**이고, 같은 `Entity` 클래스로 만들어진다
    - 상태값 하나짜리와 열 개짜리를 한 목록에 담아 순회해도 어떤 분기도 필요 없다 —
      `stats` 튜플 길이만 다르다 (D-21의 실물 증거)
    - 체력에 해당하는 상태값에는 `depleted_effect_ref`가 붙어 있고, 능력치에는 붙어 있지 않다
      (무엇이 "바닥나는 값"인지는 룰북 선언이 정한다)
    - 각 크리처의 `rulebook_id`가 자기가 속한 룰북의 식별자와 같다
  </behavior>
  <action>
  ① `src/gptrpg/rulebooks/openquest_creatures.py`를 새로 만들고 OpenQuest SRD의
  `creatures-g`(고블린)와 `creatures-s`(스켈레톤) 두 종을 그대로 옮긴다. **수치를 지어내지
  않는다** — 아래가 SRD 원문에서 확인된 값이다.

  고블린: STR 10, CON 10, DEX 17, SIZ 7, INT 11, POW 10, CHA 7 / 체력(Hit Points) 9 /
  마법점(Magic Points) 10 / 방어점(Armour Points) 2(Leather).
  스켈레톤: STR 13, CON 4, DEX 11, SIZ 11, INT 0, POW 0, CHA 0 / 체력 8 / 마법점 0 /
  방어점 2(Leather).

  각 값을 `StatEntry`로 만든다. 능력치 일곱과 방어점은 소모되는 값이 아니므로 `max=None`,
  `depleted_effect_ref=None`. 체력과 마법점은 `current == max`(만신창이가 아닌 기본 상태)로
  두고, 바닥날 때 효과 참조를 문자열로 채운다(예: 룰북 식별자를 접두어로 갖는
  `"openquest.hit_points_depleted"` 형태). 그 문자열이 가리킬 실제 연산은 M1의 D7 몫이므로
  여기서는 참조만 둔다.

  상태값 이름은 SRD가 쓰는 이름을 그대로 쓴다 — 플랫폼이 아니라 룰북이 정하는 어휘이기
  때문이다. 무기·주문·이동력·특수능력 같은 나머지 항목은 담지 않는다(룰북 데이터의 필드
  단위 상세 규격은 M0 범위 밖이라고 ROADMAP이 명시했다). 모듈 docstring에 출처 페이지
  경로 두 개(`openquestrpg.com/srd/creatures/creatures-g/`,
  `openquestrpg.com/srd/creatures/creatures-s/`)와 "메인 룰북이 아니라 SRD 페이지 원문에서
  옮겼다"는 사실, 그리고 `LICENSES.md` 참조를 남긴다.

  `OPENQUEST_CREATURES` 튜플로 둘을 묶는다.

  ② `src/gptrpg/rulebooks/dungeonworld_like.py`에 상태값 하나짜리 예시 적
  `EXAMPLE_SINGLE_STAT_FOE`를 더한다. 이 수치는 **어떤 룰북 원문에서도 오지 않았다** —
  그릇에 상태값 하나만 담아도 되는지 보이기 위한 자체 작성 예시다. 그 사실을 변수 바로 위
  주석과 모듈 docstring에 명시한다(D-18이 자체 창작 룰북을 배제한 이유가, 지어낸 것을
  실제 콘텐츠처럼 두지 않기 위함이므로 라벨이 곧 준수다).

  ③ `LICENSES.md`의 "이 라이선스 콘텐츠를 담은 파일" 목록에 `openquest_creatures.py`를 더한다.

  ④ `tests/test_entities.py`에 위 `<behavior>`의 항목들을 더한다. 마지막 항목(하나짜리와
  열 개짜리를 한 목록에서 분기 없이 순회)은 두 적을 한 튜플에 담고 각각의 상태값을 이름으로
  찾아 합계를 내는 식으로, 코드에 룰북별 분기가 필요 없음을 실물로 보인다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_entities.py -x -q &amp;&amp; uv run pytest -q &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_entities.py -q`가 20개 이상 통과한다
    - SRD 수치가 그대로 들어 있다: `grep -c "17" src/gptrpg/rulebooks/openquest_creatures.py`가 1 이상(고블린 DEX), 그리고 두 크리처의 체력 9·8과 마법점 10·0이 테스트에서 상수로 단언된다
    - 두 크리처가 각각 상태값 열 개를 갖는다는 단언이 테스트에 있다
    - 상태값 하나짜리 적이 존재한다: `grep -c "EXAMPLE_SINGLE_STAT_FOE" src/gptrpg/rulebooks/dungeonworld_like.py`가 1 이상이고, 그 적의 `stats` 길이가 1이라는 단언이 테스트에 있다
    - 자체 작성 표기가 있다: `grep -c "SRD" src/gptrpg/rulebooks/openquest_creatures.py`가 1 이상이고, `dungeonworld_like.py`의 예시 적 근처에 원문 출처가 없다는 표기가 있다
    - `LICENSES.md`가 `openquest_creatures.py`를 언급한다: `grep -c "openquest_creatures" LICENSES.md`가 1 이상이다
    - `grep -c "gptrpg.rulebooks" src/gptrpg/rules_core/entities.py`가 0이다 (그릇이 내용을 모른다)
    - `uv run pytest -q` 전체 통과
  </acceptance_criteria>
  <done>
  실제 OpenQuest SRD 크리처 두 종(상태값 열 개)과 상태값 하나짜리 적이 같은 `Entity` 그릇에
  들어가고, 그 둘을 함께 다루는 데 어떤 코드 분기도 필요 없다. 지어낸 수치는 지어냈다고
  적혀 있다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 룰북이 선언한 적 상태값 → 플랫폼 자료구조 | 손으로 쓴 수치가 검증 없이 그릇에 들어가면 이후 어떤 계산도 조용히 틀어진다. M1에서 이 자리가 업로드된 파일이 되면 완전한 신뢰 경계가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | `rules_core/entities.py` `StatEntry.__post_init__` | high | mitigate | 음수 최대값·빈 이름·빈 효과 참조를 `InvalidStatEntry`로 즉시 거부한다. 조용히 통과하면 잘못된 상한이 이후 모든 소모 계산의 기준이 되고, 그 기준은 어디서도 복원되지 않는다 (RESEARCH.md Security Domain V5) |
| T-02-10 | Tampering | `rules_core/entities.py` `Entity.__post_init__` | medium | mitigate | 한 적 안의 상태값 이름 중복을 `InvalidEntity`로 거부한다. 이름이 겹치면 "이름으로 찾기"가 어느 쪽을 돌려줄지 정해지지 않아 계산이 비결정적이 된다 |
| T-02-11 | Repudiation | `rulebooks/openquest_creatures.py` 출처 표기 | medium | mitigate | 모듈 docstring이 SRD 페이지 경로와 `LICENSES.md`를 가리키고, 자체 작성 예시는 자체 작성이라고 표기한다. 출처가 섞이면 나중에 무엇을 라이선스 아래 배포할 수 있는지 판단할 수 없다 (LAW-04) |
| T-02-SC | Tampering | 패키지 설치 | n/a | accept | 이 계획도 새 패키지를 설치하지 않는다 |

**ASVS 수준:** 1. **차단 기준:** high. V5(입력 검증)만 해당.
</threat_model>

<verification>
- `uv run pytest tests/test_entities.py -q` — RIG-08d 커버 명령 통과
- `uv run pytest -q` — 전체 통과
- `uv run lint-imports` — 전 계약 kept (rules_core가 rulebooks를 모른다)
- `uv run ruff check .` — 위반 없음
</verification>

<success_criteria>
- 두 룰북의 적이 같은 그릇에 들어간다 (성공조건 4 / RIG-08d)
- 플랫폼 코드에 룰북 고유 개념 칸이 하나도 없다 (필드 목록 완전 일치 단언으로 고정)
- OpenQuest 크리처 수치가 SRD 원문 그대로다 (D-18, RESEARCH.md Open Question 3 해소)
</success_criteria>

<output>
Create `.planning/phases/02-two-rulebooks-one-vessel/02-03-SUMMARY.md` when done.

SUMMARY에 반드시 담을 것: 적/NPC 그릇을 만들면서 **플랫폼 코드에 넣고 싶었지만 참은 개념**
(예: 체력을 특별 취급하는 칸, 상태값 개수 상한, 최대값을 필수로 만들기)을 구체적으로 적는다.
이것이 02-04의 `02-INTERFACE-CHANGES.md`에서 가장 무겁게 다뤄야 할 종류의 증거다.
</output>
