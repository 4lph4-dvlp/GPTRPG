---
phase: 02-two-rulebooks-one-vessel
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/gptrpg/cli/main.py
  - tests/test_cli.py
  - .planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md
autonomous: false
requirements: [RIG-08, HYP-03]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "명령줄에서 룰북을 골라 d100 판정을 요청할 수 있고, 같은 기록을 재생하면 그 룰북의 등급 이름이 그대로 나온다 — 두 룰북이 실제로 같은 도구로 굴러간다"
    - "새 수정치 유형을 명령줄에서 쓰는 데 인자 파싱 코드를 한 글자도 고칠 필요가 없다 — `_parse_modifier`가 이미 '유형:값:출처' 문자열을 유형 이름을 모른 채 받는다 (데이터로 버틴 자리의 실물 증거)"
    - "두 번째 룰북을 넣으면서 플랫폼 코드를 고쳐야 했는지 아닌지가 사람이 읽을 수 있는 단일 문서 한 곳에 기록된다 — git log를 훑어야 알 수 있는 상태가 아니다 (ROADMAP 성공조건 5 / D-22 / HYP-03)"
    - "그 문서가 **고친 곳**과 **고치지 않고 데이터로 버틴 곳**을 둘 다 담고, 뒤쪽을 앞쪽보다 무겁게 다룬다 (D-22: '고치고 싶었지만 참았다'가 추상화가 옳다는 가장 강력한 증거)"
    - "`EVENT_SCHEMA_VERSION`을 올렸는지 아닌지와 그 판단의 이유가 그 문서에 명시돼 있다 (RESEARCH.md Pitfall 4가 요구한 명시적 답)"
    - "문서의 '고친 곳' 목록이 실제 git diff와 일치한다 — 고친 파일을 빠뜨리거나 안 고친 파일을 적지 않았다"
    - statement: "문서가 사람이 읽고 성공조건 5를 판정할 수 있는 상태다 (고친 곳/참은 곳이 실제로 존재하고 각각에 이유가 붙어 있다)"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT 실제로 고친 플랫폼 코드를 '데이터로 버텼다'로 옮겨 적으면 안 된다 — HYP-03의 채점이 이 기록의 정직성에만 의존하고, 낙관적으로 적힌 기록은 유저 콘텐츠 마켓의 존폐 판단을 조용히 틀리게 만든다"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT 이 단계가 남긴 한계를 숨기면 안 된다 — 세 번째 판정 방식이 들어올 때 또 늘어날 분기(`_RESOLVERS`), `grading.py`에 남은 던전월드 세 이름, 리듀서의 판 1 해석 분기는 전부 기록돼야 한다"
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - .planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md
  key_links:
    - "cli roll --rulebook → ResolveCheck.rulebook_id → get_rulebook → resolution_method → 순수 함수"
    - "02-01/02-02/02-03의 SUMMARY.md '고친 곳/참은 곳' 절 → 02-INTERFACE-CHANGES.md의 단일 기록"
    - "02-INTERFACE-CHANGES.md → HYP-03 채점 → Phase 6의 여섯 가설 채점표"
---

<objective>
두 번째 룰북을 실제로 손에 쥘 수 있게 만들고(명령줄에서 룰북 선택), 이 단계의 유일한
숙제를 닫는다 — 플랫폼 코드를 고쳐야 했는지 아닌지를 사람이 읽을 수 있는 단일 문서에
기록한다 (D-22, ROADMAP 성공조건 5, HYP-03).

Purpose: 여섯 가설 중 이 단계만이 플레이 없이 데이터 작성만으로 답할 수 있다. 그 답은
코드가 아니라 **기록**이다 — "명확히 기록된다"는 문구는 사람이 직접 읽고 판단할 수 있는
한 장소를 요구하고, git log를 훑어야 확인되는 것은 추론이지 기록이 아니다.

Output: `roll --rulebook` 플래그, 그리고 고친 곳·참은 곳을 둘 다 담은
`02-INTERFACE-CHANGES.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-two-rulebooks-one-vessel/02-CONTEXT.md
@.planning/phases/02-two-rulebooks-one-vessel/02-RESEARCH.md
@.planning/phases/02-two-rulebooks-one-vessel/02-01-SUMMARY.md
@.planning/phases/02-two-rulebooks-one-vessel/02-02-SUMMARY.md
@.planning/phases/02-two-rulebooks-one-vessel/02-03-SUMMARY.md
</context>

<flagged_assumptions>
결정론적 엣지 프로브(`edge-probe.cjs`)가 이 단계의 두 요구사항을 모두
`unclassified`/`unresolved`로 돌려주었다 — 요구사항 문장만으로는 엣지 술어를 자동으로
유도하지 못했다는 뜻이다. 프로토콜상 이것은 조용한 누락이 아니라 **명시적 플래그 항목**이며,
아래 술어들은 기계 분류기가 아니라 **플래너가 RESEARCH.md·CONTEXT.md·ROADMAP 성공조건을
직접 읽고 유도한 것**이다. 검증 단계는 이 사실을 알고 읽어야 한다.

| ID | 기계 분류 결과 | 플래너가 유도한 실제 술어 | 어디서 검증되나 |
|---|---|---|---|
| RIG-08 | unclassified / unresolved | ① 두 판정 방식이 같은 `CheckOutcome`·`CheckResolved` 모양을 쓴다 ② 룰북 등급 이름이 플랫폼 세 계층 어디에도 문자열로 없다 ③ 수정치 네 유형이 전부 계산 결과를 바꾼다 ④ 상태값 1개짜리와 10개짜리 적이 같은 `Entity`에 들어간다 ⑤ 두 번째 룰북 추가에 `rules_core`의 판정·등급 함수를 새로 만드는 것 외에 기존 함수 본문을 고칠 필요가 없다 | ①②는 02-01(`test_tracer_d100.py`), ③은 02-02(`test_resolution_d100.py -k modifier`), ④는 02-03(`test_entities.py`), ⑤는 이 계획의 `02-INTERFACE-CHANGES.md` |
| HYP-03 | unclassified / unresolved | ① 두 번째 룰북이 실제로 들어가 판정이 끝까지 돈다 ② 그 과정에서 고친 플랫폼 코드가 파일 단위로 열거된다 ③ 고칠 뻔했지만 데이터로 해결한 자리가 그보다 더 자세히 열거된다 ④ 그 기록이 SUMMARY들에 흩어지지 않고 단일 문서에 있다 ⑤ 사람이 그 문서만 읽고 성공조건 5를 판정할 수 있다 | 전부 이 계획의 Task 2·Task 3 |

**유도 근거의 한계:** ⑤("고칠 필요가 없다"의 경계)는 정량 기준이 없다 — 이번 단계에서
실제로 고친 파일이 여덟 개이므로 "코드 수정 없이"라는 요구사항 문구는 **문자 그대로는 이미
거짓**이다. 이 단계가 답하는 실제 질문은 "고쳤는가"가 아니라 "고친 것이 두 번째 룰북 **때문에**
불가피했는가, 아니면 세 번째·네 번째 룰북에도 또 고쳐야 하는 종류인가"다. Task 2의 문서는
고친 자리마다 이 구분을 붙여야 한다. 이 해석 자체가 플래너의 판단이므로 Task 3의 사람 확인이
그것을 승인하거나 뒤집는다.
</flagged_assumptions>

<artifacts_this_phase_produces>
**새 파일**
- `.planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md` (D-22가 요구한
  문서. 계획 단계에서 확정한 최종 파일명이다 — 가칭이 아니다)

**새 심볼**
- `cli/main.py`: `roll` 하위 명령의 `--rulebook` 인자

**새 CLI 플래그**
- `gptrpg submit --db … --session … roll --rulebook <룰북 식별자>` (기본값:
  `dungeonworld_like` — 없으면 Phase 1과 완전히 같게 동작한다)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 명령줄에서 룰북을 고른다</name>
  <reversibility rating="reversible">인자 하나 추가. 기본값이 Phase 1 동작이라 되돌려도
  기존 사용법이 안 깨진다.</reversibility>
  <files>src/gptrpg/cli/main.py, tests/test_cli.py</files>
  <read_first>
    - `src/gptrpg/cli/main.py` — `_build_command`의 `roll` 갈래(50-57행), `roll_parser` 인자 정의(161-165행), `_parse_modifier`(27-33행), `_cmd_replay`의 출력(112-134행)
    - `src/gptrpg/session_actor/actor.py` — 02-01 이후의 `ResolveCheck` 필드 목록과 `rulebook_id` 기본값
    - `src/gptrpg/rulebooks/__init__.py` — `RULEBOOKS` 등록소와 `get_rulebook`
    - `tests/test_cli.py` — 기존 CLI 테스트가 argv 목록과 출력 문자열을 단언하는 방식
    - `tests/test_tracer.py` — 46행 부근 replay 출력 테스트와 103행의 등급 단언
  </read_first>
  <behavior>
    - `roll --rulebook openquest --target 50 --move ...`으로 판정을 넣으면 순번이 출력되고
      종료 코드가 0이다
    - 그 기록을 `replay`하면 마지막 판정 등급 줄에 OpenQuest가 선언한 등급 이름이 나온다
    - `--rulebook`을 안 주면 Phase 1과 완전히 같게 동작한다 (기존 CLI 테스트가 그대로 통과)
    - 등록되지 않은 룰북 이름을 주면 한 줄짜리 오류 메시지가 표준오류로 나가고 종료 코드가
      1이며, 파이썬 역추적이 그대로 새어 나오지 않는다
    - `--modifier target_shift:20:난이도` 형태로 02-02가 만든 새 수정치 유형을 **인자 파싱
      코드를 고치지 않고** 그대로 쓸 수 있다
  </behavior>
  <action>
  `cli/main.py`의 `roll` 하위 파서에 `--rulebook` 인자를 더한다. 기본값은
  `ResolveCheck`가 쓰는 것과 같은 던전월드 계열 식별자이며, 그 상수를
  `gptrpg.rulebooks.dungeonworld_like`에서 가져와 쓴다(문자열을 CLI에 다시 적어 넣지 않는다).
  `_build_command`의 `roll` 갈래가 그 값을 `ResolveCheck(rulebook_id=...)`로 넘긴다.

  `--target`은 그대로 재사용한다 — 굴려서 밑도는 방식에서 이 값은 기술값 백분율이다.
  `roll_parser`의 `--target` 도움말에 두 방식에서 이 값이 각각 무슨 뜻인지 한 줄로 적는다.
  인자를 새로 만들지 않는 이 선택 자체가 "같은 판정 요청 모양"의 증거이므로 Task 2의 문서에
  들어간다.

  `_parse_modifier`는 **한 글자도 고치지 않는다.** 이미 유형 이름을 모른 채 문자열을 받으므로
  02-02가 만든 세 유형이 코드 수정 없이 통과한다 — 이것이 D-22가 말한 "데이터의 type 칸으로
  해결했다"의 실물 사례다.

  등록되지 않은 룰북은 액터가 `CommandRejected`로 거부하고, `_cmd_submit`의 기존
  `except (CommandRejected, SequenceConflict)` 경로가 이미 한 줄 오류 + 종료 코드 1로 바꾼다 —
  새 예외 처리를 더하지 않는다. 이 경로가 실제로 그렇게 도는지 테스트로 확인한다.

  `tests/test_cli.py`에 위 `<behavior>` 항목을 더한다. 기존 테스트는 하나도 고치지 않는다
  (고쳐야 한다면 그것 자체가 기본값이 Phase 1 동작이 아니라는 뜻이므로 구현을 고친다).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -q &amp;&amp; uv run pytest -q &amp;&amp; uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py -q`가 통과하고, 기존 테스트가 수정 없이 전부 포함돼 있다 (`git diff --stat tests/test_cli.py`가 추가만 보이고 삭제 줄이 0이다)
    - `grep -c "rulebook" src/gptrpg/cli/main.py`가 2 이상이다
    - `git diff src/gptrpg/cli/main.py`에 `_parse_modifier` 함수 본문 변경이 없다
    - 실제 CLI 왕복이 통과한다: 임시 DB에 `roll --rulebook openquest --target 50`을 넣고 `replay`를 돌렸을 때 마지막 판정 등급 줄이 OpenQuest 선언 등급 이름 중 하나라는 테스트가 있다
    - 없는 룰북 이름에 대해 종료 코드 1 + 표준오류 한 줄을 단언하는 테스트가 있다
    - `uv run pytest -q` 전체 통과
  </acceptance_criteria>
  <done>
  명령줄 하나로 두 룰북을 골라 판정할 수 있고, 새 수정치 유형은 인자 파싱 코드를 고치지 않고
  쓸 수 있으며, Phase 1의 사용법이 하나도 안 깨졌다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 고친 곳과 참은 곳을 한 문서에 기록한다 (D-22, 성공조건 5)</name>
  <reversibility rating="reversible">문서 하나.</reversibility>
  <files>.planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md</files>
  <read_first>
    - `.planning/phases/02-two-rulebooks-one-vessel/02-01-SUMMARY.md`, `02-02-SUMMARY.md`, `02-03-SUMMARY.md` — 각 계획이 남긴 "고친 곳/참은 곳" 절
    - `.planning/phases/02-two-rulebooks-one-vessel/02-CONTEXT.md` — D-22 원문(40-42행)과 `<specifics>` 절(118행, 참은 곳이 더 중요하다는 사용자의 명시적 우선순위)
    - `.planning/phases/02-two-rulebooks-one-vessel/02-RESEARCH.md` — 다섯 통합 지점과 Pitfall 1~5(특히 Pitfall 4의 `EVENT_SCHEMA_VERSION` 판단 요구)
    - `.planning/ROADMAP.md` Phase 2 절 — 성공조건 다섯 개 원문
    - `.planning/REQUIREMENTS.md` — RIG-08·HYP-03 원문
    - `src/gptrpg/event_log/schema.py` — 6-10행의 D-12 규약 docstring과 현재 `EVENT_SCHEMA_VERSION` 값
  </read_first>
  <action>
  `.planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md`를 쓴다. **추측으로
  미리 쓰지 않는다** — 세 SUMMARY와 실제 diff에서 나온 사실만 담는다. 시작 전에
  `git diff --stat <이 단계의 첫 커밋>..HEAD -- src/ tests/ .importlinter`를 돌려 실제로
  바뀐 파일 목록을 확보하고, 그 목록과 문서의 "고친 곳"이 일치하는지 스스로 대조한다.

  문서 구성:

  **① 한 줄 답.** "두 번째 룰북을 넣으면서 플랫폼 코드를 고쳤는가?" 에 대한 한 문장 답과,
  그 답이 무엇을 뜻하는지. 요구사항 문구("코드 수정 없이")가 문자 그대로는 성립하지 않는다는
  것을 숨기지 않는다 — 실제 질문은 "고친 것이 **두 번째 룰북 때문에 한 번만** 필요한
  종류인가, 아니면 **세 번째·네 번째마다 또** 필요한 종류인가"다. 이 구분을 문서 전체의 축으로
  삼는다.

  **② 고치지 않고 데이터로 버틴 곳 (더 길고 자세하게 — 이 절이 문서의 본론이다).**
  각 항목에 ⓐ 무엇을 고칠 뻔했나 ⓑ 왜 고치고 싶었나 ⓒ 대신 무엇으로 해결했나 ⓓ 그 해결이
  세 번째 룰북에도 통하는가. 세 SUMMARY에서 나온 것을 전부 옮기되, 최소한 다음은 확인해서
  포함한다(실제로 그러했을 때만):
   - `Modifier` 자료구조 — 수정치 세 유형을 새로 만들면서 이 dataclass를 고쳤는가
   - `CheckOutcome` — d100이 자릿수 분해를 담아야 했는데 칸을 늘렸는가
   - `_parse_modifier`(CLI) — 새 수정치 유형에 인자 파싱을 고쳤는가
   - `Roller` 프로토콜 — 백분위 굴림을 위해 기존 프로토콜을 고쳤는가, 나란히 더했는가
   - `resolve_2d6`/`grade_for_total` — 2d6 경로 본문을 고쳤는가
   - `Entity`/`StatEntry` — 체력을 특별 취급하는 칸이나 상태값 개수 상한을 넣고 싶었는가

  **③ 고친 곳 (파일·줄·이유·재발 여부).** 각 항목에 ⓐ 파일 ⓑ 무엇을 ⓒ 왜 불가피했나
  ⓓ **세 번째 룰북이 들어올 때 또 고쳐야 하는가**(예/아니오와 그 이유). 최소한 다음은
  실제 diff와 대조해 포함한다: `event_log/schema.py`, `rules_core/reducer.py`,
  `rules_core/grading.py`, `rules_core/resolution.py`, `session_actor/actor.py`,
  `session_actor/live_roller.py`, `event_log/replay_roller.py`, `rules_core/dice.py`,
  `cli/main.py`, `.importlinter`.

  **④ `EVENT_SCHEMA_VERSION` 판단.** 올렸는지 안 올렸는지, 그 근거가 D-12 규약의 어느
  문구인지, 옛 판 기록을 해석하는 경로가 어디에 있는지. RESEARCH.md Pitfall 4가 "애매하게
  넘기지 말고 명시적으로 답하라"고 요구한 항목이므로 별도 절로 둔다.

  **⑤ 남은 한계 (정직하게).** 최소한 다음 셋을 포함한다: `_RESOLVERS` 분기가 판정 방식마다
  하나씩 늘어난다는 점(RESEARCH.md Pitfall 2가 "참았다가 아니라 제한적으로 고쳤다"로
  기록하라고 지목), `grading.py`에 던전월드 세 이름이 아직 남아 있다는 점과 그것을 지우지 않은
  이유, 리듀서의 판 1 해석 분기가 영구히 남는다는 점.

  **⑥ 성공조건 다섯 개 자기 채점.** ROADMAP의 다섯 조건 각각에 대해 만족/부분/미달과
  그 근거(테스트 파일명 또는 문서의 절 번호).

  문서 전체를 한국어로 쓴다. 참조번호(D-XX, T-XX)는 필요한 곳에만 쓰고, 각 절의 첫 문장은
  참조번호 없이도 읽히게 쓴다 — 이 문서는 사람이 읽고 판단하기 위한 것이다.
  </action>
  <verify>
    <automated>D=.planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md; test -f "$D" &amp;&amp; grep -qE "^#{1,3}.*(참은|버틴)" "$D" &amp;&amp; grep -qE "^#{1,3}.*고친" "$D" &amp;&amp; grep -q "EVENT_SCHEMA_VERSION" "$D" &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `02-INTERFACE-CHANGES.md`가 존재하고, "고친 곳" 절과 "참은 곳"(고치지 않고 데이터로 버틴 곳) 절이 둘 다 표제로 존재한다
    - 참은 곳 절이 고친 곳 절보다 길다: 두 절의 줄 수를 세어 비교했을 때 참은 곳 쪽이 크다 (D-22가 정한 무게 배분이 형식으로도 지켜진다)
    - 고친 곳 목록이 실제 diff와 일치한다: `git diff --name-only <phase-start>..HEAD -- src/ .importlinter`로 나온 모든 파일이 문서에 이름으로 등장한다 (하나라도 빠지면 미달)
    - 고친 곳 각 항목에 "세 번째 룰북이 들어올 때 또 고쳐야 하는가"에 대한 예/아니오 답이 붙어 있다
    - `EVENT_SCHEMA_VERSION` 판단이 별도 절로 있고, 올렸는지 여부와 그 근거, 옛 판 해석 경로의 위치가 적혀 있다
    - 남은 한계 절에 `_RESOLVERS` 분기, `grading.py`에 남은 세 이름, 리듀서의 판 1 분기 셋이 전부 언급된다
    - 성공조건 다섯 개 각각에 대한 자기 채점과 근거(테스트 파일명 또는 절 번호)가 있다
    - `uv run pytest -q` 전체 통과 (문서 작성이 코드를 건드리지 않았다)
  </acceptance_criteria>
  <done>
  사람이 이 문서 하나만 읽고 "두 번째 룰북을 넣으면서 플랫폼 코드를 고쳐야 했는가"에 답할 수
  있고, 고친 곳보다 참은 곳이 더 자세히 적혀 있으며, 목록이 실제 diff와 어긋나지 않는다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 사람이 기록을 읽고 이 단계의 답을 판정한다 (성공조건 5 / HYP-03)</name>
  <what-built>
  판정 방식이 다른 두 룰북(2d6 등급식 / OpenQuest d100 롤언더)이 같은 판정 요청·판정 결과
  모양 위에서 돌고, 그 룰북들의 적이 같은 그릇에 들어가며, 수정치 네 유형이 전부 실제로
  계산된다. 그리고 그 과정에서 플랫폼 코드를 고친 곳과 고치지 않고 데이터로 버틴 곳을
  `02-INTERFACE-CHANGES.md` 한 문서에 기록했다.

  이 단계의 성공조건 5번("플랫폼 코드를 고쳐야 했는지 아닌지가 명확히 기록된다")은 자동
  검증이 불가능하다 — "명확히 기록된다"는 사람이 읽고 판단하는 문구이기 때문이다.
  </what-built>
  <how-to-verify>
  ① 문서를 연다:
     `.planning/phases/02-two-rulebooks-one-vessel/02-INTERFACE-CHANGES.md`

  ② 읽으면서 이 네 가지를 확인한다:
     - 첫 절의 한 줄 답만 읽고도 "고쳤는가 안 고쳤는가"를 알 수 있는가
     - "고치지 않고 데이터로 버틴 곳"이 "고친 곳"보다 자세하고 설득력 있는가
       (D-22에서 사장님이 명시적으로 뒤쪽이 더 중요하다고 정했다)
     - 고친 곳마다 "세 번째 룰북이 들어올 때 또 고쳐야 하는가"에 답이 붙어 있는가 —
       이것이 "한 번만 고치면 되는 것"과 "룰북마다 또 고쳐야 하는 것"을 가르는 기준이다
     - 남은 한계가 숨겨지지 않고 적혀 있는가

  ③ 두 룰북이 실제로 도는지 직접 굴려 본다 (임시 파일에 기록이 쌓인다):
     `uv run gptrpg submit --db /tmp/oq.db --session s1 roll --rulebook openquest --target 50 --move "자물쇠 따기"`
     `uv run gptrpg submit --db /tmp/oq.db --session s1 roll --move "문을 부순다" --target 10`
     `uv run gptrpg replay --db /tmp/oq.db --session s1`
     → 판정 수가 2이고, 마지막 판정 등급 줄에 방금 굴린 쪽 룰북의 등급 이름이 나오는지 본다.
     (같은 명령을 두 번 더 굴려 d100 쪽이 OpenQuest 등급 이름들만 내는지도 볼 수 있다)

  ④ 이 단계의 답을 판정한다: **"룰북을 데이터로 표현할 수 있다"(HYP-03)가 이 기록으로
     확인되는가, 아니면 아직 부족한가.**
  </how-to-verify>
  <resume-signal>승인하려면 "approved", 아니면 문서에서 부족한 부분을 구체적으로 알려주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 명령줄 인자 → 세션 액터 명령 | 사용자가 입력한 룰북 식별자·수정치 문자열이 그대로 명령이 된다 |
| 코드 변경 사실 → 사람이 읽는 기록 | 기록이 실제와 어긋나면 가설 채점이 틀린다. 자동 검증이 불가능한 구간이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Spoofing | `cli/main.py` `--rulebook` 인자 | medium | mitigate | 등록되지 않은 식별자를 액터가 `CommandRejected`로 거부하고, CLI가 한 줄 오류 + 종료 코드 1로 바꾼다. 조용히 기본 룰북으로 대체하면 요청한 것과 다른 규칙으로 판정이 돌고 그 사실이 기록에 안 남는다 |
| T-02-13 | Repudiation | `02-INTERFACE-CHANGES.md`의 정확성 | high | mitigate | 문서의 "고친 곳" 목록을 실제 `git diff --name-only`와 기계적으로 대조하는 것을 수용 기준에 넣고, 사람 확인 체크포인트가 무게 배분과 정직성을 최종 판정한다. 이 숫자가 HYP-03 채점의 유일한 입력이므로 낙관적 기록은 곧 잘못된 사업 판단이 된다 |
| T-02-SC | Tampering | 패키지 설치 | n/a | accept | 이 계획도 새 패키지를 설치하지 않는다 |

**ASVS 수준:** 1. **차단 기준:** high. V5(입력 검증)만 해당 — 명령줄 문자열이 유일한 외부
입력이고, 인증·세션·권한 표면은 이 단계에 없다.
</threat_model>

<verification>
- `uv run pytest -q` — 전체 통과
- `uv run pytest tests/test_cli.py -q` — 새 플래그 왕복 통과
- `uv run lint-imports` / `uv run ruff check .` — 전 계약 kept, 위반 없음
- `02-INTERFACE-CHANGES.md` 존재 + 두 표제 절 + `EVENT_SCHEMA_VERSION` 절
- 사람 확인: 문서만 읽고 성공조건 5를 판정할 수 있는가 (Task 3)
</verification>

<success_criteria>
- 명령줄에서 두 룰북을 골라 판정할 수 있다
- 고친 곳과 참은 곳이 사람이 읽는 단일 문서 한 곳에 기록된다 (성공조건 5 / D-22 / HYP-03)
- 그 문서가 실제 diff와 어긋나지 않는다
- 사람이 읽고 이 단계의 답(HYP-03)을 판정했다
</success_criteria>

<output>
Create `.planning/phases/02-two-rulebooks-one-vessel/02-04-SUMMARY.md` when done.

SUMMARY에는 사람 확인 체크포인트에서 나온 판정(승인 또는 지적 사항)을 그대로 옮겨 담는다 —
이 판정이 Phase 6의 HYP-03 채점 입력이 된다.
</output>