# Phase 9: 에이전트 구조 재편 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-06
**Phase:** 9-에이전트 구조 재편
**Areas discussed:** 배경 작업 대상, 병렬 참조 수집 후보, 상황판단→서술 핸드오프, 지연 표시

---

## 배경 작업 (배경층의 첫 입주자)

**출발점:** 지금 코드엔 배경으로 돌릴 실제 작업이 하나도 없음(문맥압축·관계장부·시계조건검사
전부 나중 단계 예정) — D-64가 연 구조를 실제로 검증하려면 "진짜 뭔가"가 필요하다는 것을
먼저 짚고 질문했다.

| Option | Description | Selected |
|--------|-------------|----------|
| 최소 증명용 더미 작업 | 나중 단계 진짜 기능은 손대지 않고, "사건에 남고 재생된다"만 증명하는 작은 통계 집계 등 | |
| 나중 단계 기능 일부를 당겨온다 | Phase 14·15가 만들 기능(문맥 압축·관계 장부·시계 조건 검사) 중 하나를 최소 버전으로 지금 만든다 | ✓ |
| 이번 단계는 자리만 낸다 | 인터페이스만 만들고 내용은 뒤 단계에 맡긴다(성공조건 2 검증 사례 없음 위험) | |

**User's choice:** "나중 단계 기능 일부를 당겨온다"

**후속 질문 — 구체적으로 어떤 기능인가:**

| Option | Description | Selected |
|--------|-------------|----------|
| 위협 시계 조건 검사 (15단계 일부) | 실패 3회 고정에서 벗어나는 것 | |
| 관계 기록 최소판 (14단계 일부) | "그 인물과 무슨 일이 있었는지" 최소 기록 | |
| 문맥 압축 최소판 (14단계 일부) | 10턴 밖 대화를 접는 최소 버전 | |
| (free text) 필요한 모든 것들 | 셋 다 최소판으로 이번 단계에서 만든다 | ✓ |

**User's choice:** "필요한 모든 것들" (자유 텍스트) — 세 후보 모두 최소판으로 만든다.
**Notes:** 이 선택이 14·15단계의 로드맵 범위를 그만큼 줄인다는 것을 사용자에게 짚었고,
이어진 확인 질문에서 "네, 맞습니다 — 이대로 진행"으로 재확인받았다.

---

## 병렬 참조 수집 (실제 병렬화 후보)

**출발점:** "동시에 여러 참조를 수집한다"도 지금 코드엔 후보가 없다는 것 — 예전 구상(서브
에이전트 여럿이 각자 판단해 진행자에게 넘긴다)이 부활하는 자리라는 것을 짚고 질문했다.

| Option | Description | Selected |
|--------|-------------|----------|
| 지금은 없다 — 구조만 | 의존관계 표현 코드만 만들고 실제 병렬 작업은 이번 단계에 안 넣는다 | |
| 상황판단을 여러 조각으로 쪼갠다 | 지금 하나로 뭉쳐 있는 "상황 판단"을 여러 개의 작은 병렬 조각으로 나눈다 | ✓ |

**User's choice:** "상황판단을 여러 조각으로 쪼갠다"

**후속 질문 — 어떤 조각이 실제로 있는가 (멀티셀렉트):**

| Option | Description | Selected |
|--------|-------------|----------|
| 장면에 새 대상을 등장시키는가 | 13단계(장면) 대상 관리와 맞닿는다 | ✓ |
| 위협 시계에 영향을 주는가 | 판정 실패/이야기 진행과 별개로 시계가 움직여야 하는지 | ✓ |
| 관계에 영향을 주는가 | 누구와의 사이인지, 사이가 어떻게 바뀌는지 | ✓ |

**User's choice:** 세 조각 모두 선택.
**Notes:** 이 세 조각이 위 배경 작업 셋(시계·관계·압축 중 시계·관계)과 자연스럽게 연결된다는
점을 정리해 사용자에게 다시 확인받았다 — 장면 조각은 서술이 그 대상을 언급해야 하므로 턴
안에서 끝나야 하고, 시계·관계 조각은 턴 밖 배경으로 이어져도 된다는 구분을 함께 짚었다.

---

## 상황판단 → 서술 핸드오프 (넘기는 정보의 풍부함)

**출발점:** 지금 상황판단(사실상 없음)이 서술에게 넘기는 것은 판정 결과 한 줄
(`check_summary`)뿐이라는 것을 짚고 질문했다.

| Option | Description | Selected |
|--------|-------------|----------|
| 지금 그대로 — 결과 한 줄 | 서술 담당이 여전히 최근 대화·장면 대상·캐릭터 상태·시계 상태를 직접 보고 알아서 이야기를 짓는다 | |
| 상황판단이 몇 가지 사실을 더 정해 넘긴다 | 예: 무엇이 새로 드러났는지, 시계가 다음 칸으로 넘어갔는지를 상황판단이 미리 정해서 넘기고, 서술은 그 사실들을 문장으로만 옮긴다 | ✓ |

**User's choice:** "상황판단이 몇 가지 사실을 더 정해 넘긴다"
**Notes:** 위 병렬 판단 조각 셋의 결과가 정확히 이 "몇 가지 사실"이 된다 — 별도 질문
없이 앞선 답변들과 자연스럽게 합쳐졌다.

---

## 지연 표시 (병렬 참조 수집 타임아웃 시 화면 처리)

**출발점:** "없으면 없는 대로 진행"이 정상 동작이라고 로드맵(D-64)에 이미 정해져 있다는
것을 전제로, 실제로 일어났을 때 플레이어 화면에 티가 나야 하는지 질문했다.

| Option | Description | Selected |
|--------|-------------|----------|
| 조용히 넘어간다 | 화면엔 표시 없이 평소와 같은 서사가 나간다 — 사건 기록(로그)에는 남지만 플레이어는 못 본다 | |
| 운영자 로그에만 남긴다 | 플레이어 화면은 그대로 두되, 무엇이 늦어서 빠졌는지 서버 로그·사건 기록에 확실히 남겨 나중에 원인을 추적할 수 있게 한다 | ✓ |

**User's choice:** "운영자 로그에만 남긴다"
**Notes:** 이미 `agents/invoke.py`의 `call_with_one_retry`가 갖고 있는 stderr 경고 관례와
같은 모양이라는 것을 코드 근거로 확인했다.

---

## 마무리 확인

사용자에게 다섯 결정을 종합해 다시 정리했다 — "배경 작업 셋(시계·관계·압축) 다 최소판으로
이번 단계에서 만들고, 판단 조각 셋(장면·시계·관계)을 병렬로 돌리는 것으로 확정해도 될까요?"
→ **"네, 맞습니다 — 이대로 진행 (추천)"**으로 확정.

이후 "더 논의할 회색 지대가 남았는가"를 물었고, **"이제 기록으로 넘어가죠"**로 논의를
마쳤다.

## Claude's Discretion

- 병렬 판단 조각 셋의 실제 호출 모양 (각자 독립 LLM 호출 vs 한 호출에서 JSON 셋)
- 위협 시계 최소판이 CLOCK-02의 사람 확인 게이트를 이번 단계에서 만들지 여부 (D14는 절대
  유지)
- 관계 기록 최소판의 저장 형태 (자유 텍스트 vs 구조화 필드)
- 문맥 압축 최소판의 실제 압축 범위 (11턴째부터 실제로 접는지, 자리만 내는지)
- `AGENT_ROLES` 확장 방식과 새 역할 이름

## Deferred Ideas

이번 논의는 phase 도메인(에이전트 구조) 안에서만 진행됐다. 범위 밖으로 새로 제기된
아이디어는 없다 — CLOCK-02~05·MEM-04~07·서술 출력 검증(Phase 10)·결과 카테고리 닫힌
목록(Phase 12)이 명시적으로 이번 단계 밖이라는 점만 CONTEXT.md `<deferred>`에 재확인해
남겼다.
