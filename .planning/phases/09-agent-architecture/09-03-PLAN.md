---
phase: 09-agent-architecture
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/gptrpg/agents/context.py
  - src/gptrpg/agents/invoke.py
  - src/gptrpg/agents/prompt_assembly.py
  - src/gptrpg/agents/scene_entity_judge.py
  - src/gptrpg/turn/judgments.py
  - src/gptrpg/web/routes_actions.py
  - src/gptrpg/cli/turn_flow.py
  - tests/test_scene_entity_judge.py
  - tests/test_parallel_judgment.py
  - tests/test_agent_context_caps.py
  - tests/test_web_actions.py
  - tests/test_turn_flow_failure.py
autonomous: true
requirements: [ARCH-04, ARCH-05, ARCH-06]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ARCH-04: 판정이 끝난 뒤 세 판단(상황판단·장면 신규 대상·시계 신호)이 어떤 입력에서도 예외 없이 전부 호출된다 — 무엇을 병렬로 돌릴지 정하는 조건 분기가 소스에 한 줄도 없다"
    - "ARCH-04: 세 판단은 단 하나의 asyncio.gather 호출로 동시에 굴러가며, 그 호출을 감싸는 if/삼항이 없다는 것이 구문 트리 검사로 고정된다"
    - "ARCH-05: 세 판단 중 하나가 타임아웃되거나 죽어도 나머지 결과는 그대로 쓰이고 턴이 끝까지 간다 — 실패한 조각은 빈 판단으로 정상 경로를 탄다"
    - "ARCH-06: 네 종류의 문맥 값 객체가 각자 다른 칸만 선언하고 각자 상한을 스스로 강제한다 — 세션이 길어져도 어느 객체도 주입량이 늘지 않는다"
    - "D-03: 장면 신규 대상 판단 결과가 서술 호출 전에 사실 묶음에 반영된다 — 서술이 그 인물·사물을 언급할 수 있다"
    - "관계 영향 판단은 이 단계에 없다 — 받을 곳(관계 기록)이 Phase 14 소관이므로 판단 조각도 그때 함께 만든다"
  artifacts:
    - src/gptrpg/agents/scene_entity_judge.py
    - tests/test_scene_entity_judge.py
    - tests/test_parallel_judgment.py
    - tests/test_agent_context_caps.py
  key_links:
    - "scene_entity_judge.judge_new_entity → TurnJudgments.entity → NarrationFacts.new_entities → build_gm_prompt의 턴 조각 (이 사슬이 끊기면 (a) 판단이 죽은 코드가 된다)"
    - "turn/judgments.gather_turn_judgments의 단일 asyncio.gather ↔ tests/test_parallel_judgment.py의 구문 트리 검사 (ARCH-04를 코드 모양으로 고정하는 유일한 그물)"
  prohibitions:
    - "병렬 여부를 런타임에 고르는 코드가 생기는 것 — 조건부 gather, 한쪽만 부르는 조기 반환, 순차 실행으로 되돌아가는 갈래 전부 ARCH-04 위반이다"
    - "장면 신규 대상 판단 결과를 사건으로 기록하거나 Entity 목록에 밀어 넣는 것 — 장면 대상 관리는 Phase 11·12 소관이고 이 단계는 서술에 넘길 사실로만 쓴다"
---

<objective>
판정이 끝난 뒤 도는 병렬 판단을 두 조각으로 완성한다 — (a) 이 결과가 장면에 새
대상(인물·사물)을 등장시키는가, (b) 이 결과가 위협 시계에 영향을 주는가. 그리고
"무엇을 병렬로 돌릴지 런타임에 판단하는 코드가 없다"를 구문 트리 수준에서 못박는다.

Purpose: D-03이 정한 두 조각 중 (a)가 이 계획에서 처음 생긴다. (b)는 09-01이,
상황판단은 09-02가 이미 만들었으므로 이 계획이 셋을 한 자리에 모아 ARCH-04를 완성한다.
동시에 ARCH-06이 요구하는 "역할별 문맥 + 각자 상한"을 네 값 객체 전부에 대해 시험으로
고정한다.

Output: `scene_entity_judge` 역할의 구현, 세 코루틴을 항상 나열하는 하나의
`asyncio.gather`, 그리고 ARCH-04·ARCH-06 두 요구사항의 회귀 방지 시험 두 파일.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-agent-architecture/09-CONTEXT.md
@.planning/phases/09-agent-architecture/09-RESEARCH.md
@.planning/phases/09-agent-architecture/09-PATTERNS.md
@.planning/phases/09-agent-architecture/09-01-SUMMARY.md
@.planning/phases/09-agent-architecture/09-02-SUMMARY.md
</context>

<planning_decisions>
**DP-08 (Claude's Discretion — 병렬 판단의 호출 모양): 각자 독립된 호출 셋.**
한 번의 호출에서 JSON 여러 칸을 받아오는 안을 쓰지 않는다. 근거 셋 — ① 단일 호출은
정의상 병렬이 아니므로 ARCH-04를 "정적으로 박힌 병렬"로 만족시킬 방법이 없다 ②
캐싱 안정 블록에 서로 다른 판단 지시문 셋을 얹으면 영구 조각이 커지고 한 역할의
지시문만 바뀌어도 셋 전부의 캐시가 깨진다 ③ 역할별 제공자·모델을 따로 고르는
D-32/D-33의 구조를 판단 조각에도 그대로 이어받을 수 있다.
— **Reversibility: reversible** — 호출을 합치는 방향으로 되돌리는 것은 프롬프트 조립
함수 하나를 새로 쓰는 일이라 값싸다.

**DP-09 ((a)의 결과를 어디까지 쓰는가): 서술에 넘길 사실로만 쓴다.**
장면 신규 대상 판단이 돌려준 이름을 사건으로 기록하거나 `Entity` 목록에 넣지 않는다 —
장면 대상·캐릭터 상태 관리는 Phase 11·12 소관이라고 CONTEXT.md domain이 명시했다.
이번 단계에서 이 판단이 하는 일은 `NarrationFacts.new_entities`를 채워 서술이 그
인물·사물을 언급할 수 있게 하는 것까지다(D-04 — (a)는 턴 안에서 끝나야 한다).
— **Reversibility: reversible**
</planning_decisions>

<artifacts_this_phase_produces>
| 종류 | 이름 | 자리 |
|------|------|------|
| 에이전트 역할 구현 | `scene_entity_judge` (역할 이름은 09-01이 이미 선언) | `agents/scene_entity_judge.py` (신규) |
| 판단 결과 값 객체 | `NewEntity`, `EntityJudgment` | `agents/scene_entity_judge.py` |
| 판단 함수 | `judge_new_entity` | `agents/scene_entity_judge.py` |
| 문맥 값 객체 | `EntityJudgeContext` | `agents/context.py` |
| 문맥 상한 상수 | `ENTITY_JUDGE_RECENT_TURNS_LIMIT`, `NEW_ENTITY_LIMIT` | `agents/context.py` |
| `NarrationFacts` 새 칸 | `new_entities: tuple[str, ...]` | `agents/context.py` |
| 타임아웃 상수 | `SCENE_ENTITY_TIMEOUT_S` | `agents/invoke.py` |
| 프롬프트 조립 함수 | `build_scene_entity_prompt` | `agents/prompt_assembly.py` |
| `TurnJudgments` 새 칸 | `entity: EntityJudgment` | `turn/judgments.py` |
| 새 파일 경로 | `src/gptrpg/agents/scene_entity_judge.py` | — |
| 새 CLI 플래그 | 없음 | — |

**만들지 않는 것(명시):** 관계 영향 판단 조각(Phase 14), 새 사건 종류, `Entity` 쓰기 경로.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 장면 신규 대상 판단 (a)를 만든다</name>

  <files>
src/gptrpg/agents/context.py,
src/gptrpg/agents/invoke.py,
src/gptrpg/agents/prompt_assembly.py,
src/gptrpg/agents/scene_entity_judge.py (신규),
tests/test_scene_entity_judge.py (신규)
  </files>

  <read_first>
- `src/gptrpg/agents/clock_judge.py` — 09-01이 만든 자매 모듈. 이 태스크가 만드는 모듈은
  구조를 그대로 복제한다(닫힌 결과 dataclass + `call_with_one_retry` + 실패 시 빈 결과).
- `src/gptrpg/agents/action_classifier.py` — 닫힌 목록 패턴과 `MAX_CANDIDATES` 상한 관례.
- `src/gptrpg/agents/context.py` — 09-01/09-02가 추가한 `ContextCapExceeded`,
  `ClockJudgeContext`, `NarrationFacts`, `SITUATION_FACTS_LIMIT`.
- `src/gptrpg/agents/prompt_assembly.py` — 09-02 이후의 `build_situation_prompt`,
  좁아진 `build_gm_prompt`, `build_clock_signal_prompt`, `_format_scene_entities`.
- `src/gptrpg/agents/invoke.py` — 지금까지 쌓인 타임아웃 상수 넷과 도크스트링 관례.
- `src/gptrpg/rules_core/entities.py` — `Entity`/`StatEntry`의 실제 칸.
  </read_first>

  <behavior>
시험을 먼저 쓴다(`tests/test_scene_entity_judge.py`).
- `EntityJudgeContext`가 `recent_turns` 상한 초과에 `ContextCapExceeded`를 던진다.
- `EntityJudgeContext`의 필드 이름 집합에 시계 상태 칸도 캐릭터 상태 칸도 없다.
- 대역 제공자가 두 시도 모두 실패하면 `judge_new_entity`가 예외 없이
  `entities == ()`, `ai.ok == False`인 값을 돌려준다.
- 모델이 `NEW_ENTITY_LIMIT`보다 많은 대상을 돌려주면 앞에서부터 잘린다.
- 모델이 `kind` 칸에 닫힌 목록 밖 값을 넣은 원소는 조용히 건너뛰고 나머지는 살린다
  (원소 하나 때문에 턴 전체가 죽지 않는다 — `_parse_candidates`의 기존 규율).
- 모델이 `<think>` 블록/코드펜스로 감싼 JSON을 돌려줘도 읽힌다.
- `judge_new_entity`가 `SCENE_ENTITY_TIMEOUT_S`를 제공자에 넘긴다.
- 이미 장면에 있는 대상 이름(`ctx.scene_entities`의 표시 이름)과 같은 값을 모델이
  돌려주면 결과에서 빠진다 — "새" 대상만 남긴다.
  </behavior>

  <action>
이 태스크가 구현하는 잠금 결정 — **D-03:** 판정 하나가 끝난 뒤 두 조각을 병렬로
판단한다 — (a) 이 결과가 장면에 새 대상(인물·사물)을 등장시키는가 (b) 이 결과가 위협
시계에 영향을 주는가. 이 태스크가 만드는 것은 (a)다. 관계 영향 판단은 만들지 않는다 —
받을 곳(관계 기록)이 Phase 14 소관이라 지금 만들면 죽은 코드가 된다. **D-04:** (a)는
서술이 그 인물·사물을 언급해야 하므로 턴 안에서 끝나야 한다 — 그래서 이 판단의
타임아웃은 서술 앞에 붙는 지연으로 잡는다.

**① `src/gptrpg/agents/context.py`** — 추가만 한다.
- `ENTITY_JUDGE_RECENT_TURNS_LIMIT = 4`: 장면 신규 대상 판단은 "방금 무슨 일이 있었나"만
  보면 되므로 서술용 `RECENT_TURNS_LIMIT`(10)보다 좁다. 근거를 도크스트링에 적는다.
- `NEW_ENTITY_LIMIT = 3`: 한 턴에 서술이 새로 소개할 수 있는 대상 개수 상한.
  `MAX_CANDIDATES = 3`과 같은 이유의 상한이라는 것을 도크스트링에 적는다 — 상한이 없으면
  모델이 장면을 통째로 새로 짓는다.
- `@dataclass(frozen=True) class EntityJudgeContext`: 칸은 정확히 셋 —
  `scene_entities: tuple[Entity, ...]`, `recent_turns: tuple[str, ...]`,
  `check_summary: str`. `__post_init__`에서 `recent_turns` 상한 초과 시
  `ContextCapExceeded`. 도크스트링에 왜 시계 상태와 캐릭터 상태 칸이 없는지 적는다 —
  "새 인물·사물이 등장하는가"를 판단하는 데 그 둘이 필요 없고, 안 주는 것이 ARCH-06의
  "각자 상한"이다.
- `NarrationFacts`에 `new_entities: tuple[str, ...]` 칸을 더하고(기본값 없이 명시 인자로
  받는다 — 09-02가 도크스트링에 예고해 둔 그 칸이다), `__post_init__`에 개수가
  `NEW_ENTITY_LIMIT`을 넘으면 `ContextCapExceeded`를 던지는 검사를 더한다.
  이 칸은 표시 이름 문자열 튜플이다 — `Entity` 객체를 넣지 않는다(DP-09).

**② `src/gptrpg/agents/invoke.py`** — `SCENE_ENTITY_TIMEOUT_S = 5.0`을 같은 관례로
추가한다. 근거: 닫힌 목록을 돌려받는 경량 판단이고, D-04가 (a)는 **턴 안에서 끝나야
한다**고 정했으므로 서술 앞에 붙는 지연이 분류기와 같은 층을 넘으면 안 된다.

**③ `src/gptrpg/agents/prompt_assembly.py`** — `build_scene_entity_prompt(*,
rulebook_display_name: str, ctx: EntityJudgeContext) -> tuple[list[dict], list[dict]]`를
추가한다. `permanent` 조각은 역할 지시문 + 닫힌 출력 계약이다 — 응답은 JSON 배열이고
각 원소는 `name` 칸(표시 이름 한 단어~짧은 구)과 `kind` 칸을 갖는 객체이며, `kind`는
`"person"`과 `"thing"` 두 값만 허용하고, 새로 등장하는 대상이 없으면 빈 배열을
돌려주며, 설명 문장을 덧붙이지 않는다. 이미 장면에 있는 대상은 다시 적지 않는다는
지시도 넣는다. `session` 조각은 `_format_scene_entities(ctx.scene_entities)`,
`messages`는 최근 대화 + 판정 결과. 캐싱 순서 규약과 `_cached_block`을 그대로 쓴다.

**④ `src/gptrpg/agents/scene_entity_judge.py` (신규)** — `clock_judge.py`와 같은 모양.
- `@dataclass(frozen=True) class NewEntity`: `name: str`, `kind: str`.
- `@dataclass(frozen=True) class EntityJudgment`: `entities: tuple[NewEntity, ...]`,
  `ai: AgentResult`.
- `judge_new_entity(*, provider, model, ctx: EntityJudgeContext,
  rulebook_display_name: str) -> EntityJudgment`: `build_scene_entity_prompt`로 조립하고
  `provider.complete(..., max_tokens=1024, timeout_s=SCENE_ENTITY_TIMEOUT_S)`를
  `call_with_one_retry`로 감싼다. 실패하면 예외 없이 `entities=()`.
- 파싱은 `json_parsing.try_parse_json_array`. 원소가 dict가 아니거나 `name`이 없거나
  `kind`가 닫힌 두 값 밖이면 **그 원소만 건너뛴다**(예외를 던지지 않는다 —
  `UnknownMove`에 해당하는 예외를 만들지 않는 이유를 도크스트링에 적는다: 무브 이름과
  달리 여기의 `name`은 룰북이 정한 닫힌 목록이 아니라 모델이 짓는 자유 문자열이므로
  대조할 목록 자체가 없다).
- `ctx.scene_entities`의 `display_name` 집합과 이름이 겹치는 원소를 걸러낸 뒤
  `NEW_ENTITY_LIMIT`개로 자른다.
- 모듈 도크스트링에 DP-09를 적는다: 이 판단의 결과는 서술에 넘길 사실로만 쓰이고,
  사건으로 기록하거나 장면 대상 목록에 밀어 넣는 경로를 이 단계에서 만들지 않는다
  (Phase 11·12 소관).
  </action>

  <verify>
    <automated>uv run pytest tests/test_scene_entity_judge.py -x</automated>
    <automated>uv run lint-imports</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_scene_entity_judge.py -x`가 통과한다.
- `uv run python -c "import dataclasses as d; from gptrpg.agents.context import EntityJudgeContext; print(sorted(f.name for f in d.fields(EntityJudgeContext)))"`의 출력이
  `['check_summary', 'recent_turns', 'scene_entities']`다.
- `NEW_ENTITY_LIMIT + 1`개를 담은 `NarrationFacts` 생성이 `ContextCapExceeded`를 던진다.
- 대역 제공자가 `NEW_ENTITY_LIMIT`보다 많은 대상을 돌려줄 때 `judge_new_entity`의 결과
  개수가 정확히 `NEW_ENTITY_LIMIT`이다.
- 대역 제공자가 이미 장면에 있는 대상 이름을 돌려줄 때 그 이름이 결과에 없다.
- 저장소 전체에서 `NewEntity`를 사건 페이로드로 옮기는 코드가 없다:
  `grep -rn "NewEntity" src | grep -c "session_actor\|event_log"`의 출력이 `0`이다.
- `uv run lint-imports`가 통과한다.
  </acceptance_criteria>

  <done>
(a) 판단이 닫힌 결과를 돌려주고, 실패해도 빈 판단으로 떨어지며, 받는 문맥이 필요한 세
칸으로 좁혀져 있고 상한을 스스로 강제한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 세 판단을 하나의 gather에 나열하고 (a)의 결과를 서술까지 흘린다</name>

  <files>
src/gptrpg/turn/judgments.py,
src/gptrpg/agents/prompt_assembly.py,
src/gptrpg/web/routes_actions.py,
src/gptrpg/cli/turn_flow.py,
tests/test_web_actions.py,
tests/test_turn_flow_failure.py
  </files>

  <read_first>
- `src/gptrpg/turn/judgments.py` — 09-02가 만든 `TurnJudgments`,
  `gather_turn_judgments`(두 코루틴), `build_narration_facts`.
- `src/gptrpg/agents/scene_entity_judge.py` — Task 1이 만든 시그니처.
- `src/gptrpg/agents/prompt_assembly.py`의 `build_gm_prompt` 턴 조각 — 여기에
  `new_entities`가 실린다.
- `src/gptrpg/web/routes_actions.py`의 `confirm()`과 `src/gptrpg/cli/turn_flow.py`의
  `_turn_flow` — 09-02가 배선한 판단 구간과 `RecordAiCall` 제출 자리.
- `09-RESEARCH.md` Architecture Patterns §2의 Anti-pattern 문단.
  </read_first>

  <action>
이 태스크가 구현하는 잠금 결정 — **D-03:** 두 조각은 서로의 출력을 쓰지 않으므로 동시에
돌 수 있고, 그 의존 관계가 코드에 정적으로 박힌다(둘을 병렬로 돌릴지는 런타임 판단이
아니다). **D-04:** (a)의 결과는 서술 호출 **전에** 사실 묶음에 반영되고, (b)의 결과는
서술이 끝난 뒤 배경으로 넘어간다. **D-05:** 셋 중 하나가 실패해도 나머지 결과가 그대로
쓰이고 화면에는 아무 티도 나지 않는다.

**① `src/gptrpg/turn/judgments.py`** —
- `TurnJudgments`에 `entity: EntityJudgment` 칸을 더한다.
- `gather_turn_judgments`의 매개변수에 `entity_provider`/`entity_model`을 더하고,
  `asyncio.gather` 호출에 `asyncio.to_thread(judge_new_entity, ...)`를 **세 번째로
  나열한다.** 조건 분기를 만들지 않는다 — `gather` 호출은 여전히 함수 안에 정확히
  하나이고 그것을 감싸는 `if`도 삼항 연산자도 없다(이 성질을 Task 3이 구문 트리로
  고정한다). `EntityJudgeContext`는 이 함수 안에서 `ctx`의 장면 대상과 최근 대화
  뒤쪽 `ENTITY_JUDGE_RECENT_TURNS_LIMIT`개로 만든다.
- `build_narration_facts`가 `judgments.entity.entities`의 `name`만 뽑아
  `NarrationFacts.new_entities`에 채운다. 이미 `judge_new_entity`가 상한으로 잘라 두므로
  여기서 다시 자르지 않는다.
- 모듈 도크스트링에 세 판단이 서로의 출력을 쓰지 않는다는 사실과, 그래서 셋을 한
  `gather`에 나열하는 것으로 ARCH-04가 코드 모양으로 충족된다는 것을 적는다.

**② `src/gptrpg/agents/prompt_assembly.py`** — `build_gm_prompt`의 턴 조각(`messages`)에
`facts.new_entities`를 한 줄로 덧붙인다. 비어 있으면 그 줄을 아예 안 넣는다(빈 목록
자리표시자가 매 턴 들어가면 모델이 그것을 소재로 착각한다). 지시문에 "이 이름들은 이번
장면에 새로 등장하는 대상이니 자연스럽게 등장시켜라"를 한 줄 더한다. `system` 두
조각은 손대지 않는다 — 새 대상은 턴마다 달라지는 값이므로 캐시 대상이 아니다(DP-05).

**③ `src/gptrpg/web/routes_actions.py`** — `scene_entity_judge` 역할의 provider를
`request.app.state.provider_resolver`로 만들어 `gather_turn_judgments`에 넘기고,
그 호출의 `ai` 껍데기로 `RecordAiCall(agent_role="scene_entity_judge", ...)`을 성공·실패
관계없이 제출한다(09-02가 다른 두 역할에 대해 만든 자리 바로 옆).

**④ `src/gptrpg/cli/turn_flow.py`** — 웹과 같은 변경을 대칭으로 적용한다.

**⑤ 기존 시험 확장** — `tests/test_web_actions.py`와 `tests/test_turn_flow_failure.py`에
각각 다음 두 케이스를 더한다.
- 한 번의 턴이 `agent_role`이 `scene_entity_judge`인 `ai_invoked` 사건을 정확히 하나
  남긴다.
- (a) 판단만 두 시도 모두 실패해도 서사가 정상적으로 나오고, 나머지 두 판단 결과는
  그대로 쓰이며, 응답/종료 코드가 성공이다(ARCH-05 — 셋 중 하나가 죽는 경우).
  </action>

  <verify>
    <automated>uv run pytest tests/test_web_actions.py tests/test_turn_flow_failure.py tests/test_clock_condition_web.py tests/test_clock_condition_cli.py tests/test_narration_isolation.py -x</automated>
    <automated>uv run lint-imports</automated>
  </verify>

  <acceptance_criteria>
- 한 번의 confirm 요청이 `agent_role`이 각각 `situation_judge`·`scene_entity_judge`·
  `clock_judge`인 `ai_invoked` 사건을 하나씩 남긴다(시험으로 단언).
- (a) 판단만 실패하는 대역 구성에서 confirm 응답이 200이고 `narration_failed`가 참이
  아니며 서사 사건이 하나 이상 남는다.
- `judge_new_entity`를 부르는 자리가 저장소 전체에서 `turn/judgments.py` 한 곳뿐이다:
  `grep -rn "judge_new_entity" src | grep -v "def judge_new_entity" | wc -l`의 출력이 `2`다.
- `NarrationFacts.new_entities`가 비어 있을 때 `build_gm_prompt`의 `messages` 문자열
  길이가, 비어 있지 않을 때보다 짧다(빈 목록 줄이 안 들어간다는 행동 증거).
- `uv run lint-imports`가 통과한다.
  </acceptance_criteria>

  <done>
세 판단이 하나의 `gather`에서 동시에 돌고, (a)의 결과가 서술 프롬프트의 턴 조각까지
실제로 도달하며, 세 호출 모두 `ai_invoked` 사건으로 기록된다.
  </done>
</task>

<task type="auto">
  <name>Task 3: ARCH-04와 ARCH-06을 회귀 방지 그물로 못박는다</name>

  <files>
tests/test_parallel_judgment.py (신규),
tests/test_agent_context_caps.py (신규)
  </files>

  <read_first>
- `src/gptrpg/turn/judgments.py` — Task 2 이후의 최종 모양.
- `src/gptrpg/agents/context.py` — 네 값 객체(`TurnContext`, `ClockJudgeContext`,
  `EntityJudgeContext`, `NarrationFacts`)와 상한 상수 전부.
- `src/gptrpg/turn/context.py`의 `build_turn_context` — 저장소 사건 개수와 무관하게
  `RECENT_TURNS_LIMIT`으로 잘리는 자리.
- `09-RESEARCH.md`의 Validation Architecture 절 — 이 두 파일이 그 절의 Wave 0 격차 둘을
  닫는다.
  </read_first>

  <action>
**① `tests/test_parallel_judgment.py` (신규)** — ARCH-04를 두 층에서 잡는다.

*행동 층* — 세 판단 함수를 호출 횟수를 세는 대역으로 갈아 끼우고(`monkeypatch`로
`gptrpg.turn.judgments` 이름공간의 세 이름을 바꾼다) `gather_turn_judgments`를 서로 다른
입력 여럿으로 부른다. 최소한 다음 네 가지 입력을 포함한다 — ① 평범한 성공 판정 ②
실패 판정 ③ 장면 대상이 하나도 없는 문맥 ④ 최근 대화가 비어 있는 문맥. 어느 경우에도
세 대역이 **각각 정확히 한 번씩** 불렸다는 것을 단언한다. 어떤 입력에서도 건너뛰는
조각이 없다는 것이 "런타임에 판단하지 않는다"의 행동 증거다.

*구문 층* — `ast`로 `src/gptrpg/turn/judgments.py`를 파싱해
`gather_turn_judgments`의 `FunctionDef`를 찾고 다음을 단언한다.
- 그 함수 본문 전체에 `ast.If`도 `ast.IfExp`도 없다.
- 그 함수 본문에 `asyncio.gather` 호출이 정확히 하나이고, 그 호출의 위치 인자가 정확히
  셋이다.
- 세 인자가 전부 `asyncio.to_thread` 호출이다.
시험 도크스트링에 왜 구문 트리로 보는지 적는다: 주석·도크스트링 문구는 구문 트리에
영향을 주지 않으므로, 문자열 검색과 달리 이 검사는 설명을 고쳐 쓴다고 통과하거나
깨지지 않는다.

*동시성 증거* — 세 대역이 각각 짧게 잠들었다가 호출 시각을 기록하게 하고, 세 호출의
구간이 실제로 겹친다는 것(마지막 시작 시각이 첫 종료 시각보다 이르다)을 단언한다.
순차 실행으로 되돌아가는 회귀를 잡는 그물이다. 잠드는 시간은 시험이 느려지지 않도록
짧게 잡고, 시각 비교는 `time.monotonic`을 쓴다.

**② `tests/test_agent_context_caps.py` (신규)** — ARCH-06을 네 값 객체 전부에 대해 잡는다.
- **상한이 있다**: 각 값 객체가 자기 상한을 넘겼을 때 예외를 던진다 — `TurnContext`는
  `TooMuchContext`, 나머지 셋은 `ContextCapExceeded`. 상한 값 자체는 모듈 상수에서
  읽어 오고 시험 파일에 숫자를 다시 적지 않는다.
- **각자 다른 것을 받는다**: 네 값 객체의 필드 이름 집합을 뽑아 다음을 단언한다 —
  `NarrationFacts`에 시계 상태 칸이 없다, `ClockJudgeContext`에 캐릭터 상태 칸도 장면
  대상 칸도 없다, `EntityJudgeContext`에 시계 상태 칸도 캐릭터 상태 칸도 없다,
  `TurnContext`만 네 칸을 전부 갖는다.
- **세션 길이에 비례해 늘지 않는다**: 사건이 아주 많은(예: 최근 대화 상한의 다섯 배가
  넘는) 임시 저장소를 만들고 `build_turn_context`를 부른 뒤, 거기서 파생한 세 문맥
  객체 전부의 최근 대화 개수가 각자의 상한 이하라는 것을 단언한다. 사건 개수를 두 배로
  늘려 다시 불러도 개수가 그대로라는 것도 단언한다 — "주입량이 세션 길이에 비례해
  늘지 않는다"(ARCH-06)의 직접 증거다.
- **저장소를 훑는 경로가 없다**: `gptrpg.agents` 아래 모든 모듈이 `gptrpg.event_log`나
  `gptrpg.session_actor`를 import하지 않는다는 것을, `.importlinter` 계약과 별개로 이
  시험에서도 한 번 더 단언한다(계약 파일이 조용히 지워지는 회귀를 잡는다).
  </action>

  <verify>
    <automated>uv run pytest tests/test_parallel_judgment.py tests/test_agent_context_caps.py -x</automated>
    <automated>uv run pytest</automated>
    <automated>uv run lint-imports</automated>
  </verify>

  <acceptance_criteria>
- `uv run pytest tests/test_parallel_judgment.py -x`가 통과한다.
- 그 파일에 `ast`로 `gather_turn_judgments`를 파싱해 `ast.If`/`ast.IfExp`가 없음을
  단언하는 시험이 있다.
- 그 파일에 `asyncio.gather` 호출의 위치 인자가 정확히 셋임을 단언하는 시험이 있다.
- 그 파일에 네 가지 서로 다른 입력에서 세 판단 대역이 각각 정확히 한 번씩 불렸음을
  단언하는 시험이 있다.
- 그 파일에 세 호출의 실행 구간이 실제로 겹친다는 것을 시각으로 단언하는 시험이 있다.
- `uv run pytest tests/test_agent_context_caps.py -x`가 통과하고, 그 파일에 사건 개수를
  두 배로 늘려도 파생 문맥의 최근 대화 개수가 변하지 않음을 단언하는 시험이 있다.
- `uv run pytest` 전체와 `uv run lint-imports`가 통과한다.
  </acceptance_criteria>

  <done>
누군가 병렬 판단에 조건 분기를 넣거나 한 조각을 빼면 `tests/test_parallel_judgment.py`가
먼저 깨진다. 문맥 상한을 늘리거나 값 객체에 새 칸을 몰래 더하면
`tests/test_agent_context_caps.py`가 먼저 깨진다. 09-RESEARCH.md가 남긴 Wave 0 격차 둘이
닫힌다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 제공자 → `agents.scene_entity_judge` | 모델이 지은 자유 문자열(대상 이름)이 서술 프롬프트로 들어가는 경로 |
| 세 판단 결과 → 서술 프롬프트 턴 조각 | 모델 출력이 다음 모델의 입력이 되는 경계(09-02의 T-09-08과 같은 종류) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Input Validation (ASVS V5) | `judge_new_entity` 응답 파싱 | medium | mitigate | `kind`를 닫힌 두 값으로 대조하고, 어긋난 원소만 건너뛰며, 개수를 `NEW_ENTITY_LIMIT`으로 자른다. `name`은 대조할 룰북 목록이 없으므로 개수 상한과 중복 제거가 유일한 통제라는 것을 모듈 도크스트링에 명시한다 |
| T-09-13 | Information Disclosure | 모델이 지은 대상 이름이 서술 프롬프트로 흐름 | low | accept | 이 이름은 모델이 방금 만든 값이지 시나리오 원문이 아니다 — ARCH-02가 막으려는 유출 경로가 아니다. 서술 출력 자체의 검증은 Phase 10 소관 |
| T-09-14 | Denial of Service | 임계 경로 LLM 호출이 셋으로 늘어남 | medium | mitigate | 셋이 하나의 `asyncio.gather`로 동시에 돌아 지연이 합이 아니라 최댓값이다. 각각 `call_with_one_retry`로 상한이 잡히고 (a)는 분류기와 같은 5초 층이다. Task 3의 구간 겹침 시험이 병렬성이 실제로 유지되는지를 고정한다 |
| T-09-15 | Tampering | (a) 결과를 사건·`Entity` 목록에 쓰는 경로가 생김 | medium | mitigate | DP-09로 명시적으로 금지하고, Task 1의 수용 기준이 `NewEntity`가 `session_actor`/`event_log` 근처에 나타나지 않음을 검사한다. 장면 대상 관리는 Phase 11·12 소관 |
| T-09-16 | Elevation of Privilege | 조건부 병렬화가 슬며시 들어옴 | high | mitigate | `ast` 기반 구문 검사로 `gather_turn_judgments` 안의 모든 분기를 금지한다 — ARCH-04의 취지("AI도 로직도 그것을 틀릴 기회가 없다")를 코드 모양으로 강제한다 |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | 새 외부 패키지 없음 — 표준 라이브러리 `ast`/`asyncio`와 기존 의존성만 쓴다 |
</threat_model>

<verification>
- `uv run pytest` 전체 통과
- `uv run lint-imports` 통과
- 세 판단이 하나의 `gather`에서 항상 함께 돈다(행동·구문·시각 세 층의 시험)
- 네 값 객체가 각자 다른 칸과 각자 상한을 갖는다
</verification>

<success_criteria>
- 병렬 실행 여부를 런타임에 판단하는 코드가 없다 (ARCH-04)
- 하나가 늦어도 턴이 멈추지 않는다 (ARCH-05)
- 에이전트별로 무엇을 받는지가 코드에 명시되고 각자 상한이 있다 (ARCH-06)
- D-03의 두 조각이 전부 살아 있고 관계 영향 판단은 Phase 14로 남아 있다
</success_criteria>

<output>
Create `.planning/phases/09-agent-architecture/09-03-SUMMARY.md` when done
</output>
