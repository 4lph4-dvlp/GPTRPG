---
phase: 10-ai
plan: 07
type: execute
wave: 5
depends_on: ["10-03", "10-06"]
gap_closure: true
files_modified:
  - src/gptrpg/agents/master_gm.py
  - src/gptrpg/agents/narration_guard.py
  - src/gptrpg/event_log/schema.py
  - src/gptrpg/session_actor/actor.py
  - tests/test_master_gm.py
  - tests/test_narration_guard.py
  - tests/test_safety_flag_pipeline.py
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04]
origin: 10-REVIEW.md 경고 3건(WR-01/WR-02/WR-03)을 전부 닫는다. 오케스트레이터가 작성.
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "한 턴에 제공자 스트림 호출이 3회를 못 넘는다는 것을 **실제 호출을 세는 시험**이 단언한다 — 주석이나 상수가 아니라 시험이 그 보증을 진다(WR-01)"
    - "`matched_len <= subject_len`이 모든 판정 갈래에서 예외 없이 성립한다 — 문장 경계를 넘는 겹침에서도(WR-02)"
    - "운영자 표준오류 발췌가 실제로 걸린 글자를 보여준다 — 겹침이 다음 문장 쪽에 몰려 있어도(WR-02)"
    - "`source`와 `reason`의 말 안 되는 조합이 사건 기록에 못 들어간다 — 스키마와 액터 양쪽에서 막힌다(WR-03)"
    - "기존 804건이 전부 그대로 통과한다 — 이 계획은 회귀를 만들지 않는다"
  artifacts:
    - "`matched_len <= subject_len` 불변식 시험"
    - "턴당 제공자 스트림 호출 횟수 시험"
  key_links:
    - "`GuardVerdict.subject_len`의 의미 ↔ `SafetyFlagged.subject_len`이 영구 기록하는 값 ↔ 14단계가 그 값으로 심각도를 잰다 (세 자리의 의미가 같아야 한다)"
    - "`SafetyFlagged`의 새 교차검증 ↔ `_prepare_safety_flag`의 같은 검증 (한쪽만 있으면 다른 경로로 우회된다)"
  prohibitions:
    - statement: "`matched_len`을 `subject_len`으로 자르는 방식(리뷰의 (b)안)을 쓰지 않는다 — 유출이 실제로 얼마나 컸는지가 사라진다. 14단계 심각도 분석이 쓸 값이라 정보를 깎으면 안 된다"
      status: unverified
      verification: flagged
    - statement: "재생성 블록을 반복문으로 바꾸지 않는다 — 스트리밍 상태 기계를 구조적으로 건드리는 변경이고, 그 자리가 정확히 22분 먹통 사고가 났던 자리다. 보증은 시험이 진다"
      status: unverified
      verification: flagged
    - statement: "`EVENT_SCHEMA_VERSION`을 올리지 않는다 — 어떤 DB에도 `safety_flagged` 기록이 0건임을 확인했으므로(events.db 0 / uat9.db 0 / uat10.db 0) 다시 해석할 옛 기록이 존재하지 않는다"
      status: unverified
      verification: flagged
---

<objective>
10단계 코드 리뷰가 낸 경고 3건을 전부 닫는다. 셋 다 지금 게임을 깨뜨리지는 않지만, 셋 다
**나중에 조용히 틀리게 만드는** 종류다.

Purpose: 세 지적의 공통점은 「지금은 우연히 맞다」는 것이다 — 상수가 이름값을 못 하는데
구조상 어쩌다 맞고(WR-01), 숫자가 말이 안 되는데 아직 아무도 안 읽었고(WR-02), 조합 검증이
없는데 호출부가 마침 다 맞다(WR-03). 이 단계는 **되돌릴 수 없는 사건 기록**을 다루므로
「우연히 맞다」를 「막혀 있다」로 바꾼다.
Output: 호출 횟수를 실제로 세는 시험, 모든 갈래에서 성립하는 숫자 불변식, 그리고 스키마 층의
교차검증.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-ai/10-CONTEXT.md
@.planning/phases/10-ai/10-REVIEW.md
@.planning/phases/10-ai/10-03-SUMMARY.md
@.planning/phases/10-ai/10-06-SUMMARY.md
</context>

## 오케스트레이터가 확인한 사실 (그대로 믿어도 되지만, 어긋나면 멈추고 보고할 것)

1. **`safety_flagged` 기록이 어떤 DB에도 0건이다** — `.gptrpg/events.db` 0 / `.gptrpg/uat9.db` 0 /
   `.gptrpg/uat10.db` 0. 그래서 `subject_len`의 의미를 바꿔도 다시 해석할 옛 기록이 없다.
   이것이 Task 2가 값을 자르지 않고 의미를 바로잡는 쪽을 택할 수 있는 유일한 이유다.
2. **`matched_len > subject_len`이 실제로 재현된다** — `matched_len=24, subject_len=1`
   (문장에 1자, 다음 문장에 나머지를 두면 난다).
3. **`MAX_REGENERATIONS`는 자기 선언과 주석 밖에서 안 읽힌다** (master_gm.py:42, 399, 520).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 겹친 글자수 불변식을 바로잡는다 (WR-02, SAFE-02)</name>
  <files>src/gptrpg/agents/narration_guard.py, src/gptrpg/agents/master_gm.py, tests/test_narration_guard.py</files>
  <read_first>
    - src/gptrpg/agents/narration_guard.py (전체 — `find_source_overlap` 142-185줄, `GuardVerdict` 도크스트링의 `subject_len` 설명, `inspect_sentence` 258줄 `subject_len = len(sentence)`)
    - src/gptrpg/agents/master_gm.py (188-212줄 — 표준오류 발췌를 만드는 자리, `sentence[:STDERR_EXCERPT_CHARS]`)
    - tests/test_narration_guard.py (140-160줄 `test_find_source_overlap_across_sentence_boundary_is_blocked` — 이미 경계 넘는 경우를 만들지만 두 숫자의 관계는 안 본다)
    - .planning/phases/10-ai/10-REVIEW.md (WR-02 전문)
  </read_first>
  <behavior>
    - 경계를 안 넘는 겹침 → `subject_len == len(sentence)` (지금과 같다)
    - 경계를 넘는 겹침 → `subject_len`이 실제로 대조된 텍스트 길이를 담아 `matched_len <= subject_len`이 성립한다
    - 생각 블록·캐릭터 이탈·깨진 글자 갈래 → `subject_len == len(sentence)` (안 바뀐다)
    - 어느 갈래에서도 `matched_len <= subject_len`이 깨지지 않는다
    - 표준오류 발췌가 경계를 넘는 겹침에서 실제로 걸린 글자를 포함한다
  </behavior>
  <action>
**핵심 판단 — `subject_len`의 의미를 확정한다.** 지금 도크스트링은 「검사 대상 문장의 코드포인트
개수」라고 적혀 있는데, 원문 겹침 갈래는 실제로 `sentence + next_sentence`를 이어 붙여 대조한다.
그래서 **의미를 「이 판정의 `matched_len`을 잰 대상 텍스트의 길이」로 바로잡는다.**
  - 원문 겹침 갈래에서 겹침이 `len(sentence)`를 넘어 다음 문장까지 뻗었으면
    `subject_len = len(sentence) + len(next_sentence or "")`.
  - 넘지 않았으면 `subject_len = len(sentence)` — 지금과 같다.
  - 나머지 세 갈래는 전부 `len(sentence)` 그대로.

**값을 자르지 않는다**(리뷰의 (b)안 거부) — 유출이 실제로 얼마나 컸는지가 14단계 심각도
분석이 쓸 정보다. 분자를 깎는 대신 분모를 정직하게 만든다. `matched_len / subject_len` 비율이
그대로 의미를 갖게 되는 것이 이 선택의 이득이다.

`GuardVerdict`의 `subject_len` 도크스트링을 새 의미로 고치고, **왜 갈래마다 다른지**를 적는다 —
「원문 겹침만 두 문장을 이어 붙여 대조하기 때문이고, 그래서 분모도 이어 붙인 길이여야 한다」.

**표준오류 발췌(`master_gm.py`)** — 겹침이 경계를 넘은 경우 발췌를 `sentence`가 아니라
이어 붙인 텍스트에서 뜬다. 발췌는 **운영자 자기 터미널에만** 가고 사건 기록에는 안 들어가므로
(T-10-03) 다음 문장 내용이 들어가도 안전하다. 길이 상한은 기존 `STDERR_EXCERPT_CHARS`를 그대로
쓴다.

**시험(`tests/test_narration_guard.py`)** — 기존 `test_find_source_overlap_across_sentence_boundary_is_blocked`
옆에:
  - 경계 넘는 경우에 `matched_len <= subject_len`을 단언
  - **모든 갈래에 대한 불변식 시험** — 생각 블록/원문 겹침(경계 안·밖)/캐릭터 이탈/깨진 글자
    각각에 대해 `verdict.matched_len <= verdict.subject_len`을 단언한다. 갈래가 나중에 늘어도
    이 시험이 새 갈래를 강제하도록 시험 이름과 주석에 그 의도를 적는다.
  - 리뷰가 재현한 그 모양(문장에 1자 + 다음 문장에 나머지)을 그대로 시험으로 고정한다
  </action>
  <verify>
    <automated>uv run pytest tests/test_narration_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_narration_guard.py -x` exits 0
    - 네 갈래 전부에 대해 `matched_len <= subject_len`을 단언하는 시험이 존재한다
    - 리뷰가 재현한 「1자 + 나머지」 모양이 시험으로 고정돼 있다
    - `GuardVerdict.subject_len` 도크스트링이 새 의미와 그 이유를 적고 있다
    - `matched_len`을 자르는 코드가 **없다**
  </acceptance_criteria>
  <done>겹친 글자수가 문장 길이보다 큰 숫자가 사건 기록에 들어갈 수 없고, 운영자가 보는 발췌가 실제로 걸린 글자를 보여준다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: source × reason 조합을 스키마가 막는다 (WR-03)</name>
  <files>src/gptrpg/event_log/schema.py, src/gptrpg/session_actor/actor.py, tests/test_safety_flag_pipeline.py</files>
  <read_first>
    - src/gptrpg/event_log/schema.py (249-290줄 `SafetyFlagged`, 그리고 `CheckResolved._require_identity_from_schema_5` — **이 `model_validator` 모양을 그대로 본뜬다**)
    - src/gptrpg/session_actor/actor.py (212-217줄 상수, 741-766줄 `_prepare_safety_flag`)
    - .planning/phases/10-ai/10-REVIEW.md (WR-03 전문)
  </read_first>
  <behavior>
    - `source="classifier", reason="unknown_move"` → 통과 (지금 실제로 쓰는 조합)
    - `source="narration"` + `think_block`/`source_overlap`/`character_break`/`corrupted_glyph` → 통과
    - `source="classifier", reason="think_block"` → **거부**
    - `source="narration", reason="unknown_move"` → **거부**
    - 거부는 스키마(pydantic)에서도, 액터 명령 검증에서도 각각 일어난다 — 한쪽을 우회해도 다른 쪽이 막는다
  </behavior>
  <action>
**① `event_log/schema.py`** — `SafetyFlagged`에 `model_validator(mode="after")`를 더한다.
`CheckResolved`가 이미 쓰는 그 모양을 따른다(새 방식을 만들지 않는다). 불변식은 하나:
**`reason == "unknown_move"` 인 것과 `source == "classifier"` 인 것이 서로 필요충분이다.**
검증자 옆 주석에 **왜 이게 참인지**를 적는다 — 분류기는 닫힌 목록 밖 응답 하나만 신고하고
(SAFE-07), 나머지 넷은 전부 서사 검사가 내는 사유다. 새 사유를 더하는 사람이 어느 쪽에
속하는지 반드시 정하게 만드는 것이 이 검증자의 목적이다.

**② `session_actor/actor.py`** — `_prepare_safety_flag`에 같은 교차검증을 더한다. 기존
`_VALID_SAFETY_FLAG_SOURCES`/`_VALID_SAFETY_FLAG_REASONS` 개별 검증은 **그대로 두고** 그 뒤에
조합 검증을 잇는다(개별 검증이 먼저 걸러야 오류 메시지가 정확하다). 오류 메시지는 기존
검증들과 같은 말투로 쓰고, 어떤 조합이 왜 안 되는지 알려 준다.

**두 층에 같은 규칙을 두 번 쓰는 것이 중복이 아닌 이유**를 양쪽 주석에 남긴다 — 액터 검증은
명령이 저장소에 닿기 전에 막고, 스키마 검증은 저장소를 우회해 만든 객체까지 막는다.
`CheckResolved`가 이미 같은 이중 구조다.

**③ 시험(`tests/test_safety_flag_pipeline.py`)** — 통과해야 하는 조합 다섯 개와 거부돼야 하는
조합 최소 둘을 표로 돌린다. 거부는 스키마 층과 액터 층 **각각** 확인한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_safety_flag_pipeline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_safety_flag_pipeline.py -x` exits 0
    - `grep -c "model_validator" src/gptrpg/event_log/schema.py` 가 변경 전보다 1 이상 늘었다
    - 잘못된 조합이 스키마 층에서 거부됨을 단언하는 시험이 있다
    - 잘못된 조합이 액터 층에서 거부됨을 단언하는 시험이 있다
    - `EVENT_SCHEMA_VERSION` 이 여전히 6이다
  </acceptance_criteria>
  <done>말이 안 되는 사유·출처 조합이 되돌릴 수 없는 기록에 들어갈 수 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 턴당 호출 횟수를 시험이 지킨다 (WR-01, SAFE-04)</name>
  <files>src/gptrpg/agents/master_gm.py, tests/test_master_gm.py</files>
  <read_first>
    - src/gptrpg/agents/master_gm.py (42줄 `MAX_REGENERATIONS`, 390-410줄·515-525줄의 그 이름을 언급하는 주석, `narrate()`의 재생성 블록과 `_drive` 호출 자리 전부)
    - tests/test_master_gm.py (10-03이 만든 재생성·포기 시험들 — 가짜 제공자 모양을 그대로 재사용한다)
    - .planning/phases/10-ai/10-03-SUMMARY.md (「호출 한 번 = 타임아웃 + 재시도 1회」 규율과 최대 3회 계산)
    - .planning/phases/10-ai/10-REVIEW.md (WR-01 전문)
  </read_first>
  <behavior>
    - 깨끗한 서사 → 제공자 `stream()` 호출 1회
    - 한 번 걸리고 재생성 성공 → 2회
    - 두 번 걸려 포기 → 2회
    - 어떤 경로에서도 한 턴에 3회를 넘지 않는다
  </behavior>
  <action>
**① 거짓말하는 상수를 없앤다.** `MAX_REGENERATIONS = 1`을 지운다. 이 이름은 런타임 강제를
암시하는데 실제로는 아무 데서도 안 읽힌다 — 아무것도 안 지키면서 지킨다고 믿게 만드는 쪽이
없느니만 못하다. 그 이름을 언급하던 주석·도크스트링(399줄, 520줄)은 **구조적 사실을 직접
말하도록** 고친다: 「이 블록은 반복문이 아니라서 정확히 한 번 돈다」.

**재생성 블록을 반복문으로 바꾸지 않는다**(리뷰의 (a)안 거부) — 스트리밍 상태 기계를 구조적으로
건드리는 변경이고, 그 자리가 정확히 22분 먹통 사고가 났던 자리다. 얻는 것(이름과 강제가 한 줄)
보다 위험이 크다. 강제는 아래 시험이 진다.

**② 실제 호출을 세는 시험을 더한다 — 이것이 이 과제의 본체다.**
`tests/test_master_gm.py`에 제공자의 `stream()` 호출 횟수를 세는 가짜 제공자를 쓰고,
세 시나리오 각각에서 정확한 횟수를 단언한다:
  - 깨끗한 서사 → 정확히 1회
  - 한 번 걸리고 재생성이 통과 → 정확히 2회
  - 두 번 걸려 포기 → 정확히 2회
그리고 **어떤 경로에서도 3회를 넘지 않는다**를 단언하는 시험을 하나 더 둔다. 시험 이름과
도크스트링에 **왜 이 시험이 존재하는지**를 적는다 — 03-04에서 실제로 난 22분 먹통 사고가 이
곱셈에서 났고, 이제 그 보증을 지는 것은 주석이 아니라 이 시험이다. 나중에 누가 재생성을
반복문으로 바꾸면 **이 시험이 빨간불로 막는다** — 그게 상수가 못 하던 일이다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_master_gm.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_master_gm.py -x` exits 0
    - `grep -c "MAX_REGENERATIONS" src/gptrpg/agents/master_gm.py` 가 0이다
    - 제공자 `stream()` 호출 횟수를 세어 세 시나리오에서 각각 단언하는 시험이 존재한다
    - 3회 상한을 단언하는 시험이 존재하고, 그 도크스트링이 22분 먹통 사고를 이유로 댄다
    - 재생성 블록이 여전히 반복문이 아니다
    - `uv run pytest` 전체 스위트 exits 0
  </acceptance_criteria>
  <done>호출 횟수 보증을 주석이 아니라 돌아가는 시험이 진다.</done>
</task>

</tasks>

<verification>
- `uv run pytest` 전체 초록불 (804건 + 새 시험)
- `uv run lint-imports` 4계약 유지
- `uv run ruff check src` 통과
- `EVENT_SCHEMA_VERSION == 6`
- `matched_len <= subject_len`이 모든 갈래에서 성립
</verification>

<success_criteria>
1. 겹친 글자수가 문장 길이를 넘는 기록이 나올 수 없다
2. 말 안 되는 사유·출처 조합이 스키마와 액터 양쪽에서 막힌다
3. 턴당 호출 3회 상한을 시험이 지킨다 — 이름만 그런 상수가 아니라
4. 기존 804건이 그대로 통과한다
</success_criteria>

*Plan: 10-07 · Wave 5 · 10-REVIEW.md 경고 3건 닫기*

<output>
Create `.planning/phases/10-ai/10-07-SUMMARY.md` when done
</output>
