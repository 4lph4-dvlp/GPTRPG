---
phase: 08-identity-and-idempotency
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - tests/conftest.py
  - tests/test_web_actions.py
  - tests/test_session_actor.py
  - src/gptrpg/web/routes_actions.py
  - src/gptrpg/web/routes_characters.py
autonomous: true
requirements: [TEST-01, TEST-02, QUAL-04, QUAL-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "신원이 뜻을 갖는 시험이 가짜 상수 `\"p1\"`이 아니라 실제 캐릭터 넷(`bram`/`nari`/`seon`/`hodu`)으로 돈다 — 세션1의 버그 클래스가 CI에서 계속 초록불이던 구조가 사라졌다 (TEST-01)"
    - "여러 캐릭터가 번갈아 선언한 세션에서, AI에게 넘어가는 최근 대화에 각자의 발화에 각자의 이름이 붙는다 — 넷의 발화가 한 사람 것처럼 뭉뚱그려지지 않는다 (TEST-01)"
    - "확인 요청의 모든 자유 입력 칸에 길이·범위 상한이 걸린다 — `target`·`rulebook_id`·`modifiers` 목록과 그 항목까지 포함해 빠진 자리가 없다 (QUAL-04)"
    - "신원·점유·멱등성 어느 경로의 예외 문구·서버 로그에도 쿠키 값·비밀 열쇠·서명·브라우저 식별자·API 키가 실려 나가지 않는다 (QUAL-05)"
    - "겹치는 요청을 재현하는 시험이 실제 캐릭터 여러 명으로 돈다 — 픽스처 교체가 TEST-02 시험을 무력화하지 않았다 (TEST-02)"
    - statement: "`.gptrpg/events.db`의 세션1 기록 895건이 이 단계가 끝난 시점의 코드에서도 여전히 예외 없이 읽힌다 — 네 계획을 다 얹은 뒤에도 D-13의 약속이 지켜진다"
      verification: backstop
  artifacts:
    - tests/conftest.py
  key_links:
    - "`web.characters_data.PLAYER_CHARACTERS`(bram/nari/seon/hodu) → `tests/conftest.py`의 공유 픽스처 → 신원·점유·멱등성 시험 전부 — 이후 모든 단계의 검증이 이 픽스처 위에서 돈다 (ROADMAP Phase 8 주석)"
    - "`routes_actions._CHARACTER_NAMES` → `build_turn_context(character_names=...)` → AI 프롬프트의 최근 대화 — 각자의 발화에 각자의 이름이 붙는 유일한 경로. 2026-08-04 실전에서 여기가 끊겨 있었다"
    - "`ConfirmRequest.modifiers` → `_parse_modifier` → `ResolveCheck.modifiers` → `CheckResolved.modifiers` — 상한 없는 목록이 그대로 사건 기록의 크기가 되는 사슬"
  prohibitions:
    - statement: "픽스처를 바꾸면서 단언을 함께 느슨하게 만들지 않는다 — 이름을 `\"p1\"`에서 `\"bram\"`으로 바꾸는 김에 「어떤 이름이든 있으면 통과」로 완화하면, 이 단계가 없애려던 「구조적으로 안 보이는 버그 클래스」가 이름만 바꿔 그대로 남는다. 각자의 발화에 **각자의** 이름이 붙는지를 서로 다른 두 캐릭터로 대조해야 한다 (TEST-01)"
    - statement: "상한을 「일단 크게」 잡아 사실상 없는 것으로 만들지 않는다 — 상한의 목적은 신뢰할 수 없는 HTTP 본문이 AI 프롬프트와 사건 기록의 크기를 정하지 못하게 하는 것이다. 이미 있는 값(`MAX_ID_LEN`·`MAX_RAW_TEXT_LEN`)과 같은 자릿수를 쓰고, 새 숫자를 만들 때는 왜 그 값인지 옆에 적는다 (QUAL-04)"
    - statement: "문구 위생 점검을 「지금 눈으로 봤다」로 끝내지 않는다 — 사람이 훑어 통과시킨 규율은 다음 사람이 문구 한 줄을 더하는 순간 조용히 깨진다. 자동 시험이 실제 응답 본문과 로그 문자열을 검사하는 자리를 남긴다 (QUAL-05)"
---

<objective>
이후 모든 단계의 검증이 설 바닥을 놓는다. `"p1"` 하나로 고정된 시험 픽스처를 실제 캐릭터
여러 명으로 바꾸고, API 경계의 입력 상한을 전수로 훑고, 예외 문구에 자격 증명이 안 실린다는
것을 자동 시험으로 잠근다.

Purpose: ROADMAP이 「왜 이것이 코드의 첫 단계인가」에 적어 둔 그대로다 — 지금 스위트는
`player_id`를 `"p1"`으로 고정해서 **신원 버그 클래스를 구조적으로 볼 수 없다.** 세션1의
버그가 CI에서 계속 초록불이었던 이유이고, 이 픽스처를 안 고치면 뒤의 아홉 단계가 전부
깨진 자 위에서 검증된다.

Output: 실제 캐릭터로 도는 공유 픽스처, 각자의 이름이 각자의 발화에 붙는지 대조하는 시험,
빠진 자리 없는 입력 상한, 그리고 문구 위생 관문 시험.
</objective>

<scope_decisions>
## TEST-01의 교체 범위 — 전면 교체가 아니라 「뜻이 있는 자리」 교체 (CONTEXT.md가 계획 단계 판단으로 남긴 것)

`"p1"`은 13개 파일 44군데에 있다. 전부 바꾸는 것은 **하지 않는다.**

| 자리 | 처리 | 이유 |
|------|------|------|
| `tests/conftest.py`의 `fake_session_log`, `tests/test_web_actions.py`, `tests/test_session_actor.py` | **실제 캐릭터로 교체** | 이 세 곳의 `player_id`는 신원을 뜻한다 — 이 단계가 없애려는 버그 클래스가 정확히 여기 산다 |
| 위 픽스처를 소비하며 `"p1"`을 직접 단언하는 자리 | **따라서 교체** | 픽스처가 바뀌면 함께 바뀌어야 초록이 유지된다 |
| `test_reducer_*` · `test_grading*` · `test_resolution*` 등 순수 단위 시험 | **그대로 둔다** | 그 `"p1"`은 신원이 아니라 불투명한 토큰이다. 바꿔도 잡히는 버그가 하나도 늘지 않고, 문맥 예산만 태운다 |

**판단 기준 한 줄:** 그 자리에서 `"p1"`을 `"p2"`로 바꿨을 때 **시험의 뜻이 달라지면**
신원이고, 안 달라지면 토큰이다. 신원인 자리만 바꾼다.
</scope_decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-identity-and-idempotency/08-CONTEXT.md
@.planning/phases/08-identity-and-idempotency/08-RESEARCH.md
@.planning/phases/08-identity-and-idempotency/08-VALIDATION.md
@.planning/phases/08-identity-and-idempotency/08-01-SUMMARY.md
@.planning/phases/08-identity-and-idempotency/08-02-SUMMARY.md
@.planning/phases/08-identity-and-idempotency/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 실제 캐릭터 여러 명으로 도는 공유 픽스처</name>
  <files>tests/conftest.py, tests/test_session_actor.py</files>
  <read_first>
    - `tests/conftest.py` 전문(08-01 Task 4 변경 반영본) — 모듈 도크스트링이 적어 둔 규율(「메모리 안 객체를 그대로 돌려주면 저장·복원 과정에서 값이 상하는지 잡을 수 없다」), `_t()`/`_env()` 도우미, `_make_fake_events`의 14개 사건, `FakeSession` dataclass의 기대치 여섯 칸, 08-01이 더한 `select_character(...)` 도우미
    - `src/gptrpg/web/characters_data.py` 46~140줄 — 실제 캐릭터 넷의 식별자와 표시 이름(`bram`/`nari`/`seon`/`hodu` → 브람/나리/선/호두). **이 파일은 고치지 않는다** — 읽기 전용 출처로만 쓴다
    - `tests/test_session_actor.py` 전문(08-02·08-03 변경 반영본)
    - `tests/test_reverse_verification.py` · `tests/test_measurement.py` · `tests/test_report.py` — `fake_session_log`를 소비하며 `"p1"`을 직접 단언하는 자리가 있는지 확인(있으면 함께 고친다)
    - `.planning/phases/08-identity-and-idempotency/08-CONTEXT.md` § Claude's Discretion > TEST-01
  </read_first>
  <action>
**① `tests/conftest.py`의 `_make_fake_events`.** `"p1"`/`"p2"`를 실제 캐릭터 식별자
`"bram"`/`"nari"`로 바꾼다. 08-01이 이미 `CheckResolved`의 `person_id`/`character_id`를
채워 두었으므로, 그 값들이 각 판정을 낸 캐릭터와 **실제로 짝이 맞는지** 여기서 다시
확인한다(seq 4는 bram의 판정, seq 8은 nari의 판정, seq 12는 bram의 판정). 08-01이 넣은
`ActionDeclared.character_id`/`ActionConfirmed.character_id`도 같은 짝으로 채운다.
`FakeSession`의 기대치 여섯 칸(`token_spend`·`turn_count`·`failure_count`·`clock_advance_count`·
`declare_confirm_ms`·`confirm_narration_ms`)은 **숫자가 하나도 안 바뀐다** — 사람 이름만
바뀌었다. 바뀌었다면 사건 구성을 잘못 건드린 것이다.

**② 캐릭터 넷이 모두 등장하는 공유 픽스처를 하나 더한다.** 이름은 `four_player_session`
정도로 짓고, 실제 `EventStore`에 append한 뒤 `read_events`로 다시 읽어 돌려주는 기존
규율을 그대로 지킨다(모듈 도크스트링이 그 이유를 적어 두었다 — 저장·복원에서 값이 상하는
것을 잡기 위해서다). 넷이 각자 한 번씩 선언·확인·판정하는 최소 구성이면 충분하다.
**손으로 만드는 시험 전용 사건 만들기 도우미를 새로 만들지 않는다** — 기존 `_env()`를 쓴다.

**③ `tests/test_session_actor.py`.** `"p1"`을 실제 캐릭터로 바꾼다. 08-02·08-03이 심은
점유·멱등성 시험은 이미 실제 캐릭터를 쓰고 있으므로, 그 이전부터 있던 시험들만 대상이다.
바꾸면서 **단언을 느슨하게 만들지 않는다** — 이름만 바꾸고 검사의 강도는 그대로다.

**④ `fake_session_log`를 소비하는 시험들.** `"p1"`을 직접 단언하는 자리가 있으면 함께
고친다. 순수 단위 시험(`test_reducer_*`·`test_grading*`·`test_resolution*`)의 `"p1"`은
**그대로 둔다** — 위 § scope_decisions의 판단 기준을 적용한다.

전체 스위트가 초록으로 남는 것이 이 과제의 종료 조건이다.
  </action>
  <acceptance_criteria>
    - `grep -c '"p1"' tests/conftest.py tests/test_web_actions.py tests/test_session_actor.py`가 세 파일 모두 0이다
    - `grep -rn '"p1"' tests/ | wc -l`의 값이 44보다 작다 (교체가 실제로 일어났다)
    - `tests/conftest.py`에 캐릭터 넷이 모두 등장하는 픽스처가 있고, `list_characters()`가 돌려주는 네 식별자가 전부 그 안에 나온다
    - `FakeSession`의 여섯 기대치 숫자가 이 과제 전후로 하나도 바뀌지 않았다 (`git diff`로 확인 — 이름만 바뀌었다)
    - `uv run pytest -q` 전체 통과
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>시험이 「이름 없는 p1」이 아니라 브람·나리·선·호두로 돈다 — 이후 아홉 단계의 검증이 설 바닥이 놓였다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 각자의 발화에 각자의 이름이 붙는지 서로 다른 두 캐릭터로 대조한다 (TEST-01)</name>
  <files>tests/test_web_actions.py</files>
  <read_first>
    - `src/gptrpg/web/routes_actions.py` 69~72줄 — `_CHARACTER_NAMES` 상수와 그 도크스트링이 기록한 실전 사고(「최근 대화에서 '플레이어: '만 찍히면 네 명의 발화가 전부 한 사람 것처럼 뭉뚱그려진다, 2026-08-04 실전에서 발견」). **이 시험이 막으려는 것이 정확히 그 사고다**
    - `src/gptrpg/web/routes_actions.py` 173~180줄·364~370줄 — `build_turn_context(..., character_names=_CHARACTER_NAMES)`를 넘기는 두 자리
    - `src/gptrpg/turn/context.py` — `character_names`가 최근 대화 문자열로 바뀌는 자리(발화자 이름이 실제로 찍히는 지점)
    - `tests/conftest.py`의 `FakeProvider` — `self.calls`에 `(system, messages)` 짝을 그대로 쌓아 두는 구조. 프롬프트 내용을 시험이 직접 볼 수 있는 유일한 창이다
    - `tests/test_web_actions.py`의 `test_prompt_never_carries_clock_advance_count_or_failure_accumulator` — 프롬프트 내용을 검사하는 기존 시험의 결(그대로 따른다)
    - `.planning/phases/08-identity-and-idempotency/08-VALIDATION.md` — TEST-01 행의 자동 명령(`-k multi_character_names`)
  </read_first>
  <behavior>
    - 브람이 선언하고, 이어서 나리가 선언한 뒤 나리의 확인을 태우면 — AI에게 넘어간 프롬프트 문자열에 **「브람」과 「나리」가 둘 다** 나온다
    - 그 프롬프트에서 브람의 문장과 나리의 문장이 **서로 다른 이름표**를 달고 있다 (두 발화가 같은 이름표를 달고 있으면 실패)
    - 캐릭터 식별자(`bram`/`nari`)가 아니라 표시 이름(브람/나리)이 찍힌다 — 사람이 읽는 자리이기 때문이다
    - 넷이 모두 선언한 세션에서는 네 이름이 모두 나온다
    - 각 브라우저는 자기 캐릭터로만 선언할 수 있다 — 브람 쿠키로 나리 이름의 선언을 만들 수 없다 (08-01의 403이 이 시험 안에서도 유지됨을 함께 확인)
  </behavior>
  <action>
`tests/test_web_actions.py`에 시험 이름에 `multi_character_names`가 들어가는 시험을 더한다 —
08-VALIDATION.md의 TEST-01 자동 명령이 그 이름으로 집는다.

두 개(또는 넷)의 독립된 `TestClient`를 만들어 각각 `select-character`로 서로 다른 캐릭터를
잡게 한다(쿠키 단지가 분리되어야 서로 다른 브라우저가 된다 — 08-02 Task 2와 같은 함정이다).
각 클라이언트로 선언을 보낸 뒤, `FakeProvider.calls`에 쌓인 마지막 호출의
`(system, messages)`를 문자열로 이어 붙여 검사한다.

**핵심 단언은 「이름이 나온다」가 아니라 「두 발화가 서로 다른 이름표를 단다」다.**
「어떤 이름이든 있으면 통과」로 쓰면 네 발화가 전부 「브람」으로 찍히는 정확히 그 사고를
못 잡는다. 브람의 원문(`raw_text`)이 있는 줄과 나리의 원문이 있는 줄을 각각 찾아, 그
두 줄에 붙은 이름표가 서로 다름을 단언한다.

마지막 항목(브람 쿠키로 나리 선언 불가)은 08-01이 이미 시험하고 있지만, **이 시험 안에서
한 번 더 확인한다** — 다중 캐릭터 픽스처 위에서도 신원 규칙이 유지된다는 것이 TEST-01과
TRUST-02가 만나는 자리이고, 픽스처를 바꾸다 그 규칙을 우회하는 지름길을 만들면 여기서 걸린다.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_web_actions.py -k multi_character_names -x` 통과 (08-VALIDATION.md의 TEST-01 자동 명령 그대로)
    - 시험이 브람의 발화와 나리의 발화에 붙은 이름표가 **서로 다름**을 단언한다 — 「이름이 존재한다」만 보는 단언이 아니다
    - 시험이 표시 이름(브람/나리)을 단언한다 — 식별자(`bram`/`nari`)만 확인하는 단언이 아니다
    - 네 캐릭터가 모두 등장하는 변형 시험이 존재하고, 네 표시 이름이 모두 프롬프트에 나옴을 단언한다
    - 브람 쿠키로 나리 이름의 선언을 만들 수 없음을 이 시험 파일 안에서 한 번 더 단언한다 (TRUST-02가 다중 캐릭터 위에서도 유지)
    - `uv run pytest -q` 전체 통과
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_web_actions.py -k multi_character_names -x &amp;&amp; uv run pytest -q</automated>
  </verify>
  <done>네 명의 발화가 한 사람 것처럼 뭉뚱그려지면 CI가 빨간불을 낸다 — 2026-08-04에 실전에서만 발견됐던 사고가 이제 시험에 걸린다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API 경계 입력 상한 전수 훑기와 문구 위생 관문 (QUAL-04·QUAL-05)</name>
  <files>src/gptrpg/web/routes_actions.py, src/gptrpg/web/routes_characters.py, tests/test_web_actions.py</files>
  <read_first>
    - `src/gptrpg/web/routes_actions.py` 76~92줄 — `MAX_RAW_TEXT_LEN = 2000`·`MAX_ID_LEN = 64`와 각 도크스트링이 적어 둔 근거(「이 자리가 처음으로 신뢰할 수 없는 HTTP 본문이 된다」)
    - `src/gptrpg/web/routes_actions.py` 139~144줄(`DeclareRequest`) · 239~251줄(`ConfirmRequest`) — 상한이 걸린 칸과 **안 걸린 칸**을 하나씩 세어 확인할 자리
    - `src/gptrpg/web/routes_actions.py` 95~111줄 — `_parse_modifier`가 `modifiers` 목록의 각 문자열을 쪼개는 방식
    - `src/gptrpg/web/routes_events.py` 64~70줄 — `from_seq: int = Query(0, ge=0)`(이미 걸린 자리, 본보기)
    - `src/gptrpg/web/app.py` 46~61줄 — `SAFE_SESSION_ID` 정규식이 경로 조각의 상한 역할을 하는 자리(이미 걸림)
    - `src/gptrpg/web/routes_characters.py` — 08-01이 `SelectCharacterRequest`에 건 상한(이미 걸림)
    - `src/gptrpg/session_actor/actor.py` 197~198줄과 08-02·08-03이 더한 예외 하위 클래스들 — 문구에 값이 안 실렸는지 훑을 대상
    - `.planning/phases/08-identity-and-idempotency/08-RESEARCH.md` § Common Pitfalls > Pitfall 3 · § Security Domain의 V5 항목
  </read_first>
  <behavior>
    - `ConfirmRequest.target`이 음수이거나 터무니없이 큰 값이면 **422**로 거부된다
    - `rulebook_id`가 상한을 넘으면 **422** (`DeclareRequest`·`ConfirmRequest` 양쪽)
    - `modifiers` 목록의 **항목 수**가 상한을 넘으면 422
    - `modifiers`의 **한 항목 문자열**이 상한을 넘으면 422
    - 이미 상한이 걸려 있던 칸들(`player_id`·`character_id`·`move`·`stat`·`suggestion_*`·`raw_text`)은 동작이 하나도 안 바뀐다
    - 신원 불일치(403)·점유 충돌(409)·멱등 거부(400) 세 응답의 본문 어디에도 쿠키 값 전체·비밀 열쇠·서명 조각·`browser_id`가 부분 문자열로 들어 있지 않다
    - 같은 세 경우의 서버 로그 출력에도 위 넷이 들어 있지 않다
  </behavior>
  <action>
**① 상한 전수 훑기(QUAL-04).** 웹 계층의 **모든** 요청 모델·질의 인자·경로 조각을 한 번씩
세어 표로 확인한 뒤, 빠진 자리에만 상한을 건다. 지금 확인된 빈자리는 넷이다 —
`ConfirmRequest.target`(범위 없음), `DeclareRequest.rulebook_id`·`ConfirmRequest.rulebook_id`(길이 없음),
`ConfirmRequest.modifiers`(목록 길이 없음 + 항목 길이 없음). `target`에는 `Field(ge=..., le=...)`로
범위를, 나머지에는 `Field(max_length=...)`/`Field(max_length=...)`를 건다.

**새 숫자를 만들 때는 왜 그 값인지를 옆에 한 줄로 적는다** — `MAX_RAW_TEXT_LEN`·`MAX_ID_LEN`
두 상수가 이미 그렇게 하고 있다. 가능하면 이미 있는 두 상수를 재사용하고, 새 상수가
꼭 필요하면 같은 자리(76~92줄 근처)에 같은 형식으로 선언한다. **상한을 「일단 크게」 잡지
않는다** — 상한의 목적은 신뢰할 수 없는 본문이 AI 프롬프트와 사건 기록의 크기를 정하지
못하게 하는 것이다.

이미 상한이 걸린 자리(`session_id` 경로 조각의 정규식, `from_seq`의 `ge=0`,
`SelectCharacterRequest`, `DeclareRequest`/`ConfirmRequest`의 식별자 칸들)는 **건드리지
않는다**. 전수 확인 결과 표를 커밋 메시지 본문에 남긴다 — 다음에 「전수로 봤나」를
다시 묻지 않도록.

**② 문구 위생 관문(QUAL-05).** `tests/test_web_actions.py`에 시험 하나를 더한다:
신원 불일치(403)·점유 충돌(409)·멱등 거부(400) 세 경우를 실제로 만들고, 각 응답의 본문
전체 문자열과 `capsys`로 잡은 서버 로그 문자열에 대해 ① 발급된 쿠키 값 ② 비밀 열쇠의
16진 표현 ③ 서명 조각 ④ `browser_id` 값 넷이 **부분 문자열로 없음**을 단언한다.
08-01 Task 4가 `tests/test_web_characters.py`에 심은 `no_secret_leak` 시험과 짝이 되는
행동 경로 쪽 관문이다 — 그 시험을 옮기거나 지우지 않는다.

`session_actor`·`web` 두 층의 예외 문구를 한 번 훑어, 값이 문구에 삽입된 자리가 있으면
유형만 말하도록 고친다. `_prepare_ai_call`(`actor.py:508-511`)이 숫자 비교 실패를 말하되
실제 토큰 값을 매번 넣지는 않는 절제를 본보기로 삼는다.

**③ 단계 마무리 확인.** 이 과제가 이 단계의 마지막이므로, 08-VALIDATION.md의
phase gate를 실제로 돌린다 — 전체 스위트 초록 + `.gptrpg/events.db` 복제본을 판 5 코드로
접었을 때 예외 없음(08-01 Task 3이 심은 시험이 여전히 통과하는지). 결과를 SUMMARY에 적는다.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_web_actions.py -k max_length -x` 통과 — 새로 상한을 건 네 자리가 각각 422임을 단언하는 시험이 있다 (QUAL-04)
    - `uv run pytest tests/test_web_actions.py -k no_secret_leak -x` 통과 (QUAL-05)
    - `uv run pytest tests/test_web_characters.py -k no_secret_leak -x`가 여전히 통과한다 (08-01의 짝 시험을 지우지 않았다)
    - `grep -c 'max_length\|ge=\|le=' src/gptrpg/web/routes_actions.py`의 값이 이 과제 전보다 크다
    - 이미 상한이 걸려 있던 칸들의 동작이 안 바뀌었음을 기존 422 시험 둘(`tests/test_web_actions.py:109`·`:119`)이 통과로 증명한다
    - `uv run pytest tests/test_event_log.py -x` 통과 — 세션1 기록 895건이 이 단계가 끝난 뒤에도 판 5 코드에서 읽힌다 (phase gate)
    - `uv run pytest -q` 전체 통과, `uv run lint-imports` 종료 코드 0, `uv run ruff check src/gptrpg` 종료 코드 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run lint-imports &amp;&amp; uv run ruff check src/gptrpg</automated>
  </verify>
  <done>API 경계에 상한이 빠진 자리가 없고, 자격 증명이 문구로 새 나가지 않는다는 것이 사람의 눈이 아니라 시험으로 잠겼다.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## 이 계획이 만드는 것 (이름 단위)

**새 시험 픽스처:** `tests/conftest.py`의 `four_player_session`(캐릭터 넷이 모두 등장하는 완결 세션)

**바뀐 시험 픽스처:** `fake_session_log` — `"p1"`/`"p2"` → `"bram"`/`"nari"`, 판 5 칸(`character_id`·`person_id`) 짝 맞춤

**늘어난 입력 상한:** `ConfirmRequest.target`(범위) · `DeclareRequest.rulebook_id`·`ConfirmRequest.rulebook_id`(길이) · `ConfirmRequest.modifiers`(목록 길이 + 항목 길이) — 필요하면 `routes_actions` 상단에 새 상한 상수(근거 주석 포함)

**새 시험:** `tests/test_web_actions.py`의 `multi_character_names` 계열 · `max_length` 계열 · `no_secret_leak`(행동 경로판)

**만들지 않는 것(명시):** `src/gptrpg/web/characters_data.py`는 이 단계에서 한 글자도 안 바뀐다 — 읽기 전용 출처로만 쓴다
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 신뢰할 수 없는 HTTP 본문 → pydantic 요청 모델 | 상한이 빠진 칸은 그대로 AI 프롬프트 크기와 사건 기록 크기가 된다. `MAX_RAW_TEXT_LEN` 도크스트링이 이미 이 경계를 이름 붙여 두었다 |
| 서버 내부 값(쿠키·열쇠·서명·`browser_id`) → 응답 본문·서버 로그 | 문구 한 줄을 더하는 순간 조용히 새 나갈 수 있는 경계. 사람의 훑기로는 지켜지지 않는다 |
| 시험 픽스처 → 이후 아홉 단계의 검증 | 픽스처가 신원을 표현하지 못하면 그 위의 모든 검증이 신원 버그를 구조적으로 못 본다 — 세션1이 CI 초록불 아래에서 망가진 경로 그 자체다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-19 | Denial of Service | `ConfirmRequest.modifiers` / `target` / `rulebook_id` (상한 없음) | medium | mitigate | 목록 길이·항목 길이·정수 범위 상한을 건다. 상한 없는 목록은 그대로 `CheckResolved.modifiers`가 되어 사건 기록의 크기를 요청자가 정하게 된다. Task 3의 `max_length` 시험이 관문 (QUAL-04) |
| T-08-20 | Information Disclosure | 403·409·400 응답 본문과 서버 로그 | high | mitigate | 세 경우 각각에 대해 쿠키 값·비밀 열쇠·서명·`browser_id` 부재를 자동 시험이 단언한다. 08-01의 `test_web_characters` 짝 시험과 함께 두 파일에서 잠근다 (QUAL-05) |
| T-08-21 | Repudiation | 시험 픽스처가 신원을 표현하지 못함 | high | mitigate | 실제 캐릭터 넷으로 도는 픽스처와, 두 발화의 이름표가 **서로 다름**을 대조하는 단언. 「이름이 있으면 통과」로 느슨해지는 것을 Task 2의 수락 기준이 막는다 (TEST-01) |
| T-08-22 | Tampering | 픽스처 교체 중 단언 완화 | medium | mitigate | `FakeSession`의 여섯 기대치 숫자가 하나도 안 바뀌었음을 `git diff`로 확인한다 — 이름만 바뀌고 검사 강도는 그대로임의 기계적 증거 |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | 이 계획에 패키지 설치 명령이 없다 |

**ASVS level 1 / block on high.** high 둘(T-08-20·T-08-21)이 mitigate이고 각각 자동 시험이
관문으로 걸려 있다. 이 계획의 ASVS 대응은 V5(입력 검증 — 모든 경계에 상한)와
V2/V3(신원·세션 관련 값이 응답으로 새지 않음)이 중심이다.
</threat_model>

<verification>
- `uv run pytest -q` 전체 통과
- `uv run lint-imports` · `uv run ruff check src/gptrpg` 종료 코드 0
- `uv run pytest tests/test_web_actions.py -k multi_character_names -x` 통과
- `uv run pytest -k no_secret_leak -x` 통과 (두 파일의 짝 시험 모두)
- `uv run pytest tests/test_event_log.py -x` 통과 — 세션1 기록 895건이 판 5 코드에서 여전히 읽힌다 (phase gate)
- `grep -rn '"p1"' tests/ | wc -l`이 44보다 작고, 신원이 뜻을 갖는 세 파일에서는 0이다
</verification>

<success_criteria>
1. 신원이 뜻을 갖는 시험이 실제 캐릭터 넷으로 돈다 (TEST-01)
2. 각자의 발화에 각자의 이름이 붙는지가 두 캐릭터 대조로 단언된다 (TEST-01)
3. API 경계의 입력 상한에 빠진 자리가 없다 (QUAL-04)
4. 예외 문구·로그에 자격 증명이 실려 나가지 않는다는 것이 자동 시험으로 잠겼다 (QUAL-05)
5. 겹치는 요청 시험이 다중 캐릭터 픽스처 위에서도 그대로 돈다 (TEST-02)
6. 단계 종료 관문(전체 스위트 초록 + 세션1 기록 895건 읽기)이 통과한다
</success_criteria>

<output>
Create `.planning/phases/08-identity-and-idempotency/08-04-SUMMARY.md` when done
</output>
