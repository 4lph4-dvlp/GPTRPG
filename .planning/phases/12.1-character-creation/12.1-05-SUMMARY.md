---
phase: 12.1-character-creation
plan: 05
subsystem: web-api
tags: [event-sourcing, fastapi, character-creation, dungeonworld, sqlite]

# Dependency graph
requires:
  - phase: 12.1-character-creation
    provides: "12.1-01의 사건 판 9(character_created/party_roster_locked 포함)와 GameState.created_characters/party_roster. 12.1-02~04가 완성한 만들기 흐름 전체(인원 확정→값 결정→GM 진행→정리→동의→잠금)"
provides:
  - "web/characters_data.py 삭제 — 브람·나리·선·호두 넷이 제품 코드에서 완전히 사라지고 tests/fixtures/characters.py에서 시험 재료로만 산다(D-12, D-49)"
  - "캐릭터 목록·시트·선택·판정 컨텍스트 네 호출부가 전부 GameState.created_characters(세션에서 실제로 만들어진 캐릭터)를 유일한 출처로 본다"
  - "_current_party_state가 '파티 = 저장소에 선언된 캐릭터 전부'에서 '파티 = 이 세션의 명단(잠기면 party_roster 순서, 안 잠기면 완성 순서)'으로 바뀜(D-01/D-08)"
  - "_created_character()/_character_names() 헬퍼 — routes_actions.py의 다섯 호출부(_current_party_state 포함)가 공유하는 세션 기반 캐릭터 조회"
  - "app.state.db_path — 시험 코드가 앱의 sqlite 연결을 건드리지 않고 같은 파일에 별도 연결을 여는 이음매"
  - "tests/conftest.py의 seed_character_created()/select_character() — 시험이 세션 캐릭터를 심고 고르는 공용 지름길(제품 코드에는 없다)"
  - "PARTY_MEMBER_LIMIT 도크스트링에서 룰북 편향('실제 파티는 넷이다') 제거, CLI 캐릭터 만들기 미지원의 근거를 코드에 명시"
affects: [13, 13.1, 16]

actuals:
  tokens: 23270
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "세션 스코프 조회 헬퍼(_created_character)를 여러 라우트 호출부가 공유해, '한 곳만 고치는' Pitfall 3류 사고를 구조적으로 막는다"
    - "시험이 앱의 실행 스레드 sqlite 연결을 건드리지 않고 같은 db 파일에 독립 EventStore 연결을 열어 사건을 직접 심는 패턴(app.state.db_path)"
    - "제품 코드에 없는 지름길을 tests/conftest.py 공용 헬퍼로만 존재하게 해 CHAR-02(완성품 즉시 집기 금지)와 시험 편의를 동시에 지킨다"

key-files:
  created:
    - tests/fixtures/characters.py
  modified:
    - src/gptrpg/web/routes_characters.py
    - src/gptrpg/web/routes_actions.py
    - src/gptrpg/web/portraits.py
    - src/gptrpg/web/app.py
    - src/gptrpg/web/routes_creation.py
    - src/gptrpg/rules_core/reducer.py
    - src/gptrpg/session_actor/actor.py
    - src/gptrpg/turn/context.py
    - src/gptrpg/rulebooks/__init__.py
    - src/gptrpg/rulebooks/openquest.py
    - src/gptrpg/rulebooks/dungeonworld_like.py
    - src/gptrpg/agents/context.py
    - src/gptrpg/cli/turn_flow.py
    - tests/conftest.py
    - tests/test_web_characters.py
    - tests/test_web_actions.py
    - tests/test_imagery.py
    - tests/test_rulebook.py
    - tests/test_identity_tracer.py
    - tests/test_creation_tracer.py
    - tests/test_agent_context_caps.py
  deleted:
    - src/gptrpg/web/characters_data.py

key-decisions:
  - "Task 1·2를 한 커밋으로 냈다(계획이 명시적으로 지시) — 파일을 지우면서 세 호출부를 같은 커밋에 안 넣으면 그 사이 커밋이 import 에러다."
  - "GameState에 one_line_intro를 담는 새 칸을 추가하지 않았다 — get_characters()의 archetype 요구를 위해 GameState를 확장하는 대신, 이미 store.read_events()로 읽은 이벤트 목록을 한 번 더 훑어 character_created의 one_line_intro를 캐릭터별 마지막 값으로 뽑는다. created_characters 자체가 같은 방식(딕셔너리 갱신 = 나중 값이 이긴다)으로 최신 stats를 담는 것과 같은 규칙이라 파생 칸을 새로 만들 필요가 없었다."
  - "routes_actions.py의 직접 get_character() 호출부는 계획의 '정확히 세 곳' 조사가 놓친 네 곳(declare/confirm/confirm_resource_change/proceed)이 더 있었다 — 전부 새 _created_character() 헬퍼로 통일했다(아래 Deviations 참조)."
  - "_CHARACTER_NAMES 모듈 상수(임포트 시점에 한 번 계산되는 정적 딕셔너리)를 _character_names(party) 함수로 바꿨다 — '파티'의 정의가 세션마다 다른 값이 된 이상 이름 사전도 요청마다 다시 만들어야 한다."
  - "시험이 앱의 EventStore 연결을 재사용하지 않고 app.state.db_path로 별도 연결을 연다 — sqlite3 연결은 만든 스레드에서만 쓸 수 있어, TestClient의 lifespan 스레드에서 만들어진 연결을 시험 스레드에서 직접 쓰면 'SQLite objects created in a thread can only be used in that same thread' 예외가 난다."
  - "두 시험(named_slots 소지품 판정)의 monkeypatch(routes_actions_module.get_character 갈아 끼우기)를 걷어내고 seed_character_created(entity=...)로 세션에 실제 캐릭터를 심는 방식으로 바꿨다 — 새 아키텍처에서 _current_party_state와 declare()의 캐릭터 조회가 서로 다른 이름(_current_party_state는 GameState를 직접 읽고, declare()는 _created_character를 부른다)을 쓰므로 하나만 monkeypatch하면 나머지가 실제 fixture 값을 보는 어긋남이 생긴다. 실제로 사건을 심는 방식은 이 어긋남 자체가 구조적으로 불가능하다."
  - "test_occupancy_old_session_rejects_select_but_polling_still_succeeds의 기대값을 409→400으로 고쳤다 — 이 시험이 재현하는 '옛 세션'(만들기 사건이 아예 없던 시절의 기록)은 정의상 created_characters가 비어 있으므로, select-character의 첫 관문(CHAR-02: 이 세션에 실제로 있는 캐릭터인가)이 옛 세션 판별(_prepare_occupy, D-14)보다 먼저 걸린다. '다시보기는 되지만 새로 캐릭터를 잡을 수 없다'는 결론 자체는 그대로다 — _prepare_occupy의 옛 세션 판별 로직은 tests/test_session_actor.py가 여전히 직접 검사한다."

patterns-established:
  - "세션 스코프 캐릭터 조회를 여러 호출부가 쓸 때는 공용 헬퍼(_created_character) 하나로 모으고, 그 헬퍼가 사건을 다시 읽어 만드는 상태 재구성 비용을 감수한다 — 이 저장소가 이미 요청마다 여러 번 독립적으로 rebuild_state_from_events를 부르는 것을 용인하는 기존 패턴과 같은 결이다."

requirements-completed: [CHAR-02, CHAR-04]

coverage:
  - id: D1
    description: "완성된 캐릭터를 즉시 집어드는 경로가 제품 코드에 없다 — 만들기가 없는 세션의 캐릭터 목록이 빈 목록이고, 알려지지 않은 character_id로 select-character를 부르면 400이다(CHAR-02)"
    requirement: "CHAR-02"
    verification:
      - kind: unit
        ref: "tests/test_web_characters.py#test_character_list_is_empty_when_nobody_has_created_a_character"
        status: pass
      - kind: unit
        ref: "tests/test_web_characters.py#test_select_unknown_character_returns_400_and_sets_no_cookie"
        status: pass
      - kind: e2e
        ref: "tests/test_creation_tracer.py (만들기 완료 → 자동 점유 전체 흐름, characters_data 없이 통과)"
        status: pass
    human_judgment: false
  - id: D2
    description: "캐릭터 목록·시트·판정 컨텍스트·초상화 네 소비처가 전부 이 세션에서 만들어진 캐릭터를 본다 — 한 곳이라도 정적 상수를 보면 서로 다른 파티를 보는 문제(Pitfall 3)가 없다"
    requirement: "CHAR-04"
    verification:
      - kind: unit
        ref: "tests/test_web_characters.py#test_character_list_contains_only_characters_created_in_this_session"
        status: pass
      - kind: unit
        ref: "tests/test_web_actions.py#test_party_state_axis_value_matches_character_sheet_response"
        status: pass
      - kind: unit
        ref: "tests/test_imagery.py#test_character_list_portrait_url_points_at_the_written_file"
        status: pass
      - kind: other
        ref: "grep -rn 'characters_data' src/ (0줄)"
        status: pass
    human_judgment: false
  - id: D3
    description: "브람·나리·선·호두 넷이 제품에서 사라지고 시험 재료(tests/fixtures/characters.py)에서 D-49 값 그대로 산다"
    verification:
      - kind: unit
        ref: "tests/test_web_characters.py#test_bram_and_nari_stats_are_pinned_by_d49"
        status: pass
      - kind: other
        ref: "test ! -f src/gptrpg/web/characters_data.py && test -f tests/fixtures/characters.py"
        status: pass
    human_judgment: false
  - id: D4
    description: "PARTY_MEMBER_LIMIT이 인원의 출처가 아니라 절대 안전 밸브임이 도크스트링에서 드러나고, '실제 파티는 넷이다' 룰북 편향 문구가 사라졌다(D-01)"
    verification:
      - kind: unit
        ref: "tests/test_agent_context_caps.py#test_context_module_source_does_not_claim_the_real_party_is_four"
        status: pass
    human_judgment: false
  - id: D5
    description: "명령줄에 캐릭터 만들기를 넣지 않는 근거가 코드에 적혀 있다 — 웹·CLI를 같은 커밋에서 닫는 관례의 명시적 예외로 문서화됐다"
    verification:
      - kind: other
        ref: "grep -n '명시적 예외' src/gptrpg/cli/turn_flow.py"
        status: pass
    human_judgment: false
  - id: D6
    description: "동적 캐릭터에 초상화 자동 생성 경로가 없다는 것이 코드에 알려진 한계로 적혀 있다 — 이번 계획은 PLAYER_CHARACTERS 결합만 끊는다(결정 ④)"
    verification:
      - kind: unit
        ref: "tests/test_imagery.py (portraits 관련 전체 — 결합 해제 후에도 그림 층이 그대로 동작)"
        status: pass
    human_judgment: true
    rationale: "'초상화 없이도 화면이 안 깨진다'는 자동으로 증명되지만, 다음 단계가 이 한계를 실제로 어떻게 풀지(자유 서술→영어 프롬프트 변환)는 사람이 판단할 설계 결정이라 여기서 자동 검증할 대상이 아니다."

duration: "약 2시간 (단일 연속 세션)"
completed: 2026-08-19
status: complete
---

# Phase 12.1 Plan 05: 정적 캐릭터 넷 제거 Summary

**`web/characters_data.py`(브람·나리·선·호두 네 상수)를 삭제하고 캐릭터 목록·시트·선택·판정 컨텍스트·초상화 전부를 `GameState.created_characters`(이 세션에서 실제로 만들어진 캐릭터) 기준으로 전환해, 완성품을 30초 만에 집어드는 마지막 경로(CHAR-02)를 제품 코드에서 없앴다.**

## Performance

- **Duration:** 약 2시간(단일 연속 세션, 정밀 측정 안 됨)
- **Tasks:** 3 (Task 1: 정적 넷을 시험 재료로 이전, Task 2: 세 호출부 전환, Task 3: PARTY_MEMBER_LIMIT 주석·CLI 예외 근거)
- **Files modified:** 22 (신설 1 · 수정 20 · 삭제 1)

## Accomplishments

- `src/gptrpg/web/characters_data.py`를 삭제하고 `tests/fixtures/characters.py`로 값 그대로(D-49) 옮겼다 — 다섯 시험 파일(`test_web_characters.py`·`test_web_actions.py`·`test_imagery.py`·`test_rulebook.py`, 그리고 실제로는 `tests.fixtures.characters`를 가리키게 바뀐 conftest 공용 헬퍼)이 새 경로에서 import한다
- `routes_characters.py`의 `get_characters`/`get_character_sheet`/`select_character`/`my_character` 네 자리가 전부 세션에서 만들어진 캐릭터만 본다 — 목록의 `archetype`은 `character_created` 사건의 `one_line_intro`에서 온다(CHAR-03 재사용, `Entity`에 칸을 안 늘린다)
- `routes_actions.py`의 `_current_party_state`가 「이 세션의 명단」으로 정의를 바꿨다 — 명단이 잠기면 `party_roster` 순서, 안 잠기면 완성 순서. 새 `_created_character()`/`_character_names()` 헬퍼로 declare/confirm/confirm-resource-change/proceed 네 호출부까지 정리해, 계획의 조사가 놓쳤던 네 곳의 직접 `get_character()` 호출까지 전부 세션 기준으로 통일했다
- `portraits.py`의 `PLAYER_CHARACTERS` 결합을 끊고, 동적 캐릭터에 초상화가 없다는 것을 알려진 한계로 도크스트링에 적었다(결정 ④, 이번 계획 범위 밖)
- `PARTY_MEMBER_LIMIT`(`agents/context.py`) 도크스트링에서 "실제 파티는 넷이다" 문구를 지우고 인원의 실제 출처(`Rulebook.party_size_range` + 방장 확정, D-01)를 적었다 — 상수·검사 로직은 그대로다
- `cli/turn_flow.py`에 캐릭터 만들기를 명령줄에 넣지 않는 근거를 「웹·CLI를 같은 커밋에서 닫는다」관례의 명시적 예외로 적었다
- `web/app.py`에 `app.state.db_path`를 추가해, 시험 코드가 앱의 sqlite 연결(스레드 종속)을 건드리지 않고 같은 파일에 독립 연결을 열 수 있게 했다 — 이것이 세션 캐릭터를 시험에서 미리 심는 모든 방법의 토대다
- `tests/conftest.py`에 `seed_character_created()`/`select_character()` 공용 헬퍼를 두어, 정적 캐릭터가 사라진 뒤에도 "bram"/"nari" 같은 시험 캐릭터를 세션에 실제로 만들고 나서 고르게 했다 — 제품 코드에는 이 지름길이 없다(CHAR-02)

## Task Commits

1. **Task 1+2: 정적 넷을 시험 재료로 옮기고 세 호출부를 세션 기준으로 전환한다** — `04731fa` (feat)
2. **Task 3: PARTY_MEMBER_LIMIT 룰북 편향 제거와 CLI 예외 근거 기록** — `f8eb185` (docs)

계획이 Task 1·2를 명시적으로 한 커밋으로 지시했다(파일 삭제와 호출부 전환이 갈리면 그 사이 커밋이 import 에러가 되기 때문) — 위 표기가 두 태스크를 한 해시로 묶은 이유다.

**Plan metadata:** (이 커밋 — 아래 참조)

## Files Created/Modified

- `tests/fixtures/characters.py` (신설) — 브람·나리·선·호두 넷과 `list_characters`/`get_character`/`CharacterSummary`를 값 그대로 옮긴 시험 재료
- `src/gptrpg/web/characters_data.py` (삭제)
- `src/gptrpg/web/routes_characters.py` — 네 경로를 세션 기반 조회로 전환
- `src/gptrpg/web/routes_actions.py` — `_current_party_state` 재정의, `_created_character`/`_character_names` 신설, 다섯 호출부 통일
- `src/gptrpg/web/portraits.py` — `PLAYER_CHARACTERS` 결합 해제, 알려진 한계 문서화
- `src/gptrpg/web/app.py` — `app.state.db_path` 신설
- `src/gptrpg/web/routes_creation.py` — 도크스트링의 낡은 `characters_data` 참조 정정
- `src/gptrpg/rules_core/reducer.py` · `src/gptrpg/session_actor/actor.py` · `src/gptrpg/turn/context.py` · `src/gptrpg/rulebooks/__init__.py` · `src/gptrpg/rulebooks/openquest.py` · `src/gptrpg/rulebooks/dungeonworld_like.py` — 도크스트링에 남은 `characters_data` 문자열 정정(제품 코드 동작 변화 없음)
- `src/gptrpg/agents/context.py` — `PARTY_MEMBER_LIMIT` 도크스트링 재작성
- `src/gptrpg/cli/turn_flow.py` — CLI 예외 근거 추가, 낡은 `characters_data` 참조 정정
- `tests/conftest.py` — `seed_character_created`/`select_character` 신설·확장
- `tests/test_web_characters.py` — import 전환 + 캐릭터 시딩 삽입(22곳) + 목록 시험 2건 신설(빈 목록/생성 순서)
- `tests/test_web_actions.py` — import 전환 + monkeypatch 두 건을 실제 사건 시딩으로 교체 + `_created_character` 사용처 정리
- `tests/test_imagery.py` — import 전환 + `_run_one_turn`/목록 시험 캐릭터 시딩
- `tests/test_rulebook.py` — import 전환만
- `tests/test_identity_tracer.py` — `_select_character`/`_select_character_async` 헬퍼가 세션 캐릭터를 먼저 심도록 확장
- `tests/test_creation_tracer.py` — 낡은 "알려진 범위 경계" 도크스트링 정정
- `tests/test_agent_context_caps.py` — 소스 문자열 회귀 시험 신설

## Decisions Made

frontmatter의 `key-decisions`를 참조. 요약하면: Task 1·2를 한 커밋으로 냈고(계획 지시), `GameState`에 새 칸을 안 늘리고 사건을 다시 훑어 `one_line_intro`를 뽑았으며, `_CHARACTER_NAMES` 모듈 상수를 요청마다 다시 만드는 함수로 바꿨고, 시험은 앱의 sqlite 연결을 재사용하지 않고 `app.state.db_path`로 별도 연결을 연다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `routes_actions.py`에 계획의 조사가 놓친 직접 `get_character()` 호출이 네 곳 더 있었다**
- **Found during:** Task 2 구현 중 — `characters_data.py` 삭제 전 `grep -n 'get_character(' src/gptrpg/web/routes_actions.py`를 돌려 확인
- **Issue:** 계획서는 "`_current_party_state`가 정확히 세 호출부 중 하나"라고 적었지만, 실제로는 `declare()`·`confirm()`·`confirm_resource_change()`·`proceed()` 네 핸들러가 각자 독립적으로 `get_character(...)`를 불러 캐릭터 존재·축 검사를 했다. `characters_data.py`를 지우면 이 네 곳이 즉시 `NameError`로 죽는다.
- **Fix:** `_created_character(store, session_id, character_id)` 공용 헬퍼를 신설해 다섯 호출부(위 넷 + `_current_party_state`가 내부에서 쓰는 형태) 전부가 `GameState.created_characters`를 같은 방식으로 조회하게 통일했다. 모듈 상수 `_CHARACTER_NAMES`도 같은 이유로 `_character_names(party)` 함수로 바꿨다.
- **Files modified:** `src/gptrpg/web/routes_actions.py`
- **Verification:** `.venv/bin/python -m pytest -q tests/test_web_actions.py` 전체 통과, `grep -c 'list_characters' src/gptrpg/web/routes_actions.py` == 0
- **Committed in:** `04731fa`

**2. [Rule 3 - Blocking] `client.app.state.store`를 시험 스레드에서 직접 쓰면 sqlite3 스레드 제약 예외가 난다**
- **Found during:** Task 2 시험 확장 중 — `select_character()` 헬퍼가 세션에 캐릭터를 미리 심으려고 `client.app.state.store.read_events(...)`를 부르자 `sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread`가 발생
- **Issue:** `EventStore`의 연결은 `TestClient`의 lifespan이 여는 별도 스레드에서 만들어지는데, 시험 헬퍼는 시험 자체의 스레드에서 그 연결을 재사용하려 했다.
- **Fix:** `web/app.py`에 `app.state.db_path`를 추가해 db 파일 경로를 노출하고, `tests/conftest.py`의 `seed_character_created()`가 그 경로로 **별도의** `EventStore` 연결을 열어 시험 스레드 안에서만 쓰도록 고쳤다.
- **Files modified:** `src/gptrpg/web/app.py`, `tests/conftest.py`
- **Verification:** 스레드 예외 없이 `tests/test_web_actions.py`/`tests/test_web_characters.py` 전체 통과
- **Committed in:** `04731fa`

**3. [Rule 3 - Blocking] 정적 캐릭터가 사라지자 `conftest.select_character()`에 의존하던 시험 네 파일이 전부 400으로 깨졌다**
- **Found during:** Task 2 완료 뒤 전체 시험 실행(`89 failed`) — `characters_data.py` 소비처로 계획이 지목한 다섯 파일 밖에서도 실패가 났다
- **Issue:** 계획의 "다섯 시험 소비처" 목록은 `characters_data` **직접 import**만 추적했지, `tests/conftest.py`의 공용 `select_character()` 헬퍼를 거쳐 간접적으로 정적 캐릭터에 의존하던 `test_clock_condition_web.py`·`test_identity_tracer.py`·`test_safety_flag_pipeline.py` 세 파일은 놓쳤다(그중 `test_clock_condition_web.py`·`test_safety_flag_pipeline.py`는 헬퍼 자체를 고치자 자동으로 통과해 별도 수정이 필요 없었고, `test_identity_tracer.py`만 파일 로컬 `_select_character`/`_select_character_async`를 따로 갖고 있어 직접 고쳐야 했다).
- **Fix:** `seed_character_created()`를 conftest에 추가하고 `select_character()`가 그것부터 부르도록 고쳤다. `test_identity_tracer.py`의 파일 로컬 헬퍼 둘도 같은 방식으로 확장했다.
- **Files modified:** `tests/conftest.py`, `tests/test_identity_tracer.py`
- **Verification:** `.venv/bin/python -m pytest -q` 전체(1285건) 통과
- **Committed in:** `04731fa`

**4. [Rule 3 - Blocking] `routes_creation.py`·`rulebooks/dungeonworld_like.py`에 계획이 나열한 "다섯" 위치 밖의 `characters_data` 문자열이 더 있었다**
- **Found during:** Task 2 종료 직전 `grep -rn 'characters_data' src/` 최종 확인
- **Issue:** 계획 Task 1 ④가 정확화 대상으로 나열한 일곱 자리(`session_actor/actor.py:145,1043` 등)에 `routes_creation.py:13,27`과 `rulebooks/dungeonworld_like.py:127,157`이 빠져 있었다.
- **Fix:** 두 파일의 도크스트링을 문맥에 맞게 다시 썼다(`routes_creation.py`는 "12.1-05가 이미 옮겼다"는 현재형으로, `dungeonworld_like.py`는 과거형 참조로).
- **Files modified:** `src/gptrpg/web/routes_creation.py`, `src/gptrpg/rulebooks/dungeonworld_like.py`
- **Verification:** `grep -rn 'characters_data' src/` 결과 0줄
- **Committed in:** `04731fa`

**5. [Rule 2 - Missing Critical] `GET /characters`의 `archetype`이 `GameState`에 저장되지 않는 값(`one_line_intro`)을 요구했다**
- **Found during:** Task 2 구현 중 — 계획 텍스트가 "`archetype`은 `CharacterCreated.one_line_intro`에서 온다"고 지시했지만, `reducer.py`의 `character_created` 분기가 `one_line_intro`를 `GameState.created_characters`에 담긴 `Entity`에 싣지 않는다는 것을 발견(`Entity`에는 그 칸이 없다 — CHAR-04가 칸을 못 늘리게 한다)
- **Issue:** `GameState`에 새 파생 칸을 추가하지 않으면 목록 응답의 `archetype`을 채울 방법이 없었다
- **Fix:** `GameState`를 확장하는 대신, `get_characters()`가 이미 읽어 둔 원본 사건 목록을 한 번 더 훑어 `character_created` 이벤트의 `one_line_intro`를 캐릭터별 마지막 값으로 뽑았다 — `created_characters` 자체가 같은 "딕셔너리 갱신 = 나중 값이 이긴다" 규칙으로 최신 stats를 담는 것과 같은 방식이라 새 개념이 아니다.
- **Files modified:** `src/gptrpg/web/routes_characters.py`
- **Verification:** 실제 HTTP 창조 흐름(`party-size→step×4→complete`)을 손으로 돌려 `GET /characters`의 `archetype`이 `one_line_intro`와 일치함을 확인, `tests/test_web_characters.py::test_character_list_contains_only_characters_created_in_this_session`으로 고정
- **Committed in:** `04731fa`

**6. [Rule 1 - Bug] 두 named_slots 소지품 시험이 monkeypatch 방식으로는 통과할 수 없게 됐다**
- **Found during:** Task 2 시험 확장 — `test_declare_item_not_held_opens_retro_declaration_when_rulebook_allows`/`test_declare_item_held_and_actually_present_is_not_downgraded`가 `routes_actions_module.get_character`(존재하지 않는 이름)를 갈아 끼우려다 `AttributeError`, 이름을 `_created_character`로 바꿔도 여전히 `item_use.kind`가 `"none"`으로 잘못 나왔다
- **Issue:** 예전에는 `_current_party_state`와 `declare()`의 캐릭터 조회가 같은 이름(`get_character`)을 공유해 monkeypatch 하나로 둘 다 바뀌었다. 새 구조에서 `_current_party_state`는 `GameState.created_characters`를 직접 읽고 `declare()`는 별도로 `_created_character()`를 부르므로, 하나만 monkeypatch하면 다른 하나가 실제(fixture) 값을 보는 어긋남이 생겼다 — `actor_stats(ctx)`가 실제 fixture "bram"(던전월드류, named_slots 없음)을 봐서 소지품 판단 경로 자체가 안 열렸다.
- **Fix:** 두 시험을 monkeypatch 대신 `seed_character_created(entity=...)`로 이 세션에 실제 Cairn류 named_slots 캐릭터를 심는 방식으로 다시 썼다 — 실제 사건을 심으면 두 조회 경로가 구조적으로 같은 값을 보므로 이 어긋남이 아예 생길 수 없다.
- **Files modified:** `tests/test_web_actions.py`
- **Verification:** 두 시험 및 `tests/test_web_actions.py` 전체 통과
- **Committed in:** `04731fa`

**7. [Rule 1 - Bug] "옛 세션" 점유 거절 시험의 기대 상태 코드가 새 관문 순서와 어긋났다**
- **Found during:** Task 2 시험 확장 — `test_occupancy_old_session_rejects_select_but_polling_still_succeeds`가 409를 기대했으나 실제로는 400
- **Issue:** 이 시험이 재현하는 "옛 세션"(만들기 사건이 존재하지 않던 시절의 기록)은 정의상 `created_characters`가 비어 있다. `select-character`의 새 첫 관문("이 세션에 실제로 있는 캐릭터인가", CHAR-02)이 옛 세션 판별(`_prepare_occupy`, D-14)보다 먼저 걸리므로 400이 먼저 난다.
- **Fix:** 기대 코드를 400으로 고치고, "다시보기는 되지만 새로 캐릭터를 잡을 수 없다"는 결론 자체는 그대로라는 것과 `_prepare_occupy`의 옛 세션 판별 자체는 `tests/test_session_actor.py`가 여전히 직접 검사한다는 것을 도크스트링에 적었다.
- **Files modified:** `tests/test_web_characters.py`
- **Verification:** 해당 시험 통과
- **Committed in:** `04731fa`

**8. [Rule 2 - Missing Critical] `test_character_list_contains_all_hand_authored_characters`가 CHAR-02와 정면으로 어긋났다**
- **Found during:** Task 2 시험 확장 — 이 시험은 "목록이 손으로 쓴 캐릭터 전부를 돌려준다"를 단언했는데, 이것이 정확히 D22/D-12가 폐기하라고 한 경로다
- **Issue:** 이 시험을 그대로 두면(고쳐서 통과시키든 방치하든) CHAR-02가 요구하는 "완성된 캐릭터를 즉시 집어드는 경로가 없다"를 시험 스위트 자신이 반증하는 모순이 생긴다
- **Fix:** 계획의 behavior 명세(빈 세션은 빈 목록, 만들어진 캐릭터는 완성 순서로)를 그대로 옮긴 두 시험(`test_character_list_is_empty_when_nobody_has_created_a_character`, `test_character_list_contains_only_characters_created_in_this_session`)으로 교체했다.
- **Files modified:** `tests/test_web_characters.py`
- **Verification:** 두 신설 시험 통과
- **Committed in:** `04731fa`

---

**Total deviations:** 8 auto-fixed (5 Rule 3 - blocking, 2 Rule 1 - bug, 1 Rule 2 - missing critical)
**Impact on plan:** 전부 계획 자신의 변경(정적 상수 삭제·세션 기반 전환)이 낳은 필연적 결과를 즉시 고친 것이거나, 계획의 조사가 놓친 호출부·문자열 자리를 마저 닫은 것이다. 스코프 확장이 아니다 — 계획의 목표(CHAR-02/D-12)를 실제로 성립시키는 데 필요한 최소 작업이었다.

## Known Stubs

- **동적으로 만들어진 캐릭터에 초상화가 없다** — 계획의 결정 ④가 이번 범위 밖으로 명시적으로 미룬 것이다. `web/portraits.py` 모듈 도크스트링에 알려진 한계로 적혀 있고, 화면은 이미 초상화 없는 캐릭터를 견딘다(`_portrait_url_if_present`가 `None`을 돌려준다). 새 스텁이 아니라 계획이 처음부터 의도한 경계다.
- **명령줄(CLI)에서 캐릭터를 만들 수 없다** — 계획의 결정 ③이 명시적 예외로 판단한 것이다. `cli/turn_flow.py`에 근거가 적혀 있다. 계정/신원이 생기는 다음 마일스톤 몫이다.

## Threat Flags

없음 — 이 계획의 `<threat_model>`이 이미 여덟 위협(T-12.1-32~38, T-12.1-SC)을 등록했고, 이번 구현이 그 표를 벗어나는 새 표면(신규 네트워크 경로·인증 경로·파일 접근·스키마 경계)을 만들지 않았다. `app.state.db_path` 신설은 시험 전용 이음매이고 프로덕션 요청 처리 코드는 이 값을 읽지 않는다(도크스트링에 명시) — 새 공격 표면이 아니다.

## Issues Encountered

- **`.venv/bin/python -m lint_imports`가 아니라 `.venv/bin/lint-imports`가 실제 실행 명령이었다** — 계획의 `<verification>`이 적은 커맨드가 이 환경의 설치 방식과 살짝 달랐다(`python -m importlinter`도 동작한다). 계약 자체(`rulebooks`가 `web`/`tests`를 모른다)는 `lint-imports`로 확인했고 통과했다.
- **`.gptrpg/events.db`/`uat9.db` 로컬 스모크는 `.venv/bin/python -m pytest -q`의 일부로 이미 돈다** — 별도 명령 없이 전체 스위트 실행에 포함되어 있었고(`skipif`가 파일 존재 여부로 자동 판단), 30건 전부 통과해 판 9 코드가 옛 기록을 예외 없이 접는다는 것을 재확인했다.

## User Setup Required

None - 외부 서비스 설정 불필요.

## Next Phase Readiness

- **Phase 13(오프닝 장면)**이 시작할 수 있다: 캐릭터 만들기 전체 흐름(인원 확정 → 자기소개 → GM 정리 → 동의 → 명단 잠금 → 자동 점유)이 이제 정적 상수 없이 세션 데이터만으로 완결된다.
- **Phase 16(화면)**이 시작할 수 있다: `CharacterSelect.tsx`가 이제 실제로 빈 목록을 받을 수 있으므로("아무도 안 만들었다" 상태), 그 화면과 "캐릭터 변경하기" 버튼(접힌 할 일, D-08이 이미 답을 정했다 — 숨기거나 비활성화)을 손볼 차례다.
- **동적 캐릭터 초상화**(자유 서술 → 영어 프롬프트 변환)와 **명령줄 캐릭터 만들기**(계정/신원 체계) 둘 다 이 계획이 근거와 함께 다음 마일스톤으로 미뤄 뒀다.
- 블로커 없음. `EVENT_SCHEMA_VERSION`은 9 그대로다(이 계획이 판을 안 올렸다).

---
*Phase: 12.1-character-creation*
*Completed: 2026-08-19*

## Self-Check: PASSED

All files created/modified in this plan exist on disk
(`tests/fixtures/characters.py`, `src/gptrpg/web/routes_characters.py`,
`src/gptrpg/web/routes_actions.py`, `src/gptrpg/web/portraits.py`,
`src/gptrpg/web/app.py`, `src/gptrpg/agents/context.py`,
`src/gptrpg/cli/turn_flow.py`, `tests/conftest.py`,
`.planning/phases/12.1-character-creation/12.1-05-SUMMARY.md`),
`src/gptrpg/web/characters_data.py` no longer exists, and both task
commits (`04731fa`, `f8eb185`) are present in `git log --all`.
