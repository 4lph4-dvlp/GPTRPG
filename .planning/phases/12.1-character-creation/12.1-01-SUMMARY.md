---
phase: 12.1-character-creation
plan: 01
subsystem: rules-engine
tags: [event-sourcing, pydantic, fastapi, character-creation, dungeonworld]

# Dependency graph
requires:
  - phase: 12-stats-resources-inventory
    provides: "StatEntry 8칸 그릇, ResourceAxisDecl, `event_log/schema.py` 판 8"
  - phase: 08-identity-and-idempotency
    provides: "OccupyCharacter/character_occupied 점유 장치, 옛 세션 판별"
provides:
  - "룰북이 데이터로 선언하는 만들기 단계 형식(CreationStepDecl, 세 kind 검증)"
  - "사건 판 9 — 캐릭터 만들기 다섯 사건(party_size_fixed/creation_step_completed/creation_interjection/character_created/party_roster_locked)"
  - "GameState의 만들기 파생 칸 넷(party_size_fixed/creation_step_values/created_characters/party_roster)"
  - "세션 액터 명령 넷(FixPartySize/CompleteCreationStep/CreateCharacter/LockPartyRoster) + RosterAlreadyLocked"
  - "web/routes_creation.py — 만들기 HTTP 라우터 넷"
  - "_prepare_occupy의 판 9 대응 옛 세션 판별 정정"
affects: [12.1-02, 12.1-03, 12.1-04, 12.1-05, 12.1-06]

actuals:
  tokens: 21105
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "만들기 항목 선언(CreationStepDecl)이 ResourceAxisDecl과 같은 '이름은 룰북, 형태는 플랫폼' 분업을 따른다"
    - "사건 판 올리기(schema.py)와 리듀서 신설 분기(reducer.py)를 같은 커밋에 묶는 저장소 규율을 다섯 번째로 지켰다"
    - "GM 대화가 없는 계층에서는 완료 사건 제출 직후 같은 요청 안에서 기존 OccupyCharacter를 재사용해 자동 점유를 구현한다"

key-files:
  created:
    - src/gptrpg/web/routes_creation.py
    - tests/test_creation_tracer.py
  modified:
    - src/gptrpg/rules_core/rulebook.py
    - src/gptrpg/rules_core/reducer.py
    - src/gptrpg/event_log/schema.py
    - src/gptrpg/session_actor/actor.py
    - src/gptrpg/web/app.py
    - src/gptrpg/rulebooks/dungeonworld_like.py
    - tests/test_event_schema_migration.py
    - tests/test_session_actor.py
    - tests/fixtures/session1_expected_state.json

key-decisions:
  - "Task 1 체크포인트(option-a, 사용자 승인): 사건 판 8→9를 이번 한 번에 올리고 다섯 종류(끼어들기 포함)를 한 커밋에 함께 연다 — 명단 잠금에 짝이 되는 '푸는 사건'을 만들지 않는다(D-08)"
  - "PartySizeFixed.rulebook_min/rulebook_max는 12.1-02가 Rulebook.party_size_range를 붙이기 전까지 1/None(검증 안 함) 자리표시자로 적는다 — '검증하지 않았다'와 '범위가 무제한이다'가 사건에서는 구분되지 않으므로 12.1-02가 실제 선언을 붙인 뒤 이 값을 실제 범위로 바꿔야 한다"
  - "/creation/step·/creation/complete의 신원 대조는 '쿠키가 있는데 다른 캐릭터'만 403으로 막는다 — 첫 만들기 참가자는 아직 쿠키가 없으므로 이 검사를 통과해야 하고, 그렇지 않으면 첫 캐릭터를 아무도 못 만든다(select_character가 첫 선택에서 쿠키 없이 통과하는 것과 같은 결)"

patterns-established:
  - "만들기 항목 값 확정(creation_step_completed)이 (character_id, step_id) 키로 덮어써지고, 이전 값의 순번은 새 사건의 superseded_seq에 실린다 — 사건 기록 자체는 지워지지 않는다(D-07)"

requirements-completed: [CHAR-01, CHAR-04, CHAR-05]

coverage:
  - id: D1
    description: "룰북이 데이터로 선언한 만들기 단계 목록(name/free_text, ability_array/place_fixed_values)이 실제로 실행되어 캐릭터 한 명이 나온다 — 플랫폼 코드에 룰북 항목 이름·값이 상수로 박혀 있지 않다"
    requirement: "CHAR-01"
    verification:
      - kind: e2e
        ref: "tests/test_creation_tracer.py#test_creation_end_to_end_through_lock_and_rejection_after_lock"
        status: pass
    human_judgment: false
  - id: D2
    description: "만들어진 캐릭터가 Entity/StatEntry 그릇에 그대로 들어가고 STAT_ENTRY_FIELD_NAMES(8)/ENTITY_FIELD_NAMES(4) 고정 상수가 안 바뀐다"
    requirement: "CHAR-04"
    verification:
      - kind: unit
        ref: "tests/test_entities.py#test_stat_entry_field_names_are_exactly_eight"
        status: pass
      - kind: unit
        ref: "tests/test_event_schema_migration.py#test_creation_stat_entry_record_field_count_matches_stat_entry"
        status: pass
    human_judgment: false
  - id: D3
    description: "만들기를 마친 사람이 그 캐릭터를 기존 Phase 8 OccupyCharacter 경로로 자동 점유한다"
    requirement: "CHAR-05"
    verification:
      - kind: e2e
        ref: "tests/test_creation_tracer.py#test_creation_end_to_end_through_lock_and_rejection_after_lock"
        status: pass
    human_judgment: false
  - id: D4
    description: "만들기 완료 사건이 점유보다 먼저 기록돼도 자동 점유가 '옛 세션'으로 거절되지 않는다(_prepare_occupy 정정) — 반대로 만들기 없이 다른 사건만 있는 진짜 옛 세션은 여전히 거절된다"
    verification:
      - kind: unit
        ref: "tests/test_session_actor.py#test_occupy_succeeds_in_a_new_session_with_only_creation_events_and_no_occupation"
        status: pass
      - kind: unit
        ref: "tests/test_session_actor.py#test_occupy_still_rejected_in_old_session_with_unrelated_events_and_no_creation"
        status: pass
    human_judgment: false
  - id: D5
    description: "명단이 잠기고, 잠긴 뒤 새 캐릭터 만들기 시도(step/complete)가 거절된다(D-08) — 빈 명단 잠금도 거절된다"
    requirement: "CHAR-04"
    verification:
      - kind: e2e
        ref: "tests/test_creation_tracer.py#test_creation_end_to_end_through_lock_and_rejection_after_lock"
        status: pass
      - kind: e2e
        ref: "tests/test_creation_tracer.py#test_lock_roster_with_empty_character_ids_is_rejected"
        status: pass
    human_judgment: false
  - id: D6
    description: "사건 판 8→9 diff(event_log/schema.py)와 리듀서 다섯 신설 분기 diff(rules_core/reducer.py)가 같은 커밋에 있고, 판 8 이하 기록이 예외 없이 접힌다"
    verification:
      - kind: unit
        ref: "tests/test_event_schema_migration.py#test_committed_session1_fixture_folds_to_expected_state"
        status: pass
      - kind: unit
        ref: "tests/test_event_schema_migration.py#test_pre_phase_12_1_records_with_no_creation_events_fold_under_schema_9"
        status: pass
    human_judgment: false

duration: "not tracked precisely (checkpoint-resumed session)"
completed: 2026-08-19
status: complete
---

# Phase 12.1 Plan 01: 캐릭터 만들기 트레이서 Summary

**룰북이 선언한 두 만들기 단계(자유 서술 이름 + 정해진 숫자 배치 능력치)가 HTTP 한 경로로 캐릭터 하나를 만들고, 만든 사람이 기존 Phase 8 점유 장치로 자동 점유하고, 파티 명단이 잠기는 사건 판 9 트레이서.**

## Performance

- **Duration:** 정확히 측정 안 됨(사용자 확인 대기 뒤 이어받은 세션 — Task 1 체크포인트 승인 이후 Task 2·3 실행분만)
- **Tasks:** 3 (Task 1: 결정 체크포인트, Task 2: 트레이서 구현, Task 3: 하위 호환·왕복 시험)
- **Files modified:** 11

## Accomplishments

- `rules_core/rulebook.py`에 `CreationStepDecl`/`CreationStepKind`/`InvalidCreationStep`을 세우고, 이 계획 범위인 세 kind(`free_text`·`place_fixed_values`·`pick_one`)만 칸 조합을 완전히 검증했다(나머지 넷은 12.1-02가 채운다는 것을 도크스트링에 명시)
- `dungeonworld_like.py`에 실제 만들기 두 단계(이름·능력치 배치)를 선언해 형식이 데이터로 실행됨을 증명했다
- `EVENT_SCHEMA_VERSION` 8→9와 다섯 신설 사건(`PartySizeFixed`·`CreationStepCompleted`·`CreationInterjection`·`CharacterCreated`·`PartyRosterLocked`)을 `rules_core/reducer.py`의 다섯 신설 분기와 **같은 커밋**에 넣었다 — 이 저장소가 이 사고를 낸 다섯 번째 자리다
- `session_actor/actor.py`에 명령 넷(`FixPartySize`·`CompleteCreationStep`·`CreateCharacter`·`LockPartyRoster`)과 `RosterAlreadyLocked`를 더하고, `_prepare_occupy`의 옛 세션 판별에 `created_characters` 조건을 더해 판 9의 「만들기 사건은 있는데 점유가 없는」 정상 상태가 옛 세션으로 오판되지 않게 고쳤다
- `web/routes_creation.py`(신설) 네 경로 + `app.py` 등록으로 인원 확정 → 항목 확정 → 완성(자동 점유 포함) → 명단 잠금이 HTTP로 끝까지 통과한다
- Task 3에서 판 8 이하 기록의 하위 호환, 다섯 신설 사건의 왕복·접기, `CreationStatEntryRecord`↔`StatEntry` 칸 개수 대조, 되돌리기 접기(superseded_seq)를 시험으로 못박았다

## Task Commits

Task 1(체크포인트 결정)은 코드 변경이 없어 별도 커밋이 없다 — 사용자의 `option-a` 선택이 Task 2 커밋 메시지에 기록되어 있다.

1. **Task 2: 선언 → 값 확정 → Entity → 사건 → 자동 점유 → 명단 잠금 한 줄기를 HTTP로 끝까지 뚫는다** - `6d7e67a` (feat)
2. **Task 3: 판 9 하위 호환과 사건 왕복을 시험으로 못박는다** - `9b21a05` (test)

**Plan metadata:** (이 커밋 — 아래 참조)

## Files Created/Modified

- `src/gptrpg/rules_core/rulebook.py` - `CreationStepDecl`/`CreationStepKind`/`InvalidCreationStep`/`Rulebook.creation_steps` 신설
- `src/gptrpg/rulebooks/dungeonworld_like.py` - `creation_steps` 두 항목(이름·능력치 배치) 선언
- `src/gptrpg/event_log/schema.py` - 판 9, 다섯 신설 사건 클래스 + `CreationAxisValueRecord`/`CreationStatEntryRecord`
- `src/gptrpg/rules_core/reducer.py` - `CreationStepFold`, `GameState` 파생 칸 넷, 다섯 신설 분기
- `src/gptrpg/session_actor/actor.py` - 명령 넷 + `RosterAlreadyLocked` + `_prepare_occupy` 정정
- `src/gptrpg/web/routes_creation.py` (신설) - 만들기 HTTP 라우터 넷
- `src/gptrpg/web/app.py` - `creation_router` 등록
- `tests/test_creation_tracer.py` (신설) - HTTP end-to-end 트레이서 시험 5건
- `tests/test_event_schema_migration.py` - 판 9 하위 호환·왕복·되돌리기 접기 시험 확장
- `tests/test_session_actor.py` - 옛 세션 판별 정정 두 방향 시험 확장
- `tests/fixtures/session1_expected_state.json` - `scripts/export_session_fixture.py` 재생성(신설 GameState 칸 네 개의 기본값만 추가된 diff — 895건 실기록의 해석 결과는 변경 없음)

## Decisions Made

- **Task 1 체크포인트(option-a, 사용자 승인):** D-08 그대로 — 다섯 사건을 한 번에 판 9로 올리고, 명단 잠금에 「푸는 사건」을 만들지 않는다. 지금 당장 쓰지 않는 끼어들기 사건(12.1-04가 소비)까지 미리 여는 대가를 받아들이는 대신, 판 올리기를 이 단계 안에서 한 번만 겪는다.
- **`PartySizeFixed.rulebook_min/rulebook_max` 자리표시자:** `Rulebook.party_size_range`가 아직 없으므로(12.1-02가 붙인다) 1/None(하한만 있고 검증 안 함)으로 적는다. 12.1-02가 실제 선언을 붙인 뒤 이 값을 실제 룰북 범위로 바꿔야 한다 — WINDOWS.md에 todo로 남긴다.
- **`/creation/step`·`/creation/complete`의 신원 대조 범위:** 「쿠키가 있는데 body의 캐릭터와 다르면」만 403이다. 계획 원문이 `select_character:264-267`의 규율을 그대로 옮기라고 적었지만, 그 규율을 글자 그대로(신원이 없으면 무조건 403) 옮기면 첫 만들기 참가자(아직 쿠키가 없다)가 아무것도 제출할 수 없어 이 단계의 목적 자체가 성립하지 않는다. `select_character`가 첫 선택에서 쿠키 없이 통과하는 것과 같은 결로 해석했다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `EVENT_SCHEMA_VERSION==8` 고정 시험과 커밋된 session1 픽스처가 이 계획 자신의 스키마 판 올리기로 즉시 깨졌다**
- **Found during:** Task 2 자체 `<verify>` 실행(`tests/test_event_schema_migration.py`가 이미 그 명령에 포함되어 있었다)
- **Issue:** `test_event_schema_version_is_eight`가 리터럴 `== 8`을 단언하고, `tests/fixtures/session1_expected_state.json`이 `GameState`의 신설 네 칸(`party_size_fixed`/`creation_step_values`/`created_characters`/`party_roster`)을 모르는 채 커밋되어 있어, 판을 9로 올리자마자 두 시험이 즉시 실패했다.
- **Fix:** `test_event_schema_version_is_eight`를 `test_event_schema_version_was_eight_before_phase_12_1`(≥8만 확인)과 `test_event_schema_version_is_nine`(==9)로 나눠 기존 판 못박기 관례를 그대로 유지했다. `.venv/bin/python scripts/export_session_fixture.py .gptrpg/events.db session1 tests/fixtures`를 다시 돌려 기대 상태를 재생성했다 — diff는 신설 네 칸의 기본값 추가뿐이고, 895건 실기록의 해석(다른 모든 칸의 값)은 한 글자도 안 바뀌었다(`tests/fixtures/README.md`가 정의한 "갱신해도 되는 경우"에 정확히 해당).
- **Files modified:** `tests/test_event_schema_migration.py`, `tests/fixtures/session1_expected_state.json`
- **Verification:** `.venv/bin/python -m pytest -q tests/test_event_schema_migration.py` 전체 통과, `git diff tests/fixtures/session1_events.jsonl`가 빈 diff(재생 원본 자체는 무변경)
- **Committed in:** `6d7e67a` (Task 2 커밋)

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking)
**Impact on plan:** Task 2 자신의 스키마 변경이 낳은 필연적 부작용을 즉시 고친 것뿐이다. 스코프 확장이 아니다.

## Known Stubs

- **`PartySizeFixed.rulebook_min`/`rulebook_max`가 항상 `1`/`None`이다** — `Rulebook.party_size_range`가 아직 선언되지 않아서다(12.1-02가 붙인다). 「검증하지 않았다」와 「범위가 1~무제한이다」가 사건 기록에서 구분되지 않으므로, 12.1-02가 실제 선언을 붙인 뒤에는 반드시 그 값을 사용해야 한다. `src/gptrpg/session_actor/actor.py`의 `_prepare_fix_party_size` 주석에 이 자리를 표시해 두었다.
- **`GET /my-character`·`GET /characters/{id}`가 이 계획이 만든 세션 스코프 캐릭터를 아직 못 본다** — 여전히 `web.characters_data`의 정적 목록만 읽는다. D-12가 이 마이그레이션을 12.1-05로 명시적으로 미뤄 두었다(계획서·`routes_creation.py` 모듈 도크스트링에 이미 문서화됨) — 새 스텁이 아니라 계획이 처음부터 의도한 경계다. 12.1-01 트레이서는 이 대신 사건 기록(`character_occupied`)과 쿠키로 자동 점유를 증명한다.

## Threat Flags

없음 — 이 계획의 `<threat_model>`이 이미 아홉 위협(T-12.1-01~09)을 등록했고, 이번 구현이 그 표를 벗어나는 새 표면(신규 네트워크 경로·인증 경로·파일 접근·스키마 경계)을 만들지 않았다. `routes_creation.py`의 네 경로 전부 계획이 이미 나열한 표면 안에 있다.

## Issues Encountered

None — Task 1 체크포인트가 미리 지목한 옛 세션 판별 충돌(`_prepare_occupy`)이 계획대로 정확히 발생했고, 계획이 지시한 방식(세 번째 조건 추가)으로 그대로 해소됐다. 예상 밖의 문제는 없었다.

## User Setup Required

None - 외부 서비스 설정 불필요.

## Next Phase Readiness

- 12.1-02(값 결정 유형 완성 + 인원 범위 검증)가 시작할 수 있다: `CreationStepDecl`의 나머지 네 kind(`pick_many`·`allocate_points`·`roll_to_fill`·`derive`)를 채우고, `Rulebook.party_size_range`를 붙여 `PartySizeFixed.rulebook_min/max` 자리표시자를 실제 값으로 교체해야 한다.
- 12.1-03/04(GM 대화·끼어들기)가 시작할 수 있다: `CreationInterjection` 사건 종류는 이미 스키마에 열려 있고(`_EVENT_CLASSES`에는 아직 미등록 — `RecordInterjection` 명령을 12.1-04가 추가할 때 함께 등록할 것), 리듀서 분기도 이미 있다.
- 12.1-05(정적 넷 제거)가 시작할 수 있다: `routes_creation.py`는 처음부터 `characters_data`를 보지 않으므로 그 정적 상수를 지울 때 이 라우터는 건드릴 필요가 없다. 대신 `routes_characters.py`/`routes_actions.py`/`portraits.py` 세 호출부를 세션 스코프(`GameState.created_characters`/`party_roster`)로 옮기는 작업이 여전히 남아 있다.
- 블로커 없음.

---
*Phase: 12.1-character-creation*
*Completed: 2026-08-19*

## Self-Check: PASSED

All files created/modified in this plan exist on disk, and both task commits
(`6d7e67a`, `9b21a05`) are present in `git log --all`.
