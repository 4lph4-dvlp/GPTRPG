---
phase: 12-stats-resources-inventory
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/export_session_fixture.py
  - tests/fixtures/session1_events.jsonl
  - tests/fixtures/session1_expected_state.json
  - tests/fixtures/README.md
  - tests/test_event_schema_migration.py
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "세션1의 실제 기록을 재생해도 같은 상태가 나오는지 확인하는 회귀 시험이 **CI에서 실제로 돈다** — 로컬에만 있는 파일에 기대지 않는다 (TEST-04)"
    - "픽스처가 저장소에 커밋되어 있어 다른 사람의 체크아웃에서도 같은 시험이 돈다"
    - "재생 결과 상태를 사람이 읽을 수 있는 형태로 커밋해 두어, 상태가 달라지면 diff로 무엇이 달라졌는지 바로 보인다"
    - "판 2로 쓰인 895건이 판 8 코드로 예외 없이 접힌다 — 새 사건 종류가 늘어난 것이 옛 기록을 깨지 않는다"
    - "옛 기록에는 `resource_changed` 사건이 하나도 없다는 것이 명시적으로 단언된다 — 「새 종류가 늘 뿐이라 옛 기록에는 그 종류가 없다」는 하위 호환 근거가 실제로 성립한다"
    - "로컬 실제 파일을 여는 기존 스모크 시험은 지워지지 않고 그대로 남는다 — 사람이 로컬에서 원본으로도 다시 확인할 수 있다"
    - "픽스처를 만드는 절차가 스크립트로 남아, 다음에 다른 세션을 픽스처로 만들 때 손으로 다시 짜지 않는다"
    - "픽스처를 만들 때 원본 데이터베이스 파일은 읽기만 한다 — 복제본을 열고 원본은 절대 쓰지 않는다는 기존 관례(`tests/test_event_log.py`)를 따른다"
  artifacts:
    - "tests/fixtures/session1_events.jsonl — 커밋된 세션1 실기록 사본"
    - "tests/fixtures/session1_expected_state.json — 재생 결과 상태 스냅샷"
    - "tests/fixtures/README.md — 픽스처의 출처·판·건수·재생성 절차"
    - "scripts/export_session_fixture.py — 재생성 스크립트"
  key_links:
    - "`tests/fixtures/session1_events.jsonl` ↔ `EventStore.append` ↔ `rebuild_state_from_events` ↔ `tests/fixtures/session1_expected_state.json` — CI에서 실제로 도는 회귀 그물"
    - "`.gitignore`의 `**/.gptrpg/*` ↔ 기존 `skipif(not path.is_file())` 스모크 — 이 조합이 지금 CI에서 그 시험을 항상 건너뛰게 만든다"
  prohibitions:
    - "원본 `.gptrpg/events.db`를 열어 쓰거나 고치지 않는다 — 복제본만 연다"
    - "픽스처를 만들면서 사건 내용을 다듬거나 잘라 넣지 않는다 — 실기록 그대로다"
    - "기존 로컬 스모크 시험(`skipif` 붙은 것들)을 지우거나 대체하지 않는다"
    - "재생 결과가 달라졌을 때 기대값 파일을 무심코 갱신해 시험을 통과시키지 않는다"
---

<objective>
**「세션1 실기록으로 재생 결과를 확인한다」를 종이 위 사실에서 CI가 실제로 돌리는 그물로
바꾼다.**

지금 `tests/test_event_schema_migration.py`의 실기록 시험들은 전부
`@pytest.mark.skipif(not path.is_file())`로 감싸져 있고, 그 파일(`.gptrpg/events.db`)은
`.gitignore:16`의 `**/.gptrpg/*`에 걸려 커밋되지 않는다. 즉 **CI에서는 이 시험이 항상 조용히
건너뛰어진다.** TEST-04의 출처가 「다음에는 CI가 잡는다」인데, 지금 상태로는 잡지 못한다.

이 계획은 세션1의 895건을 저장소에 커밋된 픽스처로 옮기고, 그 픽스처를 적재해 재생한 결과가
커밋된 기대 상태와 같은지 확인하는 시험을 만든다. 기존 로컬 스모크 시험은 그대로 둔다 —
사람이 로컬에서 원본으로도 다시 확인할 수 있어야 한다.

Purpose: 회귀 그물이 「돌면 잡는다」가 아니라 「항상 돈다」여야 그물이다.
Output: 커밋된 픽스처 · 기대 상태 스냅샷 · 재생성 스크립트 · CI에서 도는 회귀 시험.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stats-resources-inventory/12-CONTEXT.md
@.planning/phases/12-stats-resources-inventory/12-RESEARCH.md
@.planning/phases/12-stats-resources-inventory/12-01-SUMMARY.md
</context>

## 조사가 확정한 것 — 다시 조사하지 말 것

- **`.gptrpg/events.db`의 895건은 전부 `schema_version = 2`다.** 과거 문서 여럿이 「판 5,
  895건」이라 적었지만 실제 파일과 어긋난다 — `tests/test_event_schema_migration.py:5-9`가 이미
  이 정정을 기록해 두었다. 판 5로 기록된 것은 `.gptrpg/uat9.db`(221건, 세션 넷)다.
- **파일에는 세션이 24개 있고 `session1`이 895건이다.** 나머지 23개 세션은 합쳐 221건이며
  디버깅·검증용 짧은 세션들이다. TEST-04가 말하는 「세션1의 실제 기록」은 `session1` 하나다.
- **테이블 모양:** `events(session_id, seq, event_type, schema_version, payload, visibility,
  caused_by_seq, recorded_at)`. `payload` 칸이 사건 전체 JSON을 그대로 들고 있으므로, 그
  칸만 줄 단위로 뽑으면 그대로 JSONL 픽스처가 된다.
- **원본은 절대 열어 쓰지 않는다는 관례가 이미 있다** — `tests/test_event_log.py`의
  `test_real_events_db_replay_smoke_test_folds_without_exception`(08-01 Task 3)이 `shutil.copy`로
  복제본만 연다.

## 플래그된 가정 (사람 확인 권장)

- **TEST-04 (edge probe `unclassified` — 자동 분류가 이 요구사항의 엣지 범주를 못 정했다):**
  「같은 상태가 나온다」의 **비교 범위**를 이 계획이 다음과 같이 해석한다 — 접은
  `GameState`의 모든 필드를 사전으로 직렬화해 통째로 비교한다(`last_seq`·`turn_count`·
  `check_count`·`failure_count`·`fails_since_clock`·`clock_segment`·`clock_advances`·
  `narration_count`·`ai_calls`·토큰 네 칸·`last_grade`·표 여섯 개 전부). 「일부 칸만
  비교한다」가 아니다. 이 해석이 요구사항 원문의 의도와 맞는지는 사람이 확인해야 한다 —
  원문은 「같은 상태가 나오는지」까지만 적었다.
- **커밋되는 픽스처에는 세션1의 실제 플레이 문장(한국어 원문)이 그대로 들어간다.** 이
  프로젝트 자신의 세션 기록이고 `docs/session1-code-review.md`가 이미 그 내용을 다루고
  있으므로 이 계획은 그대로 커밋한다고 가정한다. 그렇게 하면 안 되는 사정이 있으면 Task 1
  실행 전에 알려야 한다.

## Artifacts this phase produces

이 단계(12) 전체 목록은 `12-01-PLAN.md`의 같은 이름 절에 있다. 이 계획의 몫:

| 종류 | 이름 | 위치 |
|---|---|---|
| 스크립트 | `scripts/export_session_fixture.py` | 신설 |
| 픽스처 | `tests/fixtures/session1_events.jsonl` | 신설 |
| 픽스처 | `tests/fixtures/session1_expected_state.json` | 신설 |
| 문서 | `tests/fixtures/README.md` | 신설 |
| 시험 | `test_committed_session1_fixture_folds_to_expected_state` 외 | `tests/test_event_schema_migration.py` |

<tasks>

<task type="auto">
  <name>Task 1: 세션1 실기록 895건을 커밋되는 픽스처로 뽑아내는 스크립트를 만들고 실행한다</name>
  <precondition>`.gptrpg/events.db`가 이 체크아웃에 있고 `session1` 세션의 사건 895건을 담고 있다 — 이 파일은 `.gitignore`의 `**/.gptrpg/*`에 걸려 있어 다른 체크아웃에는 없다. 없으면 픽스처를 만들 수 없으므로 여기서 멈추고 사람에게 알린다.</precondition>
  <files>scripts/export_session_fixture.py, tests/fixtures/session1_events.jsonl, tests/fixtures/session1_expected_state.json, tests/fixtures/README.md</files>
  <read_first>
    - tests/test_event_schema_migration.py (전체 — `skipif` 관례·복제본만 여는 관례·`_session_ids` 도우미·판 못박기 시험)
    - tests/test_event_log.py (`test_real_events_db_replay_smoke_test_folds_without_exception` — `shutil.copy` 원본 보호 관례)
    - src/gptrpg/event_log/store.py (전체 — `EventStore`의 `initialize`·`append`·`read_events` 계약과 테이블 모양)
    - src/gptrpg/event_log/schema.py 95-120행·373-380행 (`EventEnvelope` 필수 칸과 `parse_event`)
    - src/gptrpg/session_actor/projection.py (`rebuild_state_from_events` 시그니처)
    - src/gptrpg/rules_core/reducer.py 30-112행 (`GameState`의 모든 필드 — 기대 상태 스냅샷이 덮어야 할 목록)
    - .gitignore 10-20행 (`**/.gptrpg/*` 규칙)
    - .planning/phases/12-stats-resources-inventory/12-RESEARCH.md § Common Pitfalls 5
  </read_first>
  <action>
①**`scripts/export_session_fixture.py`.** 명령줄 인자로 원본 데이터베이스 경로 · 세션 id ·
출력 디렉터리를 받는다. **원본은 절대 열어 쓰지 않는다** — `shutil.copy`로 임시 복제본을 만들고
그 복제본만 읽는다(`tests/test_event_log.py`가 세운 관례). 복제본에서 그 세션의 사건을
`EventStore.read_events`로 순번 순서대로 읽어, 각 사건을 `model_dump_json()`으로 직렬화한 한
줄씩 `{session}_events.jsonl`에 쓴다. 줄 순서는 순번 순서다. 이어서 같은 사건 목록을
`rebuild_state_from_events`로 접어 나온 `GameState`를 `dataclasses.asdict`로 사전으로 만들고,
**모든 필드**를 담은 JSON을 `{session}_expected_state.json`에 사람이 읽을 수 있는 들여쓰기와
정렬된 키로 쓴다(줄 단위 diff가 의미 있게 나오도록). 표 형태 필드의 키가 튜플이면(예:
`(character_id, axis)`) JSON으로 쓸 수 있게 문자열 키로 바꾸고, 그 변환 규칙을 스크립트
도크스트링과 아래 README에 명시한다. 사건 내용을 다듬거나 잘라 넣지 않는다.

②**스크립트를 실제로 실행해 픽스처 셋을 만든다.** 세션은 `session1` 하나다.
`tests/fixtures/session1_events.jsonl`의 줄 수가 정확히 895인지, 모든 줄의
`schema_version`이 2인지 확인한다. 895건 전체를 담는다 — 부분집합으로 줄이지 않는다(파일
크기가 수백 KB 수준이라 저장소에 부담이 없다는 것은 조사가 확인했다).

③**`tests/fixtures/README.md`.** 이 픽스처가 무엇인지 한 화면 안에 적는다: 출처(어떤 세션·
언제·몇 건) · 기록 판(2) · 왜 커밋했는가(`.gptrpg/*`가 gitignore 대상이라 CI에서 기존
스모크가 항상 건너뛰어졌다) · 재생성 명령 한 줄 · **기대 상태 파일을 갱신해도 되는 경우와
안 되는 경우**. 마지막 항목이 이 문서의 핵심이다 — 재생 결과가 달라졌을 때 기대값을
갱신하는 것은 「사건 해석 규칙이 의도적으로 바뀌었고 그 변경이 계획 문서에 근거를 남겼을
때」뿐이고, 그 밖에는 회귀다.

④**`.gitignore` 확인.** `tests/fixtures/` 아래가 무시되지 않는지 확인한다. 무시된다면 그
규칙만 좁힌다 — `**/.gptrpg/*` 규칙은 손대지 않는다.
  </action>
  <verify>
    <automated>test $(wc -l < tests/fixtures/session1_events.jsonl) -eq 895</automated>
    <automated>uv run python -c "import json;[json.loads(l) for l in open('tests/fixtures/session1_events.jsonl')];print('ok')"</automated>
    <automated>git check-ignore -q tests/fixtures/session1_events.jsonl && echo IGNORED && exit 1 || echo TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l tests/fixtures/session1_events.jsonl`가 895다
    - 모든 줄이 유효한 JSON이고 `schema_version` 값이 전부 2다
    - `git check-ignore tests/fixtures/session1_events.jsonl`가 아무것도 출력하지 않는다 (무시 대상이 아니다)
    - `git status --porcelain tests/fixtures/`가 네 파일(`session1_events.jsonl`·`session1_expected_state.json`·`README.md`와 디렉터리)을 추가 대상으로 보여준다
    - `tests/fixtures/session1_expected_state.json`에 `GameState`의 모든 필드 이름이 키로 들어 있다 — `python -c "import json,dataclasses;from gptrpg.rules_core.reducer import GameState;print(set(f.name for f in dataclasses.fields(GameState)) - set(json.load(open('tests/fixtures/session1_expected_state.json'))))"`의 출력이 `set()`이다
    - `scripts/export_session_fixture.py`가 `shutil.copy`로 복제본을 만든 뒤에만 데이터베이스를 연다
    - 스크립트 실행 전후로 `.gptrpg/events.db`의 수정 시각과 크기가 같다
  </acceptance_criteria>
  <done>세션1의 895건과 그 재생 결과 상태가 저장소에 커밋된 파일로 존재하고, 같은 파일을 다시 만드는 절차가 스크립트로 남았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 커밋된 픽스처로 CI에서 실제로 도는 회귀 시험을 만든다 (TEST-04)</name>
  <files>tests/test_event_schema_migration.py</files>
  <read_first>
    - tests/test_event_schema_migration.py (전체 — 절 구분 주석 관례 ①②③④, `skipif` 시험 여섯, 판 못박기 시험)
    - tests/fixtures/README.md (Task 1이 만든 것 — 기대 상태 갱신 규칙)
    - src/gptrpg/event_log/store.py (`EventStore.append`가 요구하는 사건 객체 형태)
    - src/gptrpg/event_log/schema.py 373-380행 (`parse_event`)
    - src/gptrpg/session_actor/projection.py (`rebuild_state_from_events`)
  </read_first>
  <behavior>
    - 커밋된 JSONL을 `parse_event`로 895개 사건 객체로 되돌리면 예외가 없다
    - 그 사건들을 빈 `EventStore`에 넣고 다시 읽어 접으면, 접힌 `GameState`가 커밋된 기대 상태 JSON과 **모든 필드에서** 같다
    - `skipif` 없이 돈다 — 로컬 파일이 없는 환경에서도 이 시험이 실행된다
    - 895건 안에 `resource_changed` 사건이 하나도 없다
    - 895건 안에 판 8에서 늘어난 어떤 칸도 없다 — 전부 `schema_version == 2`다
    - 같은 픽스처를 두 번 접으면 두 결과가 같다
  </behavior>
  <action>
①**절 ⑤를 새로 연다.** `tests/test_event_schema_migration.py`의 기존 절 구분 주석
(`# ---- ① ... ----`) 형식을 그대로 이어 「⑤ 커밋된 픽스처 — CI에서 실제로 도는 회귀 그물
(TEST-04)」 절을 파일 끝에 더한다. 절 머리 주석에 **왜 이 절이 필요한가**를 한 문단으로 적는다:
①②의 시험들은 `.gptrpg/*`가 gitignore 대상이라 CI에서 항상 건너뛰어지고, 그래서 「다음에는
CI가 잡는다」는 TEST-04의 목적이 지금까지 달성되지 않았다.

②**시험 넷을 더한다.** ⓐ 커밋된 JSONL을 읽어 895건이 전부 `parse_event`로 되돌아오는지.
ⓑ 그 사건들을 `tmp_path`의 새 `EventStore`에 넣고 다시 읽어 접은 `GameState`를
`dataclasses.asdict`로 사전으로 만들어 커밋된 기대 상태 JSON과 **통째로** 비교한다 — 필드
하나만 보지 않는다. 튜플 키 → 문자열 키 변환은 Task 1의 스크립트와 **같은 규칙**을 쓴다
(변환 함수를 스크립트에서 import하거나, 시험 파일 안에 같은 규칙으로 다시 쓰고 그 사실을
주석으로 밝힌다 — 두 규칙이 갈리면 이 시험이 무의미해진다). ⓒ 895건에 `resource_changed`
사건이 하나도 없다는 것을 명시적으로 단언한다 — 기존
`test_events_db_has_no_safety_flagged_events_yet`과 같은 모양이고, 「새 종류가 늘 뿐이라 옛
기록에는 그 종류가 없다」는 하위 호환 근거를 판 8에 대해 다시 못박는다. ⓓ 같은 픽스처를 두
번 접은 결과가 같다.

③**`skipif`를 붙이지 않는다.** 이 절의 시험 넷 중 어느 것에도 `skipif`가 없어야 한다 —
그것이 이 계획의 전부다. ①②③④ 절의 기존 시험은 하나도 지우거나 고치지 않는다.

④**실패했을 때 사람이 무엇을 해야 하는지를 단언 메시지에 적는다.** 기대 상태 비교가
실패하면 「사건 해석 규칙이 바뀌었다 — 의도한 변경이면 `tests/fixtures/README.md`의 갱신
규칙을 따르고, 아니면 회귀다」가 보이게 한다. 기대값을 무심코 갱신해 통과시키는 것을 막는
유일한 장치가 이 문장이다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_event_schema_migration.py -k fixture -x -q</automated>
    <automated>uv run pytest tests/test_event_schema_migration.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_event_schema_migration.py -k fixture -q` 수집 개수가 4 이상이고 전부 통과한다
    - 새 절의 시험 함수 어디에도 `skipif`가 붙어 있지 않다 — `git diff HEAD~1 -- tests/test_event_schema_migration.py | grep '^+' | grep -c skipif`가 0이다 <!-- planner-discipline-allow: skipif -->
    - `.gptrpg` 디렉터리 이름을 임시로 바꿔도(`mv .gptrpg .gptrpg-off`) `uv run pytest tests/test_event_schema_migration.py -k fixture -q`가 여전히 4건 이상 **실행**되고 통과한다 (건너뛰지 않는다). 확인 후 이름을 되돌린다
    - 기대 상태 JSON의 아무 필드 값을 한 글자 바꾸면 그 시험이 실패한다 (그물이 실제로 잡는다)
    - 기존 `skipif` 시험 여섯이 그대로 남아 있다 — `grep -c skipif tests/test_event_schema_migration.py`가 이 계획 이전과 같다
    - `uv run pytest -q`가 0으로 끝난다
  </acceptance_criteria>
  <done>세션1 실기록 재생 회귀 시험이 로컬 파일 유무와 무관하게 항상 돈다. 재생 결과가 달라지면 CI가 그 자리에서 잡고, 무엇이 달라졌는지 diff로 보인다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 `.gptrpg/events.db` → 커밋되는 픽스처 파일 | 저장소 밖 데이터가 저장소 안으로 들어오는 유일한 지점 |
| 커밋된 픽스처 → `parse_event` → `fold` | 파일 내용이 상태 재구성으로 들어오는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Tampering | 픽스처를 만들다 원본 데이터베이스를 손상시킨다 | medium | mitigate | Task 1 — `shutil.copy` 복제본만 연다. 실행 전후 원본 크기·수정 시각이 같은지 확인하는 것이 완료 조건이다 |
| T-12-13 | Repudiation | 재생 결과가 달라졌는데 기대값을 갱신해 통과시킨다 | medium | mitigate | Task 1·2 — `tests/fixtures/README.md`가 갱신 허용 조건을 명시하고, 실패 메시지가 그 규칙을 가리킨다 |
| T-12-14 | Information Disclosure | 실제 플레이 문장이 저장소에 영구히 남는다 | low | accept | 이 프로젝트 자신의 세션 기록이고 `docs/session1-code-review.md`가 이미 그 내용을 다룬다. 제3자 개인정보는 없고 참가자 표기는 캐릭터 이름이다. 위 「플래그된 가정」에 사람 확인 항목으로 남겼다 |
| T-12-15 | Denial of Service | 픽스처가 너무 커서 저장소·CI가 무거워진다 | low | accept | 조사가 실측한 원본 데이터베이스 전체가 440KB이고 세션1 사건 본문 합계가 그보다 작다 — 부담이 없다 |
| T-12-SC | Tampering | 패키지 설치 | low | accept | 이 계획은 패키지를 설치하지 않는다 — 표준 라이브러리(`sqlite3`·`shutil`·`json`)와 기존 `EventStore`만 쓴다 |
</threat_model>

<verification>
- `uv run pytest -q` 전체 통과
- `.gptrpg` 디렉터리를 임시로 치운 상태에서도 픽스처 회귀 시험이 실행되고 통과한다
- `git check-ignore tests/fixtures/*`가 아무것도 출력하지 않는다
- 원본 `.gptrpg/events.db`가 이 계획 실행 전후로 바이트 단위로 같다
</verification>

<success_criteria>
- 세션1의 실제 기록을 재생해도 같은 상태가 나오는지 확인하는 회귀 픽스처가 있고, 그 시험이 CI에서 실제로 돈다 (TEST-04)
</success_criteria>

<output>
Create `.planning/phases/12-stats-resources-inventory/12-03-SUMMARY.md` when done
</output>
