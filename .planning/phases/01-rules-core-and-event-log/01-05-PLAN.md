---
phase: 01-rules-core-and-event-log
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - tests/conftest.py
  - tests/test_reverse_verification.py
  - src/gptrpg/event_log/schema.py
autonomous: true
requirements: [RIG-06]

must_haves:
  truths:
    - "가짜로 지어낸 완결된 세션 기록 하나만으로 여섯 개의 숫자가 전부 산출된다 — 실제 토큰 소모량 / 실제 턴 수 / 문장 입력에서 행동 확인 표시까지의 시간 / 확인에서 서사 첫 글자까지의 시간 / 판정 실패 횟수와 위협 시계 진행 횟수 / 플레이어가 친 문장과 시스템 제안과 확인 여부"
    - "여섯 개를 각각 별도의 단언으로 확인하며, 하나가 실패하면 실패 메시지가 어느 숫자가 왜 안 나오는지를 이름으로 가리킨다"
    - "순번이 인접하다는 것이 인과를 뜻하지 않는다 — 두 플레이어의 사건이 번갈아 섞인 기록에서, 응답 시간 계산이 순번 인접이 아니라 인과 칸으로만 짝을 맞춘다는 것이 확인된다"
    - "가짜 세션 기록은 실제 저장소에 실제로 append 되었다가 다시 읽힌 것이다 — 메모리 안에서 만든 객체 목록이 아니라 저장·복원을 한 번 거친 기록이다"
    - "여섯 숫자 중 하나라도 산출되지 않으면 그 사실이 조용히 넘어가지 않고 테스트 실패로 드러난다"
  artifacts:
    - path: "tests/conftest.py"
      provides: "완결된 한 세션을 지어내는 픽스처"
      min_lines: 80
    - path: "tests/test_reverse_verification.py"
      provides: "여섯 숫자 역산 검증 — 이 단계의 게이트"
      min_lines: 90
  key_links:
    - from: "tests/test_reverse_verification.py"
      to: "tests/conftest.py"
      via: "fake_session_log 픽스처를 받아 여섯 숫자를 거꾸로 뽑는다"
      pattern: "fake_session_log"
    - from: "tests/conftest.py"
      to: "src/gptrpg/event_log/store.py"
      via: "지어낸 사건을 실제 저장소에 append 한 뒤 다시 읽어 돌려준다"
      pattern: "EventStore"
  prohibitions:
    - statement: "여섯 숫자 중 산출되지 않는 것이 있을 때 테스트를 느슨하게 고쳐 초록불을 만들지 않는다 — 안 나오는 숫자는 기록에 칸이 빠졌다는 신호이고, 그 신호를 지우면 Phase 6에서야 드러난다"
      status: resolved
      verification: judgment
---

<objective>
**이 단계의 게이트.** 사건 기록에 칸을 빠뜨렸는지를 실험 이후가 아니라 **지금** 잡아낸다.

방법은 거꾸로 가는 것이다. Phase 6이 프로젝트를 계속할지 정할 때 필요한 숫자 여섯 개의 목록을 놓고, 가짜 세션 기록을 하나 지어낸 뒤 "이 기록만으로 여섯 개가 전부 나오는가"를 테스트로 확인한다. 하나라도 안 나오면 칸이 빠진 것이다.

Purpose: 빠뜨린 칸은 Phase 6에 가서야 드러나고, 그때는 이미 끝난 두 세션을 다시 할 수 없다. 실험은 1주 간격으로 딱 두 번이다. 부수 효과로 Phase 4의 집계 기능이 사실상 절반 완성된다.

Output: 완결된 한 세션을 지어내는 픽스처 + 여섯 숫자를 각각 별도로 단언하는 테스트.

**산출해야 하는 여섯 가지 (D-11 원문):** 실제 토큰 소모량 / 실제 턴 수 / 문장 입력에서 행동 확인 표시까지의 시간 / 확인에서 서사 첫 글자까지의 시간 / 판정 실패 횟수 대비 위협 시계 진행 횟수 / 플레이어가 실제로 친 문장 + 시스템의 제안 + 플레이어의 확인 여부.

**이 계획이 하지 않는 것:** 실제 집계 기능을 만들지 않는다. 집계 코드는 Phase 4가 만든다. 여기서 하는 일은 "그때 만들 수 있는가"를 지금 증명하는 것뿐이다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rules-core-and-event-log/01-CONTEXT.md
@.planning/phases/01-rules-core-and-event-log/01-RESEARCH.md
@.planning/phases/01-rules-core-and-event-log/01-VALIDATION.md
@.planning/phases/01-rules-core-and-event-log/01-03-SUMMARY.md
</context>

<interfaces>
**01-03이 확정한 여섯 종류와 칸. 이 계획은 이 이름을 바꾸지 않는다.**

| `event_type` | 고유 필수 칸 |
|---|---|
| `action_declared` | `player_id`, `raw_text` |
| `action_confirmed` | `player_id`, `move`, `stat`, `system_suggestion`, `player_confirmed` |
| `check_resolved` | `move`, `rolls`, `modifiers`, `target`, `grade` |
| `narration_appended` | `text`, `chunk_index` |
| `clock_advanced` | `clock_id`, `segment_index`, `trigger` |
| `ai_invoked` | `agent_role`, `model`, `provider`, `prompt_tokens`, `completion_tokens`, `latency_ms` |

공통 봉투 칸: `session_id`, `seq`, `event_type`, `schema_version`, `visibility`, `recorded_at`, `caused_by_seq`(01-02 Task 1의 결정에 따름).

**여섯 숫자와 그 재료의 대응**

| 숫자 | 어디서 나오나 |
|---|---|
| ① 실제 토큰 소모량 | `ai_invoked` 사건들의 `prompt_tokens` + `completion_tokens` 합 |
| ② 실제 턴 수 | `action_declared` 사건의 개수 |
| ③ 문장 입력 → 행동 확인 표시 시간 | `action_confirmed.recorded_at` 빼기 그것이 가리키는 `action_declared.recorded_at` |
| ④ 확인 → 서사 첫 글자 시간 | `chunk_index` 가 0인 `narration_appended.recorded_at` 빼기 그것이 가리키는 `action_confirmed.recorded_at` |
| ⑤ 판정 실패 횟수 대비 시계 진행 횟수 | 등급이 실패인 `check_resolved` 개수 / `clock_advanced` 개수 |
| ⑥ 플레이어가 친 문장 + 제안 + 확인 여부 | `action_declared.raw_text` + `action_confirmed.system_suggestion` + `action_confirmed.player_confirmed` |

③과 ④의 "그것이 가리키는" 부분이 인과 칸이다. 이 칸이 없으면 순번 인접에 기대야 하는데, 네 명이 동시에 입력하면 순번은 섞인다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 완결된 한 세션을 지어내는 픽스처</name>
  <files>tests/conftest.py, tests/test_reverse_verification.py</files>
  <read_first>
    - `tests/conftest.py` (01-01이 만든 `PROJECT_ROOT` 와 `tmp_db_path` — 여기에 덧붙인다)
    - `src/gptrpg/event_log/schema.py` (01-03이 확정한 여섯 종류의 정확한 칸 이름)
    - `src/gptrpg/event_log/store.py` (`EventStore.append` 와 `read_events` 의 시그니처)
    - `.planning/phases/01-rules-core-and-event-log/01-RESEARCH.md` (§ Code Examples — 역방향 검증 테스트의 모양)
  </read_first>
  <action>
    `tests/conftest.py` 에 `fake_session_log` 픽스처를 추가한다. 임시 저장소 파일에 사건을 실제로 append 하고, `read_events` 로 다시 읽은 사건 목록을 돌려준다. 메모리에서 만든 객체를 그대로 돌려주면 저장·복원 과정에서 값이 상하는지를 못 잡는다.

    지어낼 세션의 내용은 아래 조건을 전부 만족해야 한다.

    **두 플레이어가 번갈아 입력한다.** `p1` 과 `p2` 두 사람의 사건이 순번상 서로 끼어들게 배치한다. 구체적으로, `p1` 이 문장을 치고(순번 0) → `p2` 가 문장을 치고(순번 1) → `p1` 의 확인이 오고(순번 2) → `p2` 의 확인이 오는(순번 3) 구간을 최소 한 번 넣는다. 이렇게 하면 "바로 앞 순번이 원인"이라는 가정이 틀린 짝을 만들어 내므로, 인과 칸을 쓰지 않는 계산은 이 픽스처에서 반드시 틀린 값을 낸다.

    **여섯 종류가 전부 등장한다.** `action_declared` 는 최소 3개(턴 수가 1보다 커야 개수 세기가 의미 있다), `action_confirmed` 는 각 선언에 하나씩, `check_resolved` 는 최소 3개(그중 최소 1개는 등급이 실패), `narration_appended` 는 서사 하나가 조각 두 개로 나뉜 형태로 최소 2개(`chunk_index` 가 0과 1), `clock_advanced` 는 1개(`trigger` 는 실패 누적), `ai_invoked` 는 2개(서로 다른 역할과 모델, 토큰 수는 0이 아닌 값).

    **재굴림이 한 번 있다.** `check_resolved` 중 하나는 `rolls` 에 눈이 네 개 들어 있다.

    **시각이 실제로 흐른다.** `recorded_at` 값을 사건마다 다르게, 그리고 응답 시간을 계산했을 때 의미 있는 숫자가 나오도록 배치한다 — 문장 입력에서 확인까지 수백 밀리초, 확인에서 서사 첫 글자까지 1~3초 정도. 실제 시각 함수를 부르지 말고 고정된 기준 시각에서 밀리초를 더해 만든다. 그래야 테스트가 실행 시각과 무관하게 항상 같은 결과를 낸다.

    **인과가 이어져 있다.** 01-02 Task 1에서 인과 칸을 두기로 했다면, 각 `action_confirmed` 는 자기 플레이어의 `action_declared` 를, 각 `check_resolved` 와 `narration_appended` 는 대응하는 `action_confirmed` 를, `clock_advanced` 는 그것을 촉발한 `check_resolved` 를 가리킨다.

    픽스처와 함께 세션 식별자를 돌려준다(`(session_id, events)` 짝 또는 이름 있는 작은 객체). 그리고 지어낸 사건 목록을 눈으로 훑을 수 있게, 픽스처 위에 어떤 순서로 무엇이 일어나는 세션인지를 주석이 아니라 픽스처 함수의 설명 문자열로 여덟 줄 이내로 적는다.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_reverse_verification.py -k fixture -x` 가 0으로 끝난다 (픽스처가 실제로 만들어지고 읽히는지 확인하는 최소 테스트를 함께 넣는다)
    - 픽스처가 돌려주는 사건 목록에 여섯 종류가 모두 최소 한 번씩 나온다
    - `action_declared` 가 3개 이상, `check_resolved` 가 3개 이상이고 그중 등급이 실패인 것이 1개 이상이다
    - `chunk_index` 가 0인 `narration_appended` 와 1인 것이 각각 존재한다
    - `rolls` 의 길이가 4인 `check_resolved` 가 하나 존재한다
    - 순번 0~3 구간에서 두 플레이어의 사건이 번갈아 나온다
    - 픽스처가 실제 `EventStore` 를 거친다 (`append` 후 `read_events` 로 읽은 결과를 돌려준다)
    - 같은 테스트를 두 번 실행해도 `recorded_at` 값들이 동일하다 (실행 시각에 의존하지 않는다)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reverse_verification.py -k fixture -x</automated>
  </verify>
  <done>실제 세션 하나의 모양을 그대로 닮은 기록이 저장소를 한 번 거쳐 테스트에 공급된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 그 기록만으로 여섯 숫자가 전부 나오는지 거꾸로 확인한다</name>
  <files>tests/test_reverse_verification.py, src/gptrpg/event_log/schema.py</files>
  <read_first>
    - `tests/conftest.py` (Task 1이 방금 만든 `fake_session_log` — 무엇이 들어 있는지)
    - `src/gptrpg/event_log/schema.py` (여섯 종류의 칸 이름 — 여기서 칸이 모자라면 이 파일을 고쳐야 한다)
    - `.planning/REQUIREMENTS.md` (MEAS-01 ~ MEAS-04 — 각 숫자의 원문 정의)
    - `.planning/phases/01-rules-core-and-event-log/01-CONTEXT.md` (D-11 — 이 작업의 근거이자 이 단계에서 값이 가장 큰 결정)
    - `.planning/phases/01-rules-core-and-event-log/01-VALIDATION.md` (D-11이 이 단계의 하중을 지는 검사라는 진술)
  </read_first>
  <behavior>
    `tests/test_reverse_verification.py` 가 먼저 빨간불이 되어야 한다. 여섯 숫자를 **각각 별도의 단언**으로 확인하고, 각 단언에 실패 메시지를 붙여 어느 숫자가 왜 안 나오는지 이름으로 드러나게 한다.

    ① **실제 토큰 소모량** — `ai_invoked` 사건들의 두 토큰 칸을 더한 값이 0보다 크고, 픽스처가 넣은 값의 합과 정확히 같다.

    ② **실제 턴 수** — `action_declared` 사건의 개수가 픽스처가 넣은 개수와 같고 1보다 크다.

    ③ **문장 입력 → 행동 확인 표시 시간** — 각 `action_confirmed` 에 대해 그것이 가리키는 `action_declared` 를 찾아 두 시각의 차이를 밀리초로 계산한다. 짝을 못 찾으면 그 자리에서 실패한다. 계산된 값이 픽스처가 의도한 값과 같다. **그리고 이 짝짓기가 "바로 앞 순번"이 아니라 인과 칸으로만 이루어진다는 것을 함께 단언한다** — 픽스처에서 두 플레이어가 번갈아 입력하므로, 순번 인접으로 짝을 맞추면 다른 값이 나온다. 두 방식의 결과가 다르다는 것 자체를 단언해 인과 칸이 실제로 일을 하고 있음을 보인다.

    ④ **확인 → 서사 첫 글자 시간** — `chunk_index` 가 0인 `narration_appended` 에 대해 그것이 가리키는 `action_confirmed` 를 찾아 두 시각의 차이를 밀리초로 계산한다. 조각이 여러 개일 때 첫 조각만 쓴다는 것이 이 단언의 요지다.

    ⑤ **판정 실패 횟수와 시계 진행 횟수** — 등급이 실패인 `check_resolved` 의 개수와 `clock_advanced` 의 개수를 각각 세고, 둘 다 픽스처가 넣은 값과 같다. 두 숫자가 모두 기록에서 나온다는 것이 요지이고, 비율을 어떻게 볼지는 Phase 4·6의 몫이다.

    ⑥ **플레이어가 친 문장 + 제안 + 확인 여부** — 모든 `action_declared` 의 `raw_text` 가 비어 있지 않고, 모든 `action_confirmed` 에 `system_suggestion` 이 비어 있지 않으며 `player_confirmed` 가 참 또는 거짓이다. 그리고 세 조각을 이어 붙여 `(친 문장, 제안, 확인 여부)` 짝의 목록을 실제로 만들어 내고, 그 개수가 턴 수와 같다.

    마지막으로 여섯 개를 전부 지나온 뒤, 시각 차이 계산이 정수 밀리초로만 이루어졌음을 단언한다 — 계산 결과의 자료형이 정수다.
  </behavior>
  <action>
    `tests/test_reverse_verification.py` 를 만들고 위 여섯 단언 + 정수 밀리초 단언을 쓴다. 시각 문자열을 밀리초로 바꾸는 작은 도우미 함수는 이 테스트 파일 안에 둔다 — 이것은 Phase 4가 만들 집계 코드의 자리이지 지금 라이브러리에 넣을 것이 아니다.

    **여섯 개 중 하나라도 나오지 않으면:** 그것은 테스트가 틀린 것이 아니라 **기록에 칸이 빠졌다는 신호**다. 이 경우 단언을 지우거나 느슨하게 고쳐 초록불을 만들지 않는다. 대신 `src/gptrpg/event_log/schema.py` 에 빠진 칸을 추가하고, 사건 모양이 바뀌었으므로 `EVENT_SCHEMA_VERSION` 을 올리고, 01-03이 만든 스키마 테스트도 새 칸에 맞게 넓힌다. 무엇이 왜 빠져 있었는지를 SUMMARY에 한 문단으로 남긴다.

    **인과 칸이 없어서 ③④가 나오지 않는 경우는 다르게 다룬다.** 01-02 Task 1에서 사람이 인과 칸을 두지 않기로 골랐다면 ③과 ④는 원리적으로 산출할 수 없다. 이때는 스키마를 마음대로 고치지 말고, ③④가 산출 불가능하다는 사실과 그 결과 Phase 6에서 응답 속도 두 지점을 잴 수 없다는 것을 SUMMARY에 명시한 뒤 사람 확인이 필요하다고 보고한다. 결정을 뒤집는 것은 이 작업의 권한이 아니다.

    테스트 이름 중 하나에 `fixture` 를 넣어 Task 1의 검증 명령이 그것만 골라 돌릴 수 있게 유지한다.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_reverse_verification.py -x` 가 0으로 끝난다
    - 파일 안에 여섯 숫자에 각각 대응하는 단언이 존재하고, 각 단언에 어느 숫자인지 이름이 든 실패 메시지가 붙어 있다
    - ③의 짝짓기가 순번 인접 방식과 다른 결과를 낸다는 것을 단언하는 테스트가 존재한다 (인과 칸을 두기로 한 경우)
    - `(친 문장, 제안, 확인 여부)` 짝의 목록을 실제로 만들어 개수를 단언하는 테스트가 존재한다
    - 시각 차이 계산 결과가 정수임을 단언하는 테스트가 존재한다
    - `uv run pytest` 전체가 0으로 끝난다
    - 스키마를 고쳤다면 `EVENT_SCHEMA_VERSION` 이 올라가 있고 `uv run pytest tests/test_event_log.py -x` 도 여전히 0으로 끝난다
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_reverse_verification.py -x</automated>
  </verify>
  <done>가짜 세션 기록 하나만으로 Phase 6이 필요로 하는 여섯 숫자가 전부 나온다. 안 나오는 것이 있다면 그것이 무엇인지 이름으로 드러난다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사건 기록 → 집계 | 기록된 값이 Phase 6의 판단 근거로 넘어가는 지점. 여기서 빠진 값은 어디서도 복원되지 않는다 |
| SQLite 파일 → 테스트 픽스처 | 저장된 JSON이 다시 객체로 복원되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-12 | Tampering | `tests/test_reverse_verification.py` | high | mitigate | 산출되지 않는 숫자가 있을 때 단언을 느슨하게 고쳐 초록불을 만드는 경로를 금지한다. 빠진 칸은 스키마를 고쳐 채우고 판 번호를 올린다. 이 검사가 무력해지면 Phase 6에서야 결손이 드러나고 그때는 실험을 다시 할 수 없다 |
| T-1-13 | Tampering | 응답 시간 짝짓기 | high | mitigate | 짝은 인과 칸으로만 맞춘다. 순번 인접에 기대는 계산은 여러 명이 동시에 입력하는 상황에서 틀린 값을 내며, 픽스처가 그 상황을 일부러 만들어 두 방식이 다른 답을 낸다는 것을 단언한다 |
| T-1-03 | Tampering | 픽스처의 저장·복원 경로 | high | mitigate | 픽스처는 실제 저장소를 거치며, 복원은 순수 JSON 파서 기반 검증으로만 이루어진다 |
</threat_model>

<verification>
- `uv run pytest tests/test_reverse_verification.py -x` 초록불 — 여섯 단언 전부 통과
- `uv run pytest` 전체 초록불
- 스키마를 고쳤다면 `EVENT_SCHEMA_VERSION` 이 올라가 있고 01-03의 스키마 테스트가 새 칸을 포함해 여전히 통과
</verification>

<success_criteria>
- 가짜 세션 기록 하나만으로 여섯 숫자가 전부 산출된다
- 여섯 개가 각각 별도 단언이라 하나가 실패하면 무엇이 빠졌는지 즉시 이름으로 드러난다
- 순번 인접이 인과를 뜻하지 않는다는 것이 픽스처와 단언으로 고정된다
- 필드 누락이 실험 이후가 아니라 실험 전에 드러난다 (이 단계에서 값이 가장 큰 결정의 실물)
</success_criteria>

## Artifacts this phase produces (이 계획이 만드는 것)

**새 테스트 심볼**

| 경로 | 심볼 |
|---|---|
| `tests/conftest.py` | `fake_session_log` 픽스처 — 두 플레이어가 번갈아 입력하고 여섯 종류가 전부 등장하며 재굴림 1회와 시계 진행 1회가 들어 있는 완결된 한 세션. 실제 저장소를 거쳐 돌려준다 |
| `tests/test_reverse_verification.py` | 여섯 숫자 각각의 단언, 인과 칸 대 순번 인접 비교 단언, 정수 밀리초 단언, 그리고 픽스처 자체를 확인하는 `fixture` 이름의 테스트 |

**조건부 변경**: 여섯 숫자 중 산출되지 않는 것이 있으면 `src/gptrpg/event_log/schema.py` 에 칸을 추가하고 `EVENT_SCHEMA_VERSION` 을 올린다. 그 경우 추가된 칸 이름을 SUMMARY에 적는다 — Phase 3·4가 그 이름을 참조한다.

<output>
Create `.planning/phases/01-rules-core-and-event-log/01-05-SUMMARY.md` when done
</output>
