---
phase: 12.3-creation-screen
plan: 02
type: execute
wave: 2
depends_on: ["12.3-01"]
files_modified:
  - src/gptrpg/web/creation_state.py
  - src/gptrpg/web/routes_creation.py
  - src/gptrpg/web/routes_events.py
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - tests/test_creation_screen_state.py
autonomous: true
requirements: [CHAR-06]

estimate:
  tokens: 88000
  raw_tokens: 59000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GM의 지목·되묻기·정리도 안내와 똑같이 사건 기록에 남아 네 탭이 같은 자리를 본다 (D-02)"
    - "네 GM 경로 전부가 같은 시점에 두 번 불려도 AI를 두 번 부르지 않는다 (D-12)"
    - "화면이 「지금 누구 차례인가 · 누가 아직 안 끝났나 · 인원이 확정됐나 · 명단이 잠겼나 · 누가 동의했나」를 폴링 응답에서 그대로 읽는다 — 화면이 기록을 접어 계산하지 않는다 (D-04)"
    - "룰북이 선언한 만들기 항목 목록을 전용 GET 경로 하나가 내려준다 — 폴링에도, AI 응답에도 싣지 않는다 (D-05)"
    - "사람이 직접 눌러 409를 받을 수 있는 만들기 경로의 `detail`이 화면까지 그대로 올라온다 (D-15)"
    - "AI 계약 위반(폴백 200)과 운영자 설정 문제(503)가 여전히 서로 다른 층으로 갈라져 나온다 (D-13)"
  artifacts:
    - src/gptrpg/web/creation_state.py
    - src/gptrpg/web/routes_events.py
    - tests/test_creation_screen_state.py
  key_links:
    - "`creation_state.unfinished_candidates`/`required_steps_filled` ← `routes_creation.py`와 `routes_events.py`가 **같은 함수를** 부른다 (판단이 두 곳에 생기지 않게)"
    - "`GameState` → `GameStateView` 만들기 칸 → `PollResponse` → `usePolling` → 화면"
    - "`Rulebook.creation_steps` → `GET /creation/steps` → `CreationStepView` → `types.ts`"
---

<objective>
**서버가 이미 하고 있는 판단을 화면이 볼 수 있는 자리로 내보내고, GM의 나머지 세 마디를 기록에 남긴다.**

12.3-01이 뚫은 줄기를 옆으로 넓힌다. 세 가지가 지금 화면 쪽으로 나오지 않는다:
- `nominate`/`follow-up`/`wrap-up`이 여전히 부른 사람에게만 답한다 (D-02)
- `GameStateView`(`routes_events.py:38`)에 만들기 칸이 하나도 없다 (D-04)
- `Rulebook.creation_steps`를 내려주는 경로가 없어 화면이 입력칸을 그릴 근거가 없다 (D-05)

**새 계산을 짓지 않는다.** `_unfinished_candidates`·`_required_steps_filled`가 이미 `routes_creation.py`에 있다 — 이 계획은 그것을 두 라우터가 같이 부를 수 있는 자리로 옮기고 폴링 응답에 싣는 일이다. Phase 12.2가 방금 고친 사고(화면이 스스로 계산해 서버와 다른 답을 보여줌)를 반복하지 않는다.

Purpose: 화면이 「무엇을 그려야 하는가」를 물어볼 창구를 만든다. 판단은 서버 한 자리에만 둔다.
Output: 공용 계산 모듈, 넓어진 `GameStateView`, 만들기 항목 선언 조회 경로, 그리고 화면이 쓸 API 표면.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-creation-screen/12.3-CONTEXT.md
@.planning/phases/12.3-creation-screen/12.3-PATTERNS.md
@.planning/phases/12.3-creation-screen/12.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GM의 나머지 세 마디를 사건으로 남기고 같은 중복 방지를 건다</name>
  <files>src/gptrpg/web/routes_creation.py, tests/test_creation_screen_state.py</files>
  <read_first>
    - `src/gptrpg/web/routes_creation.py:541-594` — `nominate_creation_speaker`(후보 목록 계산 · `CreationGmContractViolation` 폴백)
    - `src/gptrpg/web/routes_creation.py:595-678` — `creation_follow_up`(신원 대조가 맨 앞 · `required_steps_filled` 아래층 계산 · 폴백)
    - `src/gptrpg/web/routes_creation.py:679-775` — `wrap_up_creation`(`CreateCharacter` 재제출로 `character_created`를 갱신하는 방식)
    - `src/gptrpg/web/routes_creation.py`의 `_gm_dedupe_key` (12.3-01이 네 갈래를 전부 적어 둔 도우미)
    - `.planning/phases/12.3-creation-screen/12.3-PATTERNS.md` §세 가지 실패 계층 (D-13)
    - `tests/test_creation_screen_tracer.py` (12.3-01이 만든 중복 방지 시험의 모양)
  </read_first>
  <action>
    `nominate_creation_speaker`·`creation_follow_up`·`wrap_up_creation` 셋을 `announce_creation`과 **정확히 같은 모양으로** 고친다.

    각 경로에서 지킬 순서(12.3-01의 `announce_creation`이 이미 이 순서다):
    1. 기존 409/400/403 관문은 하나도 건드리지 않는다. **특히 `creation_follow_up`의 맨 앞 `read_identity` 신원 대조를 그대로 둔다** — 남의 캐릭터로 되묻기를 제출하는 경로를 막는 자리다.
    2. `_gm_dedupe_key(kind, actor.state, character_id)`를 계산한다. `nominate`는 `character_id=None`, `follow_up`은 `body.character_id`, `wrap_up`은 `None`.
    3. `actor.state.creation_gm_said`에 그 키가 있으면 **AI도 제공자 해석도 건너뛰고** 기록된 값으로 응답을 조립한다:
       · `nominate` -> `NominateSpeakerResponse(character_id=fold.target_character_id, say=fold.say)`
       · `follow_up` -> `CreationFollowUpResponse(needs_more=..., question=fold.say, required_steps_filled=...)`. `needs_more`는 기록된 `say`가 비어 있지 않은지로 판정한다 — 되물을 것이 없을 때는 애초에 사건을 남기지 않기 때문이다(아래 ④ 참조). `required_steps_filled`는 **항상 지금 상태로 다시 계산한다**(기록된 값이 아니다 — 그 사이에 값이 더 들어왔을 수 있다)
       · `wrap_up` -> 기록된 `say` + `state.created_characters`에서 다시 조립한 `intros`
    4. 키가 없으면 지금처럼 제공자를 풀고(`_AGENT_RESOLUTION_ERRORS` -> 503 그대로) AI를 부른다. `CreationGmContractViolation` 폴백 분기도 **그대로 유지한다** — 폴백으로 나온 값도 사건에 남는다. 이 세 갈래(폴백 200 / 503 / 그 밖)가 D-13이 화면에서 가르는 세 문구의 발생 지점이므로 계층을 합치지 않는다.
    5. `actor.submit(CreationGmSpoke(kind=..., say=..., target_character_id=..., dedupe_key=key))`로 기록하고 응답 모델에 `seq: int`를 더한다.
       · `nominate` -> `target_character_id`는 지목된 캐릭터
       · `follow_up` -> `target_character_id`는 `body.character_id`
       · `wrap_up` -> `target_character_id`는 `None`

    ④ **`follow_up`에서 되물을 것이 없을 때(`needs_more=False`)는 사건을 남기지 않는다.** GM이 아무 말도 안 한 것이므로 대화 줄기에 남길 말이 없다. 다만 중복 방지는 여전히 필요하므로 이 경우 `say=""`인 사건을 남기는 대신 **그대로 응답만 하고 넘어간다** — 되묻기는 사람이 그 사람의 차례에 한 번 누르는 조작이고, 다시 눌러도 값이 안 바뀐 동안은 같은 판정이 나오므로 비용이 한 번 더 드는 것이 전부다. 이 판단(왜 여기만 사건을 안 남기는가)을 라우트 도크스트링에 적는다.

    ⑤ `wrap_up_creation`의 `CreateCharacter` 재제출 루프는 **그대로 둔다.** 한 줄 소개는 이미 `character_created` 사건에 남고 있고(CHAR-03), 이 계획이 더하는 `creation_gm_spoke`는 GM이 **말한 문장**만 담는다. 두 곳에 같은 소개 문자열을 중복 저장하지 않는다.

    ⑥ `tests/test_creation_screen_state.py`를 새로 만들어 세 경로 각각에 대해 다음을 단언한다:
       · 한 번 부르면 `creation_gm_spoke` 사건이 하나 생기고 `kind`가 그 경로의 이름이다
       · 같은 상태에서 다시 부르면 사건 개수가 늘지 않고 응답의 `say`/`seq`가 같으며 대역 제공자 호출 횟수도 안 는다
       · `nominate`는 후보 목록이 실제로 바뀐 뒤(한 사람이 완성된 뒤)에는 **새 사건을 남긴다** — 중복 방지가 진행을 막지 않는다
       · 쿠키를 든 브라우저가 남의 `character_id`로 `follow-up`을 내면 여전히 403이고 사건이 하나도 안 생긴다
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_screen_state.py -x</automated>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_gm.py tests/test_creation_flow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /events`에 `creation_gm_spoke` 사건이 `kind` 값 `announce`/`nominate`/`wrap_up` 셋 다로 나타나는 시험이 통과한다
    - 「같은 상태에서 두 번 부르면 사건 개수가 안 는다」를 세 경로 각각에 대해 단언하는 시험 함수가 `tests/test_creation_screen_state.py`에 있다
    - 「후보 목록이 바뀌면 nominate가 새 사건을 남긴다」를 단언하는 시험이 있다
    - `grep -c '_AGENT_RESOLUTION_ERRORS' src/gptrpg/web/routes_creation.py` 가 이 계획 전과 같거나 크다 — 503 계층이 지워지지 않았다
    - `grep -c 'CreationGmContractViolation' src/gptrpg/web/routes_creation.py` 가 이 계획 전과 같거나 크다 — 폴백 계층이 지워지지 않았다
    - `nominate`/`follow-up`/`wrap-up` 세 응답 JSON 전부에 `seq` 키가 있다 (`follow-up`은 되물을 것이 있을 때)
  </acceptance_criteria>
  <done>GM이 하는 네 마디 전부가 기록에 남고, 어느 탭이 눌러도 AI가 그 시점에 한 번만 돈다.</done>
</task>

<task type="auto">
  <name>Task 2: 만들기 진행 상태를 폴링 응답에 싣는다 (D-04)</name>
  <files>src/gptrpg/web/creation_state.py, src/gptrpg/web/routes_creation.py, src/gptrpg/web/routes_events.py, frontend/src/api/types.ts, tests/test_creation_screen_state.py</files>
  <read_first>
    - `src/gptrpg/web/routes_events.py:38-63` — `GameStateView` 필드 나열과 「값이 한 자리에만 있어야 어긋남이 구조적으로 불가능하다」 도크스트링
    - `src/gptrpg/web/routes_events.py:64-145` — `PollResponse.check_calculations`가 Phase 12.2에서 병렬 목록으로 붙은 방식과 `poll_events`가 값을 채우는 자리
    - `src/gptrpg/web/routes_creation.py:424-470` — `_unfinished_candidates`·`_transcript_for`·`_required_steps_filled` (옮길 대상)
    - `src/gptrpg/session_actor/actor.py:691-731` — 액터 쪽의 같은 계산(`_unfinished_creation_candidates`)과 **왜 두 층에 따로 있는지**를 적은 도크스트링. `session_actor`가 `web`을 import할 수 없다는 층 계약(`.importlinter` contract:2)이 근거다
    - `.importlinter` contract:2 — `gptrpg.cli | gptrpg.web`이 최상층이므로 `web` 안에서 서로 import하는 것은 허용된다
    - `frontend/src/api/types.ts:175-200` — `GameStateView` TS 미러
    - `.planning/phases/12.2-check-total/12.2-CONTEXT.md` — 화면이 스스로 계산해 서버와 어긋난 사고를 방금 고친 자리
  </read_first>
  <action>
    ① `src/gptrpg/web/creation_state.py`를 새로 만들고 `routes_creation.py`의 `_unfinished_candidates`와 `_required_steps_filled`를 **그대로 옮긴다**(이름에서 앞 밑줄을 떼어 `unfinished_candidates`·`required_steps_filled`로 공개한다). 계산 내용은 한 글자도 바꾸지 않는다. `routes_creation.py`는 이제 이 모듈에서 import해 쓴다 — 같은 판단이 두 벌 생기는 순간이 이 계획의 유일한 위험이므로 복사하지 않는다. `_transcript_for`는 GM 프롬프트 전용이라 옮기지 않는다.
       모듈 도크스트링에 적을 것: 이 모듈이 존재하는 이유는 `routes_events.py`가 `routes_creation.py`의 비공개 도우미를 찔러 쓰지 않게 하려는 것이고, 액터 쪽 `_unfinished_creation_candidates`가 여전히 따로 있는 이유는 층 계약상 `session_actor`가 `web`을 import할 수 없기 때문이라는 것.

    ② `routes_events.py`의 `GameStateView`에 만들기 칸을 더한다. 값은 전부 `game_state`에서 나오거나 `creation_state`의 함수가 계산한다:
       · `party_size_fixed: int | None`
       · `creation_rulebook_id: str | None`
       · `party_roster: list[str] | None` — `None`과 빈 목록의 뜻이 다르다는 `GameState` 규약을 그대로 옮긴다
       · `creation_unfinished_character_ids: list[str]` — `unfinished_candidates(game_state)` 그대로
       · `creation_current_speaker_id: str | None` — `creation_gm_said` 중 `kind == "nominate"`인 것 가운데 `seq`가 가장 큰 것의 `target_character_id`. 그 사람이 이미 완성됐으면 `None`(차례가 끝났다)
       · `creation_characters: list[CreationCharacterView]` — 완성된 캐릭터 목록. 항목 칸은 `character_id` · `display_name` · `consented: bool`(`game_state.creation_consents`에서) · `required_steps_filled: bool`
       · `creation_reopened_step_ids: list[str]` — 지금 다시 열려 있는 `step_id` 목록(자기 것만 고르는 일은 화면이 `character_id`로 한다). 항목 칸은 `character_id` · `step_id`인 `CreationReopenedStepView`로 낸다
       · `creation_step_values: list[CreationStepValueView]` — **접힌 뒤의** 항목 값 전부. 항목 칸은 `character_id` · `step_id` · `kind` · `text_value` · `picked` · `axis_values` · `rolls` · `seq`. **`browser_id`는 싣지 않는다**(`CreationStepFold`에는 있지만 화면이 쓸 일이 없고, 실으면 남의 식별자가 네 탭에 뿌려진다 — T-12.3-05).
         이 칸이 필요한 이유를 도크스트링에 적는다: D-09가 「확정한 항목이 목록으로 보이고 각 항목 옆에 고치기가 있다」를 요구하는데, 화면이 사건에서 직접 접으면 「같은 키로 다시 오면 나중 것이 이긴다」는 규칙이 서버와 화면 두 곳에 생긴다. 그것이 Phase 12.2가 방금 고친 사고다 — **접는 일은 서버만 한다**
       · `creation_host_claimed: bool` — 방장이 잡혔는지 **여부만**. **`creation_host_browser_id` 값 자체는 절대 싣지 않는다** — 다른 브라우저의 식별자를 네 탭 전부에 뿌리면 그 값을 사칭할 수 있게 된다(T-12.3-05). 「내가 방장인가」는 12.3-03이 만드는 전용 경로가 **부른 사람에게만** 답한다
       `required_steps_filled`를 계산하려면 룰북이 필요하다 — `game_state.creation_rulebook_id`가 `None`이거나 `get_rulebook`이 `UnknownRulebook`을 내면 그 칸을 `False`로 두고 폴링을 절대 500으로 만들지 않는다(폴링이 깨지면 세션 전체가 안 열린다).
       새 칸마다 `GameStateView` 도크스트링과 같은 어조로 「왜 이 값이 서버에서 오는가」를 한 줄씩 적는다.

    ③ `poll_events`에서 위 칸들을 채운다. `check_calculations`가 이미 그렇듯 **사건 객체는 한 글자도 바꾸지 않는다** — 만들기 상태는 `state` 안의 파생값이다.

    ④ `frontend/src/api/types.ts`의 `GameStateView`에 같은 이름·같은 타입으로 칸을 더하고 `CreationCharacterView`·`CreationReopenedStepView`·`CreationStepValueView` interface를 선언한다. 스네이크 케이스를 그대로 쓴다(이 저장소는 변환하지 않는다).

    ⑤ 시험을 `tests/test_creation_screen_state.py`에 더한다:
       · 인원 확정 → 한 사람이 항목 둘 제출 → `GET /events`의 `state`에 `party_size_fixed`가 채워지고 `creation_unfinished_character_ids`에 그 사람이 들어 있다
       · `nominate` 뒤 `creation_current_speaker_id`가 지목된 사람이고, 그 사람이 완성되면 `None`으로 돌아간다
       · 완성 뒤 `creation_characters`에 그 캐릭터가 `display_name`과 함께 있고 `consented`가 `False`다
       · 같은 `(character_id, step_id)`로 값을 두 번 제출하면 `creation_step_values`에 그 키가 **하나만** 있고 나중 값이 실려 온다 — 접는 일은 서버가 한다
       · `creation_step_values` 항목 어느 칸에도 `browser_id`가 없다
       · **`GET /events` 응답 본문 어디에도 `browser_id` 값이 상태 칸으로 실리지 않는다** — `state` 딕셔너리의 키에 `host_browser_id`가 없다
       · 룰북 없는 세션(인원 확정 전)에도 폴링이 200이고 만들기 칸이 전부 비어 있는 기본값이다
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_screen_state.py -x</automated>
    <automated>.venv/bin/python -m lint_imports</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/gptrpg/web/creation_state.py` 가 존재하고 `unfinished_candidates`·`required_steps_filled`를 공개한다
    - `grep -c 'def _unfinished_candidates\|def _required_steps_filled' src/gptrpg/web/routes_creation.py` 가 `0`이다 — 계산이 두 벌로 남지 않았다
    - `grep -c 'creation_state' src/gptrpg/web/routes_events.py` 가 `1` 이상이고 `grep -c 'creation_state' src/gptrpg/web/routes_creation.py` 가 `1` 이상이다 — 두 라우터가 같은 함수를 부른다
    - `.venv/bin/python -c "from gptrpg.web.routes_events import GameStateView; ks=set(GameStateView.model_fields); assert {'party_size_fixed','party_roster','creation_unfinished_character_ids','creation_current_speaker_id','creation_characters','creation_host_claimed','creation_rulebook_id','creation_reopened_step_ids','creation_step_values'} <= ks; assert not any('browser_id' in k for k in ks)"` 가 종료 코드 0이다
    - `.venv/bin/python -m lint_imports` 가 종료 코드 0이다
    - `npm --prefix frontend run build` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>화면이 만들기 진행 상태를 폴링 응답 한 곳에서 읽을 수 있고, 그 계산은 서버 한 자리에만 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 룰북 만들기 항목 선언을 내려주는 전용 경로와 화면 API 표면</name>
  <files>src/gptrpg/web/routes_creation.py, frontend/src/api/types.ts, frontend/src/api/client.ts, tests/test_creation_screen_state.py</files>
  <read_first>
    - `src/gptrpg/rules_core/rulebook.py:190-260` — `CreationStepDecl`의 열여섯 칸과 일곱 `kind`
    - `src/gptrpg/rulebooks/dungeonworld_like.py:123-190` · `openquest.py:130-172` · `cairn.py:85-120` — 세 룰북이 실제로 쓰는 다섯 `kind`(`pick_one`·`free_text`·`place_fixed_values`·`allocate_points`·`roll_to_fill`·`derive`)와 각 `kind`가 채우는 칸
    - `src/gptrpg/web/routes_characters.py:179-200` — 사건을 만들지 않는 순수 GET(`get_characters`)의 모양
    - `frontend/src/api/client.ts:40-107` — `getJson`/`postJson` 공용 래퍼와 **`selectCharacter`가 왜 그것을 안 쓰는지**(D-15의 선례)
    - `src/gptrpg/web/routes_creation.py:102-220` — `FixPartySizeRequest`·`CompleteCreationStepRequest`·`AxisValueBody`의 요청 본문 모양
    - `src/gptrpg/web/routes_creation.py:322-423` — `LockPartyRosterRequest`·`InterjectionRequest`
  </read_first>
  <action>
    ① `routes_creation.py`에 순수 GET을 하나 더한다:
       `GET /sessions/{session_id}/creation/steps?rulebook_id=...` -> `list[CreationStepView]`
       · `CreationStepDecl`을 얇게 감싸는 pydantic 응답 모델 `CreationStepView`를 만든다. `CreationStepDecl`의 모든 칸을 그대로 옮긴다: `step_id`·`kind`·`label`·`required`·`provides_display_name`·`axis_names`·`options`·`pick_count`·`fixed_values`·`point_budget`·`per_target_max`·`dice_expr`·`derive_base_axis`·`derive_multiplier`·`derive_offset`·`depends_on`·`default_from`. 칸을 골라 내리지 않는다 — 화면이 어떤 `kind`의 입력칸을 그리려면 그 `kind`가 쓰는 칸이 전부 필요하다.
       · `get_rulebook(rulebook_id)`가 `UnknownRulebook`을 내면 400 + 그 예외 문자열을 `detail`로.
       · **진행 상태를 섞지 않는다**(D-05 경계) — 이 응답에는 어느 캐릭터가 무엇을 채웠는지가 들어가지 않는다. 그 값은 D-04의 폴링에 있다.
       · `rulebook_id` 기본값은 다른 만들기 경로와 같은 `DUNGEONWORLD_LIKE_ID`.
       도크스트링에 D-05의 근거를 적는다: 이 목록은 세션 중에 안 바뀌므로 1.5초 폴링에 실으면 안 바뀌는 것을 네 명에게 계속 나르고, AI 응답에 실으면 AI가 안 돌 때 입력칸을 그릴 근거조차 없어진다.

    ② `frontend/src/api/types.ts`에 `CreationStepView`를 서버 칸 이름 그대로 미러링한다. `kind`는 일곱 값의 유니온 리터럴로 적는다(`"free_text" | "pick_one" | "pick_many" | "allocate_points" | "place_fixed_values" | "roll_to_fill" | "derive"`) — 화면이 `kind`로 분기할 때 tsc가 빠진 갈래를 잡아 준다.

    ③ `frontend/src/api/client.ts`에 이 계획 시점에 **실제로 존재하는** 만들기 경로의 함수를 추가한다. `/creation/host`와 `/creation/consent`는 여기 넣지 않는다 — 12.3-03이 그 둘을 만들면서 함께 붙인다.
       공용 `getJson`으로 충분한 것: `fetchCreationSteps(sessionId, rulebookId)`
       **`selectCharacter` 방식(응답 본문의 `detail`을 읽어 `ApiError.detail`에 담기)을 복제해야 하는 것** — 사람이 직접 눌러 400/409/403을 받을 수 있는 경로들이다(D-15):
       · `fixPartySize(sessionId, playerCharacterCount, rulebookId)` -> `SeqResponse`
       · `completeCreationStep(sessionId, body)` -> `SeqResponse` (`body`는 `character_id`·`browser_id`·`step_id`·`rulebook_id`·`text_value?`·`picked?`·`axis_values?`)
       · `completeCreation(sessionId, body)` -> `CreationCompleteResponse`
       · `recordInterjection(sessionId, body)` -> `SeqResponse`
       · `nominateCreationSpeaker(sessionId, rulebookId)` -> `NominateSpeakerResponse`
       · `creationFollowUp(sessionId, characterId, rulebookId)` -> `CreationFollowUpResponse`
       · `wrapUpCreation(sessionId, rulebookId)` -> `WrapUpCreationResponse`
       `detail` 읽기 코드를 여덟 벌 복붙하지 말고, `selectCharacter`가 하던 일을 `postJsonWithDetail<T>(url, body)` 하나로 뽑아 전부 그것을 쓰게 한다. 12.3-01이 만든 `announceCreation`도 이 함수로 갈아 끼운다. 이 함수의 도크스트링에 「왜 공용 `postJson`으로는 안 되는가」를 `selectCharacter`가 적었던 이유 그대로 옮긴다(서버 `detail`을 버리면 화면이 「보내기를 눌러도 아무 일도 안 일어나는」 상태가 된다).
       모든 응답 타입(`SeqResponse`·`CreationCompleteResponse`·`NominateSpeakerResponse`·`CreationFollowUpResponse`·`WrapUpCreationResponse`·`CharacterIntroBody`)을 `types.ts`에 서버 pydantic 모델과 칸 이름을 한 글자도 다르지 않게 선언한다.

    ④ 시험을 `tests/test_creation_screen_state.py`에 더한다:
       · `GET /creation/steps?rulebook_id=dungeonworld_like`가 200이고, 돌려준 목록의 `step_id` 순서가 `DUNGEONWORLD_CREATION_STEPS` 선언 순서와 같다
       · 같은 경로를 `cairn`으로 부르면 `roll_to_fill` 항목의 `dice_expr`가 `"3d6"`/`"1d6"`으로 실려 온다 — 화면이 주사위 개수를 알 수 있다
       · `openquest`로 부르면 `allocate_points` 항목의 `point_budget`·`per_target_max`가 실려 온다
       · 모르는 `rulebook_id`면 400이고 `detail`이 비어 있지 않다
       · 이 응답 어디에도 캐릭터별 진행 값(`creation_step_values` 유래 값)이 없다 (D-05 경계)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_screen_state.py -x</automated>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend test</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/sessions/{id}/creation/steps?rulebook_id=cairn` 이 200이고 `abilities` 항목의 `dice_expr`가 `"3d6"`이다
    - `.venv/bin/python -c "from gptrpg.web.routes_creation import CreationStepView; from gptrpg.rules_core.rulebook import CreationStepDecl; import dataclasses; assert {f.name for f in dataclasses.fields(CreationStepDecl)} <= set(CreationStepView.model_fields)"` 가 종료 코드 0이다 — 선언 칸이 하나도 빠지지 않았다
    - `grep -c 'postJsonWithDetail' frontend/src/api/client.ts` 가 `8` 이상이다 (정의 + 사람이 누르는 경로 전부)
    - `grep -c 'export function fetchCreationSteps' frontend/src/api/client.ts` 가 `1`이다
    - `npm --prefix frontend run build` 가 종료 코드 0이다
    - 모르는 룰북에 400 + `detail`을 확인하는 시험이 통과한다
  </acceptance_criteria>
  <done>화면이 「무슨 항목이 있고 각각 어떤 방식으로 채우는가」를 한 번의 GET으로 알 수 있고, 사람이 누르는 모든 만들기 경로의 서버 거절 사유가 화면까지 그대로 올라온다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (12.3-02)

**새 파일:** `src/gptrpg/web/creation_state.py` · `tests/test_creation_screen_state.py`
**새 서버 함수** (`src/gptrpg/web/creation_state.py`): `unfinished_candidates` · `required_steps_filled` (`routes_creation.py`의 `_unfinished_candidates`/`_required_steps_filled`에서 이름을 바꿔 옮긴 것)
**새 HTTP 경로:** `GET /api/sessions/{session_id}/creation/steps`
**새 응답 모델** (`src/gptrpg/web/routes_creation.py`): `CreationStepView`
**새 `GameStateView` 칸** (`src/gptrpg/web/routes_events.py`): `party_size_fixed` · `creation_rulebook_id` · `party_roster` · `creation_unfinished_character_ids` · `creation_current_speaker_id` · `creation_characters` · `creation_reopened_step_ids` · `creation_step_values` · `creation_host_claimed`
**새 응답 모델** (`src/gptrpg/web/routes_events.py`): `CreationCharacterView` · `CreationReopenedStepView` · `CreationStepValueView`
**바뀐 응답 모델:** `NominateSpeakerResponse`·`CreationFollowUpResponse`·`WrapUpCreationResponse`에 `seq` 추가
**새 TypeScript 타입** (`frontend/src/api/types.ts`): `CreationStepView` · `CreationStepKind` · `CreationCharacterView` · `CreationReopenedStepView` · `CreationStepValueView` · `SeqResponse` · `CreationCompleteResponse` · `NominateSpeakerResponse` · `CreationFollowUpResponse` · `WrapUpCreationResponse` · `CharacterIntroBody`
**새 TypeScript 함수** (`frontend/src/api/client.ts`): `postJsonWithDetail` · `fetchCreationSteps` · `fixPartySize` · `completeCreationStep` · `completeCreation` · `recordInterjection` · `nominateCreationSpeaker` · `creationFollowUp` · `wrapUpCreation`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `GET /events` | 인증 없이 세션 전체 상태를 내려준다. 이 계획이 이 응답에 새 칸을 더하므로 **무엇을 싣는가**가 곧 무엇이 공개되는가다 |
| 브라우저 → `GET /creation/steps` | 룰북 **선언**만 내려준다. 세션 데이터가 섞이면 안 된다 |
| 브라우저 → GM 네 경로 | 사람이 눌러 AI 비용을 발생시키는 자리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-05 | Information Disclosure | `GameStateView` 새 칸 | high | mitigate | `creation_host_browser_id` 값을 폴링에 싣지 않고 `creation_host_claimed: bool`만 싣는다. Task 2의 승인 기준이 `GameStateView.model_fields` 어느 이름에도 `browser_id`가 없음을 실행으로 확인한다. 다른 브라우저의 `browser_id`가 네 탭에 뿌려지면 그 값을 그대로 넣어 남의 항목을 제출할 수 있다 |
| T-12.3-04 | Denial of Service | `nominate`/`follow_up`/`wrap_up` → `creation_gm` 제공자 | medium | mitigate | 세 경로 전부에 `_gm_dedupe_key` 조회를 AI 호출 **앞**에 둔다(D-12). Task 1의 승인 기준이 대역 제공자 호출 횟수로 이것을 확인한다 |
| T-12.3-09 | Denial of Service | `poll_events`의 새 계산 | high | mitigate | 룰북을 못 찾거나 만들기 상태가 반쪽이어도 폴링이 절대 500을 내지 않는다 — `required_steps_filled`를 `False`로 두고 넘어간다. 폴링이 깨지면 그 세션의 네 탭 전부가 화면을 못 연다 |
| T-12.3-01 | Spoofing | `creation_follow_up`의 신원 대조 | high | mitigate | 맨 앞 `read_identity` 대조를 그대로 유지한다. Task 1의 승인 기준이 「남의 `character_id`로 낸 되묻기는 403이고 사건이 안 생긴다」를 시험으로 고정한다 |
| T-12.3-10 | Information Disclosure | `GET /creation/steps` | low | accept | 룰북 선언은 이미 저장소에 공개된 데이터이고 세션 값을 담지 않는다. 세션 스코프 인증을 새로 만들지 않는다 — 이 저장소의 다른 GET(`/characters`)과 같은 결이다 |

**공급망:** 이 계획도 npm/pip/cargo 설치를 추가하지 않는다 — `T-{phase}-SC` 항목 없음.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` 전체가 통과한다
- `.venv/bin/python -m lint_imports` 가 통과한다
- `.venv/bin/python -m ruff check src tests` 가 통과한다
- `npm --prefix frontend run build` 와 `npm --prefix frontend test` 가 통과한다
</verification>

<success_criteria>
1. GM의 네 마디 전부가 사건으로 남고 중복 호출이 서버에서 막힌다
2. 만들기 진행 상태 판단이 서버 한 자리에만 있고 폴링이 그것을 내려준다
3. 룰북 만들기 항목 선언을 전용 GET 하나가 내려주고 진행 상태를 섞지 않는다
4. 사람이 누르는 만들기 경로의 서버 거절 사유가 `ApiError.detail`까지 살아서 올라온다
</success_criteria>

<output>
Create `.planning/phases/12.3-creation-screen/12.3-02-SUMMARY.md` when done
</output>
