---
phase: 12.3-creation-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gptrpg/event_log/schema.py
  - src/gptrpg/rules_core/reducer.py
  - src/gptrpg/session_actor/actor.py
  - src/gptrpg/web/routes_creation.py
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/App.tsx
  - frontend/src/screens/CreationScreen.tsx
  - frontend/src/screens/CharacterSelect.tsx
  - frontend/src/screens/SessionScreen.tsx
  - frontend/src/screens/Notices.tsx
  - frontend/src/panes/StatusPane.tsx
  - frontend/src/session/creationView.ts
  - frontend/src/session/creationView.test.ts
  - frontend/src/labels.ts
  - tests/test_creation_screen_tracer.py
autonomous: false
requirements: [CHAR-06]

estimate:
  tokens: 96000
  raw_tokens: 64000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "세션 링크를 연 브라우저에 자기 캐릭터가 없으면 캐릭터 만들기 화면이 뜬다 — 캐릭터 고르기 화면을 거치지 않는다 (D-10)"
    - "GM 안내를 한 번 부르면 `creation_gm_spoke` 사건이 기록에 남고, 같은 세션의 **다른** 브라우저가 폴링만으로 그 문장을 본다 (D-02)"
    - "같은 시점에 GM 안내를 다시 부르면 AI를 다시 호출하지 않고 이미 기록된 문장을 그대로 돌려준다 (D-12)"
    - "판 11 사건이 하나라도 있는 세션이 폴링에서 `UnknownEventType`을 맞지 않는다 (D-02 저장소 규율)"
    - "서버 작업과 화면 작업이 같은 계획 안에서 함께 끝난다 — 어느 한쪽만으로는 이 줄기가 성립하지 않는다 (D-01)"
    - statement: "두 개의 브라우저 탭으로 같은 세션 링크를 열고 한쪽에서 GM 안내를 부르면, 다른 탭에도 그 문장이 1.5초 안에 나타난다"
      verification: backstop
  artifacts:
    - src/gptrpg/event_log/schema.py
    - src/gptrpg/rules_core/reducer.py
    - frontend/src/screens/CreationScreen.tsx
    - frontend/src/session/creationView.ts
    - tests/test_creation_screen_tracer.py
  key_links:
    - "`schema.py` 새 사건 dataclass → `GameEvent` 유니온 → `_KNOWN_EVENT_TYPES` → `actor._EVENT_CLASSES` → `reducer.apply_event` 분기 → `types.ts` 미러 (다섯 자리를 같은 커밋에서)"
    - "`announce_creation` → `actor.submit(CreationGmSpoke)` → `EventStore` → `GET /events` → `usePolling` → `CreationScreen`"
    - "`App.tsx` Gate → `fetchMyCharacter().selected` → `CreationScreen` 또는 `SessionScreen`"
---

<objective>
**이 단계에서 처음으로 「브라우저가 부른 만들기 조작이 사건으로 남고 다른 탭이 그것을 본다」는 한 줄기를 끝에서 끝까지 뚫는다.**

지금 이 줄기는 아예 없다. `announce`/`nominate`/`follow-up`/`wrap-up` 네 경로가 `actor.submit`을 부르지 않아 GM의 말이 부른 사람에게만 돌아가고(12.3-CONTEXT §이 논의가 밝힌 것 ①), 화면 쪽에는 만들기 코드가 0줄이다.

이 계획은 그중 **안내(`announce`) 하나만** 골라 판 11 사건 → 리듀서 → 폴링 → 화면까지 실제로 흐르게 한다. 나머지 GM 세 경로·상태 창구·동의·방장·항목 조작은 이 줄기가 증명된 뒤 12.3-02~05가 옆으로 넓힌다.

**트레이서지만 시제품이 아니다** — 여기서 만드는 사건 형식·리듀서 분기·입장 갈래는 그대로 최종 코드로 남는다.

Purpose: 아키텍처(사건 판 11 · 폴링 재사용 · 입장 갈래)를 한 번의 커밋으로 검증해, 열 층을 다 쌓은 뒤에 막다른 길을 발견하는 사고를 막는다.
Output: 판 11 사건 세 종류의 선언, 안내 경로의 사건화와 중복 방지, 만들기 화면의 최소 뼈대, 그리고 그 줄기를 지키는 시험.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-creation-screen/12.3-CONTEXT.md
@.planning/phases/12.3-creation-screen/12.3-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: 사건 판 11의 모양을 확정한다 (one-way)</name>
  <reversibility rating="one-way">사건 판을 11로 올리고 그 판으로 기록된 세션이 생기면 되돌릴 수 없다 — 되돌리려면 재생·재구성 경로를 함께 손봐야 하고, 이미 쌓인 기록(사장님의 현재 세션, 저장소에 커밋된 세션1 픽스처)의 이전이 따라온다.</reversibility>
  <read_first>
    - `.planning/phases/12.3-creation-screen/12.3-CONTEXT.md` §D-02 저장소 규율 · §Claude's Discretion 「GM의 말을 몇 가지 사건으로 나누는가」·「동의를 사건으로 어떻게 담는가」
    - `src/gptrpg/event_log/schema.py:1-30` (`EVENT_SCHEMA_VERSION = 10` 도크스트링의 판 규약)
    - `src/gptrpg/event_log/schema.py:513-650` (판 9가 더한 만들기 사건 다섯 + `GameEvent` 유니온 + `_KNOWN_EVENT_TYPES`)
  </read_first>
  <decision>GM의 말을 사건 기록에 남길 때(D-02) **사건 종류를 몇 개로 나눌 것인가** — 그리고 그 결정으로 `EVENT_SCHEMA_VERSION`을 10에서 11로 올린다.</decision>
  <context>
    12.3-CONTEXT.md가 이것을 명시적으로 계획 재량으로 남겼다(§Claude's Discretion 「GM의 말을 몇 가지 사건으로 나누는가 — 안내·지목·되묻기·정리를 한 종류로 묶을지 넷으로 나눌지」).

    **왜 사람이 골라야 하는가:** 이 판으로 기록된 세션이 하나라도 생기면 그 사건 형식이 **공개된 계약**이 된다. 되돌리려면 재생·재구성 경로를 함께 손봐야 하고, 이미 쌓인 기록(현재 세션, 저장소에 커밋된 세션1 픽스처)의 이전이 따라온다. 자동으로 골라 넘길 결정이 아니다.

    **두 안이 공통으로 더하는 것 둘**(어느 쪽을 골라도 같다):
    · `CreationConsentRecorded` (`creation_consent_recorded`) — 칸: `character_id` · `browser_id` · `agree: bool` · `reopened_step_id: str | None`. 동의(`agree=True`)와 「아니요 + 항목 다시 열기」(`agree=False`)를 **한 종류로** 담는다. 지금 액터 메모리에 흩어져 있는 `_creation_consents`와 `_reopened_creation_steps` **둘 다** 이 한 사건에서 접힌다 — 하나만 사건으로 옮기면 재시작 뒤 두 값이 서로 어긋난다.
    · `CreationHostClaimed` (`creation_host_claimed`) — 칸: `browser_id` · `reason: Literal["first","succession"]` · `previous_browser_id: str | None`. D-11의 방장이 기록에 남는 자리.

    셋을 여기 한꺼번에 선언하는 이유는 **판 올림이 한 번이어야** 하기 때문이다 — 12.3-02~05는 이 선언을 소비만 하고 `schema.py`를 다시 열지 않는다.
  </context>
  <options>
    <option id="one-event">
      <name>GM의 말을 한 종류로 묶는다 — `CreationGmSpoke` 하나 (권장)</name>
      <pros>칸: `kind: Literal["announce","nominate","follow_up","wrap_up"]` · `say: str` · `target_character_id: str | None` · `dedupe_key: str`. 네 경로가 만드는 것이 전부 「GM이 한 말 한 줄」로 같은 모양이다. 리듀서 분기 하나 · TS 미러 하나 · 화면 렌더 하나로 끝나고, 「사건 종류를 늘리면 다섯 자리를 같은 커밋에」 규율을 지킬 표면이 3분의 1로 줄어든다. D-12의 중복 방지도 `dedupe_key` 한 칸으로 네 갈래를 다 덮는다</pros>
      <cons>「어떤 종류의 말인가」가 타입이 아니라 `kind` 칸 값이라, 나중에 종류마다 서로 다른 칸이 필요해지면 선택 칸이 늘어난다(예: 지목에만 있는 칸이 정리에는 `None`으로 남는다)</cons>
    </option>
    <option id="four-events">
      <name>넷으로 나눈다 — `CreationAnnounced`/`CreationSpeakerNominated`/`CreationFollowUpAsked`/`CreationWrappedUp`</name>
      <pros>종류마다 자기 칸만 갖는다. 지목에만 `target_character_id`가 있고 정리에는 없다 — pydantic이 형식을 더 좁게 강제한다. 나중에 한 종류만 칸을 늘려도 다른 셋이 영향받지 않는다</pros>
      <cons>다섯 자리(`schema.py` dataclass · `GameEvent` 유니온 · `_KNOWN_EVENT_TYPES` · `_EVENT_CLASSES` · 리듀서 분기) × 네 종류 = 스무 곳을 같은 커밋에 맞춰야 한다. 이 저장소에서 그 누락이 세 번 사고를 냈고 `reducer.py:244-282`에 그 주석이 세 번 남아 있다. TS 미러와 화면 렌더도 네 벌이 된다</cons>
    </option>
  </options>
  <resume-signal>`one-event` 또는 `four-events` 중 하나를 고른다. 고른 뒤 Task 1이 그 모양대로 다섯 자리를 한 커밋에 낸다.</resume-signal>
  <acceptance_criteria>
    - 사람이 두 안 중 하나를 명시적으로 골랐다
    - 고른 안과 그 이유가 `12.3-01-SUMMARY.md`에 적혔다
    - Task 1 이후 `grep -c 'EVENT_SCHEMA_VERSION = 11' src/gptrpg/event_log/schema.py` 가 `1`이다
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 1: 판 11 사건 세 종류를 다섯 자리에 동시에 낸다</name>
  <files>src/gptrpg/event_log/schema.py, src/gptrpg/rules_core/reducer.py, src/gptrpg/session_actor/actor.py, frontend/src/api/types.ts</files>
  <read_first>
    - `src/gptrpg/event_log/schema.py:513-650` — 판 9 추가분 다섯(`PartySizeFixed`~`PartyRosterLocked`)의 dataclass 모양, `GameEvent` 유니온, `_KNOWN_EVENT_TYPES` 열여섯
    - `src/gptrpg/event_log/schema.py:19-30` — `EVENT_SCHEMA_VERSION` 도크스트링(판 9→10이 어떤 어조로 적혔는지)
    - `src/gptrpg/rules_core/reducer.py:150-169` — `GameState`의 만들기 칸 넷(`party_size_fixed`·`creation_step_values`·`created_characters`·`party_roster`)의 docstring 어조
    - `src/gptrpg/rules_core/reducer.py:397-467` — `apply_event`의 만들기 분기 다섯. 특히 `creation_interjection`(429-434행)의 「최소 갱신인데도 분기가 있어야 하는」 주석
    - `src/gptrpg/session_actor/actor.py:425-442` — `_EVENT_CLASSES` 표
    - `frontend/src/api/types.ts:26-192` — 사건 interface + `GameEvent` 유니온 + `GameStateView`
    - `.planning/phases/12.3-creation-screen/12.3-PATTERNS.md` §Shared Patterns 「사건 종류 추가 = 4곳 동시 수정」
  </read_first>
  <action>
    Task 0에서 고른 안대로 판 11 사건을 **하나의 커밋 안에서** 다섯 자리에 전부 낸다. 한 자리라도 빠지면 그 사건이 있는 세션이 폴링마다 `UnknownEventType`을 맞고 영구히 안 열린다.

    아래 지침은 Task 0의 권장안(`one-event` — GM의 말을 `CreationGmSpoke` 하나로 묶는다)을 기준으로 쓰였다. 사람이 `four-events`를 골랐다면 GM 말 한 종류를 네 종류(`CreationAnnounced`/`CreationSpeakerNominated`/`CreationFollowUpAsked`/`CreationWrappedUp`)로 펴되 **다섯 자리 전부에 네 종류를 다 넣고**, 리듀서가 넷을 모두 `creation_gm_said`에 같은 방식으로 접게 한다. 나머지 두 사건(`CreationConsentRecorded`·`CreationHostClaimed`)과 `GameState` 칸 구성은 두 안이 같다.

    ① `src/gptrpg/event_log/schema.py`
       - `EVENT_SCHEMA_VERSION`을 `10`에서 `11`로 올리고, 모듈 도크스트링의 판 설명에 판 10→11 항목을 판 9→10 항목과 같은 어조로 추가한다 — 늘어난 것이 「칸」이 아니라 「사건 종류 셋」이라는 점과, 왜 GM의 말이 응답이 아니라 기록에 남아야 하는지(D-02)를 적는다.
       - `EventEnvelope`를 상속하는 dataclass를 `PartyRosterLocked` 뒤에 추가한다: `CreationGmSpoke`(또는 `four-events`를 골랐으면 그 네 종류)·`CreationConsentRecorded`·`CreationHostClaimed`. 칸 구성은 Task 0에서 고른 안 그대로.
       - 셋 다 `GameEvent = Annotated[Union[...]]` 목록에 추가하고, `_KNOWN_EVENT_TYPES` 집합에 `"creation_gm_spoke"`·`"creation_consent_recorded"`·`"creation_host_claimed"` 세 문자열을 추가한다. `_KNOWN_EVENT_TYPES`의 도크스트링에 적힌 개수(지금 「열여섯」)를 실제 개수로 고친다.
       - 판 11 미만 기록과의 호환: 이 셋은 **새 종류**이지 기존 사건의 새 칸이 아니므로 `_require_*_from_schema_N` 류 검증기를 추가하지 않는다. 옛 기록에는 이 종류가 아예 없다.

    ② `src/gptrpg/rules_core/reducer.py`
       - `CreationStepFold` 옆에 frozen dataclass `CreationGmLineFold`를 추가한다: `seq: int` · `kind: str` · `say: str` · `target_character_id: str | None`.
       - `GameState`에 칸 다섯을 추가한다(각 칸 docstring에 「판 11+」와 근거 결정 번호를 `party_size_fixed` 도크스트링과 같은 어조로 적는다):
         · `creation_gm_said: dict[str, CreationGmLineFold]` — `dedupe_key` -> 그 시점 GM이 한 말. 같은 키로 다시 오면 나중 것이 이긴다. D-12의 「이미 말했나」를 서버가 여기서 본다.
         · `creation_consents: dict[str, bool]` — `character_id` -> 동의 여부. D-03.
         · `reopened_creation_steps: frozenset[tuple[str, str]]` — 다시 열린 `(character_id, step_id)`. D-11 부분 재진행.
         · `creation_host_browser_id: str | None` — D-11 방장. `None`은 「아직 아무도 안 잡았다」.
         · `creation_rulebook_id: str | None` — 이 세션이 어느 룰북으로 만드는가. **`party_size_fixed` 사건의 기존 `rulebook_id` 칸에서 채운다**(새 사건 칸이 아니다) — 12.3-02의 `GameStateView`가 룰북 최소선을 계산하려면 이 값이 필요하다.
       - `apply_event`에 분기 셋을 `party_roster_locked` 분기 뒤에 추가한다:
         · `creation_gm_spoke` -> `creation_gm_said[payload["dedupe_key"]]`를 `CreationGmLineFold(seq=seq, ...)`로 덮어쓰고 `last_seq` 갱신.
         · `creation_consent_recorded` -> `payload["agree"]`가 참이면 `creation_consents[character_id] = True`. 거짓이면 `creation_consents`를 **전부 비우고** `reopened_creation_steps`에 `(character_id, payload["reopened_step_id"])`를 더한다 — 지금 `_process_reopen`이 액터 메모리에서 하는 두 동작과 정확히 같다.
         · `creation_host_claimed` -> `creation_host_browser_id = payload["browser_id"]`.
       - 기존 `party_size_fixed` 분기를 고쳐 `creation_rulebook_id=payload["rulebook_id"]`도 함께 채운다.

    ③ `src/gptrpg/session_actor/actor.py`
       - `schema.py`에서 세 클래스를 import하고 `_EVENT_CLASSES` 표에 세 항목을 추가한다. 이 표에 없으면 액터가 그 사건을 만들지 못한다(`KeyError`).
       - 이 계획에서는 `_process_consent`/`_process_reopen`을 아직 고치지 않는다 — 12.3-03이 그 둘을 사건 기록으로 옮긴다. 지금은 사건 **선언**만 있고 그것을 내는 곳이 `announce` 하나뿐이다.

    ④ `frontend/src/api/types.ts`
       - 새 세 사건을 TS `interface`로 1:1 미러링한다(`event_type` 리터럴 포함, 칸 이름은 스네이크 그대로).
       - **판 9가 더한 만들기 사건 다섯이 이 파일에 아직 없다** — `PartySizeFixedEvent`·`CreationStepCompletedEvent`·`CreationInterjectionEvent`·`CharacterCreatedEvent`·`PartyRosterLockedEvent`를 서버 dataclass 칸 그대로 함께 미러링한다. `CreationStepCompletedEvent`에는 `rolls: number[] | null`·`axis_values`·`picked`·`kind`·`superseded_seq`가 있어야 12.3-04의 주사위 연출이 성립한다. `CreationAxisValueRecord`/`CreationStatEntryRecord`도 필요한 만큼 미러링한다.
       - 여덟 종류 전부를 `GameEvent` 유니온에 추가한다. 파일 상단 도크스트링의 「사건 6종」 문구를 실제 개수로 고친다.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/ -x -k "schema or reducer or creation"</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EVENT_SCHEMA_VERSION = 11' src/gptrpg/event_log/schema.py` 가 `1`이다
    - `.venv/bin/python -c "from gptrpg.event_log.schema import _KNOWN_EVENT_TYPES; assert {'creation_gm_spoke','creation_consent_recorded','creation_host_claimed'} <= _KNOWN_EVENT_TYPES"` 가 종료 코드 0이다
    - `.venv/bin/python -c "from gptrpg.session_actor.actor import _EVENT_CLASSES; assert {'creation_gm_spoke','creation_consent_recorded','creation_host_claimed'} <= set(_EVENT_CLASSES)"` 가 종료 코드 0이다
    - `.venv/bin/python -c` 로 `gptrpg.rules_core.reducer.apply_event`를 세 종류 각각의 최소 페이로드로 불렀을 때 `UnknownEventType`이 나지 않고, `creation_consent_recorded`에 `agree=False`를 주면 `creation_consents`가 비고 `reopened_creation_steps`에 항목이 하나 생긴다
    - `.venv/bin/python -c "from gptrpg.rules_core.reducer import GameState; s=GameState(session_id='x'); assert s.creation_host_browser_id is None and s.creation_rulebook_id is None and s.creation_consents == {} and s.creation_gm_said == {}"` 가 종료 코드 0이다
    - `npm --prefix frontend run build` 가 종료 코드 0이다 (tsc가 새 interface와 유니온을 받아들인다)
    - `grep -c 'creation_gm_spoke\|creation_consent_recorded\|creation_host_claimed' frontend/src/api/types.ts` 가 `3` 이상이다
    - `grep -c 'party_size_fixed\|creation_step_completed\|creation_interjection\|character_created\|party_roster_locked' frontend/src/api/types.ts` 가 `5` 이상이다
    - `.venv/bin/python -m lint_imports` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>판 11 사건 세 종류가 서버 다섯 자리와 TS 미러에 동시에 존재하고, 세 종류를 접어도 `UnknownEventType`이 나지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 안내(`announce`)를 사건으로 남기고 서버가 중복 호출을 막는다</name>
  <files>src/gptrpg/web/routes_creation.py, tests/test_creation_screen_tracer.py</files>
  <read_first>
    - `src/gptrpg/web/routes_creation.py:504-538` — `announce_creation` 현재 구현(`actor.submit` 호출이 없는 자리)
    - `src/gptrpg/web/routes_creation.py:356-423` — `record_interjection`. **사건 하나를 기록하고 `SeqResponse`를 돌려주는 이미 있는 패턴**이 여기 있다
    - `src/gptrpg/web/routes_creation.py:81-100` — `_AGENT_RESOLUTION_ERRORS`(503)와 그 도크스트링이 설명하는 「AI 계약 위반은 이 목록에 없다」
    - `src/gptrpg/web/routes_creation.py:424-470` — `_unfinished_candidates`·`_required_steps_filled`(다음 계획이 쓸 계산이 이미 여기 있다)
    - `tests/test_creation_tracer.py:1-60` — `web_client` 조립과 `_events_of_type` 도우미
    - `tests/test_creation_gm.py:337-360` — `web_client_with_fake_provider(action_classifier=...)`로 `creation_gm` 역할까지 덮는 방식
  </read_first>
  <action>
    `announce_creation`이 GM의 안내를 **기록에 남기고**, 같은 시점에 두 번째로 불려도 AI를 다시 부르지 않게 한다(D-02, D-12).

    ① 중복 판정 열쇠를 만드는 도우미 `_gm_dedupe_key(kind: str, state: GameState, character_id: str | None = None) -> str`를 `_resolve_creation_gm_provider` 옆에 추가한다. 「그 시점」의 정의를 상태에서 뽑는다 — 시각이나 순번이 아니라 **GM 호출의 입력이 바뀌었는가**로 정한다:
       · `"announce"` -> 문자열 `"announce"` 하나. 세션당 한 번이다.
       · `"nominate"` -> `"nominate:" + "|".join(_unfinished_candidates(state))`. 누군가 끝나 후보 목록이 바뀌면 새 지목이 가능해진다.
       · `"follow_up"` -> `f"follow_up:{character_id}:{그 캐릭터의 creation_step_values 중 최대 seq}"`. 그 사람이 값을 하나 더 내면 새 되묻기가 가능해진다. 값이 하나도 없으면 `0`.
       · `"wrap_up"` -> `"wrap_up:" + str(state.creation_step_values 전체의 최대 seq)`. 동의 관문에서 「아니요」로 항목이 다시 채워지면 값이 바뀌어 GM이 다시 정리한다(D-11).
       이 도우미는 이 계획에서 `announce`만 쓰지만 네 갈래를 전부 지금 적는다 — 12.3-02가 나머지 셋을 붙일 때 열쇠 규칙을 다시 발명하지 않게 한다.

    ② `announce_creation` 본문을 고친다. 순서가 중요하다:
       1. 지금 있는 `party_roster is not None` 409 검사와 `get_rulebook` 400 검사를 그대로 둔다.
       2. `key = _gm_dedupe_key("announce", actor.state)`를 계산하고 `actor.state.creation_gm_said`에 그 키가 이미 있으면 **AI를 부르지 않고** 기록된 `say`와 그 `seq`를 그대로 응답한다. 제공자 해석(`_resolve_creation_gm_provider`)도 이 앞에서 하지 않는다 — 이미 말한 뒤라면 제공자 설정이 없어도 화면이 지난 안내를 볼 수 있어야 한다.
       3. 아직 없으면 지금처럼 제공자를 풀고(`_AGENT_RESOLUTION_ERRORS` -> 503 그대로 유지, D-13 ②의 발생 지점) `announce_requirements`를 부른다.
       4. 받은 문장을 `actor.submit(CreationGmSpoke(kind="announce", say=message, target_character_id=None, dedupe_key=key))`로 기록하고 그 `seq`를 응답에 싣는다. `RosterAlreadyLocked`/`CommandRejected`/`SequenceConflict`는 `record_interjection`과 같은 방식으로 409로 옮긴다.
       5. `AnnounceCreationResponse`에 `seq: int` 칸을 더한다. 화면이 「방금 그 사건이 어느 순번인가」를 알아야 폴링이 그것을 실어 오기 전에도 조용히 기다릴 수 있다.
       `announce_requirements`는 제공자 실패를 내부에서 폴백 문구로 흡수하므로(ARCH-05) 이 경로는 여전히 500을 내지 않는다 — 폴백 문구도 그대로 기록에 남는다.

    ③ `tests/test_creation_screen_tracer.py`를 새로 만든다. `tests/test_creation_gm.py`의 픽스처 사용법을 그대로 따른다:
       · `announce`를 한 번 부르면 `GET /events`에 `creation_gm_spoke` 사건이 정확히 하나 있고 `kind`가 `"announce"`, `say`가 비어 있지 않다
       · **같은 세션에서 `announce`를 두 번 부르면 사건이 여전히 하나이고 두 응답의 `say`와 `seq`가 같다** — 그리고 대역 제공자의 호출 횟수가 1에서 늘지 않는다(`FakeProvider`가 호출 횟수를 세는 것을 이용한다). 이것이 D-12의 시험이다
       · 인원 확정 → `announce` → `GET /events`를 `from_seq=0`으로 다시 읽었을 때 `rebuild_state_from_events`가 예외 없이 통과한다(판 11 사건이 섞인 기록을 접을 수 있다)
       · 명단이 잠긴 세션에서 `announce`는 409이고 그 `detail`이 서버가 이미 쓰는 한국어 문장 그대로다
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_screen_tracer.py -x</automated>
    <automated>.venv/bin/python -m pytest -q tests/test_creation_gm.py tests/test_creation_flow.py tests/test_creation_tracer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_creation_screen_tracer.py` 가 존재하고 전부 통과한다
    - 그 파일에 「두 번 불러도 사건이 하나」와 「대역 제공자 호출 횟수가 늘지 않는다」를 함께 단언하는 시험 함수가 있다
    - `grep -c '_gm_dedupe_key' src/gptrpg/web/routes_creation.py` 가 `2` 이상이다 (정의 + 사용)
    - `POST /api/sessions/{id}/creation/announce` 응답 JSON에 `seq` 키가 있다
    - 기존 만들기 시험 세 파일(`test_creation_flow.py`·`test_creation_gm.py`·`test_creation_tracer.py`)이 전부 통과한다 — 판 11 올림이 기존 흐름을 깨지 않았다
  </acceptance_criteria>
  <done>GM 안내가 사건 기록에 남고, 넷이 동시에 눌러도 AI가 한 번만 돌며, 두 번째 호출자는 기록된 같은 문장을 받는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 링크를 열면 만들기 화면으로 가고, 폴링이 GM의 말을 그린다</name>
  <files>frontend/src/session/creationView.ts, frontend/src/session/creationView.test.ts, frontend/src/screens/CreationScreen.tsx, frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/labels.ts, frontend/src/screens/Notices.tsx, frontend/src/screens/SessionScreen.tsx, frontend/src/panes/StatusPane.tsx, frontend/src/screens/CharacterSelect.tsx</files>
  <read_first>
    - `frontend/src/App.tsx` 전체(90행) — `Gate` 판별 유니온과 `fetchMyCharacter` 분기
    - `frontend/src/screens/CharacterSelect.tsx:93-107` — `err.detail`을 그대로 보여주는 패턴(D-15의 선례. 이 화면이 사라져도 이 패턴은 새 만들기 경로로 옮겨간다)
    - `frontend/src/session/usePolling.ts` 전체(147행) — 1.5초 · `from_seq=0` 전체 재수신 · 첫 응답을 연출에서 거르는 `primedRef`
    - `frontend/src/session/groupTurns.test.ts:1-30` — 이 저장소의 프론트엔드 단위 시험 모양(`vitest`, 순수 함수만, RTL 없음)
    - `frontend/src/labels.ts:172-248` — `COPY` 객체의 평평한 키-문자열 방식
    - `frontend/src/panes/StatusPane.tsx:285-295` — 「캐릭터 변경하기」 버튼(12.1이 접어 둔 막다른 길)
    - `frontend/src/screens/Notices.tsx` — `MissingSession`/`InvalidSession`/`Loading` 안내 화면의 모양
  </read_first>
  <behavior>
    `creationView.ts`의 순수 함수가 이 시험들을 먼저 통과해야 한다(RTL이 없으므로 화면 판단은 전부 이 순수 모듈에 모은다):
    - `gmLinesFrom(events)`: `creation_gm_spoke` 사건만 골라 `seq` 오름차순으로 `{ seq, kind, say, targetCharacterId }` 목록을 만든다. 다른 종류 사건이 섞여 있어도 무시한다
    - `gmLinesFrom([])` 는 빈 배열이다 — 「아직 아무 말도 없다」와 「불러오기 실패」를 섞지 않는다
    - `gmLinesFrom`에 같은 `seq`가 두 번 오면 하나만 남는다 (겹쳐 도착한 폴링 응답)
    - `creationErrorMessage(error)`: `ApiError.status === 503` 이면 `COPY.creationGmUnavailable`, 그 밖의 `ApiError`에 `detail`이 있으면 그 `detail`을 **그대로**, 없으면 `COPY.creationFailed` (D-13 ② / D-15)
  </behavior>
  <action>
    ① `frontend/src/session/creationView.ts`를 새로 만든다. 위 `<behavior>`의 두 함수만 넣는다 — 화면이 스스로 진행 상태를 계산하는 코드를 여기 넣지 않는다(D-04, 그 판단은 12.3-02가 서버에서 내려준다). `creationView.test.ts`를 `groupTurns.test.ts`와 같은 모양으로 먼저 쓴다.

    ② `frontend/src/api/client.ts`에 `announceCreation(sessionId, rulebookId)`를 추가한다. 반환 타입은 `AnnounceCreationResponse`(`{ message: string; seq: number }`)로 `types.ts`에 함께 선언한다. 사람이 직접 눌러 409를 받을 수 있는 경로이므로 **공용 `postJson`을 쓰지 않고** `selectCharacter`가 쓰는 방식(응답 본문의 `detail`을 읽어 `ApiError.detail`에 담기)을 그대로 복제한다(D-15).

    ③ `frontend/src/screens/CreationScreen.tsx`를 새로 만든다. 이 계획에서는 최소 뼈대다:
       · `usePolling(sessionId)`을 그대로 쓴다 — 새 조회 루프를 만들지 않는다(D-04 경계)
       · `gmLinesFrom(feed.events)`로 GM의 말을 시간순으로 그린다
       · 버튼 하나(`COPY.creationAnnounce`)가 `announceCreation`을 부르고, 부른 뒤 `feed.pollNow()`를 부른다
       · 실패는 `creationErrorMessage(error)`가 고른 문장 하나로 보여준다
       · `feed.status === "disconnected"` 면 `SessionScreen`이 쓰는 것과 **같은** 끊김 띠 문구(`COPY.disconnected`)를 쓴다(D-13 ③) — 새 문구를 만들지 않는다
       · **배치·색·간격을 설계하지 않는다.** 기존 `screen`/`screen__inner`/`t-label` 클래스만 쓴다. 생김새는 Phase 16이다

    ④ `frontend/src/App.tsx`의 `Gate`를 고친다(D-10):
       · `{ kind: "choosing" }`를 `{ kind: "creating" }`으로 갈아 끼운다
       · `fetchMyCharacter`가 `selected === true`면 `{ kind: "ready" }`, 아니면 `{ kind: "creating" }`. 조회 자체가 실패해도(400 제외) `{ kind: "creating" }`으로 보낸다 — 만들기가 유일한 입장 경로이므로 막다른 길이 구조적으로 안 생긴다
       · `case "creating"`은 `CreationScreen`을 그린다

    ⑤ **`CharacterSelect` 화면을 지운다** (D-10이 계획에 맡긴 판단).
       **주의 — 지운 심볼 이름을 새 소스에 적지 않는다.** 아래 승인 기준이 `frontend/src` 전체에서 그 이름들이 사라졌음을 negative grep으로 확인하므로, 「예전에 있던 화면」을 설명하는 주석에 그 이름을 그대로 쓰면 자기 자신이 그 검사를 깬다. 삭제 판단의 자세한 근거는 **`12.3-01-SUMMARY.md`에** 적고, `CreationScreen.tsx` 도크스트링에는 이름 없이 「만들기가 이 세션의 유일한 입장 경로다」와 그 이유만 적는다. 그 이유는: 만들기가 완성 순간 자동으로 점유까지 끝내므로(`complete_creation`) 새 브라우저가 고를 목록이 애초에 없고, 쿠키가 있는 브라우저는 `selected === true`로 세션에 바로 들어가며, 쿠키를 잃은 브라우저는 명단이 잠긴 뒤 목록을 보여줘도 전부 점유돼 있어 같은 막다른 길로 되돌아온다(Phase 8 D-01의 한계). 함께 지울 것:
       · `frontend/src/screens/CharacterSelect.tsx` 파일
       · `frontend/src/api/client.ts`의 `selectCharacter`
       · `frontend/src/labels.ts`의 `characterSelectError`·`characterListEmpty`
       · `SessionScreen`/`StatusPane`의 `onChangeCharacter` 프롭 사슬과 「캐릭터 변경하기」 버튼 — 이것이 12.1이 접어 둔 「막다른 길」 할 일이 닫히는 자리다
       **서버의 `POST /select-character` 라우트는 건드리지 않는다** — 화면이 안 부를 뿐이고, 지우는 것은 이 단계의 범위 밖이다. `COPY.characterIdentityLimit`는 `StatusPane`이 계속 쓰므로 남긴다.

    ⑥ 명단이 이미 잠겼는데 이 브라우저에 캐릭터가 없는 경우를 위해 `Notices.tsx`에 안내 하나를 더한다(`COPY.creationRosterLocked`). `CreationScreen`이 폴링 상태에서 `party_roster`가 채워진 것을 보면 이 안내로 갈아탄다. 잠긴 명단 판정에 필요한 값은 12.3-02가 `GameStateView`에 싣는다 — 지금은 `party_roster_locked` 사건이 `feed.events`에 있는지로 판정하고, 12.3-02가 그것을 상태 값 읽기로 바꾼다.

    ⑦ `frontend/src/labels.ts`에 이 계획이 쓰는 문구만 추가한다: `creationTitle`("캐릭터 만들기") · `creationAnnounce`("진행자에게 안내를 부탁하기") · `creationGmUnavailable`("진행자를 쓸 수 없는 상태입니다 — 운영자에게 알리세요", D-13 ②) · `creationFailed`("만들기 요청을 처리하지 못했어요") · `creationRosterLocked`. **서버가 보낸 거절 사유를 이 파일에 옮겨 적지 않는다**(D-15).
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/session/creationView.test.ts` 가 존재하고 `npm --prefix frontend test` 가 전부 통과한다
    - `npm --prefix frontend run build` 가 종료 코드 0이다
    - `test -e frontend/src/screens/CharacterSelect.tsx` 가 실패한다 (파일이 지워졌다)
    - `grep -rn 'CharacterSelect\|onChangeCharacter\|selectCharacter' frontend/src` 가 아무것도 찾지 못한다
    - `grep -c 'kind: "creating"' frontend/src/App.tsx` 가 `1` 이상이다
    - `grep -c 'usePolling' frontend/src/screens/CreationScreen.tsx` 가 `1` 이상이다 — 새 조회 루프를 만들지 않았다
    - `creationErrorMessage`가 `detail`을 가진 409 `ApiError`에 대해 그 `detail` 문자열을 그대로 돌려주는 시험이 있다 (D-15)
  </acceptance_criteria>
  <done>세션 링크를 연 브라우저가 캐릭터 없이 만들기 화면에 도착하고, 버튼 하나로 GM 안내를 부르면 그 문장이 폴링을 타고 화면에 그려진다. 캐릭터 고르기 막다른 길이 저장소에서 사라졌다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (12.3-01)

**새 사건 dataclass** (`src/gptrpg/event_log/schema.py`): `CreationGmSpoke` · `CreationConsentRecorded` · `CreationHostClaimed`
**새 `event_type` 문자열:** `creation_gm_spoke` · `creation_consent_recorded` · `creation_host_claimed`
**새 `GameState` 칸** (`src/gptrpg/rules_core/reducer.py`): `creation_gm_said` · `creation_consents` · `reopened_creation_steps` · `creation_host_browser_id` · `creation_rulebook_id`
**새 dataclass** (같은 파일): `CreationGmLineFold`
**새 서버 도우미** (`src/gptrpg/web/routes_creation.py`): `_gm_dedupe_key`
**바뀐 응답 모델:** `AnnounceCreationResponse`에 `seq: int` 추가
**새 TypeScript 타입** (`frontend/src/api/types.ts`): `CreationGmSpokeEvent` · `CreationConsentRecordedEvent` · `CreationHostClaimedEvent` · `PartySizeFixedEvent` · `CreationStepCompletedEvent` · `CreationInterjectionEvent` · `CharacterCreatedEvent` · `PartyRosterLockedEvent` · `CreationAxisValueRecord` · `CreationStatEntryRecord` · `AnnounceCreationResponse`
**새 TypeScript 함수** (`frontend/src/api/client.ts`): `announceCreation`
**새 파일:** `frontend/src/screens/CreationScreen.tsx` · `frontend/src/session/creationView.ts` · `frontend/src/session/creationView.test.ts` · `tests/test_creation_screen_tracer.py`
**새 순수 함수** (`frontend/src/session/creationView.ts`): `gmLinesFrom` · `creationErrorMessage`
**새 label 키** (`frontend/src/labels.ts`): `creationTitle` · `creationAnnounce` · `creationGmUnavailable` · `creationFailed` · `creationRosterLocked`
**지워지는 것:** `frontend/src/screens/CharacterSelect.tsx` · `client.ts::selectCharacter` · `COPY.characterSelectError` · `COPY.characterListEmpty` · `SessionScreen`/`StatusPane`의 `onChangeCharacter` 프롭
**새 Gate 갈래** (`frontend/src/App.tsx`): `{ kind: "creating" }` (기존 `{ kind: "choosing" }` 대체)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `/api/sessions/{id}/creation/*` | 신뢰할 수 없는 입력이 여기서 넘어온다. 쿠키가 구워지기 **전**의 참가자는 서명된 신원이 없고 자칭 `browser_id`만 있다 |
| 브라우저 → `GET /events` | 폴링이 세션의 전체 기록을 아무 인증 없이 내려준다 (기존 설계, 링크를 아는 사람 = 참가자) |
| 라우터 → `SessionActor` | 라우터가 검사한 것과 별개로 액터가 독립적으로 다시 검사한다 (12.1이 세운 이중 관문) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-01 | Spoofing | 판 11 사건을 내는 모든 새 경로 | high | mitigate | 12.1이 세운 신원 대조 순서(맨 앞 `read_identity`)를 새 경로에 그대로 복제한다. 이 계획의 `announce`는 어느 캐릭터도 지목하지 않고 세션 단위 안내만 기록하므로 캐릭터 신원 대조가 필요 없다 — 그 사실을 라우트 도크스트링에 명시해, 뒤 계획이 캐릭터를 다루는 경로에 같은 판단을 잘못 복사하지 않게 한다 |
| T-12.3-04 | Denial of Service | `announce_creation` → `creation_gm` 제공자 | medium | mitigate | `_gm_dedupe_key` + `creation_gm_said` 조회가 AI 호출 **앞**에 온다(D-12). 넷이 동시에 눌러도 AI가 한 번만 돈다 |
| T-12.3-07 | Denial of Service | `CreationGmSpoke.say` 길이 | low | mitigate | `say`는 `creation_gm` 에이전트의 `max_tokens` 제한 출력이고 사람 입력이 아니다. 사람 입력이 사건으로 가는 경로는 이 계획에 없다 — 그 경로(`/creation/step`의 `text_value`)는 이미 `MAX_RAW_TEXT_LEN`으로 막혀 있다 |
| T-12.3-08 | Tampering | 판 11 미완성 배포 (다섯 자리 중 일부만 반영) | high | mitigate | Task 1이 다섯 자리를 한 커밋으로 강제하고, 승인 기준이 `_KNOWN_EVENT_TYPES`/`_EVENT_CLASSES`/리듀서 분기를 각각 실행으로 확인한다. 누락 시 그 사건이 있는 세션이 영구히 안 열린다 |

**공급망:** 이 계획은 npm/pip/cargo 설치를 하나도 추가하지 않는다 — 새 의존성 없이 저장소에 이미 있는 것만 쓴다(`react`·`vitest`·`fastapi`·`pydantic`). 따라서 `T-{phase}-SC` 항목이 없다. 새 패키지를 도입해야 한다고 판단되면 그 자리에서 멈추고 사람에게 확인을 받는다.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` 전체가 통과한다
- `npm --prefix frontend test` 와 `npm --prefix frontend run build` 가 통과한다
- `.venv/bin/python -m lint_imports` 가 통과한다 (층 계약이 깨지지 않았다)
- `.venv/bin/python -m ruff check src tests` 가 통과한다
</verification>

<success_criteria>
1. 판 11 사건 세 종류가 서버 다섯 자리(schema dataclass · 유니온 · `_KNOWN_EVENT_TYPES` · `_EVENT_CLASSES` · 리듀서 분기)와 TS 미러에 동시에 존재한다
2. `announce`가 사건을 남기고, 같은 시점 재호출이 AI를 다시 부르지 않는다
3. 캐릭터가 없는 브라우저가 만들기 화면으로 자동으로 간다
4. 캐릭터 고르기 화면과 「캐릭터 변경하기」 막다른 길이 저장소에서 사라졌다
</success_criteria>

<output>
Create `.planning/phases/12.3-creation-screen/12.3-01-SUMMARY.md` when done
</output>
