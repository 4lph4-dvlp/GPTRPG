---
phase: 12.3-creation-screen
plan: 19
subsystem: testing
tags: [python, headless-chromium, viewport, test-gate, exit-codes, gap-closure]

# Dependency graph
requires:
  - phase: 12.3-18
    provides: "빈 값 경계(missingElements)·키/이름/차례 검사·형태 조건부 단언 가드·넓은 창 3컬럼 단언·상태 칸 25dvh 상한 단언"
provides:
  - "조작 요소(TOUCH_TARGETS) 선택자가 문서에서 하나도 못 찾으면 extract_measurements()가 판정 없이 종료 코드 2로 끝나는 빈 값 경계"
  - "이름 감시 목록(REQUIRED_SOURCE_CLASS_NAMES)이 조합 선택자가 쓰는 낱말(composer__row·composer__input·candidate·btn)까지 감시"
  - "check_fixture_still_mirrors_source()와 조작 요소 빈 값 검사가 각자 맡는 절반, 그리고 둘 다 못 잡는 중첩 구조 드리프트를 코드에 명시한 도크스트링"
  - "머리말 종료 코드 2 설명에 새 사유(TOUCH_TARGETS 선택자 0개) 반영"
affects: [creation-screen, session-screen]

actuals:
  tokens: 1500
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "item[\"targets\"].get(선택자, []) — 잰 값 칸이 통째로 없는 경우와 칸은 있는데 빈 경우를 한 자리에서 함께 잡는 패턴. TOUCH_TARGETS를 순회하는 파이썬/자바스크립트 양쪽이 이미 같은 목록을 공유하던 규율을 판정 이전 단계(잰 값을 읽는 자리)로 한 걸음 더 확장했다"

key-files:
  created: []
  modified:
    - scripts/check_narrow_viewport.py

key-decisions:
  - "새 검사는 judge() 진입부가 아니라 extract_measurements()의 형태별 검사 반복문(REQUIRED_MEASUREMENT_KEYS 검사 바로 뒤)에 넣었다 — judge()는 (통과 여부, 사유 목록) 두 칸만 돌려주는 계약이라 '판정 불가'를 표현할 칸이 없고, 그 계약을 바꾸면 호출자 두 곳(run_check/run_self_test)이 함께 흔들린다. extract_measurements()는 이미 None이라는 확인 불가 통로를 갖고 있어 호출자를 한 줄도 안 고쳤다"
  - "REQUIRED_SOURCE_CLASS_NAMES에 넣기 전 composer__row·composer__input·candidate·btn 네 낱말이 ChatPane.tsx·CreationPane.tsx 실제 파일에 있는지 grep으로 먼저 확인했다 — 없는 이름을 넣으면 이 확인이 영구히 종료 코드 2가 된다(T-12.3-95)"
  - "select·input[type=\"number\"]는 태그·속성 선택자라 이름 감시 목록에 못 넣는다는 사실을 목록 위 주석에 남겼다 — 이 둘은 조작 요소 빈 값 검사(0개면 종료 코드 2)로만 지켜진다"

patterns-established:
  - "조작 요소 빈 값 경계 — 선택자마다 1개 이상을 요구하고 하나라도 0이면 판정 자체를 안 하는 패턴. 다음에 TOUCH_TARGETS에 항목이 늘 때도 같은 검사가 자동으로 적용된다(새 코드 불필요)"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "조작 요소 선택자가 문서에서 하나도 못 찾으면 그물이 판정하지 않고 종료 코드 2로 끝나며, 어느 형태·어느 선택자가 0개였는지 표준오류에 이름으로 남는다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — .nonexistent-touch-target을 TOUCH_TARGETS에 얹음 → 수정 전 EXIT=0(결함 재현), 수정 후 EXIT=2 + 'nonexistent-touch-target'·'1440x900' 출력, 원복 후 EXIT=0"
        status: pass
    human_judgment: false
  - id: D2
    description: "--self-test(그물을 뒤집어 확인하는 경로)도 같은 조작 요소 빈 값 계약을 따른다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 선택자를 얹은 상태에서 .venv/bin/python scripts/check_narrow_viewport.py --self-test → EXIT=2"
        status: pass
    human_judgment: false
  - id: D3
    description: "이름 감시 목록이 조작 요소가 쓰는 조합 선택자 낱말(composer__row·composer__input·candidate·btn)까지 본다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "check_fixture_still_mirrors_source() → [] (빈 목록, 넷 다 실제 화면 파일에 있음 확인) · REQUIRED_SOURCE_CLASS_NAMES 두 항목에 넷 다 포함 확인"
        status: pass
    human_judgment: false
  - id: D4
    description: "손 안 댄 코드(수정 완료 상태)에서는 여전히 두 명령 모두 통과(0)다 — 새 검사가 거짓 확인 불가를 안 낸다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py → EXIT=0(네 줄 전부 통과) · --self-test → EXIT=0(함정 셋 전부 빨강)"
        status: pass
    human_judgment: false
  - id: D5
    description: "비-회귀 — pytest·frontend 시험이 오늘과 같은 결과, .tsx/src/gptrpg 0줄, 새 꾸러미 0개"
    verification:
      - kind: unit
        ref: ".venv/bin/python -m pytest -q --tb=short (1406 passed, 1 pre-existing unrelated failure) · cd frontend && npm test (7 files, 104 tests passed) · git status --short scripts/check_narrow_viewport.py만 변경"
        status: pass
    human_judgment: false

duration: ~15min
completed: 2026-08-24
status: complete
---

# Phase 12.3 Plan 19: 조작 요소 「잴 것이 없었다」를 종료 코드 2로 가르는 마지막 빈 값 경계 Summary

**`check_narrow_viewport.py`의 조작 요소(`TOUCH_TARGETS`) 선택자가 문서에서 하나도 못 찾을 때 그물이 「재 봤더니 문제없다」로 조용히 통과시키던 마지막 자리를, 잰 값을 읽는 `extract_measurements()`에서 종료 코드 2로 막고, 이름 감시 목록도 조합 선택자가 쓰는 낱말(`composer__row`·`composer__input`·`candidate`·`btn`)까지 넓혔다.**

## Performance

- **Duration:** ~15min
- **Completed:** 2026-08-24T07:58:26Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- `TOUCH_TARGETS`의 일곱(이제 실질적으로 여덟 이상까지 안전한) 선택자 중 하나라도 헤드리스 문서에서 0개 매치하면 `extract_measurements()`가 판정 없이 `None`을 돌려주고 호출자가 종료 코드 2로 끝나게 하는 검사를 추가했다 — `item["targets"].get(선택자, [])`를 써서 칸 자체가 없는 경우(예전엔 `judge()`에서 `KeyError`로 새어 파이썬 기본 종료 코드 1과 섞였다)와 칸은 있는데 빈 경우를 같은 한 자리에서 함께 잡는다
- **재현(Task 1 ①)**: 임시로 `.nonexistent-touch-target` 선택자를 `TOUCH_TARGETS`에 얹고 수정 전 코드를 돌리자 종료 코드 **0**(네 형태 전부 통과)을 찍었다 — 「재 봤더니 문제없다」와 「잴 것 자체가 없었다」가 같은 결과로 섞이던 결함을 직접 확인했다
- **실증(Task 2 ①)**: 같은 임시 선택자를 수정 후 코드에 얹고 다시 돌리자 종료 코드 **2**로 바뀌었고(표준오류에 `nonexistent-touch-target`·`1440x900` 둘 다 출력), `--self-test`도 같은 임시 선택자로 종료 코드 **2**를 냈다 — 원복 후 두 명령 모두 다시 **0**, `git diff`에 임시 선택자 흔적 0줄. 「전 0 → 후 2 → 원복 0」 세 숫자를 커밋 본문에 기록했다
- 머리말 종료 코드 `2` 설명 문단에 새 사유(「조작 요소(`TOUCH_TARGETS`) 선택자가 문서에서 하나도 못 찾음」)를 추가했다
- `REQUIRED_SOURCE_CLASS_NAMES`의 `ChatPane.tsx`·`CreationPane.tsx` 두 항목에 `composer__row`·`composer__input`·`candidate`·`btn` 네 낱말을 추가했다 — 넷 다 두 실제 화면 파일에 이미 존재함을 grep으로 먼저 확인한 뒤 넣었다. `select`·`input[type="number"]`는 태그·속성 선택자라 이 목록에 못 넣는다는 사실을 목록 위 주석에 남겼다
- `check_fixture_still_mirrors_source()`의 도크스트링에 이 함수(이름 감시)와 조작 요소 빈 값 검사가 각각 맡는 절반, 그리고 둘 다 못 잡는 것(이름은 살아 있는데 중첩 구조만 바뀌는 어긋남 — `deferred-items.md` IN-06 처지, 여기서 새로 안 닫음)을 한 문단으로 적었다

## Task Commits

1. **Task 1: 「잴 것이 없었다」를 종료 코드 2로 가른다 — 재현부터 판정까지 한 줄기로 꿴다** - `4ff8f72` (fix)
2. **Task 2: 새 검사가 실제로 빨개지는 것을 보고, 옆 그물의 눈도 같은 데까지 넓힌다** - `af9f8c1` (fix)

## Files Created/Modified

- `scripts/check_narrow_viewport.py` — 조작 요소 빈 값 경계 검사(`extract_measurements()`) · 머리말 종료 코드 2 문단 갱신 · `REQUIRED_SOURCE_CLASS_NAMES` 낱말 4개 추가(ChatPane·CreationPane 각각) · `check_fixture_still_mirrors_source()` 도크스트링에 두 그물의 절반 분담 + 한계 문단 추가(총 +58줄)

## Decisions Made

키 결정은 frontmatter `key-decisions`에 기록. 요지: (1) 새 검사는 `judge()`가 아니라 `extract_measurements()`에 넣어 (통과 여부, 사유 목록) 계약을 안 건드렸다. (2) 이름 감시 목록에 넣을 낱말 넷의 실제 존재를 넣기 전에 확인했다. (3) 태그·속성 선택자 둘은 이름 감시 대상이 아니라는 사실을 주석으로 명시했다.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `check_narrow_viewport.py`가 이제 조작 요소 경로까지 포함해 「확인을 못 했다」가 「통과」로 보이는 자리를 전부 종료 코드 2로 접는다 — 이 파일이 머리말에서 선언한 세 종료 코드 계약과 실제 코드가 조작 요소 자리까지 완전히 맞아떨어진다
- 이름 감시가 못 잡는 마지막 절반(중첩 구조 드리프트, 이름은 살아 있는데 조합 선택자가 실제로는 다른 것을 가리키게 되는 경우)은 여전히 열려 있다 — `deferred-items.md` IN-06 처지대로 실제 컴포넌트 렌더 도구(RTL/jsdom)가 이 저장소에 없어 원리적으로 못 닫는다
- 12.3-18-SUMMARY.md의 D11·D12(사람이 실제 폰으로 서사 칸 크기·조작 칸 셋을 직접 확인)는 이 계획의 범위 밖이고 여전히 열려 있다
- 12.3-20 계획이 이번 실행의 「드러낸 가정 ㉴」(표식·표현을 느슨하게 잡아 언제나 참이 되는 것을 막는 금지 사항)을 이어받는다

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-24*

## Self-Check: PASSED

- FOUND: `scripts/check_narrow_viewport.py`
- FOUND: commit `4ff8f72` (Task 1)
- FOUND: commit `af9f8c1` (Task 2)
