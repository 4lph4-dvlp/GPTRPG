---
phase: 12.3-creation-screen
plan: 16
subsystem: ui
tags: [css, layout, headless-chromium, viewport, touch-target, flexbox, chat, composer, gap-closure]

# Dependency graph
requires:
  - phase: 12.3-15
    provides: "좁은 화면 배치 회귀 그물(scripts/check_narrow_viewport.py)의 최초 판, 서사 칸 25% 최소선"
provides:
  - "폰 가로(844×390)·폰 세로(390×844) 양쪽에서 GM 제안 후보 버튼·말 쓰는 칸·보내기 단추가 화면 안에 있거나 조작부 안에서 스크롤해 닿는 CSS"
  - "「닿을 수 있는가」를 위치가 아니라 스크롤 경로 유무로 판정하는 reachable() 헬퍼 — 상태 칸의 스크롤 가능한 조작 칸을 거짓 빨강으로 안 찍는다"
  - "사람이 실제로 누르는 조작 요소 전부(TOUCH_TARGETS)를 44px 하한 + 닿을 수 있음으로 함께 판정하는 목록 기반 그물"
  - "고정판-소스 드리프트 감시를 className= 문맥의 낱말 경계 매칭으로 좁힌 check_fixture_still_mirrors_source()"
affects: [16-design-system, session-screen, creation-screen]

actuals:
  tokens: 4700
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "reachable(el) — 조상을 거슬러 오르며 overflow-y: auto|scroll은 스크롤 경로로, hidden|clip은 잘림으로 읽는 판정 함수. 위치 비교(뷰포트 밖인가)가 아니라 스크롤 경로 유무로 「닿을 수 있는가」를 가른다"
    - "TOUCH_TARGETS — (선택자, 사람이 읽을 이름, 44px 하한을 거는가) 목록 하나를 자바스크립트(측정)와 파이썬(judge 사유 문장)이 함께 도는 패턴. 대상이 늘 때 한쪽만 늘어나는 것을 구조적으로 막는다"
    - "className= 속성값(단순 문자열 · 삼항식 둘 다)만 보는 정규식 + 낱말 경계 매칭 — 주석 오검출과 부분 문자열(chat vs chat-line) 오검출을 함께 막는 고정판-소스 드리프트 감시"

key-files:
  created: []
  modified:
    - scripts/check_narrow_viewport.py
    - frontend/src/styles.css

key-decisions:
  - ".composer에 min-height: 0 + overflow-y: auto 두 줄만 더한다 — 코드 검토(12.3-REVIEW.md CR-02)가 제시한 flex: 0 0 auto + max-height: 100% 처방은 채택하지 않는다. 줄임 계수를 0으로 박으면 조작부가 안 줄어(폰 가로 335px 그대로) 156px 대화 칸을 179px 넘치고 .pane의 overflow: hidden이 다시 잘라 지금과 똑같아진다 — CR-02가 머리말 숨기기(display: none)를 한 세트로 함께 제안한 이유가 이것이다. 채택한 두 줄은 그 넷째 규칙 없이도 넘침이 정확히 0이 된다"
  - "세로 골격 대화 행 바닥(240px)을 되돌리지 않는다 — 폰 세로(390×844)엔 40dvh(337.6px)가 240px보다 이미 커서 옛 바닥이 애초에 구속력이 없었다. 되돌려도 폰 세로엔 안 통하고 12.3-15가 닫은 서사 칸 0px 결함(G-12.3-29)만 되살린다"
  - "조작부(.composer)에 줄임 금지를 따로 안 건다 — .chat의 flex: 1은 기준 크기(flex-basis)가 0이라는 뜻이고, 플렉스는 모자란 높이를 줄임 계수 × 기준 크기 비율로 나눠 문다. 기준 크기가 0인 .chat은 그 비율도 0이라 줄어들 몫을 애초에 한 톨도 안 받는다 — 그래서 「지난 말이 먼저 0까지 줄고, 그 다음에만 조작부」라는 차례가 저절로 성립한다"
  - "「잘렸다」의 판정을 뷰포트 위치 비교가 아니라 스크롤 경로 유무(reachable())로 짠다 — 상태 칸의 조작 칸은 부모(.pane--status)가 overflow-y: auto라 화면 밖에 있어도 스크롤로 닿는다. 단순 위치 비교였다면 그것을 거짓 빨강으로 찍었을 것이다"
  - "GM 제안 후보 버튼(.candidate)과 조작부 안의 다른 단추(.btn)를 TOUCH_TARGETS에서 별개 항목으로 둔다 — 실제 코드는 후보 버튼에 candidate 클래스만 쓰고 btn 클래스는 안 쓴다(PLAN.md의 서술은 둘을 섞어 적었으나 실제 마크업을 확인해 갈랐다). 임시 확인에서 두 선택자 모두 실제 폰 실측치(445px·497px·610px)로 결함을 잡는 것을 확인했다"

patterns-established:
  - "12.3-15의 「진짜 헤드리스 크로미움 + srcdoc iframe + 종료 코드 0/1/2」틀 위에, 「위치가 아니라 스크롤 경로로 닿음을 판정」하는 두 번째 층을 얹는 패턴 — 앞으로 배치 회귀 그물에 조작 요소를 추가할 때 이 reachable()을 재사용한다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "폰 가로(844×390)에서 GM 제안 후보 버튼·보내기 단추가 화면 안에 있거나 조작부 안에서 스크롤해 닿는다(CSS 수정 전 exit 1 → 수정 후 exit 0)"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "폰 세로(390×844)에서도 같은 처방이 통한다 — 세로 골격을 되돌리지 않고 조작부 쪽에서 막는다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "「화면 밖」과 「못 닿음」을 스크롤 경로로 갈라, 상태 칸의 스크롤 가능한 조작 칸이 거짓 빨강을 안 낸다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py (네 형태 전부 통과, 상태 칸 select/숫자칸 포함)"
        status: pass
    human_judgment: false
  - id: D4
    description: "고정판이 흉내 내는 클래스 이름이 소스에서 사라지거나 부분 문자열로 오검출되면 종료 코드 2로 멈춘다(className= 문맥 낱말 경계 매칭)"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — className 'composer'→'composer2', 'chat'→'chat9' 각각 exit 2, 원복 확인"
        status: pass
    human_judgment: false
  - id: D5
    description: "서사 칸 25% 최소선과 1440×900 3컬럼이 그대로다(비-회귀)"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py (1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px)"
        status: pass
    human_judgment: false
  - id: D6
    description: "사람이 실제 폰으로 놀이 화면을 열어 서사가 읽을 만한 크기로 보이는 것을 눈으로 확인한다"
    human_judgment: true
    rationale: "기계가 답하는 것은 「0이 아니다 · 세로의 4분의 1은 된다」까지다. 그 크기로 이야기를 읽을 만한지는 사람만 안다"
  - id: D7
    description: "사람이 만들기 중 자기 차례를 실제로 받은 판에서 폰으로 조작 칸 셋(능력치 배치 칸 · 점수 숫자칸 · 인원 숫자칸)을 눌러 본다"
    human_judgment: true
    rationale: "이 계획이 만드는 단언은 「높이가 44px이고 스크롤로 닿는다」까지만 답한다. 누르는 느낌은 사람만 안다"

duration: ~35min
completed: 2026-08-24
status: complete
---

# Phase 12.3 Plan 16: 폰 조작부 잘림 결함 종료 + 스크롤 경로 판정 그물 Summary

**폰 가로(844×390)에서 `.pane`의 `overflow: hidden`에 잘려 손도 못 대던 GM 제안 후보 버튼·보내기 단추를 `.composer` 두 줄(`min-height: 0` + `overflow-y: auto`)로 되살리고, 그물의 판정 기준을 「뷰포트 위치」에서 「스크롤 경로 유무」로 바꿔 상태 칸의 스크롤 가능한 조작 칸까지 정확히 가르게 만들었다.**

## Performance

- **Duration:** ~35min
- **Completed:** 2026-08-24T03:44:09Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- `scripts/check_narrow_viewport.py`의 고정판(`.pane--chat`)을 `ChatPane.tsx`의 실제 세 층(`.chat__head` → `.chat` → `.composer`, GM 제안 카드 포함)으로 확장 — 수정 전 실행이 844×390·390×844 양쪽에서 조작부 잘림으로 빨강(exit 1)을 냈고, 844×390의 조작부 아래끝(652.0px)이 계획 목표 문서가 지목한 실측치와 정확히 일치했다
- `reachable(el)` 헬퍼 신설 — 조상을 `.shell`까지 거슬러 오르며 스크롤 경로 유무로 「닿을 수 있는가」를 판정. 상태 칸의 select/숫자칸(부모가 `overflow-y: auto`)은 화면 밖에 있어도 통과하고, 조작부(부모 `.pane`이 `overflow: hidden`)만 실패로 잡힌다
- `frontend/src/styles.css`의 `.composer`에 `min-height: 0` + `overflow-y: auto` 두 줄 — 흐름 항목 기본 `min-height: auto`가 막던 축소를 풀고, `.chat`이 이미 `flex: 1; min-height: 0`이라 먼저 0까지 준 뒤에만 조작부가 자기 안에서 스크롤한다. CSS 수정 후 네 형태 전부 통과, 1440×900 3컬럼 무변경(900/900/900)
- `TOUCH_TARGETS` 목록(`.composer`·`.composer__input`·`.composer__row .btn`·`.proposal .btn`·`.candidate`·`select`·`input[type="number"]`) 신설 — 자바스크립트와 파이썬이 하나의 목록을 함께 돌며 44px 높이와 스크롤 도달성을 같이 판정. 임시 확인에서 GM 후보 버튼 445.0px·497.0px, 보내기 단추 610.0px로 계획 문서 실측치와 정확히 같은 숫자가 빨강으로 잡혔다
- `check_fixture_still_mirrors_source()`의 감시를 `className=` 문맥 낱말 경계 매칭으로 좁힘 — `REQUIRED_SOURCE_CLASS_NAMES`에 `chat__head`·`chat`·`composer`·`proposal`(ChatPane·CreationPane)을 추가하고, 이름이 사라지면 exit 2, `chat`처럼 짧은 이름이 `chat-line`·`chat__head`·`pane--chat`에 부분 문자열로 걸려 검사가 무의미해지는 것을 함께 막았다

## Task Commits

1. **Task 1: 폰 가로에서 보내기 단추가 화면 안으로 돌아온다 — 고정판·판정·CSS를 한 줄기로 꿴다** — `98a50b7` (fix)
2. **Task 2: 사람이 실제로 누르는 것 전부에 같은 판정을 건다 — 44px이어도 못 누르면 통과가 아니다** — `1f8c905` (test)
3. **Task 3: 고정판이 실제 화면과 갈리면 그때 멈춘다 — 감시를 「문자열 있음」에서 「낱말로 붙어 있음」으로 좁힌다** — `504562d` (test)

**Plan metadata:** (이 커밋 뒤에 별도로 기록됨)

## Files Created/Modified

- `scripts/check_narrow_viewport.py` — 고정판 3층 확장(`.chat__head`/`.chat`/`.composer`) · `reachable()` 헬퍼 · `TOUCH_TARGETS` 목록 기반 통합 판정 · className 낱말 경계 감시(총 571줄, +213/-29)
- `frontend/src/styles.css` — `.composer`에 `min-height: 0` + `overflow-y: auto` 두 줄 및 원인·기각 대안 셋을 담은 주석

## 빨강·초록 출력 전문

### Task 1 — RED (CSS 수정 전, 고정판·reachable()·judge ⑤만 추가된 상태)

```
  - 조작부(.composer)가 'pane pane--chat'에 잘려 아래끝 924.4px가 화면(844px) 밖이다 — 스크롤로도 못 닿는다
  - 조작부(.composer)가 'pane pane--chat'에 잘려 아래끝 652.0px가 화면(390px) 밖이다 — 스크롤로도 못 닿는다
1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px · 문서높이 900.0px · 통과
960x1080 · 상태 270.0px · 서사 378.0px · 대화 432.0px · 문서높이 1080.0px · 통과
390x844 · 상태 211.0px · 서사 295.4px · 대화 337.6px · 문서높이 844.0px · 실패
844x390 · 상태 97.5px · 서사 136.5px · 대화 156.0px · 문서높이 390.0px · 실패
EXIT=1
```

652.0px는 12.3-16-PLAN.md 목표 문서(objective)가 지목한 「보내기 단추가 610px」와는 다른 값이지만(고정판이 대상으로 삼은 것은 조작부 전체 상자의 아래끝이지 개별 단추가 아니다), Task 2 임시 확인에서 개별 단추 실측치(610.0px)가 정확히 일치함을 재확인했다(아래 참조).

### Task 1 — GREEN (CSS 수정 후)

```
1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px · 문서높이 900.0px · 통과
960x1080 · 상태 270.0px · 서사 378.0px · 대화 432.0px · 문서높이 1080.0px · 통과
390x844 · 상태 211.0px · 서사 295.4px · 대화 337.6px · 문서높이 844.0px · 통과
844x390 · 상태 97.5px · 서사 136.5px · 대화 156.0px · 문서높이 390.0px · 통과
EXIT=0
```

### Task 2 — 임시 확인(`.composer`의 `overflow-y: auto` 한 줄만 제거, 확인 뒤 원복)

```
  - 말 쓰는 칸(.composer__input) 아래끝 882.4px가 'pane pane--chat'에 잘려 화면(844px) 밖이다 — 스크롤로도 못 닿는다
  - 보내기 단추(.composer__row .btn) 아래끝 882.4px가 'pane pane--chat'에 잘려 화면(844px) 밖이다 — 스크롤로도 못 닿는다
  - 말 쓰는 칸(.composer__input) 아래끝 610.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
  - 보내기 단추(.composer__row .btn) 아래끝 610.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
  - 제안 카드의 다시 쓰기 단추(.proposal .btn) 아래끝 549.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
  - GM 제안 후보 버튼(.candidate) 1번째 아래끝 445.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
  - GM 제안 후보 버튼(.candidate) 2번째 아래끝 497.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
390x844 · 실패, 844x390 · 실패
EXIT=1
```

후보 버튼 445.0px·497.0px, 보내기 단추 610.0px — 12.3-16-PLAN.md 목표 문서가 지목한 폰 가로 실측치와 정확히 일치. 확인 후 `git status --short frontend/src/styles.css`가 빈 출력임을 확인(작업 트리 오염 없음).

### Task 2 — 오늘 코드(수정 전 상태)에서 실행

```
1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px · 문서높이 900.0px · 통과
960x1080 · 상태 270.0px · 서사 378.0px · 대화 432.0px · 문서높이 1080.0px · 통과
390x844 · 상태 211.0px · 서사 295.4px · 대화 337.6px · 문서높이 844.0px · 통과
844x390 · 상태 97.5px · 서사 136.5px · 대화 156.0px · 문서높이 390.0px · 통과
EXIT=0
```

상태 칸의 select/숫자칸이 부모(`.pane--status`)의 스크롤 경로 안에 있다는 이유로 통과 — 거짓 빨강 없음.

### Task 3 — 임시 확인 (`ChatPane.tsx`, 확인마다 원복)

```
㉠ className="composer" → "composer2":
고정판이 흉내 내는 클래스 이름이 실제 소스에서 사라졌다 — 화면 마크업이 바뀌어 이 고정판은 더 이상 실제 화면을 안 비춘다. 종료 코드 2.
  - frontend/src/panes/ChatPane.tsx — 'composer' 클래스를 못 찾음
EXIT=2

㉡ className="chat" → "chat9" (chat-line/chat__head/pane--chat 6곳은 그대로 남김):
고정판이 흉내 내는 클래스 이름이 실제 소스에서 사라졌다 — 화면 마크업이 바뀌어 이 고정판은 더 이상 실제 화면을 안 비춘다. 종료 코드 2.
  - frontend/src/panes/ChatPane.tsx — 'chat' 클래스를 못 찾음
EXIT=2
```

두 확인 모두 원복 후 `git status --short frontend/src/panes/ChatPane.tsx`가 빈 출력임을 확인.

## `.proposal .btn`과 `.candidate`를 별개 항목으로 둔 이유

PLAN.md의 Task 2 서술은 GM 제안 후보 버튼을 `.proposal .btn`로 지목했으나, 실제 `ChatPane.tsx`(337-348행)를 확인하면 후보 버튼은 `className="candidate"`만 쓰고 `btn` 클래스는 안 쓴다 — `.btn`이 실제로 잡는 것은 `.proposal` 안의 「다시 쓰기」 단추 하나뿐이다. 계획의 acceptance criteria(`grep -cE '"\.composer__input"|"\.proposal \.btn"|"\.composer__row \.btn"'`)가 요구하는 문자열은 그대로 유지하되, `.candidate`를 별도 목록 항목으로 추가해 실제로 보고된 결함(GM 제안 후보 버튼 445px·497px)을 정확한 선택자로 재게 했다 — Rule 2(누락된 핵심 기능 자동 추가)에 해당하는 조정이다.

## Decisions Made

키 결정은 frontmatter `key-decisions`에 기록. 요지: (1) 코드 검토 CR-02의 `flex: 0 0 auto` + `max-height: 100%` 처방을 명시적으로 기각하고 두 줄만 채택 — 근거를 `.composer` 규칙 위 주석에 남김. (2) 스크롤 경로 유무(`reachable()`)로 판정 — 위치 비교였다면 상태 칸이 거짓 빨강을 냈을 것. (3) `.candidate`를 `TOUCH_TARGETS`에 추가(위 참조).

## Deviations from Plan

**1. [Rule 2 - 누락된 핵심 기능] `.candidate`를 TOUCH_TARGETS에 추가**
- **Found during:** Task 2
- **Issue:** PLAN.md는 GM 제안 후보 버튼을 `.proposal .btn` 선택자로 지목했으나, 실제 `ChatPane.tsx`의 후보 버튼은 `className="candidate"`만 쓰고 `.btn`은 안 쓴다. `.proposal .btn`만으로는 실제로 보고된 결함(445px·497px)의 요소를 안 잡는다
- **Fix:** `TOUCH_TARGETS`에 `.candidate`(GM 제안 후보 버튼)를 별도 항목으로 추가. 계획의 acceptance criteria가 요구한 `.proposal .btn` 문자열은 그대로 유지(「다시 쓰기」 단추를 잡는 유효한 선택자)
- **Files modified:** scripts/check_narrow_viewport.py
- **Verification:** 임시 확인에서 `.candidate` 1번째·2번째가 정확히 445.0px·497.0px로 잡힘(위 출력 전문 참조)
- **Commit:** `1f8c905`

---

**Total deviations:** 1 auto-fixed (Rule 2)
**Impact on plan:** 계획이 지목한 실제 결함(GM 제안 후보 버튼 잘림)을 더 정확한 선택자로 재도록 보강 — scope creep 아님, 계획의 목표(사람이 실제로 누르는 것 전부에 판정을 건다)를 더 정확히 채움.

## Issues Encountered

- Task 2 임시 확인 중 첫 시도에서 `sed`로 파일 전체의 첫 `overflow-y: auto;` occurrence를 지웠는데, 그것이 `.pane--status`(162행, `.composer`와 무관)였다 — 즉시 백업본으로 원복하고, `.composer` 규칙 안의 정확한 줄 번호(1179행)를 확인한 뒤 재시도해 올바른 결과를 얻었다. 원복 확인(`git status --short`)을 매 시도마다 거쳤으므로 작업 트리 오염은 없었다.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- G-12.3-30(폰 가로/세로 조작부 잘림)이 닫혔다 — CSS 두 줄, 그물이 스스로 잡는다
- 남은 사람 확인 둘(D6·D7, `must_haves.truths`의 `verification: backstop` 두 줄과 동일): (1) 실제 폰으로 놀이 화면을 열어 서사가 읽을 만한 크기로 보이는지, (2) 만들기 중 실제 자기 차례를 받은 판에서 폰으로 조작 칸 셋(능력치 배치 select · 점수 숫자칸 · 인원 숫자칸)을 눌러 보는지 — 둘 다 기계가 답할 수 없는 영역이라 다음 실제 세션에서 사람이 확인해야 한다(12.3-15와 동일한 남은 확인)
- CHAR-06은 12.3-15에서 이미 Complete로 표시돼 있다 — 이 계획은 그 완료 상태 위에서 gap을 마저 닫은 것이라 REQUIREMENTS.md를 다시 건드리지 않았다

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-24*

## Self-Check: PASSED

- FOUND: `scripts/check_narrow_viewport.py`
- FOUND: `frontend/src/styles.css`
- FOUND: `.planning/phases/12.3-creation-screen/12.3-16-SUMMARY.md`
- FOUND: commit `98a50b7` (Task 1)
- FOUND: commit `1f8c905` (Task 2)
- FOUND: commit `504562d` (Task 3)
- FOUND: commit `3da9b92` (SUMMARY.md)
