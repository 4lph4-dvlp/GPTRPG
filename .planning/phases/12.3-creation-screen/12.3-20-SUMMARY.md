---
phase: 12.3-creation-screen
plan: 20
subsystem: testing
tags: [python, headless-chromium, self-test, exit-codes, gap-closure]

# Dependency graph
requires:
  - phase: 12.3-19
    provides: "조작 요소(TOUCH_TARGETS) 선택자 빈 값 경계 검사(extract_measurements(), 종료 코드 2) + 이름 감시 목록 확장"
provides:
  - "함정(TRAPS) 상수 넷째 칸 — 그 함정이 겨눈 단언의 표식(marker). 무엇을 겨눴는지가 코드에서 식별 가능해짐"
  - "_validate_trap_markers() — 표식이 비었거나 공백뿐이면 종료 코드 2로 막는 빈 값 경계(judge()가 못 겨눈 것을 통과로 접는 것을 막는 마지막 자리)"
  - "run_self_test()가 판정 통과 여부 하나가 아니라 「그 함정이 겨눈 표식이 사유에 실제로 있는가」까지 확인 — 세 갈래 인쇄(겨눈 단언 울림/초록/겨눈 단언 아님)"
  - "README §A 항목 2의 --self-test 종료 코드 1 설명이 두 경우(초록 · 빨강은 났지만 겨눈 단언 아님)를 모두 포함"
  - "12.3-16·17·18 범위 코드 검토 여섯 건(CR-01·WR-01·WR-02·IN-01·IN-02, 프론트매터는 6건으로 셈)의 처지 전부 deferred-items.md에 기록"
affects: [creation-screen, session-screen]

actuals:
  tokens: 4400
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "함정 상수에 넷째 칸(표식)을 추가해 「무엇을 겨눴는지」를 사람의 기억(주석)에서 코드의 값으로 승격 — _validate_shape_name_constants()와 같은 형태의 진입부 가드(_validate_trap_markers())가 표식 자신의 유효성(비어있지 않음)을 먼저 검사하는 패턴"

key-files:
  created: []
  modified:
    - scripts/check_narrow_viewport.py
    - README.md
    - .planning/phases/12.3-creation-screen/deferred-items.md

key-decisions:
  - "표식 값 셋(골격 상자·스크롤로도 못 닿는다·최소선)은 오늘 judge()의 실제 사유 문장을 grep해 각각 정확히 한 단언만 만드는 조각으로 골랐다 — 상태 칸 상한 단언은 '상한'을 쓰므로 서사 최소선의 '최소선'과 안 겹친다"
  - "Task 1 ①에서 넘침 단언(shellScrollHeight/shellClientHeight 비교 갈래)만 무력화해 재현했더니, 오늘 코드는 조작 요소 단언이 아니라 chatBottom 오버플로 하위 검사가 대신 잡아 줘 여전히 exit 0을 찍었다 — 어느 옆 단언이 대신 잡는지는 몰라도 되고, '겨눈 표식이 사유에 없다'는 사실 하나만으로 죽은 단언을 판정한다는 설계가 옳았음을 실측이 확인했다"
  - "Task 2는 코드 변경 없이 세 함정을 순서대로 죽였다 원복하는 검증만 하는 작업이라 --allow-empty 커밋을 썼다 — 증거는 커밋 본문에만 남고 diff는 0줄이다(계획의 reversibility 명시와 일치)"
  - "README는 --self-test 문단 하나만 넓히고 40행 근처 본 경로 문단(IN-02가 지적한 다른 결함)은 이번 계획의 승인 기준 밖이라 그대로 뒀다 — deferred-items.md에 이유와 다음에 볼 것을 남겼다"

patterns-established:
  - "판정 사유 문자열 표식 대조 — --self-test가 판정 함수의 (통과 여부) 계약을 안 바꾸고도 '그 함정이 겨눈 바로 그 단언이 울렸는가'를 확인하는 패턴. 다음에 함정이 늘면 넷째 칸에 그 단언만의 표식을 고르는 규칙(TRAPS 위 주석)을 따른다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "함정마다 겨눈 단언의 표식이 TRAPS 넷째 칸에 코드로 적혀 있고, --self-test가 그 표식이 실제 사유에 있는지까지 확인한다 — 통과 여부만 보던 예전 방식으로는 옆 단언이 대신 빨강을 내줘도 겨눈 단언의 죽음을 놓쳤다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py --self-test → EXIT=0, pixel-floor/clipped-composer/starved-story 각 줄에 골격 상자/스크롤로도 못 닿는다/최소선 표식이 사유에 포함됨을 실측"
        status: pass
    human_judgment: false
  - id: D2
    description: "겨눈 단언 셋을 각각 임시로 죽였을 때 --self-test가 실제로 종료 코드 1로 실패한다(Task 2, 커밋 0ad77d9 본문에 세 함정 각각의 숫자 기록)"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "넘침/닿을 수 있음/서사 최소선 단언을 각각 무력화 → --self-test EXIT=1(pixel-floor/clipped-composer/starved-story가 각각 표준오류에 출력) → 원복 → EXIT=0. Task 1 ①의 「전 0」 재현과 Task 2의 「후 1 · 원복 0」 실측이 커밋 본문에 숫자로 기록됨"
        status: pass
    human_judgment: false
  - id: D3
    description: "표식이 비었거나 공백뿐이면 종료 코드 2(확인 불가)로 끝난다 — 언제나 참인 표식이 이 확인을 다시 헐겁게 만드는 것을 막는다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "표식 하나를 빈 문자열로 바꾼 상태에서 --self-test → EXIT=2, 표준오류에 함정 이름('pixel-floor') 출력. 원복 후 다시 EXIT=0"
        status: pass
    human_judgment: false
  - id: D4
    description: "사람이 README만 보고 --self-test의 종료 코드 1이 두 경우(초록으로 나옴 · 빨강은 났지만 겨눈 단언이 아님)를 뜻한다는 것을 알 수 있다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "grep -c 'self-test' README.md == 2, README --self-test 종료 코드 1 문단에 '빨강은 났지만 다른 단언이 대신 울렸다' 취지 문장 포함 확인, git diff -- README.md가 그 문단 안에만 있음 확인(40행 근처 본 경로 문단 0줄 변경)"
        status: pass
    human_judgment: false
  - id: D5
    description: "이번 회차 코드 검토 여섯 건(CR-01·WR-01·WR-02·IN-01·IN-02) 전부의 처지가 deferred-items.md에 기록됐다 — 접은 둘의 접은 곳, 안 고친 셋의 이유와 다음에 볼 것"
    requirement: "CHAR-06"
    verification:
      - kind: manual_procedural
        ref: "deferred-items.md 새 절에 다섯 식별자 전부 출현 확인(grep), 접은 것 2건/안 접은 것 3건 표와 새로 남긴 한계 2건 확인"
        status: pass
    human_judgment: false
  - id: D6
    description: "비-회귀 — 손 안 댄 코드에서 두 명령 모두 여전히 통과(0), .tsx/src/gptrpg 0줄, 새 꾸러미 0개, frontend 빌드/시험 통과, pytest 오늘과 같은 결과(기존 flaky 제외)"
    verification:
      - kind: unit
        ref: ".venv/bin/python scripts/check_narrow_viewport.py → EXIT=0(4줄) · --self-test → EXIT=0(3줄 전부 빨강, 겨눈 표식 포함) · cd frontend && npm run build 성공 · npm test → 7 files/104 tests passed · .venv/bin/python -m pytest -q --tb=short → 1406 passed, 1 pre-existing unrelated failure(test_event_schema_migration.py, src/gptrpg 미변경 확인) · git diff --name-only에 .tsx·src/gptrpg 0건"
        status: pass
    human_judgment: false
  - id: D7
    description: "사람이 실제 폰으로 서사 칸 가독성·조작 칸 촉감을 확인하는 것은 여전히 사람 몫이다(backstop) — 이 계획도 12.3-19도 안 닫는다"
    verification: []
    human_judgment: true
    rationale: "PROJECT.md/12.3-20-PLAN.md의 must_haves.truths가 verification: backstop으로 명시 — 기계는 「0이 아니다·세로 25%는 된다」·「44px 이상이고 스크롤로 닿는다」까지만 답하고, 읽을 만한지·누르는 느낌은 사람만 안다"

duration: ~25min
completed: 2026-08-24
status: complete
---

# Phase 12.3 Plan 20: 그물을 뒤집어 확인하는 명령이 겨눈 단언 대신 아무 단언이나 보던 맹점 종료 Summary

**`--self-test`가 함정마다 「겨눈 단언」의 표식을 사유 문장에서 대조하도록 고쳐, 옆 단언이 대신 빨강을 내줘도 겨눈 단언의 죽음이 통과(0)로 새던 것을 막았다 — 표식이 비면 종료 코드 2, 겨눈 단언이 안 울리면 종료 코드 1로 갈린다.**

## Performance

- **Duration:** ~25min
- **Completed:** 2026-08-24
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- `TRAPS` 상수에 넷째 칸(겨눈 단언의 표식)을 더했다 — `골격 상자`(넘침 단언) · `스크롤로도 못 닿는다`(닿을 수 있음 단언) · `최소선`(서사 최소선 단언). 오늘 `judge()`의 실제 사유 문장을 대조해 각각 정확히 한 단언만 만드는 조각으로 골랐고, 표식을 고르는 규칙을 상수 위 주석에 한 문단으로 남겼다
- **재현(Task 1 ①)**: 넘침 단언(`shellScrollHeight`/`shellClientHeight` 비교 갈래)만 임시로 무력화하고 오늘 코드로 `--self-test`를 돌리자 종료 코드 **0**을 찍었다 — 죽은 것은 넘침 단언인데(844×390의 `chatBottom` 오버플로 하위 검사가 대신 잡아 줘) 「단언들이 살아 있다」는 결과가 나왔다. 무력화는 확인 직후 완전히 원복했다
- `_validate_trap_markers()`를 신설해 `run_self_test()` 진입부에서 `_validate_shape_name_constants()` 바로 옆에 부른다 — 표식이 비었거나 공백뿐이면 어느 함정인지 담은 사유와 함께 종료 코드 **2**로 끝낸다(실측: 표식 하나를 빈 문자열로 바꾸자 즉시 종료 코드 2와 함정 이름 출력, 원복 후 다시 0)
- `run_self_test()`의 판정 읽기를 「통과 여부」 하나에서 「통과 여부 + 겨눈 표식이 사유에 있는가」 둘로 바꿨다. 인쇄가 세 갈래로 갈린다 — 겨눈 단언이 울림(빨강 + 표식 포함 사유) · 통과(초록, 단언이 죽었다) · **빨강은 났는데 겨눈 단언이 아님**(새 갈래 — 겨눈 표식과 대신 울린 사유를 함께 인쇄). 마지막 표준오류 문장도 두 죽음 경로(초록 · 빨강이지만 안 겨눔)를 함께 담도록 고쳤다
- **실증(Task 2)**: 겨눈 단언 셋을 순서대로 하나씩 임시로 죽여 `--self-test`가 실제로 종료 코드 **1**로 실패하는 것을 눈으로 확인했다 — 넘침 단언 무력화 시 `pixel-floor`가 「빨강(겨눈 단언 아님)」 갈래로, 닿을 수 있음 단언 무력화 시 `clipped-composer`가 「초록」 갈래로, 서사 최소선 단언 무력화 시 `starved-story`가 「빨강(겨눈 단언 아님)」 갈래로(상태 칸 상한 단언이 대신 울림) 각각 잡혔다. 셋 다 원복 후 다시 종료 코드 0을 확인했고, 코드 변경이 남지 않아(`git diff` 0줄) 이 작업은 증거만 커밋 본문에 남기는 `--allow-empty` 커밋으로 기록했다
- README §A 항목 2의 `--self-test` 종료 코드 `1` 설명을 넓혔다 — 예전엔 「단언 중 하나가 죽었다(함정이 초록으로 나옴)」하나만 적혀 있었는데, 이제는 「빨강은 났지만 다른 단언이 대신 울렸다」도 같은 1이라는 것을 문장으로 밝혔다. `0`·`2` 설명과 40행 근처 본 경로 문단은 그대로 뒀다
- `deferred-items.md`에 이번 회차(12.3-16·17·18 범위) 코드 검토 여섯 건의 처지를 새 절로 남겼다 — 접은 둘(CR-01은 12.3-19, WR-01은 이 계획), 안 고친 셋(WR-02·IN-01·IN-02) 각각의 이유와 다음에 볼 것, 그리고 이번 회차가 새로 남긴 한계 둘(중첩 구조 드리프트를 두 그물 다 못 잡는 것, 표식이 다른 단언 사유에도 걸리면 확인이 다시 헐거워지는 것)

## Task Commits

1. **Task 1: 함정마다 「겨눈 단언」을 코드에 적고, 그것이 실제로 울렸는지 확인한다** - `513792b` (fix)
2. **Task 2: 겨눈 단언 셋을 하나씩 죽여 보고, 명령이 실제로 실패하는지 눈으로 본다** - `0ad77d9` (fix, --allow-empty — 증거만 커밋 본문에 기록, 코드 변경 없음)
3. **Task 3: 사람이 읽는 두 기록을 코드와 맞춘다 — README의 종료 코드와 검토 여섯 건의 처지** - `4b8e83f` (docs)

## Files Created/Modified

- `scripts/check_narrow_viewport.py` — `TRAPS` 넷째 칸(표식) 추가 · `_validate_trap_markers()` 신설 · `run_self_test()` 판정 읽기를 표식 대조로 교체 + 도크스트링을 세 종료 코드 뜻 + 표식 방식의 한계로 갱신
- `README.md` — §A 항목 2의 `--self-test` 종료 코드 `1` 설명 문단 갱신(다른 자리 무변경)
- `.planning/phases/12.3-creation-screen/deferred-items.md` — 이번 회차 코드 검토 여섯 건(CR-01·WR-01·WR-02·IN-01·IN-02)의 처지 절 추가

## Decisions Made

키 결정은 frontmatter `key-decisions`에 기록. 요지: (1) 표식 셋은 오늘 사유 문장을 직접 grep해 겹치지 않는 조각으로 골랐다. (2) Task 1 ①의 재현 결과, 오늘 코드가 조작 요소 단언이 아니라 chatBottom 오버플로 하위 검사로 대신 잡혔다는 것을 실측으로 확인했지만, 표식 대조 설계는 「누가 대신 잡았는지」와 무관하게 동작한다. (3) Task 2는 순수 검증 작업이라 코드 diff 없이 `--allow-empty` 커밋으로 증거를 남겼다. (4) README는 지적된 다른 문서 결함(IN-02 이후 40행 문단)을 이번 계획의 좁은 범위 밖으로 명시적으로 남겨뒀다.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

`.venv/bin/python -m pytest -q --tb=short` 실행 중 `tests/test_event_schema_migration.py::test_events_db_v2_records_fold_without_exception_under_schema_6` 1건이 실패했다 — 이 계획이 만든 실패가 아니다(`git show 513792b^:tests/test_event_schema_migration.py`와 현재 파일이 동일함을 확인, `src/gptrpg/` 0줄 변경). 실패 사유 자체가 "이 파일의 실제 기록은 판 2다 — CONTEXT.md/RESEARCH.md/VALIDATION.md의 「판 5」 서술은 실제 파일과 어긋난다"는 사전 존재 문서-코드 불일치이고, `deferred-items.md`가 이미 기록해 둔 「12.3-11에서 발견」 시간 의존 flaky 건과는 다른 별개의 사전 존재 실패다. 이 계획의 범위(뷰포트 확인 스크립트·README·deferred-items.md) 밖이라 여기서 고치지 않는다 — 다음에 이 시험을 여는 계획이 참고할 것.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `check_narrow_viewport.py`의 `--self-test`가 이제 「그 함정이 겨눈 바로 그 단언」이 울렸는지까지 확인한다 — 12.3-17이 스스로 약속했던 문장("각 단언이 실제로 빨강을 내는지")이 처음으로 실제 코드가 됐다
- 표식 방식의 한계(judge()의 사유 문장을 고치는 사람이 표식도 함께 봐야 한다)는 도크스트링·상수 주석·deferred-items.md 세 자리에 일관되게 남겼다
- 이번 회차 코드 검토(12.3-16·17·18 범위) 여섯 건 전부의 처지가 기록됐다 — 이 gap 종료 라운드로 남겨진 열린 항목은 WR-02(강건성 개선)·IN-01(문서 한 줄)·IN-02(README 40행 문서 완결성)뿐이고 셋 다 must-have를 안 어겨 다음 계획이 편할 때 골라 닫으면 된다
- 12.3-18-SUMMARY.md의 D11·D12(사람이 실제 폰으로 서사 칸 크기·조작 칸 셋을 직접 확인)는 여전히 열려 있다 — 이 계획도 그것을 닫지 않는다(backstop, coverage D7)
- pytest의 사전 존재 무관 실패 1건(`test_event_schema_migration.py`)이 `Issues Encountered`에 기록됐다 — 다음에 그 파일을 여는 계획이 CONTEXT.md/RESEARCH.md/VALIDATION.md의 「판 5」 서술을 실제 판 2 기록과 맞출 수 있다

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-24*

## Self-Check: PASSED

- FOUND: `scripts/check_narrow_viewport.py`
- FOUND: `README.md`
- FOUND: `.planning/phases/12.3-creation-screen/deferred-items.md`
- FOUND: commit `513792b` (Task 1)
- FOUND: commit `0ad77d9` (Task 2)
- FOUND: commit `4b8e83f` (Task 3)
