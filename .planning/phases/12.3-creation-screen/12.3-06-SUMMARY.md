---
phase: 12.3-creation-screen
plan: 06
subsystem: creation
tags: [python, fastapi, react, typescript, vitest, pytest, dedupe]

requires:
  - phase: 12.3-01
    provides: "판 11 사건(CreationGmSpoke 등) 선언, CreationScreen 뼈대, 방장 재실 신호(POST /creation/host, D-11)"
  - phase: 12.3-03
    provides: "방장 잡기·승계(_browser_last_seen, HOST_IDLE_S) — 이 계획이 그 옆에 캐릭터 재실 표를 나란히 둔다"
  - phase: 12.3-04
    provides: "CreationPane의 지목 effect 배선(CR-01) — 실행 조건이 부트스트랩 세션에서 결코 참이 안 되던 자리"
provides:
  - "완전히 새 세션에서 아무도 항목을 하나도 안 낸 채로도 GM 첫 지목이 성공한다(D-06) — 재실 신호(character_id)가 후보 목록에 더해진다"
  - "두 번째·세 번째 사람도 이어서 지목된다 — _gm_dedupe_key가 그 호출이 실제로 쓴 후보 목록에서 나온다(T-12.3-20)"
  - "재실 신호가 하나도 없으면 지목은 여전히 409로 거절되고 사건이 안 남는다(탐침 empty)"
  - "재실 기반 후보는 남은 자리 수를 넘지 않는다 — 사건 기반 후보는 상한을 넘어도 안 잘린다(T-12.3-17, 탐침 adjacency)"
  - "화면의 부트스트랩 조건이 순수 함수(shouldNominateNext) 하나가 되어 일곱 경우가 시험으로 고정된다"
affects: [12.3-07]

actuals:
  tokens: 8950
  tasks: 4
  commits: 3

tech-stack:
  added: []
  patterns:
    - "add-alongside(승격 아님) — 사건에서 나온 후보 목록(unfinished_candidates)과 재실에서 나온 목록(present_candidates)을 합치지 않고 나란히 둔다. 정리·동의 관문은 여전히 사건 기반 목록만 본다(재시작 취약값이 명단 잠금에 안 흘러든다)"
    - "중복 방지 키는 그 호출이 실제로 쓴 값을 인자로 받는다 — 재계산하면 서로 다른 후보 집합이 같은 키로 뭉쳐 지난 지목이 재사용된다(T-12.3-20)"
    - "상한은 뒤쪽(신고값)만 자르고 앞쪽(사건값)은 절대 안 자른다 — 실제로 값을 낸 사람을 후보에서 빼면 그 사람이 영영 못 끝난다"

key-files:
  created: []
  modified:
    - src/gptrpg/web/creation_state.py
    - src/gptrpg/web/routes_creation.py
    - frontend/src/api/client.ts
    - frontend/src/screens/CreationScreen.tsx
    - frontend/src/panes/CreationPane.tsx
    - frontend/src/session/creationView.ts
    - frontend/src/session/creationView.test.ts
    - frontend/src/api/creationBody.test.ts
    - tests/test_creation_screen_state.py

key-decisions:
  - "Task 0(checkpoint:decision, gate=blocking-human) — 사장님이 직접 갈래 ①(재실 신호에 character_id를 얹는다)을 선택했다. 갈래 ②(지목 호출이 자기 식별자를 낸다)는 후보가 항상 한 명이라 GM이 고를 게 없어져 D-06 취지를 흐리고, 갈래 ③(참가 사건화)은 사건 기록을 되돌릴 수 없게 바꿔야 해서 기각됐다"
  - "assumption_delta: add-alongside를 그대로 따랐다 — present_candidates를 wrap_up·동의 관문·폴링의 creation_unfinished_character_ids에 흘려보내지 않았다. 재실 표는 서버 재시작에 취약하므로 그 값이 명단 잠금 판단에 들어가면 재시작 직후 조기 잠금 오판이 날 수 있다"
  - "_gm_dedupe_key에 candidates 매개변수를 더하되 기본값 None으로 두어 다른 kind(announce/follow_up/wrap_up) 호출부는 무변경으로 남긴다 — nominate 갈래만 새 인자를 쓴다"

patterns-established:
  - "재실 표(_character_last_seen)를 present_candidates 안에서 앞(사건)·뒤(신고) 두 목록으로 나눠 합치고, 뒤쪽만 남은 자리로 자르는 패턴 — 사건 기반 값과 신고 기반 값을 같은 자료구조에 넣되 신뢰 수준이 다르다는 것을 코드 구조로 표현한다"

requirements-completed: []
# CHAR-06은 이 계획으로 아직 [x]가 안 된다 — 사람 확인이 필요한 두 켜(이
# 계획의 좁은 backstop 진실 하나 + 12.3-05가 이미 적어 둔 14개 항목
# 전체)가 남아 있고, 같은 phase의 12.3-07이 아직 SUMMARY가 없어 공유 ID
# 게이트(#2388)도 자동으로 막는다. "요구사항 조기 완료 표시 함정" 메모를
# 다시 밟지 않는다.

coverage:
  - id: D1
    description: "완전히 새 세션(아무도 항목을 안 낸 상태)에서 인원 확정 → 안내 → 지목이 200으로 성공하고, 지목 사건이 정확히 하나 기록된다(D-06)"
    requirement: "CHAR-06"
    verification:
      - kind: integration
        ref: "tests/test_creation_screen_state.py::test_nominate_after_party_size_fixed_works_with_no_prior_step_submission"
        status: pass
      - kind: other
        ref: "같은 시험을 이 계획의 서버 변경 직전 커밋(bb9e75f 이전)에 대고 실행 — 409로 실패(RED) 확인"
        status: pass
    human_judgment: false
  - id: D2
    description: "첫 사람이 완성된 뒤 두 번째 지목이 다른 사람을 가리키고 새 사건으로 남는다 — 후보 목록이 다시 비어 같은 교착이 되풀이되지 않는다"
    requirement: "CHAR-06"
    verification:
      - kind: integration
        ref: "tests/test_creation_screen_state.py::test_nominate_moves_to_the_next_present_participant_after_the_first_one_finishes"
        status: pass
    human_judgment: false
  - id: D3
    description: "재실 신호가 하나도 없으면 지목은 여전히 409로 거절되고 creation_gm_spoke 사건이 안 남는다(탐침 empty)"
    requirement: "CHAR-06"
    verification:
      - kind: integration
        ref: "tests/test_creation_screen_state.py::test_nominate_without_any_presence_signal_is_still_rejected_and_records_no_event"
        status: pass
    human_judgment: false
  - id: D4
    description: "재실 기반 후보는 남은 자리 수를 넘지 않고, 사건 기반 후보는 상한을 넘어도 안 잘린다(T-12.3-17). 같은 상태를 두 번 물으면 같은 순서가 나온다(탐침 ordering)"
    requirement: "CHAR-06"
    verification:
      - kind: integration
        ref: "tests/test_creation_screen_state.py::test_nominate_candidates_never_exceed_the_remaining_party_size"
        status: pass
    human_judgment: false
  - id: D5
    description: "화면의 부트스트랩 조건이 순수 함수(shouldNominateNext) 하나가 되어 일곱 경우(부트스트랩·2차 교착·지목 있음·안내 없음·명단 잠김·경계값·인원 미확정)가 시험으로 고정된다"
    requirement: "CHAR-06"
    verification:
      - kind: unit
        ref: "frontend/src/session/creationView.test.ts::shouldNominateNext (7건)"
        status: pass
      - kind: other
        ref: "npm --prefix frontend run build (타입 검사 포함) — 0으로 종료"
        status: pass
    human_judgment: false
  - id: D6
    description: "POST /creation/host 응답과 폴링 state·events 어디에도 browser_id가 실리지 않는다 — T-12.3-05 보호가 이번 수정으로 안 무너진다"
    requirement: "CHAR-06"
    verification:
      - kind: integration
        ref: "tests/test_creation_consent_host.py (전체 15건, 유출 방지 시험 포함)"
        status: pass
      - kind: other
        ref: "git diff에 event_log/schema.py·rules_core/reducer.py·web/routes_events.py 없음(사건 기록·폴링 응답 모양 무변경) — grep으로 확인"
        status: pass
    human_judgment: false
  - id: D7
    description: "실제 브라우저에서 인원 확정 → 안내 → 첫 사람 지목 → 항목 확정 화면(stepEditingEnabled)이 열리는 데까지 사람이 직접 밟아 본다"
    requirement: "CHAR-06"
    verification: []
    human_judgment: true
    rationale: "계획 frontmatter가 이 진실을 verification: backstop으로 명시했다 — 두 브라우저(또는 두 탭)로 실제 AI 호출(creation_gm)까지 거쳐야 확인되는 진실이라 자동화 시험이 대신할 수 없다. 오케스트레이터가 이 계획 실행 도중 '이 항목이 다시 걸리면 자동 처리하지 말고 그대로 되돌려 달라'고 명시적으로 지시했다 — 아래 §사람 확인이 필요합니다 참조"

# Metrics
duration: 체크포인트 재개 세션 — Task 0 결정 대기로 한 차례 중단됐다 정확한 총 소요는 못 잰다
completed: 2026-08-21
status: complete
---

# Phase 12.3 Plan 06: 첫 지목 교착 gap 닫기 Summary

**재실 신호(`POST /creation/host`)에 캐릭터 식별자를 얹어 서버가 「지금 누가 와 있는가」를 알게 하고, 그 값을 지목 후보 목록에 사건 기반 목록과 나란히(add-alongside) 더해 첫 지목이 구조적으로 못 일어나던 교착을 풀었다.**

## Performance

- **Tasks:** 4 (Task 0 결정 + Task 1 tracer + Task 2 TDD + Task 3 회귀 그물)
- **Files modified:** 9
- **Commits:** 3 (Task 0은 결정만, 커밋 없음)

## Accomplishments

- 완전히 새 세션(아무도 항목을 안 낸 상태)에서 인원 확정 → 안내 → 지목이 200으로 성공한다 — 오늘 코드에서는 409로 실패하던 것을 먼저 확인(RED)한 뒤 고쳤다(GREEN)
- 첫 사람이 완성된 뒤 두 번째·세 번째 사람도 이어서 지목된다 — 후보가 다시 비어 같은 교착이 되풀이되지 않는다
- 재실 신호가 하나도 없으면 여전히 409로 거절되고 사건이 안 남는다(탐침 `empty` 해소)
- 재실 기반 후보에 남은 자리 수 상한을 두되, 사건 기반 후보(실제로 값을 낸 사람)는 절대 안 잘린다(T-12.3-17, 탐침 `adjacency` 해소)
- `_gm_dedupe_key`가 그 호출이 실제로 쓴 후보 목록을 인자로 받아, 후보가 바뀌었는데 키가 안 바뀌어 지난 지목이 재사용되는 사고(T-12.3-20)를 막는다
- 화면의 부트스트랩 판단이 `shouldNominateNext` 순수 함수 하나가 되어 일곱 경우가 시험으로 고정됐다 — 이 결함이 원래 빠져나간 이유(컴포넌트 안 조건은 이 저장소의 RTL 없는 시험 방식으로 못 본다)를 구조적으로 닫는다

## Task Commits

Task 0(checkpoint:decision, gate=blocking-human)은 코드 변경이 없어 커밋이 없다 — 사장님이 갈래 ①을 직접 선택했다(오케스트레이터를 통해 전달).

1. **Task 1: 새 세션에서 첫 지목이 실제로 일어난다** - `bb9e75f` (fix)
2. **Task 2: 화면의 부트스트랩 조건을 순수 함수로 끄집어내 시험한다** - `8443a04` (test, TDD RED→GREEN)
3. **Task 3: 두 번째·세 번째 사람과 경계값을 회귀 그물로 고정한다** - `556a1a4` (test)

**Plan metadata:** (이 커밋 — 아래 최종 커밋 해시)

## Files Created/Modified

- `src/gptrpg/web/creation_state.py` - `_character_last_seen` 표, `mark_character_present`, `present_candidates`(add-alongside + 뒤쪽만 상한)
- `src/gptrpg/web/routes_creation.py` - `ClaimHostRequest.character_id`, `claim_creation_host`가 그 값으로 `mark_character_present` 호출, `_gm_dedupe_key(candidates=...)`, `nominate_creation_speaker`가 `present_candidates`를 쓰고 그 목록을 그대로 키에 넘김
- `frontend/src/api/client.ts` - `claimCreationHost(sessionId, browserId, characterId)` 세 번째 인자
- `frontend/src/screens/CreationScreen.tsx` - `beacon()`이 `myCharacterId`를 넘기고 effect 의존성에 더함
- `frontend/src/session/creationView.ts` - `shouldNominateNext(state, announced)` 신설
- `frontend/src/session/creationView.test.ts` - `shouldNominateNext` 일곱 경우
- `frontend/src/panes/CreationPane.tsx` - 지목 effect가 `shouldNominateNext`를 쓰도록 교체, `gmLinesFrom(events)` 한 번만 호출해 `wrappedUp`/`announced` 공유
- `frontend/src/api/creationBody.test.ts` - 재실 신호 본문에 `character_id`가 실리는지 확인(서버 기본값이 관대해 유일한 가드)
- `tests/test_creation_screen_state.py` - `_claim_host` 도우미, `_fix_party_size`에 `browser_id` 매개변수 추가, 새 시험 넷(부트스트랩 지목 · 다음 참가자 지목 · 상한 경계 · 빈 후보 거절)

## Decisions Made

- **Task 0 (사장님 결정, checkpoint:decision, gate=blocking-human):** 갈래 ①(재실 신호에 `character_id`를 얹는다) — 계획의 권장안 그대로. 사건 기록을 안 바꾸므로 되돌리기 쉽고, GM이 여러 후보 중 실제로 고르는 모양(D-06 취지)이 유지된다.
- **add-alongside(승격 아님):** `present_candidates`를 `wrap_up`·동의 관문·폴링의 `creation_unfinished_character_ids`에 흘려보내지 않았다 — 재실 표는 재시작에 취약하므로 그 값이 명단 잠금 판단에 들어가면 재시작 직후 조기 잠금으로 오판할 수 있다. 계획의 `<assumption_delta_decision>`을 그대로 따른 것으로, 실행 중 추가 판단은 없었다.
- 그 밖에는 계획서에 적힌 대로 실행했다 — 실행 중 새로 내린 아키텍처 판단은 없다.

## Deviations from Plan

None - 계획대로 실행했다. 유일한 실행 중 조정은 `test_nominate_candidates_never_exceed_the_remaining_party_size`에서 인원 상한 시험 값을 계획 예시의 2가 아니라 3으로 바꾼 것인데(던전월드류의 인원 하한이 3이라 2로는 애초에 `party-size` 자체가 409로 거절된다), 이것은 계획이 확정한 동작(`DUNGEONWORLD_PARTY_SIZE_RANGE`)에 시험 데이터를 맞춘 것이지 계획을 벗어난 것이 아니다.

## Issues Encountered

None.

## User Setup Required

None - 외부 서비스 설정이 필요 없다.

## 사람 확인이 필요합니다 (Task 0의 `<must_haves.truths>` backstop 진실)

**아직 하지 않은 것을 숨기지 않고 적는다.** 계획의 `must_haves.truths`에 `verification: backstop`으로 명시된 진실 하나가 이 계획의 자동화 범위 밖이다:

> "실제 브라우저에서 인원 확정 → 안내 → 첫 사람 지목 → 항목 확정 화면이 열리는 데까지 사람이 직접 밟아 본다"

이번 실행 도중 오케스트레이터가 명시적으로 지시했다 — "이 항목이 또 걸리면 자동으로 처리하지 말고 사람에게 되돌려 달라." 그래서 이 계획은 서버·화면 코드와 자동화 시험(백엔드 1373 passed, 프런트 80 passed, 기존 만들기 경로 회귀 56 passed)까지만 마쳤고, 실제 두 번째 차단점(첫 지목 후 `stepEditingEnabled` 화면이 실제로 열리는지)의 라이브 확인은 사람 몫으로 남겨 둔다.

**확인 방법(실행할 준비는 됐지만 이번 실행에서 서버를 띄우지 않았다 — 실제 AI 호출 비용이 드는 작업이라 사람이 준비됐을 때 시작하는 것이 낫다고 판단했다):**

```bash
GPTRPG_DB=.gptrpg/qa-12-3-06.db uv run uvicorn gptrpg.web.app:app --host 0.0.0.0 --port 8000
npm --prefix frontend run dev  # 포트 5173, /api를 8000으로 프록시
```

새 세션 하나만 열면 된다(이번 gap은 방장 승계나 두 번째 탭이 아니라 "혼자서도 첫 지목이 되는가"이므로):
- http://localhost:5173/?session=qa-12-3-06

**확인할 것:**
1. [ ] 인원을 정하면(예: 3명) 다음 화면(GM 안내 요청)으로 넘어간다
2. [ ] 「진행자에게 안내를 부탁하기」를 누르면 필요 항목 안내가 뜬다(실제 AI 호출, 수 초~10여 초 걸릴 수 있다)
3. [ ] **안내 뒤 몇 초 안에(수동 조작 없이) 자기소개 항목을 채우는 화면(`stepEditingEnabled`)이 실제로 열린다** — 이것이 이 계획이 고친 gap의 핵심이다. 1차·2차 검증(`12.3-VERIFICATION.md`)에서 여기가 영원히 안 열렸다
4. [ ] 항목을 하나 채우고 확정할 수 있다

**통과 기준:** 4개 전부. 실패하면 그 번호와 실제로 본 것을 그대로 적어 둔다 — 「대체로 됐다」로 넘기지 않는다. 통과하면 `12.3-VERIFICATION.md`의 해당 gap을 닫힌 것으로 갱신하고 다음 검증 라운드에서 `12.3-05-SUMMARY.md`가 남긴 나머지 항목(주사위 연출·D-08·동의 관문 이름 표시 등)으로 진행한다.

**CHAR-06은 이 계획에서도 여전히 미체크로 남는다** — 위 backstop 하나와 `12.3-05-SUMMARY.md`가 이미 적어 둔 14개 항목 전체가 아직 확인되지 않았고, 같은 phase의 `12.3-07-PLAN.md`도 아직 SUMMARY가 없어 공유 ID 게이트가 어차피 자동으로 막는다.

## Known Stubs

없음 - 이 계획이 새로 만든 스텁은 없다.

## Threat Flags

없음 - 계획의 위협 등록부(T-12.3-17/18/19/20)가 승인된 완화 계획대로 구현됐다. `_character_last_seen`이 새 프로세스 메모리 표이지만 `_browser_last_seen`(T-12.3-12)과 같은 전제 위에서 accept됐다(계획 threat_model 참조) — 새로 드러난 표면은 없다.

## Next Phase Readiness

- 첫 지목 교착이 풀려 CHAR-06 흐름(안내 → 값 확정 → GM 정리 → 동의 → 점유)의 "값 확정"이 실 사용자 경로로 처음 열릴 수 있는 조건이 갖춰졌다 — 다만 그 사실은 위 §사람 확인이 필요합니다가 아직 라이브로 못 지켰다.
- `12.3-07-PLAN.md`(agents.json 관련, wave 6)가 이 계획에 의존한다 — 다음 순서.
- 12.3-05가 남긴 14개 사람 확인 항목과 이 계획의 backstop 진실은 같은 라이브 세션에서 함께 확인할 수 있다(둘 다 `.gptrpg/agents.json`의 creation_gm/master_gm 폴백 설정을 그대로 쓴다).

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-21*

## Self-Check: PASSED

- FOUND: `.planning/phases/12.3-creation-screen/12.3-06-SUMMARY.md`
- FOUND: commit `bb9e75f` (Task 1)
- FOUND: commit `8443a04` (Task 2)
- FOUND: commit `556a1a4` (Task 3)
- FOUND: `def present_candidates(` in `src/gptrpg/web/creation_state.py`
- FOUND: `export function shouldNominateNext(` in `frontend/src/session/creationView.ts`
