---
phase: 12.3-creation-screen
plan: 18
subsystem: ui
tags: [python, headless-chromium, viewport, test-gate, exit-codes, gap-closure, docs]

# Dependency graph
requires:
  - phase: 12.3-16
    provides: "폰 조작부 잘림 결함 종료 CSS + reachable() 기반 닿을 수 있음 단언 + TOUCH_TARGETS 목록"
  - phase: 12.3-17
    provides: "--self-test 명령 + 골격 자신의 넘침을 재는 단언(shellScrollHeight/clientHeight)"
provides:
  - "빈 문서(about:blank)를 재고 판정하던 우연(잡히지 않은 TypeError)을 명시 가드로 대체한 iframe.onload"
  - "고정판에 재야 할 요소가 없으면(null) 판정 없이 종료 코드 2로 끝나는 빈 값 경계(missingElements)"
  - "측정값 키·형태 이름·차례를 함께 검사해 KeyError가 종료 코드 1로 새지 않게 하는 extract_measurements()"
  - "형태 조건부 단언(배경색·넓은 창)이 도는 이름이 SHAPES에 실제로 있는지 시작 시 확인하는 가드"
  - "sys.path 명시 + try/except로 실행 방식과 무관해진 check_insecure_origin import"
  - "넓은 창(1440×900) 대조군이 서사 칸 왼쪽 위치·폭으로 3컬럼 붕괴를 실제로 잡는 단언(storyLeft/storyWidth)"
  - "상태 칸 25dvh 상한을 좁은 형태에서 실제로 지키는 단언"
  - "json.dumps 결과의 `/` 이스케이프 + --no-sandbox 근거 주석"
  - "README §A 항목 2 갱신(4가지 측정 대상 + --self-test 소개) + 코드 검토 16건 전부의 처지 기록(deferred-items.md)"
affects: [creation-screen, session-screen]

actuals:
  tokens: 7900
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "missingElements 빈 값 경계 — JS onload가 querySelector 결과가 null인 요소를 이름으로 모아 측정값에 담아 돌려주고, 파이썬이 그것을 보고 판정 없이 종료 코드 2로 끝내는 패턴. TypeError를 우연히 제어 흐름으로 쓰는 대신 의도를 코드로 적는다"
    - "_validate_shape_name_constants() — 형태 조건부 단언(MINE_LINE_COLOR_CHECK_SHAPE·WIDE_SHAPE)이 걸린 이름이 SHAPES에 실제로 있는지 실행 시작 시 확인하는 가드. 조건부 단언을 추가할 때마다 이 가드에 한 줄 더하는 패턴을 남겼다"
    - "REQUIRED_MEASUREMENT_KEYS + zip(data, SHAPES) 이름·차례 대조 — extract_measurements()가 개수만 세던 것에서 키·이름·차례 셋을 함께 보는 것으로 강화. KeyError/순서 뒤섞임이 종료 코드 1로 새는 길을 막는다"

key-files:
  created: []
  modified:
    - scripts/check_narrow_viewport.py
    - README.md
    - .planning/phases/12.3-creation-screen/deferred-items.md

key-decisions:
  - "iframe.onload의 about:blank 가드는 '.shell 존재'로 판정 — 삽입 직후 재항해에서 요소가 null이라 던지던 TypeError에 우연히 기대던 것을, doc이 우리 고정판인지 명시로 확인하고 아니면 그냥 돌아가는 코드로 바꿨다. done()을 안 부르므로 다음 실제 로드가 정상 처리된다"
  - "missingElements는 모든 형태 측정값에 상시 존재하는 키로 설계 — 정상 측정 시 빈 배열, 요소 없음 시 무엇이 없는지 담긴 배열. REQUIRED_MEASUREMENT_KEYS 검사는 missingElements가 비어 있을 때만 나머지 키를 요구해, '요소 없음이라 나머지를 아예 안 쟀다'는 상태와 '쟀는데 키가 빠졌다'는 상태를 코드로 갈랐다"
  - "WIDE_SHAPE(1440x900)의 3컬럼 판정은 storyLeft <= OVERFLOW_TOLERANCE_PX로 — 왼쪽 상태 칸이 268px 고정 폭이므로 서사 칸이 왼쪽 끝에 붙으면 그 칸이 사라진 것이다. 다른 원인으로 상태 칸이 사라져도 결론(3컬럼이 아니다)은 같아 판정이 여전히 옳다(surfaced_assumption ㉠)"
  - "상태 칸 25dvh 상한 단언은 WIDE_SHAPE를 제외한 모든 형태에서 판정 — 넓은 형태에는 그 미디어쿼리 규칙 자체가 없으므로 판정 대상에서 뺐다"
  - "import 방어는 sys.path 명시 삽입 + try/except ImportError 이중화 — 전자는 -m 실행·pytest 수집 경로에서 자동 삽입이 안 일어나는 실제 원인을 고치고, 후자는 그래도 실패하는 경우(파일 없음 등)를 종료 코드 2로 접기 위한 안전망이다"

patterns-established:
  - "형태 조건부 단언을 추가할 때마다 _validate_shape_name_constants()에 한 줄을 더해 '조건부 단언이 조용히 안 도는 길'을 구조적으로 막는 패턴 — WR-04가 지적한 실패 모양이 다시 안 생기게 한다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "빈 문서(about:blank)를 재고 판정하는 길이 잡히지 않은 예외 대신 명시 가드로 막힌다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python scripts/check_narrow_viewport.py (네 형태 전부 통과, exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "재야 할 요소가 하나라도 고정판에 없으면 판정 없이 종료 코드 2다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — .pane--story 한 줄 제거 → exit 2, 원복 후 git status --short 빈 출력"
        status: pass
    human_judgment: false
  - id: D3
    description: "측정값 키 누락·형태 이름/차례 어긋남이 트레이스백 대신 종료 코드 2로 끝난다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — storyHeight 키 삭제 → exit 2 + 키 이름 인쇄(트레이스백 아님), 원복 확인"
        status: pass
    human_judgment: false
  - id: D4
    description: "형태 조건부 단언(배경색)이 도는 이름이 SHAPES에서 사라지면 그 사실을 인쇄하고 종료 코드 2다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — 390x844 형태 이름 변경 → exit 2 + '배경색 단언이 한 번도 실행되지 않는다' 취지 출력, 원복 확인"
        status: pass
    human_judgment: false
  - id: D5
    description: "import가 -m 실행·저장소 밖 실행 양쪽에서 트레이스백 없이 끝나고 종료 코드가 0 또는 2다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: ".venv/bin/python -m scripts.check_narrow_viewport (exit 0) · 저장소 밖에서 절대경로 실행 (exit 0)"
        status: pass
    human_judgment: false
  - id: D6
    description: "넓은 창(1440×900) 대조군이 3컬럼 붕괴를 실제로 잡는다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — frontend/src/styles.css 미디어쿼리 max-width를 1080px→2000px로 확대 → exit 1 + '3컬럼이 무너졌다' 사유, 원복 후 git status --short 빈 출력"
        status: pass
    human_judgment: false
  - id: D7
    description: "상태 칸의 25dvh 상한이 좁은 형태에서 실제로 단언으로 지켜진다"
    requirement: "CHAR-06"
    verification:
      - kind: automated_ui
        ref: "임시 확인 — .pane--status의 max-height: 25dvh 한 줄 제거 → exit 1 + 상태 칸 상한 사유, 원복 후 git status --short 빈 출력"
        status: pass
    human_judgment: false
  - id: D8
    description: "README만 보고 두 명령(check_narrow_viewport.py, --self-test)을 언제 왜 부르는지 알 수 있다"
    requirement: "CHAR-06"
    verification:
      - kind: other
        ref: "grep -c 'self-test' README.md >= 1, 2026-08-24 폰 가로 사고 언급 확인"
        status: pass
    human_judgment: false
  - id: D9
    description: "코드 검토 16건 전부의 처지가 기록으로 남고, 조용히 사라진 항목이 0건이다"
    requirement: "CHAR-06"
    verification:
      - kind: other
        ref: "grep -oE 'CR-0[1-3]|WR-0[1-7]|IN-0[1-6]' deferred-items.md | sort -u | wc -l == 16"
        status: pass
    human_judgment: false
  - id: D10
    description: "비-회귀 — 네 형태 전부 통과 · --self-test 통과 · frontend 빌드/시험 통과 · pytest 기존과 동일(1406 passed, 사전 존재 events.db 오염 실패 1건 제외)"
    verification:
      - kind: unit
        ref: "cd frontend && npm run build && npm test (7 files, 104 tests passed) · .venv/bin/python -m pytest -q --tb=short (1406 passed, 1 pre-existing unrelated failure)"
        status: pass
    human_judgment: false
  - id: D11
    description: "사람이 실제 폰으로 놀이 화면을 열어 서사가 읽을 만한 크기로 보이는 것을 눈으로 확인한다"
    human_judgment: true
    rationale: "기계가 답하는 것은 「0이 아니다 · 세로의 4분의 1은 된다」까지다. 그 크기로 이야기를 읽을 만한지는 사람만 안다"
  - id: D12
    description: "사람이 만들기 중 자기 차례를 실제로 받은 판에서 폰으로 조작 칸 셋(능력치 배치 칸 · 점수 숫자칸 · 인원 숫자칸)을 눌러 본다"
    human_judgment: true
    rationale: "조작 칸 자체가 44px여도 그 칸을 담은 조작부 전체가 화면 밖으로 잘려 있으면 애초에 「누를 화면」에 닿지 못한다는 것이 12.3-16에서 실제로 드러났다 — 이 계획의 단언은 「44px이고 스크롤로 닿는다」까지만 답한다"

duration: ~65min
completed: 2026-08-24
status: complete
---

# Phase 12.3 Plan 18: 「확인을 못 했다」가 「통과」로 보이는 남은 길들 종료 Summary

**`check_narrow_viewport.py`가 빈 문서를 재고 판정하던 우연(잡히지 않은 예외), 측정값 키 누락·형태 이름 갈림이 종료 코드 1로 새던 구멍, 넓은 창 대조군이 폭을 하나도 안 재던 헐거움을 전부 명시 가드로 닫고, README와 deferred-items.md에 코드 검토 16건 전부의 처지를 기록했다.**

## Performance

- **Duration:** ~65min
- **Completed:** 2026-08-24T04:16:23Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- `iframe.onload`가 `appendChild` 시점의 `about:blank` 재항해에서 요소가 `null`이라 던지는 TypeError에 우연히 의존하던 것을, `.shell` 존재를 명시로 확인하는 가드로 교체했다(WR-01) — 누군가 "견고화"로 null 가드를 넣어도 빈 문서를 재고 판정에 들어가는 길이 이제 코드로 막혀 있다
- 고정판의 필수 요소(`.shell`·`.pane--status`·`.pane--story`·`.pane--chat`·`.chat-line` 둘) 중 하나라도 `null`이면 `missingElements` 배열에 담아 돌려주고, 파이썬이 판정 없이 종료 코드 2로 끝나게 했다 — 임시로 `.pane--story` 한 줄을 지워 exit 2를 실측으로 확인했다
- `extract_measurements()`가 개수만 세던 것에서 키·형태 이름·차례를 함께 보는 것으로 강화됐다(WR-05) — `storyHeight` 키를 임시로 지우자 트레이스백 대신 `"1440x900 측정값에 ['storyHeight']가 없다 — 확인 불가."`가 인쇄되고 종료 코드 2로 끝남을 확인했다
- `_validate_shape_name_constants()` 신설 — `MINE_LINE_COLOR_CHECK_SHAPE`·`WIDE_SHAPE`가 `SHAPES`에 실제로 있는지 실행 시작 시 확인한다(WR-04). `390x844`를 임시로 `390xXXX`로 바꾸자 `"'390x844' 형태가 SHAPES에 없어 배경색 단언... 한 번도 실행되지 않는다"`가 인쇄되고 종료 코드 2로 끝남을 확인했다
- import를 `sys.path.insert` 명시 + `try/except ImportError`로 감쌌다(WR-05 후반) — `.venv/bin/python -m scripts.check_narrow_viewport`와 저장소 밖 절대경로 실행 둘 다 트레이스백 없이 exit 0으로 끝남을 확인했다
- 넓은 창(1440×900) 대조군이 서사 칸의 `left`/`width`(`storyLeft`/`storyWidth`)를 실제로 재게 됐다(WR-02) — 미디어쿼리 `max-width`를 임시로 2000px로 넓혀 3컬럼을 무너뜨리자 `"넓은 창인데 서사 칸이 왼쪽 끝(0.0px)에 붙었다 — 3컬럼이 무너졌다"`가 찍히고 exit 1로 끝남을 확인했다
- 상태 칸 25dvh 상한을 좁은 형태에서 실제로 판정한다(IN-02) — `.pane--status`의 `max-height: 25dvh` 한 줄을 임시로 지우자 세 좁은 형태 모두 서사 칸 0px + 상태 칸 상한 초과 사유가 함께 찍히고 exit 1로 끝남을 확인했다
- 죽은 필드 `innerWidth`를 지우고, `chatHeight` 옆에 판정에 안 쓰는 이유를 주석으로 남겼다. `json.dumps` 결과의 `/`를 이스케이프해 고정판에 `</script>`가 들어가도 호스트 문서가 안 깨지게 했고, `--no-sandbox` 근거 주석을 추가해 `grep -c 'no-sandbox'`가 2가 되게 했다(IN-05)
- README §A 항목 2를 실제로 재는 네 가지(서사 칸·골격 넘침·조작부 닿을 수 있음·넓은 창 3컬럼)에 맞춰 갱신하고, 2026-08-24 폰 가로 사고 한 문장과 `--self-test` 소개 문단(무엇을 하는지 + 종료 코드 셋)을 추가했다
- `deferred-items.md`에 코드 검토 16건(critical 3 · warning 7 · info 6) 전부의 처지를 기록했다 — 접은 12건은 계획·작업 번호와 함께, 안 접은 4건(IN-01·IN-03·IN-04·IN-06 남은 절반)은 이유와 함께. `grep -oE 'CR-0[1-3]|WR-0[1-7]|IN-0[1-6]'`로 16개 식별자 전부 확인했다

## Task Commits

1. **Task 1: 「확인을 못 했다」가 「통과」로 보이는 남은 세 길을 막는다** — `1fa0a93` (fix)
2. **Task 2: 대조군이 대조 노릇을 하게 만들고, 수집만 하고 안 쓰던 값들을 단언으로 바꾼다** — `408ba5a` (fix)
3. **Task 3: 사람이 무엇을 언제 부르는지 README에 적고, 검토 16건 전부의 처지를 기록으로 남긴다** — `af7a4f0` (docs)

**Plan metadata:** (이 커밋 뒤에 별도로 기록됨)

## Files Created/Modified

- `scripts/check_narrow_viewport.py` — 빈 문서 가드 · 빈 값 경계(missingElements) · 키/이름/차례 검사 · 형태 조건부 단언 존재 가드 · import 방어 · 넓은 창 3컬럼 단언 · 상태 칸 25dvh 상한 단언 · JSON 이스케이프 · 도크스트링 정합화(총 985줄, +226/-9)
- `README.md` — §A 항목 2 갱신(4가지 측정 대상 + 2026-08-24 사고 한 문장) + `--self-test` 소개 문단
- `.planning/phases/12.3-creation-screen/deferred-items.md` — 「2026-08-24 코드 검토 16건의 처지」 절 + 「기계가 답할 수 없는 것」 절

## 빨강·초록 출력 전문 (임시 확인)

### Task 1 — 빈 값 경계(㉢, `.pane--story` 제거)

```
1440x900 — 고정판에서 재야 할 요소가 없다: ['.pane--story']
960x1080 — 고정판에서 재야 할 요소가 없다: ['.pane--story']
390x844 — 고정판에서 재야 할 요소가 없다: ['.pane--story']
844x390 — 고정판에서 재야 할 요소가 없다: ['.pane--story']
판정하지 않는다 — 확인 불가. 종료 코드 2.
EXIT=2
```

### Task 1 — 키 누락(㉡, storyHeight 삭제)

```
1440x900 측정값에 ['storyHeight']가 없다 — 확인 불가.
EXIT=2
```

### Task 1 — 형태 이름 갈림(㉠, 390x844 → 390xXXX)

```
'390x844' 형태가 SHAPES에 없어 배경색 단언(내 줄/남의 줄 구분, G-12.3-27)이 한 번도 실행되지 않는다. 종료 코드 2.
EXIT=2
```

### Task 2 — 3컬럼 무너짐(㉠, max-width: 1080px → 2000px)

```
  - 넓은 창인데 서사 칸이 왼쪽 끝(0.0px)에 붙었다 — 3컬럼이 무너졌다
1440x900 · 상태 225.0px · 서사 315.0px · 대화 360.0px · 골격내용 900.0px · 실패
EXIT=1
```

### Task 2 — 상태 칸 상한 사라짐(㉡, max-height: 25dvh 제거)

```
  - 서사 칸이 0.0px로 최소선 270.0px(뷰포트의 25%)보다 낮다
  - 상태 칸이 648.0px로 상한 270.0px(뷰포트의 25%)를 넘었다
  - 서사 칸이 0.0px로 최소선 211.0px(뷰포트의 25%)보다 낮다
  - 상태 칸이 506.4px로 상한 211.0px(뷰포트의 25%)를 넘었다
  - 서사 칸이 0.0px로 최소선 97.5px(뷰포트의 25%)보다 낮다
  - 상태 칸이 234.0px로 상한 97.5px(뷰포트의 25%)를 넘었다
960x1080 · 상태 648.0px · 서사 0.0px · 대화 432.0px · 골격내용 1080.0px · 실패
390x844 · 상태 506.4px · 서사 0.0px · 대화 337.6px · 골격내용 844.0px · 실패
844x390 · 상태 234.0px · 서사 0.0px · 대화 156.0px · 골격내용 390.0px · 실패
EXIT=1
```

모든 임시 확인 뒤 `git status --short`가 빈 출력임을 확인해 작업 트리 오염 없음을 검증했다.

### 오늘 코드(수정 완료 상태)에서 두 명령 실행

```
$ .venv/bin/python scripts/check_narrow_viewport.py
1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px · 골격내용 900.0px · 통과
960x1080 · 상태 270.0px · 서사 378.0px · 대화 432.0px · 골격내용 1080.0px · 통과
390x844 · 상태 211.0px · 서사 295.4px · 대화 337.6px · 골격내용 844.0px · 통과
844x390 · 상태 97.5px · 서사 136.5px · 대화 156.0px · 골격내용 390.0px · 통과
EXIT=0

$ .venv/bin/python scripts/check_narrow_viewport.py --self-test
pixel-floor · 844x390 · 빨강 · 골격 내용(513.0px)이 골격 상자(390.0px)를 넘어 잘렸다
clipped-composer · 844x390 · 빨강 · 조작부 전체(.composer) 아래끝 652.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
starved-story · 390x844 · 빨강 · 서사 칸이 0.0px로 최소선 211.0px(뷰포트의 25%)보다 낮다
EXIT=0
```

## Decisions Made

키 결정은 frontmatter `key-decisions`에 기록. 요지: (1) about:blank 가드는 `.shell` 존재로 판정 — 잡히지 않은 예외에 기대던 우연을 명시 코드로 바꿨다. (2) `missingElements`는 모든 형태에 상시 존재해 「요소 없음(측정 자체를 안 함)」과 「키 빠짐(측정은 했는데 갑자기 없음)」을 코드로 갈랐다. (3) `WIDE_SHAPE`의 3컬럼 판정은 `storyLeft`로 — 상태 칸이 268px 고정 폭이라는 사실에 기댄다. (4) 상태 칸 상한 단언은 `WIDE_SHAPE`를 제외한 모든 형태에서만 돈다. (5) import 방어는 경로 명시 + try/except 이중화.

## Deviations from Plan

**1. [작업 재구성 — 실행 프로토콜 준수] Task 1/2 코드 편집을 통합 후 재분리**

계획을 따라 두 파일(고정판·판정 로직)을 편집하는 과정에서 Task 1과 Task 2가 손대는 함수(특히 `judge()`·`extract_measurements()`·`iframe.onload`)가 겹쳐 처음에는 두 작업을 한 번에 편집했다. 실행 프로토콜(작업마다 원자적으로 커밋)을 지키기 위해, 원본 파일로 되돌린 뒤 Task 1분만 다시 적용해 검증·커밋하고, 그 뒤 Task 2분을 diff로 추출해 적용·검증·커밋했다 — 최종 코드 상태는 통합 편집과 바이트 단위로 동일하고(diff 확인), 각 커밋이 각자의 `<verify>`를 독립적으로 통과한다.

- **Files modified:** scripts/check_narrow_viewport.py
- **Verification:** Task 1 커밋 단독 상태에서 exit 0/0 확인 후 Task 2 diff 적용, 최종 상태가 통합 편집본과 `diff`로 바이트 단위 일치
- **Commit:** `1fa0a93`, `408ba5a`

---

**Total deviations:** 1 (작업 재구성, 코드 변경 없음 — 계획이 지시한 산출물과 완전히 동일)
**Impact on plan:** scope creep 아님. 실행 순서만 조정했고 최종 코드는 계획이 지시한 것과 정확히 같다.

## Issues Encountered

- `12.3-16-SUMMARY.md`의 클립된 조작부 단언(WR-04 관련 코드 주석 위치)이 "닿을 수 있음 단언 ⑤"를 언급하고 있었는데, 이번 계획이 `judge()`에 새 단언(⑤ 넓은 창 3컬럼, ⑥ 상태 칸 상한)을 추가하면서 실제 번호가 밀렸다 — `TRAPS`의 `clipped-composer` 주석을 "단언 ③"으로 정정했다(사소한 문서 정합성 수정, Rule 1).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 코드 검토 16건(critical 3 · warning 7 · info 6) 전부의 처지가 `deferred-items.md`에 기록됐다 — 접은 12건(12.3-16/17/18에 분산), 안 접은 4건은 각각 이유가 있고 Phase 16(디자인 시스템)이나 「세 번째 브라우저 확인이 생길 때」로 명시적으로 미뤄졌다
- `check_narrow_viewport.py`가 이제 「확인을 못 했다」가 「통과」로 보이는 다섯 자리(빈 문서·요소 없음·키 빠짐·조건부 단언 침묵·import 실패)를 전부 종료 코드 2로 접는다 — 이 파일이 스스로 선언한 세 종료 코드 계약이 실제 코드와 맞아떨어진다
- 남은 사람 확인 둘(D11·D12, 12.3-16-SUMMARY.md의 D6·D7과 동일)은 여전히 열려 있다 — 다음 실제 세션에서 사람이 폰으로 직접 확인해야 한다
- CHAR-06은 12.3-15에서 이미 Complete로 표시돼 있다 — 이 계획은 그 완료 상태 위에서 gap을 마저 닫은 것이라 REQUIREMENTS.md를 다시 건드리지 않았다
- 이 계획으로 phase 12.3의 18개 PLAN.md 전부에 짝이 맞는 SUMMARY.md가 생겼다(`ls *-PLAN.md`/`*-SUMMARY.md` 둘 다 18개)

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-24*

## Self-Check: PASSED

- FOUND: `scripts/check_narrow_viewport.py`
- FOUND: `README.md`
- FOUND: `.planning/phases/12.3-creation-screen/deferred-items.md`
- FOUND: commit `1fa0a93` (Task 1)
- FOUND: commit `408ba5a` (Task 2)
- FOUND: commit `af7a4f0` (Task 3)
