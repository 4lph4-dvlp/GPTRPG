---
status: partial
phase: 12.3-creation-screen
source: [12.3-01-SUMMARY.md, 12.3-02-SUMMARY.md, 12.3-03-SUMMARY.md, 12.3-04-SUMMARY.md, 12.3-05-SUMMARY.md, 12.3-06-SUMMARY.md, 12.3-07-SUMMARY.md, 12.3-08-SUMMARY.md, 12.3-09-SUMMARY.md, 12.3-11-SUMMARY.md, 12.3-12-SUMMARY.md, 12.3-13-SUMMARY.md, 12.3-14-SUMMARY.md, 12.3-VERIFICATION.md]
started: 2026-08-22T00:15:00Z
updated: 2026-08-23T00:00:00Z
---

## Current Test

[testing paused — 6 items outstanding]

테스트 3~8은 G-12.3-8(되물음 막다른 골목) 뒤에 있어 지금은 걸을 수 없다.
아무도 캐릭터를 완성한 적이 없으므로 `complete` 이후의 모든 것 —
GM 정리 · 동의 · 명단 잠금 · 세션 진입 · 동의 전파 — 이 한 번도
실행된 적이 없다. G-12.3-8을 닫은 뒤 테스트 3부터 다시 걷는다.

## Tests

<!-- 이 목록의 앞 5개는 ROADMAP.md가 이 단계의 확인 관문으로 지정한
     「처음부터 끝까지 화면 조작만으로」를 순서대로 쪼갠 것이다.
     한 번도 연속으로 수행된 적이 없어 이번에 처음 걷는다. -->

### 1. 만들기 화면에 들어가고 안내가 보인다
expected: 링크를 열면 만들기 화면이 뜨고 GM의 첫 안내가 보인다
result: pass
previously: issue (severity blocker)
reported: "일반적인 http://100.81.73.102:8000는 잘 열리는데 (…) 아무것도 안 보여 / 강력 새로고침도 해보고, 다른 브라우저에서도 해보고 시크릿 모드로도 해봤는데 안 보여"
resolved_by: 12.3-11 (커밋 7ddbb81 · 검증 7차)
confirmed: |
  2026-08-22, 같은 tailscale 주소(http://100.81.73.102:8000)로 재확인. 사람 보고:
  "먼저 연 사람이 플레이 인원수를 정하는 숫자를 뜨는 화면이 나오고 다른 사용자
  브라우저에서는 방을 연 사람이 정하고 있다는 화면이 뜨면서 그 사용자는 설정을 할 수
  없어. 대신 먼저 연 사람이 창을 닫으면 일정 시간 후 남은 사람이 방장의 기능을
  이어 받는 것 같아"
  화면이 떴을 뿐 아니라 D-11(방장 = 가장 먼저 들어온 사람 · 이탈 시 승계)이
  실제로 동작하는 것까지 사람 눈으로 확인됐다.

### 2. 자기 차례를 받고 항목 값을 낸다
expected: GM이 나를 지목하고, 편집 조작이 열리며, 값을 내면 화면에 반영된다
result: issue
reported_3: |
  2026-08-23 2회차 (세션 uat23b). ③ 안내는 저절로 나온다 — 다만
  "로딩 애니메이션이 있으면 좋을 것 같음. 사람들이 모르고 계속 새로 고침 할 수도
  있을 것 같아". ④ 누구 차례인지 확인됨. ⑤ 능력치 배치 여섯 칸 전부 채우고
  확정까지 감 — 확인됨.
  막힌 곳: "모든 내용을 채우고 진행자에게 물어보기 버튼이 나오는데 누르면
  진행자가 잠시 말을 잃었지만 계속합니다 라고 안내가 뜸. 하지만 다른 플레이어들에게
  순서가 가지 않아 다른 사람들은 캐릭터 생성을 할수가 없음"
severity: blocker
closed_by_this_round: |
  G-12.3-2(인원 범위) · G-12.3-5(안내 자동 호출) · G-12.3-6(능치 배치 확정) ·
  G-12.3-7(누구 차례인지) — 넷 다 사람 눈으로 닫힌 것이 확인됐다.
  G-12.3-3/4(거절 문구)는 3~5 밖의 값을 아예 못 넣는 구조로 바뀌어
  거절이 생길 자리 자체가 사라졌다.
previously: issue (severity blocker)
progress: |
  2026-08-23 1회차 (세션 uat23a) — 부분 확인:
  ① 인원 칸이 처음부터 3으로 뜬다 → 확인됨 (G-12.3-2 닫힘)
  ② 3~5 밖의 값을 아예 넣을 수 없다 → 확인됨 (G-12.3-3/4는 거절 자체가
     안 생기는 구조로 대체됨 — 거절 문구 잔존 문제가 발생할 자리가 사라졌다)
  ③~⑤ 미확인 — 진행자 호출이 503으로 떨어져 중단. 원인은 제품이 아니라
     서버 기동 실수(`.env.local`의 NVIDIA_API_KEY 미로드). 키를 실어 재기동한
     뒤 `POST /creation/announce`가 실제 GM 문구와 함께 200을 낸 것을 확인했다.
     새 세션 이름으로 2회차를 걷는다.
observation: |
  ③에서 본 「진행자를 다시 불러오기」 단추 — 운영자 설정 결함(503)일 때는
  몇 번을 눌러도 성공할 수 없는 단추다. 이번엔 원인이 사람 쪽 실수였으므로
  결함으로 올리지 않는다. 다만 「눌러도 소용없는 단추」가 남는 자리는 맞다.
retest_reason: |
  이 테스트가 낸 결함 여섯(G-12.3-2 … G-12.3-7)이 12.3-12/13/14로 전부
  코드에서 닫혔다. 사람이 다시 걸어야 실제로 닫혔는지 안다.
reported: |
  문제 1: "아까 인원을 2명으로 정했는데, 이때 방장의 화면에 「인원 범위가 어긋난다:
  2명은 최소 3명보다 적다 (allowed=PartySizeRange(min_player_characters=3,
  max_player_characters=5), requested=2)」라는 오류 메시지가 남았어. 근데 이후 계속
  남아 있어."
  문제 2: "3명으로 설정했지만, 브라우저는 2명개만 켰어. 그러니까 「다른 사람의
  차례예요 — 자유롭게 끼어들어 말할 수 있어요」라는 문구가 나와있고 「불러오는 중…」
  라는 문구가 있고 「진행자에게 안내를 부탁하기」 버튼이 뜨긴 하는데 이걸 눌러야
  하는지 모르겠어. 3개의 안내가 모두 한번에 뜨니까 기다려야 하는지, 눌러야 하는지
  헷갈려"
reported_2: |
  재시도(브라우저 2개, 안내 단추를 양쪽에서 누름): "둘 중 나중에 누른 사용자에게 먼저
  차례가 갔어. 이 차례의 순서를 모르겠어. 이후 선택지 4개가 나오는 팝업이 나오는데,
  여기서 아무거나 고르니 다음으로 정하는 속성에 대해서는 입력창이 나와 내가 자율적으로
  쓸 수 있어. 능력 배정하기에서는 정해진 수치들을 분배할 수 있도록 되어 있었는데, 한번
  선택된 수치는 다른 수치 선택지에서 선택할 수 없었어. 이건 좋은것 같아. 근데 문제는
  1수치가 2번 고를 수 있는데, 1을 한 곳에 쓰면 나머지 1도 선택이 막혀 쓸 수가 없어.
  이건 문제야."

### 3. 되묻기 · 고치기 · 주사위
expected: GM이 되물으면 답할 수 있고, 이미 낸 값을 다시 열어 고칠 수 있으며, 주사위 연출이 판정 화면과 같아 보인다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 4. GM 정리 · 동의 · 명단 잠금
expected: GM이 캐릭터를 한 줄로 정리해 주고, 동의를 누르면 명단이 잠긴다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 5. 자기 캐릭터로 세션에 들어간다
expected: 명단 잠금 뒤 자기 캐릭터로 실제 세션 화면에 진입한다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 6. 동의 표시가 다른 브라우저로 전파된다
expected: 별도 브라우저 둘로 접속해 한쪽이 동의를 누른 뒤 다른 쪽이 자기 항목을 다시 열면, 먼저 동의한 쪽 화면의 동의 표시도 「안 누름」으로 되돌아간다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 7. 회복 5분이 답답하지 않은가 — 그리고 회복 뒤 실제로 이어지는가
expected: 지목받고 아무것도 안 한 채 5분 기다리면 차례가 다른 사람에게 넘어가고, 그 뒤 다시 지목받으면 실제로 편집 화면이 다시 열린다. 5분이 흐름을 심하게 끊지 않는다고 느껴진다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 8. D-13 세 문구가 헷갈리지 않는가
expected: 화면에 나오는 D-13 세 문구를 처음 보는 사람이 오해 없이 읽을 수 있다
result: pending
unblocked_by: 12.3-11 — 테스트 1의 blocker가 닫혀 이제 수행할 수 있다

### 9. 끼어들기 입력이 실제로 쓸 만한가 (WR-01)
expected: 남의 차례일 때 끼어들기로 한마디 넣어 본다. 「입력이 아예 안 보인다」인지 「보이는데 눌러도 거절당한다」인지 확인한다 — 어느 쪽인지에 따라 고치는 방향이 달라진다
result: pass
reported: "끼어들기 자체는 다른 화면에서 사용할 수가 있었다"
evidence: |
  사건 로그 seq 10 (01:39:51) — pc-ca9b917c가 pc-7cd76ab6의 차례 도중
  "ㅅㅂ 내 순서는 언제 돼?"를 실제로 남겼다. 입력이 보였고 받아들여졌다.
  WR-01의 두 갈래(「안 보인다」/「보이는데 거절당한다」) 중 어느 쪽도 아니다.
  다만 그 내용 자체가 G-12.3-8의 피해를 그대로 보여준다 — 앞사람이
  막다른 골목에 갇혀 있어서 뒷사람이 하염없이 기다렸다.
note: 12.3-11이 이 부분을 의도적으로 안 건드렸다 (D-08의 「남의 차례에도 말은 항상 열려 있다」 규칙 표면을 건드리는 처방이라, 사람이 먼저 겪어 보고 방향을 골라야 한다)

## Summary

total: 9
passed: 2
issues: 1
pending: 6
skipped: 0
blocked: 0

## Gaps

<!-- 코드 검증(12.3-VERIFICATION.md)이 이미 찾아 둔 gap 하나가 따로 열려 있다.
     아래는 이 UAT가 처음으로 찾은 것이다. -->

- truth: "사람이 자기 기기의 브라우저로 링크를 열면 캐릭터 만들기 화면이 뜬다 (CHAR-06, ROADMAP.md가 정한 이 단계의 확인 관문)"
  gap_id: G-12.3-1
  status: resolved
  resolved_by: |
    12.3-11 (커밋 7ddbb81 · b85e296 · 4ef8276). `crypto.randomUUID()` 호출을
    안전하지 않은 맥락에서도 있는 `crypto.getRandomValues` 기반 경로로 갈아 끼웠고,
    앱 최상단 오류 경계를 세웠으며, `scripts/check_insecure_origin.py`가
    루프백 아닌 주소로 진짜 크로미움을 붙여 이 결함군을 앞으로도 잡는다.
    확인: 고치기 전 코드 → 종료 코드 1(실패), 고친 뒤 → 0(통과),
    `--host 127.0.0.1` → 2(확인 불가; 통과로 안 접는다).
  reason: |
    User reported: "일반적인 http://100.81.73.102:8000는 잘 열리는데 (…) 아무것도 안 보여 /
    강력 새로고침도 해보고, 다른 브라우저에서도 해보고 시크릿 모드로도 해봤는데 안 보여".
    브라우저 세 개 + 시크릿 모드 + 강력 새로고침 모두 빈 화면.
  severity: blocker
  test: 1
  root_cause: |
    `frontend/src/session/browserIdentity.ts:70`이 `crypto.randomUUID()`를 부른다. 이 API는
    브라우저가 「안전한 맥락(secure context)」에서만 제공한다 — HTTPS이거나 호스트가
    `localhost`/`127.0.0.1`일 때뿐이다. 평문 http로 일반 IP(여기서는 tailscale 주소
    `100.81.73.102`)에 접속하면 `crypto.randomUUID`가 `undefined`이므로 앱이 마운트 도중
    TypeError로 죽고 `#app`이 빈 채로 남는다.

    직접 재현 (browse 헤드리스, 소스 무변경):
      http://127.0.0.1:8000/?session=uat1     → 정상 렌더링 ("캐릭터 만들기 / 이번 판 인원을 정해요")
      http://100.81.73.102:8000/?session=uat2 → 화면 텍스트 비어 있음
                                                typeof crypto.randomUUID === "undefined"
                                                window.isSecureContext === false

    **왜 다섯 라운드의 검증이 이걸 못 잡았나:** 자동 시험과 QA 도구가 전부 `127.0.0.1`로만
    페이지를 열었다 — 유일하게 secure context가 성립하는 주소다. 그래서 실패 경로가
    한 번도 실행된 적이 없다.

    **왜 사용자 환경 문제가 아니라 제품 결함인가:** README의 실험 당일 절차(§B-6)가
    참가자 넷에게 `http://{서버 기기 주소}:8000/?session={식별자}` 링크를 나눠 주라고
    지시한다. 그 링크를 받는 참가자 전원이 빈 화면을 본다. 화면이 보이는 것은 서버를
    돌리는 그 기계 하나뿐이다.
  artifacts:
    - path: "frontend/src/session/browserIdentity.ts:70"
      issue: "`crypto.randomUUID()`는 secure context 전용 — 평문 http + 일반 IP에서 undefined다. 대체 경로가 없다"
    - path: "frontend/src/main.tsx (또는 앱 최상단)"
      issue: "오류 경계(error boundary)가 없어 마운트 중 예외가 조용히 삼켜진다 — 콘솔에도 아무것도 안 남아 원인 추적이 어렵다"
    - path: "tests · QA 도구 전반"
      issue: "화면을 여는 주소가 항상 127.0.0.1이라, 실제 배포 형태(평문 http + 일반 IP)가 한 번도 실행되지 않는다"
  missing:
    - "`crypto.randomUUID()`가 없을 때 쓸 대체 식별자 생성 경로 — secure context가 아닌 곳에서도 브라우저 식별자가 만들어져야 한다"
    - "앱 최상단 오류 경계 — 마운트 중 예외가 빈 화면 대신 사람이 읽을 수 있는 메시지로 보여야 한다"
    - "127.0.0.1이 아닌 주소로 페이지를 여는 확인 경로 — 이 결함군이 다시 조용히 지나가지 않게"
  debug_session: ""

- truth: "화면이 룰북의 인원 범위 안에서만 값을 내보낸다 — 사람이 원인을 알 수 없는 거절을 안 만난다"
  gap_id: G-12.3-2
  status: resolved
  resolved_by: |
    12.3-13 Task 1 (커밋 418b01c). `GET /creation/steps`가 배열에서
    `{steps, party_size_range}` 봉투로 바뀌어 인원 확정 전에도 룰북의 실제
    범위(던전월드류 3~5·케언 1~4·오픈퀘스트 2~6)를 화면이 안다.
    `PartySizeControl`의 초기값·min·max·확정 잠금이 그 범위로 세워진다
    (거울 함수 `partySizeOutOfRange`, 시험 4건). **자동으로 확인된 것:**
    서버가 실제 범위를 봉투로 내려준다는 것(서버 시험), 화면 코드에 룰북별
    숫자가 0건이라는 것(grep). **사람이 걸어야 하는 것:** 조작의 초기값이
    실제 `<input>`에 배선돼 사람 손에서 유효한 값으로 뜨는가(12.3-13
    Summary D2, `human_judgment: true`).
  severity: major
  test: 2
  root_cause: |
    서버는 룰북의 인원 범위를 **확정에 성공한 뒤에만** 화면에 알려 준다.
    `rulebook_min`/`rulebook_max`는 `party_size_fixed` **사건 payload**에만 있고
    (`frontend/src/api/types.ts:218-219`), 확정 **전에** 화면이 읽는 만들기 상태에는
    `party_size_fixed: number | null` 하나뿐이다(:353). `src/gptrpg/web` 어디에도
    `party_size_range`를 내려보내는 자리가 없다.

    그래서 `PartySizeControl`은 진짜 최소값을 알 방법이 없어 `1`을 하드코딩한다
    (`useState(1)`, `min={1}`, `Math.max(1, ...)`, `disabled={... count < 1}`).
    12.3-11이 WR-02를 닫으며 넣은 값인데, 룰북마다 최소가 다르다 —
    던전월드류 3(`dungeonworld_like.py:193`), 케언 1(`cairn.py:124`),
    오픈퀘스트 2(`openquest.py:174`). 이번 세션의 던전월드류에서는
    **조작의 초기값 1이 이미 거절당할 값이다.**

    WR-02가 닫으려던 것이 바로 「사람이 원인을 알 수 없는 거절을 만나는 자리」인데,
    실제 최소값 대신 1을 골라 그 자리가 0명에서 1~2명으로 옮겨졌을 뿐이다.
  artifacts:
    - path: "src/gptrpg/web (만들기 상태 응답)"
      issue: "룰북의 party_size_range를 확정 전에 화면으로 안 내려보낸다 — 화면이 옳은 값을 고를 근거 자체가 없다"
    - path: "frontend/src/screens/CreationScreen.tsx:104,110,120,127"
      issue: "최소값 1을 하드코딩. 룰북별 최소(3/1/2)와 무관하고, 던전월드류에서는 초기값부터 거절 대상이다"
  missing:
    - "만들기 상태 응답에 룰북의 인원 범위(min/max)를 실어 내려보낸다 — 특정 룰북 값을 화면에 하드코딩하지 않는다"
    - "PartySizeControl의 초기값·min·max·확정 잠금을 서버가 준 범위로 세운다"

- truth: "사람이 읽는 오류 문구에 파이썬 내부 표현이 안 새어 나온다 (D-15)"
  gap_id: G-12.3-3
  status: resolved
  resolved_by: |
    12.3-13 Task 2 (커밋 5ec89f1). `session_actor/actor.py::_prepare_fix_party_size`가
    `CommandRejected(str(exc))`(예외 전체, dataclass repr 포함) 대신
    `CommandRejected(exc.reason)`(사람이 읽을 절반)로 감싼다 —
    `allowed=PartySizeRange(...)` 꼬리표가 사라진다. **자동으로 확인됨**
    (액터 시험 1건: 거절 문구에 dataclass repr 자취가 없다는 것을 단언).
    `rules_core/rulebook.py`는 한 글자도 안 바뀌었다.
  severity: major
  test: 2
  root_cause: |
    `PartySizeOutOfRange.__init__`가 메시지를
    `f"인원 범위가 어긋난다: {reason} (allowed={allowed!r}, requested={requested!r})"`로
    만든다(`src/gptrpg/rules_core/rulebook.py:577-579`). `!r`가 dataclass repr을 그대로
    박는다. `session_actor/actor.py:1428`이 `CommandRejected(str(exc))`로 그 전체를
    사람 화면까지 통째로 보낸다.

    사람이 읽을 수 있는 절반은 **이미 따로 있다** — `exc.reason`이
    "2명은 최소 3명보다 적다"다. 웹 계층이 둘 중 틀린 쪽을 쓰고 있을 뿐이다.
    (개발자용 로그에는 repr이 유용하므로 예외 자체는 그대로 두는 것이 맞다.)
  artifacts:
    - path: "src/gptrpg/session_actor/actor.py:1428"
      issue: "str(exc) 대신 exc.reason을 써야 한다 — 사람 화면에 내부 표현이 새는 유일한 지점"
  missing:
    - "CommandRejected에 사람이 읽는 reason만 싣는다"

- truth: "실패한 오류 문구가 다음 시도에 안 남는다"
  gap_id: G-12.3-4
  status: resolved
  resolved_by: |
    12.3-13 Task 3 (커밋 e186a60). `PartySizeControl`의 새 시도 시작 두
    자리(`confirm()` 진입·숫자 변경)에서 `onError(null)`을 불러 이전 거절
    문구를 비운다 — `CreationPane.tsx`가 이미 쓰는 관례를 그대로 잇는다.
    **자동으로 확인된 것:** 배선의 모양(코드 자리에 `onError(null)`이
    정확히 2건, `onError` props 타입이 `string | null`로 넓어짐 — grep·tsc).
    **사람이 걸어야 하는 것:** 이 저장소에 RTL이 없어 JSX 지역 상태 전이를
    자동으로 실행할 방법이 없다 — 오류가 실제 화면에서 사라지는 것은 사람이
    브라우저로 확인해야 한다(12.3-13 Summary D4, `human_judgment: true`).
  severity: major
  test: 2
  root_cause: |
    `CreationScreen`의 `error` 상태를 지우는 `setError(null)`이 **`announce()` 안
    한 곳에만** 있다(`CreationScreen.tsx:287`). `PartySizeControl.confirm()`은 실패 때
    `onError(...)`로 채우기만 하고, **성공해도 안 지우고**(`onDone()`만 부른다),
    사람이 숫자를 고쳐도 안 지운다. 그래서 한 번 거절당한 문구가 성공한 확정 뒤에도
    화면에 그대로 붙어 있는다.
  artifacts:
    - path: "frontend/src/screens/CreationScreen.tsx:96-102,352"
      issue: "confirm() 성공 경로와 값 변경 경로에 오류 초기화가 없다"
  missing:
    - "새 시도를 시작할 때 이전 오류를 지운다"

- truth: "지금 무엇을 해야 하는지 화면이 한 가지로만 말한다 (D-15 「이유는 한 자리에만」)"
  gap_id: G-12.3-5
  status: resolved
  resolved_by: |
    12.3-14 Task 1. `creationView.ts::shouldAnnounce`(순수 함수, 시험
    다섯 — 함수를 쓰기 전에는 실패)가 「지금 첫 안내를 부를 때인가」를
    판정하고, `CreationScreen.tsx`가 그 함수 하나로 스스로
    `POST /creation/announce`를 부른다(여러 탭이 동시에 불러도 서버의
    `_gm_dedupe_key("announce")`가 세션당 한 번으로 접는다, D-12). 안내가
    없는 동안은 렌더 갈래 순서를 뒤집어 한 블록(진행 중 문구 또는 실패
    이유+재시도 단추)만 그린다 — 「기다려라」·「눌러라」·「불러오는 중」이
    동시에 뜨던 자리가 구조적으로 사라졌다. **자동으로 확인된 것:**
    발동 조건의 다섯 갈래(부른다·인원 확정 전·이미 안내 있음·직전 실패·
    명단 잠김)가 시험으로 고정됐다는 것, 화면이 그 함수 하나만 부른다는
    것(grep). **사람이 걸어야 하는 것:** 인원을 확정한 직후 아무것도 안
    누르고 기다렸을 때 안내가 실제로 오는가 — 순수 함수 시험은 「조건이
    옳은가」까지만 답하고 「그 조건이 참이 되는 순간을 effect가 보는가」는
    의존성 목록의 문제라 자동으로 안 잡힌다.
  severity: major
  test: 2
  root_cause: |
    안내가 아직 없을 때(`gate === "done"` && `gmLines.length === 0`) 세 신호가 동시에
    렌더된다:
      1. `CreationPane.tsx:638` — "다른 사람의 차례예요 — 자유롭게 끼어들어 말할 수 있어요"
      2. `CreationPane.tsx:642` (또는 `CreationScreen.tsx:338/349`) — "불러오는 중…"
      3. `CreationScreen.tsx:362` — "진행자에게 안내를 부탁하기" 단추

    셋 중 **둘이 사실과 다르다.** `announce()`는 그 단추의 onClick에서만 불리고
    자동 호출이 어디에도 없다(`CreationScreen.tsx:285-296`) — 즉 **아무도 안 누르면
    아무 일도 안 일어난다.** 그런데 "불러오는 중…"은 무언가 진행 중이라고 말하고,
    "다른 사람의 차례예요"는 기다리라고 말한다. 사람이 기다리기를 고르면 영원히
    기다린다.

    앞선 라운드(CR-02)가 이 단추의 **표시 조건**은 고쳤지만, 그 단추가 서로 모순되는
    두 신호와 **동시에** 뜬다는 것은 안 건드렸다. 「지금 사람이 무엇을 해야 하는가」를
    하나로 정하는 자리가 없고, 세 상태가 각자 독립으로 그려진다.
  artifacts:
    - path: "frontend/src/screens/CreationScreen.tsx:337-364"
      issue: "안내 없음 상태에서 로딩 문구와 행동 단추가 함께 뜬다 — 대기와 행동을 동시에 지시한다"
    - path: "frontend/src/panes/CreationPane.tsx:638-642"
      issue: "안내가 없고 아무것도 진행 중이 아닌데 「다른 사람의 차례」와 「불러오는 중」을 낸다"
  missing:
    - "안내가 없을 때 화면이 한 가지만 말한다 — 무엇을 눌러야 하는지"
    - "실제로 진행 중이 아닐 때 「불러오는 중…」을 안 띄운다"

- truth: "능력치 배치에서 룰북이 준 값 여섯 개를 여섯 칸에 전부 배정하고 확정할 수 있다"
  gap_id: G-12.3-6
  status: resolved
  resolved_by: |
    12.3-12 (커밋 700a87f·ad82111). `creationView.ts::remainingFixedValues`
    (순수 함수, 시험 3건 — 값이 중복된 풀 2,1,1,0,0,-1을 여섯 칸에 전부
    배정해 확정까지 가는 회귀 시험)가 값의 **집합**이 아니라 **개수**로
    잠금을 판정한다. `PlaceFixedValuesControl`의 선택지 `disabled`가 그
    함수에 배선됐고, React key도 값 기준에서 자리(index) 기준으로 바뀌어
    같은 값이 둘인 풀에서 key가 겹치던 잠재 결함(T-12.3-48)도 함께 닫혔다.
    **자동으로 확인된 것:** 산수 자체(순수 함수 시험). **사람이 걸어야
    하는 것:** 그 판정이 실제 `<select>`의 `disabled`에 배선돼 사람 손에서
    실제로 도는지, 확정 뒤 다음 항목으로 넘어가는지(12.3-12 Summary D3,
    `human_judgment: true`).
  severity: blocker
  test: 2
  root_cause: |
    `PlaceFixedValuesControl`이 이미 쓴 값을 **값의 집합(Set)**으로 기억한다
    (`frontend/src/panes/CreationPane.tsx:212-214`):
      `const usedValues = new Set(Object.values(assignment).filter(...))`
    그리고 각 선택지를 `disabled={usedValues.has(value) && assignment[axisName] !== value}`
    로 잠근다(:232). 집합은 **몇 개 있었는지를 안 센다** — 값 `1`을 한 칸에 쓰면
    「1은 쓰였다」만 남아, 풀에 남은 또 하나의 `1`까지 전부 잠긴다.

    던전월드류는 여섯 칸(STR DEX CON INT WIS CHA)에 값
    `(2, 1, 1, 0, 0, -1)`을 준다(`src/gptrpg/rulebooks/dungeonworld_like.py:158,162`).
    **서로 다른 값은 넷뿐이다**(2, 1, 0, -1). 그래서 네 칸을 채우고 나면 남은 두 칸에
    고를 수 있는 값이 하나도 안 남는다.

    `complete = axisNames.every((name) => assignment[name] !== null)`(:215)이 여섯 칸
    전부를 요구하므로 **「확정」 단추가 영원히 안 켜진다** — 이 룰북으로는 캐릭터
    만들기를 끝낼 수 없다. 실험 진행 자체가 여기서 멈춘다.

    올바른 규칙은 **값이 아니라 개수**다 — 풀에 값 V가 k개 있으면 V를 k칸까지 쓸 수
    있고, k칸을 다 쓴 뒤에만 잠긴다.

    같은 뿌리의 잠재 결함이 하나 더 있다: 선택지를 `key={value}`로 그린다(:232).
    풀에 같은 값이 둘이면 React key가 안 겹쳐야 하는데 겹친다 — 풀을 「값의 목록」이
    아니라 「값의 집합」으로 다루는 같은 착각이다. 개수 기반으로 고치면서 key도
    자리(index) 기준으로 바꾼다.
  artifacts:
    - path: "frontend/src/panes/CreationPane.tsx:212-214"
      issue: "쓴 값을 Set으로 기억해 개수를 안 센다 — 같은 값이 둘 이상인 풀을 표현할 수 없다"
    - path: "frontend/src/panes/CreationPane.tsx:232"
      issue: "disabled 판정이 개수가 아닌 존재 여부다. key={value}도 중복 값에서 안 겹친다"
  missing:
    - "남은 개수로 잠근다 — 풀의 값별 개수와 현재 배정된 개수를 비교한다"
    - "선택지 key를 자리 기준으로 바꾼다"
    - "값이 중복된 풀(예: 2,1,1,0,0,-1)을 여섯 칸에 전부 배정해 확정까지 가는 회귀 시험"

- truth: "지금 누구 차례인지, 왜 그 사람인지 화면만 보고 알 수 있다"
  gap_id: G-12.3-7
  status: resolved
  resolved_by: |
    12.3-14 Task 2. `creationView.ts::creationTurn`(순수 함수, 시험 다섯
    — 지목 없음·내 차례·완성된 남의 이름·이름 항목 안 채운 남·이름 항목
    채운 완성 전의 남)이 「아무 차례도 아님 / 내 차례 / 남의 차례(이름
    또는 이름 없음)」을 판정하고, `labels.ts::creationTurnLabel`이 이름과
    「순서는 진행자가 정해요」(D-06)를 한 문장으로 조립한다.
    `CreationPane.tsx`의 대화판 첫 줄이 그 결과를 그린다 — 지목 전에는 그
    줄 자체를 안 그린다(오늘은 지목 전에도 「다른 사람의 차례예요」가
    떴다). **자동으로 확인된 것:** 이름의 두 출처(완성된 캐릭터·아직
    완성 전인 사람의 이름 항목 값)와 없는 이름을 지어내지 않는다는 것
    (`pc-` 자취 0건, grep). **사람이 걸어야 하는 것:** 이름 항목이 마지막인
    룰북(던전월드류)에서는 「아직 이름을 안 정한 분」이 대부분의 차례에
    뜬다 — 그것으로 상황을 읽을 수 있는지는 사람이 판단한다
    (`<planner_assumptions>` ④에서 알고 남긴 한계).
  severity: minor
  test: 2
  root_cause: |
    차례 순서는 **정해진 규칙이 아니라 GM(AI)이 고른다** — D-06. `nominate_speaker`가
    아직 자기소개를 안 끝낸 사람들의 닫힌 목록에서 다음 사람을 고른다
    (`src/gptrpg/agents/creation_gm.py:167-188`). 누른 순서·들어온 순서와 무관한 것이
    설계다.

    문제는 그 사실이 화면에 전혀 안 드러난다는 것이다. 화면은
    `"다른 사람의 차례예요 — 자유롭게 끼어들어 말할 수 있어요"`
    (`frontend/src/labels.ts:272`, `CreationPane.tsx:638`)만 내고 **누구 차례인지
    이름을 안 말한다.** 그래서 사람은 순서 규칙이 있는데 자기가 못 읽는 것인지,
    원래 없는 것인지 구분할 수 없다.
  artifacts:
    - path: "frontend/src/labels.ts:272 · frontend/src/panes/CreationPane.tsx:638"
      issue: "차례인 사람의 이름을 안 보여준다 — 순서가 GM의 선택이라는 사실도 안 드러난다"
  missing:
    - "누구 차례인지 이름으로 보여준다"

- truth: "항목을 다 채운 사람이 캐릭터 만들기를 끝낼 수 있고, 끝내면 다음 사람에게 차례가 넘어간다"
  gap_id: G-12.3-8
  status: failed
  reason: |
    User reported: "모든 내용을 채우고 진행자에게 물어보기 버튼이 나오는데 누르면
    진행자가 잠시 말을 잃었지만 계속합니다 라고 안내가 뜸. 하지만 다른 플레이어들에게
    순서가 가지 않아 다른 사람들은 캐릭터 생성을 할수가 없음"
  severity: blocker
  test: 2
  root_cause_primary: |
    **사람의 결정적 관찰 (2026-08-23):** "다른 사람에게는 보이지만 자기에게는
    GM의 추가 질문이 보이지 않고 있다." — 질문이 안 만들어진 것도, 안 전달된
    것도 아니다. **자기 차례인 사람 화면에서만 접힌다.**

    만들기 화면의 대화판은 자동으로 바닥까지 내려가지 않는다.
    실제 놀이 화면의 대화판 둘은 그렇게 한다:
      frontend/src/panes/ChatPane.tsx:93   node.scrollTop = node.scrollHeight
      frontend/src/panes/StoryPane.tsx:79  node.scrollTop = node.scrollHeight
    (둘 다 「사람이 위로 올려 읽는 중이면 안 끌어내린다」는 near-bottom
    가드까지 갖췄다.)
    `frontend/src/panes/CreationPane.tsx`에는 **그 배선이 없다** — 같은
    `.chat` 스크롤 상자(`styles.css:1050` `flex:1; min-height:0;
    overflow-y:auto`)를 쓰면서 새 줄이 와도 그대로 있는다.

    **왜 자기 차례인 사람에게만 걸리나.** `stepEditingEnabled`가 참이면
    대화판 **아래에** 항목 여섯 줄 + 입력칸 + 되물음 관문이 함께 그려진다
    (CreationPane.tsx:673 이하). `.chat`은 `flex:1`이라 그만큼 눌리고,
    가장 새 줄(= GM의 되물음)이 접힌 자리로 밀린다. 남의 차례인 사람은
    그 아래 블록이 통째로 없어서 대화판이 칸을 다 쓰고, 같은 줄이 그대로
    보인다. 증상이 「남에게는 보이는데 나에게는 안 보인다」로 갈리는
    이유가 정확히 이것이다.

    (앞선 회차에 이 gap을 「화면이 질문을 버린다」로 적었던 것은 절반만
    맞다 — `response.question`을 실제로 안 쓰는 것은 사실이지만, 질문
    자체는 사건 흐름을 타고 대화판에 **그려지긴 한다**. 아래 ②가 그 절반이다.)
  root_cause_secondary: |
    ② 되물음 관문 자체의 막다른 골목 — 위 스크롤 문제를 고쳐 질문이
    보이게 되어도 남는다.

    「진행자에게 물어보기」를 누르면 서버는 GM에게 되물을 것이 있는지 묻고,
    있으면 **그 질문 문장을 `question` 필드에 담아** 돌려준다
    (`routes_creation.py::creation_follow_up` → `CreationFollowUpResponse.question`).

    화면이 그 질문을 **버린다**(`CreationPane.tsx:565-569`):
      setFollowUp(response.needs_more ? { kind: "asked_more" } : { ... })
    `asked_more`는 필드가 하나도 없는 상태다 — `question`을 담을 자리가 타입에도
    없다(`FollowUpPhase`, :433-436).

    그리고 `asked_more`가 그리는 것은 **문구 한 줄뿐이고 단추가 하나도 없다**
    (:724):
      {followUp.kind === "asked_more" ? <p>{COPY.creationGmSilent}</p> : null}

    그래서 이 상태에 들어가면:
      · GM이 실제로 무엇을 물었는지 못 본다 (질문이 버려졌다)
      · 답할 자리가 없다
      · 「이만 하기」(`finishCreation` → `POST /creation/complete`)가 없다
        — 그 단추는 `ready_to_finish` 갈래에만 있다(:725-738)
      · 「더 말하기」(`sayMore`)도 없다 — 같은 갈래에만 있다

    빠져나갈 길은 이미 채운 항목의 「고치기」 단추를 눌러 `followUp`을 `idle`로
    되돌리는 것 하나뿐인데(:697), 화면 어디에도 그렇게 하라는 표시가 없다.

    **그래서 세션 전체가 언다.** 완성(`complete`)이 안 되므로
    `shouldNominateNext`가 다음 사람을 지목할 조건에 영원히 도달하지 못한다.
    2회차 서버 기록이 그대로 보여준다 — `step` 5회 · `follow-up` 1회 뒤
    `complete` **0회** · 그 뒤 `nominate` **0회**.

    문구도 사실과 다르다. `creationGmSilent`는
    "진행자가 잠시 말을 잃었지만 계속합니다"인데, `asked_more`는 GM이
    **말을 한** 경우다. 말을 잃은 쪽(`needs_more: false`)과 말을 한 쪽에
    같은 문구가 붙어 있다(:724, :727).
  artifacts:
    - path: "frontend/src/panes/CreationPane.tsx:433-436"
      issue: "FollowUpPhase의 asked_more에 GM 질문을 담을 자리가 없다"
    - path: "frontend/src/panes/CreationPane.tsx:565-569"
      issue: "서버가 준 response.question을 버린다"
    - path: "frontend/src/panes/CreationPane.tsx:724"
      issue: "asked_more가 문구 한 줄만 그린다 — 답하기·이만 하기·더 말하기가 전부 없는 막다른 골목"
    - path: "frontend/src/labels.ts:281"
      issue: "「말을 잃었다」 문구가 GM이 실제로 말한 경우에도 붙는다"
  decisive_evidence: |
    세션 uat23b 사건 로그가 순서와 시각까지 남겼다 (2026-08-23):
      01:37:54  seq 8  A가 마지막 항목(이름) 제출
      01:38:06  seq 9  GM이 **실제로 되물었다** — "그 '심심함'을 견디지 못하게
                       만든 마지막 순간 — 그때 뭐가 있었길래 견딜 수 없었나?"
                       (12초 걸렸다 — AI가 느린 것이 아니다)
      01:39:51  seq 10 B가 끼어들기: "ㅅㅂ 내 순서는 언제 돼?"
      01:42:52  seq 11 creation_host_claimed **reason: "succession"** —
                       A가 **이탈한 것으로 처리**되어 방장이 넘어갔다
      01:42:55  seq 12 그제서야 B에게 지목

    **순서가 넘어간 것은 A가 끝냈기 때문이 아니라 A를 나간 사람으로 접었기
    때문이다.** 세션 전체에서 `complete` 0회 — 아무도 캐릭터를 완성하지
    못했다. 01:47:08(seq 19) C에게도 같은 되물음이 와서 같은 자리에 다시
    갇혔다. 즉 이 골목은 **되물음을 받은 모든 사람에게 반복해서** 걸린다.

    사람이 물은 두 가지에 대한 답:
      · 클로드가 코드를 고쳐서 풀린 것인가 → 아니다. 이번 회차에 코드는
        한 줄도 안 바뀌었다(프론트 재빌드 + 서버 재기동뿐).
      · AI가 느려서 기다려야 했던 것인가 → 아니다. GM은 12초 만에 답했다.
        기다린 5분은 이탈 회복 시간이다.
  missing:
    - "GM이 되물은 질문을 화면에 보여준다"
    - "되물음을 받은 상태에서도 답하거나 「이만 하기」로 완성까지 갈 수 있는 길을 남긴다"
    - "GM이 말을 한 경우와 말을 잃은 경우에 다른 문구를 쓴다"
    - "항목을 다 채운 사람이 완성까지 가고 다음 사람이 지목되는 것을 끝까지 걷는 회귀 시험"
  debug_session: ""

- truth: "진행자를 부르는 동안 화면이 「지금 무언가 진행 중」임을 보여준다"
  gap_id: G-12.3-9
  status: failed
  reason: |
    User reported: "로딩 에니메이션이 있으면 좋을 것 같음. 사람들이 모르고 계속
    새로 고침 할 수도 있을 것 같아"
  severity: minor
  test: 2
  artifacts: []
  missing:
    - "GM 호출이 도는 동안 기다리는 표시를 낸다 — 사람이 새로고침으로 끊지 않도록"

- truth: "GM이 사람에게 말할 때 내부 식별자가 아니라 사람이 읽을 이름을 쓴다 (D-15)"
  gap_id: G-12.3-10
  status: failed
  reason: |
    사건 로그에서 발견(사람 보고가 아니라 기계 확인). GM의 지목 문장이
    내부 식별자를 그대로 읽어 준다:
      seq 3  "그럼 첫 번째로, pc-7cd76ab6 님부터 자기소개를 부탁드릴게요."
      seq 12 "다음은 pc-ca9b917c 님, 이야기를 들려주시겠어요?"
      seq 13 "다음은 pc-3d55d245 님, 이야기를 들려주시겠어요?"
  severity: minor
  test: 2
  root_cause: |
    12.3-14 Task 2가 닫은 것은 **화면의 차례 표시**다 — `creationTurn` ·
    `creationTurnLabel`이 이름을 못 찾으면 「아직 이름을 안 정한 분」으로
    적는다. 그 규율이 **GM에게 넘기는 프롬프트/GM이 뱉는 문장**에는
    적용되지 않았다. `creation_gm.py::nominate_speaker`가 후보를 내부
    식별자로 넘기고, GM이 그 문자열을 그대로 사람에게 읽어 준다.
    G-12.3-7의 `missing`이 "누구 차례인지 이름으로 보여준다"였는데
    화면 절반만 덮였다.
  artifacts:
    - path: "src/gptrpg/agents/creation_gm.py (nominate_speaker)"
      issue: "후보를 내부 식별자로 GM에게 넘겨 GM 문장에 pc-xxxxxxxx가 그대로 실린다"
  missing:
    - "GM에게 넘기는 후보를 사람이 읽을 이름(없으면 「아직 이름을 안 정한 분」류)으로 바꾼다 — 화면과 같은 규율"

- truth: "정한 인원이 다 끝내고 그 전원이 동의해야 판이 시작된다 — 아직 아무것도 안 누른 사람이 배제되지 않는다"
  gap_id: G-12.3-11
  status: resolved
  resolved_by: |
    커밋 eff22be. 액터에 `_party_size_shortfall()`을 두고 자동 진행
    경로(`_all_created_characters_consented`)와 직접 호출 경로
    (`_prepare_lock_roster`) 양쪽에서 본다 — 라우터를 우회해도 막힌다.
    화면의 `consentGate`도 같은 판정을 비춰 「이대로 시작」을 애초에 안
    그린다(D-15). 회귀 시험:
    `test_one_finisher_cannot_lock_the_roster_when_the_party_was_fixed_at_three`
    (인원 3에 한 사람만 완성·동의 → 안 잠기고, 직접 잠금은 409이며 그
    문구에 파이썬 내부 표현이 없다) + `consentGate` 시험 2건.
    파이썬 1385건·프론트 100건 통과.
  reason: |
    User reported: "이걸로 끝을 하게 되면 이대로 시작이 나오는데, 이걸 누르면
    다른 사람들이 캐릭터를 완선했든 안 했든 그냥 게임이 시작되어 버린다."
  severity: blocker
  test: 2
  root_cause: |
    잠금을 막는 검사가 **전부** `_unfinished_creation_candidates()`를 본다 —
    「항목을 하나라도 낸 사람」만 담는 목록이다. 그 함수의 doc이 스스로
    적어 놓았다: "아직 항목을 하나도 제출하지 않은 사람은 이 목록에 못
    들어간다 — 플랫폼이 아는 유일한 참가자 식별 통로가
    `CompleteCreationStep`의 `character_id`이기 때문이다."

    그래서 들어와서 아직 아무것도 안 누른 참가자는 어느 검사에도 안 걸린다.
    먼저 끝낸 한 사람이 동의를 누르면 「완성된 전원 동의」가 성립해 명단이
    잠기고, D-08은 잠금을 되돌리는 사건을 두지 않으므로 **나머지는 영구히
    배제된다.**

    방장이 정한 `party_size_fixed`는 이 세션이 몇 명짜리인지 아는 유일한
    닫힌 숫자인데, 명단 잠금 판단에서 **한 번도 안 보고 있었다**.
  decision: |
    사장님 결정(2026-08-23): 「모두가 게임 시작에 동의를 눌러야 시작」.

    이 결정은 D-08의 각주(12.1-CONTEXT.md:160-163) 「명단은 잠그고 출석은
    안 따진다」와 부딪치는 것처럼 보이지만, 두 자리는 다른 층이다 — 그
    각주는 **이미 만들어진 명단**으로 노는 날의 출석 얘기고, 여기는
    **명단을 만드는 중**이다. 기존 시험
    `test_the_whole_creation_flow_passes_for_two_people_in_order`가 그
    각주를 근거로 「셋으로 정하고 둘만 참여해도 잠긴다」를 일부러 못 박고
    있었고, 그 전제가 이 결정으로 바뀌었다(시험 주석에 남겼다).
  known_limit: |
    정원 중 한 명이 끝내 안 들어오면 판을 시작할 수 없다. 지금은
    빠져나갈 길이 없다 — 인원은 한 번만 정할 수 있기 때문이다.
    사람이 겪어 보고 필요하다고 판단하면 그때 정한다.
  artifacts:
    - path: "src/gptrpg/session_actor/actor.py"
      issue: "명단 잠금이 party_size_fixed를 안 봤다"
    - path: "frontend/src/session/creationView.ts (consentGate)"
      issue: "서버가 거절할 「이대로 시작」을 그렸다"

- truth: "「더 말하기」를 누르면 실제로 더 말할 수 있다"
  gap_id: G-12.3-12
  status: resolved
  resolved_by: |
    커밋 eff22be. 단추를 지우고 「아래 입력칸에 답을 적을 수 있어요」
    한 줄로 바꿨다.
  reason: |
    User reported: "더 말하기 버튼을 누르면 아무것도 일어나지 않고 다시
    진행자에게 물어보기 버튼만 뜬다"
  severity: minor
  test: 2
  root_cause: |
    `sayMore()`가 `followUp`을 `idle`로 되돌리기만 했다 — 화면에는
    「진행자에게 물어보기」가 다시 뜰 뿐이라 아무 일도 안 한 것처럼 보인다.

    답을 적는 칸은 **판 맨 아래에 항상 있다**(`InterjectBox`, D-08 「남의
    차례에도 말은 항상 열려 있다」 — 자기 차례에도 그린다). 되물음 관문만
    보고 있으면 그것이 안 보이는 것이 문제였지, 말할 자리가 없던 것이
    아니다.

    그리고 다시 물어도 같은 답이 온다 — 서버의
    `_gm_dedupe_key("follow_up", state, character_id)`가 그 사람의 마지막
    항목 순번으로 접기 때문에, 항목을 고치지 않는 한 새 질문이 안 나온다.
    항목을 고치는 「고치기」는 이미 `followUp`을 `idle`로 되돌린다.
    그래서 이 단추는 실제로 할 일이 없었다.
  artifacts:
    - path: "frontend/src/panes/CreationPane.tsx (sayMore)"
      issue: "상태만 되돌리는 무동작 단추"

