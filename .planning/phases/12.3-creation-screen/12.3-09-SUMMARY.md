---
phase: 12.3-creation-screen
plan: 09
subsystem: creation-screen
tags: [python, fastapi, creation-flow, dedupe-key, regression-test]

# Dependency graph
requires:
  - phase: 12.3-08
    provides: "흘려보낸 차례 회복 경로 자체(_forfeited_at/forfeited_nominee/_nomination_watermark) — 이 계획이 고친 대상"
provides:
  - "흘려보낸 것으로 판정된 참가자가 새 지목을 받으면 회복 판정을 처음부터 다시 받는다(더 이상 영구 배제가 아니다)"
  - "이미 항목 값을 낸 사람이 흘려보내져도 새 지목 사건이 실제로 생긴다(중복 방지 키에 흘려보냄 표시가 붙는다)"
  - "회복 뒤 명단 잠금(wrap-up) 영구 409가 풀린다"
affects: [12.3-VERIFICATION, CHAR-06, 4차 검증 CR-01]

# Actuals (#2632)
actuals:
  tokens: 4722
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "판정 기록에 「무엇에 대한 판정인가」를 짝으로 함께 적어 조기 반환의 적용 범위를 좁힌다(사람 단위 → 그 사람의 그 차례 단위)"
    - "회수 가능한 자격 표시를 중복 방지 키에 이어 붙여, 판정 변화가 후보 목록 불변과 무관하게 새 GM 호출 사건을 강제한다"

key-files:
  created: []
  modified:
    - src/gptrpg/web/creation_state.py
    - src/gptrpg/web/routes_creation.py
    - tests/test_creation_screen_state.py

key-decisions:
  - "_forfeited_at 값을 float에서 (float, int) 짝으로 바꿔 판정을 지목 순번에 묶었다 — 표를 지우는 대신 적용 범위를 좁히는 쪽을 골랐다(지우는 쪽은 '언제 지우나'라는 새 판단과 배경 정리 장치의 유혹을 연다, D-12 경계)"
  - "12.3-08이 '안 건드린다'고 선언했던 routes_creation.py를 한 줄 열었다 — 판정 기록만 고치면 이미 항목 값을 낸 사람(후보 앞줄)에게는 새 지목 자체가 구조적으로 안 생겨 회복 판정이 돌 기회조차 없기 때문이다(4차 검증 missing ③)"
  - "forfeited_nomination_mark는 판정을 새로 계산하지 않는다 — latest_nomination/forfeited_nominee를 그대로 불러 D-04(판단은 한 자리)를 지킨다"

patterns-established: []

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "흘려보낸 것으로 판정된 참가자가 새 지목(다른 순번)을 받으면 그 새 지목에 대해 회복 판정을 처음부터 다시 받는다 — 4차 검증 truth #20"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_ruling_holds_for_the_same_nomination_but_a_new_one_starts_the_clock_over"
        status: pass
    human_judgment: false
  - id: D2
    description: "이미 항목 값을 낸 사람이 흘려보내져도(후보 목록이 안 바뀌어도) 새 지목 사건이 실제로 생기고, 그 사람이 결국 캐릭터를 끝내 파티 전원의 명단 잠금 영구 409가 풀린다 — 4차 검증 missing ③"
    requirement: CHAR-06
    verification:
      - kind: integration
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_participant_who_already_submitted_a_value_gets_another_turn_and_unblocks_the_roster_lock"
        status: pass
    human_judgment: false
  - id: D3
    description: "흘려보냄 표시(forfeited_nomination_mark)가 결정론적이라 같은 흘려보냄 판정이 서 있는 동안 AI 호출이 반복되지 않는다(D-12 경계 유지)"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched"
        status: pass
    human_judgment: false
  - id: D4
    description: "4차 검증이 통과로 확인한 세 경계(유령이 진짜 참가자를 영구히 안 밀어냄 #15, 값 내는 사람은 차례를 안 뺏김 #16, 재시작 직후 즉시 안 풀림 #17)가 이 계획으로 안 깨졌다"
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_ghost_presence_signals_do_not_permanently_push_out_real_participants"
        status: pass
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_nominee_that_keeps_submitting_values_never_loses_the_turn"
        status: pass
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately"
        status: pass
    human_judgment: false
  - id: D5
    description: "동의 관문의 교차 전파(화면 쪽 절반) — 서로 다른 브라우저 둘로 같은 세션에 들어가 한쪽 동의 표시가 다른 쪽 재오픈에 되돌아가는지 사람이 눈으로 확인"
    verification: []
    human_judgment: true
    rationale: "이 계획은 화면(frontend/)을 한 줄도 안 고쳤다 — 4차 검증 behavior_unverified_items에서 그대로 이어받은 항목이고, QA 도구(단일 크로미움 프로필)의 구조적 제약으로 자동화할 수 없다. 서버 쪽 절반은 12.3-08의 시험이 이미 지킨다."
  - id: D6
    description: "회복 시간 5분(NOMINATION_IDLE_S)이 실제로 답답하지 않은지 사람이 체감으로 확인"
    verification: []
    human_judgment: true
    rationale: "CR-01이 이번에 닫혀 이 확인이 처음으로 의미를 갖게 됐다 — 자동화 대상이 아니라 사람의 체감 판단이다."

duration: 미측정(단일 연속 세션)
completed: 2026-08-21
status: complete
---

# Phase 12.3 Plan 09: 흘려보낸 차례가 회복 아닌 영구 배제로 굳던 결함 닫기 Summary

**`_forfeited_at`에 「어느 지목에 대한 판정인가」(지목 순번)를 함께 적고, nominate의 중복 방지 키에 흘려보냄 표시를 이어 붙여 — 한 번 놓친 사람이 새 지목으로 회복해 파티 명단 잠금까지 풀리게 한다.**

## Performance

- **Duration:** 미측정(단일 연속 세션)
- **Completed:** 2026-08-21
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `_forfeited_at`를 `(session_id, character_id) -> float`에서 `-> (float, int)`로 바꿔, `forfeited_nominee`의 조기 반환이 지금 지목의 순번(`nominated_seq`)과 일치할 때만 걸리게 했다 — 새 지목(다른 순번)은 옛 판정을 안 물려받고 회복 시계를 처음부터 다시 잰다(4차 검증 CR-01/truth #20)
- 새 함수 `forfeited_nomination_mark`를 만들어 `nominate_creation_speaker`의 중복 방지 키에 한 줄로 이어 붙였다 — 이미 항목 값을 낸 사람은 후보 앞줄에 있어 흘려보내져도 후보 목록 자체가 안 바뀌는데, 이 표시가 없으면 서버가 옛 지목을 그대로 되돌려 줘 새 지목 사건이 영원히 안 생기던 결함(4차 검증 `missing` ③)을 닫았다
- 끝에서 끝까지 시험(`test_a_forfeited_participant_who_already_submitted_a_value_gets_another_turn_and_unblocks_the_roster_lock`) 하나로 판정 함수부터 폴링·명단 잠금(409 → 200)까지 한 줄기를 회귀 그물에 묶었다
- 4차 검증 §CR-01 독립 재현을 정식 회귀 시험(`test_a_forfeited_ruling_holds_for_the_same_nomination_but_a_new_one_starts_the_clock_over`)으로 옮기고, `forfeited_nomination_mark`의 세 경계(정상 지목/지목 없음/흘려보냄 뒤 결정론)를 별도 시험으로 못박았다
- 세 새 시험 모두 **변경 전(12.3-08) 코드에서 먼저 실패하는 것을 확인**한 뒤 커밋했다 — 아래 「RED 확인」 참조

## Task Commits

Each task was committed atomically:

1. **Task 1: 한 번 놓친 사람이 새 차례를 받아 캐릭터를 끝내고, 파티가 명단을 잠근다 — 서버 한 줄기로** - `2adcfdd` (fix)
2. **Task 2: 「같은 차례면 유지, 새 차례면 다시」를 검증자의 재현 그대로 못박는다** - `2d316d8` (test)

_Note: Task 1은 `type="tracer" tdd="true"`였다 — 프로덕션 코드(①②③)와 끝에서 끝까지 시험(④), RED 확인(⑤)을 한 커밋으로 묶었다(파일 셋이 하나의 원자적 수정을 이룬다). Task 2는 검증자 재현을 옮긴 순수 회귀 시험 추가라 별도 `test` 커밋으로 분리했다._

## Files Created/Modified
- `src/gptrpg/web/creation_state.py` - `_forfeited_at` 값 모양 변경(짝으로), `forfeited_nominee` 조기 반환에 순번 비교 추가, `present_candidates`의 정렬 읽기 자리 수정, 새 함수 `forfeited_nomination_mark` 추가
- `src/gptrpg/web/routes_creation.py` - `nominate_creation_speaker`의 중복 방지 키에 `forfeited_nomination_mark(...)`를 잇는 한 줄 추가(그 밖 무변경 — `_gm_dedupe_key` 서명·본문 그대로)
- `tests/test_creation_screen_state.py` - 새 회귀 시험 셋 추가(끝에서 끝까지 1건 + 검증자 재현 이관 1건 + 표시 경계 1건)

## Decisions Made
- `_forfeited_at`를 지우는 코드는 만들지 않았다 — 지우는 대신 판정의 적용 범위를 순번으로 좁혔다(계획의 `planner_assumptions ⑤`, D-12 경계 — 배경 정리 장치의 유혹을 피한다)
- 12.3-08이 「이 계획에서 안 건드린다」고 선언했던 `routes_creation.py`를 한 줄 열었다 — 판정 기록만 고치면 재현 시나리오의 절반(이미 값을 낸 사람 → 후보 목록 불변 → 새 지목 자체가 안 생김)이 그대로 남기 때문이다(`planner_assumptions ①`)
- `forfeited_nomination_mark`는 자체 판정을 계산하지 않고 `latest_nomination`/`forfeited_nominee`만 부른다 — 판단은 한 자리라는 규율(D-04)을 지켰다

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 1 시험에서 TestClient 쿠키 공유로 인한 신원 오판 수정**
- **Found during:** Task 1 (끝에서 끝까지 시험 작성·실행)
- **Issue:** `pc-recover-2`가 캐릭터를 완성(`/creation/complete`)하면 그 요청이 서명 쿠키를 굽는데, 시험이 단일 `TestClient`(모든 "브라우저"가 같은 쿠키 저장소를 공유)를 쓰므로 그 뒤 `pc-recover-1`이 남은 항목을 제출하면 `read_identity`가 `pc-recover-2`의 쿠키를 읽어 신원 불일치 403을 낸다. 이 저장소의 다른 다중 참가자 시험(`test_creation_consent_host.py` 등)이 이미 쓰는 관례(완성 직후 `client.cookies.clear()`)를 그대로 따랐다 — 새 방어가 아니라 기존 시험 관례의 적용이다.
- **Fix:** `pc-recover-2`가 완성한 직후에 `client.cookies.clear()` 한 줄을 추가했다.
- **Files modified:** tests/test_creation_screen_state.py
- **Verification:** 해당 시험이 통과, 전체 회귀(1381건) 그대로 초록
- **Committed in:** 2adcfdd (Task 1 커밋)

---

**Total deviations:** 1 auto-fixed (1 시험 헬퍼 버그, 프로덕션 코드는 계획 원문대로 정확히 3자리(①②③)만 수정)
**Impact on plan:** 시험 작성 중 발견한 시험 자체의 결함(TestClient 쿠키 공유)이라 프로덕션 코드나 계획의 서버 쪽 판단에는 영향 없음. 스코프 확대 없음.

## RED 확인 (변경 전 코드에서 시험이 실제로 실패했다)

Task 1/2가 더한 시험 셋을 커밋 전에 `git apply -R`으로 소스 변경분만 되돌린 뒤(시험 파일은 그대로 둔 채) 실행해 확인했다:

```
$ .venv/bin/python -m pytest tests/test_creation_screen_state.py::test_a_forfeited_participant_who_already_submitted_a_value_gets_another_turn_and_unblocks_the_roster_lock tests/test_creation_screen_state.py::test_a_forfeited_ruling_holds_for_the_same_nomination_but_a_new_one_starts_the_clock_over tests/test_creation_screen_state.py::test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched -q --tb=short

FFF
test_a_forfeited_participant_who_already_submitted_a_value_gets_another_turn_and_unblocks_the_roster_lock
  AssertionError: assert None == 'pc-recover-1'   (회복 뒤 폴링이 여전히 None — 영구 배제 재현)

test_a_forfeited_ruling_holds_for_the_same_nomination_but_a_new_one_starts_the_clock_over
  AssertionError: assert 'pc-reopen-1' is None     (새 지목 seq=2인데도 옛 판정을 그대로 물려받음 — 4차 검증이 실패로 기록한 바로 그 자리)

test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched
  AttributeError: module 'gptrpg.web.creation_state' has no attribute 'forfeited_nomination_mark'

3 failed, 2 warnings in 0.42s
```

세 시험 모두 오늘(12.3-08) 코드에서 실패한다는 것을 확인한 뒤 소스 수정을 재적용하고 커밋했다.

## GREEN 확인 (수정 뒤 전체 회귀)

```
$ .venv/bin/python -m pytest -q --tb=short
...
1381 passed, 2 warnings in 14.96s
```

4차 검증 기준 1378건 + 이 계획이 더한 3건 = 1381건, 실패 0건.

4차 검증이 통과로 확인한 세 경계를 이 계획이 안 건드렸다는 것을 개별 재확인:

```
$ .venv/bin/python -m pytest tests/test_creation_screen_state.py::test_ghost_presence_signals_do_not_permanently_push_out_real_participants tests/test_creation_screen_state.py::test_a_nominee_that_keeps_submitting_values_never_loses_the_turn tests/test_creation_screen_state.py::test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately -q
...
3 passed, 2 warnings in 0.39s
```
- `test_ghost_presence_signals_do_not_permanently_push_out_real_participants`(truth #15) — 통과
- `test_a_nominee_that_keeps_submitting_values_never_loses_the_turn`(truth #16) — 통과
- `test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately`(truth #17) — 통과

## 스코프 경계 확인

- `git diff --name-only e29e9f8..HEAD -- frontend` → 빈 출력(화면을 한 줄도 안 고쳤다)
- `git diff --name-only e29e9f8..HEAD -- src/gptrpg/event_log src/gptrpg/rules_core src/gptrpg/session_actor src/gptrpg/web/routes_events.py` → 빈 출력
- `git diff -- src/gptrpg/web/routes_creation.py` → 추가만 있고 삭제 줄이 없다(5줄 추가, 0줄 삭제) — 계획이 예고한 「한 줄과 그 주석」 그대로
- `grep -n 'def _gm_dedupe_key' src/gptrpg/web/routes_creation.py` → 서명 위치·모양 변경 전과 동일
- TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커 0건(바꾼 파일 셋 전체)

## Issues Encountered
None — 위 「Deviations」의 시험 헬퍼 수정 외에는 계획 원문대로 진행됐다.

## User Setup Required
None - 외부 서비스 설정 불필요. 새 의존성·새 HTTP 표면 없음.

## Next Phase Readiness

- CR-01(4차 검증이 발견한 「회복 장치 자체가 영구 배제를 새로 연 결함」)이 시험으로 못박힌 채 닫혔다.
- CHAR-06("사람이 브라우저만으로 처음부터 끝까지")이 한 번 자리를 비웠던 참가자에게도 서버 쪽에서 성립한다 — 5분 자리 비움 한 번이 그 사람과 파티 전원의 진행을 더 이상 영구히 끝내지 않는다.
- 남은 것(이 계획 범위 밖, 다음 검증까지 그대로 이어감):
  - 동의 관문 교차 전파의 화면 쪽 절반(사람 확인, QA 도구 구조적 제약으로 자동화 불가)
  - 회복 시간 5분의 실제 체감(이제 CR-01이 닫혀 이 확인이 처음으로 의미를 갖는다)
  - `12.3-05-SUMMARY.md`의 14개 체크리스트(범위 밖으로 유지)
- 다음 단계: `/gsd-verify-work`로 Phase 12.3 전체(9개 계획)를 5차 검증.

## Self-Check: PASSED

- FOUND: src/gptrpg/web/creation_state.py
- FOUND: src/gptrpg/web/routes_creation.py
- FOUND: tests/test_creation_screen_state.py
- FOUND: .planning/phases/12.3-creation-screen/12.3-09-SUMMARY.md
- FOUND commit: 2adcfdd (Task 1)
- FOUND commit: 2d316d8 (Task 2)
- FOUND commit: aa31ad4 (this SUMMARY)

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-21*
