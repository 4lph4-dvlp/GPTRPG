---
phase: 12.3-creation-screen
plan: 04
type: execute
wave: 3
depends_on: ["12.3-01", "12.3-02"]
files_modified:
  - frontend/src/session/browserIdentity.ts
  - frontend/src/session/browserIdentity.test.ts
  - frontend/src/session/creationView.ts
  - frontend/src/session/creationView.test.ts
  - frontend/src/panes/CreationPane.tsx
  - frontend/src/screens/CreationScreen.tsx
  - frontend/src/components/DiceModal.tsx
  - frontend/src/config.ts
  - frontend/src/labels.ts
autonomous: true
requirements: [CHAR-06]

estimate:
  tokens: 92000
  raw_tokens: 61000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "만들기가 대화창 안에서 일어난다 — GM이 말하고 사람들이 답하는 줄기가 하나 있고, 내 차례가 오면 그 자리에 그 항목에 맞는 조작이 뜬다 (D-06)"
    - "남의 차례에도 말은 항상 열려 있고, 항목을 확정하는 조작만 자기 차례에 뜬다 (D-08)"
    - "확정한 항목이 목록으로 보이고, 내 차례인 동안에는 아무 항목이나 「고치기」로 다시 열 수 있다 (D-09)"
    - "주사위로 채우는 항목이 판정에서 쓰는 것과 같은 주사위 연출로 보인다 — 새 무작위 경로도 새 연출도 만들지 않는다 (D-07)"
    - "AI가 물러난 실패 · 운영자 설정 실패 · 연결 끊김이 서로 다른 문장으로 나온다 (D-13)"
    - "GM이 되묻기를 포기해도 사람이 「더 말하기 / 이걸로 끝」을 고른다 (D-14)"
    - "사람에게 이름을 두 번 묻지 않는다 — 표시 이름은 룰북의 `provides_display_name` 항목이 정한 값 하나뿐이다"
    - statement: "브라우저에서 자기 차례에 자유 서술·선택·배분·주사위 항목을 실제로 채워 보고, 남의 차례에 끼어들어 말할 수 있고, 확정한 항목을 「고치기」로 다시 열 수 있다"
      verification: backstop
  artifacts:
    - frontend/src/session/browserIdentity.ts
    - frontend/src/panes/CreationPane.tsx
    - frontend/src/session/creationView.test.ts
  key_links:
    - "`GameStateView.creation_current_speaker_id` → 「내 차례인가」 → 항목 확정 조작이 뜬다 (화면이 다시 계산하지 않는다)"
    - "`GameStateView.creation_step_values` → 확정한 항목 목록 → 「고치기」 → 같은 `step_id`로 `POST /creation/step` 재제출"
    - "`creation_step_completed.rolls` → `usePolling.onLiveEvents` → 주사위 큐 → `DiceModal`"
    - "`localStorage` → `browserIdentity` → 모든 만들기 요청의 `browser_id`/`character_id`"
---

<objective>
**만들기를 대화로 만든다.**

12.3-01이 GM의 말을 화면에 그렸고 12.3-02가 「지금 무슨 상태인가」와 「무슨 항목이 있는가」를 내려줬다. 이 계획은 그 사이를 사람이 조작할 수 있게 채운다 — 자기 차례가 오면 그 항목에 맞는 입력이 대화 줄기 그 자리에 뜨고, 끝나면 다시 대화로 돌아온다.

12.1 D-03이 정한 「캐릭터 만들기는 자기소개처럼 되어야 한다」가 이 화면의 형태를 정한다. **항목마다 화면 한 장씩 넘기는 마법사 형태를 만들지 않는다** — 그것은 12.1이 명시적으로 물린 「폼 채우기」로 되돌아가는 것이다.

**생김새를 설계하지 않는다.** 폭·간격·색·말풍선 모양·애니메이션·반응형은 Phase 16이다. 이 계획이 정하는 것은 **무엇이 화면에 있고 무엇이 어디로 흐르는가**뿐이고, 기존 CSS 클래스만 쓴다.

Purpose: 사람이 실제로 값을 채울 수 있게 한다. 이것 없이는 브라우저로 캐릭터를 만들 수 없다.
Output: 브라우저 신원 모듈, 만들기 대화판, 주사위 연출 재사용, 그리고 판단을 담은 순수 함수와 그 시험.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-creation-screen/12.3-CONTEXT.md
@.planning/phases/12.3-creation-screen/12.3-PATTERNS.md
@.planning/phases/12.3-creation-screen/12.3-01-SUMMARY.md
@.planning/phases/12.3-creation-screen/12.3-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 브라우저 신원과 화면 판단을 순수 함수로 고정한다</name>
  <files>frontend/src/session/browserIdentity.ts, frontend/src/session/browserIdentity.test.ts, frontend/src/session/creationView.ts, frontend/src/session/creationView.test.ts</files>
  <read_first>
    - `frontend/src/session/creationView.ts` — 12.3-01이 만든 `gmLinesFrom`/`creationErrorMessage`
    - `frontend/src/session/groupTurns.test.ts:1-30` · `frontend/src/session/checkSummary.test.ts` — 이 저장소의 순수 함수 시험 모양(`vitest`, RTL 없음)
    - `frontend/src/api/types.ts` — 12.3-02가 더한 `GameStateView` 만들기 칸·`CreationStepView`·`CreationStepValueView`
    - `src/gptrpg/web/cookie_auth.py`의 `new_browser_id` — 서버가 브라우저 식별자를 어떻게 짓는지(같은 규격을 흉내 낼 필요는 없다, 길이 제한만 맞춘다)
    - `src/gptrpg/web/routes_actions.py`의 `MAX_ID_LEN` — 서버가 받는 식별자 길이 상한
    - `src/gptrpg/rulebooks/dungeonworld_like.py:178-186` — `provides_display_name`이 붙은 항목(표시 이름의 유일한 출처)
    - `.planning/phases/12.3-creation-screen/12.3-CONTEXT.md` §Claude's Discretion 「쿠키가 구워지기 전의 신원」
  </read_first>
  <behavior>
    **`browserIdentity.test.ts`** — `localStorage`를 대역으로 갈아 끼우고 확인한다:
    - `getBrowserId(sessionId)`를 두 번 부르면 같은 값이다
    - 다른 `sessionId`로 부르면 다른 값이다 — 한 브라우저가 두 세션에서 같은 식별자를 쓰지 않는다
    - 돌려주는 값의 길이가 서버의 `MAX_ID_LEN` 이하다
    - `getCreationCharacterId(sessionId)`도 같은 세션에서 안정적이고, `getBrowserId`와 다른 값이다
    - `localStorage`가 던지는 환경(사생활 보호 모드 대역)에서도 예외를 밖으로 내지 않고 그 탭 안에서 안정적인 값을 돌려준다

    **`creationView.test.ts`에 더할 것** — 전부 서버 값을 **읽기만** 한다:
    - `isMyTurn(state, myCharacterId)`는 `state.creation_current_speaker_id === myCharacterId`일 때만 참이다. `creation_current_speaker_id`가 `null`이면 거짓이다 — 「아직 지목이 없다」를 「내 차례다」로 바꿔 읽지 않는다
    - `myStepValues(state, myCharacterId)`는 `state.creation_step_values`에서 내 것만 골라 `step_id`로 찾을 수 있는 지도를 만든다. **접기를 다시 하지 않는다** — 같은 `step_id`가 두 번 오면 그것은 서버 버그이므로 시험이 그런 입력을 넣지 않는다
    - `stepRows(steps, state, myCharacterId)`는 `CreationStepView` 선언 순서를 그대로 유지하고, 각 줄에 `filled: boolean`·`reopened: boolean`·`value: CreationStepValueView | null`을 붙인다. 선언에 없는 값이 상태에 있어도 줄을 만들지 않는다(룰북 선언이 순서의 유일한 출처다)
    - `canEdit(row, isMyTurn)`은 내 차례일 때 **아무 줄이나** 참이다 — 「마지막 것만 한 칸 뒤로」가 아니다(D-09)
    - `nextUnfilledStep(rows)`는 `required`이면서 안 채워진 첫 줄을 돌려주고, 전부 채워졌으면 `null`이다
  </behavior>
  <action>
    ① `frontend/src/session/browserIdentity.ts`를 새로 만든다(D-CONTEXT의 재량 항목 「쿠키가 구워지기 전의 신원」에 대한 답):
       - `getBrowserId(sessionId: string): string` — `localStorage`의 `gptrpg.browser.{sessionId}` 키에서 읽고, 없으면 `crypto.randomUUID()`로 만들어 저장한다. 세션마다 따로 두는 이유(한 브라우저가 두 세션에서 같은 식별자를 쓰면 한쪽 세션의 값으로 다른 쪽을 추측할 수 있다)를 주석에 적는다.
       - `getCreationCharacterId(sessionId: string): string` — `gptrpg.character.{sessionId}` 키. 없으면 `pc-` + `getBrowserId`의 앞 여덟 글자로 만들어 저장한다. **사람에게 보이지 않는 내부 식별자다** — 표시 이름은 룰북의 `provides_display_name` 항목이 정하므로 **사람에게 이름을 두 번 묻지 않는다.** 이 문장을 모듈 도크스트링에 적는다.
       - `localStorage` 접근을 `try`로 감싸고 실패하면 모듈 수준 메모리 지도로 떨어진다 — 사생활 보호 모드에서 화면이 아예 안 열리는 것이 더 나쁘다. 그 대신 **탭을 닫으면 자기 캐릭터로 못 돌아온다**는 한계가 생기므로, 그 사실을 Phase 8 D-01의 한계와 같은 자리(`COPY.characterIdentityLimit`)가 이미 말하고 있음을 주석으로 이어 둔다.
       - 값 길이를 서버 `MAX_ID_LEN` 이하로 유지한다. 상한 숫자를 여기 하드코딩하지 말고 `frontend/src/config.ts`에 `MAX_ID_LEN` 상수를 더해 `MAX_RAW_TEXT_LEN` 옆에 둔다.

    ② `frontend/src/session/creationView.ts`에 위 `<behavior>`의 다섯 함수를 더한다. **전부 서버가 내려준 값을 읽고 고르기만 한다** — 「누가 아직 안 끝났나」·「최소선이 채워졌나」·「지금 누구 차례인가」를 화면이 다시 계산하는 코드를 한 줄도 쓰지 않는다(D-04). 파일 도크스트링에 그 규율과 근거(Phase 12.2가 방금 고친 사고)를 적는다.

    ③ 두 시험 파일을 `<behavior>` 그대로 쓴다.
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/session/browserIdentity.test.ts` 와 `frontend/src/session/creationView.test.ts` 가 전부 통과한다
    - `grep -c 'export function getBrowserId\|export function getCreationCharacterId' frontend/src/session/browserIdentity.ts` 가 `2`다
    - `grep -c 'export function isMyTurn\|export function myStepValues\|export function stepRows\|export function canEdit\|export function nextUnfilledStep' frontend/src/session/creationView.ts` 가 `5`다
    - `creationView.ts` 안에 `creation_step_values`를 `seq`로 정렬해 덮어쓰는 코드가 없다 — 접기는 서버가 한다
    - `npm --prefix frontend run build` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>브라우저가 자기 신원을 안정적으로 들고 있고, 화면이 무엇을 그릴지 정하는 판단이 전부 시험으로 고정된 순수 함수 안에 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 만들기 대화판을 만든다 — 항목 조작·끼어들기·고치기 (D-06/D-08/D-09/D-14)</name>
  <files>frontend/src/panes/CreationPane.tsx, frontend/src/screens/CreationScreen.tsx, frontend/src/labels.ts</files>
  <read_first>
    - `frontend/src/panes/ChatPane.tsx:1-130` — 파일 서두 도크스트링(「꼬리표는 사건이 있을 때만 붙인다」·「확인 버튼만이 유일한 통로」)과 `busy`/`status`/`proposal` 상태 관리, `messageFor(error)`의 상태 코드별 분기, `MAX_RAW_TEXT_LEN` 사용
    - `frontend/src/screens/CreationScreen.tsx` — 12.3-01이 만든 뼈대
    - `frontend/src/api/client.ts` — 12.3-02가 더한 `fetchCreationSteps`·`completeCreationStep`·`completeCreation`·`recordInterjection`·`nominateCreationSpeaker`·`creationFollowUp`·`wrapUpCreation`과 공용 `postJsonWithDetail`
    - `frontend/src/api/types.ts` — `CreationStepView`의 일곱 `kind`와 각 `kind`가 쓰는 칸
    - `src/gptrpg/session_actor/actor.py:1359-1548` — `_prepare_complete_creation_step`. **각 `kind`가 서버에서 무엇을 요구하는지의 권위**다(예: `allocate_points`의 합계가 `point_budget`과 정확히 같아야 하고, `place_fixed_values`는 선언된 값 묶음과 다중집합으로 같아야 하며, `roll_to_fill`은 브라우저가 보낸 값을 **무시한다**)
    - `frontend/src/labels.ts:172-248` — `COPY` 객체
    - `frontend/src/screens/SessionScreen.tsx:230-250` — 끊김 띠(D-13 ③이 그대로 쓴다)
  </read_first>
  <action>
    ① `frontend/src/panes/CreationPane.tsx`를 새로 만든다. `ChatPane.tsx`의 상태 관리 패턴(`busy`·`status`·오류 문구 고르기)을 복제하되 **`ChatPane.tsx` 자체를 고치지 않는다** — 그 파일은 판정 턴을 묶는 로직에 붙어 있어서 만들기 대화를 억지로 끼우면 두 흐름이 서로를 망가뜨린다. Phase 16이 두 판의 생김새를 합칠지 정한다.

    판이 그리는 것(위에서 아래로 하나의 줄기, D-06):
    - **대화 줄기**: `gmLinesFrom(events)`의 GM 말과 `creation_interjection` 사건의 끼어든 말을 `seq` 순서로 섞어 한 줄기로 그린다. 각 줄에 누가 말했는지를 붙인다(GM은 GM으로, 사람은 `creation_characters`의 `display_name`으로, 아직 완성 전이라 이름이 없으면 그 사람의 `character_id`로).
    - **내 차례가 아닐 때**(`isMyTurn`이 거짓): 대화 줄기 + **끼어들기 입력만** 보인다(D-08). 항목을 확정하는 조작은 그리지 않는다. 끼어들기는 `recordInterjection`을 부르고 `during_character_id`에 `state.creation_current_speaker_id`를 싣는다. `mentioned_character_ids`는 이 계획에서 비워 보낸다(누구를 가리켰는지 뽑아내는 것은 Phase 14의 관계 장부 몫이고, 서버는 빈 목록을 정상값으로 받는다).
    - **내 차례일 때**: 대화 줄기 + 끼어들기 입력 + **항목 조작**이 함께 보인다. 끼어들기가 사라지지 않는다 — 내 차례에도 말은 열려 있다.

    ② 항목 조작 — `stepRows(...)`가 준 줄을 `kind`별로 그린다. **다섯 `kind`만 만든다**(세 룰북이 실제로 쓰는 것):
    - `free_text` -> `textarea`. 길이 상한은 `MAX_RAW_TEXT_LEN`(`config.ts`에서 import, `ChatPane`이 이미 쓰는 그 값). `text_value`로 보낸다
    - `pick_one` -> `options` 중 하나를 고르는 버튼 묶음. `picked`에 한 개를 담아 보낸다
    - `place_fixed_values` -> `axis_names` 각각에 `fixed_values`의 숫자를 하나씩 배치하는 조작. **같은 숫자를 두 축에 넣을 수 없게 화면에서 막되, 최종 판정은 서버가 한다** — 서버가 다중집합 비교로 거절하면 그 `detail`을 그대로 보여준다(D-15)
    - `allocate_points` -> `axis_names` 각각에 숫자를 넣고 남은 예산(`point_budget` - 합계)을 보여준다. **합계가 예산과 다르면 보내는 버튼을 비활성화한다** — 서버가 「남는 점수를 조용히 버릴 수 없다」로 거절하는 것과 같은 규칙이지만, 판정의 권위는 서버다(화면은 헛걸음을 줄일 뿐이다). `per_target_max`가 있으면 그 상한도 화면에서 알린다
    - `roll_to_fill` -> **입력칸이 없다.** 「굴리기」 버튼 하나가 `completeCreationStep`을 `axis_values` 없이 부른다. 서버가 주입된 `Roller`로 직접 굴린다(D-07 서버 규율, 12.1 D-04) — **브라우저가 눈을 만들어 보내는 코드를 쓰지 않는다.** 이 사실을 버튼 옆 주석에 적는다
    - `derive` -> 조작이 없다. 「자동 계산」이라고만 표시하고, `depends_on` 항목이 전부 채워지면 그때 `completeCreationStep`을 자동으로 한 번 부른다. 서버가 값을 계산한다
    - `pick_many`·`place_fixed_values` 외의 미사용 `kind`: **`pick_many`의 입력칸은 만들지 않는다.** 세 룰북 중 아무도 안 쓴다(12.3-CONTEXT 재량 항목). 대신 `kind`가 화면이 모르는 값이면 그 줄에 「이 룰북 항목은 아직 화면에서 채울 수 없어요」를 표시하고 조작을 그리지 않는다 — 조용히 빈 칸을 보여주지 않는다. tsc가 유니온 갈래 누락을 잡도록 `switch`의 기본 갈래를 명시한다

    ③ 확정한 항목 목록과 「고치기」(D-09): 채워진 줄은 값과 함께 목록으로 보이고, **내 차례인 동안에는 아무 줄이나** 「고치기」 버튼으로 다시 열 수 있다(`canEdit`). 「고치기」는 **새 경로를 부르지 않는다** — 같은 `step_id`로 `completeCreationStep`을 다시 내는 것이 곧 되돌리기다(서버가 나중 값으로 덮는다). 이 사실을 주석에 적는다. 차례가 끝난 뒤에는 「고치기」를 그리지 않는다 — 그때는 동의 관문에서 말해야 한다(12.3-05).

    ④ 되묻기와 D-14: 필수 항목이 전부 채워지면(`nextUnfilledStep`이 `null`) 「진행자에게 물어보기」 버튼이 `creationFollowUp`을 부른다.
       - `needs_more`가 참이면 `question`을 대화 줄기에 얹고 다시 자유 서술 입력으로 돌아간다
       - `needs_more`가 거짓이면 **버튼 두 개를 보인다**: `COPY.creationSayMore`(「더 말하기」)와 `COPY.creationDoneTalking`(「이걸로 끝」). 「더 말하기」는 항목 목록으로 돌아가 아무 항목이나 다시 열 수 있게 하고(값을 정하는 것이 아니라 **말할 기회**를 여는 것이다, D-14), 「이걸로 끝」은 `completeCreation`을 부른다
       - `required_steps_filled`가 거짓이면 「이걸로 끝」을 비활성화한다 — 룰북 최소선은 서버가 검사하지만 여기서 헛걸음을 줄인다

    ⑤ 세 가지 실패를 다르게 말한다(D-13). `creationErrorMessage`(12.3-01)를 모든 호출부가 쓰게 하고, 세 갈래가 실제로 갈라지는지 확인한다:
       ① AI가 물러남 — 서버가 **200 + 폴백 값**을 주므로 오류 경로에 아예 안 온다. 대신 `needs_more=false`로 되돌아온 되묻기·후보 첫 번째로 폴백된 지목처럼 「GM이 준비를 못 했지만 계속된다」가 보이는 자리에 `COPY.creationGmSilent`(「진행자가 잠시 말을 잃었지만 계속합니다」)를 한 줄 얹는다. 폴백을 조용히 정상처럼 보여주지 않는다
       ② 503 — `COPY.creationGmUnavailable`(12.3-01이 이미 만든 문구). 「기다려도 안 된다」가 이 문장의 뜻이다
       ③ 끊김 — `feed.status === "disconnected"`면 `COPY.disconnected` 그대로. **새 문구를 만들지 않는다**

    ⑥ `CreationScreen.tsx`가 `CreationPane`을 마운트하고 `fetchCreationSteps`를 한 번 불러 내려 준다(세션 중에 안 바뀌므로 다시 부르지 않는다, D-05). 룰북 식별자는 `state.creation_rulebook_id`가 채워지면 그것을 쓰고, 아직 인원이 확정되지 않았으면 항목 목록을 아예 부르지 않는다.

    ⑦ `labels.ts`에 이 계획이 쓰는 문구를 더한다: `creationGmSilent` · `creationSayMore` · `creationDoneTalking` · `creationAskGm` · `creationEdit`("고치기") · `creationRoll`("굴리기") · `creationMyTurn` · `creationOthersTurn` · `creationInterjectPlaceholder` · `creationDerivedAuto` · `creationStepKindUnsupported` · `creationBudgetLeft`. **서버가 보낸 거절 사유를 이 파일에 옮겨 적지 않는다**(D-15).
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend test</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run build` 가 종료 코드 0이다 (tsc가 `CreationStepView.kind` 유니온의 모든 갈래를 확인한다)
    - `grep -c 'MAX_RAW_TEXT_LEN' frontend/src/panes/CreationPane.tsx` 가 `1` 이상이다 — 자유 서술 길이 상한을 재사용했다
    - `grep -c 'Math.random\|crypto.getRandomValues' frontend/src/panes/CreationPane.tsx` 가 `0`이다 — 브라우저가 주사위 눈을 만들지 않는다 (D-07)
    - `roll_to_fill` 항목의 제출 호출에 `axis_values`가 실리지 않는다 (소스에서 확인 가능)
    - `COPY.creationSayMore`·`COPY.creationDoneTalking` 두 키가 `labels.ts`에 있고 `CreationPane.tsx`가 둘 다 쓴다 (D-14)
    - `COPY.disconnected` 를 `CreationPane.tsx` 또는 `CreationScreen.tsx`가 쓴다 — 끊김 문구를 새로 만들지 않았다 (D-13 ③)
    - `CreationPane.tsx`가 `frontend/src/panes/ChatPane.tsx`를 import하지 않는다 — 두 판이 서로 얽히지 않았다
    - 끼어들기 입력이 `isMyTurn` 값과 무관하게 그려지는 것이 소스에서 확인된다 (D-08)
  </acceptance_criteria>
  <done>사람이 자기 차례에 다섯 가지 방식으로 항목을 채우고, 남의 차례에 끼어들고, 확정한 항목을 다시 열 수 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 주사위 항목에 판정과 같은 연출을 붙인다 (D-07)</name>
  <files>frontend/src/components/DiceModal.tsx, frontend/src/screens/CreationScreen.tsx, frontend/src/session/creationView.ts, frontend/src/session/creationView.test.ts, frontend/src/labels.ts</files>
  <read_first>
    - `frontend/src/components/DiceModal.tsx:1-90` — 파일 도크스트링(「착지값은 반드시 `rolls[i]`와 같다」·「클릭을 삼키지 않는다」·「과거를 재생하지 않는다」)과 `PendingRoll` interface, 굴림 타이밍 상수들
    - `frontend/src/screens/SessionScreen.tsx:28-70` · `:115-150` — `QueuedRoll`/`MAX_QUEUED_ROLLS`/`shownRef`/`onLiveEvents`로 만든 주사위 큐. **이 패턴을 그대로 복제한다**
    - `frontend/src/session/usePolling.ts` — `primedRef`가 첫 응답(과거)을 연출에서 거르는 자리
    - `src/gptrpg/session_actor/actor.py:1490-1505` — `roll_to_fill` 처리. 눈이 `creation_step_completed` 사건의 `rolls`에 남고 축별 합계가 `axis_values`에 남는다. **`check_resolved`가 아니다**
    - `src/gptrpg/rules_core/dice.py`의 `Roller` · `src/gptrpg/event_log/replay_roller.py` — 만들기 주사위가 반드시 타는 경로(서버가 이미 그렇게 한다, 화면은 확인만 한다)
  </read_first>
  <action>
    **먼저 사실 하나를 붙잡는다:** 만들기의 주사위는 `check_resolved`가 아니라 **`creation_step_completed` 사건의 `rolls` 칸**에 남는다. 그래서 `SessionScreen`의 기존 큐가 자동으로 잡지 않는다. 새 무작위 경로는 없다(서버가 이미 `Roller`를 탄다) — 필요한 것은 **연출을 이 사건에도 걸어 주는 일**뿐이다.

    ① `DiceModal.tsx`에 두 번째 입력 모양을 더한다. **기존 `PendingRoll`과 그 호출부(`SessionScreen`)를 건드리지 않는다**:
       - `export interface PendingCreationRoll { creationStep: CreationStepCompletedEvent; label: string; actorName: string }`
       - `DiceModal`의 `roll` 프롭 타입을 `PendingRoll | PendingCreationRoll`로 넓히고 `"creationStep" in roll`로 좁힌다
       - 굴리는 눈의 개수·착지값·타이밍 코드는 **한 줄도 바꾸지 않는다** — `check.rolls` 대신 `creationStep.rolls`를 읽을 뿐이다. 「착지값은 반드시 `rolls[i]`와 같다」는 규율이 만들기 주사위에도 그대로 적용된다
       - 만들기 굴림에는 등급 도장·목표값·`buildCheckSummary` 호출이 없다. 대신 `axis_values`를 「축 이름 — 값」 줄로 보여준다. 판정 요약을 억지로 만들지 않는다 — 만들기 굴림에는 성공도 실패도 없다
       - 파일 도크스트링에 이 두 번째 쓰임(D-07)과 왜 같은 컴포넌트인지(같은 굴림은 같게 보여야 한다)를 적는다

    ② `CreationScreen.tsx`에 주사위 큐를 붙인다. `SessionScreen`의 패턴을 그대로 복제한다:
       - `usePolling(sessionId, onLiveEvents)`의 두 번째 인자를 쓴다 — **첫 응답(과거)을 거르는 장치가 이미 거기 있으므로** 새로고침할 때 지난 굴림이 다시 굴러가지 않는다
       - `shownRef`(이미 보여준 `seq` 집합)와 `MAX_QUEUED_ROLLS`(같은 값 3)를 같은 이름·같은 규칙으로 쓴다. 세 건 넘게 밀리면 나머지는 모달을 건너뛴다
       - 큐에 넣는 조건: `event.event_type === "creation_step_completed"` 이고 `event.rolls`가 비어 있지 않다
       - `label`은 `fetchCreationSteps`로 받아 둔 `CreationStepView`에서 `step_id`로 찾는다. 못 찾으면 `step_id`를 그대로 쓴다

    ③ `creationView.ts`에 `creationRollsFrom(events, stepsById)` 순수 함수를 더해 위 필터·짝짓기를 담고, `creationView.test.ts`에 시험을 더한다:
       - `rolls`가 `null`이거나 빈 `creation_step_completed`는 큐에 안 들어간다(자유 서술·선택은 굴림이 아니다)
       - `rolls`가 있는 사건은 `label`과 함께 나온다
       - 다른 종류 사건이 섞여도 무시한다
       - 같은 `seq`가 두 번 와도 하나만 나온다

    ④ `labels.ts`에 `creationRollTitle`(만들기 굴림 모달 제목)을 더한다. 판정용 `diceModalTitle`을 재사용하지 않는다 — 「주사위 굴림 결과」는 판정 문맥의 말이고, 여기서는 무엇을 굴렸는지가 다르다.
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export interface PendingCreationRoll' frontend/src/components/DiceModal.tsx` 가 `1`이다
    - `grep -c 'MAX_QUEUED_ROLLS' frontend/src/screens/CreationScreen.tsx` 가 `1` 이상이다 — 같은 상한 규칙을 복제했다
    - `creationRollsFrom`이 `rolls`가 비어 있는 `creation_step_completed`를 버리는 시험이 통과한다
    - `frontend/src/screens/SessionScreen.tsx` 가 이 계획에서 변경되지 않았다 (`git diff --name-only` 에 나오지 않는다)
    - `npm --prefix frontend run build` 와 `npm --prefix frontend test` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>능력치를 굴리는 항목이 판정과 같은 주사위 연출로 보이고, 새로고침해도 지난 굴림이 다시 굴러가지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (12.3-04)

**새 파일:** `frontend/src/session/browserIdentity.ts` · `frontend/src/session/browserIdentity.test.ts` · `frontend/src/panes/CreationPane.tsx`
**새 순수 함수** (`frontend/src/session/browserIdentity.ts`): `getBrowserId` · `getCreationCharacterId`
**새 순수 함수** (`frontend/src/session/creationView.ts`): `isMyTurn` · `myStepValues` · `stepRows` · `canEdit` · `nextUnfilledStep` · `creationRollsFrom`
**새 TypeScript 타입** (`frontend/src/components/DiceModal.tsx`): `PendingCreationRoll`
**새 상수** (`frontend/src/config.ts`): `MAX_ID_LEN`
**새 React 컴포넌트:** `CreationPane`
**새 label 키** (`frontend/src/labels.ts`): `creationGmSilent` · `creationSayMore` · `creationDoneTalking` · `creationAskGm` · `creationEdit` · `creationRoll` · `creationMyTurn` · `creationOthersTurn` · `creationInterjectPlaceholder` · `creationDerivedAuto` · `creationStepKindUnsupported` · `creationBudgetLeft` · `creationRollTitle`
**새 localStorage 키:** `gptrpg.browser.{sessionId}` · `gptrpg.character.{sessionId}`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → 모든 만들기 요청의 `browser_id` | 브라우저가 스스로 짓는 값이다. 사람이 개발자 도구로 바꿀 수 있다 |
| 사람 입력 → `POST /creation/step`의 `text_value` | 자유 서술이 프롬프트로 들어간다 |
| 사람 입력 → `POST /creation/interject`의 `text` | 남의 차례에도 열려 있는 입력 |
| 화면 판단 → 서버 판단 | 화면이 막는 것과 서버가 막는 것이 다르면 화면 쪽이 틀린 것이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-01 | Spoofing | `browserIdentity`의 `browser_id` | high | mitigate | 이 값은 신뢰 근거가 아니다. 완성 시점의 위조 방지는 서버의 `_prepare_create_character`가 「그 캐릭터의 항목을 실제로 낸 브라우저인가」로 이미 막고 있고, 완성 뒤에는 서명 쿠키가 신원이 된다. 화면은 새 방어를 발명하지 않고 서버가 403/409로 거절하면 그 `detail`을 그대로 보여준다. **이 값을 신뢰하는 화면 쪽 권한 판단을 만들지 않는다** — 「내 차례인가」도 서버가 내려준 `creation_current_speaker_id`와의 비교일 뿐이다 |
| T-12.3-14 | Tampering | `roll_to_fill` 제출 | high | mitigate | 화면이 눈을 만들어 보내지 않는다. 서버가 브라우저가 보낸 값을 **무시하고** 주입된 `Roller`로 직접 굴린다(12.1 D-04). 승인 기준이 `CreationPane.tsx`에 난수 호출이 없음을 grep으로 확인한다 |
| T-12.3-15 | Denial of Service | 자유 서술·끼어들기 입력 길이 | medium | mitigate | `MAX_RAW_TEXT_LEN`을 화면에서 적용하고, 서버가 `Field(max_length=MAX_RAW_TEXT_LEN)`으로 다시 막는다. 두 층 다 이미 있는 상수를 쓴다 |
| T-12.3-16 | Information Disclosure | `localStorage`의 세션별 키 | low | mitigate | 세션마다 다른 키·다른 값이라 한 세션의 값으로 다른 세션을 추측할 수 없다. 같은 값을 재사용하지 않는다 |
| T-12.3-17 | Elevation of Privilege | 화면이 조작을 비활성화하는 것만으로 규칙을 지킨다고 착각 | high | mitigate | 화면의 비활성화는 **헛걸음 줄이기**일 뿐이고 판정의 권위는 서버다. `allocate_points` 예산 검사·`place_fixed_values` 다중집합 검사·차례 밖 제출 거절이 전부 서버에 이미 있고 이 계획이 그것을 하나도 옮기거나 없애지 않는다. `CreationPane`의 각 비활성화 자리에 「서버가 같은 규칙을 다시 본다」를 주석으로 남긴다 |

**공급망:** 새 패키지 설치 없음 — `T-{phase}-SC` 항목 없음. 주사위 연출·모달·상태 관리 전부 저장소에 이미 있는 것을 쓴다.
</threat_model>

<verification>
- `npm --prefix frontend test` 와 `npm --prefix frontend run build` 가 통과한다
- `.venv/bin/python -m pytest -q` 전체가 통과한다 (서버를 안 고쳤으므로 회귀가 없어야 한다)
- 사람 확인(12.3-05의 확인 관문에서 함께 수행): 브라우저에서 자기 차례에 다섯 방식으로 항목을 채우고, 남의 차례에 끼어들고, 확정한 항목을 고쳐 본다
</verification>

<success_criteria>
1. 만들기가 대화 줄기 하나 안에서 일어나고 마법사 형태가 아니다
2. 남의 차례에도 말이 열려 있고 항목 확정만 자기 차례에 뜬다
3. 확정한 항목이 목록으로 보이고 내 차례 동안 아무 항목이나 고칠 수 있다
4. 능력치 굴림이 판정과 같은 주사위 연출로 보이고 눈은 서버가 만든다
5. 세 가지 실패가 서로 다른 문장으로 나온다
</success_criteria>

<output>
Create `.planning/phases/12.3-creation-screen/12.3-04-SUMMARY.md` when done
</output>
