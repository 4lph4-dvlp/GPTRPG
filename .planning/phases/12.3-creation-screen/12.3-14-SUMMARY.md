---
phase: 12.3-creation-screen
plan: 14
subsystem: ui
tags: [react, typescript, vitest, tdd, creation-screen]

requires:
  - phase: 12.3-13
    provides: 인원 확정 관문 세 결함(G-12.3-2/3/4) 통합 수정 — 이 계획이 닫는 안내·차례 gap 앞에 선 마지막 blocker
  - phase: 12.3-12
    provides: 능력치 배치 개수 기반 잠금 판정 — creationView.ts 모듈의 순수 함수 규율을 처음 확립
provides:
  - "shouldAnnounce(state, announced, lastAttemptFailed) — 첫 안내를 부를 때인지의 순수 판정, shouldNominateNext와 같은 부트스트랩 판정 모양"
  - "CreationScreen.tsx의 자동 안내 effect — 인원 확정 뒤 사람이 단추를 안 눌러도 화면이 스스로 POST /creation/announce를 부른다"
  - "gate === 'done' 렌더의 단일 블록 규율 — 안내가 없는 동안 진행 중 문구 또는 실패+재시도 단추 중 하나만 그린다"
  - "CreationTurn 판별 유니온 + creationTurn(state, myCharacterId, steps) — 지금 누구 차례인지(아무 차례도 아님/내 차례/남의 차례+이름) 순수 판정"
  - "creationTurnLabel(turn) — labels.ts의 문구 조립 함수, 이름과 「순서는 진행자가 정해요」를 한 문장으로 낸다"
affects: [12.3-UAT]

actuals:
  tokens: 8734
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "부트스트랩 발동 조건(effect가 자동으로 무언가를 부를지 말지)은 반드시 시험 가능한 순수 함수로 뺀다 — shouldNominateNext(12.3-06)·remainingFixedValues(12.3-12)·partySizeOutOfRange(12.3-13)에 이어 shouldAnnounce가 같은 규율의 네 번째 실증. 이 규율이 없으면 발동 조건이 틀려도(한 번도 안 불림·너무 일찍 불림·무한 재시도) 구조 검사(ref 존재·줄 순서·문구 개수)만으로는 전부 통과한다"
    - "렌더 갈래의 순서를 뒤집어 '동시에 그려질 수 있는 상태'를 구조적으로 없애는 패턴 — 조건문을 고치는 것이 아니라 갈래 순서 자체를 바꿔 상호 배타를 강제한다(gate === 'done'에서 !announced를 먼저 검사)"
    - "이름이 두 자리(완성된 캐릭터·아직 완성 전인 사람의 provides_display_name 항목값)에 흩어져 있을 때, 순수 함수가 그 순서로 찾고 둘 다 없으면 null을 반환해 호출부가 대체 문구를 고르게 하는 패턴 — 없는 값을 지어내지 않는다"

key-files:
  created: []
  modified:
    - frontend/src/session/creationView.ts
    - frontend/src/session/creationView.test.ts
    - frontend/src/labels.ts
    - frontend/src/screens/CreationScreen.tsx
    - frontend/src/panes/CreationPane.tsx
    - .planning/phases/12.3-creation-screen/12.3-UAT.md

key-decisions:
  - "Task 1과 Task 2를 각각 하나의 feat 커밋으로 묶었다(RED와 GREEN을 같은 커밋에) — 12.3-12/13이 세운 것과 같은 판단이다: 각 Task의 <reversibility>가 '순수 함수 하나와 그 시험'을 하나의 논리적 변경으로 명시한다. 다만 두 Task 사이의 diff는 완전히 분리했다 — labels.ts/creationView.ts/creationView.test.ts처럼 두 Task가 같은 파일을 건드리는 곳에서도, Task 1 커밋에는 Task 1의 변경만, Task 2 커밋에는 Task 2의 변경만 들어가도록 파일을 임시로 되돌렸다 되살리는 방식으로 갈랐다(각 커밋 시점에 tsc+vitest로 그 상태 자체가 유효한지 확인함)"
  - "COPY.loading이 CreationScreen.tsx에 남는 자리 넷(partyRoster 잠금 전이·인원 확정 전 declaration 조회·방장 재실 신호 beacon·첫 폴링 대기) 중 어느 것도 안 지웠다 — Task 3에서 넷을 전부 감사한 결과, G-12.3-5가 문제 삼은 '진행 중이 아닌데 진행 중이라고 말하는' 자리가 아니라 전부 실제로 도는 요청·전이와 묶여 있었다(아래 Threat/Deviation 절 참조)"
  - "shouldAnnounce의 발동 조건 대조(Task 1 ③-2)를 코드 주석이 아니라 이 SUMMARY에 명시적으로 적었다 — shouldAnnounce가 읽는 네 값(party_size_fixed·party_roster!==null·announced·error!==null)이 effect 의존성 목록(+ sessionId·rulebookId·announce)에 전부 들어 있음을 확인했다"

patterns-established:
  - "화면이 '지금 무엇을 해야 하는가'를 하나로 정하는 자리가 없으면 여러 신호가 동시에 뜬다 — 렌더 갈래 순서 자체를 배타적으로 재구성하는 것이 문구를 손보는 것보다 근본적인 수리다. 다음에 비슷한 '동시에 뜨는 신호' 결함을 만나면 조건문 패치보다 먼저 갈래 순서 재구성을 검토한다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "shouldAnnounce(state, announced, lastAttemptFailed) 순수 함수와 그 시험 다섯(부른다·인원 확정 전·이미 안내 있음·직전 실패·명단 잠김) — 함수를 쓰기 전에는 실패, 쓴 뒤 통과"
    requirement: "CHAR-06"
    verification:
      - kind: unit
        ref: "frontend/src/session/creationView.test.ts#첫 안내를 부를 때인지 판정한다 (G-12.3-5)"
        status: pass
      - kind: integration
        ref: "cd frontend && npx vitest run (98 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "CreationScreen.tsx가 shouldAnnounce 하나로 스스로 POST /creation/announce를 부르고, 안내가 없는 동안 gate==='done' 렌더가 한 블록(진행 중 문구 또는 실패+재시도)만 그린다 — 발동 조건을 다시 안 쓴다(grep으로 화면-호출 지점 1건 확인)"
    requirement: "CHAR-06"
    verification:
      - kind: other
        ref: "grep -c 'export function shouldAnnounce' creationView.ts = 1; announcingRef 선언+사용 4건; git diff --name-only -- src/gptrpg = 빈 출력"
        status: pass
      - kind: unit
        ref: "cd frontend && npx tsc --noEmit && npm run build (성공)"
        status: pass
    human_judgment: true
    rationale: "인원을 확정한 직후 아무것도 안 누르고 기다렸을 때 안내가 실제로 오는지는 순수 함수 시험이 답하지 못한다 — 시험은 '조건이 옳은가'까지만 답하고, '그 조건이 참이 되는 순간을 effect가 실제로 보는가'는 의존성 목록의 문제라 브라우저에서 사람이 확인해야 한다"
  - id: D3
    description: "CreationTurn 판별 유니온 + creationTurn(state, myCharacterId, steps) 순수 함수와 그 시험 다섯(지목 없음·내 차례·완성된 남의 이름·이름 항목 안 채운 남·이름 항목 채운 완성 전의 남)"
    requirement: "CHAR-06"
    verification:
      - kind: unit
        ref: "frontend/src/session/creationView.test.ts#지금 누구 차례인지 판정한다 (G-12.3-7)"
        status: pass
      - kind: integration
        ref: "cd frontend && npx vitest run (98 passed)"
        status: pass
    human_judgment: false
  - id: D4
    description: "labels.ts의 creationTurnLabel(turn)이 이름과 '순서는 진행자가 정해요'를 조립하고, CreationPane.tsx 대화판 첫 줄이 그 결과를 그린다(지목 전에는 그 줄을 안 그린다). 옛 creationOthersTurn 키는 저장소에서 0건"
    requirement: "CHAR-06"
    verification:
      - kind: other
        ref: "grep -c 'export function creationTurnLabel' labels.ts = 1; grep -rc creationOthersTurn frontend/src/ 합 = 0; grep -c 'pc-' (3파일, 비주석) = 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "이름 항목이 마지막인 룰북(던전월드류)에서 '아직 이름을 안 정한 분'이 대부분의 차례에 뜬다 — 그것으로 상황을 읽을 수 있는지 사람이 판단한다(planner_assumptions ④에서 알고 남긴 한계)"
    verification: []
    human_judgment: true
    rationale: "서버가 완성 전 캐릭터의 이름을 내려주지 않는 것은 이 계획의 의도적 범위 판단이다 — 자동 시험은 '없는 이름을 지어내지 않는다'까지만 증명하고, 그 대체 문구가 실제로 충분한 정보인지는 사람의 UX 판단이 필요하다"
  - id: D6
    description: "사람이 실제 기기 셋 이상으로 12.3-UAT.md 테스트 2~8을 처음부터 다시 걷는다 — 링크를 연 순간부터 안내가 저절로 오는지, 헷갈리는 자리가 남았는지, 누구 차례인지 화면만 보고 아는지"
    verification: []
    human_judgment: true
    rationale: "세 계획(12.3-12/13/14)이 닫은 것은 코드 레벨의 막힌 자리들이고, 흐름이 실제로 이어지는지는 사람이 걸어야 안다 — 12.3-UAT.md의 Current Test 절에 재개 안내를 적었다"

duration: ~20min
completed: 2026-08-22
status: complete
---

# Phase 12.3 Plan 14: 안내 자동 호출 + 차례 표시 통합 Summary

**인원 확정 뒤 화면이 스스로 첫 안내를 부르고(shouldAnnounce, 시험된 순수 함수), 안내가 없는 동안 「진행 중」또는 「실패+재시도」한 블록만 그리며, 지금 누구 차례인지 이름과 「순서는 진행자가 정해요」를 함께 말한다(creationTurn/creationTurnLabel).**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-08-22T07:50Z (16:50 KST)
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- `creationView.ts`에 `shouldAnnounce(state, announced, lastAttemptFailed)` 추가 — `shouldNominateNext` 옆에 같은 부트스트랩 판정 모양으로 앉는다. 시험 다섯(부른다·인원 확정 전·이미 안내 있음·직전 실패·명단 잠김)이 함수를 쓰기 전에 실패했다는 것을 확인한 뒤 통과시켰다
- `CreationScreen.tsx`: `gmLines`/`announced`/`gate` 계산과 `announce()`(useCallback)를 이른 반환(`if (partyRoster !== null)`) 위로 옮기고, `shouldAnnounce` 하나로만 판단하는 자동 안내 effect를 추가했다 — 여러 탭이 동시에 이 effect를 타도 서버의 `_gm_dedupe_key("announce")`가 세션당 한 번으로 접는다(D-12). 실패하면 자동 재시도 안 함(D-13 ②)
- `gate === "done"` 렌더에서 `!announced`가 먼저 이기게 순서를 뒤집어, 안내가 없는 동안 한 블록(진행 중 문구 `COPY.creationAnnouncing` 또는 실패 이유+재시도 단추 `COPY.creationAnnounceRetry`)만 그린다 — 「기다려라」·「눌러라」·「불러오는 중」이 동시에 뜨던 자리(G-12.3-5)가 구조적으로 사라졌다
- `creationView.ts`에 `CreationTurn` 판별 유니온과 `creationTurn(state, myCharacterId, steps)` 추가 — `creation_current_speaker_id`를 읽고 「아무 차례도 아님/내 차례/남의 차례(이름 또는 이름 없음)」로 판정한다. 이름은 완성된 캐릭터 다음 아직 완성 전인 사람의 `provides_display_name` 항목값에서 찾고, 둘 다 없으면 지어내지 않는다
- `labels.ts`에 `creationTurnLabel(turn)` 추가 — 「{이름} 차례예요 — 순서는 진행자가 정해요. 자유롭게 끼어들어 말할 수 있어요」를 조립한다. `CreationPane.tsx`의 대화판 첫 줄이 이 결과를 그리고, 지목 전에는 그 줄 자체를 안 그린다(오늘은 지목 전에도 「다른 사람의 차례예요」가 떴다)
- 전체 회귀(Task 3): frontend tsc 빈 출력·build 성공·vitest 98 passed, `.venv/bin/python -m pytest` 1385 passed, 실제 크로미움으로 루프백 아닌 주소에서 화면이 뜨는 것 확인(`check_insecure_origin.py` exit=0). 서버 diff가 `routes_creation.py`·`actor.py` 정확히 둘로 좁혀져 있음을 재확인

## Task Commits

Each task was committed atomically:

1. **Task 1: 첫 안내를 부를 때인지의 판정을 시험 가능한 순수 함수로 세우고, 화면이 그 판정으로 스스로 안내를 부르며, 안내가 없는 동안 한 가지만 말하게 만든다** - `c9f6de0` (feat, tracer/tdd — RED와 GREEN을 한 커밋에 묶음, 아래 「RED/GREEN 기록」 참조)
2. **Task 2: 지금 누구 차례인지 이름으로 말하고, 그 순서가 진행자의 선택이라는 사실을 함께 말한다** - `1456e9f` (feat, tdd — RED/GREEN을 한 커밋에 묶음)
3. **Task 3: 세 계획이 함께 선 상태를 전체 회귀와 실제 브라우저로 확인하고, 사람이 UAT를 다시 걸 수 있게 정리한다** - `cf26923` (docs)

_Task 1·2는 `tdd="true"`다. 둘 다 RED(시험 실패 확인, 출력을 아래에 그대로 남김)와 GREEN(구현으로 통과)을 지시된 순서대로 실행했지만, `test(...)`/`feat(...)` 두 커밋으로 쪼개지 않고 하나로 묶었다 — 12.3-12·12.3-13이 세운 것과 같은 판단이다: 각 Task의 `<reversibility>`가 "더하는 것은 순수 함수 하나와 그 시험(+ effect/문구 함수)뿐"이라고 명시하는 단일 논리적 변경이다. 두 Task가 같은 파일(`labels.ts`·`creationView.ts`·`creationView.test.ts`)을 건드리는 지점에서는, 각 커밋에 그 Task의 변경만 들어가도록 파일을 일시적으로 되돌렸다 되살리는 방식으로 diff를 완전히 분리했다(각 커밋 시점에 tsc+vitest로 그 중간 상태 자체가 유효함을 확인)._

## RED/GREEN 기록

### Task 1 — `shouldAnnounce` (RED)

`cd frontend && npx vitest run src/session/creationView.test.ts`, 함수를 더하기 **전**:

```
 FAIL  src/session/creationView.test.ts > 첫 안내를 부를 때인지 판정한다 (G-12.3-5) > 인원이 확정됐고 안내가 없고 직전 시도가 실패하지 않았으면 참이다
TypeError: shouldAnnounce is not a function
 FAIL  ... > 인원 확정 전(party_size_fixed === null)이면 거짓이다
TypeError: shouldAnnounce is not a function
 FAIL  ... > 이미 안내가 있으면(announced === true) 거짓이다
TypeError: shouldAnnounce is not a function
 FAIL  ... > 직전 시도가 실패했으면 거짓이다 — 자동 재시도 고리를 만들지 않는다(D-13 ②)
TypeError: shouldAnnounce is not a function
 FAIL  ... > 명단이 이미 잠겼으면(party_roster !== null) 거짓이다 — 만들기가 끝난 세션이다
TypeError: shouldAnnounce is not a function

 Test Files  1 failed (1)
      Tests  5 failed | 47 passed (52)
```

**GREEN — `shouldAnnounce`를 더한 뒤, 같은 명령:**

```
 Test Files  1 passed (1)
      Tests  52 passed (52)
```

### Task 2 — `creationTurn` (RED)

`cd frontend && npx vitest run src/session/creationView.test.ts`, 함수를 더하기 **전**:

```
 FAIL  src/session/creationView.test.ts > 지금 누구 차례인지 판정한다 (G-12.3-7) > 아직 아무도 지목되지 않았으면 아무 차례도 아니다
TypeError: creationTurn is not a function
 FAIL  ... > 내가 지목됐으면 내 차례다
TypeError: creationTurn is not a function
 FAIL  ... > 남이 지목됐고 그 사람이 이미 완성됐으면 그 이름을 낸다
TypeError: creationTurn is not a function
 FAIL  ... > 남이 지목됐고 이름 항목을 아직 안 채웠으면 이름이 없다고 답한다 — 없는 이름을 지어내지 않는다
TypeError: creationTurn is not a function
 FAIL  ... > 남이 지목됐고 이름 항목을 이미 채웠으면 완성 전이라도 그 이름을 낸다
TypeError: creationTurn is not a function

 Test Files  1 failed (1)
      Tests  5 failed | 52 passed (57)
```

**GREEN — `creationTurn`을 더한 뒤, 같은 명령:**

```
 Test Files  1 passed (1)
      Tests  57 passed (57)
```

## Task 1 ③-2 — 발동 조건 대조

`shouldAnnounce`가 읽는 네 값과 자동 안내 effect의 의존성 목록을 대조했다:

| `shouldAnnounce`가 읽는 값 | effect 의존성에 있는가 |
|---|---|
| `state.party_size_fixed` | `feed.state?.party_size_fixed` — 있음 |
| `state.party_roster !== null` | `feed.state?.party_roster !== null` — 있음 |
| `announced` | `announced` — 있음 |
| `lastAttemptFailed`(`error !== null`) | `error !== null` — 있음 |

`announce()`가 쓰는 `sessionId`·`rulebookId`도 의존성에 있다(+ `announce` 자체). 하나도 빠진 값이 없다.

## Task 3 — 「불러오는 중」 전수 감사

`CreationScreen.tsx`·`CreationPane.tsx`에서 `COPY.loading`을 그리는 자리 넷을 전부 찾아 각각이 실제로 진행 중인 상태인지 판단했다:

| 자리 | 실제로 진행 중인가 |
|---|---|
| `CreationScreen.tsx` — `partyRoster !== null && partyRoster.includes(myCharacterId)` 직후 반환 | 그렇다 — `onEntered()` effect가 `App`의 Gate를 바꾸는 중인 실제 전이 한 프레임이다 |
| `CreationScreen.tsx` — `gate === "fix" && !stepsLoaded` | 그렇다 — `fetchCreationDeclaration`(항목 선언+인원 범위) 요청이 실제로 도는 중이다(12.3-13이 이미 이 근거를 주석으로 남김) |
| `CreationScreen.tsx` — 최종 else(`gate === null` 또는 `"waiting_for_claim"`) | 그렇다 — `gate === null`은 첫 폴링 응답 대기, `"waiting_for_claim"`은 마운트 즉시 발사되는 `claimCreationHost` 방장 재실 신호(beacon) 요청이 도는 중이다 |
| `CreationPane.tsx` — `conversation.length === 0` | **이론상 도달 불가.** `CreationPane`은 `CreationScreen.tsx`가 `announced === true`일 때만 렌더하고, `announced`는 `gmLinesFrom(events)`에 `kind: "announce"` 줄이 있다는 뜻이라 그 시점엔 `conversation.length >= 1`이다. 죽은 코드이지 사람에게 거짓을 말하는 자리는 아니다 — Task 2·3 어느 쪽도 `CreationPane.tsx`의 이 조건문 자체를 만지라고 지시하지 않았고(Task 2는 대화판 "첫 줄"만, Task 3는 감사만), 죽은 조건 분기 제거는 Phase 16의 화면 재설계 경계에 더 가까워 이번 계획에서 손대지 않았다 |

**결론:** G-12.3-5가 문제 삼은 「진행 중이 아닌데 진행 중이라고 말하는」 자리는 Task 1이 이미 걷어냈다(옛 `gmLines.length === 0` 대체 문구). 남은 넷은 전부 실제 요청/전이와 묶여 있어 추가로 고칠 것이 없었다.

## Files Created/Modified

- `frontend/src/session/creationView.ts` - `shouldAnnounce` export 추가(Task 1), `CreationTurn`/`creationTurn` export 추가(Task 2)
- `frontend/src/session/creationView.test.ts` - `shouldAnnounce` 시험 5건(Task 1), `creationTurn` 시험 5건(Task 2) 추가
- `frontend/src/labels.ts` - `creationAnnounce`를 `creationAnnouncing`/`creationAnnounceRetry`로 교체(Task 1), `creationTurnSuffix`/`creationUnknownNameSubject`/`creationTurnLabel` 추가하고 `creationOthersTurn` 제거(Task 2)
- `frontend/src/screens/CreationScreen.tsx` - 훅 순서 재정리, 자동 안내 effect, `gate === "done"` 렌더 갈래 재구성, 아래쪽 독립 안내 단추 제거(Task 1)
- `frontend/src/panes/CreationPane.tsx` - 대화판 첫 줄을 `creationTurn`+`creationTurnLabel`로 교체(Task 2)
- `.planning/phases/12.3-creation-screen/12.3-UAT.md` - G-12.3-2 … G-12.3-7 여섯 gap의 `status`/`resolved_by` 갱신, `## Current Test` 재개 안내(Task 3)

## Decisions Made

- Task 1·2의 RED/GREEN 커밋을 하나로 묶었다 — 각 Task의 `<reversibility>` 원문이 이미 예정한 원자성(12.3-12·13와 같은 판단)
- `COPY.loading`이 남는 네 자리 중 어느 것도 안 고쳤다 — Task 3의 전수 감사 결과 전부 실제 진행 중인 상태와 묶여 있었다(위 표 참조). `CreationPane.tsx`의 `conversation.length === 0` 대체 문구는 새 구조에서 이론상 도달 불가한 죽은 코드이지만, 이 계획의 Task 범위(Task 2는 "첫 줄"만, Task 3는 감사만)를 넘는 정리라 손대지 않았다
- 발동 조건 대조(Task 1 ③-2)를 코드 주석이 아니라 이 SUMMARY의 표로 명시했다 — 순수 함수 시험이 "조건이 옳은가"까지만 답하고 "그 조건이 언제 참이 되는지 effect가 실제로 보는가"는 별개 질문이라는 계획의 구분을 그대로 따랐다

## Deviations from Plan

### 계획 acceptance criteria의 grep 기준선과 실측이 다른 자리 둘 (문서화, 코드 변경 없음)

**1. `grep -vE ... | grep -c 'COPY.loading' frontend/src/screens/CreationScreen.tsx` — 계획은 `2`보다 작아야 한다고 적었지만 실측은 `3`이다**
- **계획의 서술:** "오늘은 세 자리다. 남는 자리는 실제로 조회가 도는 곳뿐이다" — 이 문구는 12.3-13이 `gate === "fix"`에 새 로딩 분기(현재 line 446)를 추가하기 **전** 상태(3자리: 339·406·417, 옛 줄번호)를 기준으로 쓰인 것으로 보인다. 12.3-13이 그 분기를 정당하게 추가한 뒤 실제 기준선은 이미 4자리였다(`git show <12.3-13 이전 커밋>:frontend/src/screens/CreationScreen.tsx | grep -c COPY.loading` = 4)
- **이 계획이 한 일:** G-12.3-5가 실제로 지목한 유일한 「가짜」 자리(`gmLines.length === 0` 대체 문구)를 지웠다 — 4자리 → 3자리
- **왜 더 안 줄였는가:** 남는 세 자리(партyRoster 전이·fix-gate 조회 중·최종 catch-all)는 Task 3의 전수 감사(위 표)에서 전부 실제 진행 중인 상태로 확인됐다. 진행 중인 자리를 억지로 지우거나 문구를 바꾸는 것은 G-12.3-5가 요구한 것(거짓말 제거)이 아니라 화면 재설계이고, `<planner_assumptions>` ⑦·`must_haves.prohibitions`가 배치·구조 재설계를 Phase 16 경계로 명시적으로 막는다
- **영향:** 없음 — 이 acceptance criteria 한 줄을 기계적으로 통과시키지 못했지만, 그 criteria가 지키려는 실제 성질(「진행 중이 아닌데 진행 중이라고 말하지 않는다」)은 Task 3 감사로 별도 증명됐다

**2. `grep -vE ... | grep -c 'shouldAnnounce' frontend/src/screens/CreationScreen.tsx` — 계획은 정확히 `1`이어야 한다고 적었지만 실측은 `2`다**
- **원인:** import 문(`shouldAnnounce,`)과 호출부(`shouldAnnounce(feed.state, ...)`)가 각각 한 줄이라 bare 문자열 grep이 둘 다 센다
- **선례:** 같은 계획의 다른 acceptance criteria(`partySizeOutOfRange` 등)는 이 정확한 패턴(import+사용=2)을 `> 0`으로 허용한다 — 이 criteria만 `정확히 1`로 다르게 적혀 있다
- **실제로 증명해야 하는 성질**(criteria의 서술: "화면이 발동 조건을 다시 쓰지 않고 그 함수 하나만 부른다")은 지켜졌다 — `shouldAnnounce`를 호출하는 자리는 effect 안 한 곳뿐이고, 조건을 두 번째로 다시 쓴 자리는 없다
- **영향:** 없음 — 기계적 수치만 다르고 의도한 불변식은 성립한다

---

**Total deviations:** 2 documented (acceptance-criteria 수치 불일치, 코드 변경 없음)
**Impact on plan:** 둘 다 계획 문서 자체의 기준선 서술 오차이거나 grep 패턴의 알려진 한계(import+사용 이중 계산)다. 두 criteria가 지키려던 실제 성질은 Task 1·3에서 별도로 증명됐다.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 12.3 phase의 마지막 계획이다. 세 계획(12.3-12/13/14)이 `12.3-UAT.md` 테스트 2가 낸 결함 여섯(G-12.3-2 … G-12.3-7)을 전부 코드 레벨에서 닫았다 — **사람이 실제 기기 셋 이상으로 테스트 2부터 다시 걸어야 확정된다**(위 coverage D6, `human_judgment: true`). `12.3-UAT.md`의 `## Current Test`에 재개 안내를 적었다
- `CHAR-06` 요구사항을 이 계획에서 `Complete`로 표시한다 — `requirements.ready-ids` 조회 결과 `1/1 ready`(같은 phase의 다른 계획들이 CHAR-06을 함께 선언하던 중 이 계획이 마지막 조각을 채웠다)
- WR-01(끼어들기 입력의 노출 규칙, D-08)은 이번에도 의도적으로 안 건드렸다 — `12.3-UAT.md` 테스트 9가 사람이 직접 겪고 방향(안 보일지/보이되 거절당할지)을 고르도록 설계돼 있고, 그 확인이 이번 세 계획으로 비로소 가능해졌다
- 완성 전 캐릭터의 이름을 서버가 폴링에 실어주지 않는 한계(`<planner_assumptions>` ④)가 남아 있다 — 이 룰북(던전월드류)에서는 「아직 이름을 안 정한 분」이 대부분의 차례에 뜬다. 사람 확인에서 부족하다고 판단되면 서버를 넓히는 별도 gap으로 다룬다
- 서버 코드가 `routes_creation.py`·`actor.py` 정확히 두 파일(12.3-13이 약속한 범위)로 좁혀져 있음을 `git diff --name-only 231658e..HEAD -- src/gptrpg`로 재확인했다. `rules_core`는 이번에도 빈 출력이다

## Self-Check: PASSED

- `frontend/src/session/creationView.ts` 존재 확인 — FOUND
- `frontend/src/session/creationView.test.ts` 존재 확인 — FOUND
- `frontend/src/labels.ts` 존재 확인 — FOUND
- `frontend/src/screens/CreationScreen.tsx` 존재 확인 — FOUND
- `frontend/src/panes/CreationPane.tsx` 존재 확인 — FOUND
- `.planning/phases/12.3-creation-screen/12.3-UAT.md` 존재 확인 — FOUND
- 커밋 `c9f6de0` — FOUND (`git log --oneline --all | grep c9f6de0`)
- 커밋 `1456e9f` — FOUND (`git log --oneline --all | grep 1456e9f`)
- 커밋 `cf26923` — FOUND (`git log --oneline --all | grep cf26923`)
- acceptance_criteria 재실행 — 대부분 PASS, 둘은 위 「Deviations from Plan」에 문서화된 대로 기계적 수치 불일치(성질은 별도 증명됨):
  - `export function shouldAnnounce` in creationView.ts = 1
  - `shouldAnnounce` 시험 카운트 in test.ts = 6 (> 4)
  - `shouldAnnounce` 비주석 in CreationScreen.tsx = 2 (계획은 정확히 1 요구 — Deviation 2)
  - `creationAnnouncing` in labels.ts+CreationScreen.tsx = 각 1
  - `creationAnnounce`(구 키) 전체 합 = 0
  - `creationAnnounceRetry` in labels.ts+CreationScreen.tsx = 각 1
  - `announcingRef` 비주석 = 4 (> 1)
  - `COPY.loading` 비주석 in CreationScreen.tsx = 3 (계획은 <2 요구 — Deviation 1)
  - `export function creationTurn` in creationView.ts = 1
  - `export function creationTurnLabel` in labels.ts = 1
  - `creationOthersTurn` 전체 합 = 0
  - `순서는 진행자가 정해요` in labels.ts = 1 (> 0)
  - `creationTurnLabel` 비주석 in CreationPane.tsx = 2 (> 0)
  - `pc-` 비주석 (CreationPane.tsx+creationView.ts+labels.ts) = 0
  - `cd frontend && npx tsc --noEmit` = 빈 출력, `npm run build` = 성공
  - `cd frontend && npx vitest run` = 98 passed (기존 88 + 신규 10)
  - `.venv/bin/python -m pytest -q --tb=short` = 1385 passed
  - `.venv/bin/python scripts/check_insecure_origin.py; echo exit=$?` = exit=0
  - `git diff --name-only 231658e..HEAD -- src/gptrpg` = 정확히 `actor.py`, `routes_creation.py` 둘
  - `git diff --name-only 231658e..HEAD -- src/gptrpg/rules_core` = 빈 출력
  - TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 마커 = 없음 (5개 frontend 파일 + 2개 python 파일 + UAT.md)
  - `12.3-UAT.md`: `testing paused` = 0건, `status: failed` = 0건
- 플랜 레벨 `<verification>`의 자동 검사 전부 재확인 — PASS (2건의 grep 수치 불일치는 위에 문서화)

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-22*
