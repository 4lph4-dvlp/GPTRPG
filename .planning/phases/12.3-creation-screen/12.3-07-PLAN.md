---
phase: 12.3-creation-screen
plan: 07
type: execute
wave: 6
depends_on: ["12.3-06"]
gap_closure: true
files_modified:
  - .gptrpg/agents.json
autonomous: true
requirements: [CHAR-06]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "실제 브라우저에서 인원 확정 → 안내 → 첫 사람 지목 → 항목 확정 화면이 열리는 데까지 밟아 본 기록이 남는다 (12.3-VERIFICATION.md missing ③)"
    - "두 번째·세 번째 사람도 차례로 지목되는 것을 실제 브라우저로 확인한 기록이 남는다"
    - "주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07) — 케언 룰북으로 실제로 굴려 본다"
    - "남의 차례에도 끼어들기가 열려 있고 409가 아니라 실제로 전송된다. 항목 확정 조작은 지목된 사람에게만 보인다 (D-08)"
    - "동의 관문이 아직 안 누른 사람의 이름을 보여주고, 「고칠 게 있어요」가 다른 탭의 동의 표시를 초기화한다 (D-03/D-11)"
    - "방장 탭이 30초 넘게 조용하면 다른 탭이 이어받아 인원 확정을 진행할 수 있다 (D-11)"
    - "AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 서로 다른 문구로 뜬다 (D-13 ①②③)"
    - statement: "D-13 ②(설정 없음)를 재현하려면 `agents.json`에서 `creation_gm`과 `master_gm`을 **둘 다** 비워야 한다 — `ROLE_FALLBACKS`가 `creation_gm` 없으면 `master_gm` 선택을 그대로 물려준다"
      verification: backstop
  prohibitions:
    - "화면 배치·색·간격·애니메이션을 이 계획에서 고치지 않는다 (Phase 16)"
    - "관찰 중에 발견한 결함을 이 계획에서 즉석으로 고치지 않는다 — 기록하고 다음 판단에 넘긴다"
    - "CHAR-06을 이 계획에서 완료로 표시하지 않는다 — 열네 항목 사람 확인이 실제로 끝난 뒤 사람이 판단한다"
    - "`.gptrpg/agents.json`을 원래 값으로 되돌리지 않은 채 끝내지 않는다"
    - "확인용 세션 데이터베이스를 사장님의 실제 세션 파일에 섞지 않는다"
  artifacts:
    - .planning/phases/12.3-creation-screen/12.3-07-SUMMARY.md
  key_links:
    - "12.3-06의 재실 신호 → 첫 지목 → `stepEditingEnabled` → 그 뒤에 막혀 있던 다섯 진실 전부"
    - "`ROLE_FALLBACKS`(`src/gptrpg/agents/config.py:56-70`) → `creation_gm`이 없으면 `master_gm`을 물려받는다 → D-13 ②의 재현 조건"
---

<objective>
**12.3-06이 교착을 푼 뒤, 그 교착에 막혀 한 번도 못 본 것들을 실제로 보고 온다.**

12.3-VERIFICATION.md(2차)는 실패 하나와 「막혀서 확인 못 한」 다섯을 남겼다. 다섯은 **망가졌다고 확인된 것이 아니라 도달하지 못한 것**이다 — 코드는 있고 배선도 리뷰 수준에서는 맞다. 그러니 이 계획은 다섯을 다시 만들지 않는다. **실제로 거기까지 가서 본다.**

이 계획이 하는 일:
- 검증 보고서의 `missing` ③(고친 뒤 실제 브라우저로 재확인)을 그대로 실행한다
- 그 흐름을 끝까지 밀어 막혀 있던 네 진실(D-07 주사위 연출 · D-08 끼어들기와 확정 조작의 분리 · D-03/D-11 동의 관문과 재오픈 · D-11 방장 승계)에 도달해 관찰한다
- D-13 세 실패 문구를 실제로 재현한다 — **1차 검증의 예측이 틀렸다는 정정**을 반영해서

**고치지 않는다.** 관찰하고 기록한다. 결함이 나오면 그것은 다음 판단의 재료다.
**CHAR-06을 여기서 체크하지 않는다.** 12.3-05-SUMMARY.md의 열네 항목 사람 확인이 실제로 끝나야 사람이 판단한다.

Purpose: 「코드가 있다」와 「사람이 실제로 할 수 있다」 사이의 간격을 두 번 다시 추측으로 메우지 않는다.
Output: 관찰 기록 — 무엇이 실제로 되고 무엇이 여전히 안 되는지.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.3-creation-screen/12.3-VERIFICATION.md
@.planning/phases/12.3-creation-screen/12.3-CONTEXT.md
@.planning/phases/12.3-creation-screen/12.3-05-SUMMARY.md
@.planning/phases/12.3-creation-screen/12.3-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 새 세션을 실제 브라우저로 끝에서 끝까지 밟는다 — 지목이 진짜 일어나는가</name>
  <files>.gptrpg/agents.json</files>
  <read_first>
    - `.planning/phases/12.3-creation-screen/12.3-VERIFICATION.md` §2차 검증(이번) 방법 — 확인용 서버를 어떻게 띄웠고 어떤 세션 식별자를 썼는지. 같은 방식을 따르되 **새 세션 식별자**를 쓴다(예전 세션에는 이미 절반쯤 진행된 상태가 남아 있다)
    - `.planning/phases/12.3-creation-screen/12.3-VERIFICATION.md` §새로 발견한 결함 — 관찰해야 할 정확한 지점(안내 성공 직후 화면에 무엇이 있었는가, 끼어들기가 왜 409였는가)
    - `.planning/phases/12.3-creation-screen/12.3-06-PLAN.md` §artifacts_this_phase_produces — 12.3-06이 무엇을 바꿨는지
    - `.gptrpg/agents.json` — 지금 어떤 역할이 설정돼 있는지. **원래 내용을 통째로 따로 보관한 뒤에** 아무것도 안 건드리고 이 과제를 진행한다(Task 3이 이 파일을 바꾼다)
    - `frontend/src/screens/CreationScreen.tsx` · `frontend/src/panes/CreationPane.tsx` — 화면에 무엇이 언제 보이는지(무엇을 클릭해야 하는지)
  </read_first>
  <action>
    확인용 서버를 직접 띄운다 — 사람에게 명령을 시키지 않는다. 백엔드(uvicorn)와 프런트엔드(vite)를 각각 배경으로 올리고, 이 확인 전용의 새 데이터베이스 파일(`GPTRPG_DB`)을 쓴다. 사장님의 실제 세션 파일을 절대 안 건드린다.

    `/browse` 스킬로 브라우저를 잡고 **새 세션 식별자**로 접속해, 검증 보고서의 `missing` ③이 적은 순서 그대로 밟는다:
    1. 인원 확정 화면이 뜨는지 — 인원 3으로 확정, 200
    2. 「진행자에게 안내를 부탁하기」 — GM 안내 문장이 화면에 뜨는지
    3. **그다음 지목이 실제로 일어나는지** — `creation_gm_spoke` 사건 중 `kind`가 지목인 것이 생기고, `creation_current_speaker_id`가 채워지고, **항목 확정 조작이 화면에 실제로 나타나는지**. 5초 넘게 기다려도 안 나타나면 그 자체가 결과다
    4. 첫 사람의 필수 항목을 화면 조작으로 전부 채우고 완성시킨다
    5. 두 번째 브라우저 문맥(다른 `localStorage` = 다른 캐릭터 식별자)으로 같은 세션에 들어가, 그 사람이 이어서 지목되는지 확인한다. 세 번째도 같게 한다

    각 단계에서 화면에 실제로 보인 것(접근성 트리 또는 스크린샷)과 실제로 나간 HTTP 요청·응답 코드, 그리고 `GET .../events`가 돌려준 사건 목록을 함께 남긴다 — 「보였다」와 「기록됐다」 둘 다 확인해야 이 결함이 두 번 안 빠져나간다.

    **결함이 나오면 고치지 말고 정확히 어디서 무엇이 막혔는지 적는다** — 어느 요청이 몇 번을 돌려줬는지, 그 순간 화면에 남은 조작이 무엇이었는지, 사건 기록의 `last_seq`가 움직였는지까지.

    끝나면 서버를 정리하되 **확인용 데이터베이스 파일은 지우지 않는다** — Task 2가 이어서 그 세션을 쓴다.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' "http://localhost:8000/api/sessions/${QA_SESSION}/events?from_seq=0" 로 확인용 서버가 200을 돌려주고, 같은 응답에서 kind가 지목인 creation_gm_spoke 사건이 하나 이상 있고 creation_current_speaker_id가 null이 아닌 것을 확인한다</automated>
    <human-check>
      브라우저에서 같은 흐름을 사람이 직접 한 번 밟아 주세요 — 자동 확인이 「사건이 기록됐다」까지만 말하기 때문입니다.
      1. 새 세션 링크를 엽니다. 인원을 3으로 확정합니다
      2. 「진행자에게 안내를 부탁하기」를 누릅니다 — 진행자의 안내 문장이 뜨나요?
      3. **잠시 기다린 뒤, 항목을 채우는 조작이 화면에 실제로 나타나나요?** (이것이 이 계획 전체가 확인하려는 한 가지입니다)
      4. 항목을 몇 개 채워 봅니다 — 자연스럽게 이어지나요, 아니면 어디서 멈추나요?
      「됩니다」 또는 「어디서 멈췄는지」로 답해 주세요.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `GET /api/sessions/{새 세션}/events?from_seq=0` 응답에 `event_type`이 `creation_gm_spoke`이고 `kind`가 지목인 사건이 하나 이상 있다
    - 같은 세션의 폴링 `state`에서 `creation_current_speaker_id`가 `null`이 아닌 순간이 관찰됐다
    - 지목된 브라우저 화면에서 항목 확정 조작이 실제로 보였다는 증거(접근성 트리 또는 스크린샷)가 SUMMARY에 있다
    - 두 번째 사람이 이어서 지목된 증거(두 번째 지목 사건 + 그 사람의 화면)가 SUMMARY에 있다 — 안 됐으면 「안 됐다」와 그 지점이 SUMMARY에 있다
    - `.gptrpg/agents.json`이 이 과제 시작 시점과 바이트 단위로 같다(`git status`가 못 보는 파일이므로 해시를 남긴다)
  </acceptance_criteria>
  <done>검증 보고서가 「절대 안 일어난다」고 적은 첫 지목이 실제 브라우저에서 일어나는지 아닌지가, 추측이 아니라 관찰로 적힌다.</done>
</task>

<task type="auto">
  <name>Task 2: 교착에 막혀 한 번도 못 본 네 진실에 실제로 도달한다</name>
  <files>.gptrpg/agents.json</files>
  <read_first>
    - `.planning/phases/12.3-creation-screen/12.3-VERIFICATION.md` §frontmatter `behavior_unverified_items` — 네 항목 각각의 `test`·`expected`·`why_human`이 무엇을 보라고 적었는지
    - `frontend/src/components/DiceModal.tsx` · `frontend/src/session/creationView.ts`의 `creationRollsFrom` — 만들기 주사위가 판정 주사위와 같은 연출을 타는 경로
    - `src/gptrpg/rulebooks/cairn.py` — 능력치 3d6과 Hit Protection 1d6이 선언된 자리. 주사위 연출을 보려면 **케언 룰북**이 필요하다(던전월드류에는 굴리는 항목이 없다)
    - `frontend/src/screens/CreationScreen.tsx:64` — `DEFAULT_RULEBOOK_ID`가 하드코딩돼 있다(의도된 미완성). 케언으로 확인하려면 이 자리나 세션 상태를 어떻게 다뤄야 하는지 먼저 읽고 판단한다
    - `frontend/src/panes/CreationPane.tsx`의 `InterjectBox`와 `stepEditingEnabled` — D-08이 요구하는 「항상 열림 / 자기 차례에만 뜸」의 정확한 조건
    - `src/gptrpg/web/creation_state.py`의 `HOST_IDLE_S`(30초)와 `frontend/src/config.ts`의 `HOST_BEACON_MS` — 방장 승계를 실제로 기다려야 하는 시간
  </read_first>
  <action>
    Task 1이 남긴 확인용 서버와 세션을 이어서 쓴다. 검증 보고서의 `behavior_unverified_items` 넷을 순서대로 실제로 밟는다.

    **① D-07 주사위 연출.** 케언 룰북으로 새 확인용 세션을 하나 더 열어(던전월드류에는 굴리는 항목이 없다) Hit Protection 굴림이 있는 자리까지 간다. 주사위 모달이 실제로 뜨는지, 판정에서 쓰는 것과 같은 모양인지 관찰한다. 화면이 지금 룰북 식별자를 하드코딩하고 있다는 것을 먼저 확인하고(위 `read_first`), 그것 때문에 케언에 도달할 수 없으면 **그 사실 자체를 결과로 적는다** — 화면을 이 계획에서 고치지 않는다.

    **② D-08 끼어들기.** 브라우저 문맥 둘로 같은 세션에 들어가, A가 지목된 상태에서 B의 화면을 본다. B에게 끼어들기 칸이 보이는지, 실제로 보내지는지(200인지 409인지), 그리고 B에게 **항목 확정 조작은 안 보이는지**를 함께 확인한다. 2차 검증에서 409로 막혔던 그 자리가 이번엔 어떻게 되는지가 요점이다.

    **③ D-03/D-11 동의 관문과 재오픈.** 전원을 완성시켜 동의 관문까지 간다. 아직 안 누른 사람이 **이름으로** 보이는지(식별자가 아니라), 「고칠 게 있어요」로 항목 하나를 다시 열었을 때 그것이 다른 탭에도 전파되고 이미 누른 사람의 동의 표시가 초기화되는지 확인한다.

    **④ D-11 방장 승계.** 방장 탭을 30초 넘게 방치한 뒤(신호가 두 번 이상 빠져야 한다) 다른 탭에서 인원 확정 조작이 나타나는지 본다. 새 세션으로 확인한다 — 인원이 이미 확정된 세션에서는 이 관문 자체가 안 보인다.

    넷 각각에 대해 **관찰한 것**과 **기대와 어긋난 것**을 나눠 적는다. 고치지 않는다.
  </action>
  <verify>
    <automated>각 확인용 세션의 GET /api/sessions/{id}/events 응답에서 이 과제가 밟은 사건들(creation_step_completed의 rolls 칸 · creation_interjected · creation_consent_recorded · creation_host_claimed의 두 번째 건)이 실제로 기록됐는지를 확인한다</automated>
    <human-check>
      아래 넷은 사람 눈으로만 판단할 수 있습니다. 각각 「그렇다 / 아니다 + 무엇이 이상했는지」로 답해 주세요.
      1. **주사위**: 캐릭터를 만들다 나오는 주사위가, 게임 중 판정할 때 보는 주사위와 같아 보이나요?
      2. **남의 차례**: 다른 사람이 자기 이야기를 하는 동안, 내 화면에서 말을 걸 수 있나요? 그리고 그때 내 항목을 확정하는 조작은 안 보이는 게 맞나요?
      3. **마지막 확인**: 다 만든 뒤 「누가 아직 안 눌렀는지」가 **이름으로** 보이나요? 「고칠 게 있어요」를 눌렀을 때 다른 사람 화면도 따라 바뀌나요?
      4. **방장이 사라졌을 때**: 인원을 정하던 사람이 창을 닫고 30초쯤 지나면, 다른 사람 화면에 인원 정하는 조작이 나타나나요?
    </human-check>
  </verify>
  <acceptance_criteria>
    - 넷 각각에 대해 「관찰한 것」과 「기대와 어긋난 것」이 SUMMARY에 따로 적혀 있다
    - ①에서 케언 룰북에 도달했거나, 도달할 수 없었던 이유(화면의 룰북 하드코딩)가 정확한 파일·행과 함께 적혀 있다
    - ②에서 끼어들기 요청의 실제 응답 코드가 적혀 있다
    - ③에서 동의 관문에 실제로 보인 문자열(이름인지 식별자인지)이 적혀 있다
    - ④에서 기다린 실제 초 수와 그 뒤 화면 상태가 적혀 있다
    - `.gptrpg/agents.json`이 이 과제 시작 시점과 바이트 단위로 같다
  </acceptance_criteria>
  <done>다섯 「도달 못함」 중 넷이 「도달해서 이렇게 보였다」로 바뀐다 — 좋게든 나쁘게든.</done>
</task>

<task type="auto">
  <name>Task 3: D-13 세 실패 문구를 실제로 갈라 본다 — 1차 예측의 정정을 반영해서</name>
  <files>.gptrpg/agents.json</files>
  <read_first>
    - `src/gptrpg/agents/config.py:56-70` — `ROLE_FALLBACKS`. **`creation_gm` 키가 없으면 `master_gm` 선택을 그대로 물려받는다** — 1차 검증이 「`creation_gm`이 없으면 항상 503」이라고 예측했고 2차 검증이 그것을 틀렸다고 정정한 근거가 정확히 이 표다
    - `src/gptrpg/web/routes_creation.py:645-652` — `_resolve_creation_gm_provider`와 `_AGENT_RESOLUTION_ERRORS`가 503으로 올라가는 경로
    - `frontend/src/session/creationView.ts`의 `creationErrorMessage` — 503과 그 밖의 `ApiError`를 어떻게 다른 문구로 가르는지(D-13 ②/D-15)
    - `frontend/src/labels.ts`의 `creationGmUnavailable`·`creationFailed`·끊김 띠 문구 — 사람이 실제로 보게 될 세 문장
    - `src/gptrpg/agents/creation_gm.py:167-210` — `CreationGmContractViolation`이 나는 조건(AI가 후보 밖을 지목하거나 할 말이 비었을 때). D-13 ①을 재현하려면 AI가 그런 답을 내게 만들어야 한다
    - `.gptrpg/agents.json` 원본 — **Task 1에서 보관해 둔 사본**. 이 과제가 끝나면 여기로 정확히 되돌린다
  </read_first>
  <action>
    세 실패를 하나씩 만들어 화면 문구가 실제로 갈리는지 본다. 매 경우 **직전에 `.gptrpg/agents.json`의 사본을 뜨고, 확인이 끝나면 즉시 되돌린다.**

    **② 설정 없음(503)부터 한다** — 재현 조건이 가장 까다롭고, 1차 검증의 예측이 여기서 틀렸다. `creation_gm`만 지우면 `ROLE_FALLBACKS`가 `master_gm`을 물려주므로 **503이 안 난다**(2차 검증에서 실제로 200이 나왔다). **`creation_gm`과 `master_gm`을 둘 다** 없앤 설정으로 서버를 다시 띄우고, 만들기 화면에서 GM을 부르는 조작(안내 또는 지목)이 503을 내는지, 화면이 「진행자를 쓸 수 없는 상태입니다」 계열의 고정 문구를 보이는지 확인한다.

    **① 계약 위반(200 폴백).** AI가 후보 목록 밖을 지목하거나 할 말이 빈 응답을 내도록 만든다 — 실제 제공자를 그렇게 만들 수 없으면, 이 저장소가 이미 가진 대역 제공자 경로(`tests/conftest.py`의 `FakeProvider`)를 확인용 서버에 끼우거나, 그것도 안 되면 **어떤 방법으로도 라이브 재현이 안 되는 이유**를 적고 대신 서버 시험 하나로 폴백 문구가 200과 함께 나오는 것을 확인한다. 사람이 봐야 하는 것은 「기다리면 되는 상황」으로 읽히는지다.

    **③ 연결 끊김.** 확인용 백엔드를 끄고 화면을 그대로 둔다. 7회 연속 폴링 실패(약 10.5초) 뒤 끊김 띠가 뜨는지, 그것이 위 둘과 **다른 문구**인지 확인한다.

    세 문구를 나란히 적어 실제로 서로 다른지 판정한다. 다르지 않으면 그것이 결과다 — 고치지 않는다.

    **마지막에 `.gptrpg/agents.json`을 Task 1에서 보관한 원본으로 정확히 되돌리고, 해시가 같은 것을 확인한다.** 이 파일은 git이 안 보는 파일이라 되돌리지 않으면 아무도 못 알아챈다.
  </action>
  <verify>
    <automated>.gptrpg/agents.json의 sha256이 Task 1에서 기록한 원본 해시와 같다. 그리고 세 경우 각각에서 GM 호출이 돌려준 실제 HTTP 상태 코드(503 / 200 / 요청 자체 실패)가 기록에 남았다</automated>
    <human-check>
      세 가지 잘못된 상황에서 화면이 보여 준 문장을 나란히 놓았습니다. 사람이 이 셋만 보고 **다음에 무엇을 해야 하는지**가 구분되나요?
      - ①(진행자가 이상한 답을 함): 기다릴 필요 없이 그냥 계속하면 된다는 뜻으로 읽히나요?
      - ②(진행자 설정이 없음): 기다려도 안 되고 운영자에게 알려야 한다는 뜻으로 읽히나요?
      - ③(연결이 끊김): 잠시 뒤 돌아온다는 뜻으로 읽히나요?
      셋이 헷갈리면 어느 둘이 헷갈리는지 말씀해 주세요.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 세 경우 각각에서 화면에 실제로 뜬 문자열이 SUMMARY에 그대로 인용돼 있다
    - ②에서 `creation_gm`과 `master_gm`을 **둘 다** 비운 설정을 썼다는 것이 SUMMARY에 적혀 있고, 그 조합에서 실제로 503이 났는지 아닌지가 적혀 있다
    - ①이 라이브로 재현됐거나, 재현할 수 없었던 이유가 적혀 있고 대신 무엇으로 확인했는지가 적혀 있다
    - ③에서 끊김 띠가 뜨기까지 걸린 실제 시간이 적혀 있다
    - `.gptrpg/agents.json`의 sha256이 Task 1에서 기록한 원본 해시와 같다
    - `.venv/bin/python -m pytest -q --tb=short` 가 0으로 끝난다(설정 파일 조작이 아무것도 안 남겼다는 확인)
  </acceptance_criteria>
  <done>세 실패가 실제로 다른 문구로 갈리는지가 관찰로 적히고, 설정 파일이 원래대로 돌아온다.</done>
</task>

</tasks>

<planner_assumptions>
- **이 계획은 아무것도 고치지 않는다.** 관찰 중에 나온 결함은 SUMMARY에 적고 다음 판단(추가 gap closure 또는 재검증)으로 넘긴다. 지금 고치면 「고친 뒤 확인했다」와 「확인하다 고쳤다」가 섞여 무엇이 원인이었는지 다시 흐려진다 — 이 단계가 이미 두 번 겪은 실패 모양이다.
- **CHAR-06을 여기서 완료 표시하지 않는다.** 12.3-05-SUMMARY.md §사람 확인의 열네 항목이 실제로 완주돼야 하고, 그 판단은 사람의 몫이다. 요구사항을 계획 하나가 앞질러 체크하는 것이 이 저장소의 알려진 실패 모양이다.
- **탐지기 결과:** api-coverage `detected: false`, assumption-delta `detected: false`. 사람이 읽고 확인한 결과도 같다 — 이 계획은 코드를 하나도 안 바꾸므로 외부 API 표면도, 신원 모델 전환도 없다. `COVERAGE.md`의 기존 선언이 그대로 유효하다. 스키마 푸시도 해당 없다(이 저장소에 ORM이 없고, 이 계획은 파일을 안 고친다).
- **탐침 경계 넷:** 12.3-06이 `empty`와 `ordering`을 술어로 해소했고 `adjacency`를 시험으로 고정했다. `encoding`은 12.3-06의 가정(`character_id`는 항상 ASCII)이 그대로 유효하다 — 이 계획이 새로 여는 경계가 없다.
</planner_assumptions>

<artifacts_this_phase_produces>
이 계획은 **소스 파일을 하나도 만들지 않는다.** 만드는 것은 기록뿐이다.

- `.planning/phases/12.3-creation-screen/12.3-07-SUMMARY.md` — 다음 다섯 절을 반드시 담는다:
  1. **첫 지목 라이브 재확인** — 인원 확정 → 안내 → 지목 → 항목 확정 화면까지, 단계별 HTTP 상태와 화면 증거
  2. **두 번째·세 번째 사람** — 이어서 지목됐는지
  3. **막혀 있던 네 진실** — D-07 / D-08 / D-03·D-11 / D-11 승계, 각각 「관찰한 것」과 「어긋난 것」
  4. **D-13 세 문구** — 실제로 뜬 문자열 세 개를 나란히
  5. **새로 발견한 결함** — 있으면 파일·행·재현 절차까지. 없으면 「없음」이라고 적는다
- 확인용 데이터베이스 파일(`.gptrpg/qa-12-3-0*.db`) — git이 안 보는 로컬 파일. 지우지 않고 남겨 다음 재검증이 이어 쓸 수 있게 한다
- `.gptrpg/agents.json`은 **바뀌지 않은 채로** 남는다(Task 3이 되돌린다)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 확인용 서버 ↔ 사장님의 실제 세션 데이터 | 확인 절차가 실제 세션 기록을 건드리면 되돌릴 수 없다 |
| 이 계획 ↔ `.gptrpg/agents.json` | git이 안 보는 파일이라, 바뀐 채로 남으면 아무도 못 알아챈다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-21 | Tampering | `.gptrpg/agents.json` | high | mitigate | Task 1이 원본의 sha256을 먼저 기록하고, Task 1·2·3 각각의 승인 기준이 그 해시와의 일치를 요구한다. Task 3의 마지막 동작이 복원이다 — git이 이 파일을 안 보므로 해시가 유일한 그물이다 |
| T-12.3-22 | Tampering | 사장님의 실제 세션 데이터베이스 | high | mitigate | 확인용 서버는 반드시 이 확인 전용의 `GPTRPG_DB` 경로로 띄운다. 기본 경로(`.gptrpg/events.db`)를 쓰지 않는다. 세션 식별자도 확인 전용으로 새로 짓는다 |
| T-12.3-23 | Denial of Service | 확인용 서버 프로세스 | low | accept | 배경 프로세스가 남으면 포트를 점유한다. 각 과제 끝에 정리하되, 남더라도 로컬 개발 환경의 포트 하나일 뿐이라 받아들인다 |
| T-12.3-24 | Information disclosure | 스크린샷·접근성 트리 기록 | low | mitigate | 확인용 세션에만 접속하므로 실제 놀이 기록이 캡처에 안 들어간다. 캡처에 `browser_id`류 식별자가 보이면 SUMMARY에 옮길 때 지운다(T-12.3-05와 같은 규율) |
| T-12.3-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 계획에 패키지 설치 작업이 없다 — 새 의존성을 하나도 더하지 않는다 |
</threat_model>

<verification>
- Task 1~3의 관찰이 전부 SUMMARY에 기록됐다 — 「확인했다」가 아니라 **무엇을 봤는지**가 적혔다
- `.gptrpg/agents.json`의 sha256이 계획 시작 시점과 같다
- `.venv/bin/python -m pytest -q --tb=short` 가 0으로 끝난다
- `git status`에 소스 파일 변경이 하나도 없다(이 계획은 코드를 안 고친다)
</verification>

<success_criteria>
- 검증 보고서가 「절대 안 일어난다」고 적은 첫 지목이 실제 브라우저에서 일어나는지가 관찰로 판정됐다
- 「막혀서 확인 못 함」 다섯 항목이 「확인했다 — 이렇게 보였다」 또는 「여전히 못 봤다 — 이 지점에서 막혔다」로 전부 바뀌었다
- D-13 세 문구가 실제로 갈리는지가 인용된 문자열로 판정됐다
- 확인 절차가 저장소에도 사장님 데이터에도 아무 흔적을 안 남겼다
</success_criteria>

<output>
Create `.planning/phases/12.3-creation-screen/12.3-07-SUMMARY.md` when done
</output>
