---
phase: 12.3-creation-screen
plan: 10
subsystem: creation-screen
tags: [python, fastapi, creation-flow, dedupe-key, regression-test, gap-closure]

# Dependency graph
requires:
  - phase: 12.3-09
    provides: "회복 판정 자체(_forfeited_at 짝 구조, forfeited_nomination_mark) — 이 계획이 그 장치가 새로 연 결함을 닫는다"
provides:
  - "회복이 끝난 뒤 아무것도 안 바뀐 채로 지목을 다시 부르면, 흘려보내지기 전의 옛 지목 기록이 아니라 회복 지목(최신)을 그대로 돌려준다"
  - "흘려보냄 표시가 회복으로 판정이 풀려도 뒤로 안 돌아간다(단조) — 회복 전/후 시대의 중복 방지 키가 구조적으로 겹칠 수 없다"
affects: [12.3-VERIFICATION, CHAR-06, 5차 검증 CR-01, 12.3-REVIEW CR-01/WR-01]

# Actuals (#2632)
actuals:
  tokens: 4833
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "회수 가능한 판정 표시를 「지금 상태」(이진, 뒤로 돌아감)가 아니라 「이 세션에서 마지막으로 판정된 사실」(단조, 앞으로만 감)로 조립한다 — 판정이 풀려도 표시가 안 사라지게 만들어 중복 방지 키의 시대 겹침을 구조적으로 막는다"

key-files:
  created: []
  modified:
    - src/gptrpg/web/creation_state.py
    - src/gptrpg/web/routes_creation.py
    - tests/test_creation_screen_state.py

key-decisions:
  - "검증 보고서·코드 리뷰가 제안한 「지목이 하나라도 있으면 늘 순번을 표시에 싣는다」는 문자 그대로 안 따랐다 — 지목 순번은 호출의 결과이지 입력이 아니라서, 그대로 하면 같은 시점 재호출마다 새 순번이 나와 D-12(같은 시점 재호출은 AI를 다시 안 부른다)가 깨진다. 계획 작성 중 CALL1/2/3 추적으로 실제 확인한 뒤, 표시에 싣는 것을 「마지막 지목의 순번」(출력)이 아니라 「마지막으로 판정된 흘려보냄」(입력)으로 바꾸는 대안을 골랐다."
  - "`_forfeited_at`을 지우거나 순번만 비교하는 대신, 표시 자체를 「이 세션의 모든 흘려보냄 판정 중 적용 대상 순번이 가장 큰 것」으로 재정의해 단조성(뒤로 안 돌아감)을 확보했다 — 지우는 코드는 여전히 안 만든다(D-12 경계, 12.3-09가 이미 세운 판단을 유지)."
  - "`routes_creation.py`의 실행 코드 줄(`key = key + creation_state.forfeited_nomination_mark(...)`)은 한 글자도 안 고쳤다 — 바뀐 것은 그 줄이 부르는 함수의 의미뿐이라서다. 다만 이제 틀린 말이 된 그 위 주석 문단은 새 의미로 교체했다(다음 사람이 옛 의미를 믿고 또 고치는 것을 막는다)."

patterns-established: []

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "회복이 끝난 뒤 아무것도 안 바뀐 상태에서 지목을 세 번째로 불러도(재시도·이중 클릭·경쟁 폴링), 서버가 흘려보내지기 전의 옛 지목 기록이 아니라 회복 지목(최신)을 그대로 돌려준다 — 다른 참가자의 완성에 기대지 않는다 (5차 검증 truth #22, missing ①②, 12.3-REVIEW CR-01)"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_third_nominate_call_after_recovery_returns_the_latest_nomination_not_the_pre_forfeit_one"
        status: pass
    human_judgment: false
  - id: D2
    description: "흘려보냄 표시는 한 번 앞으로 나아가면 뒤로 안 돌아간다 — 회복으로 판정이 풀려도 빈 문자열로 안 돌아가고, 더 큰 순번의 판정이 나오면 앞으로만 간다(단조성)"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched"
        status: pass
    human_judgment: false
  - id: D3
    description: "비-회귀 — 같은 시점 재호출(한 번도 안 흘려보낸 세션이든, 흘려보냄이 있었던 세션이든)은 여전히 AI를 다시 안 부른다(D-12, truth #4)"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_nominate_called_twice_in_the_same_state_does_not_call_the_provider_again"
        status: pass
      - kind: integration
        ref: "tests/test_creation_screen_state.py#test_a_third_nominate_call_after_recovery_returns_the_latest_nomination_not_the_pre_forfeit_one"
        status: pass
    human_judgment: false
  - id: D4
    description: "5차 검증이 통과로 확인한 네 경계(유령이 진짜 참가자를 영구히 안 밀어냄 #15, 값 내는 사람은 차례를 안 뺏김 #16, 재시작 직후 즉시 안 풀림 #17, 흘려보낸 사람도 새 지목으로 회복해 명단 잠금이 풀림 #20·#21)가 이 계획으로 안 깨졌다"
    verification:
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_ghost_presence_signals_do_not_permanently_push_out_real_participants"
        status: pass
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_nominee_that_keeps_submitting_values_never_loses_the_turn"
        status: pass
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately"
        status: pass
      - kind: unit
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_ruling_holds_for_the_same_nomination_but_a_new_one_starts_the_clock_over"
        status: pass
      - kind: integration
        ref: "tests/test_creation_screen_state.py#test_a_forfeited_participant_who_already_submitted_a_value_gets_another_turn_and_unblocks_the_roster_lock"
        status: pass
    human_judgment: false
  - id: D5
    description: "화면 쪽 회귀 없음 — tsc --noEmit 빈 출력, vitest run 80건 통과. frontend/ 아래 파일을 한 줄도 안 고쳤다"
    verification:
      - kind: other
        ref: "cd frontend && npx tsc --noEmit"
        status: pass
      - kind: unit
        ref: "cd frontend && npx vitest run (80 passed)"
        status: pass
    human_judgment: false
  - id: D6
    description: "동의 관문의 교차 전파(화면 쪽 절반) — 서로 다른 브라우저 둘로 같은 세션에 들어가 한쪽 동의 표시가 다른 쪽 재오픈에 되돌아가는지 사람이 눈으로 확인"
    verification: []
    human_judgment: true
    rationale: "이 계획도 화면(frontend/)을 한 줄도 안 고쳤다 — 5차 검증 behavior_unverified_items에서 그대로 이어받은 항목이고, QA 도구(단일 크로미움 프로필)의 구조적 제약으로 자동화할 수 없다. 서버 쪽 절반은 12.3-08의 시험이 이미 지킨다."

duration: 약 30분(단일 연속 세션)
completed: 2026-08-22
status: complete
---

# Phase 12.3 Plan 10: 회복 뒤 옛 지목이 되돌아오는 결함 닫기 Summary

**흘려보냄 표시(`forfeited_nomination_mark`)를 「지금 흘려보낸 상태인가」(이진, 뒤로 돌아감)에서 「이 세션에서 마지막으로 판정된 흘려보냄」(단조, 앞으로만 감)으로 재정의해, 회복 뒤 재호출이 리듀서가 영구 보관한 옛 지목 기록으로 되돌아가던 5차 검증 CR-01을 닫았다.**

## Performance

- **Duration:** 약 30분(단일 연속 세션)
- **Completed:** 2026-08-22
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `forfeited_nomination_mark`의 본문·docstring을 전면 교체했다 — 판정을 새로 계산하지 않고(`forfeited_nominee` 호출 유지, D-04) `_forfeited_at`의 이 세션 항목 중 **적용 대상 지목 순번이 가장 큰 것**으로 표시를 조립한다. 회복으로 판정이 풀려도(`forfeited_nominee`가 `None`을 돌려줘도) `_forfeited_at` 자체는 안 지워지므로 표시가 빈 문자열로 안 돌아간다.
- `routes_creation.py`의 중복 방지 키 조립 코드 줄은 한 글자도 안 바꾸고, 이제 틀린 말이 된 그 위 주석 문단만 새 의미로 교체했다.
- 새 끝-끝 회귀 시험(`test_a_third_nominate_call_after_recovery_returns_the_latest_nomination_not_the_pre_forfeit_one`)으로 「회복 뒤 세 번째 지목 호출이 옛 기록이 아니라 회복 기록을 돌려준다」를 다른 참가자의 캐릭터 완성에 기대지 않고 못박았다 — 이 우회 없는 재현이 이번 gap이 두 라운드를 살아남은 정확한 이유(4차/5차 검증의 유일한 끝-끝 시험은 다른 참가자 완성이 후보 목록을 우연히 바꿔 충돌을 가렸다)다.
- 검증 보고서·코드 리뷰가 제안한 「지목이 있으면 늘 순번을 표시에 싣는다」는 문자 그대로 따르지 않았다 — CALL1/2/3 추적으로 그 처방이 D-12(같은 시점 재호출은 AI를 다시 안 부른다)를 깨는 것을 계획 작성 중 실제 실행해 확인했고, 대신 표시를 「출력(지목 순번)」이 아니라 「입력(마지막 판정된 흘려보냄)」으로 바꿔 같은 목적을 D-12를 안 깨고 이뤘다.
- `test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched`를 WR-01(부분 문자열 확인이 순번 부재를 못 잡던 구멍)까지 포함해 정확 비교로 재작성하고, 「회복 뒤에도 표시가 안 돌아간다」·「단조성」 두 경계를 추가했다.

## Task Commits

Each task was committed atomically:

1. **Task 1: 회복 뒤 세 번째 지목 호출이 옛 기록을 안 돌려준다 — HTTP 한 줄기로 빨갛게 만들고 초록으로 바꾼다** - `72344e4` (fix)
2. **Task 2: 표시의 경계 셋을 실제로 검증력 있게 못박고(WR-01 포함) 전체 회귀를 돌린다** - `9579a04` (test)

_Note: Task 1은 `type="tracer" tdd="true"`였다 — 프로덕션 코드(`creation_state.py`·`routes_creation.py`)와 끝-끝 회귀 시험을 한 커밋으로 묶었다(RED 확인 뒤 GREEN으로 전환한 하나의 원자적 수정). Task 2는 기존 시험의 단언 교체·경계 추가라 별도 `test` 커밋으로 분리했다._

## Files Created/Modified
- `src/gptrpg/web/creation_state.py` - `forfeited_nomination_mark` 본문·docstring 전면 교체(이름·서명 그대로) — 「지금 흘려보낸 상태」 → 「이 세션에서 마지막으로 판정된 흘려보냄」. `forfeited_nominee`·`present_candidates`·`latest_nomination`·`_forfeited_at`은 안 바뀜.
- `src/gptrpg/web/routes_creation.py` - `nominate_creation_speaker` 안 815-818행대 주석 문단만 새 의미로 갱신, 코드 줄(`key = key + ...`)은 무변경.
- `tests/test_creation_screen_state.py` - 새 끝-끝 회귀 시험 1건 추가(Task 1) + 기존 `test_a_forfeited_nomination_changes_the_dedupe_mark_but_a_healthy_one_leaves_it_untouched`의 단언 교체·경계 추가·docstring 재작성(Task 2).

## Decisions Made
- `_forfeited_at`을 지우거나 조건부로 무시하는 코드를 만들지 않았다 — 지우는 순간 표시가 뒤로 돌아가 이번 결함이 그대로 되살아난다(계획 자신의 금지 조항, D-12 경계 유지).
- 표시 모양(`#after:{character_id}:{nominated_seq}`)은 12.3-09가 이미 쓰던 것을 그대로 재사용했다 — 사건에 남은 옛 키와 모양이 같아야 판 이관이 필요 없다.
- 서버 재시작이 남기는 좁은 구멍(`_forfeited_at`이 메모리 표라 재시작하면 빈다)은 알고 남긴 한계로 계획서에 명시했다 — 진짜로 닫으려면 흘려보냄을 사건으로 남겨야 하고(새 사건 종류 + `EVENT_SCHEMA_VERSION` 올림), 그것은 이 계획의 금지 조항이자 되돌리기 어려운 결정이다.

## Deviations from Plan

None - 계획 원문대로 정확히 진행됐다. Task 1의 첫 시도에서 `provider.calls` 절대 개수 단언이 announce 호출까지 포함하는지 놓쳐(announce도 provider를 한 번 쓴다) `assert len(provider.calls) == 1`이 RED 단계와 무관하게 실패했다 — 계획의 `<behavior>` 자체는 "CALL 1 뒤 provider 호출 수"만 요구했으므로, 이것은 시험 자체의 사소한 계산 실수(announce 호출을 안 세었다)였지 소스나 계획 판단의 문제가 아니다. GREEN으로 넘어가기 전에 절대 개수 대신 델타(호출 전후 차이)로 바꿔 바로잡았다 — 별도 deviation으로 분류할 만한 스코프 확대가 아니라 시험 작성 중의 통상적 수정이라 이 절에는 안 올린다.

## Issues Encountered
None.

## User Setup Required
None - 외부 서비스 설정 불필요. 새 의존성·새 HTTP 표면 없음.

## RED 확인 (변경 전 코드에서 시험이 실제로 실패했다)

```
$ .venv/bin/python -m pytest tests/test_creation_screen_state.py -q --tb=short -k "third_nominate_call_after_recovery"
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_a_third_nominate_call_after_recovery_returns_the_latest_nomination_not_the_pre_forfeit_one _
tests/test_creation_screen_state.py:1200: in test_a_third_nominate_call_after_recovery_returns_the_latest_nomination_not_the_pre_forfeit_one
    assert third.json() == second.json()
E   AssertionError: assert {'character_i...요?', 'seq': 4} == {'character_i...요?', 'seq': 5}
E
E     Omitting 2 identical items, use -vv to show
E     Differing items:
E     {'seq': 4} != {'seq': 5}
1 failed, 30 deselected, 2 warnings in 0.51s
```

CALL 3(세 번째 지목 호출, 아무것도 안 바뀐 상태)이 실제 최신 기록(CALL 2, 회복 지목, `seq=5`)이 아니라 흘려보내지기 전의 옛 기록(CALL 1, `seq=4`)을 그대로 돌려주는 것을 소스 무변경 상태에서 값으로 확인했다 — 5차 검증 §CR-01과 정확히 같은 값 형태(오래된 순번이 최신 순번보다 작게 재사용됨)의 실증이다.

## GREEN 확인 (수정 뒤)

```
$ .venv/bin/python -m pytest tests/test_creation_screen_state.py -q --tb=short -k "third_nominate_call_after_recovery"
.                                                                        [100%]
1 passed, 30 deselected, 2 warnings in 0.22s

$ .venv/bin/python -m pytest tests/test_creation_screen_state.py tests/test_creation_consent_host.py -q --tb=short
...............................................                          [100%]
47 passed, 2 warnings in 1.99s

$ .venv/bin/python -m pytest tests/test_creation_screen_state.py -q --tb=short -k "dedupe_mark or third_nominate_call_after_recovery"
..                                                                       [100%]
2 passed, 29 deselected, 2 warnings in 0.38s

$ .venv/bin/python -m pytest -q --tb=short
1382 passed, 2 warnings in 14.93s

$ cd frontend && npx tsc --noEmit
(빈 출력, exit 0)

$ cd frontend && npx vitest run
Test Files  7 passed (7)
     Tests  80 passed (80)
```

오늘까지 통과하던 46건 + Task 1이 더한 1건 = 47건(두 시험 파일 기준), 저장소 전체 1382건(4차 검증 기준 1378 + 12.3-09가 더한 3 + 이 계획이 더한 1) 전부 초록. 5차 검증이 통과로 확인한 여섯 경계(#4·#15·#16·#17·#20·#21)도 개별 재확인해 안 깨진 것을 확인했다.

## 스코프 경계 확인

- `git diff --name-only -- frontend src/gptrpg/rules_core src/gptrpg/event_log src/gptrpg/session_actor src/gptrpg/web/routes_events.py` → 빈 출력(이 다섯 자리를 한 줄도 안 고쳤다)
- `git diff --name-only -- src/gptrpg` → `src/gptrpg/web/creation_state.py`, `src/gptrpg/web/routes_creation.py` 정확히 두 줄
- `git diff -U0 -- src/gptrpg/web/routes_creation.py`의 `+`로 시작하는 줄이 전부 주석(`#`)이다 — 이 파일에서 실행되는 코드는 0줄 변경.
- `.venv/bin/python -c "from gptrpg.web import creation_state as c; import inspect; s=inspect.getsource(c.forfeited_nomination_mark); print('#after:' in s, 'forfeited_nominee(' in s)"` → `True True` — 표시 모양이 12.3-09와 같고, 판정 갱신 부수효과 호출이 그대로 남아 있다.
- 바꾼 파일 셋(`creation_state.py`·`routes_creation.py`·`tests/test_creation_screen_state.py`)에 TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커 0건.

## Next Phase Readiness

- 5차 검증이 발견한 새 CR-01(회복 장치 자체가 옛 기록 재사용을 새로 연 결함)이 다른 참가자의 완성에 기대지 않는 시험으로 못박힌 채 닫혔다.
- CHAR-06("사람이 브라우저만으로 처음부터 끝까지")이 서 있는 공개 HTTP 경로(`POST /creation/nominate`)의 응답 계약이 이제 특정 조건에서도 거짓이 되지 않는다.
- 남은 것(이 계획 범위 밖, 다음 검증까지 그대로 이어감):
  - 동의 관문 교차 전파의 화면 쪽 절반(사람 확인, QA 도구 구조적 제약으로 자동화 불가)
  - 회복 시간 5분(`NOMINATION_IDLE_S`)의 실제 체감(5차 검증이 「이제 이 확인이 처음으로 의미를 갖는다」고 적었고, 이 계획이 마지막 코드 결함을 닫아 다음 라운드에서 실제로 해볼 수 있다)
  - ROADMAP.md가 명시한 확인 관문 — 실제 브라우저로 캐릭터 만들기를 처음부터 끝까지 화면 조작만으로 한 번 통과
  - 서버 재시작이 남기는 좁은 창(계획서 `planner_assumptions ③ ㉮`에 알고 남긴 한계로 명시 — 화면은 이 창에 안 닿고, 다음 흘려보냄 판정이 스스로 낫는다)
  - `12.3-05-SUMMARY.md`의 14개 체크리스트(범위 밖으로 유지)
- 다음 단계: `/gsd-verify-work`로 Phase 12.3 전체(10개 계획)를 재검증.

## Self-Check: PASSED

- FOUND: src/gptrpg/web/creation_state.py
- FOUND: src/gptrpg/web/routes_creation.py
- FOUND: tests/test_creation_screen_state.py
- FOUND: .planning/phases/12.3-creation-screen/12.3-10-SUMMARY.md
- FOUND commit: 72344e4 (Task 1)
- FOUND commit: 9579a04 (Task 2)

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-22*
