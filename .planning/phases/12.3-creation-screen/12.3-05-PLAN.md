---
phase: 12.3-creation-screen
plan: 05
type: execute
wave: 4
depends_on: ["12.3-01", "12.3-02", "12.3-03", "12.3-04"]
files_modified:
  - frontend/src/panes/CreationPane.tsx
  - frontend/src/screens/CreationScreen.tsx
  - frontend/src/session/creationView.ts
  - frontend/src/session/creationView.test.ts
  - frontend/src/config.ts
  - frontend/src/labels.ts
  - src/gptrpg/cli/turn_flow.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/12.3-creation-screen/COVERAGE.md
autonomous: true
requirements: [CHAR-06]

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "방장이 이번 판의 인원을 화면에서 정하고, 방장이 아닌 사람에게는 그 조작이 안 보인다 (D-11)"
    - "방장이 창을 닫아도 다른 사람이 이어받아 인원을 정할 수 있다 — 방이 영영 멈추지 않는다 (D-11)"
    - "동의 관문에서 「누가 아직 안 눌렀는지」가 이름까지 보인다 — 숫자만 보이지 않는다 (D-03)"
    - "「아니요」를 고르면 어느 항목을 다시 열지 화면에서 고를 수 있고, 그 뒤 GM이 다시 정리하고 다시 묻는다 (D-11)"
    - "서버가 거절하면 서버가 보낸 이유가 그대로 보인다 — 화면이 옮겨 적은 문구가 아니다 (D-15)"
    - "전원 동의로 명단이 잠기면 자기 캐릭터로 세션 화면에 들어간다 — 이 단계는 거기서 끝난다"
    - "`CHAR-06`이 `REQUIREMENTS.md`와 `ROADMAP.md` 두 곳에 실제로 존재한다"
    - statement: "실제 브라우저에서 캐릭터 만들기를 처음부터 끝까지 진행한다 — 안내 → 값 확정 → GM 정리 → 동의 → 점유까지 화면 조작만으로 끝난다"
      verification: backstop
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/12.3-creation-screen/COVERAGE.md
    - frontend/src/panes/CreationPane.tsx
  key_links:
    - "`POST /creation/host` 주기 호출 → `you_are_host` → 인원 확정 조작이 방장에게만 보인다"
    - "`GameStateView.creation_characters[].consented` → 아직 안 누른 사람의 `display_name`"
    - "전원 동의 → 서버가 같은 요청에서 `LockPartyRoster` → `party_roster` → 세션 화면 전환"
---

<objective>
**만들기의 처음과 끝을 채워 브라우저 한 대로 캐릭터 만들기가 완주되게 한다.**

12.3-04가 가운데(값 채우기)를 만들었다. 남은 것은 앞뒤 두 개다:
- **앞**: 인원을 정하는 사람이 누구인가 — 방장이 화면에서 인원을 확정한다(D-11)
- **뒤**: 다 만든 뒤의 동의 관문 — 누가 아직 안 눌렀는지 이름까지 보이고, 「아니요」로 항목 하나를 다시 열 수 있고, 전원 동의로 명단이 잠기면 세션 화면으로 들어간다(D-03/D-11)

그리고 이 단계가 만들기로 한 요구사항 번호를 실제로 만든다 — `CHAR-06`이 `REQUIREMENTS.md`와 `ROADMAP.md` 두 곳에 없으면 이 단계가 무엇을 닫았는지 아무 데도 안 적힌다.

**여기서 끝난다.** 동의 직후의 첫 장면(오프닝)은 Phase 13이다. 이 계획이 하는 마지막 일은 **세션 화면에 들어가는 것**이다.

Purpose: 12.1의 검증이 남긴 유일한 사람 확인 항목을 실제로 통과 가능하게 만든다.
Output: 방장 인원 확정, 동의 관문, 세션 진입, 요구사항 번호, 그리고 확인 관문.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.3-creation-screen/12.3-CONTEXT.md
@.planning/phases/12.3-creation-screen/COVERAGE.md
@.planning/phases/12.3-creation-screen/12.3-03-SUMMARY.md
@.planning/phases/12.3-creation-screen/12.3-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 방장이 화면에서 인원을 정한다 (D-11)</name>
  <files>frontend/src/screens/CreationScreen.tsx, frontend/src/panes/CreationPane.tsx, frontend/src/session/creationView.ts, frontend/src/session/creationView.test.ts, frontend/src/labels.ts</files>
  <read_first>
    - `.planning/phases/12.3-creation-screen/12.3-03-SUMMARY.md` — `POST /creation/host`의 정확한 응답 칸과 `HOST_IDLE_S` 값
    - `frontend/src/api/client.ts`의 `claimCreationHost`·`fixPartySize`(12.3-02/03이 만든 것)
    - `frontend/src/session/usePolling.ts:120-147` — `POLL_INTERVAL_MS`(1.5초)와 `useEffect`의 타이머 정리 방식. 방장 신호 주기를 여기에 맞춰 정한다
    - `src/gptrpg/rules_core/rulebook.py`의 `PartySizeRange` · `src/gptrpg/rulebooks/dungeonworld_like.py`의 `DUNGEONWORLD_PARTY_SIZE_RANGE`(3~5) · `cairn.py`·`openquest.py`의 같은 값 — 화면이 고를 수 있는 범위의 출처
    - `src/gptrpg/agents/context.py`의 `PARTY_MEMBER_LIMIT` — 절대 안전 상한(서버가 400으로 막는다)
    - `frontend/src/screens/CreationScreen.tsx` — 12.3-01/04이 만든 화면
  </read_first>
  <action>
    ① `CreationScreen`이 **방장 신호를 주기적으로 보낸다.** `useEffect` 안에 `window.setInterval`을 두고 `claimCreationHost(sessionId, getBrowserId(sessionId))`를 부른다. 주기는 `HOST_IDLE_S`(30초)의 절반인 **15초** — 한 번 놓쳐도 유휴로 오판되지 않는다. 이 계산을 상수 옆 주석에 적는다(`HOST_BEACON_MS`를 `frontend/src/config.ts`에 둔다). 마운트 직후에도 한 번 부른다. 언마운트에서 타이머를 정리한다.
       **`party_roster`가 채워지면 신호를 멈춘다** — 명단이 잠긴 뒤에는 방장 개념이 쓸모없고, 서버도 그때는 아무 사건을 안 낸다.
       응답의 `you_are_host`를 상태로 들고 있는다. **이 값을 화면이 스스로 추측하지 않는다** — 폴링의 `creation_host_claimed`는 「누군가 잡았다」만 알려주고 「내가인가」는 이 응답만 안다.

    ② 인원이 아직 확정되지 않았을 때(`state.party_size_fixed === null`) 화면이 갈린다:
       - **`you_are_host`가 참이면**: 인원을 고르는 조작을 보인다. 룰북 권장 범위를 `fetchCreationSteps`와 **같은 방식으로 서버에서 받아 쓰지 않는다** — `GET /creation/steps`는 항목 선언만 내려주므로 범위가 없다. 그래서 **범위를 화면에 하드코딩하지 않고**, 사람이 숫자를 고르면 `fixPartySize`를 부르고 서버가 범위 밖이면 409 + `detail`로 거절한 문장을 그대로 보여준다(D-15). 서버가 이미 룰북 범위(`validate_party_size`)와 절대 상한(`PARTY_MEMBER_LIMIT`) 둘을 검사하므로 화면이 세 번째 검사를 만들지 않는다.
       - **`you_are_host`가 거짓이고 `creation_host_claimed`가 참이면**: `COPY.creationWaitingForHost`(「방을 연 사람이 인원을 정하는 중이에요」)를 보인다. 조작을 그리지 않는다.
       - **둘 다 거짓이면**(아직 아무도 안 잡았다): `COPY.loading`을 보인다. 다음 신호가 곧 방장을 정한다.
       - **방장이 사라져 내가 이어받으면** `you_are_host`가 참으로 바뀌고 조작이 나타난다. 화면이 따로 안내를 띄우지 않는다 — 조작이 생기는 것이 곧 안내다. 사람이 못 알아채는 것이 문제가 아니라, 아무도 못 하는 상태로 멈추는 것이 문제였다.

    ③ `creationView.ts`에 `partySizeGate(state, youAreHost)` 순수 함수를 더하고 시험을 쓴다. 네 값 중 하나를 돌려준다: `"fix"`(내가 방장, 내가 정한다) · `"waiting_for_host"` · `"waiting_for_claim"` · `"done"`(이미 확정됨). 시험이 네 갈래를 전부 덮는다.

    ④ `labels.ts`에 `creationPartySizeTitle` · `creationPartySizeConfirm` · `creationWaitingForHost` · `creationHostTookOver`를 더한다.
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `partySizeGate`가 네 갈래를 돌려주는 시험이 전부 통과한다
    - `grep -c 'HOST_BEACON_MS' frontend/src/config.ts` 가 `1`이고 그 값이 `HOST_IDLE_S`의 절반 이하다
    - `CreationScreen.tsx`가 `party_roster`가 채워지면 방장 신호 타이머를 멈추는 것이 소스에서 확인된다
    - 인원 범위 숫자(예: `3`·`5`)가 `CreationScreen.tsx`/`CreationPane.tsx`에 하드코딩되어 있지 않다 — 판정은 서버가 한다
    - `npm --prefix frontend run build` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>방장이 화면에서 인원을 정하고, 방장이 사라지면 다른 사람에게 그 조작이 나타난다.</done>
</task>

<task type="auto">
  <name>Task 2: 동의 관문과 세션 진입 (D-03/D-11/D-15)</name>
  <files>frontend/src/panes/CreationPane.tsx, frontend/src/screens/CreationScreen.tsx, frontend/src/session/creationView.ts, frontend/src/session/creationView.test.ts, frontend/src/labels.ts</files>
  <read_first>
    - `.planning/phases/12.3-creation-screen/12.3-03-SUMMARY.md` — `POST /creation/consent`의 요청·응답 칸과 「아니요」의 동작(앞서 받은 동의 전부 무효)
    - `src/gptrpg/web/routes_creation.py`의 `record_creation_consent` 도크스트링 — `agree=false`면 `step_id`가 **필수**이고, 이 경로에 「명단에서 사람을 뺀다」가 없다는 경계
    - `frontend/src/api/types.ts`의 `GameStateView.creation_characters`(`display_name`·`consented`)와 `party_roster`
    - `frontend/src/api/client.ts`의 `recordCreationConsent`·`wrapUpCreation`
    - `frontend/src/App.tsx` — 12.3-01이 고친 `Gate`. 명단이 잠긴 뒤 세션 화면으로 넘어가는 자리
    - `frontend/src/screens/CharacterSelect.tsx`가 있던 자리의 `err.detail` 패턴(12.3-01 SUMMARY가 어디로 옮겼는지 적어 두었다)
  </read_first>
  <action>
    ① **정리 단계**: 아직 안 끝난 사람이 없으면(`state.creation_unfinished_character_ids`가 비었고 `creation_characters`가 비어 있지 않으면) 「진행자에게 정리를 부탁하기」 버튼이 `wrapUpCreation`을 부른다. GM의 정리 문장은 폴링의 `creation_gm_spoke`(`kind="wrap_up"`)로 대화 줄기에 들어오고, 캐릭터별 한 줄 소개는 `character_created` 사건에서 나온다. 넷이 동시에 눌러도 서버가 한 번만 부른다(D-12) — 화면이 따로 잠그지 않는다.

    ② **동의 관문**: 정리가 끝나면(`kind="wrap_up"`인 GM 말이 기록에 있으면) 관문을 보인다.
       - `state.creation_characters`를 **전부** 줄로 그린다. 각 줄에 `display_name`과 「동의함 / 아직」 표시. **이름이 보인다**(D-03) — 「3명 중 2명 동의」 같은 숫자만 보여주지 않는다. 실제 테이블에서 서로 책상을 둘러보는 것과 같아야 하기 때문이다.
       - 내 줄에 버튼 두 개: 「이대로 시작」(`agree=true`)과 「고칠 게 있어요」(`agree=false`).
       - 「고칠 게 있어요」를 누르면 **어느 항목을 다시 열지 고르는 목록**을 보인다 — 내 `stepRows`에서 하나를 골라 `step_id`를 실어 보낸다. `step_id` 없이 `agree=false`를 보내지 않는다(서버가 400으로 거절한다).
       - 「아니요」가 성공하면 서버 응답의 `message`를 그대로 보여준다(D-15) — 그 문장이 이미 「이 항목이 다시 열렸다 — 다시 채우고 나면 GM이 다시 정리해서 다시 묻는다」를 한국어로 말하고 있다. 화면이 같은 말을 옮겨 적지 않는다.
       - **앞서 받은 동의가 전부 무효가 된다**는 것은 폴링의 `creation_characters[].consented`가 전부 거짓으로 돌아오는 것으로 자연히 보인다 — 화면이 따로 알림을 만들지 않는다.
       - 관문 전체에 `creationErrorMessage`를 건다: 403이면 서버가 보낸 「캐릭터를 다시 선택해 주세요」가 그대로 보인다.

    ③ **명단 잠금과 세션 진입**: `state.party_roster`가 `null`이 아니게 되면 화면이 세션으로 넘어간다. `App.tsx`의 `Gate`를 다시 조회하지 말고, `CreationScreen`이 `onEntered(characterId)` 콜백으로 `App`에 알려 `{ kind: "ready", characterId }`로 바꾼다 — `completeCreation`이 이미 쿠키를 구워 두었으므로 새 조회가 필요 없다.
       **명단이 잠겼는데 내 캐릭터가 없으면**(늦게 들어온 브라우저) 12.3-01이 만든 `COPY.creationRosterLocked` 안내로 간다. 그 문장이 「명단이 잠겨 이번 판에는 들어갈 수 없다」와 그 이유를 말한다 — 막다른 길을 말없이 두지 않는다.
       **여기서 멈춘다.** 세션 화면에 들어간 뒤의 첫 장면은 Phase 13이다.

    ④ `creationView.ts`에 `consentGate(state)` 순수 함수를 더한다: `"not_ready"`(아직 만드는 사람이 있다) · `"needs_wrap_up"` · `"open"`(관문이 열렸다) · `"locked"`. 그리고 `pendingConsenters(state)`가 `consented`가 거짓인 캐릭터의 `display_name` 목록을 돌려준다. 시험이 네 갈래와 이름 목록을 전부 덮는다. **어느 함수도 사건을 접지 않는다** — `state`를 읽을 뿐이다.

    ⑤ `labels.ts`에 `creationAskWrapUp` · `creationConsentTitle` · `creationConsentYes` · `creationConsentNo` · `creationConsentPending` · `creationConsentDone` · `creationPickStepToReopen`을 더한다. **서버가 보낸 거절 사유와 재진행 안내(`ConsentResponse.message`)는 여기에 넣지 않는다**(D-15).
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
    <automated>.venv/bin/python -m pytest -q tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `consentGate`와 `pendingConsenters`의 시험이 전부 통과하고, `pendingConsenters`가 `character_id`가 아니라 `display_name`을 돌려주는 것을 단언한다 (D-03)
    - `agree=false`를 보낼 때 `step_id`가 항상 함께 실리는 것이 소스에서 확인된다
    - `ConsentResponse.message`를 화면이 그대로 렌더링한다 — 같은 뜻의 문구가 `labels.ts`에 새로 추가되지 않았다
    - `CreationScreen`이 `party_roster`가 채워지면 `onEntered`를 부르고 `App.tsx`가 `{ kind: "ready" }`로 바꾸는 것이 소스에서 확인된다
    - 이 계획에서 오프닝 장면·첫 서사를 만드는 코드가 추가되지 않았다 (Phase 13 범위)
    - `npm --prefix frontend run build` 와 `npm --prefix frontend test` 가 종료 코드 0이다
  </acceptance_criteria>
  <done>동의 관문이 이름까지 보여주고, 「아니요」로 항목 하나를 다시 열 수 있고, 전원 동의로 명단이 잠기면 자기 캐릭터로 세션 화면에 들어간다.</done>
</task>

<task type="auto">
  <name>Task 3: CHAR-06을 만들고 CLI 예외 근거를 코드에 갱신한다</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, src/gptrpg/cli/turn_flow.py, .planning/phases/12.3-creation-screen/COVERAGE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` 의 CHAR 절(CHAR-01~05, 112~116행 부근) — 항목 형식과 어조
    - `.planning/ROADMAP.md` §Phase 12.3 — `**Requirements**: TBD (논의 전 — ...)` 줄과 `**Plans:** 0 plans` 줄, 그리고 `Plans:` 아래 `- [ ] TBD ...` 줄
    - `src/gptrpg/cli/turn_flow.py:250-270` — 12.1이 남긴 「캐릭터 만들기를 명령줄에 넣지 않는다」 주석
    - `.planning/phases/12.3-creation-screen/COVERAGE.md` — 계획 시점에 이미 적어 둔 선언들
  </read_first>
  <action>
    ① `.planning/REQUIREMENTS.md`의 CHAR 절 끝(CHAR-05 다음)에 항목 하나를 더한다. 기존 항목과 같은 형식(`- [ ] **CHAR-06**: ...`)을 쓰고 **아직 체크하지 않는다** — 이 요구사항은 사람 확인(Task 4)이 통과해야 닫힌다. 문장은 12.3-CONTEXT.md가 정한 그대로:

       `사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 — 안내 → 값 확정 → GM 정리 → 동의 → 점유까지 화면 조작만으로 끝난다.`

       그 아래에 한 줄 근거를 붙인다: 이 문장이 Phase 12.1의 검증이 남긴 유일한 사람 확인 항목을 그대로 옮긴 것이고, 12.1은 화면이 없어 그 시험을 수행할 수 없었으며(12.1의 성공 조건 다섯이 화면을 요구하지 않았다) 그래서 Phase 12.3이 그것을 이어받았다는 것.

    ② `.planning/ROADMAP.md` §Phase 12.3의 **`Requirements` 줄 하나만** 바꾼다:
       `**Requirements**: TBD (논의 전 — ...)` -> `**Requirements**: CHAR-06`
       `**Plans:**` 개수와 `Plans:` 목록 다섯 줄은 **계획 시점에 이미 채워졌다** — 값이 실제 파일과 맞는지 읽어서 확인만 하고, 어긋나 있으면 그때만 고친다.
       **`**Goal**` 줄과 「왜 새로 생겼나」 블록은 손대지 않는다** — 이미 실제 내용이 들어 있고 이 단계의 구속력 있는 범위다. **`Write`가 아니라 `Edit`으로 그 한 줄만 바꾼다** — 통째로 다시 쓰면 다른 단계 항목이 전부 날아간다.

    ③ `src/gptrpg/cli/turn_flow.py:250-270`의 주석에 한 문단을 덧붙인다. 지우지 말고 이어 쓴다:
       Phase 12.3이 캐릭터 만들기를 **브라우저에서** 끝까지 되게 만들었고, 그 과정에서 세 개념(동의 기록·방장·GM 지목)이 더해졌지만 **셋 다 캐릭터 신원과 브라우저 신원 위에 서 있어서** 명령줄에는 여전히 앉을 자리가 없다는 것. 그래서 12.1의 예외 판단이 12.3에서도 그대로 유지되고, 그것을 바꾸려면 명령줄에 신원 개념을 먼저 만들어야 하며 그것은 계정이 생기는 다음 마일스톤의 몫이라는 것.

    ④ `.planning/phases/12.3-creation-screen/COVERAGE.md`의 §Spec-less probe fallback에 한 줄을 갱신한다: `CHAR-06`이 이제 실제로 존재하므로, 다음에 이 단계를 다시 검증할 때는 그 번호로 탐침을 돌릴 수 있다는 것. 나머지 절은 손대지 않는다.
  </action>
  <verify>
    <automated>grep -c 'CHAR-06' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <automated>.venv/bin/python -m pytest -q tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CHAR-06' .planning/REQUIREMENTS.md` 가 `1` 이상이고 `grep -c 'CHAR-06' .planning/ROADMAP.md` 가 `1` 이상이다
    - `.planning/ROADMAP.md`에 `Requirements**: TBD` 문자열이 Phase 12.3 절에 더 이상 없다
    - `.planning/ROADMAP.md` §Phase 12.3의 `Plans:` 목록에 다섯 줄이 있고 각 줄이 실제 파일 이름을 가리킨다
    - `.planning/ROADMAP.md`의 다른 단계(Phase 12.2·13 등) 항목이 이 커밋에서 변경되지 않았다 (`git diff`로 확인)
    - `src/gptrpg/cli/turn_flow.py`의 기존 예외 주석이 지워지지 않고 문단이 **덧붙여졌다**
    - `.venv/bin/python -m pytest -q tests/` 전체가 통과한다 (주석만 고쳤으므로 회귀가 없어야 한다)
  </acceptance_criteria>
  <done>이 단계가 무엇을 닫는지가 요구사항 번호로 존재하고, CLI 예외 판단의 근거가 코드에 최신 상태로 남았다.</done>
</task>

<task type="auto">
  <name>Task 4: 확인 관문 — 실제 브라우저에서 처음부터 끝까지 만들어 본다</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 12.3 「이관된 확인 항목」 — 이 문장이 이 단계의 확인 관문이다
    - `.planning/phases/12.3-creation-screen/12.3-CONTEXT.md` §요구사항 — CHAR-06의 문장
  </read_first>
  <action>
    실행자가 **사람이 확인할 환경을 먼저 갖춘다** — 사람에게 명령을 치게 하지 않는다.
    ① 개발 서버를 배경으로 띄우고(`uv run uvicorn gptrpg.web.app:app --reload` 또는 저장소가 쓰는 실행 방법) 준비될 때까지 기다린다.
    ② `npm --prefix frontend run dev`를 배경으로 띄우고 포트를 잡는다.
    ③ 새 세션 식별자 하나를 정해 두 개의 링크 문구(`http://{host}/?session={id}`)를 `12.3-05-SUMMARY.md`에 적어, 사람이 복사만 하면 되게 한다.
    ④ 아래 `<human-check>`의 14개 항목을 그대로 `12.3-05-SUMMARY.md`에 체크 목록으로 옮겨 둔다. 이 목록이 `CHAR-06`과 로드맵의 이관된 확인 항목을 그대로 편 것이다.
    ⑤ **`CHAR-06`을 여기서 체크하지 않는다** — 사람이 14개 결과를 보고한 뒤에만 `- [x]`로 바꾼다. 하나라도 실패하면 체크하지 않고 실패 내용을 항목 번호와 함께 남긴다. 그것이 `/gsd-plan-phase 12.3 --gaps`의 입력이 된다.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/ -x</automated>
    <automated>npm --prefix frontend test</automated>
    <human-check>
      **준비:** 서버를 띄우고(`uv run uvicorn gptrpg.web.app:app --reload` 또는 저장소가 쓰는 실행 방법) 브라우저 탭 **두 개**로 같은 링크(`http://{host}/?session={새 세션 id}`)를 연다. 두 번째 탭은 다른 브라우저 프로필이나 사생활 보호 창을 쓴다 — 같은 브라우저의 두 탭은 `localStorage`를 공유해 같은 사람이 된다.

      **확인할 것 (전부 화면 조작만으로):**
      1. 링크를 열면 캐릭터 고르기 화면을 **거치지 않고** 만들기 화면이 뜬다 (D-10)
      2. 먼저 연 탭에만 인원을 정하는 조작이 보인다. 나중 탭에는 「방을 연 사람이 정하는 중」이 보인다 (D-11)
      3. 인원을 정한 뒤 GM 안내가 **두 탭 모두에** 나타난다 (D-02) — 부른 탭에만 뜨면 실패다
      4. 두 탭에서 각각 항목을 채운다. 자유 서술 · 목록에서 고르기 · 숫자 배치(던전월드) 또는 주사위 굴리기(케언)를 최소 한 번씩 해 본다
      5. 주사위 항목에서 **판정에서 보던 것과 같은 주사위 연출**이 뜬다 (D-07)
      6. 남의 차례에도 **말하는 입력이 열려 있고**, 항목을 확정하는 조작은 자기 차례에만 있다 (D-08)
      7. 확정한 항목 옆의 「고치기」로 **앞선 항목**(마지막 것 말고)을 다시 열어 값을 바꿀 수 있다 (D-09)
      8. 전원이 끝나면 GM 정리와 한 줄 소개가 두 탭 모두에 뜬다
      9. 동의 관문에 **아직 안 누른 사람의 이름이 보인다** — 숫자만 보이면 실패다 (D-03)
      10. 한쪽에서 「고칠 게 있어요」로 항목 하나를 다시 열면, 다른 탭의 동의 표시도 함께 초기화된다 (D-11)
      11. 다시 채우고 다시 정리하고 **전원 동의**하면 두 탭 모두 자기 캐릭터로 **세션 화면에 들어간다**
      12. 새로고침해도 지난 GM 말이 다시 「말하지」 않고 주사위가 다시 굴러가지 않는다 (첫 응답 거르기)
      13. **서버를 껐다 켠 뒤에도** 모인 동의가 그대로 남아 있다 (D-03) — 만들기 중간에 한 번 시도해 본다
      14. `agents.json`을 잘못된 제공자로 바꾸고 GM을 부르면 「진행자를 쓸 수 없는 상태입니다」가 뜬다. 서버를 끄면 끊김 띠가 뜬다. 두 문장이 **다르다** (D-13 ②③)

      **통과 기준:** 위 14개 전부. 이 목록이 CHAR-06과 로드맵의 이관된 확인 항목을 그대로 편 것이다.
      실패한 항목은 번호와 함께 그대로 적는다 — 「대체로 됐다」로 넘기지 않는다.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 개발 서버와 프론트엔드가 실제로 떠 있고, 사람이 복사만 하면 되는 링크 두 개가 `12.3-05-SUMMARY.md`에 적혔다
    - 14개 항목이 `12.3-05-SUMMARY.md`에 번호가 붙은 체크 목록으로 옮겨졌다
    - `.venv/bin/python -m pytest -q tests/` 와 `npm --prefix frontend test` 가 전부 통과한다 — 사람에게 깨진 것을 확인시키지 않는다
    - `.planning/REQUIREMENTS.md`의 `CHAR-06`이 이 시점에는 아직 `- [ ]`다 (사람 보고 뒤에만 `- [x]`가 된다)
  </acceptance_criteria>
  <done>사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행했고, 그 결과가 항목별로 기록됐다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (12.3-05)

**새 순수 함수** (`frontend/src/session/creationView.ts`): `partySizeGate` · `consentGate` · `pendingConsenters`
**새 상수** (`frontend/src/config.ts`): `HOST_BEACON_MS`
**새 프롭** (`frontend/src/screens/CreationScreen.tsx`): `onEntered(characterId)`
**새 label 키** (`frontend/src/labels.ts`): `creationPartySizeTitle` · `creationPartySizeConfirm` · `creationWaitingForHost` · `creationHostTookOver` · `creationAskWrapUp` · `creationConsentTitle` · `creationConsentYes` · `creationConsentNo` · `creationConsentPending` · `creationConsentDone` · `creationPickStepToReopen`
**새 요구사항 번호:** `CHAR-06` (`.planning/REQUIREMENTS.md` CHAR 절 · `.planning/ROADMAP.md` §Phase 12.3)
**갱신되는 문서:** `.planning/ROADMAP.md` §Phase 12.3의 `Requirements`/`Plans` 자리표시자 · `src/gptrpg/cli/turn_flow.py`의 CLI 예외 주석 · `.planning/phases/12.3-creation-screen/COVERAGE.md`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `POST /creation/party-size` | 한 번만 가능한 조작. 방장이 아닌 사람이 부르면 안 된다 |
| 브라우저 → `POST /creation/consent` | 남의 동의를 대신 누르거나, 남의 항목을 다시 열게 만들 수 있는 자리 |
| 브라우저 → `POST /creation/host` (주기 신호) | 15초마다 도는 호출. 브라우저가 스스로 켜고 끈다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-11 | Elevation of Privilege | 인원 확정 조작 | high | mitigate | 화면이 방장에게만 조작을 보이는 것은 **안내**이고, 실제 관문은 12.3-03이 `fix_party_size`에 건 403이다. 화면을 우회해 직접 부르면 서버가 막는다. Task 1의 승인 기준이 범위 숫자를 화면에 하드코딩하지 않았음을 확인해, 화면이 판정자 행세를 하지 않게 한다 |
| T-12.3-02 | Tampering | 동의 관문 | high | mitigate | 동의는 서명 쿠키 신원으로만 통과한다(12.3-03이 유지한 `identity is None or ...` -> 403). 화면은 자기 줄에만 버튼을 그리지만, 남의 줄에 버튼을 억지로 만들어도 서버가 거절하고 그 `detail`이 그대로 보인다 |
| T-12.3-18 | Tampering | 「고칠 게 있어요」의 `step_id` | medium | mitigate | 화면은 **내 `stepRows`에서만** 고르게 한다. 남의 `step_id`를 보내도 서버의 `_prepare_reopen`이 `character_id`로 판정하므로 남의 항목을 열 수 없다 — 화면이 보내는 `character_id`는 쿠키와 대조된다 |
| T-12.3-19 | Denial of Service | 15초 방장 신호 | medium | mitigate | 명단이 잠기면 타이머를 멈추고, 서버는 방장이 안 바뀌면 사건을 내지 않는다. 사건이 쌓이지 않고 요청만 15초마다 한 번 — 1.5초 폴링보다 열 배 드물다 |
| T-12.3-20 | Repudiation | 사람 확인 결과 | medium | mitigate | Task 4가 실패 항목을 번호로 적게 강제하고, 하나라도 실패하면 `CHAR-06`을 체크하지 못하게 한다. 「대체로 됐다」로 요구사항이 닫히는 것이 이 저장소의 알려진 실패 모양이다 |

**공급망:** 새 패키지 설치 없음 — `T-{phase}-SC` 항목 없음.
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` 전체가 통과한다
- `npm --prefix frontend test` 와 `npm --prefix frontend run build` 가 통과한다
- `.venv/bin/python -m ruff check src tests` · `.venv/bin/python -m lint_imports` 가 통과한다
- 사람 확인(Task 4) 14개 항목의 결과가 항목별로 기록됐다
</verification>

<success_criteria>
1. 방장이 화면에서 인원을 정하고, 방장이 사라져도 방이 안 멈춘다
2. 동의 관문이 아직 안 누른 사람의 **이름**을 보여준다
3. 「아니요」로 항목 하나를 다시 열 수 있고 서버가 보낸 안내가 그대로 보인다
4. 전원 동의로 명단이 잠기면 자기 캐릭터로 세션 화면에 들어간다 — 그리고 거기서 멈춘다
5. `CHAR-06`이 `REQUIREMENTS.md`와 `ROADMAP.md` 두 곳에 존재한다
6. 사람이 실제 브라우저에서 처음부터 끝까지 만들어 보고 그 결과가 항목별로 기록됐다
</success_criteria>

<output>
Create `.planning/phases/12.3-creation-screen/12.3-05-SUMMARY.md` when done
</output>
