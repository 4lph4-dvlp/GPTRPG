---
phase: 12.3-creation-screen
plan: 11
subsystem: ui
tags: [react, vite, secure-context, error-boundary, headless-chromium, gap-closure]

requires:
  - phase: 12.3-creation-screen (12.3-10)
    provides: 캐릭터 만들기 화면 전체 흐름(인원 확정 → 항목 선언 → 정리 → 동의 → 완성), 5차 검증 CR-01 닫힘
provides:
  - "루프백이 아닌 주소(평문 http + 일반 IP)로 화면이 실제로 뜬다는 것을 진짜 헤드리스 크로미움으로 확인하는 반복 가능한 확인 경로(scripts/check_insecure_origin.py)"
  - "안전한 맥락 전용 API(crypto.randomUUID) 의존을 제거한 브라우저 식별자 생성"
  - "마운트·렌더 도중 예외가 빈 화면 대신 사람이 읽을 문장으로 남는 앱 최상단 오류 경계"
  - "인원 확정 입력에 0명을 구조적으로 못 보내는 화면 가드(WR-02 닫힘)"
affects: [12.3-creation-screen (사람 확인/UAT), 13.1-narrative-lead, 실험 준비 절차]

actuals:
  tokens: 9875
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "브라우저 API를 새로 부를 때 「안전한 맥락 없이도 있는가」를 실측하고 docstring에 근거를 남긴다(갈래 추가가 아니라 호출 교체)"
    - "확인 스크립트의 확인 불가(2)와 통과(0)를 절대 안 섞는 종료 코드 계약 — 파이썬 표준 라이브러리 + 이 기계에 이미 있는 헤드리스 브라우저만 쓰고 새 의존성을 안 더한다"
    - "React 최상단 오류 경계는 기존 전체 화면 안내(Notices.tsx)의 클래스 골격을 그대로 재사용한다(Phase 16 시각 설계 경계를 안 건드림)"

key-files:
  created:
    - scripts/check_insecure_origin.py
    - .planning/phases/12.3-creation-screen/deferred-items.md
  modified:
    - frontend/src/session/browserIdentity.ts
    - frontend/src/session/browserIdentity.test.ts
    - frontend/src/screens/Notices.tsx
    - frontend/src/main.tsx
    - frontend/src/labels.ts
    - frontend/src/screens/CreationScreen.tsx
    - README.md

key-decisions:
  - "crypto.randomUUID() → crypto.getRandomValues 기반 newOpaqueId()로 갈아 끼웠다(갈래 추가 아님) — 실측(크로미움 151)으로 평문 http + LAN 주소에서도 getRandomValues는 있다는 것을 확인했고, 갈래를 두면 이번 결함과 같은 「특정 환경에서만 도는 줄」 함정을 한 자리 더 만들기 때문"
  - "확인 스크립트의 확인-불가 경로마다 sys.exit(2)를 직접 호출한다 — return 2로 버블링만 시키면 「확인 불가를 통과와 절대 안 섞는다」는 성질이 코드로 안 드러나 acceptance criteria(주석 아닌 실제 sys.exit(2) 호출 카운트)를 못 지킨다"
  - "WR-02(0명 입력 차단)를 이 계획에 접어 넣었다 — 같은 파일, 같은 증상 가족(「사람이 화면만 보고 원인을 알 수 없다」), 두 줄 수리라 잠긴 결정을 안 건드림. WR-01(끼어들기 노출 규칙)은 D-08의 표면을 건드리므로 사람이 고를 몫으로 명시적으로 미룸"
  - "오류 경계 동작 확인 시 App.tsx에 무조건 throw를 넣었더니 `npm run build`의 tsc 단계가 그 아래 코드의 타입 좁히기를 잃어 실패했다 — 임시 확인 한 번에 한해 `npx vite build`(esbuild, 타입 미검사)로 대체했다(Rule 3 blocking-issue 자동 수정). 최종 상태는 App.tsx가 원상 복구된 뒤 `npm run build`(tsc 포함)로 다시 확인해 통과했다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "루프백이 아닌 주소(평문 http + 일반 IP)로 헤드리스 크로미움이 화면을 열어 '캐릭터 만들기'를 실제로 읽는다 — 소스 변경 전에는 실패(exit=1, 빈 #app)했다는 기록과 나란히"
    requirement: CHAR-06
    verification:
      - kind: e2e
        ref: ".venv/bin/python scripts/check_insecure_origin.py (headless chromium, non-loopback IPv4)"
        status: pass
      - kind: unit
        ref: "frontend/src/session/browserIdentity.test.ts#안전한 맥락이 아닌 브라우저(crypto.randomUUID 없음)에서도 식별자를 만들고 예외를 안 던진다"
        status: pass
    human_judgment: false
  - id: D2
    description: "마운트 도중 예외가 나면 빈 화면 대신 제목·안내·실제 오류 문장이 남는다(앱 최상단 ErrorBoundary) — 실제로 예외를 일으켜 헤드리스 브라우저로 확인"
    verification:
      - kind: e2e
        ref: "App.tsx에 임시 throw를 넣고 돌린 check_insecure_origin.py 출력 — #app 안에 '화면을 여는 중에 문제가 생겼어요' + '오류 경계 확인용 임시 예외' 확인, 이후 되돌림(git diff -- frontend/src/App.tsx 빈 출력)"
        status: pass
    human_judgment: false
  - id: D3
    description: "인원 확정 입력에 0명을 구조적으로 못 보낸다(WR-02) + README 사전 점검 한 줄 + 저장소 전체 회귀 통과"
    requirement: CHAR-06
    verification:
      - kind: unit
        ref: "frontend/src/screens/CreationScreen.tsx grep 단언 (busy || count < 1, Math.max(1)"
        status: pass
      - kind: integration
        ref: "cd frontend && npx tsc --noEmit && npm run build && npx vitest run (81 passed)"
        status: pass
      - kind: integration
        ref: ".venv/bin/python -m pytest -q --tb=short (1382 passed, 재실행 기준)"
        status: pass
    human_judgment: false
  - id: D4
    description: "화면 끝에서 끝까지(안내 → 값 확정 → GM 정리 → 동의 → 점유)를 실제 별도 기기 넷으로 걸어 보는 것 — 12.3-UAT.md 테스트 2~8"
    human_judgment: true
    rationale: "이 계획은 첫 화면이 뜨는가까지만 자동으로 확인한다. 여덟 단계 흐름과 동의 교차 전파의 화면 쪽 절반은 실제 별도 기기가 있어야 확인 가능하고, 이 계획이 그 확인의 전제(화면이 뜬다)를 처음으로 만들었을 뿐이다 — 사람이 직접 걸어야 안다."

duration: 46min
completed: 2026-08-22
status: complete
---

# Phase 12.3 Plan 11: 참가자 링크로 화면이 실제로 뜨게 만든 secure-context gap 닫기 Summary

**`crypto.randomUUID()`(안전한 맥락 전용)를 `crypto.getRandomValues` 기반 생성으로 교체하고, 루프백 아닌 주소에서 헤드리스 크로미움으로 렌더를 확인하는 `scripts/check_insecure_origin.py`를 신설해, 다섯 라운드 내내 한 번도 실행되지 않았던 실패 경로를 처음으로 밟았다.**

## Performance

- **Duration:** 46 min
- **Started:** 2026-08-22T09:58:00Z
- **Completed:** 2026-08-22T10:44:00Z
- **Tasks:** 3
- **Files modified:** 9 (2 created, 7 modified)

## Accomplishments

- **RED로 확인한 실제 결함.** `.venv/bin/python scripts/check_insecure_origin.py`를 소스 변경 전에 돌려 종료 코드 `1`과 빈 `<div id="app"></div>`를 확보했다(이 기계의 `192.168.0.20`, 대조군 `127.0.0.1`은 정상 렌더 — 안전한 맥락 결함임을 확진).
- **생성 호출을 갈아 끼워 GREEN.** `browserIdentity.ts`의 `getBrowserId`가 이제 `crypto.getRandomValues` 기반 `newOpaqueId()`를 부른다(32글자 16진수). 저장 키·`pc-` 접두어·`getOrCreate`의 읽기→기억→생성 순서는 무변경 — 이미 값을 가진 브라우저는 영향을 안 받는다. 확인 스크립트가 이제 종료 코드 `0`.
- **앱 최상단 오류 경계.** `Notices.tsx`에 `ErrorBoundary` 클래스 컴포넌트를 더하고 `main.tsx`에서 `<App/>`을 감쌌다 — 마운트 중 예외가 나면 빈 화면 대신 안내 문구 + 실제 예외 문장이 남고 `console.error`로도 흔적이 남는다. `App.tsx`에 임시 예외를 넣어 실제로 동작을 확인한 뒤 되돌렸다.
- **WR-02 닫힘.** `PartySizeControl`의 입력 보정(`Math.max(1, Math.trunc(...) || 1)`)과 확정 단추 잠금(`disabled={busy || count < 1}`)으로 「0명」 요청이 구조적으로 서버에 안 간다.
- **README 사전 점검.** §실험 당일 실행 절차 A에 `check_insecure_origin.py` 한 줄과 왜 `127.0.0.1` 확인으로 대신할 수 없는지 설명을 더했다. 뒤따르는 항목 번호(3~20)와 두 교차 참조를 함께 갱신했다.
- **확인 경로가 확인 불가를 통과로 안 바꾼다.** `--host 127.0.0.1`을 주면 종료 코드 `2`(자체 거절). 실제 코드에 `sys.exit(2)` 호출 지점 5곳(주석 제외).

## Task Commits

1. **Task 1: 루프백이 아닌 진짜 주소로 화면을 열어 빨강을 확인하고, 식별자 생성 한 줄을 갈아 끼워 초록으로 바꾼다** - `7ddbb81` (feat)
2. **Task 2: 마운트 도중 무엇이 터져도 빈 화면 대신 사람이 읽을 문장이 남는다 — 앱 최상단 오류 경계** - `b85e296` (feat)
3. **Task 3: 「0명」을 보낼 수 없게 막고(WR-02), 이 확인을 진행자 절차에 붙이고, 전체 회귀를 돌린다** - `4ef8276` (fix)

**Plan metadata:** (다음 커밋 — 이 SUMMARY와 STATE/ROADMAP/REQUIREMENTS)

## RED/GREEN 실행 기록 (Task 1 — 필수 증거)

### 소스 변경 전 (RED, 종료 코드 1)

```
실패 — 192.168.0.20(루프백 아님, 참가자 링크와 같은 형태)에서 화면이 그려지지 않았다. #app 내용:
<div id="app"></div>


</body>
[대조군] http://127.0.0.1:8973/ (루프백, 안전한 맥락) → 통과 — 캐릭터 만들기 찾음
[본 검사] http://192.168.0.20:8973/ (루프백 아님, 참가자가 여는 형태) → 실패
exit=1
```

### 소스 변경 후 (GREEN, 종료 코드 0)

```
[대조군] http://127.0.0.1:8973/ (루프백, 안전한 맥락) → 통과 — 캐릭터 만들기 찾음
[본 검사] http://192.168.0.20:8973/ (루프백 아님, 참가자가 여는 형태) → 통과 — 캐릭터 만들기 찾음
통과 — 192.168.0.20에서 '캐릭터 만들기'가 실제로 그려졌다.
exit=0
```

### 루프백 거절 (`--host 127.0.0.1`, 종료 코드 2 — 확인 불가를 통과로 안 바꾼다)

```
'127.0.0.1'는 루프백 주소다 — 이 확인은 루프백으로는 원리적으로 성립하지 않는다(안전한 맥락이 항상 참이 되어 실패 경로가 실행되지 않는다). 종료 코드 2.
exit=2
```

### 단위 시험 (RED → GREEN)

- RED: `안전한 맥락이 아닌 브라우저(crypto.randomUUID 없음)…` 시험이 `TypeError: crypto.randomUUID is not a function`으로 실패(소스 변경 전).
- GREEN: 소스 변경 후 `npx vitest run src/session/browserIdentity.test.ts` → **6 passed**(기존 5 + 새 1건).

## 오류 경계 동작 확인 기록 (Task 2)

`frontend/src/App.tsx`의 `App` 함수 첫 줄에 `throw new Error("오류 경계 확인용 임시 예외");`를 임시로 넣고 돌린 확인의 출력(`#app` 내용, 대조군·본 검사 둘 다 경계가 잡음 — 둘 다 무조건 던지므로 실패로 보이지만 **빈 화면이 아니라는 것**이 확인 목적이다):

```
대조군(127.0.0.1)조차 실패했다 — 이것은 안전한 맥락 결함이 아니라 서버나 빌드가 통째로 망가진 것이다. #app 내용:
<div id="app"><div class="screen"><div class="screen__inner"><div class="screen__title"><h1 class="t-display">화면을 여는 중에 문제가 생겼어요</h1></div><div class="notice"><p class="t-body">이 브라우저에서 화면을 그리지 못했어요. 진행자에게 아래 문장을 그대로 알려 주세요.</p><p class="t-label"><code>오류 경계 확인용 임시 예외</code></p></div></div></div></div>
[대조군] http://127.0.0.1:8973/ (루프백, 안전한 맥락) → 실패
[본 검사] http://192.168.0.20:8973/ (루프백 아님, 참가자가 여는 형태) → 실패
exit=1
```

확인 뒤 임시 예외를 지웠다 — `git diff --name-only -- frontend/src/App.tsx`가 빈 출력. 되돌린 뒤 `npm run build`(tsc 포함) → `check_insecure_origin.py` 재확인 → 종료 코드 `0`.

## 전체 회귀 (Task 3)

- `cd frontend && npx tsc --noEmit` — 빈 출력.
- `cd frontend && npm run build` — 성공.
- `cd frontend && npx vitest run` — **81 passed**(오늘 80건 + Task 1이 더한 1건).
- `.venv/bin/python -m pytest -q --tb=short` — 재실행 기준 **1382 passed**(1건 flaky, 아래 참조).
- `.venv/bin/python scripts/check_insecure_origin.py` — 종료 코드 `0`.
- `git diff --name-only -- src/gptrpg` — 빈 출력(서버 코드 무변경).
- `git diff --name-only -- src/gptrpg frontend/src/panes` — 빈 출력.

## Files Created/Modified

- `scripts/check_insecure_origin.py` — 신설. 루프백 아닌 IPv4를 스스로 찾아 화면을 재빌드하고 임시 서버를 띄운 뒤 헤드리스 크로미움으로 실제 렌더를 확인. 종료 코드 0/1/2 계약.
- `frontend/src/session/browserIdentity.ts` — `getBrowserId`의 생성 콜백을 `crypto.getRandomValues` 기반 `newOpaqueId()`로 교체. 모듈·함수 docstring을 새 근거로 갱신.
- `frontend/src/session/browserIdentity.test.ts` — 안전한 맥락이 아닌 `crypto` 전역 대역 시험 1건 추가.
- `frontend/src/screens/Notices.tsx` — `ErrorBoundary` 클래스 컴포넌트 추가(기존 안내 골격 재사용).
- `frontend/src/main.tsx` — `StrictMode` 안쪽에 `ErrorBoundary`로 `App`을 감쌈.
- `frontend/src/labels.ts` — `appCrashedTitle`/`appCrashed` 문구 추가.
- `frontend/src/screens/CreationScreen.tsx` — `PartySizeControl`의 입력 보정 + 확정 단추 잠금(WR-02).
- `README.md` — §실험 당일 실행 절차 A에 사전 점검 한 줄, 항목 번호 재정렬.
- `.planning/phases/12.3-creation-screen/deferred-items.md` — 신설. flaky 시험 1건 기록(아래 참조).

## Decisions Made

frontmatter의 `key-decisions` 참조 — 요약하면: (1) 안전한 맥락 API를 「대체 갈래」가 아니라 「호출 교체」로 없앴다(실측 근거), (2) 확인 스크립트가 확인-불가 경로마다 명시적 `sys.exit(2)`를 부른다(코드로 드러나야 검증 가능), (3) WR-02는 이 계획에 접어 넣고 WR-01은 사람이 고를 몫으로 명시적으로 미뤘다, (4) 오류 경계 확인의 임시 예외 한 번에 한해 `npm run build` 대신 `npx vite build`를 썼다(아래 Deviations 참조, 최종 상태는 `npm run build`로 재확인).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 오류 경계 동작 확인 시 `npm run build`가 tsc 단계에서 실패**
- **Found during:** Task 2 ④(오류 경계 동작 확인 — 임시 예외 삽입 뒤 빌드)
- **Issue:** `App.tsx` 첫 줄에 무조건 `throw`를 넣자, TypeScript가 그 아래 코드(널 좁히기가 필요한 `sessionId`, `Gate` 판별 유니온)를 「도달 불가」로 취급하면서 원래는 통과하던 타입 좁히기가 사라져 `npx tsc`가 4개의 타입 오류를 냈다. `npm run build`는 `tsc && vite build`라 tsc가 실패하면 `vite build`가 아예 안 돌아 **`frontend/dist`가 재빌드되지 않고 옛 빌드로 남는다** — 이 계획이 경계하는 바로 그 침묵(`<planner_assumptions>` ⑦)과 같은 모양이 될 뻔했다.
- **Fix:** 이 한 번의 임시 확인에 한해 `npx vite build`(esbuild 트랜스파일만, 타입 미검사)로 대체해 `dist`를 실제로 갱신한 뒤 확인 스크립트를 돌렸다. 확인이 끝나자마자 임시 예외를 지우고 `npm run build`(tsc 포함) 정식 경로로 다시 빌드해 통과를 확인했다 — 최종 저장소 상태에는 이 대체가 전혀 안 남는다(커밋된 코드에 영향 없음, 검증 절차상의 일회성 우회일 뿐).
- **Files modified:** 없음(검증 절차만 조정, 커밋되는 파일 변경 없음)
- **Verification:** `git diff --name-only -- frontend/src/App.tsx`가 빈 출력, 최종 `npm run build` 성공, `check_insecure_origin.py` 종료 코드 0.
- **Committed in:** N/A(검증 단계, 코드 변경 없음)

**2. [Rule 1 - Bug] `componentDidCatch` 문자열이 acceptance criteria가 요구하는 정확히 1회를 넘어 docstring에도 나타남**
- **Found during:** Task 2 acceptance criteria 검증
- **Issue:** `Notices.tsx`의 `ErrorBoundary` 위 docstring이 실제 메서드 이름 `componentDidCatch`를 문장 안에 그대로 인용해, `grep -c 'componentDidCatch'`가 `2`(docstring 1 + 실제 메서드 1)로 나와 「정확히 1」 기준을 못 지켰다.
- **Fix:** docstring 문장을 "아래 예외 처리 훅이 …"로 바꿔 메서드 이름을 문자 그대로 반복하지 않게 했다. 실제 메서드 정의는 그대로.
- **Files modified:** `frontend/src/screens/Notices.tsx`
- **Verification:** `grep -c 'componentDidCatch' frontend/src/screens/Notices.tsx` → `1`.
- **Committed in:** `b85e296` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking-issue 검증 절차 우회, 1 acceptance-criteria 정합 수정)
**Impact on plan:** 둘 다 최종 산출물의 동작이나 성질을 안 바꾼다 — 검증 방법의 일회성 조정과 문서 문구 정합이다. 스코프 크리프 없음.

## Issues Encountered

- **Pre-existing flaky test (out of scope).** `.venv/bin/python -m pytest -q --tb=short`를 처음 돌렸을 때 `tests/test_clock_condition_web.py::test_condition_advance_segment_index_ignores_any_number_embedded_in_why` 1건이 실패했다 — 이벤트 타임스탬프 밀리초 필드가 우연히 `.999Z`로 끝나면(1000분의 1) `"999" not in json.dumps(...)` 단언이 깨지는 시간 의존 flaky 시험이다. 즉시 재실행하면 통과(`1 passed`), 전체 재실행도 `1382 passed`. `git diff --name-only -- src/gptrpg`가 이 계획 내내 빈 출력이므로 이 계획이 만든 결함이 아니다 — 고치지 않고 `.planning/phases/12.3-creation-screen/deferred-items.md`에 근거와 함께 남겼다(스코프 경계 규칙).

## User Setup Required

None - 외부 서비스 설정 불필요. 새 npm/pip/cargo 꾸러미도 하나도 안 더했다(확인 스크립트는 이 기계에 이미 있는 크로미움과 파이썬 표준 라이브러리만 쓴다).

## Next Phase Readiness

- **CHAR-06의 가장 앞 전제(화면이 뜬다)가 참이 됐고, 그 전제가 다시 조용히 거짓이 되는 길(확인 못 함 → 통과로 오인)을 스크립트가 구조적으로 막는다.** 이 확인을 진행자가 실험 당일 매번 돌릴 자리가 README에 생겼다.
- **`12.3-UAT.md` 테스트 2~8은 여전히 사람이 걸어야 안다.** 이 계획은 그 확인의 전제(다른 기기로 화면이 뜬다)를 처음으로 열었을 뿐이다 — 실제 기기 셋 이상으로 안내 → 값 확정 → GM 정리 → 동의 → 점유까지 화면 조작만으로 걸어 보는 사람 확인이 다음 관문이다.
- **동의 교차 전파의 화면 쪽 절반**(서버 쪽은 12.3-08 회귀 시험 + 6차 검증이 이미 확인)도 실제 별도 기기 둘로 처음 시도 가능해졌다 — 아직 시도되지 않았다.
- **WR-01(끼어들기 노출 규칙)은 이 계획이 의도적으로 미뤘다** — 위 사람 확인을 하는 동안 실제로 만져 보고 「안 보인다 / 못 누른다」 중 하나를 사람이 고른 뒤 별도 gap-closure로 닫을 것.
- **flaky 시험 1건**이 `deferred-items.md`에 남아 있다 — 다음에 `test_clock_condition_web.py`를 여는 계획이 참고할 것.

## Self-Check: PASSED

- 생성 파일 9개 전부 `[ -f ]`로 존재 확인.
- 세 Task 커밋 해시(`7ddbb81`, `b85e296`, `4ef8276`) 전부 `git log --oneline --all`에서 확인.

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-22*
