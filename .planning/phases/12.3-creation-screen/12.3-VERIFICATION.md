---
phase: 12.3-creation-screen
verified: 2026-08-24T01:57:29Z
status: gaps_found
score: 6/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 28/29
  gaps_closed:
    - "CHAR-06 조기 완료 표시 문제 — 12.3-VERIFICATION.md(2026-08-22)가 열어 둔 gap. 사람이 그 뒤 `12.3-UAT.md` 테스트 2~9·12·13을 실제 기기로 완주했고(9회차·13회차), REQUIREMENTS.md의 CHAR-06 `Complete` 표시는 그 인적 확인과 실제로 일치한다. 이번 검증에서 되돌리지 않는다"
  gaps_remaining: []
  regressions:
    - "12.3-15(2026-08-24)가 G-12.3-29(서사 칸 0px)를 닫으면서 **폰 가로(844×390)** 의 대화 칸을 240px→156px로 줄여 대화 조작부(전송칸·GM 제안 후보 버튼)를 더 밀어낸 것. 아래 gap #2 참조. **귀속 정정(orchestrator 실측):** 같은 잘림이 폰 세로(390×844)에서도 관측되지만 그쪽은 이번 변경이 만든 것이 아니다 — 옛 CSS(2f410f2)와 새 CSS로 각각 헤드리스 크로미움 측정을 돌린 결과 390×844의 대화 칸은 두 판 모두 337.6px이고 차지하는 상자도 506.4px~844px로 **완전히 동일**하다(옛: 상태 506.4 + 서사 0.0 + 대화 337.6 / 새: 상태 211.0 + 서사 295.4 + 대화 337.6). 셋째 행이 `minmax(240px, 40dvh)`일 때 40dvh=337.6px > 240px라 바닥값이 애초에 구속력이 없었기 때문이다. 폰 세로의 조작부 잘림은 저장소 첫 커밋부터 있던 기존 결함이다"
gaps:
  - truth: "세 칸 바닥의 합이 화면 높이를 넘어 전체가 넘치면 그물(scripts/check_narrow_viewport.py)이 잡는다 — 12.3-15-PLAN.md의 must_have #2, '넘침을 고쳐서 만든 통과는 통과가 아니다'"
    status: failed
    reason: |
      `scripts/check_narrow_viewport.py:217`이 재는 `scrollHeight`는
      `iframe.contentDocument.documentElement.scrollHeight`다. 그런데
      `frontend/src/styles.css:143-148`의 `.shell`이 `height: 100dvh;
      overflow: hidden`이라, 자식이 아무리 넘쳐도 `.shell`의 렌더 박스
      자체는 절대 뷰포트 높이를 넘지 않는다 — `documentElement.scrollHeight`는
      **항상 정확히 뷰포트 높이**가 된다. 넘침 단언(judge() ②,
      `check_narrow_viewport.py:318-323`)은 원리적으로 발화할 수 없다.

      독립 재현(이 검증이 직접 실행): `.shell`의 셋째 행 바닥을
      `minmax(0, 40dvh)`에서 이 계획 자신이 "함정"이라고 이름 붙인
      `minmax(240px, 40dvh)`로, 둘째 행을 `minmax(0, 1fr)`에서
      `minmax(240px, 1fr)`로 바꿔 돌리면(정확히 12.3-15-PLAN.md §187-191이
      기각했다고 적은 그 처방):

      ```
      844x390 · 상태 33.0px · 서사 240.0px · 대화 240.0px · 문서높이 390.0px · 통과
      ```

      세 행의 합(33+240+240=513px)이 뷰포트(390px)를 123px 넘겼는데도
      **통과(exit 0)**다. 확인 뒤 `frontend/src/styles.css`를 원본으로
      복원했고 `git status --short`가 비어 있음을 확인했다(작업 트리
      오염 없음).
    artifacts:
      - path: "scripts/check_narrow_viewport.py"
        issue: "judge()의 넘침 단언(②, 217행·318-323행)이 .shell{overflow:hidden}에 가려 절대 발화하지 않는다 — 12.3-REVIEW.md CR-01과 동일 결함, 이 검증이 독립적으로 재확인했다"
    missing:
      - "문서 scrollHeight가 아니라 .shell 자신의 넘침(scrollHeight vs clientHeight)이나 각 pane 내용의 실제 아래끝(getBoundingClientRect().bottom)을 뷰포트와 비교하는 단언으로 교체한다 (12.3-REVIEW.md CR-01이 구체적 패치를 제시했다)"
      - "고친 뒤 이 검증이 쓴 함정 CSS(둘째 행 minmax(240px,1fr) · 셋째 행 minmax(240px,40dvh))로 844×390을 다시 돌려 실패(exit 1)로 바뀌는지 확인 — 그것이 이 단언이 살아 있다는 유일한 증거다"

  - truth: "좁은 화면(폰)에서도 놀이 화면의 대화 조작부(전송칸·GM 제안 응답 버튼)가 화면 안에 있어 실제로 누를 수 있다 — 12.3-15가 「배치가 다시 무너지면 빨갛게 보인다」고 내세운 바로 그 화면인데 그물이 이것을 못 잡는다. 폰 가로(844×390)는 이번 변경이 악화시킨 것이고, 폰 세로(390×844)는 기존 결함이다(귀속 정정은 re_verification.regressions 참조)"
    status: failed
    reason: |
      12.3-15 Task 2가 세로 골격의 대화 행 바닥을 `minmax(240px, 40dvh)`에서
      `minmax(0, 40dvh)`로 낮추면서, 844×390(폰 가로)의 대화 칸 높이가
      240px → 156px로 줄었다. `.pane--chat`의 실제 자식은 `.chat__head`
      (고정 ~50px) · `.chat`(flex:1, min-height:0 — 줄어듦) ·
      `.composer`(flex 미지정 → 기본 flex-shrink는 되지만 min-height가
      auto라 내용 크기 밑으로는 안 줄어듦, GM 제안이 뜨면 그 안에
      `.proposal`까지 포함)의 세 층인데, `.pane`이 `overflow: hidden`이라
      `.composer`가 줄지 못하고 넘친 만큼은 스크롤 없이 그냥 잘린다.

      독립 재현(이 검증이 직접 실행, `ChatPane.tsx:265-300`의 실제 마크업을
      그대로 옮겨 헤드리스 크로미움으로 측정):

      ```
      844x390(폰 가로) · 뷰포트 390px
        GM 제안 후보 버튼 아래끝: 445px · 497px   (둘 다 화면 밖)
        전송 단추 아래끝:        610px            (화면 밖)
        composer 아래끝:         652px            (화면 밖)

      390x844(폰 세로) · 뷰포트 844px
        전송 단추 아래끝:        882.4px           (화면 밖 — 세로에서도 넘는다)
        composer 아래끝:         924.4px           (화면 밖)
        GM 제안 후보 버튼 아래끝: 717.4px · 769.4px (화면 안)
      ```

      **귀속 정정(orchestrator가 옛/새 CSS를 각각 실측):** 위 두 형태 중
      이번 변경이 **만든** 것은 폰 가로뿐이다. 폰 세로(390×844)의 대화 칸은
      옛 CSS(`minmax(240px, 40dvh)`)와 새 CSS(`minmax(0, 40dvh)`)에서
      **똑같이 337.6px**이고, 차지하는 상자도 506.4px~844px로 완전히 같다
      (옛: 상태 506.4 + 서사 0.0 + 대화 337.6 / 새: 상태 211.0 + 서사 295.4
      + 대화 337.6). 40dvh=337.6px가 240px보다 크므로 옛 판의 240px 바닥은
      애초에 구속력이 없었다. 따라서 폰 세로의 전송 단추 잘림은 저장소
      첫 커밋부터 있던 **기존 결함**이며, 12.3-15가 만든 것이 아니다.
      폰 가로(844×390)에서만 대화 칸이 240px → 156px로 줄어 실제로
      악화되었다. 다음 gap-closure 회차가 이 구분을 지켜야 한다 —
      「12.3-15를 되돌리면 낫는다」는 처방은 폰 세로에는 통하지 않고,
      되돌리면 서사 0px 결함(G-12.3-29)만 되살아난다.

      `.pane { overflow: hidden }`이라 스크롤로도 못 닿는다. 즉 폰
      가로에서는 GM 제안에 답할 버튼이 하나도 안 보이고, 폰 세로에서도
      제안이 뜬 상태에서는 전송 단추가 화면 밖으로 밀린다 — 이 화면의
      목적(주사위 굴릴 것을 고르고 행동을 보낸다)이 흔한 두 폰 형태 모두에서
      성립하지 않는다. `scripts/check_narrow_viewport.py`의 고정판은
      `.chat__head`·`.chat`·`.composer`·`.proposal`을 하나도 흉내 내지
      않으므로(REQUIRED_SOURCE_CLASS_NAMES에도 없음) 이 결함을 못 잡는다
      — 12.3-REVIEW.md CR-02·CR-03과 동일 결함, 이 검증이 독립적으로
      재확인했다.

      범위에 대한 판단: 이 결함이 사는 화면(`SessionScreen`/`ChatPane`)은
      엄밀히는 CHAR-06(캐릭터 만들기 자체)의 바깥이다 — `CreationScreen`은
      `.shell`의 고정 3행 그리드가 아니라 `min-height: 100dvh`의 자연스러운
      문서 스크롤(`.screen`/`.screen__inner`)을 쓰므로 같은 함정에 안
      걸린다(이 검증이 직접 확인). 그러나 이 결함은 **12.3-15 Task 2가
      이번 phase 안에서 직접 만든 것**이고, 12.3-15의 성공 조건 자체가
      "이 배치 결함이 다시 생기면 빨갛게 보인다"를 내세웠는데 정확히
      그 반대(새 배치 결함이 생겼는데 그물이 초록을 찍는다)가 벌어졌다.
    artifacts:
      - path: "frontend/src/styles.css"
        line: 196
        issue: "@media (max-width: 1080px)의 grid-template-rows 셋째 행 바닥이 240px→0으로 내려가면서, .composer/.proposal의 줄지 않는 내용이 짧은 화면에서 화면 밖으로 잘린다"
      - path: "frontend/src/styles.css"
        line: 151
        issue: ".pane { overflow: hidden }"이라 잘린 부분에 스크롤로 닿을 방법이 없다"
      - path: "scripts/check_narrow_viewport.py"
        issue: "고정판(fixture)이 .chat__head·.composer·.proposal을 안 담아 이 종류의 결함을 원리적으로 못 잡는다 — REQUIRED_SOURCE_CLASS_NAMES 감시 목록에도 없다"
    missing:
      - ".pane--chat 안에서 .composer(조작부)는 안 줄고, .chat(지난 말)만 줄게 만든다(flex:1 1 0; min-height:0) — 필요하면 .composer 자체에 overflow-y:auto로 내부 스크롤을 준다 (12.3-REVIEW.md CR-02가 구체적 패치를 제시했다)"
      - "고정판에 실제 .chat__head·.composer·.proposal 구조를 채우고, .composer 아래끝과 .proposal .btn 전부가 화면 안에 있다는 단언을 그물에 추가한다 (CR-03)"

deferred: []

behavior_unverified_items: []

human_verification:
  - test: "실제 폰으로 놀이 화면(세션 진입 후)을 열어 서사가 읽을 만한 크기로 보이는지 확인한다"
    expected: "35dvh 안팎으로 확보된 서사 칸의 글자 크기·줄 간격이 실제로 읽기에 답답하지 않다"
    why_human: "기계가 답하는 것은 「0이 아니다 · 세로의 4분의 1은 된다」까지다. 그 크기로 이야기를 읽을 만한지는 사람만 안다 (12.3-15-PLAN.md must_haves, verification: backstop)"
  - test: "만들기 중 자기 차례를 실제로 받은 판에서 폰으로 조작 칸 셋(능력치 배치 select · 점수 숫자칸 · 인원 숫자칸)을 눌러 본다"
    expected: "세 칸 모두 손가락으로 눌러 답답하지 않다"
    why_human: "이 계획이 만드는 단언은 「높이가 44px이다」까지만 답한다. 누르는 느낌은 사람만 안다 (12.3-15-PLAN.md must_haves, verification: backstop). 위 gap #2가 드러난 지금은 이 확인이 한층 더 중요하다 — 조작 칸 자체는 44px여도, 그 칸을 담은 조작부 전체가 화면 밖으로 잘려 있으면 애초에 「누를 화면」에 닿지 못한다"
---

# Phase 12.3: 캐릭터 만들기 화면 Verification Report

**Phase Goal:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 **브라우저만으로** 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다
**Verified:** 2026-08-24T01:57:29Z
**Status:** gaps_found
**Re-verification:** Yes — 이전 검증(2026-08-22T18:20:00Z, `passed`, 28/29)이 남긴 gap(CHAR-06 조기 완료 표시)은 그 뒤 사람의 실기기 완주로 실제로 닫혔다. 그러나 그 이후 진행된 재확인 UAT 회차(테스트 11)와 gap-closure 계획 12.3-15(2026-08-24)가 새 결함 셋(G-12.3-27·28·29)을 열었다 닫는 과정에서, 이 검증이 **새로운 회귀 하나**(대화 조작부가 좁은 화면에서 화면 밖으로 잘림)를 독립적으로 찾아냈다.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | 사람이 브라우저만으로 캐릭터 만들기를 안내→값 확정→GM 정리→동의→점유까지 처음부터 끝까지 진행할 수 있다 (CHAR-06, roadmap 확인 관문) | ✓ VERIFIED | `12.3-UAT.md` 테스트 1·2·3·4·5·6·7·8·9·12·13이 전부 `pass` — 사람이 2026-08-23~24 실제 기기(브라우저 3개+시크릿 모드 포함)로 완주. REQUIREMENTS.md:117/359가 이미 `Complete` — 이 검증에서 되돌리지 않음(원 설계 의도: 인적 확인 완료 전 조기완료 표시만 문제였고, 그 조건은 이제 충족됨) |
| 2 | 폰이나 좁은 창에서 서사 칸이 0px가 아니다 (G-12.3-29) | ✓ VERIFIED | 이 검증이 직접 `.venv/bin/python scripts/check_narrow_viewport.py` 실행 — `390×844→295.4px`, `960×1080→378px`, `844×390→136.5px`, `1440×900→900px(대조군)`. `git show 2f410f2^:frontend/src/styles.css`로 12.3-15 이전 CSS를 되돌려 같은 그물을 돌리면 세 형태 모두 `0px`로 재현되어(exit 1), 그물이 실제로 이 결함을 잡는다는 것도 확인 |
| 3 | 세 칸 바닥의 합이 화면 높이를 넘어 넘치면 그물이 잡는다 — "넘침을 고쳐서 만든 통과는 통과가 아니다" (12.3-15 must_have #2) | ✗ FAILED | 아래 Gaps 참조. `.shell{overflow:hidden}`이 `documentElement.scrollHeight`를 항상 뷰포트 높이로 못 박아 넘침 단언이 원리적으로 발화 불가 — 이 계획이 직접 "함정"이라 이름 붙인 CSS를 얹어 844×390에서 513px/390px 넘침에도 통과(exit 0)임을 독립 재현 |
| 4 | 만들기 조작 칸(select·input[number]) 44px 이상이 기계로 단언된다 (G-12.3-28) | ✓ VERIFIED | `judge()` ③(`check_narrow_viewport.py:326-337`)이 형태 조건 없이 매번 실행됨을 코드로 확인. 현재 코드에서 44px로 통과 |
| 5 | 내가 한 말의 본문이 남의 말과 갈리고, 그 변경이 만들기·놀이 화면 양쪽에 반영된다 (G-12.3-27) | ✓ VERIFIED | `styles.css:1093-1112`의 `.chat-line--mine`에 `background: rgba(201,162,39,0.1)` 확인. 같은 클래스를 `ChatPane.tsx`와 `CreationPane.tsx` 양쪽이 공유. 그물의 배경색 단언(390×844)도 통과 |
| 6 | 비-회귀: 넓은 창(1440×900)의 3컬럼 배치가 안 바뀐다 | ✓ VERIFIED | 이 검증의 그물 실행 결과 `1440x900 · 상태 900.0px · 서사 900.0px · 대화 900.0px` — 변경 전후 동일 |
| 7 | 비-회귀: `npm run build && npm test`·`pytest`가 통과하고, 새 npm/pip 패키지·`.tsx`·서버 코드 변경이 0건이다 | ✓ VERIFIED | 오케스트레이터가 이 검증 착수 전 독립적으로 재실행해 확인함(104 프론트 시험 · 1406 파이썬 시험 통과, 무관한 기존 실패 1건, `.tsx`/`src/gptrpg` 변경 0). 이 검증은 이 항목을 재실행하지 않고 그 결과를 받아들임 — 이번에 발견한 회귀(gap #2)는 레이아웃 결함이라 vitest(jsdom 없는 순수 함수 시험)로는 원리적으로 안 잡히는 종류이므로, 시험 통과 자체가 gap #2의 반증이 되지 않는다 |
| 8 | 좁은 화면(폰)에서도 놀이 화면의 대화 조작부(전송칸·GM 제안 응답 버튼)가 화면 안에 있어 실제로 누를 수 있다 | ✗ FAILED | 아래 Gaps 참조. 844×390에서 GM 제안 후보 버튼·전송 단추 전부가 뷰포트(390px) 밖(445~610px)에 있고 스크롤 불가. 390×844에서도 전송 단추(882.4px)가 뷰포트(844px) 밖으로 밀림 — 이 검증이 실제 `ChatPane.tsx` 마크업으로 헤드리스 크로미움을 직접 돌려 재현 |
| 9 | (backstop) 사람이 실제 폰으로 놀이 화면을 열어 서사가 읽을 만한 크기로 보이는지 확인한다 | ? 인적 확인 필요 | 아직 미실시 — `12.3-15-PLAN.md` must_haves에 `verification: backstop`으로 명시 |
| 10 | (backstop) 사람이 만들기 중 실제 차례를 받은 판에서 폰으로 조작 칸 셋을 눌러 본다 | ? 인적 확인 필요 | 아직 미실시. gap #2 발견으로 이 확인의 중요성이 커짐(조작 칸 자체는 44px여도 조작부 전체가 화면 밖으로 잘리는 사례가 이미 확인됐다) |

**Score:** 6/10 truths verified (2 failed, 2 human verification pending)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/check_narrow_viewport.py` | 진짜 헤드리스 크로미움으로 좁은 화면 배치를 재는 회귀 그물 | ⚠️ 부분 결함 | 존재·와이어링(README §A 항목 2에서 안내)은 확인되나, 넘침 단언(judge ②)이 구조적으로 죽어 있고(gap #1) 고정판이 `.chat__head`/`.composer`/`.proposal`을 담지 않아 gap #2 종류의 결함을 원리적으로 못 잡는다. 서사 높이 단언(judge ①)과 배경색 단언(judge ④)은 실제로 작동함을 되돌려 확인(원본 CSS→exit 1, 현재 CSS→exit 0) |
| `frontend/src/styles.css` | 세로 골격 재분배 + `.chat-line--mine` 본문 구분 | ⚠️ 절반 성공 | 서사 칸 0px 결함(G-12.3-29)과 본문 구분 결함(G-12.3-27)은 실제로 닫혔다. 그러나 같은 수정(Task 2, 대화 행 바닥 240px→0)이 새 회귀(gap #2)를 만들었다 |
| `README.md` | §A 항목 2에 새 사전 점검 명령 추가 | ✓ VERIFIED | `README.md:35`에 `.venv/bin/python scripts/check_narrow_viewport.py` 확인 |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `scripts/check_narrow_viewport.py` | `frontend/src/styles.css` | `file://` 절대 경로 링크 | ✓ WIRED | 이 검증이 실제로 실행해 확인 |
| `.chat-line--mine`(`styles.css:1093`) | `CreationPane.tsx`·`ChatPane.tsx` | 공유 클래스 | ✓ WIRED | 두 파일 모두 `chat-line--mine`를 사용 |
| `check_narrow_viewport.py` | `check_insecure_origin.py` | `CHROMIUM_CANDIDATES`/`find_chromium` import | ✓ WIRED | 이 검증의 독립 스크립트에서도 같은 import로 재사용 확인 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| 서사 칸이 0px가 아니다(현재 코드) | `.venv/bin/python scripts/check_narrow_viewport.py` | exit 0, 네 형태 전부 통과, 390×844→295.4px | ✓ PASS |
| 그물이 원래 결함을 잡는다(원복 CSS) | `git show 2f410f2^:frontend/src/styles.css`로 임시 교체 후 실행, 확인 뒤 복원 | exit 1, 세 형태 0px | ✓ PASS(그물이 살아 있음을 확인) |
| 그물이 함정 CSS(픽셀 바닥)의 넘침을 잡는다 | 위 gap #1 절차대로 임시 교체 후 실행, 확인 뒤 복원 | exit 0(513px/390px 넘침인데 통과) | ✗ FAIL — gap #1 |
| 좁은 화면에서 대화 조작부가 화면 안에 있다 | 이 검증이 작성한 독립 스크립트(실제 `ChatPane.tsx` 마크업 이식) | 844×390·390×844 모두 전송/후보 버튼 뷰포트 밖 | ✗ FAIL — gap #2 |
| 넓은 창 3컬럼 비회귀 | `.venv/bin/python scripts/check_narrow_viewport.py` 출력의 1440×900 행 | 900/900/900 (변경 전과 동일) | ✓ PASS |

작업 트리 정리: 위 세 절차 모두 `git status --short frontend/src/styles.css`가 빈 출력임을 확인한 뒤 원본으로 복원했다 — 이 검증이 코드베이스를 오염시키지 않았다.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| CHAR-06 | 12.3-01~15 (전부) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ✓ SATISFIED (기존 Complete 유지) | `12.3-UAT.md` 인적 확인 완료(테스트 1~9·12·13 pass). 이번 검증이 찾은 gap #1·#2는 CHAR-06의 문면(캐릭터 **만들기** 화면)이 아니라 12.3-15가 이 phase 안에서 추가로 손댄 **놀이 화면**(SessionScreen/ChatPane) 레이아웃 코드에서 발견됐다 — `CreationScreen`은 `.shell` 고정 그리드가 아니라 자연 스크롤 레이아웃(`.screen`)을 쓰므로 같은 함정에 안 걸림을 이 검증이 확인했다. CHAR-06 자체를 되돌릴 근거는 없다 — 다만 12.3-15가 "이 phase 안에서 닫는다"고 선택한 작업의 산출물이 자기 자신의 성공 조건(비-회귀·그물 보호)을 만족하지 못했으므로 gaps_found로 보고한다 |

**Orphaned requirements:** 없음 — `.planning/REQUIREMENTS.md`가 Phase 12.3에 매핑한 요구사항은 CHAR-06 하나뿐이고, 15개 계획 전부가 그것만 선언한다(grep으로 교차 확인).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `scripts/check_narrow_viewport.py` | 198-225 | `iframe.onload`가 `about:blank` 항해에도 한 번 더 불림 — 지금은 `statusEl`이 `null`이라 던지는 잡히지 않은 TypeError가 우연히 제어 흐름 노릇을 함(12.3-REVIEW.md WR-01) | ⚠️ Warning | 누군가 null 가드를 "견고화"로 넣으면 그물이 빈 문서를 재고 통과를 찍게 될 위험 |
| `scripts/check_narrow_viewport.py` | 93-102 | 1440×900 "대조군"이 좌우 폭을 전혀 안 잼 — 3컬럼이 무너져도 안 잡힘(WR-02) | ⚠️ Warning | 대조군이 대조 노릇을 못함. 이번 검증에서는 실제로 3컬럼이 안 무너졌으므로 이번 판정에는 영향 없음 |
| `scripts/check_narrow_viewport.py` | 91 | `MINE_LINE_COLOR_CHECK_SHAPE` 문자열이 `SHAPES`와 검증 없이 결합 — 형태 이름이 갈리면 조용히 스킵됨(WR-04) | ⚠️ Warning | 현재는 정상 작동 확인. 향후 SHAPES 리팩터 시 조용히 깨질 수 있음 |
| `frontend/src/styles.css` | 1112 | `padding-right: 10px` — 매직 넘버, 파일 자신의 "간격은 4의 배수" 규율과 어긋남(IN-04) | ℹ️ Info | 기능에 영향 없음 |

이 표는 12.3-REVIEW.md(2026-08-24 라운드)가 이미 상세히 진단한 것 중 이 검증이 골든패스(BLOCKER 둘)와 별개로 재확인 가치가 있다고 판단한 항목만 추렸다 — 전체 16건(critical 3·warning 7·info 6)은 `12.3-REVIEW.md` 참조.

### Human Verification Required

### 1. 실제 폰으로 서사 칸 크기가 읽을 만한지

**Test:** 실제 폰으로 놀이 화면(세션 진입 후)을 열어 서사 칸을 본다
**Expected:** 확보된 서사 칸(약 35dvh)의 글자 크기·줄 간격이 실제로 읽기에 답답하지 않다
**Why human:** 기계는 「0이 아니다·세로의 4분의 1은 된다」까지만 답한다 (12.3-15-PLAN.md, `verification: backstop`)

### 2. 실제 폰으로 만들기 조작 칸 셋을 눌러 보기

**Test:** 만들기 중 자기 차례를 실제로 받은 판에서 폰으로 능력치 배치 select·점수 숫자칸·인원 숫자칸을 눌러 본다
**Expected:** 세 칸 모두 손가락으로 눌러 답답하지 않다
**Why human:** 기계는 「높이가 44px다」까지만 답한다 (12.3-15-PLAN.md, `verification: backstop`). gap #2가 드러난 지금은 「조작부 전체가 화면 안에 있는가」도 함께 확인할 가치가 있다

### Gaps Summary

이번 검증은 두 갈래로 나뉜다.

**CHAR-06 본체(캐릭터 만들기 자체)는 그대로 완료 상태다.** UAT 아홉 회차가 실기기로
안내→값 확정→되묻기→GM 정리→동의→점유까지 전부 사람 눈으로 확인했고, REQUIREMENTS.md의
`Complete` 표시는 그 확인과 실제로 일치한다 — 되돌릴 근거가 없다.

**12.3-15(2026-08-24, 이 phase의 마지막 계획)가 "phase 범위 밖이지만 여기서 닫는다"고
선택해 손댄 놀이 화면(SessionScreen) 레이아웃 코드에서 새 결함이 발견됐다.** 12.3-REVIEW.md의
코드 검토(2026-08-24, BLOCKER 3건)가 먼저 지적했고, 이 검증이 두 핵심 주장(CR-01·CR-02)을
각각 독립적으로 재현해 확인했다:

1. **그물의 넘침 단언이 죽어 있다** — `.shell{overflow:hidden}`이 `scrollHeight`를 항상
   뷰포트 높이로 못 박아, "픽셀 바닥을 줘서 넘치게 만드는 처방을 그물이 거절한다"는
   12.3-15 자신의 must_have #2가 거짓이다. 이 계획이 스스로 "함정"이라 이름 붙인 CSS를
   얹어 844×390에서 513px 내용이 390px 뷰포트에 들어가는데도 통과함을 확인했다.
2. **바로 그 보호가 없는 상태에서 실제로 넘침이 생겼다** — 12.3-15 Task 2가 대화 행
   바닥을 240px에서 0으로 내리면서, 짧은 화면에서 `.composer`(전송칸)와 `.proposal`
   (GM 제안 버튼)이 화면 밖으로 잘린다. `.pane{overflow:hidden}`이라 스크롤도 안 된다.
   폰 가로(844×390)에서는 GM 제안에 답할 방법이 아예 없고, 폰 세로(390×844)에서도
   제안이 뜬 상태에서는 전송 버튼이 화면 밖으로 밀린다.

두 결함 모두 CHAR-06의 문면(캐릭터 만들기 화면)이 아니라 **그 다음 화면**(세션/놀이
화면)에서 발견됐다 — `CreationScreen`은 이 함정에 안 걸리는 다른 레이아웃(`.screen`,
자연 스크롤)을 쓴다는 것을 이 검증이 확인했다. 그러나 12.3-15 자신의 성공 조건이
"이 배치 결함이 다시 생기면 빨갛게 보인다"였는데 정확히 그 반대(새 결함이 생겼는데
그물이 초록을 찍는다)가 벌어졌으므로, 이 phase의 마지막 계획이 스스로 내건 약속을
지키지 못한 것으로 보고 `gaps_found`로 판정한다.

---

_Verified: 2026-08-24T01:57:29Z_
_Verifier: Claude (gsd-verifier)_
