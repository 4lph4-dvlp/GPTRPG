---
phase: 12.3-creation-screen
verified: 2026-08-21T21:30:00Z
status: gaps_found
score: 13/15 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/13
  gaps_closed:
    - "GM이 첫 자기소개 순서를 지목해 항목 확정 화면이 열린다 (D-06) — 12.3-06(재실 신호에 character_id를 얹는 갈래 ①)이 교착을 풀었고, 12.3-07이 실제 브라우저로 인원 확정 → 안내 → 지목 → 항목 확정 화면 → 세 사람 전원 완성까지 끝에서 끝으로 라이브 재현했다. 이번 3차 검증이 관련 코드(creation_state.py·routes_creation.py·creationView.ts·CreationPane.tsx)를 직접 읽어 그 배선이 실제로 존재함을 다시 확인했고, `tests/test_creation_screen_state.py` 23건을 직접 재실행해 통과를 확인했다."
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "재실 신호 상한이 「위조 방지」를 실제로 한다 — 유령(가짜) 캐릭터 식별자가 진짜 참가자를 지목 후보에서 밀어낼 수 없다 (T-12.3-17, present_candidates docstring 자체가 이 이름을 자처한다)"
    status: failed
    reason: "12.3-06이 첫 지목 교착을 푼 방법(재실 신호에 character_id를 얹는다, 갈래 ①)이 그 대가로 새 결함을 열었다. `ClaimHostRequest.character_id`(routes_creation.py:203)는 최소 길이도 없고 서명도 요구하지 않는 — 브라우저가 아무 문자열이나 자기 신고로 낼 수 있는 값이다. `present_candidates`(creation_state.py:84-117)는 이 신고값에 남은 자리 수만큼 상한을 두지만, 그 항목이 「진짜 참가자인가」는 전혀 확인하지 않는다 — 먼저 도착한 신호가 상한 안의 자리를 그대로 차지한다. 이번 검증이 소스를 하나도 안 고친 채 `present_candidates`를 직접 두 번 불러 재현했다: 정원 2인 세션에 유령 둘(ghost-1, ghost-2)을 먼저 심고 진짜 참가자 둘(real-1, real-2)을 뒤에 심으면, 후보 목록에 진짜 참가자가 단 한 명도 안 남는다(`('ghost-1', 'ghost-2')`만 나옴). 유령이 지목되면 `creation_current_speaker_id`가 그 유령의 식별자로 영구히 고정되고(routes_events.py의 `_creation_current_speaker_id`는 지목된 사람이 `created_characters`에 들어와야만 풀린다), 화면의 `shouldNominateNext`(creationView.ts:224)는 `creation_current_speaker_id !== null`이면 무조건 false를 돌려줘 재지목 자체가 다시는 안 일어난다. 이 계획이 원래 고치려던 교착(「아무도 지목 안 됨」)보다 더 나쁜, 회복 경로가 전혀 없는 교착(「엉뚱한 유령이 영원히 지목된 채 멈춤」)이 될 수 있다. `12.3-REVIEW.md`의 CR-01이 이미 정확히 같은 결함을 코드 읽기만으로 찾아냈고, 이번 검증이 그것을 독립적으로 코드 재실행까지 해서 확인했다."
    artifacts:
      - path: "src/gptrpg/web/creation_state.py:76-117 (present_candidates)"
        issue: "docstring이 상한을 「T-12.3-17, 위조 방지」라고 부르지만, 실제로 하는 일은 개수만 자르는 것이다 — 그 항목이 진짜 참가자인지는 어디에서도 확인 안 함"
      - path: "src/gptrpg/web/routes_creation.py:200-210 (ClaimHostRequest.character_id)"
        issue: "최소 길이도, 서명도, browser_id와의 대조도 없다 — 아무 문자열이나 신고하면 그대로 재실 표에 얹힌다"
      - path: "src/gptrpg/web/routes_events.py (_creation_current_speaker_id)"
        issue: "지목된 대상이 created_characters에 들어와야만 풀린다 — 유령은 절대 거기 못 들어가므로 영구히 안 풀림"
      - path: "frontend/src/session/creationView.ts:224 (shouldNominateNext)"
        issue: "creation_current_speaker_id가 null이 아니면 무조건 false — 유령이 지목된 뒤 재지목·시간초과 경로가 전혀 없다"
    missing:
      - "present_candidates의 상한이 개수뿐 아니라 신원도 지키게 한다 — 12.3-REVIEW.md CR-01이 제시한 세 갈래(문서 정정만으로는 부족·재지목/시간초과 경로 신설·선점 순서를 최신 신호 우선으로 변경) 중 하나 이상"
      - "지목된 사람이 HOST_IDLE_S 동안 CompleteCreationStep을 한 번도 안 내면 creation_current_speaker_id를 다시 null로 되돌리는 회복 경로"
      - "유령이 진짜 참가자를 실제로 밀어내는 시나리오(예: 정원 2에 유령 2 먼저 + 진짜 참가자 2가 후보에서 완전히 빠지는 경우)를 직접 확인하는 회귀 시험 — 이번 검증이 임시로 돌린 재현 스크립트를 정식 시험으로 옮긴다"
deferred: []
behavior_unverified_items:
  - truth: "이미 동의를 누른 다른 사람이 있는 상태에서, 한 사람의 「고칠 게 있어요」 재오픈이 그 다른 사람의 동의 표시도 함께 초기화해 그 사람의 다른 탭 화면에도 반영된다 (D-03/D-11, 교차 전파)"
    test: "실제로 서로 다른 브라우저(또는 사생활 보호 창) 둘 이상으로 같은 세션에 들어가, 한쪽이 동의를 누른 뒤 다른 쪽이 자기 항목을 재오픈했을 때 먼저 동의한 쪽의 화면에서 동의 표시가 다시 「안 누름」으로 바뀌는지 확인"
    expected: "동의를 이미 누른 사람의 화면도 다른 탭에서 발생한 재오픈에 따라 자동으로 동의 표시가 초기화된다"
    why_human: "12.3-07이 이 항목을 시도했지만 QA 도구(단일 크로미움 프로필)의 구조적 제약 — localStorage와 서명 쿠키가 프로필 전체에 전역 공유되고, 캐릭터 완성 시점마다 서버가 새 쿠키를 발급해 이전 캐릭터의 쿠키를 덮어쓴다 — 때문에 「두 사람이 동시에 각자 유효한 서명 쿠키를 갖는」 상태 자체를 못 만들었다. 실제 별도 브라우저가 아니면 재현이 안 된다(CDP로도 우회 불가 — browse 도구가 쿠키 값을 읽는 CDP 메서드를 의도적으로 막아 둠)"
coincidental_reliance_items: []
---

# Phase 12.3: 캐릭터 만들기 화면 Verification Report

**Phase Goal:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 브라우저만으로 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다.
**Verified:** 2026-08-21 (재검증, 3차)
**Status:** gaps_found
**Re-verification:** Yes — 2차 검증이 라이브로 찾은 「첫 지목이 절대 안 일어난다」는 gap이 12.3-06·12.3-07로 닫혔는지 확인하러 다시 열었다. 확인하는 과정에서 그 수정 자체가 새로 여는 결함(CR-01)을 코드 재실행으로 독립 확인했다.

## 결론 먼저

**2차가 찾은 「첫 지목 교착」은 진짜로 닫혔다.** 12.3-06이 재실 신호(`POST
/creation/host`)에 `character_id`를 함께 싣는 방법(사장님이 직접 고른 갈래 ①)으로
교착을 풀었고, 12.3-07이 실제 브라우저로 새 세션 하나를 처음부터 끝까지(인원 확정 →
GM 안내 → 첫 지목 → 항목 확정 화면 실제로 열림 → 세 사람 전원 완성 → 되묻기 →
동의 관문 이름 표시 → 방장 승계 → D-13 세 문구)까지 라이브로 밟았다 — 스크린샷·
HTTP 응답·사건 기록을 남겼다. 이번 3차 검증이 관련 소스(`creation_state.py`·
`routes_creation.py`·`routes_events.py`·`creationView.ts`·`CreationPane.tsx`)를
직접 읽어 그 배선이 실제로 존재함을 확인했고, `tests/test_creation_screen_state.py`
23건을 직접 재실행해 통과를 확인했다. 여기까지는 확실히 앞으로 나아간 것이다.

**하지만 그 교착을 푼 바로 그 수정이, 더 나쁜 새 결함을 열어 놓고 있다.**
`12.3-REVIEW.md`(코드 리뷰)가 짚은 CR-01을 이번 검증이 **소스를 한 글자도 안 고친 채,
`present_candidates` 함수를 직접 두 번 불러 재현**했다: 정원 2인 세션에 유령
캐릭터 둘(`ghost-1`, `ghost-2`)이 먼저 「나 여기 있다」 신호를 보내고 진짜 참가자
둘(`real-1`, `real-2`)이 뒤에 오면, 지목 후보 목록에 **진짜 참가자가 단 한 명도
안 남는다.** 유령이 지목되면 그 뒤로는 재지목도, 시간 초과도, 어떤 회복 경로도
코드에 없다 — 지목된 사람이 `created_characters`에 못 들어오는 한
`creation_current_speaker_id`가 영원히 그 유령을 가리키고, 화면은 영원히 그
누구의 차례도 아닌 채로 멈춘다. 이 함수의 docstring 스스로 이 상한을
「T-12.3-17, 위조 방지」라고 부르지만, 실제로 하는 일은 **개수만 자르는 것**이지
「그 항목이 진짜 참가자인가」는 전혀 안 본다.

**이것은 이론적 위험이 아니다** — 정상적인 사용만으로도 쉽게 일어난다. `myCharacterId`가
브라우저 `localStorage` 기반이라 같은 사람이 두 기기(휴대폰+노트북)나 사생활 보호
창을 열면 서로 다른 식별자를 갖고, 두 탭 모두 15초마다 재실 신호를 조용히 보낸다 —
실제로 만들 생각이 없는, 먼저 열어 봤다가 방치한 탭도 후보 자리를 채운다.

**목표는 아직 완전히는 성립하지 않는다.** CHAR-06이 요구하는 흐름(안내 → 값 확정
→ GM 정리 → 동의 → 점유)이 정상 경로로는 이번에 처음으로 끝까지 라이브로
성립했지만, 그 경로를 여는 장치 자체에 「사람이 브라우저만으로 끝까지 할 수
있다」를 뒤집을 수 있는 새 회복 불가능한 막다른 길이 남아 있다 — 이걸 그대로 두고
CHAR-06을 완료로 표시할 수 없다.

## 3차 검증(이번) 방법

1. `12.3-VERIFICATION.md`(2차본, 이번에 교체됨)를 읽어 2차가 남긴 실패 하나와
   막힌 다섯 항목을 파악했다.
2. `12.3-06-PLAN.md`/`-SUMMARY.md`, `12.3-07-PLAN.md`/`-SUMMARY.md`,
   `12.3-REVIEW.md`를 전부 읽어 「무엇을 고쳤다고 주장하는지」와 「무엇을 관찰했다고
   주장하는지」를 분리해 파악했다.
3. `12.3-REVIEW.md`가 짚은 CR-01을 관련 소스(`creation_state.py:34-140`,
   `routes_creation.py:190-270,750-840`, `routes_events.py`의
   `_creation_current_speaker_id`, `creationView.ts`의 `shouldNominateNext`,
   `CreationPane.tsx`의 `stepEditingEnabled`/`myTurn`)를 직접 읽어 대조했다 —
   리뷰의 주장을 그대로 믿지 않고 코드 자체에서 같은 결론이 나오는지 확인했다.
4. **CR-01을 소스 변경 없이 직접 재현**했다 — `present_candidates`를 순수 함수
   그대로 스크립트에서 두 번 호출해, 유령 신호가 진짜 참가자를 후보에서
   완전히 밀어내는 것을 확인했다(아래 §CR-01 재현 참조).
5. `.venv/bin/python -m pytest tests/test_creation_screen_state.py -q`(23건)를
   직접 재실행해 통과를 확인했다.
6. 이번 검증이 만든 변경 파일(TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커)을
   `creation_state.py`·`routes_creation.py`·`client.ts`·`CreationScreen.tsx`·
   `CreationPane.tsx`·`creationView.ts`·`test_creation_screen_state.py`에 대해
   재확인했다 — 0건.
7. 서버를 새로 띄우지 않았다 — 12.3-07이 이미 같은 흐름을 라이브 브라우저로
   충분히 밟았고, 그 증거(스크린샷·HTTP·사건 기록)가 SUMMARY에 구체적으로
   남아 있어 다시 반복할 필요가 없었다. 대신 그 SUMMARY의 주장을 코드 대조와
   재현 스크립트로 검산했다.

## 첫 지목 교착 — 진짜로 닫혔음을 확인

`12.3-06-SUMMARY.md`가 주장한 코드 변경을 직접 읽어 대조했다:

- `src/gptrpg/web/creation_state.py:84-117`의 `present_candidates`가 실제로
  존재하고, `unfinished_candidates`(사건 기반, 절대 안 잘림)를 앞에 놓고
  `_character_last_seen`(재실 기반)을 뒤에 붙이는 것을 확인했다.
- `src/gptrpg/web/routes_creation.py`의 `nominate_creation_speaker`가 실제로
  `creation_state.present_candidates(state, session_id)`를 부르고, 그 결과를
  `_gm_dedupe_key`에 인자로 넘기는 것을 확인했다(재계산 아님 — T-12.3-20 지킴).
- `frontend/src/session/creationView.ts:214-231`의 `shouldNominateNext`가
  실제로 존재하고, "안내됨 + 명단 안 잠김 + 인원 확정됨 + 지목 없음 + 완성
  인원 < 정원"일 때 true를 돌려주는 것을 확인했다 — 부트스트랩(아무도 항목을
  안 낸) 상태에서도 true다.
- `tests/test_creation_screen_state.py`를 직접 실행해 23건 전부 통과를
  확인했다(신규 시험 넷 — 부트스트랩 지목·다음 참가자 지목·상한 경계·빈 후보
  거절 — 포함).
- `12.3-07-SUMMARY.md`가 남긴 라이브 증거(스크린샷 3장, HTTP 상태 코드,
  `creation_gm_spoke(kind=nominate)` 사건 기록)는 이 코드 배선이 실제 AI 호출을
  거쳐 실제 화면에 반영됨을 보여준다 — 코드 대조와 라이브 증거가 서로 일치한다.

**결론: 2차가 찾은 실패(「GM이 첫 순서를 지목해 항목 확정 화면이 열린다」)는
이제 정상 사용 경로에서 성립한다.**

## 새로 확인한 결함 (3차) — CR-01, 재현으로 독립 확인

`12.3-REVIEW.md`의 CR-01은 코드를 읽기만 해서 찾은 것이었다. 이번 검증은 그
주장을 소스를 전혀 안 고친 채 **직접 재현**해 코드 읽기가 아니라 실행 결과로
확인했다.

```python
# src/gptrpg/web/creation_state.py의 present_candidates를 그대로 호출
sid = "cr01-poc-session"
creation_state.mark_character_present(sid, "ghost-1")   # 유령이 먼저 신고
creation_state.mark_character_present(sid, "ghost-2")
creation_state.mark_character_present(sid, "real-1")    # 진짜 참가자가 나중에
creation_state.mark_character_present(sid, "real-2")

state = GameState(session_id=sid, party_size_fixed=2)
present_candidates(state, sid)
# → ('ghost-1', 'ghost-2')
```

**정원 2인 세션에서 유령 둘이 상한 전부를 선점하고, 진짜 참가자 둘은 후보
목록에 단 한 명도 안 남는다.** `ClaimHostRequest.character_id`(`routes_creation.py:203`)는
최소 길이도, 서명도, `browser_id`와의 대조도 요구하지 않으므로 이 재현은
서버가 실제로 받아들이는 입력 범위 안이다.

**여기서 멈추지 않는 이유가 회복 경로의 부재다:**

- 유령이 지목되면 `creation_current_speaker_id`가 그 유령의 식별자로
  고정된다(`routes_events.py`의 `_creation_current_speaker_id`는 지목 대상이
  `state.created_characters`에 들어와야만 `None`으로 되돌아간다 — 유령은
  값을 낼 수 없으므로 절대 못 들어간다).
- `shouldNominateNext`(`creationView.ts:224`)는
  `state.creation_current_speaker_id !== null`이면 무조건 `false`를
  돌려준다 — 재지목 effect가 다시는 안 돈다.
- `stepEditingEnabled`(`CreationPane.tsx:442`)는 `myTurn`(지목된 사람 본인)일
  때만 열린다 — 유령은 브라우저가 없으므로 아무도 이 조작을 못 연다.

**결과: 이 계획이 닫으려던 교착(「아무도 지목 안 됨」)보다 더 나쁜 교착(「엉뚱한
유령이 영원히 지목된 채 아무도 못 풂」)이 실제로 코드 실행으로 재현된다.**
`12.3-REVIEW.md` CR-01이 코드 읽기로 찾은 결론과 이번 재현의 결과가 정확히
일치한다 — 리뷰 이후 어떤 커밋도 이 결함을 고치지 않았다(`git log`상
`d4e700e`가 마지막 커밋이고, 그 커밋은 REVIEW.md 문서 자체만 바꿨다).

**왜 이번 3차 이전에는 아무도 이걸 못 잡았나:**
- 12.3-06의 새 시험 넷은 전부 "정상 신호"만 준다(`test_nominate_candidates_never_exceed_the_remaining_party_size`도
  다섯 신호가 전부 같은 모양의 정상 신호다) — 신원 위조 경로를 시험하지
  않는다.
- 12.3-07의 라이브 재확인도 세 사람 전원이 정상적으로 참여하는 경로만
  밟았다 — 유령을 일부러 심어 보지 않았다.
- `12.3-REVIEW.md`는 코드를 읽고 CR-01을 정확히 짚었지만, 코드를 고치는
  것은 그 계획의 범위 밖이었다(리뷰 커밋 `d4e700e`는 문서만 바꿨다).

## Goal Achievement

### Observable Truths

| # | Truth | Status (2차) | Status (3차, 이번) | Evidence |
|---|-------|------|------|----------|
| 1 | 세션 링크를 열면 캐릭터 고르기 화면 없이 만들기 화면이 바로 뜬다 (D-10) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 2차 라이브 확인 유지, 이번 회귀 없음 |
| 2 | 방장이 화면에서 인원을 정할 수 있다 (D-11) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 2차 라이브 확인 유지 |
| 3 | GM의 안내·지목·되묻기·정리가 사건 기록에 남아 다른 탭도 폴링으로 본다 (D-02) | ✗ FAILED (부분, 지목부터 막힘) | ✓ **VERIFIED** | 12.3-07 라이브 — 안내·지목·되묻기·정리 전부 사건으로 기록되고 화면에 반영됨을 확인. 이번 검증은 관련 코드(`RecordGmSpoke`, `routes_events.py`의 폴링 노출)를 대조해 배선을 재확인했다 |
| 4 | 같은 시점 재호출에 AI를 다시 부르지 않는다 (D-12) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `_gm_dedupe_key`가 nominate 갈래에서 실제 후보 목록을 인자로 받는 것을 코드로 재확인(T-12.3-20) |
| 5 | 방장 브라우저 식별자가 폴링 events 배열에 새지 않는다 (T-12.3-05) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 12.3-06이 새로 더한 `character_id`도 응답·사건에 안 실림을 코드로 확인 |
| 6 | GM이 첫 자기소개 순서를 지목해 항목 확정 화면이 열린다. 두 번째·세 번째도 이어진다 (D-06) | ✗ FAILED (교착) | ✓ **VERIFIED (교착 해소)** | 12.3-07 라이브 — 세 사람 전원이 화면 조작만으로 순서대로 지목·완성됨. 이번 검증이 관련 코드를 직접 대조하고 시험 23건 재실행으로 확인 |
| 7 | 「안내」 버튼이 안내 전에 사라지지 않는다 (CR-02) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 8 | 주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ✓ VERIFIED (라이브, 최종 시각 판단은 사람 몫) | 12.3-07 — 케언 룰북으로 실제 굴림, DiceModal 스크린샷·사건 기록(`rolls` 칸에 실제 눈) 확보. 「판정 화면과 정말 같아 보이는가」는 아래 사람 확인 목록에 남긴다 |
| 9 | 남의 차례에도 말이 항상 열려 있고, 확정 조작만 자기 차례에 뜬다 (D-08) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ✓ VERIFIED (라이브) | 12.3-07 — 3번째 탭이 2번째 사람 차례 중 끼어들기 200으로 성공, 항목 확정 UI는 처음부터 안 보임을 확인 |
| 10 | 동의 관문이 이름까지 보여주고, 자기 항목을 「고칠 게 있어요」로 다시 열면 자기 동의가 초기화된다 (D-03) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ✓ VERIFIED (자기 자신 한정) | 12.3-07 — 세 캐릭터 전부 표시 이름으로 렌더링 확인, 재오픈 시 그 캐릭터 자신의 `consented`가 True→False로 되돌아감을 사건 기록으로 확인 |
| 11 | 다른 사람이 이미 동의한 상태에서, 한 사람의 재오픈이 그 다른 사람의 동의 표시도 함께 초기화하고 다른 탭에도 반영된다 (D-03/D-11, 교차 전파) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ **PRESENT_BEHAVIOR_UNVERIFIED (여전히 미도달)** | 12.3-07이 시도했으나 QA 도구(단일 크로미움 프로필)의 구조적 제약(쿠키·localStorage 전역 공유)으로 「두 사람이 동시에 각자 유효한 인증 상태」를 만들 수 없었다 — 사람이 실제 별도 브라우저로 확인해야 한다 |
| 12 | 방장이 조용해지면 다른 브라우저가 이어받는다 (D-11) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ✓ VERIFIED (라이브) | 12.3-07 — 36초 뒤 `creation_host_claimed(reason=succession)` 사건 기록, 화면에 승계 캡션 + 인원 확정 조작 노출 확인 |
| 13 | AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 다른 문구로 갈린다 (D-13 ①②③) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ✓ VERIFIED (문구 자체는 다름, 헷갈림 여부는 사람 판단) | 12.3-07 — ②는 `creation_gm`·`master_gm` 둘 다 비운 라이브 재현으로 503 문구 확인(1차 예측 정정 그대로), ③은 서버 종료 28초 뒤 배너 확인, ①은 서버 시험(`FakeProvider`)으로 200 폴백 문구 확인. 세 문구가 실제로 다른 문자열임은 확인됐으나 「헷갈리지 않는가」는 아래 사람 확인 목록에 남긴다 |
| 14 | `CHAR-06`이 `REQUIREMENTS.md`·`ROADMAP.md`에 존재한다 | ✓ VERIFIED | ✓ VERIFIED | `grep CHAR-06 REQUIREMENTS.md` 재확인 — 여전히 의도적으로 미체크(`- [ ]`), 이번 검증도 그 판단이 옳음을 확인했다 |
| 15 | **[신규]** 재실 후보 상한이 신원까지 검증해, 유령 캐릭터가 진짜 참가자를 지목 후보에서 밀어낼 수 없다 (T-12.3-17이 자처한 "위조 방지") | (해당 항목 없음 — 12.3-06이 처음 만듦) | ✗ **FAILED** | `present_candidates`를 소스 무변경으로 직접 재현 — 유령 둘이 정원 2를 전부 선점하고 진짜 참가자 둘이 후보에서 완전히 빠짐(§CR-01 재현 참조). 회복 경로(재지목·시간초과)가 코드 어디에도 없어, 이 결함이 실제로 발생하면 그 세션의 파티 전원이 영구히 막힌다 |

**Score:** 13/15 verified (1 present-but-behavior-unverified, 1 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/gptrpg/web/creation_state.py` (`present_candidates`, `mark_character_present`) | 재실 신호 기반 지목 후보 계산 | ⚠️ **HOLLOW (신원 검증 없음)** | 함수는 존재하고 배선도 되어 있으며 정상 경로에서는 작동한다(교착 해소 확인) — 그러나 docstring이 자처하는 "위조 방지"를 실제로 하지 않는다. 재현으로 확인 |
| `src/gptrpg/web/routes_creation.py` (`ClaimHostRequest`, `nominate_creation_speaker`) | 재실 신호 수신 + 지목 라우트 | ✓ VERIFIED (배선) / ⚠️ 신원 검증 없음 | `character_id` 필드가 존재하고 `mark_character_present`를 호출한다 — 다만 그 필드 자체가 검증되지 않은 입력이다 |
| `frontend/src/session/creationView.ts` (`shouldNominateNext`) | 화면의 부트스트랩 지목 조건 순수 함수 | ✓ VERIFIED | 소스 확인 + `frontend/src/session/creationView.test.ts`에 7건 이상 시험 존재(코드로 확인, 재실행은 안 함 — 12.3-06 SUMMARY가 실행 기록을 남겼고 이번 검증은 백엔드 시험만 재실행했다) |
| `frontend/src/panes/CreationPane.tsx` (지목 effect) | `shouldNominateNext` 사용 | ✓ VERIFIED | 소스 확인 — `state.creation_unfinished_character_ids.length`를 더는 조건으로 안 씀 |
| `tests/test_creation_screen_state.py` | 신규 시험 넷(부트스트랩·다음 참가자·상한 경계·빈 후보 거절) | ✓ VERIFIED (라이브 재실행) | `.venv/bin/python -m pytest tests/test_creation_screen_state.py -q` → 23 passed, 이번 검증이 직접 실행 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `CreationScreen`(재실 신호) | `POST /creation/host` | `claimCreationHost(sessionId, browserId, characterId)` | ✓ WIRED (코드 확인) | `character_id`가 실제로 실림 |
| `CreationPane`(자동 지목 effect) | `POST /creation/nominate` | `shouldNominateNext` → `nominateCreationSpeaker` | ✓ **WIRED and LIVE (12.3-07이 실증)** | 2차의 "WIRED but DEAD"가 이제 실제로 작동함을 라이브로 확인 |
| `ClaimHostRequest.character_id` | `present_candidates`(후보 상한) | `mark_character_present` | ✗ **WIRED but UNSAFE (신규 발견, 재현으로 확인)** | 배선은 되어 있으나, 상한이 신원을 안 지켜 유령이 진짜 참가자를 밀어낼 수 있다 |
| `nominate_creation_speaker`(유령 지목됨) | 재지목 회복 경로 | (없음) | ✗ **MISSING** | `creation_current_speaker_id`가 한 번 유령을 가리키면 되돌릴 코드 경로가 존재하지 않는다 |
| `InterjectBox`(끼어들기) | `POST /creation/interject` | `recordInterjection` | ✓ WIRED and LIVE (12.3-07이 실증) | 2차의 409가 실제 화자가 존재하는 지금은 200으로 성공함을 확인 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `present_candidates`가 유령 신호로 진짜 참가자를 밀어낸다 (CR-01) | 스크래치 스크립트에서 `present_candidates` 직접 호출(소스 무변경) | `('ghost-1', 'ghost-2')` — 진짜 참가자 0명 후보에 남음 | ✗ **FAIL (결함 재현)** |
| `test_creation_screen_state.py` 전체 | `.venv/bin/python -m pytest tests/test_creation_screen_state.py -q` | `23 passed` | ✓ PASS |
| 12.3-06/07이 변경한 파일에 debt 마커 | `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` (7개 파일) | 0건 | ✓ PASS |
| `.gptrpg/agents.json`이 12.3-07 이후 원상 복구됐는지 | SUMMARY의 sha256 대조(시작/종료 일치 기록) | 코드 재실행은 안 했으나 SUMMARY의 두 해시가 문자 그대로 일치 | ? SKIP (문서 대조만, 라이브 재확인 안 함 — 낮은 위험) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CHAR-06 | 12.3-01~07 (공유) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ✗ BLOCKED | `.planning/REQUIREMENTS.md`에 여전히 의도적으로 미체크(`- [ ]`) — 이번 검증이 그 판단이 옳음을 다시 확인했다. 정상 경로는 이번에 처음으로 끝까지 라이브 성립했지만, 그 경로를 여는 장치 자체(재실 신호)에 회복 불가능한 새 결함(CR-01)이 남아 있고, 12.3-05가 남긴 14개 사람 확인 항목 중 다수(교차 전파·새로고침 회귀·서버 재시작 후 동의 유지 등)가 아직 한 번도 수행되지 않았다 |

**Orphaned requirements:** 없음 — 1·2차와 동일.

### Anti-Patterns Found

이번 검증이 재확인한 12.3-06/07 변경 파일(`creation_state.py`,
`routes_creation.py`, `client.ts`, `CreationScreen.tsx`, `CreationPane.tsx`,
`creationView.ts`, `test_creation_screen_state.py`)에 TBD/FIXME/XXX/TODO/HACK/
PLACEHOLDER 계열 마커 0건. 1차가 이미 기록한 `DEFAULT_RULEBOOK_ID` 하드코딩
(의도된 미완성, Phase 16 경계)은 이번 검증의 새 발견이 아니며, 12.3-07이
이 한계를 우회해서라도 케언 경로를 라이브로 확인했다는 사실은 이미 기록돼
있다.

**CR-01은 anti-pattern이 아니라 §Goal Achievement의 정식 gap으로 위에 이미
반영했다** — 코드 냄새가 아니라 재현 가능한 기능 결함이기 때문이다.

### Human Verification Required

아래는 이번 검증이 새로 정리한 목록이다 — `behavior_unverified_items`(1건)와
그 외 사람의 시각·언어 판단이 필요한 항목, 그리고 `12.3-05-SUMMARY.md`의
14개 체크리스트 중 아직 한 번도 수행되지 않은 항목을 모았다.

#### 1. 동의 관문의 교차 전파 (behavior_unverified — 최우선)

**Test:** 실제 별도 브라우저(또는 사생활 보호 창) 둘 이상으로 같은 세션에
들어간다. 한쪽이 동의를 누른 뒤, 다른 쪽이 자기 항목 하나를 「고칠 게
있어요」로 다시 연다.
**Expected:** 먼저 동의를 눌렀던 사람의 화면에서도 동의 표시가 자동으로
「안 누름」으로 되돌아간다.
**Why human:** 12.3-07이 시도했으나 QA 도구(단일 크로미움 프로필)가 서명
쿠키와 `localStorage`를 탭 전체에 전역 공유해, 두 사람이 동시에 각자
유효한 인증 상태를 가질 수 없었다 — 실제 별도 브라우저가 필요하다.

#### 2. 주사위 연출이 판정 화면과 정말 같아 보이는가 (D-07)

**Test:** `task2-dice-modal.png`(12.3-07이 남긴 스크린샷)를 실제 판정(전투·
능력 판정) 화면의 주사위 모달과 나란히 비교한다.
**Expected:** 같은 컴포넌트·같은 연출로 보인다.
**Why human:** 코드 수준에서는 같은 `DiceModal.tsx`를 재사용함이 확인됐고
라이브로 실제로 떴다는 것도 확인됐다 — 남은 것은 "보기에 같은가"라는 시각
판단뿐이다.

#### 3. D-13 세 문구가 실제로 헷갈리지 않는가

**Test:** 아래 세 문장만 놓고 판단한다.
- ①(계약 위반, 200): `{character_id} 님, 이야기를 들려주시겠어요?`
- ②(설정 없음, 503): "진행자를 쓸 수 없는 상태입니다 — 운영자에게 알리세요"
- ③(연결 끊김): "연결이 끊겼어요. 자동으로 다시 시도하고 있어요 — 새로고침하지 마세요"

**Expected:** ①은 기다릴 필요 없다는 뜻으로, ②는 기다려도 소용없다는
뜻으로, ③은 잠시 뒤 돌아온다는 뜻으로 각각 구분된다.
**Why human:** 문자열 자체가 다르다는 것은 확인됐다 — "다음에 무엇을
해야 하는지가 구분되는가"는 언어 판단이다.

#### 4. 12.3-05가 남긴 14개 사람 확인 항목 — 아직 한 번도 전부 완주되지 않음

`12.3-05-SUMMARY.md` §사람 확인이 필요합니다에 14개 체크리스트가 있다.
12.3-07의 라이브 관찰이 그중 다수(1·3·5·6·8·9번)를 실질적으로 뒷받침하지만,
**공식 체크리스트 자체는 아직 누구도 처음부터 끝까지 순서대로 밟지 않았다.**
특히 다음은 12.3-07도 아직 다루지 않았다:

- 2번: 먼저 연 탭에만 인원 확정 조작이 보이고, 나중 탭엔 "방을 연 사람이
  정하는 중" 문구가 보이는지(진짜 별도 브라우저로, 방장이 정하기 **이전**
  시점에)
- 7번: 확정한 항목 중 **마지막이 아닌 앞선 항목**을 「고치기」로 다시 열 수
  있는지(12.3-07은 「이름·모습」 하나만 열어 봤다)
- 12번: 새로고침해도 지난 GM 말이 다시 "말하지" 않고 주사위가 다시 굴러가지
  않는지
- 13번: 서버를 껐다 켠 뒤에도 모인 동의가 그대로 남아 있는지(만들기 중간에)

**전체 목록은 `12.3-05-SUMMARY.md`를 그대로 참조할 것** — 이번 검증이 되풀이해
옮기지 않는다.

### Gaps Summary

**2차가 찾은 「첫 지목 교착」은 진짜로 닫혔다.** 12.3-06의 코드 수정과
12.3-07의 라이브 재확인을 이번 검증이 코드 대조와 시험 재실행으로 독립
확인했다 — CHAR-06의 정상 경로(안내 → 값 확정 → GM 정리 → 동의 → 점유)가
처음으로 끝까지 라이브로 성립했다.

**그런데 그 교착을 푼 수정 자체가 새 결함을 열어 놓았다.** `12.3-REVIEW.md`
CR-01이 코드 읽기로 짚은 것을 이번 검증이 소스를 안 고친 채 직접 재현했다 —
재실 신호가 신원을 검증하지 않아, 유령 캐릭터 식별자가 진짜 참가자를 지목
후보에서 완전히 밀어낼 수 있고, 한번 유령이 지목되면 되돌릴 방법이 코드
어디에도 없다. 이건 이 계획이 원래 고치려던 교착보다 더 나쁘다 — 회복
경로가 아예 없기 때문이다. 이 결함을 고치기 전에는 「사람이 브라우저만으로
끝까지 할 수 있다」는 CHAR-06의 약속이 정상 참가자 전원에게 항상 성립한다고
말할 수 없다(멀티 디바이스 사용이나 방치된 탭만으로도 우연히 촉발될 수 있다).

**남은 사람 확인도 아직 끝나지 않았다.** 동의 관문의 교차 전파(다른 사람
화면에도 반영되는지)는 이번에도 QA 도구의 구조적 한계로 라이브 확인이 안
됐고, 12.3-05의 14개 체크리스트도 아직 한 번도 처음부터 끝까지 순서대로
완주되지 않았다.

**CHAR-06은 이번에도 완료로 표시하지 않는다** — 위 CR-01을 먼저 닫고, 남은
사람 확인 항목이 실제로 끝난 뒤 사람이 판단할 일이다.

---

_Verified: 2026-08-21 (재검증, 3차)_
_Verifier: Claude (gsd-verifier)_
