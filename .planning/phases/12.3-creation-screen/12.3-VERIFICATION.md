---
phase: 12.3-creation-screen
verified: 2026-08-24T05:10:00Z
status: gaps_found
score: 13/15 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/10
  gaps_closed:
    - "넘침 단언(judge ②)이 .shell{overflow:hidden}에 가려 절대 발화할 수 없던 결함 — 12.3-17 Task 1이 .shell 자신의 scrollHeight/clientHeight + 세 칸 아래끝 비교로 교체했고, 이 검증이 --self-test의 pixel-floor 함정으로 직접 재현해 빨강(exit 1 상당)을 확인했다"
    - "폰 가로(844×390)에서 GM 제안 후보 버튼·보내기 단추·조작부 전체가 화면 밖으로 잘려 스크롤로도 못 닿던 결함 — 12.3-16 Task 1이 .composer에 min-height:0 + overflow-y:auto 두 줄을 더했고, 커밋 98a50b7 본문에 수정 전(RED, exit 1) → 수정 후(GREEN, exit 0) 증거가 남아 있다. 이 검증이 직접 재실행해 네 형태 전부 통과를 재확인했다"
    - "그물의 고정판이 대화판 세 층(chat__head·chat·composer·proposal)을 안 담아 이 종류의 결함을 원리적으로 못 잡던 결함 — 12.3-16 Task 1·3이 고정판을 실제 구조로 채우고 REQUIRED_SOURCE_CLASS_NAMES 감시 목록에 이름을 추가했다"
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "조작 요소(TOUCH_TARGETS) 판정이 「확인 불가」를 「통과」로 접지 않는다 — 12.3-16 must_haves.prohibitions '종료 코드 2(확인 불가)를 0(통과)으로 접지 않는다'"
    status: failed
    reason: |
      `judge()`의 조작 요소 단언(③, `scripts/check_narrow_viewport.py:688-702`)은
      `TOUCH_TARGETS`의 각 선택자로 `measurement["targets"][selector]`를 꺼내
      그 배열을 도는데, 그 배열이 비어 있으면(선택자가 실제 DOM에서 0개를
      찾았으면) 루프가 한 번도 안 돌아 어떤 사유 문장도 안 쌓인다 —
      결과적으로 judge()는 "확인했더니 문제없다"와 "확인을 못 했다"를
      구분하지 못하고 후자를 전자로 접는다.

      이 파일이 스스로 여러 번 선언한 규율("2를 0으로 접지 않는다",
      머리말 47-53행)과 정확히 같은 종류의 빈 값 경계인데, TOUCH_TARGETS
      경로만 그 규율 밖에 있다. `missingElements`(400-426행)와
      `REQUIRED_MEASUREMENT_KEYS`(558-573행)는 이 경계를 지키지만, 둘 다
      TOUCH_TARGETS의 각 선택자가 최소 1개는 찾았는지는 검사하지 않는다
      — `targets`라는 최상위 키가 있는지만 본다.

      독립 재현(이 검증이 직접 확인): `TOUCH_TARGETS`(162-170행)의 일곱
      선택자와 `build_inner_html()`의 고정판 마크업(275-304행 근처)을
      대조해, 지금은 일곱 선택자 전부가 고정판에서 1개 이상 매치되므로
      이 경로가 현재 안 걸린다는 것을 확인했다 — 즉 **지금 이 순간의
      실제 화면 판정은 정확하다.** 문제는 다음에 `CreationPane.tsx`/
      `ChatPane.tsx`의 마크업이 바뀌어(예: 버튼 클래스 이름이 바뀌거나
      구조가 한 단계 더 깊어져) `.composer__row .btn`이나 `.proposal .btn`
      같은 조합 선택자가 실제 소스와 갈라질 때다 —
      `REQUIRED_SOURCE_CLASS_NAMES`는 이런 조합 선택자를 애초에 감시하지
      않으므로 못 잡고, judge()는 그 조작 요소를 "잰 게 없으니 문제없다"로
      조용히 통과시킨다. 12.3-18의 목표 자체("확인을 못 했다가 통과로
      보이는 남은 길들이 막힌다")가 정확히 이 모양의 구멍을 셋 닫았는데,
      네 번째가 새 코드 자리(12.3-16 Task 2)에 남았다.
    artifacts:
      - path: "scripts/check_narrow_viewport.py"
        line: 688-702
        issue: "TOUCH_TARGETS 루프가 entries가 빈 배열이어도 종료 코드 2로 접지 않고 조용히 통과시킨다"
    missing:
      - "extract_measurements() 또는 judge() 진입부에서 TOUCH_TARGETS의 각 선택자에 대해 len(item['targets'].get(selector, [])) >= 1을 확인하고, 하나라도 0개면 사람이 읽을 사유와 함께 판정 불가(종료 코드 2)로 끝낸다"

  - truth: "--self-test가 각 함정이 겨눈 그 단언이 실제로 빨강을 내는지 확인한다 — 12.3-17이 run_self_test()의 도크스트링에서 스스로 약속한 것('함정 셋을 스스로 얹어 각 단언이 실제로 빨강을 내는지 뒤집어 확인한다'), 그리고 12.3-17의 목적('초록만 내는 그물은 아무것도 증명하지 않는다')"
    status: failed
    reason: |
      `run_self_test()`(796-884행)는 함정마다 `judge(target)`을 부르고
      `passed`(사유가 하나도 없는가) 하나만 본다 — 어느 사유가 찍혔는지는
      안 본다. `TRAPS` 상수(747행 근처)도 (이름, 덧씌울 CSS, 겨눈 형태)
      3튜플뿐이라 "이 함정이 겨눈 단언"을 코드가 식별할 방법이 없다.

      독립 재현(이 검증이 직접 실행): `.venv/bin/python
      scripts/check_narrow_viewport.py --self-test`의 실제 출력—

      ```
      pixel-floor · 844x390 · 빨강 · 골격 내용(513.0px)이 골격 상자(390.0px)를 넘어 잘렸다
      clipped-composer · 844x390 · 빨강 · 조작부 전체(.composer) 아래끝 652.0px가 'pane pane--chat'에 잘려 화면(390px) 밖이다 — 스크롤로도 못 닿는다
      starved-story · 390x844 · 빨강 · 서사 칸이 0.0px로 최소선 211.0px(뷰포트의 25%)보다 낮다
      ```

      코드 검토(12.3-REVIEW.md WR-01)가 `reasons` 전체를 찍어 확인한 바로는,
      `pixel-floor`는 넘침 단언(②)뿐 아니라 조작 요소 단언(③) 일곱 개
      전부와 함께 빨강을 냈고, `starved-story`는 서사 최소선 단언(①)뿐
      아니라 상태 칸 상한 단언(⑥)도 함께 빨강을 냈다 — `clipped-composer`
      하나만 겨눈 단언 하나로 깔끔하게 빨강이 났다. 즉 만약 미래에 넘침
      단언(②)이 다시 죽어도(예: shellScrollHeight 비교가 실수로 지워져도),
      `pixel-floor` 함정은 조작 요소 단언(③)이 대신 빨강을 내 주므로
      `--self-test`는 여전히 exit 0(통과)을 찍는다 — 죽은 것은 ②인데
      "단언들이 살아 있다"는 결과가 나온다. 12.3-17이 이 결함을 막겠다고
      만든 명령 자신이, 12.3-17이 막으려던 것과 같은 모양의 맹점을 새로
      들여왔다.
    artifacts:
      - path: "scripts/check_narrow_viewport.py"
        line: 868-875
        issue: "run_self_test()가 judge()의 전체 사유 목록에서 '그 함정이 겨눈 단언'이 아니라 '사유가 하나라도 있는가'만 본다 — 다른 단언이 대신 빨강을 내면 겨눈 단언이 죽어도 감지되지 않는다"
      - path: "scripts/check_narrow_viewport.py"
        line: 747
        issue: "TRAPS가 (이름, CSS, 겨눈 형태) 3튜플뿐이라 '겨눈 단언'을 코드가 식별할 표식이 없다"
    missing:
      - "TRAPS에 '이 함정이 겨눈 단언의 사유 문장에 반드시 있어야 하는 표식'을 넷째 칸으로 추가하고, run_self_test()가 passed뿐 아니라 그 표식이 reasons 안에 실제로 있는지도 확인한다"

deferred: []
---

# Phase 12.3: 캐릭터 만들기 화면 검증 보고서

**단계 목표:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 브라우저만으로 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다.
**검증 시각:** 2026-08-24T05:10:00Z
**상태:** gaps_found
**재검증 여부:** 예 — 이번 회차(12.3-16 · 12.3-17 · 12.3-18, `--gaps-only`)가 7차 검증이 연 gap 둘을 닫았는지 확인하는 것이 주 목적이었다

## 요약

이번 회차가 목표로 삼은 두 gap(넘침 단언이 원리적으로 발화 불가 · 폰에서
조작부가 화면 밖으로 잘림)은 **실제로 닫혔다** — 이 검증이 직접
`scripts/check_narrow_viewport.py`와 `--self-test`를 실행해 확인했고, 커밋
이력에 수정 전/후 증거가 남아 있다.

그런데 같은 회차가 새로 만든 코드(TOUCH_TARGETS 루프 · `--self-test`의
판정 방식) 안에, **이 회차 자신이 여러 번 명문화한 규율("확인 못 함을
통과로 접지 않는다")을 어기는 자리가 두 곳 새로 생겼다.** 둘 다
`12.3-REVIEW.md`(2026-08-24, 이 회차 범위 검토)가 critical로 지목했고,
이 검증이 코드를 직접 읽고 스크립트를 직접 실행해 독립적으로
재확인했다 — SUMMARY.md의 자체 평가에 기대지 않았다.

**지금 이 순간 폰 화면 자체는 정상이다** — 조작부가 화면 안에 있고,
`--self-test`도 오늘 함정 셋 전부에서 빨강을 낸다. 두 gap은 "오늘 화면이
깨져 있다"가 아니라 "이 그물이 앞으로 같은 종류의 결함을 놓칠 수 있는
자리가 남아 있다"는 것이다. 그러나 이 두 계획의 존재 이유 자체가
"그물이 스스로를 잡게 만든다"였고, 그 약속이 자신의 새 코드 안에서 다시
샜으므로 이번 회차의 must-have를 완전히 충족했다고 볼 수 없다.

## 목표 달성 현황

### 관찰 가능한 진실

| # | 진실 | 상태 | 근거 |
|---|------|------|------|
| 1 | 폰 가로(844×390)에서 대화 조작부가 화면 안으로 돌아오거나 스스로 스크롤해 닿는다 | ✓ VERIFIED | `.venv/bin/python scripts/check_narrow_viewport.py` 직접 실행 — 844x390 줄이 `대화 156.0px · 통과`. 커밋 98a50b7 본문에 수정 전(exit 1, 652.0px 잘림) → 수정 후(exit 0) 증거 |
| 2 | 폰 세로(390×844)에서도 같은 처방이 듣고, 귀속(기존 결함 vs 이번 악화)이 정확히 적혀 있다 | ✓ VERIFIED | 실행 결과 390x844 줄 `대화 337.6px · 통과`. `.composer` CSS 주석(styles.css:1163-1184)에 기각한 대안 셋과 귀속 근거가 그대로 남아 있다 |
| 3 | 그물이 스스로를 재현한다 — CSS 수정 전 빨강, 수정 후 초록이 커밋 이력에 남는다 | ✓ VERIFIED | `git show 98a50b7` 본문에 RED(exit 1, 두 형태 실패 실측치)와 GREEN(exit 0, 3컬럼 무변경) 둘 다 있다 |
| 4 | 「잘렸다」의 판정이 뷰포트 비교가 아니라 스크롤 경로 유무(`reachable()`)로 이뤄진다 | ✓ VERIFIED | `grep -c reachable` = 3(정의+호출). `.pane--status`의 스크롤 가능한 조작 칸이 거짓 빨강을 안 낸다는 것을 오늘 실행(exit 0, 네 형태 통과)이 보여준다 |
| 5 | 고정판이 실제 화면과 갈리면 종료 코드 2가 난다 — 감시가 `className=` 문맥 낱말 경계로 좁혀졌다 | ✓ VERIFIED | `REQUIRED_SOURCE_CLASS_NAMES`(177-194행)에 `chat__head`·`chat`·`composer`·`proposal` 등이 들어갔고, `_class_name_present_in_jsx()`가 낱말 경계 정규식(203-211행)을 쓴다 |
| 6 | 넘침 단언이 원리적으로 발화할 수 있게 됐다 — `.shell` 자신의 넘침 + 세 칸 아래끝으로 교체 | ✓ VERIFIED | `--self-test`의 `pixel-floor` 함정이 실제로 빨강("골격 내용(513.0px)이 골격 상자(390.0px)를 넘어 잘렸다")을 낸다 — 이 검증이 직접 실행해 확인 |
| 7 | 함정을 얹으면 그물이 실제로 빨개진다(3종 함정 전부) | ✓ VERIFIED | `--self-test` 실행 결과 세 함정(pixel-floor·clipped-composer·starved-story) 전부 빨강, 오늘 코드(함정 없음)는 exit 0 |
| 8 | 조작 요소(TOUCH_TARGETS) 판정이 「확인 불가」를 「통과」로 접지 않는다 | ✗ FAILED | 아래 gap #1. 선택자가 0개를 찾아도 실패 사유가 안 쌓여 조용히 통과로 읽힌다(현재 고정판에서는 안 걸리지만 방어 코드 자체가 없다) |
| 9 | `--self-test`가 「그 함정이 겨눈 단언」이 살아 있는지를 확인한다(다른 단언이 대신 우연히 안 가려 준다) | ✗ FAILED | 아래 gap #2. 셋 중 둘(pixel-floor·starved-story)이 겨눈 단언 아닌 다른 단언과 함께 빨강을 내, 겨눈 단언만 죽어도 여전히 exit 0이 나올 수 있다 |
| 10 | 넓은 창(1440×900) 대조군이 실제로 3컬럼을 잰다(좌우 폭까지) | ✓ VERIFIED | 실행 결과 `상태 900.0px · 서사 900.0px · 대화 900.0px`. `storyLeft`/`storyWidth` 측정(코드 463-468행)과 judge() 단언 ⑤(720-725행) 확인 |
| 11 | 좁은 형태에서 상태 칸이 뷰포트의 25%를 넘지 않는지도 판정한다 | ✓ VERIFIED | judge() 단언 ⑥(727-738행) 확인. 좁은 세 형태 모두 25% 이내로 통과 |
| 12 | 사람이 README만 보고 두 명령을 언제 왜 부르는지 알 수 있다 | ✓ VERIFIED | README.md §A 항목 2에 조작부/제안 언급과 `--self-test` 소개 문단(종료 코드 셋 포함)이 실제로 있다 |
| 13 | 코드 검토가 남긴 16건 전부의 처지가 기록으로 남는다(조용히 사라진 항목 0건) | ✓ VERIFIED | `deferred-items.md`에서 CR-01~03·WR-01~07·IN-01~06 16개 식별자가 전부 확인됨(grep 고유 개수 16) |
| 14 | 비-회귀: `.tsx` 0줄 · `src/gptrpg/` 0줄 · 새 패키지 0개 | ✓ VERIFIED | `git diff --name-only 5dd0f22..HEAD`(`.planning/` 제외)가 `README.md`·`frontend/src/styles.css`·`scripts/check_narrow_viewport.py` 셋뿐 |
| 15 | 비-회귀: 서버·화면 자동 시험이 이번 변경으로 새로 깨진 것이 없다 | ✓ VERIFIED | frontend `npm test` 104/104 통과. `.venv/bin/python -m pytest -q` 1406 통과·1 실패(무관한 기존 flaky 시험, `src/`·`tests/` 미변경으로 확인) |

**점수:** 13/15 진실 검증됨 (0건 present-behavior-unverified)

### 사람이 아직 확인해야 하는 것 (기계가 답할 수 없음)

12.3-16·17·18의 `must_haves.truths`에 `verification: backstop`으로 명시된
둘은 이번 회차로 닫히지 않는다 — 기계 단언은 「0이 아니다·25%는 된다」,
「44px이고 스크롤로 닿는다」까지만 답한다:

### 1. 폰에서 서사 칸 글자가 실제로 읽을 만한가

**확인:** 실제 폰 기기로 놀이 화면을 열어 서사 칸의 글자 크기·줄 간격이
답답하지 않은지 눈으로 본다.
**기대:** 읽는 데 불편함이 없다.
**왜 사람인가:** 기계가 답하는 것은 「0이 아니다·세로의 4분의 1은 된다」까지이고, 그 크기로 이야기를 읽을 만한지는 사람만 안다.

### 2. 만들기 중 자기 차례에 조작 칸 셋을 실제로 눌러 보기

**확인:** 만들기 중 자기 차례를 받은 판에서 폰으로 능력치 배치 칸·점수 숫자칸·인원 숫자칸을 실제로 눌러 본다.
**기대:** 손가락으로 답답함 없이 누를 수 있다.
**왜 사람인가:** 기계 단언은 「44px 이상이고 스크롤로 닿는다」까지만 답한다. 누르는 느낌은 사람만 안다.

`12.3-UAT.md` 테스트 11("대화판이 눈에 잘 들어오고 손가락으로 누를 만한가")이
바로 이 둘을 담당하는데, 이번 문서 기준 여전히 `[pending]`이다 — 아직
사람이 걷지 않았다.

### 필요 산출물

| 산출물 | 기대 | 상태 | 세부 |
|---|---|---|---|
| `scripts/check_narrow_viewport.py` | 그물이 넘침·조작부 잘림·대조군 붕괴·확인불가 경로를 모두 종료 코드로 정확히 가른다 | ⚠️ 부분 — TOUCH_TARGETS 빈 매치 경로와 --self-test의 표적 특이성 둘이 빠졌다 | 위 gap #1·#2 |
| `frontend/src/styles.css` | `.composer`가 잘리는 대신 스스로 스크롤한다 | ✓ VERIFIED | `min-height: 0` + `overflow-y: auto`, 근거 주석 포함 |
| `README.md` | 사람이 두 명령·`--self-test`를 언제 왜 부르는지 안다 | ✓ VERIFIED | §A 항목 2 갱신 확인 |
| `deferred-items.md` | 검토 16건 전부 처지 기록 | ✓ VERIFIED | 16개 식별자 전부 확인 |

### 요구사항 커버리지

| 요구사항 | 출처 계획 | 설명 | 상태 | 근거 |
|---|---|---|---|---|
| CHAR-06 | 12.3-16·12.3-17·12.3-18 | 사람이 브라우저만으로 캐릭터 만들기를 끝까지 진행할 수 있다 | ✓ 실질 충족(사람 확인 2건 미완) | `12.3-UAT.md` 테스트 1~9·12·13이 사람 손으로 pass, 테스트 11(폰 가독성·조작 촉감)은 pending. REQUIREMENTS.md는 이미 Complete로 표시돼 있고 이전 검증 회차가 그 표시를 되돌리지 않기로 판단했다(7차 검증) — 이번 회차도 그 판단을 유지한다. 이번 gap 둘은 CHAR-06의 기능 자체가 아니라 그 기능을 지키는 회귀 그물의 견고성에 관한 것이다 |

### 코드 검토 대조 (12.3-REVIEW.md, 이번 회차 범위)

`12.3-REVIEW.md`(2026-08-24T04:29:56Z)가 이번 회차(12.3-16·17·18)의
변경 파일 셋(`check_narrow_viewport.py`·`styles.css`·`README.md`)만
다시 검토해 critical 2 · warning 2 · info 2를 냈다. 이 검증이 코드를
직접 읽고 스크립트를 직접 실행해 여섯 건을 독립적으로 재확인했다:

| 식별자 | 요지 | 이 검증의 판단 |
|---|---|---|
| CR-01 | TOUCH_TARGETS 빈 매치가 조용히 통과로 읽힌다 | **재현됨 — gap #1로 승격** |
| WR-01 | `--self-test`가 겨눈 단언이 아니라 아무 단언이나 빨강이면 통과로 본다 | **재현됨 — gap #2로 승격** |
| WR-02 | 상태 칸 25% 상한 판정이 실제 CSS 폭 기준(1080px)이 아니라 형태 이름 문자열 비교다 | 확인됨(현재 `SHAPES`에서는 결과가 우연히 같음). must-have 위반은 아니라 gap으로 올리지 않음 — 강건성 개선 사항으로 남긴다 |
| IN-01 | `className=` 정규식이 중첩 중괄호(템플릿 리터럴 `${...}`)를 못 넘는다 | 확인됨. 현재 소스에 해당 패턴 없음(리뷰가 실측), 실패 방향도 안전한 쪽(오탐→종료 코드 2)이라 급하지 않음 |
| IN-02 | README 40행의 실패(1) 사유 나열이 「상태 칸 25% 상한 초과」를 빠뜨렸다 | 확인됨(README.md:40에 여전히 없음). 문서 완결성 문제이며 12.3-18의 명시 승인 기준 밖이라 gap으로 올리지 않음 |

WR-02·IN-01·IN-02는 must-have를 직접 위반하지 않는 강건성/문서
개선 사항으로 분류했다 — CR-01·WR-01만 「확인 못 함이 통과로 보인다」는
이번 회차의 핵심 규율을 새 코드 자리에서 직접 어기므로 gap으로
승격했다.

### 안티패턴 스캔

이번 회차가 건드린 세 파일(`check_narrow_viewport.py`·`styles.css`·
`README.md`)에서 TBD·FIXME·XXX·TODO·HACK·PLACEHOLDER류 표식은
발견되지 않았다. `.composer` CSS 규칙 위 주석에 기각한 대안 근거가
명시적으로 남아 있어(코드 검토가 반만 따른 이유), 부채 표식이 아니라
의도 기록이다.

### 행동 스팟체크

| 확인 | 명령 | 결과 | 상태 |
|---|---|---|---|
| 기본 경로 4형태 통과 | `.venv/bin/python scripts/check_narrow_viewport.py` | exit 0, 네 줄 전부 `통과` (1440x900→960x1080→390x844→844x390 차례) | ✓ PASS |
| `--self-test` 3함정 전부 빨강 | `.venv/bin/python scripts/check_narrow_viewport.py --self-test` | exit 0, pixel-floor·clipped-composer·starved-story 전부 빨강 | ✓ PASS |
| TOUCH_TARGETS 고정판 실측 | `.composer`·`.composer__input`·`.composer__row .btn`·`.proposal .btn`·`.candidate`·`select`·`input[type=number]` 마크업 존재 확인(grep) | 일곱 선택자 전부 고정판에 존재 — 현재는 CR-01(new) 경로가 안 걸림 | ✓ PASS(현재 상태 기준) |
| README/설명서 일치 | `grep -c self-test README.md` | 2 (소개 문단 + 명령 예시) | ✓ PASS |

### 사람 검증 필요 항목

아직 사람이 실제 기기로 걷지 않은 항목(위 「사람이 아직 확인해야 하는
것」과 동일):

1. **폰에서 서사 칸 글자가 실제로 읽을 만한가** — 기계는 25% 이상만 답한다.
2. **만들기 중 자기 차례에 조작 칸 셋을 실제로 눌러 보기** — 기계는 44px+스크롤로 닿음까지만 답한다.

### Gaps 요약

이번 회차가 처음 목표한 두 결함(넘침 단언 원리적 불발·폰 조작부 잘림)은
실제로 닫혔고, 이 검증이 코드 실행으로 직접 확인했다. 그런데 그 처방을
구현하며 새로 쓴 코드 두 자리가, 이 회차 자신이 되풀이해 명문화한
규율("확인 못 함을 통과로 접지 않는다", "초록만 내는 그물은 아무것도
증명하지 않는다")을 어긴다 — TOUCH_TARGETS 판정이 선택자 매치 0개를
"문제없음"으로 읽고, `--self-test`가 겨눈 단언이 아니라 아무 단언이나
빨강이면 통과로 본다. 둘 다 코드 검토(12.3-REVIEW.md CR-01·WR-01)가
critical로 지목했고, 이 검증이 코드를 직접 읽고 스크립트를 직접
실행해 독립적으로 재현했다. 오늘 이 순간의 실제 폰 화면은 정상이지만,
그것을 지키는 그물 자신에 아직 못 닫힌 맹점 둘이 남아 있으므로
gaps_found로 접는다.

---

_검증: 2026-08-24T05:10:00Z_
_검증자: Claude (gsd-verifier)_
