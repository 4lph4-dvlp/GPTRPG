---
phase: 12.3-creation-screen
verified: 2026-08-21T02:55:00Z
status: gaps_found
score: 6/13 must-haves verified
behavior_unverified: 5
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/13
  gaps_closed:
    - "방장이 이번 판의 인원을 화면에서 정할 수 있다 (D-11) — fixPartySize()가 browser_id를 안 보내 방장 본인도 403을 맞던 결함. 커밋 fc12642로 고쳐졌고, 이번 재검증이 실제 브라우저로 인원 3명 확정 → 200 → 다음 화면 전환까지 라이브로 재현해 확인했다."
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "GM이 첫 자기소개 순서를 지목해 항목 확정 화면이 열린다 (D-06) — 인원 확정 다음의 실행 가능한 단계"
    status: failed
    reason: "party-size gap을 고친 뒤 실제로 그 다음까지 가 보니 새 차단점이 나왔다. CR-01이 배선한 자동 지목 effect(CreationPane.tsx:512-532)는 `state.creation_unfinished_character_ids.length > 0`일 때만 `nominateCreationSpeaker`를 부른다. 그런데 이 후보 목록의 유일한 출처(`creation_state.unfinished_candidates`, creation_state.py:76-94)는 '이미 CompleteCreationStep을 한 번이라도 제출한 사람'만 담는다 — 그 함수 자신의 docstring이 이 불변식을 명시한다('플랫폼이 아는 유일한 참가자 식별 통로가 CompleteCreationStep의 character_id이기 때문이다'). 완전히 새 세션에서는 아무도 항목을 제출한 적이 없으므로 이 목록이 영원히 비고, 지목 effect가 단 한 번도 안 불린다. `CreationPane`의 화면 조작(`stepEditingEnabled = myTurn || myReopenedRow !== null`)은 지목된 사람(myTurn)이거나 재오픈된 항목이 있을 때만 열리므로, 첫 사람조차 스스로 항목을 낼 방법이 화면에 없다. 자유 끼어들기(interject)로 우회할 수도 없다 — `duringCharacterId`가 `creation_current_speaker_id ?? myCharacterId`로 자기 자신에 떨어져 서버가 409('자기 차례에는 끼어드는 것이 아니라 말하는 것이다')로 막는다(라이브로 직접 확인). 결과: 인원 확정과 GM 안내(announce)까지는 실제로 성립하지만 — announce는 실제 AI 호출(nim/nemotron-3-ultra, 약 10.7초)이 200으로 돌아와 화면에 항목 목록이 뜨는 것까지 라이브로 확인했다 — 그 다음 어떤 조작도 화면에 없다. D-06('자기소개 순서는 GM이 지목한다 ... 아무도 먼저 나서지 않아 정적하는 상황이 구조적으로 없다')이 세운 설계 의도가 실제로는 지켜지지 않는다. 서버 단 시험(test_nominate_records_a_single_creation_gm_spoke_event 등)이 이걸 못 잡은 이유: 시험이 `_submit_name_step`을 nominate보다 먼저 **직접 HTTP로** 호출해 후보를 미리 만들어 둔다 — 화면이 그 첫 제출을 낼 방법이 있는지는 어느 시험도 묻지 않는다. 12.3-REVIEW.md의 CR-01 수정 제안 자체도 '대화 줄기가 비어 있고 아직 완성 안 된 후보가 있으면'이라고 적어, 후보가 이미 있다는 것을 전제했다 — 리뷰도 이 부트스트랩 경우를 놓쳤다."
    artifacts:
      - path: "frontend/src/panes/CreationPane.tsx:511-532"
        issue: "자동 지목 effect가 creation_unfinished_character_ids.length > 0 조건에 갇혀 있다 — 부트스트랩(아무도 아직 제출 안 함) 경우를 처리 못한다"
      - path: "src/gptrpg/web/creation_state.py:76-94 (unfinished_candidates)"
        issue: "후보 목록의 유일한 출처가 '이미 제출된 CompleteCreationStep'이다 — 서버가 첫 제출 전에는 어떤 character_id도 후보로 알 방법이 없다"
      - path: "src/gptrpg/web/routes_creation.py:779-782 (nominate_creation_speaker)"
        issue: "candidates가 비면 409('아직 자기소개를 안 끝낸 사람이 없다')를 낸다 — 부트스트랩 상황에서 이 409가 항상 난다(화면은 애초에 이 경로를 안 불러 사람 눈에는 안 보인다)"
    missing:
      - "서버가 party_size_fixed 이후 첫 후보를 알 방법을 만든다 — 예: nominate가 후보가 비었을 때 party_size_fixed 범위 안에서 요청자 자신의 character_id를 새 후보로 받아들이는 경로를 열거나, 방장 재실 비콘과 별개로 참가자가 '나 여기 있다'를 알리는 신호를 도입한다"
      - "CreationPane의 부트스트랩 effect 조건을 '기존 후보가 있을 때'뿐 아니라 '아무도 아직 시작 안 한 상태(unfinished_character_ids와 created_characters 둘 다 빈 상태, party_size_fixed는 있음)'도 다루도록 넓힌다"
      - "고친 뒤 실제 브라우저로 재확인: 인원 확정 → 안내 → 첫 사람이 지목돼 항목 확정 화면이 실제로 열리는지, 그다음 두 번째·세 번째 사람도 이어서 지목되는지"
behavior_unverified_items:
  - truth: "주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07)"
    test: "위 지목 gap을 고친 뒤, 케언 룰북으로 Hit Protection 굴림을 실제로 채워보고 연출을 확인"
    expected: "판정 화면과 같은 주사위 애니메이션이 뜬다"
    why_human: "지목 gap에 막혀 항목 확정 화면 자체에 도달하지 못해 라이브로 확인 못함 — 코드는 존재(DiceModal.tsx, PendingCreationRoll 유니온)"
  - truth: "남의 차례에도 말이 항상 열려 있고, 확정 조작만 자기 차례에 뜬다 (D-08)"
    test: "두 캐릭터로 항목을 채우며 서로의 차례에 InterjectBox가 계속 열려 있는지, 확정 조작은 자기 차례에만 뜨는지 확인"
    expected: "InterjectBox는 항상 열려 있고, 항목 편집 UI는 지목된 사람에게만 보인다"
    why_human: "지목 gap에 막혀 '누군가의 차례'가 성립하는 상태 자체에 아직 도달 못함. 이번 검증에서 InterjectBox는 지목 전에도 렌더링되는 것은 확인했으나(코드 확인) 409로 막혀 실제 사용은 못했다"
  - truth: "동의 관문이 이름까지 보여주고 「고칠 게 있어요」로 항목을 다시 열 수 있다 (D-03/D-11)"
    test: "전원 완성 후 동의 관문에서 실제로 이름이 보이는지, 재오픈이 다른 탭의 동의 표시를 초기화하는지"
    expected: "이름 목록이 보이고 재오픈이 전파된다"
    why_human: "지목 gap 때문에 이번 검증에서도 이 화면까지 도달하지 못했다 — 코드 리뷰 수준에서는 존재·배선이 확인된다"
  - truth: "방장이 조용해지면 다른 브라우저가 이어받는다 (D-11)"
    test: "방장 탭을 30초 넘게 방치한 뒤 다른 탭에서 인원 확정 조작이 나타나는지 확인. 승계된 뒤에도 지목 gap이 여전히 막는지 함께 확인"
    expected: "다른 탭이 이어받아 인원 확정을 진행할 수 있다 — 하지만 승계 자체가 지목 gap을 풀지는 않는다는 점을 사람이 확인해야 한다"
    why_human: "단위 시험(test_idle_host_is_succeeded_by_another_browser_and_cannot_reclaim)은 서버 로직만 확인한다. party-size gap은 이번에 풀렸지만, 그 다음 지목 gap이 승계와 무관하게 여전히 막는다 — 이 인과관계를 사람이 라이브로 재확인해야 한다"
  - truth: "AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 다른 문구로 갈린다 (D-13 ①②③)"
    test: "①agents.json을 일부러 계약 위반하는 값으로, ②creation_gm 관련 설정을 실제로 지워서, ③서버를 꺼서 각각 확인"
    expected: "세 문구가 서로 다르게 뜬다"
    why_human: "이전 검증(1차)의 예측을 이번 재검증이 정정한다 — .gptrpg/agents.json에 creation_gm 키가 없어도 항상 503이 나는 것이 아니다. src/gptrpg/agents/config.py의 ROLE_FALLBACKS가 creation_gm 없으면 master_gm 선택을 그대로 물려준다(config.py:56-70). 이번 재검증에서 실제로 announce가 503이 아니라 200으로 성공했다(nim/nemotron-3-ultra-550b-a55b, 약 10.7초, 실제 AI 응답 수신). D-13②(설정 없음)를 실제로 재현하려면 creation_gm도 master_gm도 agents.json에서 완전히 비워야 한다 — 이번 재검증은 그 조합도, ①·③ 조합도 라이브로 못 봤다(지목 gap에 막혀 두 번째 GM 호출(nominate)까지 도달 못함)"
coincidental_reliance_items: []
---

# Phase 12.3: 캐릭터 만들기 화면 Verification Report

**Phase Goal:** 만들기가 서버에만 있고 화면이 없어 사람이 브라우저로 캐릭터를 만들 수 없다 — 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있게 한다.
**Verified:** 2026-08-21 (재검증, 2차)
**Status:** gaps_found
**Re-verification:** Yes — 1차 검증(2026-08-21T00:00:00Z)이 찾은 party-size 403 결함의 수정을 확인하러 다시 열었다

## 결론 먼저

**1차가 찾은 결함은 진짜로 고쳐졌다.** `fixPartySize()`가 이제 `browser_id`를 요청
본문에 싣는다(커밋 `fc12642`). 이번 재검증이 실제 브라우저로 새 세션을 열어 인원
3명을 확정해 봤고, 서버가 200을 돌려주고 화면이 다음 단계(GM 안내)로 실제로
넘어가는 것을 직접 관찰했다 — 더는 방장 본인조차 막히던 그 403이 안 뜬다. 새로
추가된 회귀 시험(`creationBody.test.ts`)도 직접 실행해 통과를 확인했다.

**하지만 그 다음 단계로 실제로 들어가 보니, 새로운 차단점이 나왔다.** 인원 확정 →
GM 안내(실제 AI 호출, 200)까지는 성립한다. 그런데 **GM이 첫 번째 사람을 지목하는
일이 구조적으로 절대 일어나지 않는다** — CR-01이 배선한 자동 지목 장치는 "이미
누군가 항목을 하나라도 낸" 상태에서만 작동하는데, 완전히 새 세션에는 그런 사람이
있을 수 없다(닭이 먼저냐 달걀이 먼저냐의 교착). 그 결과 `창 확정 화면
(stepEditingEnabled)`이 영원히 안 열리고, 자유 끼어들기로 우회하려 해도 서버가
409로 막는다 — 전부 실제 브라우저로 직접 재현해 확인했다.

**목표는 여전히 성립하지 않는다.** CHAR-06이 요구하는 흐름(안내 → 값 확정 → GM
정리 → 동의 → 점유)에서 "값 확정"이 아직 한 번도 실제 사용자 경로로 열린 적이
없다 — 이번에도 그 자리에 도달한 사람이 여전히 아무도 없다.

## 1차 검증 이력 (요약 — 전문은 git 이력의 이전 커밋에 있다)

- **찾은 결함:** `frontend/src/api/client.ts`의 `fixPartySize()`가 `browser_id`를
  요청 본문에 안 실어, 방장이 이미 자동으로 정해진 뒤(마운트 즉시 비콘) 방장 본인이
  인원 확정 버튼을 눌러도 서버가 `host != ""`로 항상 403을 냈다.
- **재현:** 두 개 세션(`qa-verify-2`·`qa-verify-3`)에서 100% 재현.
- **점수:** 9/13 (4 present-but-behavior-unverified, 1 failed).
- **상태:** `gaps_found`.

## 2차 검증(이번) 방법

1. 커밋 `fc12642`를 읽어 `fixPartySize`/`PartySizeControl`/`creationBody.test.ts`
   세 파일의 diff를 직접 확인했다.
2. `frontend/src/api/client.ts`의 만들기 POST 헬퍼 열한 개 전부를
   `src/gptrpg/web/routes_creation.py`의 대응 요청 모델과 대조해, "서버가 permissive
   default를 두고 화면이 그 필드를 안 보내는" 같은 유형의 결함이 더 있는지 점검했다
   — 아래 §다른 만들기 경로 점검 참조.
3. `npm --prefix frontend test -- creationBody` (2 passed)와
   `pytest tests/test_creation_consent_host.py::test_host_browser_can_fix_party_size`
   (1 passed)를 단일 명명 시험으로 직접 실행했다.
4. 이미 떠 있는 확인용 서버(uvicorn :8000 · vite :5173,
   `GPTRPG_DB=.gptrpg/qa-12-3-05.db`, `.gptrpg/agents.json`에 `action_classifier`·
   `master_gm`만 설정됨)에 `/browse`로 접속해 **새 세션
   `qa-verify-12-3-3`**을 열고, 인원 확정부터 실제로 진행해 봤다 — 이 과정에서
   새 차단점을 발견했다.

## 인원 확정 gap — 라이브로 고쳐졌음을 확인

```
GET  /?session=qa-verify-12-3-3                         → 200, "이번 판 인원을 정해요"
POST /api/sessions/qa-verify-12-3-3/creation/party-size  → 409 (2명 — 룰북 범위 미달, 정상 검증)
POST /api/sessions/qa-verify-12-3-3/creation/party-size  → 200 (3명 — 성공)
```
3명으로 다시 시도하자 200이 돌아왔고, 화면이 곧바로 "다른 사람의 차례예요 —
자유롭게 끼어들어 말할 수 있어요" + "진행자에게 안내를 부탁하기" 버튼으로 전환됐다
— 1차 검증에서 100% 재현되던 403이 더는 뜨지 않는다. `curl .../events` 응답에도
`party_size_fixed` 사건(`player_character_count: 3`)이 정확히 기록됐다.

## 새로 발견한 결함 (2차) — 첫 지목이 구조적으로 안 일어난다

「진행자에게 안내를 부탁하기」를 누르자 실제 AI 호출(nim
`nvidia/nemotron-3-ultra-550b-a55b`)이 약 10.7초 걸려 200으로 돌아왔고, 화면에
GM의 안내 문장(필요 항목 6개 나열)이 실제로 떴다 — `creation_gm_spoke` 사건도
정확히 하나(`kind: "announce"`) 기록됐다. **여기까지는 CHAR-06이 성립한다.**

그런데 그 다음부터 화면에 조작할 것이 하나도 안 남는다:

```
$B accessibility (안내 성공 직후)
- paragraph: 다른 사람의 차례예요 — 자유롭게 끼어들어 말할 수 있어요
- text: "진행자 ...(안내 문장)..."
- textbox "끼어들어 말하기…"
- button "보내기" [disabled]
```

끼어들기 칸에 글자를 채우면 버튼은 활성화되지만, 눌러 보면:

```
POST /api/sessions/qa-verify-12-3-3/creation/interject → 409
화면 문구: "자기 차례에는 끼어드는 것이 아니라 말하는 것이다"
```

5초 넘게 기다려도 지목(`nominate`) 사건은 한 번도 안 생겼다(`curl .../events`로
직접 확인, `last_seq`가 2에서 안 움직임). **원인을 소스에서 확인했다:**

- `frontend/src/panes/CreationPane.tsx:512-532`의 자동 지목 effect(CR-01이 이번
  단계에서 배선한 것)는 `state.creation_unfinished_character_ids.length > 0`일
  때만 `nominateCreationSpeaker`를 부른다.
- 이 목록(`src/gptrpg/web/creation_state.py:76-94`,
  `creation_state.unfinished_candidates`)의 유일한 출처는 **이미
  `CompleteCreationStep`을 한 번이라도 낸 사람**이다 — 함수 자신의 docstring이
  이렇게 밝힌다: "플랫폼이 아는 유일한 참가자 식별 통로가 `CompleteCreationStep`의
  `character_id`이기 때문이다."
- 완전히 새 세션에는 아직 아무도 항목을 낸 적이 없으므로 이 목록이 **영원히
  빈다** — 지목 effect가 단 한 번도 안 불린다.
- `CreationPane`의 항목 확정 UI는 `stepEditingEnabled = myTurn ||
  myReopenedRow !== null` 하나로만 열린다(`CreationPane.tsx:441`). `myTurn`은
  지목된 사람만 참이다(`creationView.ts:80-82`). 지목이 영원히 안 일어나니
  `stepEditingEnabled`도 영원히 거짓이다.
- 자유 끼어들기로 우회할 길도 없다 — `duringCharacterId`가
  `state.creation_current_speaker_id ?? myCharacterId`로 자기 자신에게 떨어져
  서버(`InterjectionRequest` 처리부)가 "자기 차례에는 끼어드는 것이 아니라
  말하는 것이다" 409로 막는다.

**결과:** 인원 확정과 GM 안내는 실제로 열렸지만, 그 다음(첫 사람 지목 → 항목
확정)은 실 사용자 경로에서 **절대** 일어나지 않는다 — 코드 배선이 아니라 후보
목록 계산의 전제(누군가 이미 시작했어야 후보가 된다)와 지목의 목적(아무도
안 시작해도 GM이 먼저 지목한다, D-06) 자체가 서로 모순된다.

**왜 서버 시험도 코드 리뷰도 이걸 못 잡았나:**
`tests/test_creation_screen_state.py::test_nominate_records_a_single_creation_gm_spoke_event`
등은 `_submit_name_step`을 nominate보다 **먼저 직접 HTTP로** 불러 후보를 미리
만들어 둔다 — 화면이 그 첫 제출을 실제로 낼 방법이 있는지는 어느 시험도 묻지
않는다. `12.3-REVIEW.md`의 CR-01 수정 제안 문구도 "대화 줄기가 비어 있고 **아직
완성 안 된 후보가 있으면** 자동으로 부르거나"라고 적어 후보가 이미 존재한다는
것을 전제했다 — 리뷰 자신도 이 부트스트랩 경우를 놓쳤다. D-06(12.1-CONTEXT.md
146행)의 원래 설계 의도 — "아무도 먼저 나서지 않아 정적하는 상황이 구조적으로
없다" — 가 실제로는 정확히 그 상황을 만들고 있다.

## 다른 만들기 경로 점검 (요청받은 점검 1) — 같은 유형의 결함은 더 없음

`frontend/src/api/client.ts`의 만들기 POST 헬퍼 열한 개 전부를
`src/gptrpg/web/routes_creation.py`의 요청 모델과 대조했다.

| 헬퍼 (client.ts) | 서버 요청 모델 | `browser_id` 필드 | 화면이 실제로 보내는가 |
|---|---|---|---|
| `fixPartySize` | `FixPartySizeRequest` | `default=""` (permissive) | ✓ 이제 보낸다 (fc12642) |
| `claimCreationHost` | `ClaimHostRequest` | `min_length=1` (필수) | ✓ 보낸다 — 안 보내면 422로 크게 실패, 조용한 403이 아니다 |
| `completeCreationStep` | `CompleteCreationStepRequest` | `min_length=1` (필수) | ✓ 보낸다 |
| `completeCreation` | `CreateCharacterRequest` | `min_length=1` (필수) | ✓ 보낸다 |
| `recordInterjection` | `InterjectionRequest` | `min_length=1` (필수) | ✓ 보낸다 |
| `recordCreationConsent` | `ConsentRequest` | `min_length=1` (필수) | ✓ 보낸다 |
| `announceCreation`/`nominateCreationSpeaker`/`creationFollowUp`/`wrapUpCreation` | 각각 `rulebook_id`만 (`default=DUNGEONWORLD_LIKE_ID`) | 해당 없음 | ✓ 화면이 항상 명시적으로 `rulebookId`를 싣는다 |

**결론:** `FixPartySizeRequest.browser_id`가 **유일하게** permissive default를 둔
자리였고, 그것이 곧 1차가 찾은 결함이었다 — 이제 고쳐졌다. 나머지 `browser_id`
필드는 전부 `min_length=1`(필수)이라, 화면이 빠뜨리면 조용한 403이 아니라 시끄러운
422로 즉시 드러난다 — 같은 유형의 숨은 결함이 될 수 없는 모양이다.
`LockPartyRosterRequest`(`/creation/lock-roster`)는 client.ts 어디에서도 호출되지
않는다 — 동의 전원 완료 시 서버가 내부적으로 잠그는 경로로 보이며, 이번 점검
범위(화면이 부르는 헬퍼)에는 해당하지 않는다.

## Goal Achievement

### Observable Truths

| # | Truth | Status (1차) | Status (2차, 이번) | Evidence |
|---|-------|------|------|----------|
| 1 | 세션 링크를 열면 캐릭터 고르기 화면 없이 만들기 화면이 바로 뜬다 (D-10) | ✓ VERIFIED | ✓ VERIFIED | 라이브 재확인 — `qa-verify-12-3-3` 접속 시 즉시 만들기 화면 |
| 2 | 방장이 화면에서 인원을 정할 수 있다 (D-11) | ✗ FAILED | ✓ **VERIFIED (고쳐짐)** | 라이브 확인 — 3명으로 200, 다음 화면 전환. §인원 확정 gap 참조 |
| 3 | GM의 안내·지목·되묻기·정리가 사건 기록에 남아 다른 탭도 폴링으로 본다 (D-02) | ✓ 코드 / ⚠️ 라이브 미확인 | ✗ **FAILED (부분)** | 안내(announce)는 라이브로 성립(실제 AI 호출, 200) — 지목(nominate)부터는 §새로 발견한 결함 때문에 절대 안 일어난다 |
| 4 | 같은 시점 재호출에 AI를 다시 부르지 않는다 (D-12) | ✓ VERIFIED | ✓ VERIFIED (재확인 안 함, 회귀 없음) | 시험 코드 불변 — 이번 재검증은 이 경로를 다시 건드리지 않았다 |
| 5 | 방장 브라우저 식별자가 폴링 events 배열에 새지 않는다 (T-12.3-05) | ✓ VERIFIED | ✓ VERIFIED | 라이브 재확인 — `creation_host_claimed.browser_id == ""` |
| 6 | 항목 확정 화면(stepEditingEnabled)이 실제로 열린다 | ✓ 코드 / ⚠️ 라이브 미확인 | ✗ **FAILED (라이브 재현)** | 실제로 열어 봤지만 안 열린다 — §새로 발견한 결함의 핵심. CR-01은 "지목 호출을 배선"했지만 "지목이 실제로 성공하는 조건"까지는 못 채웠다 |
| 7 | 「안내」 버튼이 안내 전에 사라지지 않는다 (CR-02) | ✓ VERIFIED | ✓ VERIFIED | 라이브 재확인 — 안내 성공 전엔 버튼 존재, 성공 후 자연스럽게 사라짐(대화 진행으로 대체) |
| 8 | 주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (원인 이관) | 위 지목 gap에 여전히 막혀 도달 못함 — 원인이 party-size에서 지목으로 바뀌었을 뿐 |
| 9 | 남의 차례에도 말이 항상 열려 있고, 확정 조작만 자기 차례에 뜬다 (D-08) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | InterjectBox는 지목 전에도 렌더링되는 것을 라이브로 확인했으나(409로 막힘), "자기 차례" 자체가 아직 성립한 적이 없어 정확한 확인은 못함 |
| 10 | 동의 관문이 이름까지 보여주고 「고칠 게 있어요」로 재오픈된다 (D-03/D-11) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 위 지목 gap에 막혀 도달 못함 |
| 11 | 방장이 사라지면 다른 브라우저가 이어받는다 (D-11) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 서버 단 시험은 여전히 통과. 승계돼도 지목 gap은 안 풀린다는 점이 새로 분명해졌다 |
| 12 | AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 다른 문구로 갈린다 (D-13) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (예측: 항상 503) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (예측 **정정**) | 1차의 "creation_gm 없어서 항상 503" 예측이 틀렸다 — `ROLE_FALLBACKS`가 `master_gm`으로 물려주고, 실제로 이번에 announce가 200으로 성공했다(라이브 확인). D-13 세 문구는 여전히 라이브 미확인 |
| 13 | `CHAR-06`이 `REQUIREMENTS.md`·`ROADMAP.md`에 존재한다 | ✓ VERIFIED | ✓ VERIFIED | `grep CHAR-06 REQUIREMENTS.md` 재확인, 여전히 의도적으로 미체크(`- [ ]`) — 이번 재검증도 그 판단이 옳음을 다시 확인했다(다른 지점에서지만 여전히 막힘) |

**Score:** 6/13 verified (5 present-but-behavior-unverified, 2 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/api/client.ts` | 만들기 화면 API 표면 | ✓ **VERIFIED (고쳐짐)** | `fixPartySize`가 이제 `browserId` 매개변수를 받아 `browser_id`를 싣는다. 다른 열 헬퍼 전부 대조 확인 — 같은 유형 결함 없음 |
| `frontend/src/screens/CreationScreen.tsx` | 만들기 화면 뼈대 + 인원 확정 배선 | ✓ VERIFIED (라이브) | `PartySizeControl`에 `browserId={myBrowserId}` prop이 실제로 흐른다. 소스+라이브 둘 다 확인 |
| `frontend/src/api/creationBody.test.ts` | 화면이 실제로 내보내는 POST 본문 확인(신규) | ✓ VERIFIED | 단일 명명 시험 직접 실행, 2 passed. `browser_id` 필드를 빼면 실패하는 것도 커밋 메시지가 확인함(재실행은 안 함) |
| `frontend/src/panes/CreationPane.tsx` (지목 effect) | 아무도 지목 안 됐을 때 자동으로 지목을 요청 | ⚠️ **HOLLOW (조건 불충족)** | effect 자체는 존재하고 배선돼 있지만(코드+라이브 둘 다 렌더는 확인), 그 실행 조건(`creation_unfinished_character_ids.length > 0`)이 부트스트랩 세션에서 결코 참이 될 수 없다 — 라이브로 5초+ 대기해도 한 번도 안 불림을 확인 |
| `src/gptrpg/web/creation_state.py` (`unfinished_candidates`) | 지목 후보 목록 계산 | ⚠️ **설계 불일치** | 코드는 정확히 문서화된 대로 동작하지만, 그 불변식("이미 제출한 사람만 후보")이 D-06의 설계 의도("아무도 안 나서도 GM이 지목")와 정면으로 모순된다 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `PartySizeControl`(인원 확정) | `POST /creation/party-size` | `fixPartySize` | ✓ **WIRED (고쳐짐, 라이브 확인)** | 3명 확정 → 200 → 화면 전환까지 직접 관찰 |
| `CreationScreen`(안내 버튼) | `POST /creation/announce` | `announceCreation` | ✓ WIRED (라이브 확인, 신규) | 실제 AI 호출 200, GM 문장이 화면에 렌더링됨을 확인 |
| `CreationPane`(자동 지목 effect) | `POST /creation/nominate` | `nominateCreationSpeaker` | ✗ **WIRED but DEAD (라이브 확인, 신규 발견)** | 함수 호출 자체는 배선돼 있지만, 실행 조건이 부트스트랩 세션에서 결코 만족되지 않아 5초+ 관찰 동안 단 한 번도 실행되지 않았다 |
| `InterjectBox`(끼어들기) | `POST /creation/interject` | `recordInterjection` | ✗ **NOT_USABLE (라이브 확인)** | 호출은 가지만 `duringCharacterId`가 자기 자신으로 떨어져 서버가 항상 409로 거절한다(지목 전 상태에서는) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 방장이 인원을 확정할 수 있다 (D-11) | `/browse` — 3명 입력 후 "인원 확정" 클릭 | `POST .../creation/party-size → 200`, 화면 전환 | ✓ **PASS (1차에서 FAIL → 이번 PASS)** |
| GM 안내가 실제로 성립한다 (D-02) | `/browse` — "진행자에게 안내를 부탁하기" 클릭 | `POST .../creation/announce → 200` (10.7초, 실제 AI), GM 문장 렌더링 | ✓ PASS (신규 확인) |
| 첫 사람이 지목돼 항목 확정 화면이 열린다 (D-06) | 안내 성공 후 5초+ 대기, `curl .../events` 폴링 | `creation_gm_spoke(kind=nominate)` 사건 없음, `creation_current_speaker_id: null` 그대로 | ✗ **FAIL (신규 발견)** |
| 끼어들기로 첫 항목을 우회 제출할 수 있는가 | 끼어들기 칸에 텍스트 입력 후 전송 | `POST .../creation/interject → 409` | ✗ FAIL (예상된 설계상 거절이지만, 다른 경로도 전혀 없음을 확인) |
| `fixPartySize`가 `browser_id`를 싣는다 (회귀 방지) | `npx vitest run src/api/creationBody.test.ts` (frontend/) | 2 passed | ✓ PASS |
| 방장 관문 시험 (서버) | `pytest tests/test_creation_consent_host.py::test_host_browser_can_fix_party_size` | 1 passed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CHAR-06 | 12.3-01~05 (공유) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ✗ BLOCKED | `.planning/REQUIREMENTS.md`에 여전히 의도적으로 미체크(`- [ ]`) — 이번 재검증이 그 판단이 여전히 옳음을 확인했다. 막히는 지점이 "인원 확정"에서 "첫 지목"으로 한 단계 더 들어갔을 뿐이다 |

**Orphaned requirements:** 없음 — 1차와 동일.

### Anti-Patterns Found

이번 재검증이 확인한 변경 파일(`frontend/src/api/client.ts`,
`frontend/src/screens/CreationScreen.tsx`, `frontend/src/api/creationBody.test.ts`)에
TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커 0건. 1차가 이미 기록한
`DEFAULT_RULEBOOK_ID` 하드코딩(의도된 미완성, WINDOWS.md #7)은 이번 재검증의
새 발견이 아니다.

### Human Verification Required

아래 §frontmatter의 `human_verification`(behavior_unverified_items에 병합)과
동일한 내용이다. 핵심은 **위 새 gap(첫 지목)을 먼저 고쳐야** 이 항목들에 도달할 수
있다는 것이다 — 이번에도 사람이 확인 절차를 밟으면 1차보다 한 단계 더 갔지만
여전히 두 번째 실 조작(항목 채우기)에서 막힌다.

CHAR-06의 14개 항목 사람 확인 체크리스트는 여전히 한 번도 수행되지 않았다
(`12.3-05-SUMMARY.md` §사람 확인이 필요합니다) — CHAR-06을 체크하려면 위 gap을
고친 뒤 그 14개 항목을 사람이 실제로 두 브라우저 이상으로 완주해야 한다.

### Gaps Summary

**1차 gap은 진짜로 닫혔다.** `fixPartySize`가 `browser_id`를 이제 보내고,
라이브로 인원 확정이 실제로 성공하는 것을 확인했다 — 새 회귀 시험
(`creationBody.test.ts`)도 실행해 통과를 확인했고, 그 시험이 진짜 가드라는 것도
커밋 메시지가 밝힌 대로다(필드를 빼면 실패).

**하지만 그 다음 단계로 더 들어가 보니 새 gap이 나왔다.** GM이 첫 사람을
지목하는 일이 부트스트랩 세션에서 구조적으로 절대 일어나지 않는다 — 지목 후보
목록이 "이미 제출한 사람"만 담는데, 아무도 제출할 방법이 지목 전에는 없기
때문이다(닭이 먼저냐 달걀이 먼저냐). 이 결함은 party-size gap이 화면 자체를 가려서
1차 검증도, 코드 리뷰(12.3-REVIEW.md CR-01)도 볼 수 없었던 자리다 — 이번에
party-size를 통과해서야 처음으로 실제로 드러났다.

CHAR-06이 요구하는 흐름(안내 → 값 확정 → GM 정리 → 동의 → 점유)에서 "안내"까지는
이번에 처음으로 라이브로 성립을 확인했다. "값 확정"은 여전히 실 사용자 경로로
한 번도 열린 적이 없다 — 그 뒤(GM 정리·동의·점유)는 코드 리뷰 수준의 존재·배선
확인만 있고 여전히 라이브 미도달이다.

---

_Verified: 2026-08-21 (재검증, 2차)_
_Verifier: Claude (gsd-verifier)_
