---
phase: 12.3-creation-screen
verified: 2026-08-21T22:15:00Z
status: gaps_found
score: 18/20 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 13/15
  gaps_closed:
    - "재실 신호 상한이 「위조 방지」를 실제로 한다 — 유령(가짜) 캐릭터 식별자가 진짜 참가자를 지목 후보에서 밀어낼 수 없다 (T-12.3-17) — 12.3-08 Task 1·2가 forfeit-and-demote(회수 가능한 자격) 방식으로 닫았다. 이번 4차 검증이 `present_candidates`/`forfeited_nominee`를 직접 읽고, `test_ghost_presence_signals_do_not_permanently_push_out_real_participants`를 재실행해 통과를 확인했다 — 정원 2에 유령 둘이 먼저 와도 결국 진짜 참가자 둘이 후보 전부가 된다."
  gaps_remaining: []
  regressions:
    - "12.3-08이 위 gap을 닫은 바로 그 메커니즘(`_forfeited_at`)이 새 회복 불가능 배제를 열었다 — 12.3-REVIEW.md CR-01. 아래 신규 gap 참조. 이것은 「같은 gap이 그대로 남음」이 아니라 「같은 자리에서 형태가 바뀐 새 결함」이므로 regressions로 기록한다."
gaps:
  - truth: "회복이 영구 배제로 바뀌지 않는다 — 흘려보낸 것으로 판정된 식별자가 나중에 다시 지목되면 그 새 지목에 대해 회복 판정을 다시 받는다 (12.3-08-PLAN.md must_haves.prohibitions 「회복이 영구 배제로 바뀌지 않는다」, 12.3-REVIEW.md CR-01)"
    status: failed
    reason: "`forfeited_nominee`(creation_state.py:139-192)가 `(session_id, nominee)` 키만으로 `_forfeited_at`를 조회한다(158행 `if key in _forfeited_at: return nominee`) — `nominated_seq`(그 판정이 어느 지목 사건에 적용되는지)는 전혀 비교하지 않는다. `_forfeited_at`를 지우는 코드는 소스 전체(`src/`, `tests/`, `frontend/`)에 단 한 곳도 없다(grep으로 재확인: 쓰기는 190행 한 곳, 읽기는 158행과 239행뿐). 그 결과 GM이 같은 사람을 **새 지목**(다른 seq)으로 다시 지목해도 `forfeited_nominee`는 그 새 지목을 한 번도 평가하지 않고 즉시 '이미 흘려보냄'으로 되돌린다 — `routes_events.py`의 `_creation_current_speaker_id`가 이 값을 그대로 `creation_current_speaker_id: null`로 옮기므로, 그 캐릭터는 **다시는 자기 차례를 화면에서 못 받는다**. `frontend/src/session/creationView.ts`의 `canEdit`(145행)이 `myTurn`(=`isMyTurn`, `creation_current_speaker_id === myCharacterId`)에만 기대므로, `CreationPane.tsx`의 항목 편집 UI(`stepEditingEnabled`)도 다시는 안 열린다. 이번 4차 검증이 소스를 한 글자도 안 고친 채 직접 재현했다(아래 §CR-01 독립 재현 참조) — 새 지목(seq=2)을 줘도 `forfeited_nominee`가 즉시 그 대상을 다시 흘려보낸 것으로 판정한다. 유령뿐 아니라 **진짜 참가자**도 걸린다: 5분간 항목을 하나도 못 낸 정당한 이유(자리 비움·긴 서술 작성 중 네트워크 문제 등)로 한 번 흘려보내진 사람이, 다른 후보가 소진돼 GM이 다시 그 사람을 지목하는 순간부터 그 파티는 영구 교착에 빠진다 — 그 사람이 이미 항목을 하나라도 냈었다면 `unfinished_candidates`에 영원히 남아 `wrap_up_creation`(명단 잠금)까지 막는다. 이것은 12.3-08 계획서 자신이 `must_haves.prohibitions`에 명시한 '회복이 영구 배제로 바뀌지 않는다'는 원칙을 정면으로 어긴다. `12.3-REVIEW.md` CR-01이 코드 읽기로 짚은 것을 이번 검증이 독립적으로(스크립트 재현 + 소스 재대조) 확인했다."
    artifacts:
      - path: "src/gptrpg/web/creation_state.py:139-192 (forfeited_nominee)"
        issue: "조기 반환(158행)이 `nominee` 식별자만 보고 `nominated_seq`를 안 본다 — 새 지목이 와도 옛 판정을 그대로 되돌린다"
      - path: "src/gptrpg/web/creation_state.py:103-107 (_forfeited_at)"
        issue: "docstring 자신이 '한 번 들어가면 세션 동안 안 지운다'고 적었고, 실제로 지우는 코드가 소스 어디에도 없다(grep 재확인) — 그런데 158행의 조회 조건이 그 사실과 충돌한다: docstring은 재시작 전까지 유효하다는 뜻이지만, 실제 영향은 '그 캐릭터가 다시는 지목 못 받는다'로 나타난다"
      - path: "src/gptrpg/web/routes_events.py:187-199 (_creation_current_speaker_id)"
        issue: "`forfeited_nominee(...) == nominee`이면 무조건 `None` — 그 판정이 지금 지목(최신 seq)에 대한 것인지 옛 지목에 대한 것인지 구분하지 않고 그대로 폴링에 흘려보낸다"
      - path: "frontend/src/session/creationView.ts:145 (canEdit), frontend/src/panes/CreationPane.tsx:442 (stepEditingEnabled)"
        issue: "`creation_current_speaker_id`가 그 캐릭터에 대해 영원히 null이면 이 캐릭터는 화면으로 다시는 항목을 낼 방법이 없다 — 서버가 막는 게 아니라 화면이 다시는 그 사람을 지목된 상태로 못 보여줘서 생기는 결과"
    missing:
      - "`_forfeited_at`를 `(session_id, character_id)` 키가 아니라 `12.3-REVIEW.md` CR-01의 Fix가 제시한 대로 `(session_id, character_id) -> (판정 시각, 그 판정이 적용되는 nominated_seq)`로 바꾸고, `forfeited_nominee`의 조기 반환 조건에 `forfeited[1] == nominated_seq` 비교를 더한다 — 새 지목(다른 seq)이 오면 옛 판정을 무시하고 처음부터 다시 잰다"
      - "흘려보낸 사람이 **같은 seq로 다시 조회될 때만** 흘려보낸 상태가 유지되고, **새 지목(다른 seq)을 받으면** 다시 정상적으로 회복 시계가 도는지 확인하는 회귀 시험 — `test_a_stalled_nomination_is_released_and_the_next_one_lands_on_someone_else`는 '다른 사람에게 넘어가는 것'만 확인하고, 흘려보낸 바로 그 사람이 재지목됐을 때 회복되는지는 이 phase의 어떤 시험도 확인하지 않는다(12.3-REVIEW.md CR-01이 이미 지적, 이번 검증이 재확인)"
      - "그 사람이 흘려보내지기 전에 항목을 하나라도 냈던 경우(`unfinished_candidates`에 남는 경우)에 대한 회귀 시험 — 이 경로는 `wrap_up_creation`의 영구 409 거절로 이어져 파티 전원이 막힌다"
deferred: []
behavior_unverified_items:
  - truth: "이미 동의를 누른 다른 사람이 있는 상태에서, 한 사람의 「고칠 게 있어요」 재오픈이 그 다른 사람의 동의 표시도 함께 초기화해 그 사람의 다른 탭 화면에도 반영된다 (D-03/D-11, 교차 전파 — 화면 쪽 절반)"
    test: "실제로 서로 다른 브라우저(또는 사생활 보호 창) 둘 이상으로 같은 세션에 들어가, 한쪽이 동의를 누른 뒤 다른 쪽이 자기 항목을 재오픈했을 때 먼저 동의한 쪽의 화면에서 동의 표시가 다시 「안 누름」으로 바뀌는지 확인"
    expected: "동의를 이미 누른 사람의 화면도 다른 탭에서 발생한 재오픈에 따라 자동으로 동의 표시가 초기화된다"
    why_human: "12.3-08 Task 3이 이 항목의 서버 쪽 절반(폴링 응답 자체가 두 캐릭터 모두의 `consented`를 되돌리는가)을 `test_one_persons_reopen_clears_every_other_characters_consent_in_the_polled_state`로 회귀 그물에 고정했다 — 이번 검증이 재실행해 통과를 확인했다. 그러나 화면 쪽 절반(실제로 다른 사람의 화면에 그려지는가)은 여전히 QA 도구(단일 크로미움 프로필)의 구조적 제약 — 서명 쿠키와 localStorage가 프로필 전체에 전역 공유되고, 캐릭터 완성 시점마다 서버가 새 쿠키를 발급해 이전 캐릭터의 쿠키를 덮어쓴다 — 때문에 실제 별도 브라우저 없이는 재현이 안 된다"
coincidental_reliance_items: []
---

# Phase 12.3: 캐릭터 만들기 화면 Verification Report

**Phase Goal:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 브라우저만으로 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다.
**Verified:** 2026-08-21 (재검증, 4차)
**Status:** gaps_found
**Re-verification:** Yes — 3차 검증이 재현으로 확인한 CR-01(유령이 진짜 참가자를 영구히 밀어냄)을 12.3-08이 닫았는지 확인하러 다시 열었다. 확인하는 과정에서, 그 수정을 대상으로 방금 돈 코드 리뷰(`12.3-REVIEW.md`)가 지목한 **같은 번호의 새 CR-01**(흘려보낸 판정이 영원히 안 풀림)을 독립적으로 코드 재실행까지 해서 확인했다.

## 결론 먼저

**3차가 재현한 gap(유령이 정원을 선점해 진짜 참가자를 후보에서 완전히 밀어냄)은 진짜로 닫혔다.** 12.3-08이 「신원을 검증하는 대신 자격을 회수 가능하게 만든다」는 원칙으로 `forfeited_nominee`(회복 판정)와 `present_candidates`의 새 정렬 규칙(흘려보낸 식별자를 뒤로 민다)을 더했다. 이번 4차 검증이 관련 코드(`creation_state.py`·`routes_events.py`)를 직접 읽고, `tests/test_creation_screen_state.py`·`tests/test_creation_consent_host.py`(43건)를 직접 재실행해 통과를 확인했다 — 정원 2인 세션에 유령 둘이 먼저 와도 5분 뒤에는 결국 진짜 참가자 둘이 후보 전부가 된다.

**하지만 그 수정 자체가, 방금 돈 코드 리뷰(`12.3-REVIEW.md`)가 지목한 새 CR-01만큼 심각한 새 결함을 열어 놓았다 — 그리고 이번 검증이 그것을 소스 한 글자도 안 고친 채 직접 재현해 독립 확인했다.** `_forfeited_at`(누가 차례를 흘려보냈는지 적는 표)는 `(session_id, character_id)` 키로만 조회되고 **어느 지목 사건에 대한 판정인지는 전혀 안 본다.** 이 표를 지우는 코드는 소스 전체 어디에도 없다. 그 결과, GM이 한 번 흘려보낸 것으로 판정된 사람을 **새로** 지목해도(전혀 다른 지목 사건, 다른 순번) `forfeited_nominee`는 그 새 지목을 평가조차 안 하고 즉시 '흘려보냄'으로 되돌린다 — 그 캐릭터는 **세션이 끝날 때까지 다시는 자기 차례를 화면에서 못 받는다.**

이건 유령에게만 일어나는 일이 아니다. **진짜 참가자**가 정당한 이유(자리 비움, 긴 서술 작성 중 네트워크 지연)로 5분간 항목을 하나도 못 내 한 번 흘려보내지면, 다른 후보가 소진돼 GM이 그 사람을 다시 지목하는 순간부터 그 사람은 영구히 차례를 못 받는다 — 그리고 항목을 하나라도 냈던 적이 있다면 `unfinished_candidates`에 영원히 남아 **파티 전원의 명단 잠금까지** 막는다.

**이것은 12.3-08 계획서 자신이 `must_haves.prohibitions`에 명시적으로 적어 둔 원칙 — 「회복이 영구 배제로 바뀌지 않는다 — 어떤 참가자도 자기 파티에서 영영 캐릭터를 못 만드는 상태에 빠지지 않는다」 — 을 정면으로 어긴다.** 3차가 닫은 gap(유령이 첫 순간에 밀어냄)보다 이번 CR-01이 특별히 나쁜 이유는, 이번 것은 **회복 장치 자체가 회복을 영구히 막는다**는 점이다 — 우회할 방법이 화면에도 서버에도 없다.

**목표는 아직 완전히는 성립하지 않는다.** CHAR-06을 완료로 표시할 수 없다.

## 4차 검증(이번) 방법

1. `12.3-VERIFICATION.md`(3차본, 이번에 교체됨)를 읽어 3차가 남긴 실패 하나(구 CR-01, T-12.3-17 신원 미검증)와 `missing` 세 항목을 파악했다.
2. `12.3-08-PLAN.md`(`must_haves.truths`·`must_haves.prohibitions` 포함)와 `12.3-08-SUMMARY.md`를 전부 읽어 「무엇을 고쳤다고 주장하는지」를 파악했다.
3. 방금 돈 `12.3-REVIEW.md`(status: issues_found, critical 2건)를 읽어 CR-01(신규, 12.3-08의 새 코드를 대상)과 CR-02(옛 CR-01, 미해결로 재확인됨)를 파악했다.
4. `src/gptrpg/web/creation_state.py` 전문(293행)을 직접 읽어 `forfeited_nominee`·`present_candidates`·`_forfeited_at`의 실제 동작을 리뷰의 주장과 대조했다 — 158행의 조기 반환이 `nominated_seq`를 비교하지 않는다는 것을 직접 확인했다.
5. `grep -rn "_forfeited_at" src/ tests/ frontend/`로 이 표를 지우는 코드가 소스 전체에 하나도 없다는 것을 재확인했다(쓰기 190행 1곳, 읽기 158·239행 2곳뿐).
6. **CR-01을 소스 변경 없이 직접 재현**했다 — `forfeited_nominee`를 순수 함수처럼 두 번 호출해, 같은 사람에 대한 **새 지목(다른 seq)** 이 옛 흘려보냄 판정을 그대로 물려받는 것을 확인했다(아래 §CR-01 독립 재현 참조).
7. `routes_events.py`의 `_creation_current_speaker_id`와 `frontend/src/session/creationView.ts`의 `canEdit`/`isMyTurn`, `frontend/src/panes/CreationPane.tsx`의 `stepEditingEnabled`를 직접 읽어, 서버의 영구 `null`이 화면의 영구 편집 불가로 이어지는 배선을 확인했다.
8. `.venv/bin/python -m pytest tests/test_creation_screen_state.py tests/test_creation_consent_host.py -q --tb=short`(43건)와 `.venv/bin/python -m pytest -q --tb=short`(전체 1378건)를 직접 실행해 통과를 확인했다 — 회귀는 없다.
9. `git diff --name-only d8e787b HEAD -- frontend`, `-- src/gptrpg/event_log src/gptrpg/rules_core src/gptrpg/session_actor`, `-- src/gptrpg/web/routes_creation.py`를 실행해 12.3-08이 스스로 선언한 스코프 경계(화면·사건기록·리듀서·액터·`routes_creation.py` 불변)가 실제로 지켜졌는지 확인했다 — 전부 빈 출력, 지켜짐.
10. 이번 검증이 다룬 변경 파일 4개(`creation_state.py`·`routes_events.py`·`test_creation_screen_state.py`·`test_creation_consent_host.py`)에 TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커를 재확인했다 — 0건.

## CR-01 독립 재현 (이번 검증이 소스 무변경으로 직접 실행)

```python
# src/gptrpg/web/creation_state.py의 forfeited_nominee/latest_nomination을 그대로 호출
sid = "cr01-repro-session"
creation_state.mark_character_present(sid, "real-1")
state = GameState(session_id=sid, party_size_fixed=1)
state = dataclasses.replace(state, creation_gm_said={
    "n1": CreationGmLineFold(seq=1, kind="nominate", say="...", target_character_id="real-1"),
})

forfeited_nominee(state, sid)                    # -> None (관측 시계 처음 맞춤)
# 시계만 NOMINATION_IDLE_S 넘게 되감는다 (값은 그대로)
creation_state._nomination_watermark[("cr01-repro-session", "real-1")] = (1, time.monotonic() - 305)
forfeited_nominee(state, sid)                    # -> "real-1"  (흘려보냄으로 판정됨, 정상)

# GM이 같은 사람을 "새" 지목(seq=2, 전혀 다른 지목 사건)으로 다시 지목한다
state2 = dataclasses.replace(state, creation_gm_said={
    "n1": CreationGmLineFold(seq=1, kind="nominate", say="...", target_character_id="real-1"),
    "n2": CreationGmLineFold(seq=2, kind="nominate", say="...", target_character_id="real-1"),
})
latest_nomination(state2)          # -> ("real-1", 2)  — 새 지목이 맞다
forfeited_nominee(state2, sid)     # -> "real-1"        — 새 지목을 한 번도 안 보고 즉시 다시 흘려보냄
```

**실제 실행 결과(2026-08-21, 이번 검증이 직접 돌림):**
```
latest_nomination (1st): ('real-1', 1)
forfeited_nominee before any time passes: None
forfeited_nominee after idle: real-1
latest_nomination (fresh, seq=2): ('real-1', 2)
forfeited_nominee for the FRESH nomination: real-1
=> real-1 permanently excluded even after a brand-new nomination: True
```

**새 지목(seq=2)이 왔는데도 `forfeited_nominee`는 그 사실을 전혀 반영하지 못하고 즉시 다시 "흘려보냄"을 돌려준다.** `routes_events._creation_current_speaker_id`가 이 값을 그대로 폴링의 `creation_current_speaker_id: null`로 옮기므로, `real-1`(유령이 아니라 정원 1명 세션의 **유일한 진짜 참가자**로 지어낸 시나리오다)은 다시는 차례를 못 받는다 — 파티가 영구 교착에 빠진다.

`tests/test_creation_screen_state.py`에서 12.3-08이 더한 시험 넷을 확인한 결과, 이 시나리오(같은 사람이 새 지목으로 재지목되는 경우)를 검증하는 시험은 **하나도 없다**:
- `test_a_stalled_nomination_is_released_and_the_next_one_lands_on_someone_else` — "**다른** 사람에게" 넘어가는 것만 확인
- `test_ghost_presence_signals_do_not_permanently_push_out_real_participants` — 유령 둘이 흘려보내지고 진짜 참가자로 넘어가는 것만 확인(흘려보낸 유령이 다시 지목되는 경로는 안 봄)
- `test_a_nominee_that_keeps_submitting_values_never_loses_the_turn` — 애초에 안 흘려보내지는 경로
- `test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately` — 표가 비었을 때(재시작) 경로, `_forfeited_at`에 이미 값이 있는 경우가 아님

`12.3-REVIEW.md`의 CR-01이 코드 읽기로 짚은 결론(158행 조기 반환이 `nominated_seq`를 안 본다, `_forfeited_at`를 지우는 코드가 없다, 두 결함이 겹치면 파티 전원의 명단 잠금까지 막힌다)과 이번 재현의 결과가 정확히 일치한다.

## Goal Achievement

### Observable Truths

| # | Truth | Status (3차) | Status (4차, 이번) | Evidence |
|---|-------|------|------|----------|
| 1 | 세션 링크를 열면 캐릭터 고르기 화면 없이 만들기 화면이 바로 뜬다 (D-10) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `frontend/` 무변경 확인(`git diff` 빈 출력) — 회귀 위험 없음 |
| 2 | 방장이 화면에서 인원을 정할 수 있다 (D-11) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 상동 |
| 3 | GM의 안내·지목·되묻기·정리가 사건 기록에 남아 다른 탭도 폴링으로 본다 (D-02) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 상동 |
| 4 | 같은 시점 재호출에 AI를 다시 부르지 않는다 (D-12) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `routes_creation.py` 무변경 확인 |
| 5 | 방장 브라우저 식별자가 폴링 events 배열에 새지 않는다 (T-12.3-05) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 상동 |
| 6 | GM이 첫 자기소개 순서를 지목해 항목 확정 화면이 열린다. 정상 경로에서 두 번째·세 번째도 이어진다 (D-06) | ✓ VERIFIED | ✓ VERIFIED (정상 경로, 변경 없음) | 12.3-07 라이브 증거 유지. **주의:** 이 truth는 "정상(비흘려보냄) 경로"만 다룬다 — 흘려보낸 뒤 재지목되는 경로는 아래 #20(신규 gap)이 별도로 다룬다 |
| 7 | 「안내」 버튼이 안내 전에 사라지지 않는다 (CR-02) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 8 | 주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07) | ✓ VERIFIED (사람 판단 잔존) | ✓ VERIFIED (변경 없음) | 12.3-07 라이브 증거 유지, 「정말 같아 보이는가」는 사람 확인 목록에 잔존 |
| 9 | 남의 차례에도 말이 항상 열려 있고, 확정 조작만 자기 차례에 뜬다 (D-08) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 상동 |
| 10 | 동의 관문이 이름까지 보여주고, 자기 항목을 재오픈하면 자기 동의가 초기화된다 (D-03, 자기 자신) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 상동 |
| 11 | 다른 사람이 이미 동의한 상태에서, 한 사람의 재오픈이 그 다른 사람의 동의 표시도 초기화하고 다른 탭에도 반영된다 (D-03/D-11, 교차 전파 — 화면 쪽) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ **PRESENT_BEHAVIOR_UNVERIFIED (서버 쪽 절반은 이번에 시험으로 고정됨)** | 12.3-08 Task 3이 `test_one_persons_reopen_clears_every_other_characters_consent_in_the_polled_state`로 서버 쪽 절반(폴링 응답이 두 캐릭터 모두를 되돌림)을 회귀 그물에 고정 — 이번 검증이 재실행해 통과 확인. 화면 쪽 절반은 여전히 QA 도구 구조적 제약으로 미도달 |
| 12 | 방장이 조용해지면 다른 브라우저가 이어받는다 (D-11) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 13 | AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 다른 문구로 갈린다 (D-13) | ✓ VERIFIED (문구만, 헷갈림은 사람 판단) | ✓ VERIFIED (변경 없음) | 상동 |
| 14 | `CHAR-06`이 `REQUIREMENTS.md`·`ROADMAP.md`에 존재한다 | ✓ VERIFIED | ✓ VERIFIED | `grep CHAR-06 REQUIREMENTS.md` 재확인 — 여전히 의도적으로 미체크(`- [ ]`) |
| 15 | 가짜(유령) 재실 신호가 진짜 참가자를 지목 후보에서 영구히 밀어내지 않는다 (T-12.3-17) | ✗ FAILED | ✓ **VERIFIED (닫힘)** | `test_ghost_presence_signals_do_not_permanently_push_out_real_participants` 재실행 통과. `present_candidates`의 새 정렬 규칙(흘려보낸 식별자를 시각순으로 뒤로 민다)을 코드로 직접 대조. 정원 2에 유령 둘이 먼저 와도, 유령들이 5분 뒤 흘려보내지면 진짜 참가자 둘이 후보 전부가 됨을 확인 |
| 16 | 자기 차례에 항목 값을 계속 내고 있는 사람은 그 차례를 절대 안 뺏긴다 | (12.3-08 신규) | ✓ VERIFIED | `test_a_nominee_that_keeps_submitting_values_never_loses_the_turn` 재실행 통과. 코드 확인: 값 제출마다 `nomination_progress_seq`가 커져 `mark`가 바뀌므로 워터마크가 다시 맞춰진다 |
| 17 | 서버를 재시작한 직후에는 어떤 지목도 즉시 안 풀린다 (T-12.3-13과 같은 규율) | (12.3-08 신규) | ✓ VERIFIED | `test_a_restart_of_the_watermark_table_never_releases_a_nomination_immediately` 재실행 통과. 코드 확인: 워터마크가 비어 있으면(재시작 직후) 판정 대신 시계를 처음부터 다시 잰다(158행 조기 반환 이전에 이 경로가 먼저 걸린다 — `key not in _forfeited_at`이므로) |
| 18 | `present_candidates`의 docstring이 실제로 막는 범위(개수)만 적고, 신원 쪽은 `forfeited_nominee`를 가리킨다 | (12.3-08 신규) | ✓ VERIFIED | `grep -c "위조 방지" creation_state.py` → 0. `grep -c "T-12.3-17"` → 1(번호는 유지). docstring 직접 읽어 "상한이 막는 범위는 개수뿐" 문구와 `forfeited_nominee` 언급을 확인 |
| 19 | 한 사람의 재오픈이 다른 캐릭터의 `consented`를 폴링 응답에서 되돌린다는 것이 회귀 시험으로 고정된다 (서버 쪽 배선) | (12.3-08 신규) | ✓ VERIFIED | `test_one_persons_reopen_clears_every_other_characters_consent_in_the_polled_state` 재실행 통과 |
| 20 | **[최우선 신규]** 회복이 영구 배제로 바뀌지 않는다 — 흘려보낸 것으로 판정된 식별자가 새 지목(다른 seq)을 받으면 그 새 지목에 대해 다시 회복 판정을 받는다 (12.3-08-PLAN.md 자신의 `must_haves.prohibitions`, 12.3-REVIEW.md CR-01) | (해당 없음 — 12.3-08이 처음 만든 코드에 대한 신규 발견) | ✗ **FAILED (BLOCKER)** | `forfeited_nominee`를 소스 무변경으로 직접 재현 — 새 지목(seq=2)이 옛 판정(seq=1 시점)을 그대로 물려받아 즉시 다시 흘려보냄으로 판정됨(§CR-01 독립 재현 참조). `_forfeited_at`를 지우는 코드가 소스 전체에 없음을 grep으로 확인. 이 truth를 검증하는 시험이 12.3-08의 신규 시험 넷 중 하나도 없음을 확인 |

**Score:** 18/20 verified (1 present-but-behavior-unverified, 1 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/gptrpg/web/creation_state.py` (`latest_nomination`, `nomination_progress_seq`, `forfeited_nominee`, `present_candidates`) | 회복 판정 장치 + 정렬 규칙 | ⚠️ **HOLLOW (회복이 영구 배제로 새는 경로가 있음)** | 세 함수 모두 존재하고 배선도 되어 있으며, "다른 사람에게 넘어가는" 정상 경로와 "값을 계속 내면 안 뺏긴다"는 경계는 실제로 지켜진다 — 그러나 "같은 사람이 새로 지목되면 다시 기회를 받는다"는 계획 자신의 원칙을 실제로는 안 지킨다. CR-01 재현으로 확인 |
| `src/gptrpg/web/routes_events.py` (`_creation_current_speaker_id`) | `creation_state` 위임 | ✓ VERIFIED (배선) | `grep -c "for fold in game_state.creation_gm_said.values()" routes_events.py` → 0(복제 안 함), `creation_state.latest_nomination(`·`creation_state.forfeited_nominee(` 호출 각각 존재 확인 |
| `tests/test_creation_screen_state.py` | 신규 시험 넷 | ✓ VERIFIED (라이브 재실행, 그러나 CR-01 경로는 미포함) | `.venv/bin/python -m pytest tests/test_creation_screen_state.py tests/test_creation_consent_host.py -q` → 43 passed, 이번 검증이 직접 실행. 4개 시험 모두 존재 확인. 다만 "흘려보낸 바로 그 사람이 재지목됐을 때"를 다루는 시험은 이 넷 중 없다(CR-01 근거) |
| `tests/test_creation_consent_host.py` (`test_one_persons_reopen_clears_every_other_characters_consent_in_the_polled_state`) | 교차 전파 서버 쪽 배선 회귀 시험 | ✓ VERIFIED | 존재 확인 + 재실행 통과 |
| `frontend/*` | 이번 계획에서 무변경 | ✓ VERIFIED | `git diff --name-only d8e787b HEAD -- frontend` 빈 출력 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `ClaimHostRequest.character_id`(재실 신호) | `present_candidates`(후보 상한 정렬) | `mark_character_present` → `forfeited_nominee`(demote) | ✓ **WIRED and FIXED (3차 gap 닫힘)** | 유령이 정원을 영구히 선점하는 경로는 이제 없다 — 5분 뒤 뒤로 밀리고 진짜 참가자가 들어온다 |
| `nominate_creation_speaker`(흘려보낸 사람이 다른 후보로 대체됨) | 다음 지목 | `_gm_dedupe_key`(바뀐 후보 목록) | ✓ WIRED and LIVE | `test_a_stalled_nomination_is_released_and_the_next_one_lands_on_someone_else` 통과로 확인 |
| **흘려보낸 사람이 새로 재지목됨(다른 seq)** | **회복 판정 재평가** | `forfeited_nominee`의 `key in _forfeited_at` 조기 반환 | ✗ **BROKEN (신규 발견, 재현으로 확인)** | 새 지목이 옛 판정을 그대로 물려받는다 — 회복 시계가 다시 안 돈다. 이 링크가 이번 gaps_found의 본체다 |
| `ReopenCreationStep`(한 사람) | 다른 캐릭터의 `consented`(폴링) | `creation_consents={}` 통째 초기화 | ✓ WIRED (시험으로 신규 고정) | `test_one_persons_reopen_clears_every_other_characters_consent_in_the_polled_state` — 서버 쪽 배선만, 화면 쪽은 여전히 사람 확인 필요 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `forfeited_nominee`가 새 지목(다른 seq)을 받아도 옛 흘려보냄 판정을 그대로 돌려준다 (CR-01) | 스크래치 스크립트에서 `forfeited_nominee`/`latest_nomination` 직접 호출(소스 무변경) | `real-1`이 새 지목(seq=2)에서도 즉시 흘려보냄으로 판정됨 | ✗ **FAIL (결함 재현)** |
| `_forfeited_at`를 지우는 코드가 소스 어디에도 없다 | `grep -rn "_forfeited_at" src/ tests/ frontend/` | 쓰기 1곳(190행), 읽기 2곳(158·239행), 삭제 0곳 | ✗ **FAIL (결함 뒷받침)** |
| `test_ghost_presence_signals_do_not_permanently_push_out_real_participants`가 실제로 3차 gap을 닫는다 | `.venv/bin/python -m pytest tests/test_creation_screen_state.py::test_ghost_presence_signals_do_not_permanently_push_out_real_participants -q` | `1 passed` | ✓ PASS |
| `test_creation_screen_state.py` + `test_creation_consent_host.py` 전체 | `.venv/bin/python -m pytest tests/test_creation_screen_state.py tests/test_creation_consent_host.py -q --tb=short` | `43 passed` | ✓ PASS |
| 백엔드 전체 시험 | `.venv/bin/python -m pytest -q --tb=short` | `1378 passed` | ✓ PASS (회귀 없음) |
| 12.3-08이 변경한 파일에 debt 마커 | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` (4개 파일) | 0건 | ✓ PASS |
| 12.3-08이 선언한 스코프 경계(frontend·event_log·rules_core·session_actor·routes_creation.py 불변) | `git diff --name-only d8e787b HEAD -- {각 경로}` | 전부 빈 출력 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CHAR-06 | 12.3-01~08 (공유) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ✗ BLOCKED | `.planning/REQUIREMENTS.md`에 여전히 의도적으로 미체크(`- [ ]`) — `12.3-08-SUMMARY.md`도 스스로 `requirements.mark-complete`를 호출하지 않기로 판단했다고 명시. 이번 검증이 그 판단이 옳음을 확인했다: 정상 경로와 유령-선점 경로는 이번에 닫혔지만, 그 수정 장치 자체(`forfeited_nominee`)가 진짜 참가자를 영구히 배제할 수 있는 새 경로(CR-01)를 열어 놓았다 |

**Orphaned requirements:** 없음 — 1~4차와 동일.

### Anti-Patterns Found

이번 검증이 재확인한 12.3-08 변경 파일(`creation_state.py`, `routes_events.py`, `test_creation_screen_state.py`, `test_creation_consent_host.py`)에 TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커 0건.

`12.3-REVIEW.md`가 함께 지적한 **WR-01**(`frontend/src/screens/CreationScreen.tsx:89-124`, 인원 확정 입력창에 빈 값 방지 클라이언트 검증이 없어 일반 오류 문구로 떨어짐)은 이번 계획(`12.3-08`)이 건드리지 않은 기존 코드이고, 경미한 UX 문제(서버가 최종 방어선으로 이미 422를 낸다 — 사람이 다시 입력하면 그만이다)라 CHAR-06 달성 자체를 막지는 않는다. gap으로 올리지 않고 여기 기록만 남긴다.

**CR-01(신규, 회복 영구 배제)은 anti-pattern이 아니라 §Goal Achievement의 정식 gap(#20)으로 이미 반영했다** — 코드 냄새가 아니라 계획 자신의 명시적 금지 조항을 어기는, 재현 가능한 기능 결함이기 때문이다.

### Human Verification Required

#### 1. 동의 관문의 교차 전파, 화면 쪽 절반 (behavior_unverified — 여전히 최우선)

**Test:** 실제 별도 브라우저(또는 사생활 보호 창) 둘 이상으로 같은 세션에 들어간다. 한쪽이 동의를 누른 뒤, 다른 쪽이 자기 항목 하나를 「고칠 게 있어요」로 다시 연다.
**Expected:** 먼저 동의를 눌렀던 사람의 화면에서도 동의 표시가 자동으로 「안 누름」으로 되돌아간다.
**Why human:** 12.3-08 Task 3이 서버 쪽 배선(폴링 응답이 실제로 두 캐릭터 모두를 되돌린다는 것)을 시험으로 고정했지만, QA 도구(단일 크로미움 프로필)가 서명 쿠키와 `localStorage`를 탭 전체에 전역 공유해 두 사람이 동시에 각자 유효한 인증 상태를 가질 수 없다 — 실제 별도 브라우저가 필요하다.

#### 2. 주사위 연출·D-13 세 문구·12.3-05의 14개 체크리스트 — 3차와 동일, 아직 미완주

이번 계획이 화면을 전혀 안 건드렸으므로 3차 검증이 남긴 나머지 사람 확인 항목(주사위 연출이 판정 화면과 정말 같아 보이는가, D-13 세 문구가 헷갈리지 않는가, `12.3-05-SUMMARY.md`의 14개 체크리스트 — 특히 2·7·12·13번)은 이번에도 그대로 남는다. 반복해 옮기지 않는다 — `12.3-VERIFICATION.md`(3차본, 이번에 교체됨)와 `12.3-05-SUMMARY.md`를 참조할 것.

#### 3. `NOMINATION_IDLE_S`(5분)가 실제로 답답하지 않은가

**Test:** 실제로 지목된 뒤 아무것도 안 내고 기다려 본다.
**Expected:** 5분 대기가 게임 흐름을 심하게 끊지 않는다고 느껴진다.
**Why human:** 이 맞바꿈의 값은 실제로 한 번 겪어봐야 판단이 선다(상수 하나만 바꾸면 조정 가능, `12.3-08-PLAN.md` <verification> 참조). CR-01이 먼저 닫히지 않으면 이 항목은 어차피 의미가 없다 — 흘려보낸 뒤 재지목되면 5분이 아니라 영원히 안 풀리기 때문이다.

### Gaps Summary

**3차가 재현한 gap(유령이 정원을 선점해 진짜 참가자를 영구히 밀어냄)은 진짜로 닫혔다.** 12.3-08의 forfeit-and-demote 방식이 실제로 작동함을 이번 검증이 코드 대조와 시험 재실행으로 독립 확인했다.

**그런데 그 수정 자체가, 계획서 자신이 명시적으로 금지한 새 결함을 열어 놓았다.** `_forfeited_at`가 지목 사건의 순번(seq)이 아니라 캐릭터 식별자만으로 조회돼, 한 번 흘려보낸 것으로 판정된 사람은 GM이 그 사람을 **새로** 지목해도 그 새 지목을 한 번도 평가받지 못하고 영원히 회복 불가 상태로 남는다. 이 표를 지우는 코드는 소스 어디에도 없다. 이번 검증이 소스를 안 고친 채 이 결함을 직접 재현했고, 12.3-08의 신규 회귀 시험 넷 중 이 정확한 시나리오(흘려보낸 바로 그 사람이 재지목되는 경우)를 다루는 것이 하나도 없음을 확인했다.

**이것은 이론적 위험이 아니다.** 유령뿐 아니라 자리를 잠시 비웠거나 긴 서술을 쓰느라 5분을 넘긴 **진짜 참가자**도 걸릴 수 있고, 걸리면 그 사람은 세션이 끝날 때까지 다시는 자기 캐릭터를 완성할 방법이 화면에 없다 — 항목을 하나라도 냈던 적이 있다면 파티 전원의 명단 잠금까지 막는다. 이건 12.3-08-PLAN.md의 `must_haves.prohibitions`에 적힌 「회복이 영구 배제로 바뀌지 않는다」는 원칙을 정면으로 어기는 것이고, `12.3-REVIEW.md`의 CR-01이 코드 읽기로 짚은 것과 이번 검증의 독립 재현이 정확히 일치한다.

**남은 사람 확인도 아직 끝나지 않았다.** 동의 관문의 교차 전파(화면 쪽)는 이번에도 QA 도구의 구조적 한계로 라이브 확인이 안 됐고, `12.3-05-SUMMARY.md`의 14개 체크리스트도 아직 한 번도 처음부터 끝까지 순서대로 완주되지 않았다.

**CHAR-06은 이번에도 완료로 표시하지 않는다** — 위 CR-01(gap #20)을 먼저 닫고, 남은 사람 확인 항목이 실제로 끝난 뒤 사람이 판단할 일이다.

---

_Verified: 2026-08-21 (재검증, 4차)_
_Verifier: Claude (gsd-verifier)_
