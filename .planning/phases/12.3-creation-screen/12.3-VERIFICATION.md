---
phase: 12.3-creation-screen
verified: 2026-08-22T18:20:00Z
status: gaps_found
score: 28/29 must-haves verified
behavior_unverified: 6
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 21/22
  gaps_closed:
    - "능력치 배치에서 값이 중복된 풀(예: 2, 1, 1, 0, 0, -1)이 여섯 칸을 전부 못 채워 「확정」이 영원히 안 켜지던 blocker (G-12.3-6) — 12.3-12가 잠금 판정을 값의 집합(Set)에서 개수 기반 순수 함수(remainingFixedValues)로 갈아 끼우고, 값이 중복된 풀을 여섯 칸에 전부 배정해 확정까지 가는 회귀 시험 3건을 추가했다. 이번 검증이 코드를 직접 읽어 `new Set`이 이 컴포넌트 범위에서 사라지고 `remainingFixedValues` 하나로 잠금을 판정하며 선택지 key가 자리(index) 기준으로 바뀐 것을 재확인했다"
    - "인원 확정 조작의 초기값이 룰북 최소값을 몰라 이미 거절당할 값(1)으로 떠 있던 결함 (G-12.3-2) — 12.3-13이 `GET /creation/steps`를 배열에서 `{steps, party_size_range}` 봉투로 넓혀 인원 확정 전에도 룰북의 실제 범위가 화면에 도달하게 했다. `PartySizeControl`이 이제 서버 범위로 초기값·min·max·확정 잠금을 세운다(순수 함수 `partySizeOutOfRange`가 서버 판정의 거울). 화면 코드에 룰북별 숫자가 0건임을 grep으로 재확인했다"
    - "인원 거절 문구에 파이썬 dataclass repr(`allowed=PartySizeRange(...)`)이 그대로 새던 결함 (G-12.3-3, D-15) — 12.3-13이 `actor.py::_prepare_fix_party_size`를 `CommandRejected(str(exc))`에서 `CommandRejected(exc.reason)`으로 한 줄 교체했다. 이번 검증이 `exc.reason`이 실제로 그 메서드 범위에 정확히 1건, `str(exc)`가 0건임을 재확인했다 — 이 결함은 pytest가 실제로 예외를 발생시켜 문자열을 단언하므로 서버 쪽 절반은 행위 증거로 VERIFIED다"
    - "성공한 확정 뒤에도 이전 거절 문구가 화면에 계속 남던 결함 (G-12.3-4) — 12.3-13이 `PartySizeControl`의 새 시도 시작 두 자리(`confirm()` 진입·숫자 변경)에서 `onError(null)`을 호출하도록 배선했다. 이번 검증이 그 컴포넌트 범위에서 `onError(null)`이 정확히 2건임을 재확인했다"
    - "GM의 안내가 없을 때 「다른 사람의 차례예요」·「불러오는 중…」·「진행자에게 안내를 부탁하기」 세 신호가 동시에 뜨고, 그중 둘이 거짓이던 결함 (G-12.3-5, D-15) — 12.3-14가 `shouldAnnounce` 순수 함수(시험 5건)로 「지금 첫 안내를 부를 때인가」를 판정하고, 화면이 그 함수 하나로 스스로 `POST /creation/announce`를 부르며, 안내가 없는 동안 렌더 갈래 순서를 뒤집어 한 블록만 그리게 했다. 이번 검증이 `shouldAnnounce`가 effect 안 한 곳에서만 불리는 것과, 발동 조건 네 값이 effect 의존성 목록에 전부 있다는 SUMMARY의 대조표를 코드와 직접 대조해 확인했다"
    - "누구 차례인지 이름을 안 말해 「이 차례의 순서를 모르겠다」는 혼란을 낳던 결함 (G-12.3-7) — 12.3-14가 `creationTurn`(판별 유니온, 시험 5건)과 `creationTurnLabel`을 더해 이름과 「순서는 진행자가 정해요」(D-06)를 한 문장으로 조립하고, 대화판 첫 줄이 그 결과를 그린다(지목 전에는 줄 자체를 안 그린다). 이름이 없을 때 식별자(`pc-`)를 대신 보여 주지 않는다는 것도 grep으로 재확인했다"
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "REQUIREMENTS.md의 CHAR-06 완료 표시가 실제 검증 상태와 일치한다 — 이 phase의 목표 문장 자체가 요구하는 「사람이 브라우저만으로 처음부터 끝까지」는 아직 사람이 확인하지 않았다"
    status: failed
    reason: |
      `.planning/REQUIREMENTS.md:117`이 `- [x] **CHAR-06**`으로, `:359`가 `| CHAR-06 | Phase 12.3 | Complete |`로 바뀌어 있다(커밋 `c0b6b3f`, 12.3-14-SUMMARY.md의 `requirements-completed: [CHAR-06]`을 GSD 마감 절차가 자동으로 반영한 결과 — `requirements.ready-ids`가 「이 요구사항을 선언한 계획 전부가 실행 완료됐다」를 「요구사항이 달성됐다」로 오인했다).

      그런데 이 phase의 목표 문장과 `12.3-UAT.md`의 `## Current Test` 절이 지금 이 순간에도 요구하는 것은 사람이 실제 기기로 처음부터 끝까지 화면 조작만으로 캐릭터 만들기를 완주하는 것이다. `12.3-UAT.md`의 `## Summary`는 여전히 `total: 9, passed: 1, issues: 1, pending: 7`이고, 세 gap-closure 계획이 코드 레벨에서 닫은 것은 테스트 2를 막던 여섯 결함이지 테스트 2 자체의 재실행이 아니다 — `## Current Test`가 그 문장 그대로 "**사람이 실제 기기 셋 이상으로 테스트 2부터 다시 걷는다.**"라고 적고 있다.

      6·7차 검증 둘 다 정확히 이 이유로 "CHAR-06은 이번에도 완료로 표시하지 않는다"를 반복해서 명시했다(`git log`의 이전 `12.3-VERIFICATION.md` 버전 참조). 사람이 아직 아무도 걷지 않은 상태에서 요구사항 문서가 `Complete`로 바뀐 것은, 이 프로젝트가 이미 알고 있는 실패 양상(요구사항 조기 완료 표시 — 여러 계획에 걸친 요구사항을 계획 실행 완료와 혼동)의 실제 사례다.
    artifacts:
      - path: ".planning/REQUIREMENTS.md:117"
        issue: "`- [x] **CHAR-06**` — 사람 확인 없이 체크됨"
      - path: ".planning/REQUIREMENTS.md:359"
        issue: "`| CHAR-06 | Phase 12.3 | Complete |` — 매핑 표도 함께 바뀜"
      - path: ".planning/phases/12.3-creation-screen/12.3-14-SUMMARY.md"
        issue: "`requirements-completed: [CHAR-06]`을 선언 — 이 계획 자신의 산출물(코드 레벨 gap 닫기) 범위에 대한 표기였을 뿐인데 마감 절차가 REQUIREMENTS.md의 체크박스를 바꾸는 근거로 그대로 썼다"
    missing:
      - "`.planning/REQUIREMENTS.md:117`을 `- [ ]`로, `:359`를 `Pending`으로 되돌린다"
      - "사람이 `12.3-UAT.md` 테스트 2~8을 실제 기기로 완주해 `## Summary`가 `passed: 9`가 된 뒤에만 CHAR-06을 Complete로 바꾼다"
      - "(선택) GSD 마감 절차가 `verification: backstop`이 달린 `must_haves.truths`(사람 확인 항목)를 가진 요구사항을 `requirements.ready-ids`만으로 자동 완료 처리하지 않도록 하는 별도 개선 — 이 phase의 범위를 넘는 도구 문제이므로 이 gap은 문서 상태 되돌리기만 요구한다"
deferred:
  - truth: "WR-01(끼어들기 입력이 항상 자기 자신을 대상으로 보내 서버에 거절당하는 문제, `CreationPane.tsx`)"
    addressed_in: "사람이 `12.3-UAT.md` 테스트 9를 실기기로 겪은 뒤 방향을 고르는 별도 gap-closure"
    evidence: "12.3-11-PLAN.md·12.3-14-PLAN.md 둘 다 prohibitions에서 D-08(「남의 차례에도 말은 항상 열려 있다」)의 노출 규칙 표면을 건드리는 처방을 명시적으로 미룬다. `12.3-UAT.md` 테스트 9가 `result: pending`으로 남아 있다"
behavior_unverified_items:
  - truth: "능력치 배치에서 값이 중복된 풀도 여섯 칸을 전부 채우고 확정 단추가 실제 <select>에서 켜진다 (G-12.3-6)"
    test: "실제 브라우저에서 던전월드류로 능력치 배치 조작을 열어, 값 `1`을 한 칸에 쓴 뒤 남은 `1`이 다른 칸의 선택지에 여전히 남아 있는지, 여섯 칸을 전부 채운 뒤 「확정」 단추가 실제로 켜지고 눌러서 다음 항목으로 넘어가는지 확인"
    expected: "같은 값이 둘인 풀에서도 여섯 칸을 전부 채울 수 있고, 확정 뒤 다음 항목으로 넘어간다"
    why_human: "`remainingFixedValues`의 산수 자체는 순수 함수 시험 3건이 증명한다. 그 판정이 실제 `<select>`의 `disabled` 속성에 배선돼 사람 손에서 클릭할 때마다 정확히 갱신되는지는 이 저장소에 RTL이 없어 자동으로 실행할 방법이 없다"
  - truth: "인원 확정 조작의 초기값·min·max가 실제 `<input>`에서 서버가 내려준 룰북 범위로 뜬다 (G-12.3-2)"
    test: "던전월드류(최소 3) 세션에서 인원 조작을 열었을 때 처음부터 3 이상의 값이 떠 있는지, 2로 내리려 해도 안 내려가는지 확인"
    expected: "화면을 처음 열자마자 유효한 값이 떠 있어 거절을 만나지 않는다"
    why_human: "`partySizeOutOfRange`가 서버 판정과 같은 규칙임은 순수 함수 시험 4건이 증명한다. `useState(range.min_player_characters)` 등 props가 실제 렌더된 `<input>`의 `value`/`min`/`max`에 반영되는지는 RTL 없이 자동 확인이 안 된다"
  - truth: "인원 확정을 다시 시도하거나 숫자를 고치면 이전 거절 문구가 실제 화면에서 사라진다 (G-12.3-4)"
    test: "2명으로 거절당한 뒤 3명으로 고쳐 확정에 성공했을 때, 화면에 남아 있던 「2명은 최소 3명보다 적다」 문구가 실제로 사라지는지 확인"
    expected: "성공한 확정 뒤 또는 숫자를 바꾼 순간 이전 오류 문구가 안 보인다"
    why_human: "`onError(null)` 호출이 코드에 정확히 2건 있다는 것은 grep과 타입 검사로 확인됐다. 그 호출이 실제 React 상태를 갱신해 화면에서 문구가 사라지는 것은 JSX 지역 상태 전이라 RTL 없이 자동 확인이 안 된다"
  - truth: "인원을 확정한 직후 아무것도 안 누르고 기다리면 첫 안내가 실제로 저절로 온다 (G-12.3-5)"
    test: "인원을 확정한 뒤 어떤 단추도 안 누르고 기다려, 「진행자가 첫 안내를 준비하고 있어요」가 뜨고 잠시 뒤 GM의 첫 안내가 실제로 나타나는지 확인"
    expected: "사람이 아무것도 안 눌러도 안내가 자동으로 온다 — 「기다려야 하나 눌러야 하나」로 헷갈리는 자리가 없다"
    why_human: "`shouldAnnounce`의 다섯 갈래는 순수 함수 시험이 증명하고, 그 함수가 effect 의존성 목록과 어긋나지 않는다는 것은 SUMMARY의 수동 대조표로 확인됐다. 그러나 「조건이 참이 되는 순간을 실제 브라우저의 effect가 실시간으로 포착해 실제 HTTP 요청을 실제로 내는가」는 정적 분석이 아니라 런타임 관찰이 필요하다 — SUMMARY 자신도 이것을 `human_judgment: true`로 표시했다"
  - truth: "지목이 왔을 때 화면만 보고 누구 차례인지 안다 — 이름을 모르면 「아직 이름을 안 정한 분」이 실제로 뜬다 (G-12.3-7)"
    test: "GM이 누군가를 지목했을 때 대화판 첫 줄에 이름(또는 이름 미정 문구)과 「순서는 진행자가 정해요」가 실제로 함께 뜨는지, 아무도 지목되지 않은 동안에는 그 줄 자체가 안 보이는지 확인"
    expected: "차례 문구가 사실과 다르게 뜨는 경우가 없다"
    why_human: "`creationTurn`/`creationTurnLabel`의 다섯 갈래는 순수 함수 시험이 증명한다. 대화판 첫 줄 렌더(`turnLabel !== null ? <p>… : null`)가 실제 화면에서 그 결과대로 나타나는지는 RTL 없이 자동 확인이 안 된다"
  - truth: "이미 동의를 누른 다른 사람이 있는 상태에서, 한 사람의 「고칠 게 있어요」 재오픈이 그 다른 사람의 동의 표시도 함께 초기화해 그 사람의 다른 탭 화면에도 반영된다 (D-03/D-11, 교차 전파 — 화면 쪽 절반, 7차부터 이월)"
    test: "실제로 서로 다른 브라우저(또는 사생활 보호 창) 둘 이상으로 같은 세션에 들어가, 한쪽이 동의를 누른 뒤 다른 쪽이 자기 항목을 재오픈했을 때 먼저 동의한 쪽 화면의 동의 표시도 「안 누름」으로 되돌아가는지 확인"
    expected: "동의를 이미 누른 사람의 화면도 다른 탭에서 발생한 재오픈에 따라 자동으로 동의 표시가 초기화된다"
    why_human: "서버 쪽 절반은 회귀 시험이 고정했다. 화면 쪽 절반은 QA 도구(단일 크로미움 프로필)가 쿠키·`localStorage`를 전역 공유해 자동화 재현이 안 된다. 이번 라운드도 이 truth를 안 건드렸다"
coincidental_reliance_items: []
human_verification:
  - test: "`.planning/REQUIREMENTS.md`의 CHAR-06을 되돌리고, 사람이 실제 기기 셋 이상으로 `12.3-UAT.md` 테스트 2를 처음부터 다시 걷는다 — 인원 확정 → 능력치 배치(중복 값 포함) → 안내 자동 발동 → 차례 문구까지, 이번 세 계획이 코드 레벨로 닫은 여섯 자리가 실제로 이어지는지 확인" (최우선)
    expected: "테스트 2가 `result: pass`로 바뀌고, 이어서 테스트 3~8을 실제 기기로 완주한다. 완주하면 `.planning/REQUIREMENTS.md`의 CHAR-06을 그때 다시 Complete로 바꾼다"
    why_human: "이 라운드가 자동으로 확인한 것은 여섯 개 순수 함수 판정의 산수와 배선의 '모양'까지다. 그 판정이 실제 `<select>`/`<input>`/effect에서 사람 손과 브라우저 타이밍으로 실제로 도는지는 RTL이 없는 이 저장소에서 사람만 확인할 수 있다"
  - test: "WR-01(끼어들기 입력이 남의 차례·내 차례 상관없이 자기 자신을 대상으로 보내지는가)을 실제 기기로 만져 보고 「안 보인다」/「눌러도 거절당한다」 중 실제로 어느 쪽인지 확인한다 — `12.3-UAT.md` 테스트 9"
    expected: "실제 결과에 따라 별도 gap-closure로 닫을지 판단할 근거가 생긴다"
    why_human: "12.3-11·12.3-14 둘 다 D-08 표면을 건드리는 처방을 의도적으로 미뤘다"
  - test: "동의 관문의 교차 전파, 화면 쪽 절반 — 실제 별도 브라우저 둘 이상으로 확인 (7차부터 이월)"
    expected: "먼저 동의를 눌렀던 사람의 화면에서도 동의 표시가 자동으로 「안 누름」으로 되돌아간다"
    why_human: "QA 도구의 구조적 제약(단일 크로미움 프로필의 전역 쿠키·저장소 공유)으로 자동화가 안 된다"
  - test: "회복 시간 5분(`NOMINATION_IDLE_S`)이 실제로 답답하지 않은가 (7차부터 이월)"
    expected: "5분 대기가 게임 흐름을 심하게 끊지 않는다고 느껴지고, 다시 지목받았을 때 실제로 항목을 채울 수 있다"
    why_human: "체감 UX는 사람만 판단할 수 있다"
  - test: "주사위 연출·D-13 세 문구·`12.3-05-SUMMARY.md`의 14개 체크리스트 (이전 라운드부터 이월, 아직 미완주)"
    expected: "`12.3-05-SUMMARY.md`·이전 `12.3-VERIFICATION.md`(git 이력)가 적은 기준대로 사람이 확인"
    why_human: "시각적 판단·UX 체감이라 코드 검증으로 대신할 수 없다"
---

# Phase 12.3: 캐릭터 만들기 화면 Verification Report

**Phase Goal:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 브라우저만으로 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다.
**Verified:** 2026-08-22 (재검증, 8차 — 14개 계획 전체 상태 반영)
**Status:** gaps_found
**Re-verification:** Yes — 7차 검증이 human_needed로 남긴 상태에서, 사람이 `12.3-UAT.md` 테스트 2를 실기기로 걷다가 만난 결함 여섯(G-12.3-2 … G-12.3-7, 그중 G-12.3-6은 blocker)을 닫는 세 gap-closure 계획(12.3-12/13/14)이 이번에 실행됐다. 그 여섯이 코드 레벨에서 실제로 닫혔는지, 그리고 그 사이 `.planning/REQUIREMENTS.md`가 CHAR-06을 사람 확인 없이 `Complete`로 바꾼 것이 옳은지를 확인했다.

## 결론 먼저

**세 계획이 닫겠다고 주장한 여섯 결함은 코드 레벨에서 전부 실제로 닫혔다.** SUMMARY의 서술에 기대지 않고 각 결함의 근본 원인이 있던 자리를 직접 grep·awk로 재확인했다:

- G-12.3-6(blocker): `PlaceFixedValuesControl` 범위에 `new Set` 0건, `remainingFixedValues` 1건, 선택지 key가 `${index}:${value}` — 값의 **집합**이 아니라 **개수**로 잠금을 판정한다.
- G-12.3-2: `CreationScreen.tsx`에 `min_player_characters` 리터럴 하드코딩 0건, `partySizeOutOfRange`가 확정 잠금을 판정, 룰북 미선언 대체값(`ABSOLUTE_MINIMUM_PARTY_SIZE = 1`)이 이름 붙은 상수로 감싸져 있다.
- G-12.3-3: `actor.py::_prepare_fix_party_size` 범위에 `exc.reason` 1건, `str(exc)` 0건 — 이 하나는 pytest가 실제로 예외를 던져 문자열을 단언하므로 **행위 증거로 VERIFIED**다(서버 로직이 실제로 실행됐다).
- G-12.3-4: `PartySizeControl` 범위에 `onError(null)` 정확히 2건(확정 진입·숫자 변경).
- G-12.3-5: `shouldAnnounce` 함수 1건 export, `CreationScreen.tsx`가 그 함수 하나로만 자동 호출을 판단(호출 지점 1곳), 서버 `src/gptrpg` 무변경.
- G-12.3-7: `creationTurn`/`creationTurnLabel` 각 1건 export, `CreationPane.tsx` 대화판 첫 줄이 그 결과를 그리고, `pc-` 식별자 대체 표시 0건.

**그런데 그중 다섯(G-12.3-2/4/5/6/7)은 「사람 손이 실제 `<select>`/`<input>`/effect에서 도는가」를 요구하는 truth다.** 이 저장소에는 RTL이 없어 JSX 지역 상태 전이·effect 발동 타이밍을 자동으로 실행할 방법이 없다는 것은 이 저장소가 스스로 여러 번 문서화한 제약이다(`creationView.ts` 모듈 docstring, `shouldNominateNext` 선례). 순수 함수 시험은 「고를 수 있는가」·「부를 조건이 옳은가」의 산수까지만 답한다. **그 산수가 실제 브라우저에서 실제로 도는지는 아직 아무도 걷지 않았다** — `12.3-UAT.md`의 `## Current Test` 절이 그 문장 그대로 요구하고 있고, `## Summary`도 여전히 `passed: 1, issues: 1, pending: 7`이다. 이 검증은 그 다섯을 ⚠️ PRESENT_BEHAVIOR_UNVERIFIED로 분류하고 인간 확인 목록에 올린다.

**그런데도 `.planning/REQUIREMENTS.md`는 CHAR-06을 `Complete`로 바꿔 놓았다(`- [x]`, 표의 `Complete`).** 대조해 보니 이것은 12.3-14-SUMMARY.md의 `requirements-completed: [CHAR-06]` 선언을 GSD 마감 절차가 자동 반영한 결과다 — 이 phase의 11개 계획 전부가 `requirements: [CHAR-06]`을 선언했고, 마지막 계획(12.3-14)이 끝나자 `requirements.ready-ids`가 "이 요구사항을 선언한 계획이 전부 실행됐다"를 "요구사항이 달성됐다"로 오인했다. **이것은 이 프로젝트가 이미 겪은 실패 양상이다** — 여러 계획에 걸친 요구사항을 계획 실행 완료와 혼동해 조기에 Complete로 찍는 함정. 6차·7차 검증이 정확히 같은 이유로 "CHAR-06은 이번에도 완료로 표시하지 않는다"를 반복해서 명시했는데, 이번에는 사람이 아직 아무도 테스트 2를 다시 걷지 않은 채로 문서가 먼저 Complete가 됐다. **이 검증은 이것을 gap으로 다룬다** — 코드는 옳게 움직였는데 문서가 앞서 나갔다.

**따라서 이번 라운드의 상태는 `gaps_found`다.** 코드에는 새 blocker나 STUB이 없다(6차 검증이 처음 발견한 secure-context 결함과 질적으로 다르다). 유일한 gap은 문서 상태와 실제 검증 상태의 어긋남이고, 되돌리는 데 코드 변경이 필요 없다.

## 8차 검증(이번) 방법

1. `12.3-UAT.md` 전문을 읽어 `## Current Test`가 요구하는 것(사람이 테스트 2부터 다시 걷는다)과 `## Gaps` 절의 여섯 항목(G-12.3-2 … G-12.3-7, 각각 `status: resolved`·`resolved_by`)을 파악했다.
2. `12.3-REVIEW.md`(이번 라운드 범위, 파일 11개)를 읽어 BLOCKER 0·WARNING 2(WR-01 언마운트 가드 없음·WR-02 자동 호출의 AI 중복 호출 확대)·INFO 1(순환 임포트, 무해 확인됨)임을 확인했다.
3. `12.3-12-PLAN.md`/`12.3-13-PLAN.md`/`12.3-14-PLAN.md` 전문(must_haves·prohibitions·key_links·acceptance_criteria 포함)과 세 SUMMARY 전문을 읽어 주장의 정확한 모양을 파악했다.
4. 여섯 결함 각각의 근본 원인이 있던 자리를 SUMMARY 서술에 기대지 않고 직접 grep·awk로 재실행했다(위 「결론 먼저」의 여섯 항목 — 전부 재확인됨, 아래 Required Artifacts 절 참조).
5. `git diff --name-only 231658e..HEAD -- src/gptrpg`가 `actor.py`·`routes_creation.py` 정확히 둘만 내는 것, `-- src/gptrpg/rules_core`가 빈 출력인 것을 재확인했다 — 예외 클래스 자신과 검증 규칙은 안 바뀌었다.
6. 이번 라운드가 바꾼 11개 파일 전부에 `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` 마커가 0건임을 재확인했다(debt marker gate 통과).
7. `.planning/REQUIREMENTS.md`의 CHAR-06 줄(117·359행)을 읽어 `Complete`로 바뀐 것을 발견하고, `git log -- .planning/REQUIREMENTS.md`로 정확히 어느 커밋(`c0b6b3f`, 12.3-14 마감 커밋)이 그것을 바꿨는지 추적했다. `12.3-14-SUMMARY.md`의 `requirements-completed: [CHAR-06]` 선언과 그 근거("`requirements.ready-ids` 조회 결과 1/1 ready")를 대조했다.
8. `12.3-UAT.md`의 `## Summary`(`passed: 1, issues: 1, pending: 7`)와 `## Current Test`(사람이 아직 테스트 2를 안 걸었다는 것을 명시)를 REQUIREMENTS.md의 `Complete` 표시와 대조해 어긋남을 확정했다.
9. `.planning/STATE.md`가 `status: verifying`으로 정확히 기록돼 있음을 확인했다.
10. 각 결함이 요구하는 것이 「고를 수 있는가/부를 조건이 옳은가」의 순수 산수인지, 아니면 「실제 DOM/effect에서 도는가」의 런타임 행위인지를 갈라 behavior-dependent truth 여섯을 식별했다(위 `behavior_unverified_items`).
11. 이 세션이 이미 실행한 `.venv/bin/python -m pytest -q --tb=short`(1385 passed)와 `cd frontend && npx vitest run`(98 passed, 7 files)의 결과를 재실행하지 않고 그대로 인용했다(전체 스위트 반복 실행 회피 규율).

## Goal Achievement

### Observable Truths — 7차부터 이월(변경 없음)

| # | Truth | Status (7차) | Status (8차, 이번) | Evidence |
|---|-------|------|------|----------|
| 1 | 세션 링크(평문 http + 일반 IP)를 열면 캐릭터 만들기 화면이 실제로 뜬다 (CHAR-06, ROADMAP.md 확인 관문) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `browserIdentity.ts` 무변경, `check_insecure_origin.py` exit=0 — 12.3-14 Task 3이 재확인(`.venv/bin/python scripts/check_insecure_origin.py; echo exit=$?` → `exit=0`) |
| 1b | 마운트 도중 무엇이 터져도 빈 화면으로 안 끝난다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `Notices.tsx`의 `ErrorBoundary` 무변경 |
| 2 | 방장이 화면에서 인원을 정할 수 있다 (D-11) | ✓ VERIFIED | ✓ VERIFIED (구현 변경, 세부는 아래 새 truth #24) | `PartySizeControl`이 12.3-13에서 크게 다시 배선됨 — 조작 자체는 여전히 있다 |
| 3 | GM의 안내·지목·되묻기·정리가 사건 기록에 남아 다른 탭도 폴링으로 본다 (D-02) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | 서버 사건 스키마 무변경 |
| 4 | 같은 시점 재호출에 AI를 다시 부르지 않는다 (D-12) | ✓ VERIFIED | ✓ VERIFIED (변경 없음, 단 12.3-REVIEW.md WR-02가 경쟁 빈도 증가를 경고 — 아래 Anti-Patterns 참조) | `_gm_dedupe_key` 무변경 |
| 5 | 방장 브라우저 식별자가 폴링 events 배열에 새지 않는다 (T-12.3-05) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 6 | GM이 첫 자기소개 순서를 지목해 항목 확정 화면이 열린다 (D-06) | ✓ VERIFIED (실기기 미도달) | ✓ VERIFIED (변경 없음) | 라이브 증거 유지 |
| 7 | 「안내」 버튼이 안내 전에 사라지지 않는다 (CR-02) | ✓ VERIFIED | **대체됨 — 아래 새 truth #27이 이어받음** | 12.3-14가 이 단추를 `creationAnnounceRetry`(실패 재시도 전용)로 뜻을 바꾸고 자동 호출을 더했다 — CR-02가 지키던 성질(안내 전에 단추가 안 뜬다)은 구조적으로 더 강해졌다는 것을 SUMMARY가 명시 |
| 8 | 주사위로 채우는 항목이 판정과 같은 DiceModal 연출로 보인다 (D-07) | ✓ VERIFIED (사람 판단 잔존) | ✓ VERIFIED (변경 없음) | 사람 확인 목록에 잔존 |
| 9 | 남의 차례에도 말이 항상 열려 있고, 확정 조작만 자기 차례에 뜬다 (D-08) | ✓ VERIFIED | **대체됨 — 아래 새 truth #28이 이어받음** | 차례 문구 자체가 `creationTurn`/`creationTurnLabel`로 재구성됨. D-08의 「자유롭게 끼어들어 말할 수 있어요」 문구는 `creationTurnSuffix`로 그대로 유지 |
| 10 | 동의 관문이 이름까지 보여주고, 자기 항목을 재오픈하면 자기 동의가 초기화된다 (D-03, 자기 자신) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 11 | 다른 사람이 이미 동의한 상태에서 재오픈이 그 사람 화면에도 반영된다 (D-03/D-11, 교차 전파 — 화면 쪽) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | ⚠️ **PRESENT_BEHAVIOR_UNVERIFIED (변경 없음, 이월)** | 아래 `behavior_unverified_items` 참조 |
| 12 | 방장이 조용해지면 다른 브라우저가 이어받는다 (D-11) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 13 | AI 계약 위반(200 폴백)·설정 없음(503)·연결 끊김이 다른 문구로 갈린다 (D-13) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | `creationErrorMessage` 무변경 |
| 14 | `CHAR-06`이 `REQUIREMENTS.md`·`ROADMAP.md`에 존재한다 | ✓ VERIFIED | ✓ VERIFIED (존재는 유지, 단 완료 표시 자체는 아래 새 truth #29에서 FAILED) | `grep CHAR-06` 존재 확인 |
| 15 | 가짜 재실 신호가 진짜 참가자를 지목 후보에서 영구히 밀어내지 않는다 (T-12.3-17) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 16 | 자기 차례에 값을 계속 내는 사람은 그 차례를 절대 안 뺏긴다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 17 | 서버 재시작 직후 어떤 지목도 즉시 안 풀린다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 18 | `present_candidates` docstring이 실제 범위(개수)만 적는다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 19 | 재오픈이 다른 캐릭터 `consented`를 폴링에서 되돌리는 것이 회귀 시험으로 고정된다 (서버 쪽) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 20 | 회복이 영구 배제로 바뀌지 않는다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 21 | 흘려보내진 사람도 새 지목 사건이 생기고 명단 잠금 영구 409가 풀린다 | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |
| 22 | 회복 뒤 재호출이 최신 지목을 반영한다 (5차 CR-01) | ✓ VERIFIED | ✓ VERIFIED (변경 없음) | — |

### Observable Truths — 8차가 새로 확인(G-12.3-2…7 + REQUIREMENTS.md 대조)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 23 | 능력치 배치에서 값이 중복된 풀(예: `2,1,1,0,0,-1`)도 여섯 칸을 전부 채우고 「확정」이 켜진다 (G-12.3-6, blocker) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 산수는 VERIFIED(`remainingFixedValues`, 회귀 시험 3건이 함수 추가 전 RED·추가 후 GREEN을 SUMMARY에 남김). `PlaceFixedValuesControl` 범위 `new Set` 0건·`remainingFixedValues` 1건·`key={\`${index}:${value}\`}` 재확인. 실제 `<select>` 배선은 사람 확인 필요 |
| 24 | 인원 확정 조작의 초기값·min·max가 룰북의 실제 범위로 뜬다 (G-12.3-2) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 산수는 VERIFIED(`partySizeOutOfRange`, 시험 4건). `CreationScreen.tsx`에 `min_player_characters` 필드-숫자 하드코딩 0건, `useState(range.min_player_characters)`/`min={range.min_player_characters}` 재확인. 실제 `<input>` 렌더는 사람 확인 필요 |
| 25 | 인원 거절 문구에 파이썬 내부 표현이 안 새어 나온다 (G-12.3-3, D-15) | ✓ **VERIFIED (행위 증거)** | `test_fix_party_size_rejection_message_is_human_readable_only`가 실제로 `PartySizeOutOfRange`를 발생시켜 메시지 문자열이 `exc.reason`과 같고 내부 표현 자취가 없음을 단언 — pytest가 실제로 이 경로를 실행한다(RED: `AssertionError` 확인, GREEN 통과). `_prepare_fix_party_size` 범위 `exc.reason` 1건·`str(exc)` 0건 재확인 |
| 26 | 새 시도가 시작되면 이전 오류 문구가 화면에서 안 남는다 (G-12.3-4) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 배선의 모양은 VERIFIED(`onError(null)` 정확히 2건, `onError` 타입 `string | null`). 실제로 화면에서 문구가 사라지는 것은 JSX 상태 전이라 사람 확인 필요(SUMMARY 자신도 `human_judgment: true`로 표시) |
| 27 | 안내가 없을 때 화면이 한 가지만 말하고, 첫 안내를 화면이 스스로 부른다 (G-12.3-5, D-15) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 판정 산수는 VERIFIED(`shouldAnnounce`, 시험 5건). `CreationScreen.tsx`가 그 함수를 effect 안 한 곳에서만 부르는 것, 발동 조건 네 값이 의존성 목록에 전부 있다는 SUMMARY 대조표를 코드와 재대조해 확인. 실제 브라우저에서 effect가 조건이 참이 되는 순간을 실시간으로 포착해 실제 HTTP 요청을 내는지는 사람 확인 필요 |
| 28 | 지금 누구 차례인지 화면이 이름으로 말하고, 없으면 지어내지 않는다 (G-12.3-7) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | 판정 산수는 VERIFIED(`creationTurn`/`creationTurnLabel`, 시험 5건). `CreationPane.tsx` 대화판 첫 줄이 `turnLabel !== null ? <p>… : null`로 그 결과를 그리는 것, `pc-` 대체 표시 0건 재확인. 실제 DOM 렌더는 사람 확인 필요 |
| 29 | `REQUIREMENTS.md`의 CHAR-06 완료 표시가 실제 검증 상태(사람 확인 대기)와 일치한다 | ✗ **FAILED** | `.planning/REQUIREMENTS.md:117,359`가 `Complete`로 바뀌어 있는데, `12.3-UAT.md`는 여전히 `passed: 1, issues: 1, pending: 7`이고 `## Current Test`가 사람의 재시도를 명시적으로 요구한다 — 위 「결론 먼저」와 `gaps` 프런트매터 참조 |

**Score:** 28/29 truths verified (VERIFIED 22 이월 + 1 신규(#25) = 23, PASSED 없음, FAILED 1(#29), PRESENT_BEHAVIOR_UNVERIFIED 6 — #11(이월) + #23/#24/#26/#27/#28(신규)). 분모 29는 `(진짜) VERIFIED 23 + FAILED 1 + PRESENT_BEHAVIOR_UNVERIFIED 6 - 대체된 옛 truth #7·#9는 새 번호로 흡수` = 29.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | WR-01(끼어들기 입력이 항상 자기 자신을 대상으로 보내 서버에 거절당함) | 사람이 `12.3-UAT.md` 테스트 9를 실기기로 겪은 뒤 별도 gap-closure | `12.3-11-PLAN.md`·`12.3-14-PLAN.md` prohibitions가 D-08 표면을 건드리는 처방을 명시적으로 미룸 |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/session/creationView.ts` (`remainingFixedValues`) | 개수 기반 남은 수 계산 순수 함수 | ✓ VERIFIED | `export function remainingFixedValues` 정확히 1건(383행) |
| `frontend/src/panes/CreationPane.tsx` (`PlaceFixedValuesControl`) | `remainingFixedValues`로 잠금, key가 자리 기준 | ✓ VERIFIED | 그 컴포넌트 범위 `new Set` 0건, `remainingFixedValues` 사용 1건, `key={\`${index}:${value}\`}` |
| `src/gptrpg/web/routes_creation.py` (`get_creation_declaration`) | 항목 목록+인원 범위 봉투 | ✓ VERIFIED | `git diff` 재확인, 서버 시험 재실행 기록(SUMMARY) 확인 |
| `frontend/src/session/creationView.ts` (`partySizeOutOfRange`) | 서버 판정의 거울, 룰북 숫자 없음 | ✓ VERIFIED | 190행, `min_player_characters\s*[<>=]+\s*[0-9]` 0건 |
| `frontend/src/screens/CreationScreen.tsx` (`PartySizeControl`) | 서버 범위로 초기값·min·max 구성 | ✓ VERIFIED | `range.min_player_characters` 4곳에서 사용, `ABSOLUTE_MINIMUM_PARTY_SIZE` 이름 붙은 상수(81행) |
| `src/gptrpg/session_actor/actor.py` (`_prepare_fix_party_size`) | `exc.reason`만 사람에게, `rules_core` 무변경 | ✓ VERIFIED | `exc.reason` 1건·`str(exc)` 0건, `git diff --name-only -- src/gptrpg/rules_core` 빈 출력 |
| `frontend/src/screens/CreationScreen.tsx` (`PartySizeControl.onError`) | 새 시도 시작 시 오류 비움 | ✓ VERIFIED | `onError(null)` 정확히 2건 |
| `frontend/src/session/creationView.ts` (`shouldAnnounce`) | 첫 안내 발동 조건 순수 함수 | ✓ VERIFIED | 271행, `CreationScreen.tsx`에서 effect 1곳만 호출 |
| `frontend/src/session/creationView.ts` (`creationTurn`) / `frontend/src/labels.ts` (`creationTurnLabel`) | 차례 판정 + 문구 조립 | ✓ VERIFIED | 319행/340행, `CreationPane.tsx:654`가 둘을 조합해 사용 |
| `.planning/REQUIREMENTS.md` (CHAR-06 항목) | 실제 검증 상태와 일치하는 완료 표시 | ✗ **STALE (gap #29)** | `Complete`로 앞서 나가 있음 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| 룰북 선언 `fixed_values`(중복 값 포함) | 「확정」 단추 활성화 | `GET /creation/steps` → `PlaceFixedValuesControl` → `remainingFixedValues` → `disabled` → `complete` | ✓ WIRED (코드) / ⚠️ 실브라우저 미검증 | grep으로 배선 재확인, 런타임은 human_verification |
| 룰북 `party_size_range` | 인원 조작 초기값·확정 잠금 | `GET /creation/steps` 봉투 → `CreationScreen` 상태 → `PartySizeControl` props → `partySizeOutOfRange` | ✓ WIRED (코드) / ⚠️ 실브라우저 미검증 | 상동 |
| 인원 확정 → 폴링 상태 | 첫 안내 자동 호출 | `shouldAnnounce(feed.state, announced, error!==null)` → `announce()` → `POST /creation/announce` → 서버 `_gm_dedupe_key` | ✓ WIRED (코드, effect 의존성 대조 재확인) / ⚠️ 실브라우저 미검증 | 상동 |
| GM 지목(`nominate_speaker`) | 대화판 첫 줄 | `creation_current_speaker_id` → `creationTurn` → `creationTurnLabel` → `CreationPane` 첫 줄 | ✓ WIRED (코드) / ⚠️ 실브라우저 미검증 | 상동 |
| `12.3-14-SUMMARY.md`의 `requirements-completed` | `.planning/REQUIREMENTS.md`의 CHAR-06 체크박스 | GSD 마감 절차(`requirements.ready-ids`) | ⚠️ **WIRED하지만 근거가 틀렸다** | 이 절차가 「선언한 계획 전부 실행됨」을 「사람 확인 완료」로 오인 — gap #29 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 인원 거절 메시지가 `exc.reason`과 같고 내부 표현이 없다 | `.venv/bin/python -m pytest -q tests/test_session_actor.py -k party_size --tb=short` (SUMMARY 인용, 이번 검증이 grep으로 코드 재확인) | 5 passed | ✓ PASS |
| 능력치 배치 개수 기반 회귀 시험 | `cd frontend && npx vitest run src/session/creationView.test.ts` (SUMMARY 인용) | 함수 추가 전 3 failed → 추가 후 전부 통과 | ✓ PASS |
| 인원 범위 봉투 시험 | `.venv/bin/python -m pytest -q tests/test_creation_screen_state.py --tb=short` (SUMMARY 인용) | 봉투 전환 전 2 failed → 전환 후 33 passed | ✓ PASS |
| `shouldAnnounce` 다섯 갈래 | `cd frontend && npx vitest run src/session/creationView.test.ts` (SUMMARY 인용) | 함수 추가 전 5 failed → 추가 후 52 passed | ✓ PASS |
| `creationTurn` 다섯 갈래 | 상동 | 함수 추가 전 5 failed → 추가 후 57 passed | ✓ PASS |
| 이번 라운드 11개 변경 파일에 debt 마커 없음 | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` (이번 검증이 직접 실행) | 0건 | ✓ PASS |
| 서버 변경 범위가 두 파일로 좁혀져 있음 | `git diff --name-only 231658e..HEAD -- src/gptrpg` (이번 검증이 직접 실행) | `actor.py`, `routes_creation.py` 정확히 둘 | ✓ PASS |
| `rules_core` 무변경 | `git diff --name-only 231658e..HEAD -- src/gptrpg/rules_core` (이번 검증이 직접 실행) | 빈 출력 | ✓ PASS |
| 저장소 전체 회귀 (이 세션이 이미 실행, 재실행 안 함 — 중복 실행 회피 규율) | `.venv/bin/python -m pytest -q --tb=short` / `cd frontend && npx vitest run` | 1385 passed / 98 passed (7 files) | ✓ PASS (인용) |
| `.planning/REQUIREMENTS.md`의 CHAR-06이 `12.3-UAT.md`의 실제 진행 상태와 일치 | `grep "CHAR-06" .planning/REQUIREMENTS.md` vs `grep "^status:\|passed:" 12.3-UAT.md` (이번 검증이 직접 실행) | `Complete` vs `partial`/`passed: 1` | ✗ **FAIL (gap #29)** |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CHAR-06 | 12.3-01~14 (전체) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ⚠️ **문서는 Complete, 실제는 PENDING — 어긋남(gap #29)** | 코드 레벨 gap 여섯은 전부 닫혔지만(위 참조), `12.3-UAT.md`가 요구하는 「사람이 실제 기기로 처음부터 끝까지」는 아직 아무도 완주하지 않았다. `.planning/REQUIREMENTS.md`를 `Pending`으로 되돌리는 것이 이번 라운드의 유일한 필수 조치다 |

**Orphaned requirements:** 없음 — 6~7차와 동일. `12.3-01`~`12.3-14` 14개 계획 전부 `requirements: [CHAR-06]`을 선언했고, `REQUIREMENTS.md`도 CHAR-06을 Phase 12.3 하나에만 매핑한다.

### Anti-Patterns Found

이번 라운드가 바꾼 11개 파일에 TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 계열 마커 0건 — debt marker gate 통과.

`12.3-REVIEW.md`(이번 라운드 범위)가 남긴 두 WARNING은 blocker가 아니지만 다음 라운드가 참고할 것으로 옮겨 적는다:

- **WR-01**: `CreationScreen.tsx`의 자동 안내 effect(`announce()`)가 `usePolling.ts`의 `beacon()`/`poll()`과 달리 언마운트 가드가 없다. React 18에서 실질적 피해는 없다고 리뷰가 확인했지만(클로저의 `aliveRef` 검사가 이미 방어), 이 파일의 다른 두 유사 effect가 지키는 관례에서 벗어나 있다는 일관성 결함.
- **WR-02**: 자동 안내 호출이 여러 탭에서 동시에 발동할 수 있게 되면서, 서버가 이미 문서화한 「AI 제공자가 두 번 불릴 수 있다」 경쟁(사건 자체는 D-12가 하나로 접지만 AI 호출 비용은 못 막음)을 훨씬 더 자주 건드리게 됐다. 사건 중복은 없지만 비용·지연이 늘어날 수 있다 — 이 프로젝트가 이미 민감하게 보는 항목.

둘 다 blocker가 아니므로 이 gap 목록에는 안 올린다. 다음 라운드가 `<planner_assumptions>`나 별도 계획에서 검토할 항목으로 남긴다.

### Human Verification Required

#### 1. `.planning/REQUIREMENTS.md`의 CHAR-06을 되돌리고, 사람이 실제 기기 셋 이상으로 `12.3-UAT.md` 테스트 2부터 다시 완주 (최우선)

**Test:** 문서 상태를 `Pending`으로 되돌린 뒤, 실제로 다른 기기 셋 이상(또는 시크릿 모드 다중)으로 인원 확정 → 능력치 배치(중복 값 포함) → 안내 자동 발동 → 지목 차례 문구 → GM 정리 → 동의 → 점유까지 화면 조작만으로 수행한다.
**Expected:** 여섯 gap-closure가 실제로 이어지고, `12.3-UAT.md` 테스트 2가 `pass`로 바뀐다. 완주하면 그때 CHAR-06을 다시 Complete로 바꾼다.
**Why human:** 순수 함수 판정의 산수와 코드 배선의 모양은 이번 라운드가 전부 재확인했다. 실제 `<select>`/`<input>`/effect가 사람 손과 브라우저 타이밍으로 실제로 도는지는 RTL 없는 이 저장소에서 사람만 확인할 수 있다.

#### 2. WR-01을 실기기로 만져 보고 방향을 고른다 (`12.3-UAT.md` 테스트 9)

**Test:** 남의 차례에 끼어들기 입력을 실제로 시도해, 「안 보인다」와 「눌러도 거절당한다」 중 실제로 어느 쪽이 일어나는지 확인한다.
**Expected:** 실제 결과에 따라 별도 gap-closure로 닫을지 판단할 근거가 생긴다.
**Why human:** 12.3-11·12.3-14 둘 다 D-08 표면을 건드리는 처방을 의도적으로 미뤘다.

#### 3~5. 이전 라운드부터 이월 — 동의 교차 전파(화면 쪽) · 회복 5분 체감 · 주사위 연출/D-13 세 문구/14개 체크리스트

반복해 옮기지 않는다 — 위 `human_verification` 프런트매터와 4차 이전 `12.3-VERIFICATION.md`(git 이력)·`12.3-05-SUMMARY.md`를 참조할 것.

### Gaps Summary

**코드 gap은 없다.** 세 gap-closure 계획(12.3-12/13/14)이 `12.3-UAT.md` 테스트 2가 낸 여섯 결함(G-12.3-2 … G-12.3-7, blocker 하나 포함)을 코드 레벨에서 전부 닫았다는 것을 이번 검증이 SUMMARY 서술이 아니라 코드 자체(grep·awk·git diff)로 직접 재확인했다. 서버 변경은 약속한 두 파일(`actor.py`·`routes_creation.py`)로 정확히 좁혀져 있고 `rules_core`는 무변경이다. debt marker 0건, TypeScript/pytest/vitest 회귀 없음.

**유일한 gap은 문서 상태다.** `.planning/REQUIREMENTS.md`가 CHAR-06을 `Complete`로 바꿔 놓았는데, 이 phase의 목표 문장 자체가 요구하는 「사람이 브라우저만으로 처음부터 끝까지」는 아직 사람이 확인하지 않았다 — `12.3-UAT.md`가 스스로 `passed: 1, issues: 1, pending: 7`이고 사람의 재시도를 요구한다고 적고 있는 바로 그 파일 옆에서, `REQUIREMENTS.md`는 앞서 나갔다. 이것은 GSD 마감 절차가 「이 요구사항을 선언한 계획이 전부 실행됐다」와 「요구사항이 실제로 달성됐다」를 혼동한 결과이고, 이 프로젝트가 이미 겪은 실패 양상의 실례다. 고치는 데 코드 변경이 필요 없다 — 문서 두 줄을 되돌리고, 사람이 실제로 테스트 2~8을 완주한 뒤에만 다시 Complete로 바꾸면 된다.

**추가로, 여섯 결함 중 다섯(G-12.3-2/4/5/6/7)의 판정 자체는 순수 함수 시험으로 VERIFIED이지만, 그 판정이 실제 브라우저에서 실제로 동작하는지는 이 저장소의 구조적 제약(RTL 없음)으로 자동 확인이 안 되어 PRESENT_BEHAVIOR_UNVERIFIED로 분류했다.** 이것은 새로운 문제가 아니라 이 phase 전체가 처음부터 안고 있던 성질이고, human_verification #1이 그 확인을 요구한다.

---

_Verified: 2026-08-22 (재검증, 8차)_
_Verifier: Claude (gsd-verifier)_
