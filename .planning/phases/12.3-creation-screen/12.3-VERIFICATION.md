---
phase: 12.3-creation-screen
verified: 2026-08-24T08:30:00Z
status: passed
score: 15/15 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 13/15
  gaps_closed:
    - "조작 요소(TOUCH_TARGETS) 판정이 「확인 불가」를 「통과」로 접지 않는다 — 12.3-19가 extract_measurements()에 선택자별 빈 값 경계 검사를 신설했다(item[\"targets\"].get(선택자, []) == 0개면 종료 코드 2). 이 검증이 임시로 존재하지 않는 선택자(.nonexistent-touch-target)를 TOUCH_TARGETS에 얹어 독립 재현 — 종료 코드 2와 선택자 이름이 표준오류에 실제로 찍히는 것을 직접 확인했고, 패치는 즉시 원복해 git diff 0줄을 확인했다"
    - "--self-test가 각 함정이 겨눈 그 단언이 실제로 빨강을 내는지 확인한다 — 12.3-20이 TRAPS 넷째 칸(표식)과 run_self_test()의 표식 대조, _validate_trap_markers() 빈 값 가드를 신설했다. 이 검증이 넘침 단언(shellScrollHeight 비교)만 임시로 무력화(if False:)하고 --self-test를 돌려, 정확히 round 8이 지목한 시나리오(chatBottom 넘침 검사가 대신 빨강을 내 줌)를 재현했다 — 결과는 종료 코드 1과 'pixel-floor · 빨강(겨눈 단언 아님) · 겨눈 표식 골격 상자가 사유에 없음'으로, round 8 당시의 거짓 통과(0)가 이번엔 정확히 실패(1)로 잡혔다. 표식을 빈 문자열로 바꾼 별도 패치로는 종료 코드 2(확인 불가)도 독립 재현했다. 두 패치 모두 즉시 원복해 git diff 0줄을 확인했다"
  gaps_remaining: []
  regressions: []
deferred: []
---

# Phase 12.3: 캐릭터 만들기 화면 검증 보고서 (9차 — gap 종료 재검증)

**단계 목표:** Phase 12.1이 서버에 만들어 둔 캐릭터 만들기를 사람이 브라우저만으로 끝까지 할 수 있게 한다 — 안내를 읽고, 자기 차례에 서술하고, 되묻는 말에 답하고, 동의하고, 자기 캐릭터로 세션에 들어가는 데까지 화면이 따라온다.
**검증 시각:** 2026-08-24T08:30:00Z
**상태:** passed
**재검증 여부:** 예 — 8차 검증(`13/15`, gaps_found)이 연 gap 둘(TOUCH_TARGETS 빈 값 경계 · `--self-test` 표적 특이성)을 닫는 gap 종료 계획 12.3-19·12.3-20이 실행됐고, 이 회차는 두 계획의 SUMMARY.md 주장을 신뢰하지 않고 코드를 직접 읽고 스크립트를 직접 실행·패치·원복해 독립 재현했다.

## 요약

8차 검증이 연 gap 둘 모두 **코드에 실제로 존재하고, 이 검증이 독립적으로 재현해 확인했다.**

- **gap #1 (TOUCH_TARGETS 빈 값 경계)**: `extract_measurements()`(608-690행)에 조작 요소 선택자마다 `len(item["targets"].get(selector, [])) == 0`을 확인하는 검사가 신설됐다. 이 검증이 존재하지 않는 선택자를 임시로 얹어 실행한 결과, 수정 전이라면 통과(0)했을 상황이 정확히 종료 코드 **2**(어느 선택자였는지 이름과 함께)로 갈렸다.
- **gap #2 (`--self-test` 표적 특이성)**: `TRAPS`(809-865행)에 넷째 칸(표식)이 생겼고, `run_self_test()`(887-1018행)가 판정 통과 여부가 아니라 「겨눈 표식이 실제 사유 문장에 있는가」까지 확인한다. 이 검증이 넘침 단언만 임시로 무력화해 8차 검증이 실측했던 바로 그 시나리오(다른 단언이 대신 빨강을 내 줌)를 재현한 결과, 8차 당시의 거짓 통과(0)가 이번엔 정확히 종료 코드 **1**(`빨강(겨눈 단언 아님)`)로 잡혔다. 표식을 빈 문자열로 바꾼 별도 패치로는 `_validate_trap_markers()`가 종료 코드 **2**로 막는 것도 확인했다.

두 재현 모두 코드를 임시로 패치했다가 즉시 원복했고(`git diff --stat scripts/check_narrow_viewport.py`가 각 패치 직후 빈 출력임을 확인), 원복 후 기본 경로(exit 0, 네 형태 통과)와 `--self-test`(exit 0, 함정 셋 전부 겨눈 단언 적중)가 다시 정상임을 재확인했다.

이번 gap 종료 회차가 건드린 파일은 `scripts/check_narrow_viewport.py`·`README.md` 둘뿐이고(`git diff --name-only 4ff8f72~1..HEAD -- . ':!.planning'` 확인), `.tsx`·`src/gptrpg/` 0줄이다. 회귀 시험도 이 검증이 직접 재실행해 확인했다 — pytest 1406 passed·1 failed(사전 존재 무관 flaky, `test_event_schema_migration.py` — `src/`·`tests/` 미변경 확인), frontend vitest 104/104 passed. 코드 검토(`12.3-REVIEW.md`, 2026-08-24 재생성)는 critical 0·warning 1(WR-02, 의도적 보류로 이미 기록됨)·info 2로, CR-01·WR-01의 해결을 이 검증과 별개로 재확인했다.

## 목표 달성 현황

### 관찰 가능한 진실

| # | 진실 | 상태 | 근거 |
|---|------|------|------|
| 1 | 폰 가로(844×390)에서 대화 조작부가 화면 안으로 돌아오거나 스스로 스크롤해 닿는다 | ✓ VERIFIED | `.venv/bin/python scripts/check_narrow_viewport.py` 이 검증이 직접 재실행 — 844x390 줄 `대화 156.0px · 통과` |
| 2 | 폰 세로(390×844)에서도 같은 처방이 듣는다 | ✓ VERIFIED | 같은 실행 결과 390x844 줄 `대화 337.6px · 통과` |
| 3 | 그물이 스스로를 재현한다 — CSS 수정 전 빨강, 수정 후 초록이 커밋 이력에 남는다 | ✓ VERIFIED | `git show 98a50b7`(12.3-16) 존재 확인 — 이번 회차 미변경 영역, 8차 검증에서 이미 실측 |
| 4 | 「잘렸다」의 판정이 뷰포트 비교가 아니라 스크롤 경로 유무(`reachable()`)로 이뤄진다 | ✓ VERIFIED | `grep -c reachable scripts/check_narrow_viewport.py` = 3(정의+호출), 이번 회차 미변경 |
| 5 | 고정판이 실제 화면과 갈리면 종료 코드 2가 난다 — 감시가 `className=` 문맥 낱말 경계로 좁혀졌다 | ✓ VERIFIED | `check_fixture_still_mirrors_source()`를 이 검증이 직접 import·실행 — `[]`(드리프트 없음). `REQUIRED_SOURCE_CLASS_NAMES`에 12.3-19가 추가한 `composer__row`·`composer__input`·`candidate`·`btn` 포함 확인 |
| 6 | 넘침 단언이 원리적으로 발화할 수 있게 됐다 | ✓ VERIFIED | `--self-test`의 `pixel-floor` 함정이 실제로 빨강("골격 내용(513.0px)이 골격 상자(390.0px)를 넘어 잘렸다")을 낸다 — 이 검증이 직접 실행해 확인 |
| 7 | 함정을 얹으면 그물이 실제로 빨개진다(3종 함정 전부) | ✓ VERIFIED | `--self-test` 재실행 결과 세 함정(pixel-floor·clipped-composer·starved-story) 전부 빨강, exit 0(겨눈 단언 전부 적중) |
| 8 | 조작 요소(TOUCH_TARGETS) 판정이 「확인 불가」를 「통과」로 접지 않는다 | ✓ VERIFIED | **8차 gap #1 종료.** `extract_measurements()`(678-689행)에 빈 값 경계 신설. 이 검증이 `.nonexistent-touch-target`을 `TOUCH_TARGETS`에 임시로 얹어 독립 재현 — 종료 코드 **2**, 표준오류에 `1440x900`과 선택자 이름 출력. 패치 즉시 원복(`git diff` 0줄) |
| 9 | `--self-test`가 「그 함정이 겨눈 단언」이 살아 있는지를 확인한다 | ✓ VERIFIED | **8차 gap #2 종료.** `TRAPS` 넷째 칸(표식, 809-865행) + `run_self_test()`의 표식 대조(989-1008행) + `_validate_trap_markers()`(868-884행). 이 검증이 넘침 단언만 무력화(`if False:`)해 8차가 실측한 「chatBottom 넘침이 대신 잡아 줌」 시나리오를 재현 — 결과 종료 코드 **1**, `pixel-floor · 빨강(겨눈 단언 아님) · 겨눈 표식 '골격 상자'가 사유에 없음`. 표식을 빈 문자열로 바꾼 별도 재현에서는 `_validate_trap_markers()`가 종료 코드 **2**(`'pixel-floor' 함정의 표식이 비었거나 공백뿐이다`)로 막는 것도 확인. 두 패치 모두 즉시 원복 |
| 10 | 넓은 창(1440×900) 대조군이 실제로 3컬럼을 잰다(좌우 폭까지) | ✓ VERIFIED | 재실행 결과 `상태 900.0px · 서사 900.0px · 대화 900.0px · 통과`. 코드(720-725행 근처 ⑤) 변경 없음 |
| 11 | 좁은 형태에서 상태 칸이 뷰포트의 25%를 넘지 않는지도 판정한다 | ✓ VERIFIED | judge() 단언 ⑥ 코드 변경 없음. 재실행 결과 좁은 세 형태 모두 통과 |
| 12 | 사람이 README만 보고 두 명령을 언제 왜 부르는지 안다 — `--self-test`의 종료 코드 1이 두 경우(초록·빨강이지만 겨눈 단언 아님)를 뜻한다는 것도 안다 | ✓ VERIFIED | `grep -c self-test README.md` = 2. §A 항목 2의 종료 코드 `1` 문단이 "빨강은 났지만 다른 단언이 대신 울렸다"를 명시적으로 담고 있음을 직접 읽어 확인 |
| 13 | 코드 검토가 남긴 항목 전부의 처지가 기록으로 남는다(조용히 사라진 항목 0건) | ✓ VERIFIED | `deferred-items.md`에 이번 회차(12.3-16·17·18 범위) 여섯 건(CR-01·WR-01·WR-02·IN-01·IN-02, 접은 것 2·안 접은 것 3)과 이전 16건 절이 모두 존재. 오늘 재생성된 `12.3-REVIEW.md`가 같은 6항목(CR-01·WR-01 해결, WR-02·IN-01·IN-02 의도적 보류)을 독립적으로 재확인 |
| 14 | 비-회귀: `.tsx` 0줄 · `src/gptrpg/` 0줄 · 새 패키지 0개 | ✓ VERIFIED | `git diff --name-only 4ff8f72~1..HEAD -- . ':!.planning'`이 `README.md`·`scripts/check_narrow_viewport.py` 둘뿐임을 이 검증이 직접 확인 |
| 15 | 비-회귀: 서버·화면 자동 시험이 이번 변경으로 새로 깨진 것이 없다 | ✓ VERIFIED | 이 검증이 직접 재실행: `.venv/bin/python -m pytest -q --tb=line` → 1406 passed·1 failed(`test_event_schema_migration.py`, 사전 존재·`src/gptrpg/` 무변경 확인). `cd frontend && npm test -- --run` → 7 files/104 tests passed |

**점수:** 15/15 진실 검증됨 (0건 present-behavior-unverified)

### 재현 절차 상세 (gap #1)

```
$ (임시로 TOUCH_TARGETS에 .nonexistent-touch-target 추가)
$ .venv/bin/python scripts/check_narrow_viewport.py
1440x900 — 조작 요소 선택자가 문서에서 하나도 못 찾음: ['.nonexistent-touch-target(존재하지 않는 조작 요소(검증용))'] — 판정하지 않는다. 확인 불가.
EXIT=2
$ (원복) → git diff --stat scripts/check_narrow_viewport.py → (빈 출력)
```

### 재현 절차 상세 (gap #2)

```
$ (넘침 단언 shellScrollHeight 비교를 `if False:`로 임시 무력화)
$ .venv/bin/python scripts/check_narrow_viewport.py --self-test
다음 함정이 겨눈 단언을 울리지 못했다(초록으로 나왔거나, 빨강은 났지만 다른 단언이 대신 울렸다): pixel-floor
pixel-floor · 844x390 · 빨강(겨눈 단언 아님) · 겨눈 표식 '골격 상자'가 사유에 없음 · 대신 울린 사유: 대화 칸(chatBottom) 아래끝 513.0px가 뷰포트(390px)를 넘었다
clipped-composer · 844x390 · 빨강 · ...
starved-story · 390x844 · 빨강 · ...
EXIT=1
$ (원복) → git diff --stat scripts/check_narrow_viewport.py → (빈 출력)

$ (별도 패치: pixel-floor 표식을 빈 문자열로)
$ .venv/bin/python scripts/check_narrow_viewport.py --self-test
'pixel-floor' 함정의 표식이 비었거나 공백뿐이다 — 이 상태면 어떤 사유 문장에나 들어 있는 것으로 읽혀 확인이 항상 통과가 된다. 종료 코드 2.
EXIT=2
$ (원복) → git diff --stat scripts/check_narrow_viewport.py → (빈 출력)
```

두 재현 모두 8차 검증(`13/15`)이 gap #1·#2의 근거로 제시했던 바로 그 실패 모양(선택자 0개가 조용히 통과 / 겨눈 단언이 아닌 다른 단언이 대신 빨강을 내 줘도 통과)을 이 검증이 스스로 만들어 확인했고, 지금 코드는 두 경우 모두 정확한 종료 코드(2, 1)로 가른다.

### 필요 산출물

| 산출물 | 기대 | 상태 | 세부 |
|---|---|---|---|
| `scripts/check_narrow_viewport.py` | 그물이 넘침·조작부 잘림·대조군 붕괴·확인불가 경로를 모두 종료 코드로 정확히 가른다(TOUCH_TARGETS 빈 값 경계 + `--self-test` 표적 특이성 포함) | ✓ VERIFIED | gap #1·#2 재현으로 확인. 기본 경로 exit 0(4형태), `--self-test` exit 0(3함정 겨눈 단언 전부 적중) |
| `README.md` | 사람이 두 명령·`--self-test`의 종료 코드 1(두 경우) 뜻을 안다 | ✓ VERIFIED | §A 항목 2 문단 직접 읽어 확인 |
| `deferred-items.md` | 검토 항목 전부 처지 기록, 조용히 사라진 항목 0건 | ✓ VERIFIED | 이번 회차 절 + 이전 16건 절 모두 존재 |

### 요구사항 커버리지

| 요구사항 | 출처 계획 | 설명 | 상태 | 근거 |
|---|---|---|---|---|
| CHAR-06 | 12.3-19·12.3-20 (원 출처 12.3-01~18) | 사람이 브라우저만으로 캐릭터 만들기를 처음부터 끝까지 진행할 수 있다 | ✓ SATISFIED | `REQUIREMENTS.md:117`에 이미 `[x]` Complete로 표시(8차 이전부터 유지). 이번 두 계획은 CHAR-06의 기능 자체가 아니라 그 기능을 지키는 회귀 그물(`check_narrow_viewport.py`)의 견고성 gap 둘을 닫는 것이 목적이었고, 이 검증이 둘 다 실제로 닫혔음을 독립 재현했다. `12.3-UAT.md` 테스트 11(폰 가독성·조작 촉감)은 여전히 backstop(사람 확인 항목)으로 열려 있으나, 이는 8차 이전부터의 기존 상태이고 이번 회차의 범위가 아니다 |

**ORPHANED 요구사항 없음** — `REQUIREMENTS.md`에서 Phase 12.3에 매핑된 요구사항은 CHAR-06 하나뿐이고, 12.3-19·12.3-20 두 PLAN 모두 `requirements: [CHAR-06]`을 선언했다.

### 코드 검토 대조 (12.3-REVIEW.md, 2026-08-24 재생성)

| 식별자 | 요지 | 이 검증의 판단 |
|---|---|---|
| CR-01 | TOUCH_TARGETS 빈 매치가 조용히 통과로 읽힌다 | **해결됨** — 이 검증이 독립 재현(위 gap #1 재현 절차) |
| WR-01 | `--self-test`가 겨눈 단언이 아니라 아무 단언이나 빨강이면 통과로 본다 | **해결됨** — 이 검증이 독립 재현(위 gap #2 재현 절차) |
| WR-02 | 상태 칸 25% 상한 판정이 실제 CSS 폭 기준(1080px)이 아니라 형태 이름 문자열 비교다 | 확인됨(코드에 `measurement["name"] != WIDE_SHAPE` 여전히 존재). must-have 위반 아님 — `deferred-items.md`가 이미 강건성 개선 사항으로 명시적으로 보류한 항목. gap으로 안 올림 |
| IN-01 | `className=` 정규식이 중첩 중괄호를 못 넘는다 | 확인됨. 현재 소스 넷에 해당 패턴 없음(`check_fixture_still_mirrors_source()` 재실행 결과 `[]`로 재확인), 실패 방향도 안전한 쪽. 급하지 않음 |
| IN-02 | README 40행의 실패(1) 사유 나열이 「상태 칸 25% 상한 초과」를 빠뜨렸다 | 확인됨(README.md:40 여전히 없음). 문서 완결성 문제이며 12.3-20의 명시 승인 기준 밖 — `deferred-items.md`가 이유와 다음에 볼 것을 기록. gap으로 안 올림 |

### 안티패턴 스캔

이번 회차가 건드린 두 파일(`scripts/check_narrow_viewport.py`·`README.md`)에서 `TBD`·`FIXME`·`XXX`·`TODO`·`HACK`·`PLACEHOLDER`류 표식은 발견되지 않았다(`grep -n -E` 빈 결과).

### 행동 스팟체크

| 확인 | 명령 | 결과 | 상태 |
|---|---|---|---|
| 기본 경로 4형태 통과 | `.venv/bin/python scripts/check_narrow_viewport.py` | exit 0, 네 줄 전부 `통과` | ✓ PASS |
| `--self-test` 3함정 전부 겨눈 단언 적중 | `.venv/bin/python scripts/check_narrow_viewport.py --self-test` | exit 0, pixel-floor·clipped-composer·starved-story 전부 `빨강 ·` (겨눈 표식 포함) | ✓ PASS |
| gap #1 독립 재현(TOUCH_TARGETS 빈 값) | 임시 선택자 추가 후 재실행 | exit 2, 선택자 이름 표준오류 출력 | ✓ PASS |
| gap #2 독립 재현(겨눈 단언 대조) | 넘침 단언 무력화 후 `--self-test` | exit 1, "빨강(겨눈 단언 아님)" 갈래 | ✓ PASS |
| gap #2 독립 재현(표식 빈 값) | 표식 빈 문자열로 치환 후 `--self-test` | exit 2, "표식이 비었거나 공백뿐이다" | ✓ PASS |
| 드리프트 감시 정상 | `check_fixture_still_mirrors_source()` 직접 import·호출 | `[]` (드리프트 없음) | ✓ PASS |
| 비-회귀 파일 범위 | `git diff --name-only 4ff8f72~1..HEAD -- . ':!.planning'` | `README.md`·`scripts/check_narrow_viewport.py` 둘뿐 | ✓ PASS |
| 비-회귀 pytest | `.venv/bin/python -m pytest -q --tb=line` | 1406 passed, 1 failed(사전 존재 무관) | ✓ PASS |
| 비-회귀 frontend | `cd frontend && npm test -- --run` | 7 files / 104 tests passed | ✓ PASS |

모든 패치(gap #1·#2 재현용 임시 수정)는 확인 직후 원본 파일로 원복했고, 각 원복 후 `git diff --stat scripts/check_narrow_viewport.py`가 빈 출력임을 확인해 저장소에 흔적이 남지 않았음을 검증했다.

### 사람 검증 필요 항목

없음 — 이번 회차(12.3-19·12.3-20)의 must-have는 전부 기계로 확인 가능한 항목이었고 전부 검증됐다.

`12.3-16·17·18`의 backstop 항목 둘(폰 서사 칸 가독성, 만들기 중 조작 칸 촉감 확인)은 8차 이전부터 열려 있는 별개 항목이며, 이번 두 gap 종료 계획의 범위가 아니다(각 PLAN의 `must_haves.truths`가 명시적으로 `verification: backstop`으로 표시하고 있고, 두 SUMMARY 모두 "이 계획도 안 닫는다"고 스스로 기록했다). `12.3-UAT.md` 테스트 11이 여전히 이 항목을 담당하며, 이 페이즈가 CHAR-06을 이미 Complete로 표시한 이후에도 계속 열려 있던 기존 상태다 — 이번 gap 종료 회차가 새로 만든 미확인 항목이 아니므로 이번 VERIFICATION.md의 gaps나 human_verification 목록에 다시 올리지 않는다.

### Gaps 요약

없음. 8차 검증이 연 gap 둘(TOUCH_TARGETS 빈 값 경계 · `--self-test` 표적 특이성) 모두 코드에 실제로 존재하며, 이 검증이 SUMMARY.md의 주장을 신뢰하지 않고 코드를 직접 읽고, 두 결함을 각각 임시로 재현하는 패치를 스스로 만들어 실행한 뒤 즉시 원복하는 방식으로 독립 검증했다. 두 재현 모두 8차 검증이 기록했던 실패 모양을 정확히 재현했고, 지금 코드는 그 두 경로를 정확한 종료 코드(2, 1)로 가른다. 회귀 시험도 이 검증이 직접 재실행해 확인했다(pytest 1406/1407, frontend 104/104, 실패 1건은 사전 존재·무관). 코드 검토도 오늘 재생성된 결과(critical 0)와 일치한다. Phase 12.3 목표(브라우저만으로 캐릭터 만들기 완주)는 CHAR-06으로 이미 충족되어 있고, 이번 회차는 그 목표를 지키는 회귀 그물 자신의 마지막 두 맹점을 닫았다.

---

_검증: 2026-08-24T08:30:00Z_
_검증자: Claude (gsd-verifier)_
