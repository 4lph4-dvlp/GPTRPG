---
phase: 12.3-creation-screen
plan: 12
subsystem: ui
tags: [react, typescript, vitest, tdd, creation-screen]

requires:
  - phase: 12.3-11
    provides: secure-context 의존 제거, 앱 최상단 오류 경계 — 화면 자체가 안정적으로 뜨는 전제
provides:
  - "던전월드류의 값이 중복된 능력치 묶음(2, 1, 1, 0, 0, -1)을 여섯 칸에 전부 배정하고 「확정」을 켤 수 있는 화면 판정"
  - "개수 기반 잠금 판정을 시험 가능한 순수 함수(remainingFixedValues)로 분리한 패턴"
affects: [12.3-UAT, Phase 16]

actuals:
  tokens: 1849
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "화면 판단(잠금·활성화 조건)을 JSX 밖 순수 함수로 빼서 RTL 없는 이 저장소에서도 시험 가능하게 만드는 패턴 — creationView.ts 모듈의 기존 관례를 그대로 잇는다"
    - "선택지 목록을 자리(index) 기준 React key로 그리는 것 — 값이 중복될 수 있는 목록에서는 값 기준 key가 항상 위험 신호"

key-files:
  created: []
  modified:
    - frontend/src/session/creationView.ts
    - frontend/src/session/creationView.test.ts
    - frontend/src/panes/CreationPane.tsx

key-decisions:
  - "잠금 판정의 근거를 「값의 집합」에서 「값의 개수」로 바꾼다 — 서로 다른 값의 중복 배정 방지는 사라지지 않고 개수 1의 특수한 경우로 남는다"
  - "같은 값을 두 번 보여 주는 선택지 표시는 접지 않는다(하나로 접어 '개수 남음'으로 표시하는 대안 기각) — 이 조작의 뜻 자체가 '정해진 숫자 묶음을 자리에 배치한다'라서 룰북이 준 묶음을 그대로 보이는 쪽이 사람의 머릿속 모형과 맞는다. 표시 방식 재설계는 Phase 16 경계"

patterns-established:
  - "판단이 JSX 안에 있으면 이 저장소는 시험할 방법이 없다(RTL 없음) — 결함이 여섯 라운드의 코드 검증을 살아남은 이유가 정확히 그것이었다. 화면 판단은 반드시 creationView.ts 같은 순수 함수 모듈로 뺀다"

requirements-completed: [CHAR-06]

coverage:
  - id: D1
    description: "값이 중복된 풀(예: 2, 1, 1, 0, 0, -1)에서 값별 남은 배정 가능 개수를 계산하는 순수 함수 remainingFixedValues와 그 회귀 시험 — 함수를 더하기 전에는 실패, 더한 뒤에는 통과"
    requirement: "CHAR-06"
    verification:
      - kind: unit
        ref: "frontend/src/session/creationView.test.ts#남은 개수로 배정 가능한 값을 고른다 (G-12.3-6)"
        status: pass
    human_judgment: false
  - id: D2
    description: "PlaceFixedValuesControl이 값의 집합 대신 개수 기반 판정(remainingFixedValues)으로 <select> 잠금을 결정하고, 선택지 key가 값이 아니라 자리(index) 기준이 된다"
    requirement: "CHAR-06"
    verification:
      - kind: unit
        ref: "cd frontend && npx vitest run (84 passed)"
        status: pass
      - kind: other
        ref: "cd frontend && npx tsc --noEmit && npm run build"
        status: pass
      - kind: integration
        ref: ".venv/bin/python -m pytest -q --tb=short (1382 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "실제 브라우저에서 능력치 배치 조작으로 여섯 칸을 전부 채우고 「확정」을 눌러 다음 항목으로 넘어간다 — 12.3-UAT.md 테스트 2를 다시 걷는다"
    verification: []
    human_judgment: true
    rationale: "순수 함수 시험이 답하는 것은 「고를 수 있는가」의 산수까지다. 그 판정이 실제 <select>의 disabled에 배선돼 사람 손에서 실제로 도는지, 확정 뒤 다음 항목으로 넘어가는지는 사람이 브라우저로 걸어야 안다"

duration: 12min
completed: 2026-08-22
status: complete
---

# Phase 12.3 Plan 12: 능력치 배치 개수 기반 잠금 판정 Summary

**던전월드류가 주는 값 묶음(2, 1, 1, 0, 0, -1)이 서로 다른 값 넷뿐이라 여섯 칸을 못 채우고 「확정」이 영원히 안 켜지던 blocker를, 시험 가능한 순수 함수(remainingFixedValues)로 닫았다.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-08-22T16:07:00+09:00 (약)
- **Completed:** 2026-08-22T16:11:33+09:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `creationView.ts`에 `remainingFixedValues(fixedValues, assignment): Map<number, number>` export 추가 — 값이 아니라 개수를 근거로 「지금 한 칸 더 쓸 수 있는가」를 판정한다
- 값이 중복된 풀(2, 1, 1, 0, 0, -1)을 여섯 칸에 전부 배정해 확정 조건까지 가는 회귀 시험 3건 추가 — 함수를 더하기 전엔 실패, 더한 뒤엔 통과함을 값으로 확보
- `PlaceFixedValuesControl`을 그 함수에 배선 — 값의 집합(`new Set`)으로 세던 지역 변수를 지우고, 선택지 `disabled` 판정을 남은 개수 기준으로 교체
- 선택지 React key를 값 기준(`key={value}`)에서 자리 기준(`key={\`${index}:${value}\`}`)으로 교체 — 같은 값이 둘인 풀에서 key가 겹치던 잠재 결함(T-12.3-48)도 함께 닫음

## Task Commits

Each task was committed atomically:

1. **Task 1: 값이 중복된 풀을 여섯 칸에 전부 배정해 확정까지 가는 회귀 시험을 먼저 빨갛게 만들고, 개수 기반 순수 함수로 초록으로 바꾼다** - `700a87f` (feat, tracer/tdd — RED와 GREEN을 한 커밋에 묶음, 아래 「RED/GREEN 기록」 참조)
2. **Task 2: PlaceFixedValuesControl을 개수 기반 판정에 배선하고 선택지 key를 자리 기준으로 바꾼다** - `ad82111` (fix)

_Task 1은 `type="tracer" tdd="true"`다. RED(시험 실패 확인)와 GREEN(함수 추가로 통과)을 지시대로 순서를 지켜 실행했으나, 계획이 명시적으로 커밋을 하나로 요구했으므로("더하는 것은 export 함수 하나와 그 시험뿐" — 되돌리기가 그 둘을 함께 지우는 것) `test(...)`/`feat(...)` 두 커밋으로 쪼개지 않고 `feat(12.3-12): ...` 한 커밋으로 묶었다. RED 단계의 실제 vitest 실패 출력은 아래 절에 그대로 남겼다._

## RED/GREEN 기록 (Task 1)

**RED — 함수를 더하기 전, `cd frontend && npx vitest run src/session/creationView.test.ts`:**

```
 FAIL  src/session/creationView.test.ts > 남은 개수로 배정 가능한 값을 고른다 (G-12.3-6) > 같은 값이 둘 있는 풀에서 하나를 쓰면 나머지 하나가 남는다
TypeError: remainingFixedValues is not a function

 FAIL  src/session/creationView.test.ts > 남은 개수로 배정 가능한 값을 고른다 (G-12.3-6) > 값 여섯 개를 여섯 칸에 전부 배정하고 확정까지 간다
TypeError: remainingFixedValues is not a function

 FAIL  src/session/creationView.test.ts > 남은 개수로 배정 가능한 값을 고른다 (G-12.3-6) > 풀에 하나뿐인 값은 여전히 한 칸에만 쓸 수 있다
TypeError: remainingFixedValues is not a function

 Test Files  1 failed (1)
      Tests  3 failed | 40 passed (43)
```

**GREEN — `remainingFixedValues` 함수를 더한 뒤, 같은 명령:**

```
 Test Files  1 passed (1)
      Tests  43 passed (43)
   Duration  356ms
```

## Files Created/Modified
- `frontend/src/session/creationView.ts` - `remainingFixedValues` export 추가(값별 남은 개수를 계산하는 순수 함수, 기존 export 열넷은 무변경)
- `frontend/src/session/creationView.test.ts` - 값이 중복된 풀을 쓰는 회귀 시험 셋 추가(`describe("남은 개수로 배정 가능한 값을 고른다 (G-12.3-6)")`, 기존 시험 43→~3건 추가로 43건 통과 확인 후 40+3=43)
- `frontend/src/panes/CreationPane.tsx` - `PlaceFixedValuesControl`의 잠금 판정을 개수 기반으로, 선택지 key를 자리 기준으로 교체

## Decisions Made
- 잠금 판정 근거를 개수로 바꾸되 서로 다른 값의 중복 배정 방지(사장님이 UAT에서 좋게 평가한 동작)는 개수 1의 특수 경우로 유지 — 새 개념을 안 만들고 기존 성질을 다시 세우는 쪽을 선택
- 같은 값을 두 번 보여 주는 표시를 접지 않음(계획의 `planner_assumptions` ①을 그대로 따름) — 표시 방식 재설계는 Phase 16 경계

## Deviations from Plan

None - plan executed exactly as written. Task 1의 커밋을 RED/GREEN 각각이 아니라 하나로 묶은 것은 계획 원문(`<reversibility>`: "더하는 것은 export 함수 하나와 그 시험뿐")이 이미 예정한 원자성이며 이탈이 아니다.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

- `12.3-UAT.md` 테스트 2를 막던 blocker가 코드 레벨에서 닫혔다 — **사람이 실제 브라우저로 다시 걸어야 확정된다** (위 coverage D3, `human_judgment: true`). 이 계획은 그 확인을 자동화하지 않는다.
- 테스트 3~8은 테스트 2가 통과해야 진행 가능 — 이 계획이 그 막힘을 풀었으므로 다음은 사람의 UAT 재시도다.
- 서버 코드(`src/gptrpg/`)는 한 줄도 안 바뀌었다 — `git diff --name-only -- src/gptrpg`가 빈 출력으로 확인됨.
- 룰북 값 배열(`2, 1, 1, 0, 0, -1`)은 제품 코드(`creationView.ts`, `CreationPane.tsx`)에 0건 — 시험 재료로만 쓰였다.

## Self-Check: PASSED

- `frontend/src/session/creationView.ts` 존재 확인 — FOUND
- `frontend/src/session/creationView.test.ts` 존재 확인 — FOUND
- `frontend/src/panes/CreationPane.tsx` 존재 확인 — FOUND
- 커밋 `700a87f` — FOUND (`git log --oneline --all | grep 700a87f`)
- 커밋 `ad82111` — FOUND (`git log --oneline --all | grep ad82111`)
- 모든 acceptance_criteria 재실행 — 전부 PASS (grep 카운트·tsc·build·vitest 84 passed·pytest 1382 passed·git diff 빈 출력 전부 재확인됨, 위 절 참조)
- 플랜 레벨 `<verification>`의 자동 검사 8건 전부 재확인 — PASS

---
*Phase: 12.3-creation-screen*
*Completed: 2026-08-22*
