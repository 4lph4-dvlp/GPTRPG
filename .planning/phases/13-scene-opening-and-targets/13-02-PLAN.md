---
phase: 13-scene-opening-and-targets
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/gptrpg/session_actor/actor.py
  - src/gptrpg/web/routes_creation.py
  - frontend/src/api/client.ts
  - frontend/src/screens/CreationScreen.tsx
  - frontend/src/panes/CreationPane.tsx
  - tests/test_creation_gm.py
  - tests/test_gm_slot.py
autonomous: true
requirements: [SCENE-01]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "**겹친 요청이 AI를 두 번 부르지 않는다** — 만들기의 GM 호출 네 곳(안내·지목·되묻기·정리)과 오프닝 한 곳, 다섯 자리 전부. 오늘은 두 탭이 동시에 들어오면 둘 다 큐 **밖** 사전 검사를 통과해 둘 다 제공자를 부르고, 큐 **안** 재검사(`AlreadyGmSpoken`)는 사건만 하나로 접는다 — **AI 호출 비용과 지연은 이미 샌 뒤다** (D-03)"
    - "**한 자리를 고쳐 다섯이 닫힌다** — 라우트마다 가드를 하나씩 다는 방식을 쓰지 않는다(D-03 명시). 13-01이 세운 `ClaimGmSlot`/`ReleaseGmSlot` 한 쌍을 다섯 호출부가 공유한다"
    - "**슬롯이 영구히 잠기지 않는다** — 제공자 실패·타임아웃·예외 어느 쪽이든 `finally`가 슬롯을 돌려준다. 한 번 실패한 세션에서 아무도 GM을 다시 못 부르는 상태를 만들지 않는다"
    - "**「다른 탭이 지금 부르고 있다」가 오류로 보이지 않는다** — 겹친 요청은 409를 받고, 화면은 그것을 실패가 아니라 「기다리면 온다」로 읽는다. 결과는 폴링이 실어 온다(D-02: 아무나 부르되 서버가 한 번만 낸다)"
    - "**AI 호출 횟수가 시험으로 고정된다** — 「사건이 하나다」가 아니라 **「제공자가 한 번 불렸다」** 를 세는 시험이다. 오늘의 결함은 정확히 「사건은 하나인데 호출은 둘」이라 사건만 세는 시험은 이 결함을 못 본다"
    - "**비-회귀:** 겹치지 않은 정상 경로의 동작이 한 글자도 안 바뀐다 — 안내·지목·되묻기·정리가 지금과 같은 응답을 같은 조건에 돌려주고, 이미 기록된 말을 다시 부르지 않는 캐시 경로도 그대로다"
  prohibitions:
    - statement: "「방장 탭만 부른다」는 프런트엔드 완화책을 쓰지 않는다 (D-03) — 사장님이 2026-08-23에 명시적으로 거부했다. 「여러 탭이 부른다」를 「방장이 탭을 닫으면 아무도 안 부른다」로 바꾸는 거래일 뿐이다"
      status: unresolved
    - statement: "라우트마다 가드를 하나씩 새로 달지 않는다 (D-03) — 다섯 자리가 같은 모양이라는 것이 이 수리의 전제다. 가드가 다섯 벌이면 여섯 번째 호출부가 생길 때 다시 샌다"
      status: unresolved
    - statement: "「지금 누가 AI를 부르는 중인가」를 사건으로 남기지 않는다 — 그 값이 서버 재시작을 넘어 살아남으면 재시작 뒤 슬롯이 영구히 잠긴다. 사건에 남길 것은 **말한 결과**뿐이다"
      status: unresolved
    - statement: "이미 기록된 GM의 말을 다시 만들지 않는다 (D-12) — 캐시 경로가 제공자 해석조차 하지 않는 성질(제공자 설정이 없어도 지난 안내를 볼 수 있다)을 이 수리가 깨지 않는다"
      status: unresolved
  artifacts:
    - tests/test_gm_slot.py
  key_links:
    - "겹친 두 요청 → 큐 밖 캐시 조회(둘 다 통과) → **큐 안 `ClaimGmSlot(dedupe_key)`** → 하나만 `GmSlotClaimed`, 나머지는 `GmSlotBusy` → 슬롯을 딴 쪽만 제공자를 부른다 → `RecordGmSpoke` → `finally`의 `ReleaseGmSlot`"
    - "`GmSlotBusy` → 라우트 409 → 화면이 오류로 안 읽고 대기 유지 → 폴링이 `creation_gm_spoke` 사건을 실어 온다 → 두 탭이 같은 말을 본다"
    - "슬롯 키 = `_gm_dedupe_key(...)`가 만드는 바로 그 문자열 — 중복 방지 기준이 두 벌로 갈라지지 않는다"
---

<objective>
**12.3 D-12의 「AI는 같은 자리에서 한 번만 불린다」는 결과는 지켜지는데 문언은 안 지켜지고 있다.**

만들기 화면의 GM 호출 네 곳(`routes_creation.py:787·860·993·1129`)이 전부 같은 모양이다:

1. 큐 **밖**에서 `state.creation_gm_said`를 조회해 「이미 말했나」를 본다
2. 없으면 제공자를 부른다 ← **AI 호출이 여기서 일어난다**
3. `RecordGmSpoke`를 제출한다 — 큐 **안**의 `_prepare_gm_spoke`가 두 번째를 `AlreadyGmSpoken`으로 막는다

①은 단일 소비자 큐 **밖의 일반 속성 읽기**라, 두 탭이 겹치면 **둘 다 통과해 둘 다 ②를 지난다.** ③이 사건은 하나로 접지만 **AI는 이미 두 번 불린 뒤다.** 12.3-14가 첫 안내를 자동 발동으로 바꾼 뒤로 **참가자 넷이면 네 번이 기본값**이 됐다.

13-01이 오프닝을 **다섯 번째 자동 발동 호출**로 새로 뚫으면서 같은 구멍을 다시 낼 뻔했고, 그래서 그 계획이 큐 **안** 슬롯(`ClaimGmSlot`/`ReleaseGmSlot`)을 먼저 세웠다. 이 계획은 그 자리를 기존 네 곳으로 넓힌다 — **한 자리를 고쳐 다섯이 닫힌다**(D-03이 명시적으로 요구한 모양이고, 라우트마다 가드를 하나씩 다는 방식은 채택하지 않기로 한 것이다).

Purpose: 오프닝을 자동 발동으로 만든 대가로 새로 뚫린 구멍을 같은 단계에서 닫는다 — 그리고 그 구멍이 원래 있던 네 자리도 함께 닫는다.
Output: 다섯 GM 호출 자리가 같은 슬롯을 공유하고, 「제공자가 몇 번 불렸는가」를 세는 회귀 시험이 그것을 고정한다.
</objective>

<planner_assumptions>
① **13-01이 `ClaimGmSlot`/`ReleaseGmSlot`/`GmSlotBusy`/`GmSlotClaimed`/`GmSlotReleased`를 이미 만들어 뒀다.** 이 계획은 그것을 새로 발명하지 않고 호출부만 넓힌다. 13-01의 SUMMARY에서 실제 시그니처를 먼저 확인할 것 — 계획서가 적은 이름과 다르면 **실제 코드를 따르고** 그 차이를 SUMMARY에 적는다.

② **슬롯 키는 `_gm_dedupe_key(...)`가 만드는 문자열을 그대로 쓴다.** 새 키 체계를 만들면 「중복 방지 기준」이 두 벌로 갈라지고, 지목(`nominate`)처럼 키에 흘려보냄 표시까지 이어 붙는 자리에서 둘이 어긋난다.

③ **`GmSlotBusy`에 409를 돌려주는 것이 이 계획의 유일한 API 계약 변화다.** 지금은 겹친 두 요청이 둘 다 200을 받는다(둘째는 `AlreadyGmSpoken` 경로로 첫째의 값을 받는다). 슬롯이 앞으로 옮겨 가면 둘째는 **아직 값이 없는 순간**에 막히므로 돌려줄 값 자체가 없다 — 그래서 409다. 화면이 그것을 오류로 안 읽게 만드는 것이 Task 2다.
</planner_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-scene-opening-and-targets/13-CONTEXT.md
@.planning/phases/13-scene-opening-and-targets/13-RESEARCH.md
@.planning/phases/13-scene-opening-and-targets/13-PATTERNS.md
@.planning/phases/13-scene-opening-and-targets/13-01-SUMMARY.md
@.planning/todos/pending/2026-08-23-gm-call-fires-twice-across-tabs.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 「제공자가 몇 번 불렸는가」를 세는 시험을 먼저 빨갛게 만들고, 네 GM 호출 자리를 13-01의 슬롯 뒤로 옮긴다</name>
  <reversibility rating="costly">만들기 GM 호출 네 곳과 오프닝 한 곳이 같은 검사 자리를 공유하게 된다(D-03 원문의 등급). 되돌리려면 다섯 자리를 함께 되돌려야 한다.</reversibility>
  <behavior>
    `tests/test_gm_slot.py` — **제공자 호출 횟수**를 세는 시험이다(사건 개수가 아니다).
    - 안내(`announce`): 두 요청이 겹쳐 들어오면 → 제공자 호출 **1회**, `creation_gm_spoke` 사건 **1건**, 한쪽은 200(말을 받는다)·다른 쪽은 409
    - 지목(`nominate`): 같은 후보 목록으로 겹쳐 들어오면 → 제공자 호출 1회
    - 되묻기(`follow_up`): 같은 캐릭터로 겹쳐 들어오면 → 제공자 호출 1회
    - 정리(`wrap_up`): 겹쳐 들어오면 → 제공자 호출 1회
    - **겹치지 않은 정상 경로:** 한 번 부르고 끝나면 제공자 호출 1회 + 200 + 지금과 같은 응답 모양
    - **이미 말한 뒤:** 캐시 경로가 그대로 이긴다 — 제공자 호출 **0회**, 200, 지난 말 그대로(제공자 설정이 아예 없어도 성공한다)
    - **실패해도 슬롯이 풀린다:** 제공자가 예외를 던진 뒤 같은 키로 다시 부르면 **막히지 않고** 다시 시도된다(제공자 호출 2회)
  </behavior>
  <files>tests/test_gm_slot.py, src/gptrpg/web/routes_creation.py, src/gptrpg/session_actor/actor.py</files>
  <read_first>
    - `.planning/todos/pending/2026-08-23-gm-call-fires-twice-across-tabs.md` **전문** — 원 문제, 채택하지 않기로 한 것, 해법 방향이 전부 적혀 있다. 이 계획이 그 문서의 이행이다
    - `src/gptrpg/web/routes_creation.py:723-760`(`_gm_dedupe_key` 전문 — 네 갈래의 키 규칙)와 **네 호출 자리 각각**: `:786-830`(`announce_creation`) · `:860-935`(`nominate_speaker`, 키에 `forfeited_nomination_mark`가 이어 붙는 자리 포함) · `:993-1090`(`follow_up`, `CREATION_FOLLOW_UP_MAX` 상한 갈래가 캐시 경로 **뒤**에 와야 하는 이유가 주석에 있다) · `:1129-1260`(`wrap_up`, 캐시 경로가 `fallback_intros`를 다시 조립하는 이유가 주석에 있다). **네 곳의 「캐시 조회 → AI 호출 → 제출」 순서를 눈으로 대조한다** — 같은 모양이라는 것이 이 수리의 전제다
    - `src/gptrpg/session_actor/actor.py`의 `ClaimGmSlot`/`ReleaseGmSlot`/`GmSlotBusy`/`GmSlotClaimed`/`GmSlotReleased`(13-01이 만든 것)와 `_prepare_gm_spoke`(1934-1970행) — 큐 안 재검사가 **왜 그것만으로는 부족한지**를 그 도크스트링이 이미 적고 있다
    - `src/gptrpg/web/routes_actions.py`의 오프닝 라우트(13-01이 만든 것) — **슬롯을 쓰는 정본이다.** `try/finally`의 정확한 모양을 그대로 베낀다
    - `tests/test_creation_gm.py` **전문** — 가짜 제공자를 어떻게 꽂는지, `TestClient`를 어떻게 여는지. **두 브라우저 시험은 중첩하지 말고 각자 순차 `with` 블록으로 연다**(중첩하면 액터의 `asyncio.Queue`가 다른 이벤트 루프를 쥐고 교착한다, 08-02 기록)
    - `tests/test_creation_consent_host.py` 또는 겹친 요청을 재현하는 기존 시험이 있으면 그 방식 — 없으면 `asyncio.gather`로 두 요청을 같은 루프에서 동시에 띄우는 방식을 쓴다
  </read_first>
  <action>
**⓪ 먼저 시험을 써서 빨간 것을 확보한다(RED).** `tests/test_gm_slot.py`(신규)에 `<behavior>`의 갈래를 하나씩 고정한다.

핵심은 **가짜 제공자에 호출 카운터를 단다**는 것이다 — 이 결함은 「사건은 하나인데 호출은 둘」이라, **사건 개수만 세는 시험은 이 결함을 절대 못 본다.** 시험 머리에 그 사실을 주석으로 적는다(이 저장소가 12.3에서 아홉 차례 반복해 겪은 실패 모양 — 감시 장치가 「확인 못 한 것」을 「통과」로 접는 것 — 과 같은 종류의 함정이다).

겹침 재현: 같은 `TestClient` 안에서 두 요청을 `asyncio.gather`로 동시에 띄운다. 가짜 제공자의 `complete`에 짧은 대기(예: `time.sleep`을 스레드 안에서)를 넣어 **첫 호출이 아직 안 끝난 동안 둘째가 사전 검사를 지나가게** 만든다 — 이 대기가 없으면 겹침이 재현되지 않아 시험이 초록으로 거짓 통과한다. 대기가 왜 필요한지를 주석으로 적는다.

`.venv/bin/python -m pytest -q --tb=short tests/test_gm_slot.py`를 돌려 **네 갈래가 「제공자 호출 2회」로 실패**하는 것을 확보하고, 그 출력을 SUMMARY에 그대로 옮겨 적는다. **이 빨간 출력이 이 계획의 존재 증명이다** — 오늘 결함이 실재한다는 것을 코드로 보인 것이고, 초록만 있는 SUMMARY는 아무것도 증명하지 않는다.

**① 네 호출 자리를 슬롯 뒤로 옮긴다(GREEN)** — `web/routes_creation.py`.

네 곳 **전부** 같은 모양으로 고친다. 기존 캐시 조회는 **지우지 않고 그대로 남긴다** — 그것이 「이미 말했으면 제공자 해석조차 안 한다」(설정이 없어도 지난 말을 볼 수 있다)라는 별개의 성질을 지키는 자리다. 슬롯은 그 **뒤에, 제공자 해석·AI 호출 앞에** 들어간다:

1. (기존 그대로) 캐시 조회 → 있으면 지난 값을 돌려주고 끝
2. **신설** — `await actor.submit(ClaimGmSlot(key))`. `GmSlotClaimed`면 통과, `GmSlotBusy`면 **409**로 끝낸다(`detail`은 「진행자를 지금 부르는 중이에요 — 잠시 뒤 화면에 나타납니다」류의 사람이 읽을 한국어 한 줄)
3. **신설** — 여기서부터 끝까지를 `try/finally`로 감싸고 `finally`에서 `await actor.submit(ReleaseGmSlot(key))`를 무조건 부른다
4. (기존 그대로) 제공자 해석 → AI 호출 → `RecordGmSpoke` 제출 → 응답
5. (기존 그대로) `AlreadyGmSpoken` 포착 경로를 **남긴다** — 슬롯이 「동시」를 막고 이것이 「이미 끝남」을 막는다. 둘 다 필요하다는 것을 각 자리에 한 줄 주석으로 적는다

**지목(`nominate`)의 키는 `forfeited_nomination_mark`까지 이어 붙인 최종 문자열을 슬롯 키로 쓴다** — 캐시 조회가 쓰는 것과 **같은 값**이어야 한다. 두 벌로 갈라지면 흘려보냄 회복 직후에 슬롯이 엉뚱한 키를 잡는다.

**되묻기(`follow_up`)의 상한 갈래(`CREATION_FOLLOW_UP_MAX`)는 슬롯보다 앞에 둔다** — 상한에 닿아 GM을 아예 안 부르는 경로가 슬롯을 잡았다 놓는 헛일을 하지 않게 한다. 단 **캐시 조회보다는 뒤**라는 기존 순서(그 이유가 주석에 있다)는 그대로다.

**정리(`wrap_up`)의 `CreateCharacter` 재제출 건너뛰기**도 기존 그대로 캐시 경로에 남는다.

**② 슬롯이 `RecordGmSpoke`보다 먼저 풀리지 않게 한다.** `finally`가 `RecordGmSpoke` 제출 **뒤**에 돌아야 한다 — 슬롯을 먼저 놓고 사건을 나중에 쓰면 그 틈에 들어온 셋째 요청이 캐시도 슬롯도 못 보고 또 AI를 부른다. `try` 블록이 응답 조립까지 감싸는지 눈으로 확인하고, 그 이유를 주석으로 적는다.

**③ 시험을 초록으로 만들고 두 출력을 나란히 남긴다.** `.venv/bin/python -m pytest -q --tb=short tests/test_gm_slot.py`가 전부 통과하는 출력을 ⓪의 빨간 출력과 나란히 SUMMARY에 붙인다.

**④ 접힌 할 일을 닫는다.** `.planning/todos/pending/2026-08-23-gm-call-fires-twice-across-tabs.md`를 `.planning/todos/done/`으로 옮기고, 파일 아래에 「어느 계획이 어떻게 닫았는지」 두세 줄을 더한다 — 큐 밖 사전 검사를 남긴 채 큐 안 **슬롯**을 앞에 세웠고, 그래서 캐시 성질(설정 없이도 지난 말을 본다)과 중복 방지가 둘 다 성립한다는 것.

**넘지 말 것:** 프런트엔드를 이 Task에서 안 연다(Task 2). `_gm_dedupe_key`의 키 규칙을 한 글자도 안 바꾼다 — 이 계획은 **언제 검사하는가**만 옮긴다. `_prepare_gm_spoke`의 `AlreadyGmSpoken` 단락을 지우지 않는다. 오프닝 라우트를 안 고친다(13-01이 이미 옳게 썼다). 새 사건 종류를 안 만들고 `EVENT_SCHEMA_VERSION`을 안 올린다 — 슬롯은 사건이 아니다.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q --tb=short tests/test_gm_slot.py</automated>
    <automated>.venv/bin/python -m pytest -q --tb=short tests/test_creation_gm.py tests/test_creation_flow.py tests/test_creation_follow_up_answer.py tests/test_scene_opening.py</automated>
    <automated>.venv/bin/python -m pytest -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - **네 갈래가 「제공자 호출 2회」로 실패한 빨간 pytest 출력**이 `13-02-SUMMARY.md`에 그대로 붙어 있고, 고친 뒤 초록 출력이 나란히 붙어 있다. **이 항목이 이 Task의 본체다** — 초록만 있는 SUMMARY는 결함이 실재했다는 것을 증명하지 않는다.
    - `.venv/bin/python -m pytest -q --tb=short tests/test_gm_slot.py` 가 종료 코드 `0`이다.
    - `.venv/bin/python -m pytest -q --tb=short` 가 종료 코드 `0`이다 — 기존 시험이 하나도 안 깨진다.
    - `grep -vE '^\s*#' src/gptrpg/web/routes_creation.py | grep -c 'ClaimGmSlot'` 가 정확히 `4`이다 — 네 자리 **전부** 옮겨졌다(하나라도 빠지면 그 자리가 계속 샌다).
    - `grep -vE '^\s*#' src/gptrpg/web/routes_creation.py | grep -c 'ReleaseGmSlot'` 가 정확히 `4`이다 — 네 자리 전부 슬롯을 돌려준다.
    - `grep -vE '^\s*#' src/gptrpg/web/routes_creation.py | grep -c 'finally:'` 가 `3`보다 크다.
    - `grep -vE '^\s*#' src/gptrpg/web/routes_creation.py | grep -c 'AlreadyGmSpoken'` 가 `3`보다 크다 — 큐 안 「이미 끝남」 방어선이 지워지지 않았다.
    - `git diff HEAD~1 -- src/gptrpg/web/routes_creation.py | grep -c '^-.*_gm_dedupe_key'` 가 `0`이다 — 키 규칙을 안 건드렸다.
    - `grep -c 'EVENT_SCHEMA_VERSION = 12' src/gptrpg/event_log/schema.py` 가 `1`이다 — 판을 또 올리지 않았다.
    - `test -f .planning/todos/done/2026-08-23-gm-call-fires-twice-across-tabs.md` 가 참이고 `test -f .planning/todos/pending/2026-08-23-gm-call-fires-twice-across-tabs.md` 가 거짓이다.
    - `git diff --name-only HEAD~1 -- frontend/` 의 출력이 비어 있다.
  </acceptance_criteria>
  <done>겹친 두 요청이 제공자를 **한 번만** 부르는 것이 네 GM 자리 전부에서 시험으로 고정됐고, 그 시험이 고치기 전에는 「2회」로 빨갰으며, 캐시 경로와 「이미 끝남」 방어선은 그대로 살아 있고, 실패한 호출도 슬롯을 돌려준다.</done>
</task>

<task type="auto">
  <name>Task 2: 「다른 탭이 지금 부르는 중」이 화면에 오류로 안 보이게 한다</name>
  <reversibility rating="reversible">더하는 것은 상태 코드 하나를 갈라 읽는 갈래다. 그 코드가 안 오는 기존 경로의 동작이 안 바뀐다.</reversibility>
  <files>frontend/src/api/client.ts, frontend/src/screens/CreationScreen.tsx, frontend/src/panes/CreationPane.tsx</files>
  <read_first>
    - `frontend/src/api/client.ts` **전문** — 기존 POST 헬퍼가 오류를 어떻게 던지는지, 상태 코드를 실어 보내는지. **13-01이 오프닝 호출에서 409를 어떻게 갈라 읽었는지**를 먼저 보고 같은 방식을 쓴다(두 벌이 갈라지지 않게)
    - `frontend/src/screens/CreationScreen.tsx`의 `announce()`(12.3-14가 고친 자리)와 그 실패 처리 — 실패가 `error` 상태에 남고, 그 값이 자동 발동 조건(`shouldAnnounce`)을 거짓으로 만들며, 재시도 단추가 뜬다. **409가 그 경로로 새면 「다른 탭이 부르는 중」이 「실패했으니 다시 눌러라」로 보인다**
    - `frontend/src/panes/CreationPane.tsx:509-540`(자동 지목 effect)와 되묻기·정리 호출부 — 나머지 세 자리의 실패 처리
    - `frontend/src/session/creationView.ts`의 `creationErrorMessage` — 503(운영자 설정 문제)은 고정 문구, 그 밖의 서버 이유는 그대로(D-13 ②/D-15). **409는 이 함수에 도달하기 전에 갈라져야 한다**
    - `.planning/phases/12.3-creation-screen/12.3-CONTEXT.md`의 **D-13**(세 가지 실패를 다르게 말한다) — 409는 그 셋 중 어느 것도 아니다. **실패가 아니기 때문이다**
  </read_first>
  <action>
**① 409를 오류가 아닌 것으로 갈라 읽는다** — `frontend/src/api/client.ts`.

13-01이 오프닝 호출에서 쓴 방식을 그대로 쓴다 — 던지는 오류 객체가 상태 코드를 실어 호출부가 구분할 수 있게 한다(이미 그런 모양이면 그대로 쓰고, 아니면 **한 자리에서** 갈라 준다. 두 벌로 갈라지면 다섯 호출부의 처리가 어긋난다).

**② 네 만들기 호출부가 409를 「기다리면 온다」로 읽는다** — `CreationScreen.tsx`(안내) · `CreationPane.tsx`(지목·되묻기·정리).

각 자리에서:
- 409면 `error` 상태를 **채우지 않는다.** 대기 표시를 유지하고, in-flight ref를 놓아 다음 폴링 뒤 조건이 다시 참이 되면 자연히 재시도되게 둔다 — 다만 폴링이 결과를 실어 오면 조건 자체가 거짓이 되므로 실제로는 다시 안 부른다.
- 409가 아닌 실패는 **지금 그대로** 처리한다 — 503은 `COPY.creationGmUnavailable`, 그 밖은 서버가 보낸 이유 그대로, 자동 재시도 고리는 없다(D-13, 12.3-14가 세운 규율).
- 각 자리에 한 줄 주석: **409는 실패가 아니라 「같은 세션의 다른 탭이 지금 그 말을 만들고 있다」이고, 그 결과는 사건으로 남아 폴링이 실어 온다**(D-02: 아무나 부르되 서버가 한 번만 낸다). 이것을 오류로 그리면 사람이 「내가 눌러야 하나」로 돌아간다 — 12.3-14가 없앤 바로 그 혼동이다.

**③ 문구를 새로 만들지 않는다.** 409에는 화면에 새 문장이 붙지 않는다 — 이미 떠 있는 대기 표시가 사실을 말하고 있다. `COPY`에 새 키를 더하지 않는다.

**넘지 말 것:** 서버를 안 고친다. `creationErrorMessage`의 기존 갈래를 안 바꾼다. 자동 재시도 고리를 안 만든다. 새 CSS·새 문구·새 컴포넌트를 안 만든다. 「방장 탭만 부른다」류의 호출자 제한을 절대 넣지 않는다(D-03).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
    <automated>cd frontend &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` 이 빈 출력이고 `npm run build` 가 성공한다.
    - `cd frontend && npx vitest run` 이 종료 코드 `0`이다.
    - `grep -vE '^\s*\*|^\s*//|^\s*/\*' frontend/src/screens/CreationScreen.tsx | grep -c '409'` 가 `0`보다 크다.
    - `grep -vE '^\s*\*|^\s*//|^\s*/\*' frontend/src/panes/CreationPane.tsx | grep -c '409'` 가 `2`보다 크다 — 지목·되묻기·정리 세 자리가 전부 갈라 읽는다.
    - `git diff HEAD~1 -- frontend/src/labels.ts | wc -l` 이 `0`이다 — 새 문구를 안 더했다.
    - `git diff --name-only HEAD~1 -- src/gptrpg/` 의 출력이 비어 있다.
    - `grep -rin 'host.*only\|방장.*만' frontend/src/screens/CreationScreen.tsx frontend/src/panes/CreationPane.tsx | grep -ci 'announce\|nominate\|wrap_up\|follow_up'` 가 `0`이다 — 호출자를 방장으로 제한하는 완화책이 안 들어갔다(D-03).
  </acceptance_criteria>
  <done>겹친 탭이 받는 409가 화면에서 실패로 보이지 않고 대기 상태가 유지되며, 결과는 폴링이 실어 온다 — 「기다려야 하나 눌러야 하나」가 이 갈래에서 다시 생기지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (13-02)

**새 파일**
- `tests/test_gm_slot.py`

**옮긴 파일**
- `.planning/todos/pending/2026-08-23-gm-call-fires-twice-across-tabs.md` → `.planning/todos/done/`

**새 심볼**
- 없음 — 이 계획은 13-01이 만든 `ClaimGmSlot`/`ReleaseGmSlot`/`GmSlotBusy`/`GmSlotClaimed`/`GmSlotReleased`의 **호출부만** 넓힌다. 새 클래스·함수·상수·사건 종류·스키마 판을 하나도 만들지 않는다.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 여럿 → 만들기 GM 라우트 넷 | 겹친 요청이 같은 세션의 같은 dedupe 키를 노린다 |
| 라우트 → AI 제공자 | 요청 하나가 돈이 드는 외부 호출을 일으킨다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Denial of Service | 만들기 GM 호출 네 곳 | high | mitigate | 큐 안 `ClaimGmSlot`이 AI 호출 **앞**에 선다 — 탭 N개가 동시에 들어와도 제공자 호출은 1회. 시험이 **호출 횟수**로 고정한다(사건 개수가 아니다) |
| T-13-08 | Denial of Service | 슬롯 영구 점유 | high | mitigate | 네 자리 전부 `try/finally`로 `ReleaseGmSlot`을 무조건 부른다. 「제공자 실패 뒤 같은 키로 다시 부르면 막히지 않는다」를 시험이 고정한다 |
| T-13-09 | Tampering | 슬롯 키 ↔ dedupe 키 어긋남 | medium | mitigate | 슬롯 키로 `_gm_dedupe_key(...)`의 **최종 문자열**(지목의 흘려보냄 표시 포함)을 그대로 쓴다. 새 키 체계를 만들지 않는다 |
| T-13-10 | Information Disclosure | 409 `detail` 문구 | low | mitigate | 사람이 읽을 한국어 한 줄만 담는다 — 세션 내부 상태·다른 브라우저 식별자·dedupe 키를 싣지 않는다(12.3의 `creation_host_browser_id` 유출 판단과 같은 규율) |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | **설치 대상 없음** — 새 꾸러미가 생기면 이 계획을 벗어난 것이다 |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short` 전체 초록
- `cd frontend && npx tsc --noEmit && npm run build && npx vitest run` 전부 성공
- 빨강(호출 2회) → 초록(호출 1회) 두 pytest 출력이 SUMMARY에 나란히 있다
- `grep -c 'ClaimGmSlot' src/gptrpg/web/routes_creation.py` 가 정확히 4 — 다섯 자리 중 넷이 이 계획, 하나(오프닝)는 13-01
</verification>

<success_criteria>
1. 겹친 두 요청이 GM 제공자를 한 번만 부른다 — 네 만들기 자리 전부에서 시험으로 고정
2. 그 시험이 고치기 전에는 「2회」로 실패했다는 출력이 기록에 남아 있다
3. 제공자가 실패해도 슬롯이 풀려 같은 키로 다시 시도할 수 있다
4. 이미 말한 뒤의 캐시 경로가 제공자 설정 없이도 그대로 성공한다
5. 겹친 탭이 받는 409가 화면에서 오류로 보이지 않는다
6. `_gm_dedupe_key`의 키 규칙·`AlreadyGmSpoken` 방어선·사건 스키마 판이 하나도 안 바뀐다
</success_criteria>

<output>
Create `.planning/phases/13-scene-opening-and-targets/13-02-SUMMARY.md` when done
</output>
