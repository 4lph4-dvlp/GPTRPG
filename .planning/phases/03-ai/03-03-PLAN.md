---
phase: 03-ai
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/gptrpg/agents/invoke.py
  - src/gptrpg/agents/action_classifier.py
  - src/gptrpg/agents/master_gm.py
  - tests/test_agents_retry.py
  - tests/test_master_gm.py
autonomous: true
requirements: [MEAS-02, RIG-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "action_classifier 호출은 5초를 넘기면 끊긴다 (D-27)"
    - "master_gm 호출은 15초를 넘기면 끊긴다 — 이 값은 이번 논의로 새로 정한 것이 아니라 이미 확정된 응답 속도 결정을 그대로 쓴 것이다 (D-27 이 명시적으로 건드리지 않기로 한 값)"
    - "타임아웃이든 모델 오류든 실패하면 **정확히 한 번** 더 시도하고 끝낸다 — 한 번의 호출 요청이 제공자를 세 번 이상 때리는 경로가 없다 (D-28)"
    - "실패 종류에 따라 갈래가 갈리지 않는다 — 오류 종류별 코드 구분과 종류별 재시도 규칙을 만들지 않는다 (D-28 이 v1 에서 잘라낸 것)"
    - "재시도까지 실패하면 action_classifier 는 예외를 밖으로 던지지 않고 **빈 후보**를 돌려준다 — 이미 있는 「무브 없음」 경로가 그대로 이어받는다. 새 분기 코드를 만들지 않는다 (D-29)"
    - "실패한 호출도 응답 껍데기를 돌려준다 — 성공/실패 칸이 거짓이고, 걸린 시간은 두 번의 시도를 합친 **실제로 잰 값**이며, 토큰 칸은 0이다. 실패를 기록에서 지우지 않는다 (D-30, MEAS-02)"
    - "서사는 마침표·물음표·느낌표로 끝나는 문장마다 조각 하나로 나온다 — 문장 끝 표시가 하나도 없는 응답도 조각 하나로 나오고 0개가 되지 않는다 (RIG-03)"
    - "스트림이 문장 중간에서 끝나도 버퍼에 남은 글자가 마지막 조각으로 나온다 — 조용히 잘려 사라지지 않는다 (RIG-03)"
    - "공백뿐인 조각은 내보내지 않는다 — 빈 텍스트는 서사 사건으로 기록될 수 없다 (Phase 1 의 `AppendNarration` 검증 규율)"
    - "한국어 문장 종결(…다. / …요. / …까?)이 영어 문장과 같은 규칙으로 조각난다 (RIG-03)"
    - statement: "실제 제공자가 타임아웃을 어떤 예외로 알리는지 — 다섯 SDK 가 서로 다른 예외 형을 쓸 수 있고, 이 계획은 종류를 가리지 않고 잡는 쪽을 골랐으므로 실제 타임아웃이 정말 잡히는지는 실제 호출로만 확인된다"
      verification: backstop
  artifacts:
    - src/gptrpg/agents/invoke.py
    - tests/test_agents_retry.py
    - tests/test_master_gm.py
  key_links:
    - "`invoke.call_with_one_retry` 가 돌려주는 실패 껍데기 → CLI 의 `RecordAiCall` → `ai_invoked` 사건 — 이 사슬이 끊기면 Phase 6 이 실패율을 셀 수 없고, 성공한 호출만 남아 원가·품질 숫자가 실제보다 좋아 보인다"
    - "`classify` 의 빈 후보 → §4.7 의 「무브 없음」 경로 → 플레이어가 다음 턴에 직접 판정 명령을 친다(D-36) — 새 되돌리기 화면을 만들지 않는 근거"
  prohibitions:
    - statement: "실패하거나 거부된 호출을 기록에서 빼거나 성공으로 뭉뚱그려, 나중에 가설 채점(HYP-04·HYP-05)이 실제보다 좋게 나오게 만들지 않는다"
      status: kept
      verification: flagged-unverified
---

<objective>
03-01 이 뚫어 둔 길의 **실패 쪽**을 만든다. 응답이 안 오거나 모델이 오류를 내도 게임이 멈추지 않게 하는 최소 규칙 — 타임아웃 두 값, 재시도 한 번, 그리고 실패했을 때 이미 있는 「무브 없음」 경로로 떨어지는 길이다. 함께 서사 조각내기를 트레이서가 증명한 것보다 단단하게 만든다.

Purpose: 에이전트 응답 껍데기와 경량 모델의 타임아웃·재시도는 v1 에만 있고 유지·폐기 진술이 없던 항목이다(`STATE.md` Blockers #2). ROADMAP 이 「여기서 최소한만 정하고 넘어갈 것」으로 지목했고 D-27~D-30 이 그 최소한을 정했다. 이 계획은 정해진 최소한을 정확히 그만큼만 만든다.

Output: 호출 한 번을 감싸는 타임아웃·재시도 층, 실패 시 빈 후보로 떨어지는 분류기, 문장 경계가 단단해진 진행자.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 호출 한 번을 감싸는 타임아웃·재시도 층</name>
  <files>src/gptrpg/agents/invoke.py, tests/test_agents_retry.py</files>
  <read_first>
    - `src/gptrpg/agents/envelope.py` — `AgentResult` 의 다섯 칸. 실패 껍데기가 어떤 값을 갖는지 여기서 정해진다
    - `src/gptrpg/agents/providers/base.py` — `complete`/`stream` 의 `timeout_s` 매개변수가 이미 프로토콜에 있다는 것
    - `src/gptrpg/session_actor/actor.py` 187~195행 — 소비자 루프의 `except Exception as exc: ... future.set_exception(exc)`. 「조용히 삼키지 않는다, 부르는 쪽에 전달한다」 규율을 여기서 베낀다
    - `tests/conftest.py` — `FakeProvider` 픽스처. 이 시험은 호출 횟수를 세고 일부러 실패시키는 변형이 필요하다
    - `.planning/phases/03-ai/03-CONTEXT.md` — D-27·D-28·D-30 원문
  </read_first>
  <behavior>
    - 첫 시도가 성공하면 제공자는 정확히 1번 불린다
    - 첫 시도가 실패하고 두 번째가 성공하면 제공자는 정확히 2번 불리고, 돌려주는 껍데기의 성공 칸이 참이다
    - 두 시도 모두 실패하면 제공자는 정확히 2번 불리고 — 3번은 절대 아니고 — 돌려주는 껍데기의 성공 칸이 거짓이다
    - 두 번 다 실패한 껍데기의 `elapsed_ms` 는 두 시도를 합친 값이고 0 이상이다. `prompt_tokens`·`completion_tokens` 는 둘 다 0이다
    - 예외의 **종류에 따라 동작이 달라지지 않는다** — 서로 다른 예외 형 두 개로 같은 시험을 돌리면 같은 결과가 나온다
    - `CLASSIFIER_TIMEOUT_S` 는 5.0, `GM_TIMEOUT_S` 는 15.0 이다
  </behavior>
  <action>
`agents/invoke.py` 를 만든다. 상수 셋을 선언한다 — `CLASSIFIER_TIMEOUT_S = 5.0` (D-27), `GM_TIMEOUT_S = 15.0` (이미 확정되어 있던 응답 속도 결정을 그대로 쓴다. D-27 이 이 값은 건드리지 않는다고 명시했다), `MAX_ATTEMPTS = 2` (D-28 — 첫 시도 + 재시도 한 번, 그것으로 끝).

`call_with_one_retry(fn: Callable[[], AgentResult], *, timeout_s: float) -> AgentResult` 를 만든다. `fn` 은 이미 `timeout_s` 를 제공자에 넘기도록 묶인 무인자 호출이다. 동작은 다음과 같다 — 벽시계로 시작 시각을 잰다. `MAX_ATTEMPTS` 번까지 `fn()` 을 부른다. 성공하면 그 껍데기의 `elapsed_ms` 를 지금까지 잰 총 경과 시간으로 갈아 끼워 돌려준다(제공자가 잰 한 시도의 시간이 아니라 사람이 기다린 총 시간이 MEAS-02 가 재려는 값이다). 예외가 나면 예외 **종류를 가리지 않고** 잡아 다음 시도로 넘어간다 — 오류 종류별 코드나 종류별 재시도 규칙을 만들지 않는다(D-28 이 v1 에서 잘라낸 것). 재시도 사이에 지연을 두지 않는다(즉시 재시도) — 이유를 도크스트링에 한 줄 적는다: 두 타임아웃(5초·15초)이 이미 사람이 느끼는 지연의 상한이고, 여기에 대기를 더하면 D-33 의 응답 속도 목표를 그만큼 더 밀어낸다.

두 시도가 모두 실패하면 예외를 밖으로 던지지 않고 실패 껍데기를 돌려준다 — 성공 칸 거짓, 값 `None`, `elapsed_ms` 는 두 시도의 총 경과, 토큰 두 칸은 0. 마지막 예외는 버리지 않고 껍데기와 함께 **부르는 쪽이 볼 수 있게** 로그 대신 반환값에 담는다: 이를 위해 `agents/envelope.py` 를 건드리지 않고 `invoke.py` 안에 `@dataclass(frozen=True) FailedCall` 을 두어 `result: AgentResult` 와 `last_error_text: str` 두 칸을 갖게 하고, `call_with_one_retry` 는 성공/실패 어느 쪽이든 `AgentResult` 를 돌려주되 마지막 예외의 문자열을 `agents.invoke.LAST_ERROR_TEXT` 같은 전역이 아니라 선택적 반환 경로로 전달하는 방식을 쓴다 — 구체적으로 `call_with_one_retry` 는 `tuple[AgentResult, str | None]` 을 돌려주고 두 번째 칸이 마지막 실패 사유 문자열이다. 전역 상태를 만들지 않는다. 실패 사유 문자열에는 예외의 문자열 표현만 넣고 자격 증명이 들어갈 수 있는 값(요청 헤더·환경 변수)은 넣지 않는다.

`tests/test_agents_retry.py` 를 만든다. `FakeProvider` 를 감싸 호출 횟수를 세고 지정한 횟수만큼 지정한 예외를 던지는 작은 시험용 이중체를 시험 파일 안에 선언한다(`conftest.py` 는 03-01 소유이므로 이 계획은 자기 시험 파일 안에서만 이중체를 만든다). 위 `<behavior>` 여섯 항목을 그대로 시험으로 옮긴다. 특히 「3번은 절대 아니다」는 호출 횟수를 정확히 2로 단언해 못박는다.
  </action>
  <verify>
    <automated>cd /home/alpha-pi/GPTRPG && uv run pytest tests/test_agents_retry.py -x && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_agents_retry.py -x` 가 0으로 끝난다
    - `python -c "from gptrpg.agents.invoke import CLASSIFIER_TIMEOUT_S, GM_TIMEOUT_S, MAX_ATTEMPTS; assert (CLASSIFIER_TIMEOUT_S, GM_TIMEOUT_S, MAX_ATTEMPTS) == (5.0, 15.0, 2)"` 가 0으로 끝난다
    - 두 시도 모두 실패시키는 시험이 제공자 호출 횟수를 정확히 2로 단언한다
    - 서로 다른 예외 형 두 개로 같은 시험을 돌려 같은 결과가 나오는 시험이 존재한다
    - `uv run lint-imports` 가 계약 세 개를 전부 통과한다
  </acceptance_criteria>
  <done>호출 한 번을 5초/15초 안에 끊고 실패하면 딱 한 번 더 시도한 뒤 실패 껍데기를 돌려주는 층이 있고, 그 껍데기의 걸린 시간이 사람이 실제로 기다린 총 시간이다. 커밋되어 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 두 에이전트를 타임아웃·재시도 층 뒤로 옮기고, 실패를 「무브 없음」으로 떨어뜨린다</name>
  <files>src/gptrpg/agents/action_classifier.py, src/gptrpg/agents/master_gm.py, tests/test_agents_retry.py</files>
  <read_first>
    - `src/gptrpg/agents/action_classifier.py` — 03-01 이 만든 `classify` 의 현재 서명과 `UnknownMove` 검증 자리
    - `src/gptrpg/agents/master_gm.py` — 03-01 이 만든 `narrate` 와 `chunk_sentences`
    - `src/gptrpg/agents/invoke.py` — Task 1 이 만든 `call_with_one_retry` 와 두 타임아웃 상수
    - `src/gptrpg/agents/envelope.py` — `AgentResult`
    - `.planning/phases/03-ai/03-CONTEXT.md` — D-29 원문(재시도까지 실패하면 「판정 없이 진행」으로 자동으로 떨어진다), D-36(되돌리기 화면을 만들지 않는 이유)
  </read_first>
  <behavior>
    - 제공자가 두 번 다 실패해도 `classify` 는 예외를 던지지 않는다 — 후보가 빈 `Proposal` 을 돌려주고, 그 안의 응답 껍데기 성공 칸이 거짓이다
    - 제공자가 두 번 다 실패한 뒤 돌려받은 껍데기의 `elapsed_ms` 가 0 이상이고 토큰 두 칸이 0이다 (기록에 남길 수 있는 값이다)
    - 모델이 룰북 목록에 없는 무브 이름을 돌려주면 `UnknownMove` 가 뜬다 — 이것은 재시도 대상이 아니라 즉시 드러나야 할 잘못이다 (모델 실패와 목록 위반은 다른 일이다)
    - `narrate` 가 쓰는 타임아웃은 15초이고 `classify` 가 쓰는 타임아웃은 5초다
    - 제공자가 첫 조각을 흘려보낸 뒤 스트림 도중에 실패하면 이미 나온 조각은 살아남고, 껍데기의 성공 칸이 거짓이 된다
  </behavior>
  <action>
`action_classifier.classify` 를 고쳐 제공자를 직접 부르지 않고 `call_with_one_retry(..., timeout_s=CLASSIFIER_TIMEOUT_S)` 를 거치게 한다. 돌려받은 껍데기의 성공 칸이 거짓이면 **예외를 던지지 않고** 후보가 빈 `Proposal(candidates=(), ai=그 껍데기)` 를 돌려준다 — §4.7 의 「무브 없음」 경로가 이것을 그대로 이어받는다(D-29). 새 분기 코드나 새 실패 상태를 만들지 않는다: 「모델이 아무 무브도 못 골랐다」와 「모델이 응답을 못 했다」가 플레이어에게는 같은 화면(판정 없이 진행)으로 보인다는 것이 D-29 가 고른 설계다. 그 이유를 함수 도크스트링에 한 줄 남긴다.

무브 목록 위반은 다르게 다룬다 — 모델이 응답을 하긴 했는데 룰북 목록에 없는 이름을 골랐다면 그것은 제공자 장애가 아니라 계약 위반이므로 `UnknownMove` 를 그대로 던진다. 재시도 층이 이 예외를 잡아 다시 시도하는 일이 없도록, 목록 대조는 `call_with_one_retry` **밖에서** — 껍데기를 돌려받은 뒤에 — 한다. 이 배치 이유도 도크스트링에 남긴다.

`master_gm.narrate` 를 고쳐 `provider.stream` 호출을 `GM_TIMEOUT_S` 로 묶는다. 스트리밍은 「호출 한 번」과 모양이 달라 재시도 의미가 애매하다 — 첫 조각이 나오기 전에 실패했으면 재시도하고, 이미 조각이 하나라도 나갔으면 재시도하지 않고 거기서 끝낸다(이미 화면에 찍힌 문장을 두 번 쓰게 만들지 않는다). 이 규칙을 함수 도크스트링에 적고, 어느 쪽이든 끝난 뒤 `provider.last_result()` 가 성공/실패가 반영된 껍데기를 돌려주게 한다.

`tests/test_agents_retry.py` 에 위 `<behavior>` 다섯 항목을 시험으로 더한다. 스트림 도중 실패 시험은 이중체가 조각 두 개를 낸 뒤 예외를 던지게 만들어, 나온 조각 두 개가 살아 있고 껍데기 성공 칸이 거짓인지 확인한다.
  </action>
  <verify>
    <automated>cd /home/alpha-pi/GPTRPG && uv run pytest tests/test_agents_retry.py -x && uv run pytest -q && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_agents_retry.py -x` 가 0으로 끝난다
    - 제공자를 두 번 다 실패시켰을 때 `classify` 가 예외 없이 빈 후보를 돌려주는 것을 단언하는 시험이 존재한다
    - 룰북 목록에 없는 무브 이름에 대해 `UnknownMove` 가 뜨는 것을 단언하는 시험이 존재하고, 그때 제공자 호출 횟수가 1인 것도 함께 단언한다 (목록 위반이 재시도를 유발하지 않는다)
    - `uv run pytest -q` 로 `tests/test_turn_tracer.py` 를 포함한 기존 시험이 전부 그대로 통과한다 (트레이서가 만든 성공 경로가 깨지지 않았다)
    - `uv run lint-imports` 가 계약 세 개를 전부 통과한다
  </acceptance_criteria>
  <done>두 에이전트가 타임아웃·재시도 층 뒤에서 돌고, 분류기 실패가 예외가 아니라 「무브 없음」이라는 정상 결과로 떨어지며, 실패한 호출도 기록할 수 있는 껍데기를 남긴다. 커밋되어 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 서사 문장 조각내기를 경계값까지 단단하게</name>
  <files>src/gptrpg/agents/master_gm.py, tests/test_master_gm.py</files>
  <read_first>
    - `src/gptrpg/agents/master_gm.py` — `chunk_sentences` 의 현재 구현
    - `src/gptrpg/event_log/schema.py` 104~115행 — `NarrationAppended` 도크스트링(「`chunk_index` 가 0인 사건의 시각이 서사 첫 글자 시점이다」, 판정과 서사를 나눈 이유)
    - `src/gptrpg/session_actor/actor.py` `_prepare_narration` — 빈 텍스트와 음수 `chunk_index` 를 거부하는 검증
    - `tests/test_resolution_edges.py` — Phase 1 이 세운 경계값 시험의 작성 방식(경계·인접·빈입력·순서)
  </read_first>
  <behavior>
    - 문장 셋짜리 응답이 조각 셋으로 나온다
    - 문장 끝 표시가 하나도 없는 한 덩어리 응답이 조각 **하나**로 나온다 (0개가 아니다)
    - 스트림이 문장 중간에서 끊긴 응답의 남은 글자가 마지막 조각으로 나온다
    - 델타 경계가 문장 끝 표시 한가운데를 가르는 경우(한 델타가 마침표 직전까지, 다음 델타가 마침표부터)에도 문장이 정확히 하나로 합쳐진다
    - 공백·줄바꿈뿐인 델타만 오면 조각이 0개다
    - 한국어 종결(…다. / …요. / …까?)이 영어 문장과 같은 규칙으로 조각난다
    - 문장 사이 여러 칸 공백·줄바꿈이 조각 앞뒤에서 다듬어져 빈 조각을 만들지 않는다
    - 조각 순서가 들어온 순서와 같다 (뒤집히거나 뒤섞이지 않는다)
  </behavior>
  <action>
`chunk_sentences` 를 위 `<behavior>` 여덟 항목이 전부 참이 되도록 다듬는다. 핵심은 세 가지다 — ① 문장 끝 판정은 **버퍼 전체**에 대해 하지 델타 하나에 대해 하지 않는다(델타 경계가 마침표를 가를 수 있다) ② 마지막에 버퍼에 남은 것은 문장 끝 표시가 없어도 반드시 한 조각으로 내보낸다 ③ 다듬은 결과가 빈 문자열이면 내보내지 않는다. 언어별 형태소 분석기를 붙이지 않는다 — 구두점 경계로 충분하고, 기록에 남는 것은 조각을 이어 붙인 원문이라 조각 경계가 언어학적으로 완벽할 필요가 없다(RESEARCH.md 「Don't Hand-Roll」).

`narrate` 가 흘려보내는 조각에 대해 부르는 쪽이 붙일 `chunk_index` 가 0부터 1씩 오르도록, `narrate` 를 `enumerate` 하기 좋은 순수 반복자로 유지한다 — 색인을 `narrate` 안에서 세지 않는다(부르는 쪽이 이미 사건 순번과 함께 세고 있다).

`tests/test_master_gm.py` 를 만들어 위 여덟 항목을 각각 시험 하나로 옮긴다. 델타를 잘게 쪼갠 목록으로 직접 넘겨 제공자 없이도 도는 순수 함수 시험으로 만든다. 여기에 더해 `narrate` 를 시험용 이중체로 한 번 돌려 나온 조각 수가 2 이상이고 순서가 유지되는지 확인한다.
  </action>
  <verify>
    <automated>cd /home/alpha-pi/GPTRPG && uv run pytest tests/test_master_gm.py -x && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_master_gm.py -x` 가 0으로 끝난다
    - `tests/test_master_gm.py` 에 위 `<behavior>` 여덟 항목에 대응하는 시험 함수가 각각 존재한다 (여덟 개 이상)
    - 문장 끝 표시가 없는 한 덩어리 입력에 대해 결과 길이가 1임을 단언하는 시험이 존재한다
    - 델타 경계가 문장 끝 표시를 가르는 입력에 대해 결과 길이가 1임을 단언하는 시험이 존재한다
    - 한국어 종결 세 형태를 담은 시험이 존재한다
    - `uv run pytest -q` 로 기존 시험이 전부 그대로 통과한다
  </acceptance_criteria>
  <done>서사 조각내기가 여덟 개의 경계 상황에서 전부 기대대로 동작하고, 어떤 입력에서도 글자가 조용히 사라지지 않는다. 커밋되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 프로세스 → 제공자 API (재시도 경로) | 실패한 호출이 다시 나간다 — 횟수가 곧 비용이다 |
| 제공자 응답 → 무브 목록 대조 | 모델 출력이 닫힌 목록과 대조되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04b | Denial of Service (자기 비용) | `agents/invoke.py` | high | mitigate | `MAX_ATTEMPTS = 2` 를 상수로 못박고, Task 1 의 합격 기준이 실패 시 제공자 호출 횟수를 **정확히 2**로 단언한다. 재시도 사이 지연이 없으므로 한 턴이 붙잡는 총 시간의 상한은 두 타임아웃의 두 배(분류기 10초, 진행자 30초)로 계산 가능하다 |
| T-03-01b | Tampering / Elevation of Privilege | `action_classifier` 목록 대조 | medium | mitigate | 목록 대조를 재시도 층 **밖에** 두어, 목록 위반이 재시도로 덮이지 않고 `UnknownMove` 로 즉시 드러나게 한다. Task 2 의 합격 기준이 이때 호출 횟수가 1임을 단언한다 |
| T-03-08 | Repudiation | 실패한 호출이 기록에 안 남음 | high | mitigate | 실패해도 예외가 아니라 응답 껍데기를 돌려준다 — 성공 칸 거짓, 실제로 잰 시간, 토큰 0. 부르는 쪽이 이것을 그대로 `RecordAiCall` 로 기록한다. 실패를 지우면 Phase 6 의 실패율·원가가 실제보다 좋아 보인다(금지 항목으로도 명시했다) |
| T-03-09 | Information Disclosure | 실패 사유 문자열 | high | mitigate | 실패 사유에는 예외의 문자열 표현만 담고 요청 헤더·환경 변수는 담지 않는다 — 자격 증명이 오류 메시지를 타고 새는 경로를 막는다 |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (이 계획 몫)

**새 모듈**
- `src/gptrpg/agents/invoke.py`
- `tests/test_agents_retry.py`, `tests/test_master_gm.py`

**새 함수**
- `call_with_one_retry(fn, *, timeout_s) -> tuple[AgentResult, str | None]`

**새 상수**
- `CLASSIFIER_TIMEOUT_S` (= 5.0), `GM_TIMEOUT_S` (= 15.0), `MAX_ATTEMPTS` (= 2)

**바뀌는 기존 심볼 (03-01 이 만든 것)**
- `action_classifier.classify` — 제공자를 직접 부르지 않고 재시도 층을 거치며, 실패 시 예외 대신 빈 후보를 돌려준다
- `master_gm.narrate` — 15초 타임아웃과 「첫 조각 이후에는 재시도하지 않는다」 규칙이 붙는다
- `master_gm.chunk_sentences` — 경계 여덟 상황을 전부 다루도록 다듬어진다
</artifacts_produced>

<verification>
- `uv run pytest` 전체 통과 (기존 + `tests/test_agents_retry.py` + `tests/test_master_gm.py`)
- `uv run lint-imports` 계약 세 개 전부 통과
- `tests/test_turn_tracer.py` 가 그대로 통과 (성공 경로 회귀 없음)
</verification>

<success_criteria>
응답이 안 와도 게임이 멈추지 않는다 — 5초/15초에 끊고, 딱 한 번 더 시도하고, 그래도 안 되면 이미 있는 「판정 없이 진행」 경로로 조용히가 아니라 **기록을 남기며** 떨어진다. 서사 조각내기는 여덟 개의 경계 상황에서 글자를 흘리지 않는다.
</success_criteria>

<output>
Create `.planning/phases/03-ai/03-03-SUMMARY.md` when done
</output>
