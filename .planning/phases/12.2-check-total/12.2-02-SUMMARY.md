---
phase: 12.2-check-total
plan: 02
subsystem: rules-core
tags: [pydantic, react, vitest, fastapi, dice-resolution]

requires:
  - phase: 12.2-check-total
    provides: "12.2-01의 check_calculation.py/check_views.py/checkSummary.ts 「한 곳」과 CheckResolved.total/rulebook_id(스키마 판 10)"
provides:
  - "ConfirmResponse.rulebook_id · ConfirmResponse.calculation — 서사 실패 분기·정상 분기 두 조립 자리가 같은 커밋에서 같이 채움"
  - "CheckBreakdown.tsx가 summary prop(CheckSummary) 기반으로 그림 — 자체 산수 제거"
  - "DiceModal.tsx가 buildCheckSummary(check, calculation)만 씀 — 착지값·연출 타이밍은 무변경"
  - "usePolling의 onLiveEvents가 (events, calculations) 두 인자를 넘김 — SessionScreen이 seq로 짝지어 큐에 저장"
  - "buildCheckSummaryFromConfirmResponse — 즉시 응답 출처의 얇은 진입점(checkSummary.ts)"
affects: [12.2-03]

actuals:
  tokens: 8791
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "화면 세 자리(TurnCard·CheckBreakdown·DiceModal)가 전부 buildCheckSummary 한 함수를 통해서만 서버 계산 줄을 읽는다 — 네 번째 화면이 생겨도 새 산수를 안 만든다(D-14)"
    - "폴링 파생값(계산 줄)을 연출 큐까지 실어 보낼 때도 새 짝짓기 코드를 안 만든다 — 12.2-01의 indexCalculations를 그대로 재사용(seq 열쇠)"
    - "ConfirmResponse의 두 조립 자리(서사 실패/정상)는 항상 같은 커밋·같은 인자 순서로 고친다 — 위에 이유를 한 줄 주석으로 남긴다"

key-files:
  created: []
  modified:
    - src/gptrpg/web/routes_actions.py
    - tests/test_web_actions.py
    - frontend/src/api/types.ts
    - frontend/src/components/CheckBreakdown.tsx
    - frontend/src/panes/ChatPane.tsx
    - frontend/src/session/checkSummary.ts
    - frontend/src/session/checkSummary.test.ts
    - frontend/src/components/DiceModal.tsx
    - frontend/src/screens/SessionScreen.tsx
    - frontend/src/session/usePolling.ts

key-decisions:
  - "buildCheckSummary의 매개변수 타입을 CheckResolvedEvent 전체에서 Pick<...,\"rolls\"|\"modifiers\"|\"target\"|\"grade\">(CheckFacts)로 좁혔다 — ConfirmResponse는 seq/person_id 등 나머지 봉투 칸이 없어, 새 타입을 만드는 대신 실제로 쓰는 칸만 요구하도록 함수 시그니처를 좁혀 세 화면이 완전히 같은 함수를 공유하게 했다"
  - "DiceModal 괄호 표시(구 「눈 합 ± 보정치」)는 summary.rows를 flatMap해 조각을 순서대로 이어 그리는 것으로 교체했다 — 지금은 줄이 하나뿐이라 flatMap과 줄 구분 렌더링이 같은 결과를 내고, 12.2-03이 다시 굴림 줄 나눔(D-12)을 넣을 때 이 자리를 다시 봐야 한다는 것을 알려진 한계로 남긴다"

requirements-completed: []

coverage:
  - id: D1
    description: "즉시 응답(ConfirmResponse)의 total·rulebook_id·calculation이 저장된 판정 값과 같다 — 2d6은 roll_over, d100은 roll_under(D-07/D-14, 12.2-01이 미룬 칸을 채움)"
    verification:
      - kind: unit
        ref: "tests/test_web_actions.py::test_confirm_response_carries_total_and_calculation"
        status: pass
      - kind: unit
        ref: "tests/test_web_actions.py::test_confirm_response_d100_rulebook_direction_is_roll_under"
        status: pass
    human_judgment: false
  - id: D2
    description: "서사 생성이 실패한 응답에서도 total·rulebook_id·calculation 셋이 정상 분기와 똑같이 실린다 — 두 조립 자리가 갈리지 않는다(D-14)"
    verification:
      - kind: unit
        ref: "tests/test_web_actions.py::test_narration_failed_branch_carries_the_same_calculation"
        status: pass
    human_judgment: false
  - id: D3
    description: "판정이 없는 응답(confirmed: false)에서 total·calculation이 둘 다 null이다 — 「계산 줄이 없다」와 「판정이 없다」가 안 섞인다"
    verification:
      - kind: unit
        ref: "tests/test_web_actions.py::test_confirm_rejected_response_has_null_total_and_calculation"
        status: pass
    human_judgment: false
  - id: D4
    description: "CheckBreakdown이 summary prop 기반으로 서버 계산 줄을 순서대로 그리고, 계산 줄이 null이면 「합계 기록 없음」을 보인다(D-05/D-09) — 자체 산수 세 줄이 사라졌다"
    verification:
      - kind: unit
        ref: "frontend/src/session/checkSummary.test.ts::buildCheckSummaryFromConfirmResponse (2개)"
        status: pass
      - kind: unit
        ref: "cd frontend && npx tsc --noEmit && npm run build"
        status: pass
    human_judgment: false
  - id: D5
    description: "주사위 연출(DiceModal)이 폴링에서 짝지어 온 계산 줄로 buildCheckSummary를 부르고, 이야기 화면·검산 창과 같은 함수·같은 결과를 낸다(D-14) — 착지값(rolls[i])·연출 타이밍은 안 바뀌었다"
    verification:
      - kind: unit
        ref: "frontend/src/session/checkSummary.test.ts::세 화면 자리가 같은 입력에서 같은 요약을 낸다"
        status: pass
      - kind: unit
        ref: "cd frontend && npx tsc --noEmit && npm test -- --run && npm run build"
        status: pass
    human_judgment: false
  - id: D6
    description: "d100 룰북(OpenQuest)으로 브라우저에서 실제 판정을 굴려 검산 창·연출·이야기 화면 세 자리가 십의 자리·일의 자리→37 흐름을 사람이 눈으로 따라갈 수 있는지, 그리고 세 화면이 같은 숫자를 말하는지 확인하는 것(D-15)"
    verification: []
    human_judgment: true
    rationale: "12.2-01 SUMMARY가 이미 같은 사유로 이 확인을 미뤘다 — 「사람이 따라갈 수 있는가」는 자동 시험이 판단할 수 없는 부분이고, 이 계획은 세 화면 전부를 D-15의 확인 대상으로 만들었을 뿐 실제 브라우저 확인 자체는 여전히 사람 몫이다. 12.2-03의 사람 확인 관문에서 함께 확인한다."
---

# Phase 12.2 Plan 2: 검산 창·주사위 연출을 서버 계산 줄로 교체 Summary

**`ConfirmResponse`의 `total`/`rulebook_id`/`calculation` 세 칸을 두 조립 자리(서사 실패/정상)에서 같이 채우고, `CheckBreakdown`·`DiceModal` 두 화면이 자체 산수를 버리고 `checkSummary.ts::buildCheckSummary` 한 곳으로 서버 계산 줄을 그려 화면 세 자리가 d100에서도 같은 숫자(37)를 말한다.**

## Performance

- **Tasks:** 3/3
- **Files modified:** 10 (0 created, 10 modified)
- **Commits:** 3 (task별 1개씩)

## Accomplishments

- `ConfirmResponse`에 `rulebook_id`·`calculation` 두 칸을 더하고, 서사 실패 분기(1101행대)와 정상 분기(1156행대) 양쪽에 `total=check_event.total`·`rulebook_id=check_event.rulebook_id`·`calculation=calculation_view_for(check_event)`를 같은 순서로 넣었다 — 12.2-01이 「후속 계획이 채운다」고 적어 둔 도크스트링이 코드에서 사라졌다(`grep -c '후속 계획이'` → 0)
- 새 시험 5개(`test_confirm_response_carries_total_and_calculation`, `test_confirm_response_d100_rulebook_direction_is_roll_under`, `test_confirm_rejected_response_has_null_total_and_calculation`, `test_narration_failed_branch_carries_the_same_calculation`)로 총 세 갈래(정상/거부/서사실패) + 두 룰북 방향(roll_over/roll_under)을 덮었다
- `CheckBreakdown.tsx`의 props를 `{ rolls, modifiers, target }`에서 `{ summary: CheckSummary }` 하나로 바꾸고 자체 산수 세 줄(`sum + value`, `modifierTotal`)을 지웠다 — `ChatPane.tsx`가 새 얇은 진입점 `buildCheckSummaryFromConfirmResponse`로 응답을 요약으로 바꿔 넘긴다
- `DiceModal.tsx`가 `buildCheckSummary(check, calculation)`만 쓰도록 교체했다 — 굴리는 연출(타이밍·착지값)은 한 글자도 안 바뀌었고, 합계·괄호 안 조각·목표값 방향만 서버 값으로 바뀌었다
- `usePolling`의 `onLiveEvents` 시그니처를 `(events, calculations)`로 넓히고, `SessionScreen`이 `indexCalculations`(12.2-01)로 이 배치의 계산 줄을 seq로 짝지어 `QueuedRoll{ check, calculation }`로 큐에 넣는다 — 새 짝짓기 코드를 안 만들었다
- `checkSummary.test.ts`에 4개 시험을 더해 「응답 출처 = 사건 출처」·「계산 줄 null일 때 원시값 그대로」·「세 화면 호출 자리가 순수 함수 수준에서 같은 결과」를 확정했다

## Task Commits

1. **Task 1: 즉시 응답에 합계·룰북 이름·계산 줄을 싣는다** - `300f971` (feat)
2. **Task 2: 검산 창이 서버가 만든 계산 줄을 그린다** - `5340b69` (feat)
3. **Task 3: 주사위 연출이 같은 계산 줄을 쓴다** - `022d2d9` (feat)

## Files Created/Modified

- `src/gptrpg/web/routes_actions.py` - `ConfirmResponse.rulebook_id`/`.calculation` 추가, 두 조립 자리에서 `check_views.calculation_view_for` 호출
- `tests/test_web_actions.py` - 계산 줄 관련 시험 5개
- `frontend/src/api/types.ts` - `ConfirmResponse.rulebook_id`/`.calculation`, `total` 도크스트링 갱신
- `frontend/src/components/CheckBreakdown.tsx` - `summary` prop 기반 재작성
- `frontend/src/panes/ChatPane.tsx` - `buildCheckSummaryFromConfirmResponse` 배선
- `frontend/src/session/checkSummary.ts` - `CheckFacts` 타입 좁힘, `buildCheckSummaryFromConfirmResponse` 신설
- `frontend/src/session/checkSummary.test.ts` - 응답 출처/세 화면 동일성 시험 4개
- `frontend/src/components/DiceModal.tsx` - `buildCheckSummary` 소비, 자체 산수 제거
- `frontend/src/screens/SessionScreen.tsx` - `QueuedRoll` 도입, `onLiveEvents`가 계산 줄을 받아 seq로 짝지음
- `frontend/src/session/usePolling.ts` - `onLiveEvents` 시그니처에 `calculations` 인자 추가

## Decisions Made

- `buildCheckSummary`의 첫 인자 타입을 `CheckResolvedEvent` 전체에서 `Pick<CheckResolvedEvent, "rolls"|"modifiers"|"target"|"grade">`(`CheckFacts`)로 좁혔다 — `ConfirmResponse`는 `seq`·`person_id` 같은 사건 봉투 칸이 없어, 이 함수가 실제로 읽는 네 칸만 요구하도록 시그니처를 좁혀 세 화면이 새 타입 변환 없이 완전히 같은 함수를 공유하게 했다.
- `DiceModal`의 괄호 표시(구 「눈 합 ± 보정치」)는 `summary.rows`를 `flatMap`해 조각을 순서대로 이어 그리는 것으로 교체했다 — 지금은 계산 줄이 한 줄뿐이라 이 방식과 줄별 렌더링이 같은 결과를 내지만, 12.2-03이 다시 굴림 줄 나눔(D-12)을 넣으면 이 자리를 다시 봐야 한다(알려진 한계, 아래 참조).

## Deviations from Plan

None — plan executed exactly as written. 세 태스크 모두 `<action>` 절이 지목한 자리만 고쳤고, 새 방어 코드나 새 자료구조를 추가하지 않았다.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Limitations (not stubs — explicit scope boundary)

- `DiceModal`의 계산 조각 괄호 표시는 `summary.rows`를 평평하게 이어 그린다 — 다시 굴림(D-12, 줄이 여럿인 경우)이 실제로 웹 경로에 연결되면(아직 시험에서만 불린다, 12.2-CONTEXT 「이 단계 밖」) 이 자리가 줄 구분을 다시 그려야 할 수 있다. 12.2-03이 버려진 눈·다시 굴림 표시를 다룰 때 함께 확인할 것.
- d100 실제 브라우저 확인(D-15 — 십의 자리·일의 자리→37 흐름을 사람이 눈으로 따라갈 수 있는가, 세 화면이 같은 숫자를 말하는가)은 여전히 사람 몫이다 — 12.2-01이 이미 같은 이유로 미뤘고, 이 계획도 자동 시험까지만 닫는다.

## Next Phase Readiness

- 12.2-03(버려진 눈·다시 굴림 표시, D-11/D-12/D-13, D-15 사람 확인 관문)이 이 계획이 완성한 화면 세 자리 위에 바로 얹을 수 있다 — `CalculationSegment.discarded` 칸이 이미 있고, 세 화면 모두 `buildCheckSummary` 한 곳을 거치므로 다시 굴림 줄 나눔을 그 함수 안에서만 고치면 세 화면에 한꺼번에 반영된다.
- 화면 세 자리(`TurnCard`·`CheckBreakdown`·`DiceModal`) 전부에서 자체 산수가 사라졌다(D-14 완성) — 네 번째 화면이 생겨도 새 산수를 만들 필요가 없다.

## Self-Check: PASSED

All modified files verified present on disk with expected content (`routes_actions.py` calculation_view_for call sites: 2, `CheckBreakdown.tsx` summary prop, `DiceModal.tsx` buildCheckSummary call, `usePolling.ts`/`SessionScreen.tsx` indexCalculations usage). All three task commits (`300f971`, `5340b69`, `022d2d9`) verified present in `git log --oneline`.

---
*Phase: 12.2-check-total*
*Completed: 2026-08-19*
