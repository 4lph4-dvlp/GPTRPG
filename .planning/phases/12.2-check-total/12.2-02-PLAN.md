---
phase: 12.2-check-total
plan: 02
type: execute
wave: 2
depends_on: ["12.2-01"]
files_modified:
  - src/gptrpg/web/routes_actions.py
  - frontend/src/api/types.ts
  - frontend/src/components/CheckBreakdown.tsx
  - frontend/src/components/DiceModal.tsx
  - frontend/src/panes/ChatPane.tsx
  - frontend/src/screens/SessionScreen.tsx
  - frontend/src/session/usePolling.ts
  - frontend/src/session/checkSummary.test.ts
  - tests/test_web_actions.py
autonomous: true
requirements: []

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "화면 세 자리가 전부 자체 산수를 그만두고 같은 한 곳(`checkSummary.ts`)이 만든 값을 그린다 — 네 번째 화면이 생겨도 같은 결함이 다시 안 생긴다 (D-14)"
    - "`ConfirmResponse.total`이 「항상 `None`」이 아니다 — 판 10 판정에서 서버가 저장된 합계를 그대로 실어 보내고, 「후속 계획이 채운다」고 적어 둔 주석이 코드에서 사라진다 (D-14, 12-01이 남긴 미완 항목)"
    - "서사 생성이 실패한 응답에도 판정 결과와 계산 줄이 똑같이 실린다 — 두 조립 자리가 갈리지 않는다"
    - "주사위 연출이 폴링 피드에서 받은 계산 줄을 쓰고, 연출이 보여주는 합계와 이야기 화면의 판정 줄이 같은 숫자를 말한다 (D-08/D-14)"
    - "즉시 응답으로 그리는 검산 창(`CheckBreakdown`)에서 d100 눈 `[3, 7]`이 `10`이 아니라 `37`로 보이고, 십의 자리·일의 자리 → 백분위 → 보정치 → 합계 순서가 그대로 보인다 (D-09)"
    - "판정 방향이 화면에 나타난다 — 목표값 옆에 「넘기」인지 「밑돌기」인지가 서버가 보낸 값으로 적힌다 (D-07)"
    - "합계가 안 남은 예전 판정은 세 화면 어디에서도 0이나 빈칸이 아니라 「합계 기록 없음」으로 보인다 (D-05)"
    - statement: "판정이 없는 응답(`confirmed: false`)에서 계산 줄이 `null`이고 화면이 판정 줄 자체를 안 그린다 — 「계산 줄이 없다」와 「판정이 없다」가 섞이지 않는다"
      verification: backstop
    - statement: "화면 세 자리 중 어느 하나만 고쳐 나머지가 다른 숫자를 말하는 상태가 이 계획이 끝난 시점에 존재하지 않는다 — 세 파일 전부에서 자체 산수 세 줄이 사라진다"
      verification: backstop
  artifacts:
    - "src/gptrpg/web/routes_actions.py — `ConfirmResponse.rulebook_id` · `ConfirmResponse.calculation` · 두 조립 자리에서 채워지는 `total`"
    - "frontend/src/components/CheckBreakdown.tsx — `summary` prop 기반으로 교체"
    - "frontend/src/components/DiceModal.tsx — `PendingRoll.calculation` 소비"
    - "frontend/src/api/types.ts — `ConfirmResponse.rulebook_id` · `ConfirmResponse.calculation`"
  key_links:
    - "`CheckResolved.total` ↔ `ConfirmResponse.total` ↔ `CheckBreakdown` — 12-01이 「후속 계획이 채운다」고 적어 둔 그 선"
    - "`routes_actions.py` 서사 실패 분기(1101-1116) ↔ 정상 분기(1156-1170) — 한쪽만 고치면 서사가 실패한 턴만 틀린 검산을 본다"
    - "`PollResponse.check_calculations` ↔ `usePolling` ↔ `onLiveEvents` ↔ `PendingRoll.calculation` ↔ `DiceModal` — 연출이 계산 줄을 받는 유일한 경로"
    - "`buildCheckSummary` ↔ 세 컴포넌트 — 「서버가 준 것을 어떻게 읽는가」를 정하는 단 하나의 자리(D-14)"
  prohibitions:
    - statement: "화면 코드에 룰북별 계산을 다시 두지 않는다 — 세 컴포넌트 중 어디에도 눈을 더하거나 십의 자리를 조합하는 코드가 남지 않는다 (D-08/D-14)"
      status: resolved
      verification: test
    - statement: "`ConfirmResponse`의 두 조립 자리 중 한쪽만 고치지 않는다 — 서사 실패 분기와 정상 분기는 항상 같은 커밋에서 같이 바뀐다"
      status: resolved
      verification: test
    - statement: "합계가 없을 때 0이나 빈 문자열로 때우지 않는다 (D-05)"
      status: resolved
      verification: test
    - statement: "`CheckBreakdown`/`DiceModal`에 룰북 이름으로 분기하는 조건문을 넣지 않는다 — 화면은 역할 이름을 꼬리표와 강조 정도로만 쓴다 (D-10 경계)"
      status: resolved
      verification: judgment
---

<objective>
**12.2-01이 세운 줄기 위에 나머지 두 화면과 즉시 응답 경로를 붙인다.**

12.2-01이 끝나면 이야기 화면(`TurnCard`)만 서버가 만든 계산 줄을 그린다. `CheckBreakdown`은
여전히 `ConfirmResponse`에서 오는 값으로 자체 산수를 하고, `DiceModal`은 폴링 피드를 쓰지만
계산 줄을 받지 않아 역시 자체 산수를 한다. **d100에서 화면 세 자리가 서로 다른 숫자를 말하는
상태**가 남는다 — 이 계획이 그것을 없앤다.

Purpose: D-14의 핵심은 「하나만 고치면 반쪽으로 남는다」이다. 셋이 같은 한 곳을 쓰게 만드는
것이 목적이고, 그 한 곳은 12.2-01이 이미 만들었다. 이 계획은 나머지 둘을 거기에 꽂고,
`routes_actions.py`가 12-01 때부터 「후속 계획이 채운다」고 적어 둔 칸을 실제로 채운다.
Output: `ConfirmResponse`에 합계·룰북 이름·계산 줄 · 검산 창과 주사위 연출 교체.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-check-total/12.2-CONTEXT.md
@.planning/phases/12.2-check-total/12.2-RESEARCH.md
@.planning/phases/12.2-check-total/12.2-PATTERNS.md
@.planning/phases/12.2-check-total/12.2-VALIDATION.md
@.planning/phases/12.2-check-total/12.2-01-SUMMARY.md
</context>

## 조사가 확정한 것 — 다시 조사하지 말 것

- **`ConfirmResponse` 조립 자리는 두 곳이다** — 서사 실패 분기(`routes_actions.py:1101-1116`)와
  정상 분기(`1156-1170`). 인자 목록이 `narration_failed` 하나만 다르고 나머지는 같다.
  서사가 실패해도 판정 결과는 돌려주는 구조라 **한쪽만 고치면 갈린다.**
- **두 자리 다 `check_event`를 쓴다** — `routes_actions.py:845`에서
  `store.read_events(session_id, from_seq=resolve_seq)[0]`으로 **저장소를 다시 읽어** 얻은
  `CheckResolved` 객체다. 12.2-01이 스키마에 두 칸을 넣었으므로 `check_event.total`과
  `check_event.rulebook_id`가 그대로 있다 — 새 계산이 필요 없다.
- **룰북 객체가 이미 스코프에 있다** — `routes_actions.py:757`의
  `rulebook = get_rulebook(body.rulebook_id)`. 다만 계산 줄은 `check_views.calculation_view_for`가
  사건의 `rulebook_id`로 직접 찾으므로 이 변수를 넘길 필요가 없다(지난 판정과 방금 판정이
  **같은 함수**를 쓰게 하는 것이 D-14의 요지다).
- **`ConfirmResponse.total`의 도크스트링(604-624행)이 「이번 계획은 항상 `None`이다 — 후속
  계획이 `CheckResolved`에 칸을 늘리면 채운다」고 적혀 있다.** 이 계획이 그 문장을 지운다.
- **`CheckBreakdown`은 `ConfirmResponse`만 쓴다** — `ChatPane.tsx:36-39`의
  `CheckBreakdownData` 지역 타입, `172행` 부근에서 채우고 `372-376행`에서 그린다.
  `ChatPane.tsx:172` 부근 주석이 「`CheckBreakdown`이 rolls/modifiers로 다시 더한다」는
  지금의 전제를 적고 있다 — 이 계획이 그 주석을 갱신한다.
- **`DiceModal`은 `ConfirmResponse`를 안 쓴다** — `SessionScreen.tsx:108-138`의 `onLiveEvents`가
  폴링으로 새로 도착한 `check_resolved` 원시 사건을 큐에 넣고 그것이 `PendingRoll.check`가 된다
  (`SessionScreen.tsx:264` 부근). 방금 굴린 판정도 이 경로로 온다.
- **`usePolling`의 `onLiveEvents` 시그니처가 지금은 `(events: GameEvent[]) => void`다.**
  계산 줄을 연출까지 보내려면 이 콜백이 계산 줄도 같이 넘겨야 한다 — 12.2-01이 `SessionFeed`에
  `calculations` 맵을 이미 넣었지만, 큐에 넣는 시점에 짝지어야 연출이 사건과 계산 줄을
  같이 들고 다닌다.
- **세 자리의 산수는 문자 그대로 같다** — `CheckBreakdown.tsx:31-33`,
  `TurnCard.tsx:56-58`(12.2-01이 이미 지웠다), `DiceModal.tsx:136-138`.

## Artifacts this phase produces

12.2-01-PLAN.md의 「Artifacts this phase produces」 표가 이 단계 전체의 기호 목록이다.
이 계획이 만드는 것은 그 표에서 「계획」 열이 `12.2-02`인 행이다:

| 종류 | 이름 | 위치 |
|---|---|---|
| 필드 | `ConfirmResponse.rulebook_id` · `ConfirmResponse.calculation` | `web/routes_actions.py` |
| 필드 | `ConfirmResponse.rulebook_id` · `.calculation` | `frontend/src/api/types.ts` |
| prop | `CheckBreakdown.summary` | `frontend/src/components/CheckBreakdown.tsx` |
| 필드 | `PendingRoll.calculation` | `frontend/src/components/DiceModal.tsx` |
| 시험 함수 | `test_confirm_response_carries_total_and_calculation` · `test_narration_failed_branch_carries_the_same_calculation` | `tests/test_web_actions.py` |

이 계획이 소비하는(만들지 않는) 기호는 12.2-01이 만든 `buildCheckSummary`·`indexCalculations`·
`CheckCalculationView`·`calculation_view_for`·`segmentRoleLabel`·`directionLabel`·
`COPY.checkTotalMissing`이다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 즉시 응답에 합계·룰북 이름·계산 줄을 싣는다 — 두 조립 자리를 같은 커밋에 (D-14)</name>
  <files>src/gptrpg/web/routes_actions.py, tests/test_web_actions.py</files>
  <read_first>
    - src/gptrpg/web/routes_actions.py 555-640행 (`ModifierView` 선언 · `ConfirmResponse` 전체 필드와 `total`의 「항상 None」 도크스트링 · `narration_failed`가 세운 「기본값을 갖는 새 칸」 관례)
    - src/gptrpg/web/routes_actions.py 740-770행·840-860행 (`get_rulebook` 호출 자리 · `check_event`를 저장소에서 되읽는 자리)
    - src/gptrpg/web/routes_actions.py 1080-1175행 (두 `ConfirmResponse` 조립 자리 전문 — 인자 순서까지)
    - src/gptrpg/web/check_views.py (12.2-01이 만든 `CheckCalculationView`와 `calculation_view_for`의 두 갈래 — 언제 `None`을 돌려주고 언제 예외를 올리는지)
    - src/gptrpg/event_log/schema.py `CheckResolved` (12.2-01이 넣은 `total`·`rulebook_id` 두 칸)
    - tests/test_web_actions.py (전체 — confirm 엔드포인트 시험이 앱·저장소·가짜 제공자를 세우는 방식과 서사 실패를 만드는 방식)
    - .planning/phases/12.2-check-total/12.2-PATTERNS.md § `routes_actions.py::ConfirmResponse`
  </read_first>
  <behavior>
    - 판 10 판정을 웹으로 확인하면 응답의 `total`이 `null`이 아니라 그 판정의 저장된 합계와 같다
    - 같은 응답의 `rulebook_id`가 그 판정을 굴린 룰북 식별자와 같다
    - 같은 응답의 `calculation`이 조각 목록·합계·목표값·방향을 갖고, 그 `total`이 응답의 `total`과 같다
    - 서사 생성이 실패한 응답(`narration_failed: true`)에서도 `total`·`rulebook_id`·`calculation` 셋이 정상 분기와 **똑같이** 실린다
    - 판정이 일어나지 않은 응답(`confirmed: false`)에서는 `calculation`이 `null`이고 `total`이 `null`이다
    - d100 룰북으로 확인한 판정의 `calculation.direction`이 밑돌아야 성공을 뜻하는 값이고, 2d6 룰북에서는 넘어야 성공을 뜻하는 값이다
  </behavior>
  <action>
①**`ConfirmResponse`에 칸 둘을 더하고 하나의 도크스트링을 폐기한다.**
`rulebook_id: str | None = None`과 `calculation: CheckCalculationView | None = None`을
`total` 옆에 더한다(`check_views`에서 import — 라우터끼리 서로 import하지 않는다).
`total: int | None = None`의 기존 도크스트링(「`CheckResolved`가 이 값을 저장하지 않으므로 …
이번 계획은 항상 `None`이다 — 후속 계획이 `CheckResolved`에 칸을 늘리면 채운다」)을 **지우고**,
새 도크스트링으로 바꾼다: 판 10 이상 판정에서는 사건에 저장된 합계가 그대로 실리고,
판 10 미만 판정에서는 `None`이며 그때 화면은 「합계 기록 없음」을 보인다(D-05). 세 새 칸은
전부 기본값을 가져 다른 응답 조립 자리를 안 건드려도 되게 한다 — `narration_failed`가 세운
관례 그대로다.

②**두 조립 자리를 **함께** 고친다.** 서사 실패 분기(1101-1116행)와 정상 분기(1156-1170행)
양쪽에 `total=check_event.total`, `rulebook_id=check_event.rulebook_id`,
`calculation=calculation_view_for(check_event)` 세 인자를 같은 순서로 더한다. 한쪽만 고치면
서사가 실패한 턴에서만 검산이 틀리는 비대칭 결함이 생긴다 — 두 자리 위에 그 이유를 한 줄
주석으로 남긴다. 계산 줄은 `check_views.calculation_view_for` 한 함수로만 만든다 —
`confirm()` 안에서 룰북 조회나 계산을 새로 하지 않는다. 이렇게 해야 방금 굴린 판정과 지난
판정이 **문자 그대로 같은 함수**를 지난다(D-14).

③**시험을 더한다.** `tests/test_web_actions.py`에
`test_confirm_response_carries_total_and_calculation`을 더해 `<behavior>`의 첫 세 갈래와
방향 두 갈래를 덮고, `test_narration_failed_branch_carries_the_same_calculation`을 더해 서사
실패 분기 응답이 정상 분기와 같은 세 칸을 갖는지 확인한다. 서사 실패는 그 파일이 이미 쓰는
방식으로 만든다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_actions.py -x -q</automated>
    <automated>uv run pytest tests/test_web_events.py tests/test_check_calculation.py -q</automated>
    <automated>uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - 「항상 None」 주석이 사라졌다: `grep -c '후속 계획이' src/gptrpg/web/routes_actions.py`가 0이다 (이 계획 이전 값은 1이다) <!-- planner-discipline-allow: 후속 계획이 -->
    - 두 자리가 같이 바뀐다: `git show HEAD -- src/gptrpg/web/routes_actions.py | grep -c '^+.*calculation=calculation_view_for'`가 2다
    - `grep -c 'total=check_event.total' src/gptrpg/web/routes_actions.py`가 2다
    - `grep -c 'rulebook_id=check_event.rulebook_id' src/gptrpg/web/routes_actions.py`가 2다
    - 계산을 새로 하지 않는다: `grep -c 'build_check_calculation' src/gptrpg/web/routes_actions.py`가 0이다 (빌더는 `check_views`를 통해서만 불린다) <!-- planner-discipline-allow: build_check_calculation -->
    - `uv run pytest tests/test_web_actions.py -k calculation -x`가 0으로 끝나고, 그 안에 서사 실패 분기를 확인하는 케이스가 있다
    - `uv run pytest tests/test_web_actions.py -q`가 0으로 끝난다
    - `uv run ruff check src tests`가 0으로 끝난다
  </acceptance_criteria>
  <done>웹으로 판정을 확인하면 응답에 합계·룰북 이름·계산 줄이 실려 오고, 서사가 실패한 경우에도 똑같이 실린다. 12-01이 남긴 「후속 계획이 채운다」 주석이 코드에서 사라졌다.</done>
  <reversibility rating="reversible">응답에 기본값을 가진 칸을 더하는 것뿐이다 — 기존 소비처가 안 깨진다.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 검산 창이 서버가 만든 계산 줄을 그린다 — 화면 둘째 자리 (D-09/D-14)</name>
  <files>frontend/src/api/types.ts, frontend/src/components/CheckBreakdown.tsx, frontend/src/panes/ChatPane.tsx, frontend/src/session/checkSummary.test.ts</files>
  <read_first>
    - frontend/src/components/CheckBreakdown.tsx (전체 — 머리 도크스트링 네 문단과 `CheckBreakdownProps` 세 칸, 지금의 자체 산수와 그리는 모양)
    - frontend/src/panes/ChatPane.tsx 30-45행·165-180행·365-380행 (`CheckBreakdownData` 지역 타입 · `breakdown` state를 채우는 자리와 그 옆 주석 · `CheckBreakdown` 호출부)
    - frontend/src/session/checkSummary.ts (12.2-01이 만든 `CheckSummary`·`buildCheckSummary`의 정확한 반환 모양)
    - frontend/src/session/checkSummary.test.ts (12.2-01이 만든 시험 — 여기에 이어 붙인다)
    - frontend/src/api/types.ts 255-295행 (`ConfirmResponse`와 `total` 주석)
    - frontend/src/labels.ts (12.2-01이 만든 `segmentRoleLabel`·`directionLabel`·`COPY.checkTotalMissing`)
    - frontend/src/components/TurnCard.tsx (12.2-01이 고친 뒤의 `CheckLine` — 조각을 그리는 방식을 여기에 맞춘다)
  </read_first>
  <behavior>
    - 계산 줄이 실린 응답에서 검산 창이 조각을 서버가 보낸 순서 그대로 그리고, d100 판정에서 `37`이 보이고 `10`은 안 보인다
    - 계산 줄이 `null`인 응답(판 10 미만 판정)에서 검산 창이 합계 자리에 「합계 기록 없음」을 보이고, 눈·보정치·목표값은 그대로 보인다
    - 목표값 옆에 방향(넘기/밑돌기)이 보이고, 방향을 모르면(`null`) 그 자리에 아무것도 안 보인다
    - `buildCheckSummary`가 `ConfirmResponse` 출처와 폴링 사건 출처 양쪽에서 같은 모양의 값을 돌려준다 — 두 출처를 구분하는 분기가 그 함수 안에 없다
  </behavior>
  <action>
①**`frontend/src/api/types.ts` — `ConfirmResponse`를 파이썬 쪽과 맞춘다.**
`rulebook_id?: string | null`과 `calculation?: CheckCalculationView | null`을 더하고,
`total` 위의 기존 주석(「`CheckResolved`가 이 값을 저장하지 않아 서버는 항상 `null`을 보낸다 …
화면은 이 칸을 신뢰하지 않고 `rolls`/`modifiers`에서 직접 다시 더한다」)을 **지우고** 새
주석으로 바꾼다: 판 10 이상 판정에서는 서버가 저장된 합계를 보내고 화면은 그것을 그대로 쓴다,
판 10 미만 판정에서는 `null`이고 그때 「합계 기록 없음」이 보인다.

②**`CheckBreakdown.tsx`를 계산 줄 소비자로 교체한다.** props를 `{ rolls, modifiers, target }`
세 칸에서 `{ summary: CheckSummary }` 하나로 바꾼다 — 「서버가 준 것을 어떻게 읽는가」의
결정은 `checkSummary.ts` 한 곳에만 있고 이 컴포넌트는 그리기만 한다(D-14). 자체 산수 세 줄을
지운다. 그리는 순서는 `summary.rows`의 줄 → 각 줄의 조각을 **서버가 보낸 순서 그대로**이고,
각 조각은 `segmentRoleLabel(segment.role)` 꼬리표와 값을 함께 보인다. 보정치 조각은 `source`가
있으면 `modifierSourceLabel(segment.source)`을 함께 보인다(지금 이 컴포넌트가 하던 것과 같은
표시). 합계는 `summary.total`을 쓰되 `summary.totalMissing`이면 그 자리에
`COPY.checkTotalMissing`을 넣는다 — 0으로 때우지 않는다(D-05). 목표값 옆에
`directionLabel(summary.direction)`을 덧붙인다(D-07). 머리 도크스트링 네 문단 중 「합계는
`ConfirmResponse.total`(서버 값)을 안 쓴다 …」 문단과 「목표값을 넘어야 성공인지 밑돌아야
성공인지는 … 이 컴포넌트는 그 비교를 말하지 않고 숫자만 보인다」 문단을 **둘 다** 새 사실로
교체한다: 이제 서버가 계산 줄과 방향을 보내고 이 컴포넌트는 그것을 그대로 그린다.
룰북 이름으로 분기하는 조건문을 넣지 않는다 — 역할 이름은 꼬리표와 강조 정도로만 쓴다(D-10 경계).

③**`ChatPane.tsx` 배선.** `CheckBreakdownData` 지역 타입을 `ConfirmResponse`에서 온
`calculation`까지 담도록 넓히고, `CheckBreakdown` 호출부에서
`buildCheckSummary(...)`가 만든 `summary` 하나만 넘긴다. `buildCheckSummary`는
`CheckResolvedEvent` 모양을 받으므로, 응답에서 온 값(`rolls`·`modifiers`·`target`·`grade`·
`total`·`rulebook_id`)을 그 모양으로 맞춰 넘긴다 — 필요하면 12.2-01의 `checkSummary.ts`에
「응답에서 온 값으로 요약을 만드는」 얇은 진입점을 하나 더한다(계산은 여전히 없다).
`172행` 부근의 「`CheckBreakdown`이 rolls/modifiers로 다시 더한다」는 주석을 새 사실로 바꾼다.

④**시험.** `checkSummary.test.ts`에 이번 갈래 둘을 더한다 — 응답 출처로 만든 요약과 폴링
사건 출처로 만든 요약이 같은 입력값에서 같은 결과를 내는 것, 그리고 계산 줄이 `null`일 때
`totalMissing`이 참이고 원시 눈·보정치·목표값이 그대로 실리는 것.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 자체 산수가 사라졌다: `grep -c 'sum + value' frontend/src/components/CheckBreakdown.tsx`가 0이고 `grep -c 'modifierTotal' frontend/src/components/CheckBreakdown.tsx`가 0이다 <!-- planner-discipline-allow: modifierTotal -->
    - 「항상 null」 전제가 사라졌다: `grep -c '다시 더한다' frontend/src/api/types.ts frontend/src/components/CheckBreakdown.tsx frontend/src/panes/ChatPane.tsx`의 세 줄이 전부 0이다 (이 계획 이전 값은 각각 1·0·1이다) <!-- planner-discipline-allow: 다시 더한다 -->
    - 한 곳을 쓴다: `grep -c 'buildCheckSummary' frontend/src/panes/ChatPane.tsx`가 1 이상이다
    - 문구가 컴포넌트에 안 박힌다: `grep -c '합계 기록 없음' frontend/src/components/CheckBreakdown.tsx`가 0이다 <!-- planner-discipline-allow: 합계 기록 없음 -->
    - 룰북 분기가 없다: `grep -c 'openquest\|dungeonworld\|cairn' frontend/src/components/CheckBreakdown.tsx`가 0이다
    - `cd frontend && npx tsc --noEmit`이 0으로 끝난다
    - `cd frontend && npm test -- --run`이 0으로 끝나고, `checkSummary.test.ts`에 계산 줄이 없는 경우의 케이스가 있다
    - `cd frontend && npm run build`가 0으로 끝난다
  </acceptance_criteria>
  <done>검산 창이 서버가 만든 계산 줄을 순서 그대로 그리고, d100 판정에서 십의 자리·일의 자리 → 백분위 → 보정치 → 합계가 보인다. 계산 줄이 없는 옛 판정에서는 「합계 기록 없음」이 보인다.</done>
  <reversibility rating="costly">`CheckBreakdown`의 props 형태가 바뀌므로 호출부가 같이 움직인다 — 호출부는 `ChatPane.tsx` 한 곳뿐이다.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 주사위 연출이 같은 계산 줄을 쓴다 — 화면 셋째 자리 (D-14)</name>
  <files>frontend/src/components/DiceModal.tsx, frontend/src/screens/SessionScreen.tsx, frontend/src/session/usePolling.ts, frontend/src/session/checkSummary.test.ts</files>
  <read_first>
    - frontend/src/components/DiceModal.tsx (전체 — 머리 도크스트링의 「착지값은 반드시 `rolls[i]`와 같다」 규약 · `PendingRoll` 인터페이스 · 연출 타이밍 상수들 · 자체 산수와 합계를 보이는 자리)
    - frontend/src/screens/SessionScreen.tsx 95-145행·255-275행 (`onLiveEvents`가 큐를 채우는 방식 · `DiceModal` 호출부와 `PendingRoll` 조립)
    - frontend/src/session/usePolling.ts (12.2-01이 `calculations`를 넣은 뒤의 모양 · `onLiveEvents` 콜백을 부르는 자리)
    - frontend/src/session/checkSummary.ts (`buildCheckSummary` 반환 모양)
    - frontend/src/components/TurnCard.tsx · frontend/src/components/CheckBreakdown.tsx (12.2-01·Task 2가 고친 뒤의 그리는 방식 — 세 자리가 같은 값을 같은 방식으로 읽는지 맞춘다)
  </read_first>
  <behavior>
    - 판정 사건이 새로 도착하면 연출 큐에 사건과 **그 사건의 계산 줄이 짝지어** 들어간다 — 순번으로 짝을 찾고, 계산 줄이 없으면 `null`로 들어간다
    - 연출이 멈춘 뒤 보이는 합계가 이야기 화면의 판정 줄이 보이는 합계와 같다 — d100에서 둘 다 `37`(+보정치) 계열이고 `10`이 아니다
    - 계산 줄이 없는 판정(판 10 미만)이 연출로 오면 합계 자리에 「합계 기록 없음」이 보이고 연출 자체는 정상적으로 돈다
    - 구르는 동안의 착지값은 여전히 `check.rolls[i]`와 같다 — 계산 줄이 연출의 눈을 바꾸지 않는다
  </behavior>
  <action>
①**계산 줄을 연출 큐까지 실어 보낸다.** `usePolling`의 `onLiveEvents` 콜백 시그니처를
`(events: GameEvent[], calculations: CheckCalculationView[]) => void`로 넓히고, 폴링 응답에서
온 그 배치의 계산 줄을 그대로 함께 넘긴다. `SessionScreen`의 `onLiveEvents`가
`check_resolved` 사건을 큐에 넣을 때 **순번으로 짝지은 계산 줄**을 함께 넣도록
`PendingRoll`에 `calculation: CheckCalculationView | null`을 더한다. 짝을 찾을 때는
12.2-01의 `indexCalculations`를 쓴다 — 새 조회 코드를 만들지 않는다.

②**`DiceModal.tsx`를 교체한다.** 자체 산수 세 줄을 지우고
`buildCheckSummary(check, calculation)`이 돌려준 값만 쓴다. 연출이 멈춘 뒤(`showSum`) 보이는
자리에 `summary.total`을 넣되 `summary.totalMissing`이면 `COPY.checkTotalMissing`을 넣고,
괄호 안의 「눈 합 ± 보정치」 표시를 서버가 보낸 조각 목록을 순서대로 그리는 것으로 바꾼다 —
`TurnCard`·`CheckBreakdown`과 같은 방식(`segmentRoleLabel` 꼬리표)이다. 목표값 옆에
`directionLabel(summary.direction)`을 덧붙인다. **연출 자체(굴리는 눈·타이밍·착지값)는 안
건드린다** — 머리 도크스트링의 「착지값은 반드시 `rolls[i]`와 같다」 규약이 그대로 유지되고,
`check.rolls`가 여전히 굴릴 주사위 개수와 착지값의 출처다. 도크스트링에 한 문단을 더한다:
합계는 이제 화면이 더한 값이 아니라 서버가 사건에 남긴 값이고, 연출과 이야기 화면이 같은
숫자를 말한다.

③**시험.** `checkSummary.test.ts`에 「같은 사건과 같은 계산 줄로 만든 요약이 세 화면 어디서
불러도 같다」를 확인하는 케이스를 더한다 — 세 컴포넌트를 렌더하지 않고 순수 함수 수준에서
같은 입력이 같은 출력을 내는 것으로 충분하다.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run</automated>
    <automated>cd frontend &amp;&amp; npm run build</automated>
    <automated>.venv/bin/python -m pytest -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - 화면 세 자리 어디에도 자체 산수가 없다: `grep -c 'sum + value' frontend/src/components/DiceModal.tsx frontend/src/components/TurnCard.tsx frontend/src/components/CheckBreakdown.tsx frontend/src/session/checkSummary.ts`의 네 줄이 전부 0이다
    - 세 자리가 같은 함수를 쓴다: `grep -l 'buildCheckSummary' frontend/src/components/DiceModal.tsx frontend/src/components/TurnCard.tsx frontend/src/panes/ChatPane.tsx`가 세 파일을 전부 돌려준다
    - 연출 규약이 유지된다: `grep -c 'check.rolls' frontend/src/components/DiceModal.tsx`가 1 이상이다 (착지값의 출처가 여전히 사건의 눈이다)
    - 짝짓기를 새로 만들지 않는다: `grep -c 'indexCalculations' frontend/src/screens/SessionScreen.tsx frontend/src/session/usePolling.ts`의 합이 1 이상이다
    - `cd frontend && npx tsc --noEmit`이 0으로 끝난다
    - `cd frontend && npm test -- --run`이 0으로 끝난다
    - `cd frontend && npm run build`가 0으로 끝난다
    - `.venv/bin/python -m pytest -q --tb=short`가 0으로 끝난다
  </acceptance_criteria>
  <done>주사위 연출이 멈춘 뒤 보이는 합계가 서버가 사건에 남긴 값이고, 이야기 화면·검산 창과 같은 숫자를 말한다. 화면 세 자리 전부에서 자체 산수가 사라졌다.</done>
  <reversibility rating="costly">`onLiveEvents` 콜백 시그니처와 `PendingRoll` 모양이 바뀌므로 `usePolling`·`SessionScreen`·`DiceModal`이 같이 움직인다.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 룰북 데이터 → 계산 조각의 역할 이름·출처 → 브라우저 DOM | 12.2-01이 연 것과 같은 경계. 이 계획은 소비처를 둘 더한다(`CheckBreakdown`·`DiceModal`) |
| 브라우저 → `POST /sessions/{sid}/actions/confirm` | 기존 경계 — 이 계획은 요청 형식을 안 바꾸고 응답에만 칸을 더한다 |

이 계획도 **새 신뢰 경계를 만들지 않는다** — AI 개입 없음, 인증·세션 계약 무변경, 요청
형식 무변경. RESEARCH.md §Security Domain의 판정을 그대로 쓴다(ASVS L1).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.2-01 | Tampering | 계산 조각의 `role`/`source` 자유 문자열 → `CheckBreakdown.tsx`·`DiceModal.tsx` | low | mitigate | 새 방어를 만들지 않는다. `TurnCard.tsx` 도크스트링이 세운 기존 규약(모든 글자는 JSX 자식으로만, `dangerouslySetInnerHTML`은 이 저장소 어디에도 없음)이 이미 막는다 — Task 2·3이 두 새 소비처에서도 그 규약이 지켜지는지 확인한다 |
| T-12.2-05 | Tampering | 두 조립 자리 중 한쪽만 고쳐 서사 실패 턴만 다른 값을 봄 | medium | mitigate | Task 1이 두 자리를 같은 커밋에서 고치고, 서사 실패 분기를 따로 확인하는 시험을 둔다. 인수 기준이 diff에서 두 자리를 센다 |
| T-12.2-06 | Denial of Service | 계산 줄이 `null`인 옛 판정에서 화면이 터짐 | medium | mitigate | `buildCheckSummary`가 `null`을 정상값으로 다루고 `totalMissing`으로 표현한다(12.2-01). Task 2·3이 그 갈래를 각각 시험으로 덮는다 |
| T-12.2-SC | Tampering | 패키지 설치 | low | accept | 이 계획은 `npm`/`pip`/`cargo` 설치 태스크가 없다 — 12.2-RESEARCH.md § Package Legitimacy Audit가 「해당 없음」으로 확인했다 |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q --tb=short` 전체 통과
- `cd frontend && npx tsc --noEmit && npm test -- --run && npm run build` 전체 통과
- `uv run ruff check src tests` 통과
- `git show HEAD -- src/gptrpg/web/routes_actions.py`의 diff에 `ConfirmResponse` 조립 자리 **두 곳**이 다 들어 있는지 확인
- 화면 세 자리(`CheckBreakdown`·`TurnCard`·`DiceModal`) 전부에서 자체 산수가 사라졌는지 grep으로 확인
</verification>

<success_criteria>
- 화면 세 자리가 전부 같은 한 곳을 통해 서버의 계산 줄을 그린다 (D-14)
- `ConfirmResponse.total`이 실제 값을 싣고, 12-01이 남긴 미완 주석이 사라졌다
- 서사 실패 응답과 정상 응답이 같은 검산 정보를 싣는다
- 계산 줄이 없는 옛 판정이 세 화면 어디에서도 「합계 기록 없음」으로 정직하게 보인다 (D-05)
- 목표값 옆에 판정 방향이 서버가 보낸 값으로 적힌다 (D-07)
</success_criteria>

<output>
Create `.planning/phases/12.2-check-total/12.2-02-SUMMARY.md` when done
</output>
