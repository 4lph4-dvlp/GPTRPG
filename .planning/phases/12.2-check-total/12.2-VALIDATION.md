---
phase: 12.2
slug: check-total
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-19
---

# Phase 12.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `12.2-RESEARCH.md` §Validation Architecture. Per-task rows are filled
> once PLAN.md task IDs exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (backend)** | pytest 9.1.1 — `pyproject.toml` dev group, `[tool.pytest.ini_options] testpaths = ["tests"]`, `asyncio_mode = "auto"` |
| **Framework (frontend)** | vitest ^4.1.10 — `frontend/package.json` |
| **Config file** | `pyproject.toml` (backend) · none for vitest — defaults + `*.test.ts` filename convention |
| **Quick run command** | `uv run pytest tests/test_event_log.py tests/test_event_schema_migration.py -q` (backend) · `cd frontend && npm test -- --run` (frontend) |
| **Full suite command** | `.venv/bin/python -m pytest -q --tb=short` · `cd frontend && npx tsc --noEmit && npm test && npm run build` |
| **Phase gate extras** | `uv run lint-imports` · `uv run ruff check src tests` |
| **Estimated runtime** | quick ~5s · full backend ~60s · full frontend ~90s |

---

## Sampling Rate

- **After every task commit:** Run the narrowed pytest/vitest command for the files that task touched
- **After every plan wave:** Run `.venv/bin/python -m pytest -q --tb=short` + `cd frontend && npx tsc --noEmit && npm test`
- **Before `/gsd-verify-work`:** Full suite green + `npm run build` + `lint-imports` + `ruff check`, plus the D-15 human check (one OpenQuest roll in the browser)
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

*Seeded from the research decision→test map. Task IDs are bound during `/gsd-execute-phase`
once PLAN.md exists; the Requirement column carries CONTEXT.md decision IDs because this
phase has no REQ-ID mapping (`Requirements: TBD` in ROADMAP).*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-01 / D-02 — `CheckResolved` requires `total`/`rulebook_id` from schema 10 | — | N/A | unit | `uv run pytest tests/test_event_log.py -k check_resolved -q` | ✅ (extend `UNIQUE_FIELDS`) | ⬜ pending |
| TBD | TBD | TBD | D-01 / D-02 — pre-10 records still readable without the new fields | — | N/A | unit | `uv run pytest tests/test_event_schema_migration.py -q` | ✅ template exists | ⬜ pending |
| TBD | TBD | TBD | D-02 — version pinned at `EVENT_SCHEMA_VERSION == 10` | — | N/A | unit | `uv run pytest tests/test_event_schema_migration.py -k version -q` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-04 — replay comparison: stored `total` == recomputed `total` | — | N/A | integration | `uv run pytest tests/test_dice_replay.py -q` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-08 / D-09 / D-10 — segment builder across 2d6 · d100 · bonus dice · reroll | T-12.2-01 | Role names stay data; renderer never interprets them | unit | `uv run pytest tests/test_resolution.py tests/test_resolution_d100.py -q` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-14 — three screens read the server's segments through one shared place | T-12.2-01 | Segment strings render as JSX children only | unit (frontend) | `cd frontend && npm test -- --run` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | RESEARCH §3 — `reducer.py` unchanged, fold still correct | — | N/A | unit | `uv run pytest tests/test_event_log.py -k fold -q` | ✅ existing coverage | ⬜ pending |
| TBD | TBD | TBD | RESEARCH §6 — past checks carry segments through the polling feed | — | N/A | integration | `uv run pytest tests/ -k poll_events -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_event_schema_migration.py` — add `test_event_schema_version_was_nine_before_phase_12_2` (`>= 9`) and `test_event_schema_version_is_ten` (`== 10`)
- [ ] `tests/test_event_log.py::UNIQUE_FIELDS["check_resolved"]` — add the `total` and `rulebook_id` keys (7 parametrized tests then cover the new required fields automatically, satisfying 11-CONTEXT D-03: re-pin the frozen field list, never delete the test)
- [ ] New unit tests for the pure segment-builder function — at minimum 6 combinations: 2d6 without/with reroll, d100 without bonus / with bonus / with penalty / with push
- [ ] D-04 replay-comparison test — new (extend `tests/test_dice_replay.py`, pattern at `tests/test_dice_replay.py:27-46`). **Must synthesize its own schema-10 events**: `tests/fixtures/session1_events.jsonl` is schema 2 and carries no `total`, and must not be overwritten
- [ ] New frontend unit test for the shared "one place" function, colocated per the `groupTurns.test.ts` convention
- [ ] No framework install needed — pytest and vitest are both already installed and configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The d100 check line lets a person follow rolls `[3, 7]` → `37` → `+2` → `39, target 55` | D-15 | "Can a human follow the arithmetic?" is a legibility judgement no assertion can make | Load an OpenQuest (d100 roll-under) session in the browser, make one check, read the breakdown line and confirm the tens/ones path is visible |
| Reroll display (first roll / reroll split, total only on the last line) | D-12 / D-13 | `reroll_2d6` / `push_d100` are not wired to the web or actor path yet — unreachable from the browser this phase | Cannot be exercised manually this phase. Covered by automated segment-builder unit tests only; note this gap explicitly at verification time rather than claiming browser confirmation |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags (vitest must run with `--run`)
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
