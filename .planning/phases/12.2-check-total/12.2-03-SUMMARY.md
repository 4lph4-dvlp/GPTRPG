---
phase: 12.2-check-total
plan: 03
subsystem: rules-core
tags: [pydantic, react, vitest, fastapi, dice-resolution]

requires:
  - phase: 12.2-check-total
    provides: "12.2-01/02가 세운 CalculationSegment.discarded 칸·CalculationRow 여러 개·buildCheckSummary 「한 곳」(D-14) — 이 계획은 그 자료 구조에 값만 채운다"
provides:
  - "check_calculation.py — d100 보너스/페널티 십의 자리 채택 판정(자리 우선, 값 아님)과 2d6/d100 다시 굴림의 「처음 굴림/다시 굴림」 줄 나눔, 합계는 마지막 줄에만"
  - "checkSummary.ts — rowLabel(줄 꼬리표)·rollDiscarded[]·rollRoles[] 세 화면이 공유하는 한 곳"
  - "CheckBreakdown/TurnCard/DiceModal — 버려진 눈 흐림 표시 + 글자 꼬리표(색만으로 안 전함), 다시 굴림 줄 구분"
  - "DiceModal — 주사위 모양이 서버 role(die/tens/units)을 따름, 착지 후에만 흐림 적용, 명시적 확인 버튼으로 결과를 읽을 시간 확보"
  - "D-15 사람 확인 — OpenQuest(d100)로 브라우저에서 굴려 세 화면이 같은 합계를 보이고 눈→합계 경로를 따라갈 수 있음을 확인, 승인됨"
affects: []

actuals:
  tokens: 8000
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "채택 판정은 값이 아니라 자리(순서)로 정한다 — 동값 두 개일 때 값 비교로 채택을 고르면 채택이 둘 이상 될 수 있어, 항상 가장 앞 자리를 채택으로 본다"
    - "줄 나눔은 겹침 횟수에 무관한 구조적 규칙(2d6은 2개씩, d100 첫 줄은 1+|dice_delta|+1개, 이후 줄은 2개씩)으로 쓴다 — 사건에 재굴림 횟수 칸이 없어도 배열 길이만으로 나뉜다"
    - "화면 표시 판단(줄 꼬리표·흐림 여부)은 세 컴포넌트가 각자 하지 않고 checkSummary.ts 한 곳의 순수 함수가 만든다(D-14 규율을 새 표시에도 적용)"

key-files:
  created: []
  modified:
    - src/gptrpg/rules_core/check_calculation.py
    - tests/test_check_calculation.py
    - frontend/src/labels.ts
    - frontend/src/session/checkSummary.ts
    - frontend/src/session/checkSummary.test.ts
    - frontend/src/components/CheckBreakdown.tsx
    - frontend/src/components/TurnCard.tsx
    - frontend/src/components/DiceModal.tsx
    - src/gptrpg/web/routes_actions.py
    - tests/fixtures/characters.py
    - tests/test_web_actions.py

key-decisions:
  - "동값 두 개가 채택 후보일 때 앞 자리를 채택으로 정했다(값이 아니라 자리) — 값으로 고르면 채택 조각이 정확히 하나라는 불변식이 깨질 수 있다"
  - "다시 굴림 줄 나눔은 지금 겹침이 최대 1회라는 실사용 가정(RESEARCH A2) 위에서도 겹침 횟수에 무관한 일반 규칙으로 짰다 — 다시 굴림이 실제 플레이 경로에 붙을 미래 계획이 이 규칙을 다시 안 짜도 된다"
  - "주사위 연출은 착지(showSum)가 끝난 뒤에만 흐림을 입힌다 — 굴리는 동안 흐려지면 「버려진 눈도 실제로 굴렸다」는 D-13의 약속이 흐려진다"

requirements-completed: []

coverage:
  - id: D1
    description: "보너스/페널티 d100에서 서버가 resolve_d100과 같은 규칙(부호>0 최솟값/부호<0 최댓값/동값은 앞 자리)으로 채택 십의 자리를 정하고 나머지를 discarded=True로 표시한다 (D-11)"
    verification:
      - kind: unit
        ref: "tests/test_check_calculation.py -k penalty"
        status: pass
      - kind: unit
        ref: "tests/test_check_calculation.py -k discard"
        status: pass
    human_judgment: false
  - id: D2
    description: "2d6 다시 굴림과 d100 push가 「처음 굴림/다시 굴림」 두 줄로 나뉘고, 보정치·합계는 마지막 줄에만 붙는다 (D-12)"
    verification:
      - kind: unit
        ref: "tests/test_check_calculation.py -k reroll"
        status: pass
      - kind: unit
        ref: "tests/test_check_calculation.py -k push"
        status: pass
    human_judgment: false
  - id: D3
    description: "세 화면(CheckBreakdown/TurnCard/DiceModal)이 버려진 눈을 흐리게, 글자 꼬리표와 함께 같은 방식으로 그린다 — 문구는 labels.ts에서만 나온다 (D-11)"
    verification:
      - kind: unit
        ref: "frontend/src/session/checkSummary.test.ts"
        status: pass
      - kind: other
        ref: "cd frontend && npx tsc --noEmit && npm test -- --run && npm run build"
        status: pass
    human_judgment: false
  - id: D4
    description: "주사위 연출이 버려진 눈까지 전부 굴리고, 착지 뒤 안 골린 것이 서버 role에 맞는 모양으로 흐려진다 (D-13)"
    verification:
      - kind: unit
        ref: "frontend/src/session/checkSummary.test.ts (rowLabel/rollDiscarded/rollRoles 케이스)"
        status: pass
    human_judgment: false
  - id: D5
    description: "사장님이 OpenQuest(d100)로 브라우저에서 실제로 한 번 굴려 세 화면의 합계가 같고 눈→합계 경로를 따라갈 수 있음을 확인한다 (D-15)"
    verification: []
    human_judgment: true
    rationale: "「사람이 화면을 보고 따라갈 수 있는가」는 자동 시험이 판단할 수 없다 — 사장님이 실제 브라우저에서 [8,6]→86(목표 55, 밑돌아야 성공)을 확인하고 승인함(2026-08-20)"

duration: ~4h (사람 확인 대기·발견된 결함 3건 수정 포함)
completed: 2026-08-20
status: complete
---

# Phase 12.2 Plan 3: 버려진 눈·다시 굴림 표시 + 사람 확인 관문 Summary

**서버가 d100 보너스/페널티에서 채택된 십의 자리를 정확히 표시하고 다시 굴림을 「처음 굴림/다시 굴림」 두 줄로 나누며, 화면 세 자리가 같은 표시를 그린다 — OpenQuest 브라우저 확인(D-15)에서 [8,6]→86 검산 경로를 사람이 직접 확인하고 승인했다.**

## Performance

- **Tasks:** 3/3 (Task 1·2는 자동 실행, Task 3은 사람 확인 관문 — 승인됨)
- **Files modified:** 8 (이 계획 자신의 두 태스크 기준, 0 created, 8 modified). 관문 통과 과정에서 발견되고 오케스트레이터가 이 계획 밖에서 고친 결함까지 포함하면 11개 (아래 Deviations 참조)
- **Commits:** 2 (task별 1개씩) + 관문 통과 과정에서 발견된 결함 수정 3개(이 계획 범위 밖, 별도 커밋)

## Accomplishments

- `check_calculation.py`의 d100 조각 생성기가 십의 자리 채택을 **자리**로 정한다 — 같은 값이 여럿이면 앞 자리가 채택, `resolve_d100`의 채택 규칙(`dice_delta` 부호: `>0`→최솟값, `<0`→최댓값, `0`→유일값)을 문자 그대로 재현. 페널티에서 첫 값을 채택으로 가정하는 실수가 시험으로 즉시 잡히게 못박았다
- 같은 함수가 2d6 `reroll_2d6`과 d100 `push_d100` 양쪽의 줄 나눔을 구조적 규칙(겹침 횟수 무관)으로 만든다 — 앞 줄은 눈 조각만, `total`이 `None`, 마지막 줄에만 보정치·합계가 붙는다
- `tests/test_check_calculation.py`에 보너스/페널티/동값/2d6 다시 굴림/d100 push 다섯 갈래 시험을 더해, 지금 브라우저에서 도달할 수 없는 이 표시들의 유일한 검증 수단으로 못박았다
- `checkSummary.ts`가 `rowLabel`(처음 굴림/다시 굴림)·`rollDiscarded[]`를 만드는 순수 함수 하나를 세 화면에 공유시켜(D-14 규율 유지), `CheckBreakdown`·`TurnCard`·`DiceModal`이 같은 클래스·같은 순서로 흐림+글자 꼬리표를 그린다
- `DiceModal`은 굴리는 주사위 개수를 그대로 `check.rolls.length`로 유지(버려진 눈까지 전부 굴림)하면서, 착지(`showSum`) 이후에만 버려진 자리에 흐림을 입혀 연출과 검산 줄이 같은 서버 값에서 나온 같은 이야기를 하게 했다
- **D-15 사람 확인 통과:** 사장님이 OpenQuest로 실제 브라우저에서 굴려 눈 `[8, 6]`이 검산 창·연출·이야기 화면 세 자리 모두 합계 86(목표 55, 밑돌아야 성공)으로 일치함을 확인·승인했다

## Task Commits

1. **Task 1: 서버가 버려진 눈을 표시하고 굴림 줄을 나눈다 (D-11/D-12)** - `b66ce7d` (feat)
2. **Task 2: 화면이 버려진 눈을 흐리게, 다시 굴림을 줄로 나눠 보인다 (D-11/D-12/D-13)** - `57d7435` (feat)
3. **Task 3: OpenQuest(d100)로 브라우저에서 한 번 굴려 검산 줄을 눈으로 확인한다 (D-15)** - 코드 변경 없음, 사람 확인 관문 통과(아래 참조)

## Files Created/Modified

- `src/gptrpg/rules_core/check_calculation.py` - 십의 자리 채택(자리 우선) 표시, 2d6/d100 줄 나눔(D-11/D-12)
- `tests/test_check_calculation.py` - 보너스·페널티·동값·2d6 재굴림·d100 push 5갈래 시험
- `frontend/src/labels.ts` - `checkRollFirst`/`checkRollAgain`/`checkDiscarded` 문구
- `frontend/src/session/checkSummary.ts` - 줄 꼬리표·버려짐 표시를 만드는 공유 함수
- `frontend/src/session/checkSummary.test.ts` - 줄 하나/줄 둘 갈래 + 버려짐 표시 통과 시험
- `frontend/src/components/CheckBreakdown.tsx` - 검산 창에 흐림+꼬리표+줄 구분 표시
- `frontend/src/components/TurnCard.tsx` - 이야기 화면 판정 줄에 같은 표시
- `frontend/src/components/DiceModal.tsx` - 연출 착지 후 흐림 적용, `rollRoles` 기반 주사위 모양

## Decisions Made

- 동값 두 개가 채택 후보일 때 값이 아니라 **자리**(가장 앞)로 채택을 정했다 — 값으로 고르면 채택 조각이 정확히 하나라는 불변식이 깨질 수 있다.
- 줄 나눔 규칙은 지금 다시 굴림이 최대 1회라는 실사용 가정(RESEARCH A2) 위에서도 겹침 횟수에 무관한 일반 규칙으로 짰다 — 다시 굴림이 실제 플레이 경로에 연결될 미래 계획이 이 규칙을 다시 짤 필요가 없다.
- `DiceModal`은 착지(`showSum`)가 끝난 뒤에만 흐림을 입힌다 — 굴리는 도중에 흐려지면 「버려진 눈도 실제로 굴렸다」는 D-13의 약속이 훼손된다.

## Deviations from Plan

### D-15 관문 통과 과정에서 발견되고, 이 계획 밖에서 수정된 결함 3건

**이 계획(12.2-03) 자신의 Task 1·2는 계획 그대로 실행됐다.** 아래 세 건은 Task 3(사람 확인 관문)에서 사장님이 실제 브라우저로 굴리다 발견한 결함이고, 오케스트레이터가 이 계획의 `<files_modified>` 목록 밖에서 별도 커밋으로 고쳤다 — 이 계획이 만든 결함이 아니라 이 계획이 처음으로 D-15를 브라우저까지 밀어붙여서 드러난, 더 오래된 결함들이다.

**1. [범위 밖, 사전 존재 결함] 액션 라우트가 룰북을 요청 기본값에서 가져오던 문제**
- **발견:** Task 3 — D-15 확인을 완전히 막고 있었다
- **문제:** 브라우저가 `rulebook_id`를 요청에 안 실었고, `DeclareRequest`/`ConfirmRequest`/`ConfirmResourceChangeRequest`/`ProceedRequest` 네 모델이 조용히 `DUNGEONWORLD_LIKE_ID`로 기본값을 채우고 있었다. 그래서 OpenQuest 캐릭터도 던전월드류 무브를 받았고, `action_confirmed`가 이미 기록된 뒤 판정 단계에서 400이 났다
- **고침:** 네 요청 모델에서 그 칸을 제거하고 `character.rulebook_id`를 읽도록 바꿈. `DeclareRequest`/`ProceedRequest`에 `extra="forbid"` 추가. `tests/fixtures/characters.py`에 `OPENQUEST_CHARACTER` 추가하고 룰북이 안 맞던 OpenQuest/Cairn 시험 여러 개를 재배선
- **파일:** `src/gptrpg/web/routes_actions.py`, `tests/fixtures/characters.py`, `tests/test_web_actions.py` 외
- **커밋:** `6e7c2e5` (이 계획 밖)
- **이 계획과의 관계:** D-11/D-12/D-13이 만드는 표시와 무관 — D-15가 브라우저에 도달하기 위한 전제 조건 결함

**2. [일부 범위 안, 일부 범위 밖] 주사위 모양·모달 타이밍·죽은 턴 스피너 3건**
- **발견:** Task 3 — 브라우저 확인 중
- **문제 ①(D-13과 직접 연결, 이 계획의 갭):** 주사위 모양이 값으로 정해져 d100에서 십의 자리 8이 숫자로, 일의 자리 6이 6면체 눈으로 그려졌다 — 연출과 검산 줄이 「같은 서버 값」을 말한다는 이 계획 자신의 약속을 어겼다. `CheckSummary`에 `rollRoles`를 추가하고 `DiceModal`이 role→모양으로 매핑하도록 고침
- **문제 ②③(범위 밖):** 모달이 타이머로 자동 닫혀 검산 경로를 읽기 전에 사라졌다(명시적 확인 버튼으로 교체), 확인됐지만 판정 사건이 없는 턴이 「판정을 기다리는 중」으로 영원히 도는 문제(더 이상 최신이 아니면 미해결로 표시)
- **커밋:** `0a0c9fe` (이 계획 밖)
- **이 계획과의 관계:** 문제 ①만 이 계획(D-13)이 원래 닫으려던 갭이다. ②③은 12.2-01/02가 만든 모달·턴 카드 로직의 별개 결함이다

**3. [범위 밖] 확인 버튼이 안 눌리던 문제**
- **발견:** Task 3 확인 중
- **문제:** 오버레이가 `pointer-events: none`이라(입력창·스크롤을 살리려는 의도) 확인 버튼도 같이 안 눌렸다 — Esc만 동작
- **고침:** 버튼에만 `pointer-events` 되살림, 굴리는 동안만 반응하는 `window` pointerdown 리스너 복원(닫기는 확인 버튼·Esc로만)
- **커밋:** `5c28766` (이 계획 밖)
- **이 계획과의 관계:** 이 계획의 D-11/D-12/D-13/D-15 어느 것과도 직접 무관 — D-15 확인 도중 발견된 UI 결함

---

**총 3건이 이 계획 밖에서 발견·수정됨.** 1건(문제 ①)만 이 계획이 지목한 D-13 갭을 실제로 닫았다. 나머지는 더 오래되고 더 넓은 결함이었으나, 이 계획이 D-15를 브라우저까지 처음 밀어붙였기 때문에 드러났다.

## Issues Encountered

None beyond the deviations above.

## User Setup Required

None - no external service configuration required.

## Known Limitations (not stubs — explicit scope boundary)

- **D-11(버려진 눈 흐림)·D-12(다시 굴림 줄 나눔)는 여전히 브라우저에서 도달할 수 없다.** `reroll_2d6`/`push_d100`은 시험에서만 불리고, `BONUS_DICE` 수정치를 만드는 제품 코드가 저장소 어디에도 없다 — OpenQuest의 난이도는 `TARGET_SHIFT`로 구현돼 보너스 주사위 경로 자체를 안 쓴다. 이 두 표시의 검증은 자동 시험(`tests/test_check_calculation.py`, `checkSummary.test.ts`)이 유일한 수단이고, D-15의 사람 확인은 다시 굴림 없는 보통 판정만 덮었다 — 계획 자신이 처음부터 이렇게 못박았다(backstop 항목).
- **`.gptrpg/events.db`에 확인용 임시 캐릭터 데이터가 남아 있다.** D-15 확인에 실제 캐릭터가 필요했는데 캐릭터 만들기 화면이 아직 없어서(Phase 12.3), 스크래치패드 일회성 스크립트로 세션 `oq-verify`에 캐릭터 `hana`, 세션 `oq-smoketest`에 `test-dummy`를 심었다. 제품 코드가 아니고 커밋되지 않았다 — Phase 12.3이 실제 캐릭터 만들기 화면을 붙이면 자연히 대체된다.

## Next Phase Readiness

- 서버 계산 줄(D-01~D-14)과 그 위의 화면 표시(D-11/D-12/D-13)가 전부 완성됐고, D-15 사람 확인까지 승인됨 — Phase 12.2 「판정 합계를 서버가 보낸다」 전체가 닫혔다.
- Task 3 관문에서 드러난 라우트 룰북 기본값 결함(`6e7c2e5`)이 고쳐져, 앞으로의 모든 확인 작업에서 OpenQuest/Cairn 캐릭터가 자신의 룰북으로 정상 판정된다 — 다음 단계(Phase 12.3 캐릭터 만들기 화면)가 이 위에서 안전하게 시작할 수 있다.
- `.gptrpg/events.db`의 임시 확인용 캐릭터(`hana`/`test-dummy`)는 제품 데이터가 아니다 — Phase 12.3 작업 시 이 사실을 알고 있을 것.

## Self-Check: PASSED

- `src/gptrpg/rules_core/check_calculation.py`, `frontend/src/components/DiceModal.tsx`, `frontend/src/session/checkSummary.ts` — FOUND, 각 파일이 위 설명대로의 내용을 담고 있음을 확인.
- 커밋 `b66ce7d`, `57d7435`, `6e7c2e5`, `0a0c9fe`, `5c28766` 전부 `git log --oneline`에서 FOUND.

---
*Phase: 12.2-check-total*
*Completed: 2026-08-20*
