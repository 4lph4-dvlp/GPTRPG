---
phase: 12.2-check-total
verified: 2026-08-20T04:30:00Z
status: passed
score: 15/15 결정(D-01~D-15) 검증
behavior_unverified: 0
overrides_applied: 0
re_verification: no — 첫 검증
---

# Phase 12.2: 판정 합계를 서버가 보낸다 Verification Report

**Phase Goal:** 판정 합계를 서버가 보낸다 — 룰북과 무관하게 검산이 맞는다.
**Verified:** 2026-08-20
**Status:** passed
**Re-verification:** No — 첫 검증

이 단계에는 REQ-ID가 매핑돼 있지 않다(REQUIREMENTS.md grep 결과 없음, 계획 세 개
모두 `requirements: []`). 그래서 실제 요구사항 역할을 하는 `12.2-CONTEXT.md`의
결정 D-01~D-15와 계획 세 개의 `must_haves`를 코드와 직접 대조했다. SUMMARY.md의
주장은 하나도 그대로 믿지 않고, 아래는 전부 이번 검증에서 직접 읽거나 실행해서
확인한 것이다.

## Goal Achievement

### Observable Truths (D-01~D-15)

| # | 결정 | 상태 | 근거 |
|---|------|------|------|
| D-01 | 판정 기록에 합계가 남는다(`CheckResolved.total` = `CheckOutcome.total`) | ✓ VERIFIED | `schema.py:235-239` 필드 확인. 이전 기록 마이그레이션 없음(`git show HEAD -- tests/fixtures/session1_events.jsonl`가 비어 있음, `schema_version:2` 895건 그대로) |
| D-02 | 판 10부터 합계·룰북 필수, 판 10 미만은 허용 | ✓ VERIFIED | `schema.py:252-256` `_require_total_from_schema_10` 검증기 직접 읽음. `test_freshly_written_schema_10_check_resolved_event_folds_without_exception` PASS |
| D-03 | 판정 기록이 어느 룰북인지 스스로 말한다(`rulebook_id`) | ✓ VERIFIED | `schema.py:240-244`. `check_views.py::calculation_view_for`가 `event.rulebook_id`만으로 룰북을 찾아 지난 판정의 계산 줄을 만든다 |
| D-04 | 저장된 합계가 규칙 코어 재생과 어긋나면 시험이 터진다 | ✓ VERIFIED | `test_dice_replay.py::test_replay_matches_stored_total_for_2d6`·`_for_d100` 직접 실행, PASS. 커밋된 픽스처(`session1_events.jsonl`)는 열지 않는다(`grep -c 'session1_events'`가 0) |
| D-05 | 합계 없는 옛 판정은 「합계 기록 없음」, 0으로 안 때운다 | ✓ VERIFIED | `checkSummary.ts::buildCheckSummary`가 `calculation===null`일 때 `totalMissing=true`. 세 컴포넌트(`TurnCard.tsx:106-107`·`CheckBreakdown.tsx:31,50`·`DiceModal.tsx:231`) 전부 `COPY.checkTotalMissing` 렌더 확인 |
| D-06 | 한 세션 안에 두 모양(합계 있음/없음)이 섞여도 예외 규칙 없음 | ✓ VERIFIED | 구조적으로 D-01(마이그레이션 없음)+D-05(레코드별 표시)의 직접 결과 — 판별은 레코드 단위 `total is None` 검사뿐, 세션·시점에 따른 분기가 코드 어디에도 없음 |
| D-07 | 판정 방향은 사건이 아니라 응답으로만 | ✓ VERIFIED | `CheckResolved`에 방향 필드 없음(schema.py 확인). `check_calculation.py::direction_for`가 명시적 매핑 표(`_DIRECTIONS`)로 응답(`CheckCalculationView.direction`)에만 싣는다 |
| D-08 | 서버가 계산 줄을 만들고, 화면엔 룰북별 계산이 없다 | ✓ VERIFIED | `checkSummary.ts`에 산수 0줄(grep 확인). `check_calculation.py`가 `_build_two_d6`/`_build_d100_roll_under`에서 전부 계산 |
| D-09 | 계산 줄이 역할·값·출처 조각 목록(완성 문장 아님) | ✓ VERIFIED | `CalculationSegmentView(role, value, source, discarded)` — pydantic 모델, 문자열 조립 없음(`check_views.py`) |
| D-10 | 역할 이름은 자유 문자열, 닫힌 목록 없음 | ✓ VERIFIED | Python `role: str`(Literal 없음), TS `role: string`(유니온 없음). `segmentRoleLabel`이 모르는 값을 원문 그대로 반환(정규화·소문자화 없음, `labels.ts:150-151`) |
| D-11 | 버려진 십의 자리가 흐리게 함께 보인다 | ✓ VERIFIED | `check_calculation.py:170-183` 채택 규칙(`dice_delta>0`→최솟값,`<0`→최댓값,동값은 앞자리). `test_bonus_dice_marks_the_larger_tens_as_discarded`·`test_penalty_dice_marks_the_smaller_tens_as_discarded`·`test_tied_tens_values_discard_only_the_later_duplicate` 직접 실행 PASS. 세 컴포넌트 모두 `segment.discarded`로 흐림 클래스+`COPY.checkDiscarded` 꼬리표 렌더 |
| D-12 | 다시 굴림은 「처음 굴림/다시 굴림」 두 줄, 합계는 마지막 줄에만 | ✓ VERIFIED | `_build_two_d6`/`_build_d100_roll_under`의 줄 나눔 로직 직접 읽음. `test_reroll_2d6_splits_into_two_rows_with_total_on_the_last`·`test_push_d100_splits_into_two_rows_with_total_on_the_last` PASS |
| D-13 | 연출이 버려진 눈까지 전부 굴리고 멈춘 뒤 흐려진다 | ✓ VERIFIED | `DiceModal.tsx`가 `check.rolls.length`개를 굴리고(`grep` 확인) `showSum` 이후에만 흐림 적용. `shapeFor(summary.rollRoles[index])`로 주사위 모양도 서버 role을 따름(mid-phase fix `0a0c9fe`) |
| D-14 | 세 화면이 한 곳(`checkSummary.ts`)만 쓴다 | ✓ VERIFIED | `TurnCard.tsx:91`·`DiceModal.tsx:70`이 `buildCheckSummary` 직접 호출, `CheckBreakdown.tsx`는 `summary` prop만 받고 `ChatPane.tsx`가 `buildCheckSummaryFromConfirmResponse`로 만들어 넘김. 세 파일 어디에도 `sum + value`/`modifierTotal`/자체 reduce 없음(grep 확인) |
| D-15 | OpenQuest(d100) 브라우저 확인 — 사람이 검산을 따라갈 수 있다 | ✓ VERIFIED (사람 확인 완료) | 검증 컨텍스트에 따라 이미 완료·승인됨: 눈 [8,6]→86, 목표 55(밑돌아야 성공), 세 화면 일치. 재요청하지 않음 |

**Score:** 15/15 결정 검증 (0건 미검증, 0건 미확인)

### Required Artifacts (세 계획 통합)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/gptrpg/rules_core/check_calculation.py` | 계산 조각 빌더·방향 매핑 | ✓ VERIFIED | 신설 확인, `web`/`event_log`/`rulebooks` import 없음(`.importlinter` contract:2 KEPT) |
| `src/gptrpg/web/check_views.py` | 응답 View + 어댑터 | ✓ VERIFIED | `calculation_view_for` 두 정상 갈래(`None`)/예외 갈래 직접 읽음 |
| `src/gptrpg/event_log/schema.py` | `EVENT_SCHEMA_VERSION=10`, `CheckResolved.total`/`.rulebook_id` | ✓ VERIFIED | 코드 직접 확인 |
| `src/gptrpg/session_actor/actor.py` | `_prepare_resolve_check` 페이로드 확장 | ✓ VERIFIED | `outcome.total`·`command.rulebook_id` 확인 |
| `src/gptrpg/web/routes_events.py` | `PollResponse.check_calculations` | ✓ VERIFIED | 병렬 목록, `events` 사건 객체 자체는 안 바뀜 |
| `src/gptrpg/web/routes_actions.py` | `ConfirmResponse.rulebook_id`·`.calculation`, 두 조립 자리 | ✓ VERIFIED | 1132-1134·1192-1194 두 자리 모두 같은 세 인자(`total`/`rulebook_id`/`calculation`) 확인 |
| `frontend/src/session/checkSummary.ts` | 「한 곳」, 산수 없음 | ✓ VERIFIED | 코드 전체 읽음 — 산수 0줄 |
| `frontend/src/components/TurnCard.tsx`·`CheckBreakdown.tsx`·`DiceModal.tsx` | 세 화면 전부 서버 계산 줄 소비 | ✓ VERIFIED | 세 파일 모두 `buildCheckSummary`(직접 또는 prop 경유) 사용, 자체 산수 없음 |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `CheckOutcome.total` | `CheckResolved.total` | `_prepare_resolve_check` 페이로드 | ✓ WIRED | actor.py 확인 |
| `CheckResolved.rulebook_id` | `build_check_calculation` | `get_rulebook`→`calculation_view_for` | ✓ WIRED | check_views.py 확인 |
| `_BUILDERS` | `_RESOLVERS` | 열쇠 집합 동일성 | ✓ WIRED | `test_every_resolver_has_a_segment_builder` PASS |
| `PollResponse.check_calculations` | `TurnCard` | `usePolling`→`SessionScreen`→`StoryPane` | ✓ WIRED | 배선 코드 확인, `groupTurns.ts` 무변경 확인 |
| **네 액션 라우트 → `character.rulebook_id`** | (검증 컨텍스트가 지목한 사전 결함 수정) | — | ✓ WIRED | `DeclareRequest`/`ConfirmRequest`/`ConfirmResourceChangeRequest`/`ProceedRequest` 어디에도 룰북 요청 칸이 없고, 네 라우트 모두 `character.rulebook_id`를 읽는다(라인 429·772·891·1527·1554). 캐릭터 로드 전에 룰북 조회로 가는 경로 없음 |

### Behavioral Spot-Checks (실제 실행, SUMMARY 신뢰 안 함)

| Behavior | Command | Result | Status |
|---|---|---|---|
| 페널티/보너스/동값 채택 규칙 | `pytest tests/test_check_calculation.py -k "percentile or penalty or discard or reroll or push or resolver"` | 7개 전부 PASS | ✓ PASS |
| D-04 재생 대조 | `pytest tests/test_dice_replay.py -k matches_stored_total` | 2개 PASS | ✓ PASS |
| 즉시 응답 계산 줄(정상/거부/서사실패) | `pytest tests/test_web_actions.py -k calculation` | 3개 PASS | ✓ PASS |
| 판 10 하위 호환·리듀서 무변경 | `pytest tests/test_event_log.py tests/test_event_schema_migration.py -k "fold or reducer or session1"` | 29개 PASS | ✓ PASS |
| 백엔드 전체 | `.venv/bin/python -m pytest -q` (1회) | 1323 passed | ✓ PASS |
| 계층 경계 | `uv run lint-imports` | 4 kept, 0 broken | ✓ PASS |
| 프론트 타입 | `cd frontend && npx tsc --noEmit` | 0 오류 | ✓ PASS |
| 프론트 시험 | `cd frontend && npm test -- --run` | 32 passed (4 파일) | ✓ PASS |
| 픽스처 무변경 | `git show HEAD -- tests/fixtures/session1_events.jsonl` | 비어 있음, `schema_version:2` 895건 그대로 | ✓ PASS |
| 디버트 마커 | `grep -E 'TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER'` (이번 단계가 고친 17개 파일) | 무관한 기존 상수(`SHOW_IMAGE_PLACEHOLDER`) 1건 외 없음 | ✓ PASS |

### Requirements Coverage

REQUIREMENTS.md에 12.2를 매핑하는 REQ-ID가 없다(grep 결과 없음, 검증 지시사항과
일치). 계획 세 개 모두 `requirements: []` — 오펀 요구사항 없음, 요구사항 커버리지
표는 해당 없음.

### Anti-Patterns Found

없음. 이번 단계가 고친 17개 파일에서 TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER 패턴을
스캔했고, 유일하게 걸린 `SHOW_IMAGE_PLACEHOLDER`는 이 단계와 무관한 기존 이미지
연출 플래그(파일명이 아니라 상수 이름에 우연히 걸림)다.

### 코드 리뷰(12.2-REVIEW.md) 요약 — 새로 만들지 않고 참조만

이미 실행된 리뷰가 critical 0 · warning 2 · info 2를 찾았다. 둘 다 이번 단계의
`must_haves`를 위반하지 않는 견고성/UX 갭이라 phase goal 달성에는 영향이 없지만,
사장님이 알아야 할 내용이라 요약한다:

- **WR-01**: `poll_events`가 `calculation_view_for` 예외를 잡지 않는다 — 지금은
  `BONUS_DICE`/다시 굴림을 만드는 제품 코드가 없어 터지지 않지만, 나중에 다시
  굴림이 실제 플레이 경로에 연결되면 판정 기록 하나가 잘못돼도 그 세션의 폴링
  전체가 영구히 막힐 수 있다. 이번 검증에서 `routes_events.py:101-106`이 정말
  try/except 없이 리스트 컴프리헨션인지 직접 확인했다 — 리뷰 서술이 정확하다.
- **WR-02**: `DiceModal`에서 「드러내기」 클릭과 확인 버튼이 같은 화면 좌표에
  겹칠 수 있는 타이밍 경합. mid-phase fix(`5c28766`)가 버튼 클릭 가능 여부는
  고쳤지만 이 타이밍 경합 자체는 리뷰가 지적한 별개 문제로 남아 있다.
- IN-01·IN-02는 사소한 코드 정리 항목.

이 넷은 이번 단계의 D-01~D-15 어느 것도 막지 않는다 — phase goal(「합계를
서버가 보내고 검산이 맞는다」)과 무관한 견고성 개선 항목이다.

### Human Verification Required

없음. D-15(OpenQuest 브라우저 확인)는 검증 컨텍스트에 따라 이미 사람이 직접
확인·승인했다(눈 [8,6]→86, 목표 55 밑돌아야 성공, 세 화면 일치) — 재요청하지
않는다. D-11/D-12(버려진 눈·다시 굴림 표시)는 계획 자신이 처음부터 「브라우저
도달 불가, 자동 시험이 유일한 검증 수단」이라고 못박은 backstop 항목이고, 실제로
`BONUS_DICE`를 만드는 제품 코드가 저장소 어디에도 없다(`grep -rl` 확인) — 이것은
사람 확인 누락이 아니라 계획이 사전에 선언한 범위 밖이므로 새 human-verification
항목으로 만들지 않는다.

### Gaps Summary

없음. 세 계획의 `must_haves`(truths·artifacts·key_links·prohibitions) 전부와
`12.2-CONTEXT.md`의 D-01~D-15 전부를 코드에서 직접 읽거나 시험을 실행해 확인했다.
mid-phase fix 세 건(`6e7c2e5`·`0a0c9fe`·`5c28766`)도 커밋 로그와 코드 양쪽에서
확인됐고, D-15를 실제로 가능하게 만든 전제 조건 결함(액션 라우트의 룰북 기본값
문제)이 정확히 수정돼 있다 — 네 라우트 모두 캐릭터에서 룰북을 읽고, 클라이언트가
보낸 룰북 칸은 요청 모델 어디에도 남아 있지 않다.

---

_Verified: 2026-08-20_
_Verifier: Claude (gsd-verifier)_
