---
status: testing
phase: 11-rulebook-vocabulary
source: [11-VERIFICATION.md]
started: 2026-08-17
updated: 2026-08-17
---

## Current Test

number: 1
name: 「값이 0이다 / 개념이 아예 없다 / 규칙으로 안 센다 / 그냥 빠뜨렸다」 네 가지 구분에 빠진 경계가 있는지 사람이 훑는다
expected: |
  세 가지 핵심 구분은 이미 시험으로 못 박혀 있다는 것을 검증에서 확인했다.
  남은 질문은 「그 시험들이 놓친 경계가 더 있는가」 하나뿐이다.
  없다고 보시면 그대로 종결, 있다고 보시면 Phase 12 계획에 반영한다.
awaiting: user response

## Tests

### 1. 네 가지 구분에 빠진 경계가 있는지 사람이 훑는다

expected: 자동 탐침 도구가 이 항목에 대해 「어떤 질문을 던져야 할지 모르겠다」고 남긴 자리다. 계획서 자신이 「검증 단계에서 사람에게 그대로 올린다 — 자동 통과 처리하지 않는다」고 못 박아 뒀다. grep이나 시험 실행으로는 답이 안 나온다.
result: [pending]

**사장님이 판단하실 것:** 캐릭터의 어떤 수치에 대해 아래 넷이 서로 구분되어야 하는데, 지금 덮인 것 외에 더 필요한 경계가 있는지.

| 상황 | 예 | 지금 상태 |
|------|-----|----------|
| 값이 0이다 | 마법점이 0인 언데드 — 개념은 있고 지금 값이 0 | 시험으로 고정됨 |
| 개념이 아예 없다 | 이 룰북에 마법점이라는 것 자체가 없음 | 시험으로 고정됨 |
| 규칙으로 안 센다 | 소지품은 있지만 숫자로 세지 않음(진행자 재량) | 시험으로 고정됨 |
| 그냥 빠뜨렸다 | 선언을 깜빡한 것 — 등록 시점에 거부됨 | 시험으로 고정됨 |

### 2. 새 환경에서 「판정 없이 이야기가 이어지는」 기능이 실제로 켜지는지

expected: 이 기능이 실전에서 작동하려면 분류기 모델이 충분히 커야 한다. 작은 모델(8B)에서는 「문을 연다」조차 판정이 필요한 것으로 잘못 분류돼 이 화면이 거의 안 켜졌다 — 11-06 확인 관문에서 실측됐고 사장님이 큰 모델로 올리는 결정을 내렸다. 그런데 그 설정 파일(`.gptrpg/agents.json`)은 git이 무시하므로 저장소에 안 남는다.
result: [pending]

**사장님이 판단하실 것:** 이 설정이 저장소에 안 남는 것을 그대로 둘지, 아니면 어딘가에 기록·자동화할지.

- 지금 설정: 분류기 `nemotron-3-super-120b`, 진행자 `nemotron-3-ultra-550b`
- 근거 문서: `11-MODEL-FINDING.md`, `11-NARRATION-LANGUAGE-FINDING.md` (둘 다 커밋됨)
- 위험: 새 기기에 받거나 다른 사람이 받으면 기본 설정으로 돌아가 기능이 조용히 안 켜진다

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
