---
phase: 11-rulebook-vocabulary
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/gptrpg/web/routes_characters.py
  - frontend/src/api/types.ts
  - frontend/src/panes/StatusPane.tsx
  - frontend/src/labels.ts
  - tests/test_web_characters.py
autonomous: false
requirements: [RULE-11, RULE-12]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`form == \"none\"`으로 선언된 축은 `CharacterSheetView` 응답 **자체에서** 빠진다 — 응답 JSON 본문에 그 축 이름이 문자열로도 등장하지 않는다. 화면이 숨기는 것이 아니라 서버가 안 보낸다 (RULE-12 성공 기준 2)"
    - "여섯 표현 형태 각각이 `StatusPane`에 자기 갈래를 갖는다 — 세그먼트 원은 `ThreatClock` 컴포넌트를 재사용하고, 이름 붙은 칸·태그 목록·자원 주사위는 새 갈래로 그려진다 (RULE-11)"
    - "자원 축이 하나도 없는 캐릭터의 시트가 회색 빈 패널이 아니라 「이 룰북은 세는 수치를 쓰지 않는다」는 의도된 문구를 보인다 (RULE-11 empty · Claude's Discretion)"
    - "`max === 0`인 축의 게이지 폭이 `NaN%`가 아니라 `0%`다 (RULE-11 precision)"
    - "시트 응답의 `stats` 순서가 `Entity.stats` 선언 순서와 같고, 어디서도 다시 정렬하지 않는다 (RULE-11 ordering)"
    - "`form=\"numeric\", current=0`인 축은 화면에 `0`으로 그려지고, 「개념이 없다」로 선언된 축은 화면에 아예 없다 — 둘이 화면에서 서로 다르게 보인다 (성공 기준 3)"
    - statement: "여섯 형태가 실제로 사람 눈에 어떻게 그려지는지는 이 저장소에 프론트엔드 자동 시험 틀이 없어 자동 단언으로 증명되지 않는다 — Task 3의 사람 확인 관문이 그 자리를 맡는다"
      verification: backstop
  artifacts:
    - "src/gptrpg/web/routes_characters.py — `form == \"none\"` 축 제외 조립"
    - "frontend/src/panes/StatusPane.tsx — 여섯 갈래 렌더링"
    - "frontend/src/labels.ts — 자원 축 관련 새 문구"
    - "tests/test_web_characters.py — `none` 축 제외 시험"
  key_links:
    - "`StatEntry.form` ↔ `StatEntryView.form` ↔ `types.ts` `StatEntry[\"form\"]` — 세 곳이 같은 여섯 리터럴이어야 화면이 형태를 못 읽는 일이 없다"
    - "`StatEntry.current`/`.max` ↔ `ThreatClock`의 `segment`/`segmentCount` — 세그먼트 원 재사용이 성립하는 지점"
    - "서버 응답 제외 로직 ↔ 프론트엔드 렌더 갈래 — 프론트에 `form === \"none\"` 분기가 생기면 서버 제외가 안 된 것이다"
  prohibitions:
    - "`form == \"none\"` 축을 프론트엔드 조건부 렌더링으로 숨기는 방식으로 구현하지 않는다"
    - "빈 자원 축 목록을 회색 빈 패널이나 비활성화된 칸으로 그리지 않는다"
    - "여섯 형태 중 하나라도 렌더 갈래 없이 조용히 아무것도 안 그리는 상태로 두지 않는다"
    - "`StatEntryView`/`types.ts`의 칸 이름을 `StatEntry`와 한 글자라도 다르게 짓지 않는다"
    - "프론트엔드에 새 차트·아이콘 npm 패키지를 도입하지 않는다"
    - "던전월드/PbtA 고유 규칙을 화면 어휘로 승격하지 않는다"
---

<objective>
**나머지 다섯 표현 형태를 화면까지 붙이고, 「안 쓴다」를 서버 응답에서 사라지게 한다.**

11-01이 `numeric` 한 형태로 뚫어 놓은 줄기에 `clock` / `named_slots` / `tag_list` /
`usage_die` / `none` 다섯을 붙인다. 그중 `none`은 **그리지 않는 것이 아니라 서버가
안 보내는 것**으로 구현한다 — 응답 JSON에 축 이름이 문자열로도 남지 않아야 「완전히
사라진다」가 시험으로 증명된다.

Purpose: RULE-12 성공 기준 2의 실패 모양(「빈 인벤토리 패널을 회색으로 보여주는 것」)은
프론트엔드가 조건부로 숨기는 순간 그대로 재현된다. 응답 계약 층에서 끊는다.
Output: 여섯 갈래 `StatusPane` · `none` 축을 거르는 시트 응답 조립 · 응답 계약 시험.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rulebook-vocabulary/11-CONTEXT.md
@.planning/phases/11-rulebook-vocabulary/11-RESEARCH.md
@.planning/phases/11-rulebook-vocabulary/11-PATTERNS.md
@.planning/phases/11-rulebook-vocabulary/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

| 종류 | 이름 | 위치 |
|---|---|---|
| 함수 | `_visible_stats(entity, rulebook)` | `src/gptrpg/web/routes_characters.py` |
| 컴포넌트 | `SlotGrid` · `TagList` · `UsageDie` | `frontend/src/panes/StatusPane.tsx` |
| 문구 키 | `COPY.noResourceAxes` · `COPY.emptySlot` · `COPY.usageDieSpent` | `frontend/src/labels.ts` |
| 시험 | `test_none_axis_excluded_from_sheet_response` | `tests/test_web_characters.py` |
| 시험 | `test_sheet_stats_preserve_declaration_order` | `tests/test_web_characters.py` |
| 시험 | `test_rulebook_with_zero_axes_returns_empty_stats` | `tests/test_web_characters.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 「안 쓴다」 축을 시트 응답 조립 단계에서 제외한다</name>
  <files>src/gptrpg/web/routes_characters.py, tests/test_web_characters.py</files>
  <read_first>
    src/gptrpg/web/routes_characters.py,
    src/gptrpg/web/characters_data.py,
    src/gptrpg/rules_core/rulebook.py,
    src/gptrpg/rules_core/entities.py,
    tests/test_web_characters.py,
    .planning/phases/11-rulebook-vocabulary/11-01-SUMMARY.md
  </read_first>
  <behavior>
    - 룰북이 어떤 축을 `form="none"`으로 선언하고 개체가 그 이름의 `StatEntry(form="none", none_kind=...)`를 갖고 있으면, 시트 응답의 `stats`에 그 항목이 없다
    - 그 응답 본문 문자열 어디에도 그 축 이름이 등장하지 않는다 (`"소지품" not in response.text`)
    - `none`이 아닌 다섯 형태의 축은 전부 응답에 그대로 실린다
    - `resource_axes=()`인 룰북을 쓰는 개체의 시트 응답은 `stats: []`이고 HTTP 200이다
    - 응답 `stats`의 순서가 `Entity.stats` 선언 순서와 같다
  </behavior>
  <action>
`web/routes_characters.py`에 모듈 사적 헬퍼 `_visible_stats(entity, rulebook)`을
신설한다. `entity.stats`를 선언 순서 그대로 훑으면서, 그 이름의
`ResourceAxisDecl.form`이 `"none"`인 축과 `StatEntry.form`이 `"none"`인 항목을 **둘 다**
버리고 나머지를 순서 그대로 돌려준다. 정렬을 새로 하지 않는다 — `list_characters()`가
세운 「선언 순서를 그대로 유지한다」 관례와 같은 이유다.

`get_character_sheet`를 고친다. 지금은 `CharacterSheetView(**asdict(entity))` 한 줄로
전부를 그대로 옮기는데, 이제 `gptrpg.rulebooks.get_rulebook(entity.rulebook_id)`으로
룰북을 찾아 `_visible_stats`를 태운 뒤 `stats`만 그 결과로 갈아 끼워 조립한다.
`UnknownRulebook`이 올라오면 500이 아니라 그대로 예외로 두지 말고
`HTTPException(status_code=500, ...)`로 감싸지도 말 것 — 등록되지 않은 룰북을 가리키는
캐릭터는 11-02의 등록 검증이 이미 막았으므로, 여기서 방어 코드를 새로 만들면 죽은
분기가 된다. `get_rulebook`이 던지는 예외를 그대로 올라가게 둔다.

`StatEntryView`/`CharacterSheetView`의 도크스트링에 이 제외가 **서버 쪽 책임**이라는
문장을 한 줄 박는다 — 프론트엔드가 숨기는 방식이면 응답 JSON에 축 이름이 그대로
실려 나가 「완전히 사라진다」가 문자 그대로 깨진다는 것(11-RESEARCH.md Pitfall 2).

`tests/test_web_characters.py`에 시험을 추가한다. 기존 파일의 `web_client` 픽스처와
`test_known_character_sheet_matches_characters_data`(32행) 스타일을 그대로 따른다.
- `test_none_axis_excluded_from_sheet_response`: 임시 룰북과 임시 캐릭터를
  `RULEBOOKS`/`PLAYER_CHARACTERS`에 넣고(`try`/`finally`로 지운다 —
  `tests/test_session_actor.py:296`이 세운 관례), 응답 `stats`에 그 축이 없고
  `response.text`에 축 이름 문자열이 없다는 것을 **둘 다** 단언한다. 이름은 이 시험
  안에서만 쓰는 값으로 짓고 특정 룰북 어휘를 쓰지 않는다.
- `test_rulebook_with_zero_axes_returns_empty_stats`: `resource_axes=()`인 룰북과 그
  룰북을 쓰는 축 없는 캐릭터로 200 + `stats == []`를 단언한다.
- `test_sheet_stats_preserve_declaration_order`: 브람의 응답 `stats` 이름 순서가
  `PLAYER_CHARACTERS["bram"].stats`의 이름 순서와 완전히 같다는 것을 단언한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_characters.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_web_characters.py -k none_axis_excluded -x` 종료 코드 0
    - `uv run pytest tests/test_web_characters.py -k zero_axes -x` 종료 코드 0
    - `uv run pytest tests/test_web_characters.py -k preserve_declaration_order -x` 종료 코드 0
    - `uv run pytest tests/test_web_characters.py -q` 종료 코드 0 (기존 24개 시험 전부 유지)
    - `src/gptrpg/web/routes_characters.py`에 `def _visible_stats(` 문자열이 있다
    - 「안 쓴다」로 선언된 축 이름이 `GET /sessions/{sid}/characters/{cid}` 응답 본문 문자열에 등장하지 않는다
  </acceptance_criteria>
  <done>「안 쓴다」로 선언된 축이 서버 응답 자체에서 빠지고, 그 사실이 응답 본문 문자열 단언으로 고정됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 캐릭터 판이 여섯 표현 형태를 각자 자기 모양으로 그린다</name>
  <files>frontend/src/panes/StatusPane.tsx, frontend/src/labels.ts, frontend/src/api/types.ts</files>
  <read_first>
    frontend/src/panes/StatusPane.tsx,
    frontend/src/components/ThreatClock.tsx,
    frontend/src/labels.ts,
    frontend/src/api/types.ts,
    .planning/phases/11-rulebook-vocabulary/11-PATTERNS.md
  </read_first>
  <action>
`StatusPane.tsx`의 `StatRows`를 `stat.form` 여섯 값에 대한 명시적 갈래로 만든다.
갈래별 모양:
- `numeric`: 11-01이 만든 두 모양을 그대로 유지한다 — `stat.max`가 `null`이면 부호 붙은
  숫자, 있으면 `stat.current`/`stat.max` 게이지. `stat.max === 0`이면 폭 `0`.
- `clock`: `frontend/src/components/ThreatClock.tsx`를 **그대로 재사용**한다 —
  `segment={stat.current}` `segmentCount={stat.max}`로 넘긴다. 새 컴포넌트를 만들지 않고,
  위협 시계용 `pulsing`은 넘기지 않는다(자원 축은 재촉하는 장치가 아니다 —
  `StatusPane.tsx` 도크스트링의 「재촉하지 않는다」 규율).
- `named_slots`: `stat.slot_values` 배열을 고정 길이 격자로 그린다. `null`인 칸은
  `COPY.emptySlot` 문구로, 채워진 칸은 그 문자열로 표시한다. 칸 개수는 배열 길이를
  그대로 쓴다 — 상한 상수를 코드에 두지 않는다.
- `tag_list`: `stat.tags`를 칩 목록으로 그린다. 빈 배열이면 축 이름 + 「없음」에 해당하는
  중립 표시를 하되, 회색 비활성 패널로 그리지 않는다.
- `usage_die`: `stat.current`를 「d{면수}」 형태로 표시하고, `0`이면 `COPY.usageDieSpent`
  문구로 소진 상태를 보인다.
- `none`: **갈래를 만들지 않는다.** 서버가 이미 안 보내므로 이 값이 화면에 도달할 수
  없다. 타입스크립트가 여섯 값을 요구하면 도달 불가 갈래에 명시적 주석 한 줄과 함께
  `null`을 돌려주되, 그 자리에 「회색으로 보여주기」에 해당하는 어떤 표시도 넣지 않는다.

새 하위 컴포넌트 `SlotGrid` · `TagList` · `UsageDie`를 같은 파일 안에 만든다 — 위협
시계처럼 여러 화면이 공유하는 것이 아니라 이 판 전용이므로 `components/`로 빼지 않는다.

`sheet.stats`가 빈 배열이면 `StatRows`가 `COPY.noResourceAxes`를 한 줄로 보인다 —
자원 축이 하나도 없는 룰북에서 사람이 「고장 났나」로 오해하지 않게 하는 자리다
(CONTEXT.md Claude's Discretion). 회색 빈 패널이나 비활성 칸을 그리지 않는다.

`frontend/src/labels.ts`의 `COPY` 객체에 문구 셋을 추가한다 — 기존 항목과 같은 스타일
(짧은 안내, 존댓말 종결):
`noResourceAxes: "이 룰북은 세는 수치를 쓰지 않아요"`,
`emptySlot: "빈 칸"`,
`usageDieSpent: "다 씀"`.

`frontend/src/api/types.ts`는 11-01이 이미 여덟 칸으로 넓혀 놓았으므로 이번에는
`slot_values`/`tags`가 화면에서 쓰이는 실제 모양(`(string | null)[] | null`,
`string[] | null`)과 어긋나지 않는지만 확인하고 필요한 만큼만 고친다. 백엔드
`StatEntryView`와 칸 이름이 한 글자도 달라지지 않게 유지한다.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` 종료 코드 0
    - `cd frontend && npm run build` 종료 코드 0
    - `grep -c 'stat.form ===' frontend/src/panes/StatusPane.tsx` 결과가 5 이상
    - `frontend/src/panes/StatusPane.tsx`에 `<ThreatClock` 문자열이 있다
    - `frontend/src/labels.ts`에 `noResourceAxes:` · `emptySlot:` · `usageDieSpent:` 세 키가 있다
    - `grep -c 'function SlotGrid\|function TagList\|function UsageDie' frontend/src/panes/StatusPane.tsx` 결과가 3
  </acceptance_criteria>
  <done>여섯 형태가 각자 자기 갈래를 갖고, 세그먼트 원은 기존 위협 시계 컴포넌트를 새 코드 없이 재사용하며, 축이 하나도 없는 시트는 회색 빈 패널이 아니라 의도된 한 줄을 보인다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 여섯 형태가 실제로 어떻게 보이는지 눈으로 확인</name>
  <what-built>
캐릭터 판이 자원 축을 여섯 가지 모양으로 그립니다 — 숫자, 게이지, 세그먼트 원,
가방 칸 격자, 표식 목록, 자원 주사위. 그리고 「이 룰북은 이 개념을 안 쓴다」로 선언된
축은 서버가 아예 안 보내서 화면에 흔적도 남지 않습니다.

이 저장소에는 화면을 자동으로 검사하는 틀이 없어서(그 작업은 Phase 16입니다),
여섯 모양이 실제로 사람 눈에 어떻게 보이는지는 자동 시험이 대신해 주지 못합니다.
  </what-built>
  <how-to-verify>
1. 서버와 화면을 띄웁니다 (평소 실험 세션 여는 방식 그대로).
2. 브람을 고르고 좌측 캐릭터 판을 봅니다 — 체력이 게이지로, 나머지 능력치가 부호 붙은
   숫자로 예전과 똑같이 보이는지 확인합니다. **숫자가 하나라도 예전과 다르면 그 자리에서
   멈추고 알려 주세요** (브람·나리 수치는 바뀌면 안 됩니다).
3. 축이 하나도 없는 캐릭터 판을 엽니다 — 회색 빈 상자가 아니라 「이 룰북은 세는 수치를
   쓰지 않아요」 한 줄이 보이는지 확인합니다. **빈 회색 패널이 보이면 실패입니다.**
4. 「안 쓴다」로 선언된 축이 있는 룰북으로 캐릭터 판을 엽니다 — 그 축의 이름이 화면
   어디에도 없어야 합니다. 회색으로 흐릿하게라도 보이면 실패입니다.
5. 가방 칸·표식 목록·자원 주사위 세 모양이 각각 읽을 만하게 보이는지, 사람을
   재촉하거나 몰아세우는 느낌(붉은 색, 경고 문구)이 없는지 봅니다.
  </how-to-verify>
  <resume-signal>「승인」이라고 답하거나, 어느 단계에서 무엇이 이상했는지 알려 주세요.</resume-signal>
</task>

</tasks>

<threat_model>
> ASVS Level 1 · 차단 기준: high 이상

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 시트 응답 → 브라우저 | 「안 쓴다」로 선언된 축의 존재가 네트워크로 새는지 여부가 걸린 자리 |
| 룰북 데이터의 문자열 → 화면 렌더 | `slot_values`·`tags`가 사람이 적은 문자열 그대로 화면에 들어가는 자리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Information Disclosure | `CharacterSheetView` 조립 | high | mitigate | `_visible_stats`가 `form == "none"` 축을 응답 조립 단계에서 제외하고, 응답 본문 문자열에 축 이름이 없다는 것을 시험이 단언한다 (Task 1) |
| T-11-10 | Tampering | `SlotGrid`/`TagList` 문자열 렌더 | medium | mitigate | 문자열을 React 자식 노드로만 넣고 `dangerouslySetInnerHTML`을 쓰지 않는다 — React 기본 이스케이프가 유일한 경로가 된다 (Task 2) |
| T-11-11 | Denial of Service | `SlotGrid` 칸 개수 | low | accept | 칸 개수는 룰북 선언(`slot_count`)이 정하고 지금 신뢰 경계는 저장소 안 소스다. 화면 상한은 두지 않는다 — M1의 룰북 업로드 시점에 재검토한다 |
| T-11-SC | Tampering | 패키지 설치 | low | mitigate | 이 계획은 npm 패키지를 새로 설치하지 않는다. 설치가 필요해지면 `package.json` 변경 전에 사람 확인 관문을 먼저 통과해야 한다 |
</threat_model>

<verification>
- `uv run pytest -q` 전체 초록
- `cd frontend && npx tsc --noEmit` 및 `npm run build` 종료 코드 0
- Task 3 사람 확인 관문 통과
</verification>

<success_criteria>
- 「안 쓴다」 선언이 서버 응답에서 사라진다(화면이 숨기는 것이 아니다)
- 여섯 형태가 전부 화면에 자기 모양을 갖는다
- 축이 없는 시트가 고장처럼 보이지 않는다
</success_criteria>

<output>
Create `.planning/phases/11-rulebook-vocabulary/11-03-SUMMARY.md` when done
</output>
