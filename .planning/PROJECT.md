# GPTRPG

## What This Is

TRPG를 잘 모르고 룰북을 읽을 시간도 없지만, 친구들과 실시간으로 몰입할 세계를 원하는 사람을 위한 온라인 플랫폼이다. AI가 이야기 진행자 노릇을 하고, 주사위와 판정은 사람이 검산할 수 있는 순수 코드가 처리한다. 특정 룰북 전용 시스템이 아니라 **룰북을 데이터로 받아들이는 범용 시스템**을 만든다.

## Core Value

**이야기가 어떻게 끝나는지 보고 싶어서 일주일 뒤에 다시 접속한다.**

위협 시계가 돌고 있고 아직 멈추지 않았다 — 이 제품이 팔아야 하는 감정은 그것 하나다. 성장·보상은 재방문 유인이 아니다.

---

## ⚠️ 지금 어디에 있나 — 실험을 멈추고 도구부터 고친다

2026-08-04에 실제로 사람 넷을 불러 첫 세션을 돌렸다. 결과는 "재미있다/없다"가 아니라
**"도구가 망가져서 잴 수가 없다"**였다. 세 가지가 동시에 터졌다 — AI가 네 명을 한 사람으로
인식했고, 참가자 한 명의 문장에 AI가 캐릭터를 이탈했고, 세션 후반에 진행자 지시문 전체가
서사에 그대로 쏟아졌다. 전수 감사 결과는 `docs/session1-code-review.md`에 있다.

**그래서 실험 게이트를 내리고 프로토타입 완성으로 방향을 바꿨다 (D-62).**

| 원래 M0 구성 | 지금 |
|---|---|
| RIG 8건 — 실험 도구 | **전부 검증 완료.** 다만 세션1이 이 도구들이 다인원에서 무너진다는 것을 보였다 |
| MEAS 6건 — 자동 계측 | 코드에 그대로 남긴다. 판정 입력이 아니라 **관찰값**으로 강등 |
| EXP 4건 — 참가자·세션 운영 | **보류.** 프로토타입 완성 후 재검토 |
| HYP 6건 — 가설 판정 게이트 | **보류.** 「멈춤 판정」은 로드맵에서 내린다 |

**킬 크리테리아를 버린 것이 아니라 시점을 옮긴 것이다.** 특히 원가 상한($1/인·시간)은
언젠가 답해야 하는 숫자이고, 계측 코드가 살아 있으므로 프로토타입을 돌릴 때마다 저절로
쌓인다. 사람마다 답이 갈리는 판정(재미·비경험자 진입·봐주기)만 뒤로 미뤘다.

지금 만드는 것은 **끝까지 돌아가는 프로토타입**이다. 화면이 투박한 것은 여전히 괜찮지만,
**세션 중간에 무너지는 것은 이제 괜찮지 않다.**

---

## Current Milestone: v1.1 돌아가는 프로토타입

**Goal:** 사람 실험 없이, 세션을 끝까지 돌려도 무너지지 않는 GPTRPG 코어를 만든다.

**Target features:**
- 신원 검증 — 누가 어떤 캐릭터로 무엇을 했는지 서버가 확인한다
- AI 출력 검증과 탈옥 방지 — 지시문이 새거나 진행자가 캐릭터를 이탈하지 않는다
- 능력치가 판정에 반영되고 캐릭터 상태가 플레이로 변하는가 — **결정부터 다시 한다** (D-20 재논의)
- 턴 처리 안정성 — 재시도가 주사위를 두 번 굴리지 않는다
- 기억 유지 — 관계 장부 주입과 문맥 압축기로 세션 후반에도 앞을 기억한다
- 위협 시계가 이야기에 반응한다 — 진행 규칙 3가지 병용 + 진행자 수동 경로
- 파티 전원의 상태가 AI에게 보인다
- 프론트엔드 재감사 — React 재작성 이후 감사된 적이 없다
- 회귀 방지 — 다인원·적대적 입력을 자동 테스트가 잡는다

**Out (보류):** 참가자 모집 · 세션2 진행 · 가설 6건 판정. 자동 집계 코드는 남긴다.

---

## Business Context

- **Customer**: TRPG를 잘 모르는 국내·국외 일반 유저. 친구 파티가 1순위, 낯선 사람 매칭은 나중(M2)
- **Revenue model**: 등급제 + 소프트 상한, 파티/솔로 요금 분리. 무제한 정액제는 성립하지 않는다(솔로가 파티보다 1인당 7~8배 비쌈). 무료 단위는 "첫 에피소드"(시계 1~2칸)
- **Success metric**: **1인 1시간당 원가.** $1 초과 + 저가 모델로도 안 내려가면 프로젝트를 멈춘다
- **Strategy notes**: `docs/GPTRPG-cost-analysis.md` (참고용 분석 — 결정의 출처가 아니다)

---

## Requirements

### Validated

- RIG-02, RIG-06 — Phase 1(규칙 코어와 사건 기록)에서 검증. 판정 계산은 순수 코드로만 일어나고(AI가 수치를 바꿀 지점 없음), 모든 사건이 순서대로 기록되어 상태는 그 기록에서만 재구성된다. `01-VERIFICATION.md` 참조.
- RIG-08, HYP-03 — Phase 2(룰북 두 개를 같은 그릇에)에서 검증. 판정 방식이 다른 두 룰북(2d6 등급식·d100 롤언더)과 그 적들이 플랫폼 코드를 고치지 않고 데이터로만 들어간다는 것을 실제 코드로 확인(`02-INTERFACE-CHANGES.md`가 고친 곳/참은 곳을 사후 대조). `02-VERIFICATION.md` 참조.
- RIG-01, RIG-03, MEAS-02, MEAS-04 — Phase 3(AI 진행자 한 턴)에서 검증. 자유 문장 → 분류 → 사람 확인 → 판정 → 문장 단위 서사까지 한 턴이 끝까지 도는 것을 다섯 제공자 프로토콜과 실제 NIM·OpenRouter 라이브 호출로 확인. 라이브 UAT에서 실제 버그 2건(제공자 헤더 인코딩, 서사 실패 시 크래시)을 발견·수정·재검증. `03-VERIFICATION.md` 참조.
- RIG-04, RIG-05, RIG-07, MEAS-01, MEAS-03 — Phase 4(실험 도구 완성)에서 검증. 네 명이 링크 하나로 같은 세션을 동시에 보고 재접속해도 이어붙는 것, 위협 시계·실패 카운터의 화면 표시와 3회 자동 진행, 캐릭터 읽기 전용 시트, 토큰·턴·시계 진행 자동 집계까지 실제 브라우저 두 탭 라이브 QA로 확인. QA 중 실제 버그 2건(CLI 프로세스 재시작 시 위협 시계 상태 유실, `confirm` 검증 실패 시 사건 기록에 짝 잃은 사건이 남는 문제)을 발견·수정·재검증. `04-VERIFICATION.md`·`04-REVIEW.md` 참조. **RIG-01~08 전부 검증 완료.**

### Active

현재 마일스톤 = **v1.1 돌아가는 프로토타입**. 항목별 상세는 `.planning/REQUIREMENTS.md`.

- [ ] **ARCH** — 에이전트 구조(D-17)를 다시 정한다 — 코드보다 먼저
- [ ] **TRUST** — 누가 무엇을 했는지 서버가 검증한다 (세션1 사고의 근본 구조)
- [ ] **SAFE** — AI가 내놓은 것을 검증 없이 화면에 뿌리지 않는다 (지시문 유출·탈옥)
- [ ] **RULE** — 능력치가 판정에 들어가고 캐릭터 상태가 플레이로 변한다 (D-20 재논의 선행)
- [ ] **MEM** — 세션 후반에도 AI가 앞을 기억한다 (관계 장부 · 문맥 압축기)
- [ ] **CLOCK** — 위협 시계가 이야기에 반응한다 (진행 규칙 3가지 병용 + 수동 경로)
- [ ] **FE** — React 재작성 이후 감사된 적 없는 프론트엔드를 감사하고 고친다
- [ ] **QUAL** — 감사에서 나온 나머지 항목 (경계값·예외 관례·계측 정확도)
- [ ] **TEST** — 다인원·적대적 입력을 자동 테스트가 잡는다

**보류 (프로토타입 완성 후 재검토):** EXP-01~04 · HYP-01~06. 계측 MEAS-01~06은 코드에
그대로 있으며 관찰값으로 계속 쌓인다.

### Out of Scope

**M0 실험에서 안 만드는 것** — 최소 도구 범위가 「안 필요」로 명시한 항목. 전부 손으로 대체한다.

- 캐릭터 만들기 화면 — 캐릭터는 손으로 준비하거나 구두 안내로 만든다
- 매칭·로비 — 링크 하나로 대체
- 코스메틱 — 실험이 재미를 재는 데 관여하지 않는다
- 룰북 저작 도구 — 룰북 데이터를 손으로 쓴다
- 리캡 자동 생성 — 손으로 써서 보여줘도 "이어할 수 있나"는 검증된다
- 안전 장치 UI — 실험에서는 구두로 대체
- 결제·계정 — 실험 참가자는 4명이다

**이번 마일스톤 밖으로 미룬 것**

- 공개방 매칭 · 톤 필터 · 신고·차단 · 성인 인증 — 낯선 사람을 받는 순간 같이 필요해지는 묶음이라 분리할 수 없다 (M2)
- 솔로 모드 · 비동기 플레이 — 아키텍처상 거의 공짜로 붙지만 먼저 만들 이유가 없다 (M2)
- 좌표계 · 전술 그리드 · 시야 계산 — 첫 타깃 룰북에 그리드가 없어 놓을 보드가 없다 (M4)
- D&D 5e — 가장 복잡한 것으로 엔진을 검증한다는 논리를 폐기했다 (M4)
- 파티 채팅 · 보이스 · 친구 시스템 — 플레이어 간 대화는 외부 메신저에서 끝난다
- 검색 기반 기억 주입 — 검색 품질이라는 실패 지점을 추가하면 "재미있나"를 잴 수 없다

---

## Context

### 타깃 유저와 전제

회사·학교가 끝나고 친구들과 실시간으로 몰입할 세계를 원하지만, 룰북을 읽을 시간은 없는 사람. 플레이 시간은 고정 세션이 아니라 하고 싶은 만큼이다.

전제 세 가지 — ① 플레이어끼리의 대화는 외부 메신저에서 일어난다(플랫폼은 소셜 레이어가 아니고 입력창은 행동 선언용) ② 언제든 끊고 이어할 수 있어야 한다 ③ 언젠가는 처음 보는 사람과의 플레이를 받는다.

해결하는 문제 — 룰북을 읽어야 시작할 수 있다(숫자를 서술로 번역) / 진행할 사람이 없다(AI가 진행, 판정은 코드가) / 언제 다시 모일지 못 정한다(세션 개념을 버린다) / 낯선 사람과 하기가 불안하다(안전 장치를 기본으로).

### 세 개의 층 — 모든 기능은 어느 층인지 밝힌다

| 층 | 무엇 | 예 |
|---|---|---|
| ① 플랫폼 기능 | 룰북과 무관하게 우리가 제공 | 안전 장치, 끊고 이어하기, 이벤트 기록 |
| ② 표현 어휘 | 룰북이 말하는 것을 담는 그릇 | 캐릭터 만들기 7가지 동작, 판정 방식, 효과 표현 |
| ③ 룰북 규칙 | 룰북 설계자가 정한 것 | 성장·죽음·장비·회복의 구체적 규칙 |

**③을 ①에 올리면 다른 룰북이 망가진다.** 기능을 제안할 때 층을 밝히는 것이 이 프로젝트의 규약이다.

**첫 타깃이 던전월드 계열이라는 이유로 그 룰북의 규칙을 플랫폼에 박지 않는다.** 우리가 표현하지 못하는 규칙은 "자동화율이 낮다"로 정직하게 표시하고 재량 판정으로 넘긴다.

### 용어 — TRPG 용어를 그대로 쓰지 않는다

| 이 프로젝트의 용어 | 뜻 |
|---|---|
| **위협 시계** | 플레이어가 막지 않으면 순서대로 일어날 나쁜 일들의 목록. 시나리오의 뼈대 |
| **시계 칸** | 위협 시계의 항목 하나. "우물물이 검게 변한다" 같은 사건 하나 |
| **파국** | 시계가 끝까지 갔을 때 벌어지는 일 |
| **판정** | 주사위를 굴려 성공·실패를 정하는 것 |
| **무브** | 판정이 발동하는 상황의 종류 |
| **GM 대응** | 플레이어가 실패했을 때 진행자가 할 수 있는 일의 목록 |
| **재량 판정** | 룰북에 없는 상황을 그 자리에서 판단하는 것. 정통 TRPG의 절반이 이것 |
| **판정 방식** | 주사위 판정 수학을 담은 코드. 룰북 계열마다 하나 |
| **효과 표현** | 주문·특성의 효과를 조합 가능한 원자 연산으로 적는 데이터 형식 |
| **자동화율** | 룰북의 판정 중 시스템이 자동 처리하는 비율. 남은 몫은 재량 판정 |

### 문서 권위 구조

`docs/GPTRPG-M0-decisions.md`가 **단일 권위**다. 모든 문서가 이 문서를 권위로 지목한다.

| 문서 | 성격 |
|---|---|
| `docs/GPTRPG-M0-decisions.md` | **권위. D1~D33 잠금** |
| `docs/GPTRPG-design-plan.md` | 유효한 기획 명세서 (v2) |
| `docs/GPTRPG-design-plan-v1-archive.md` | **폐기.** 이 문서의 내용을 살아 있는 명세로 읽으면 안 된다 |
| `docs/GPTRPG-cost-analysis.md` | 참고용 분석. 문서 스스로 "결정의 출처가 아니다"라고 적는다 |
| `docs/superpowers/specs/2026-07-30-m0-closeout-design.md` | 이미 반영된 변경 지시서. 독립 출처가 아니다 |

정리된 형태는 `.planning/intel/` 아래 있다 — `decisions.md`(D1~D33 전문) · `requirements.md` · `constraints.md`(A절 유효 / B절 폐기) · `context.md`.

### 아직 안 풀린 것

문서 정리 과정에서 자동으로 풀리지 않은 항목 3건과, 2026-08-05 코드 대조에서 새로 드러난 항목
3건이다. 앞의 셋은 `.planning/INGEST-CONFLICTS.md`, 뒤의 셋은 `docs/PIPELINE.md` §9가 상세다.

1. **폐기된 v1 기획서에 폐기 표기가 없다.** 파일명 말고는 단서가 없어서, 그 파일만 따로 열어 본 사람은 폐기된 설계를 유효한 명세로 읽는다. → 파일 머리에 상태 한 줄을 넣거나 이후 수집에서 제외한다.
2. **v1에만 있고 유지·폐기 진술이 어디에도 없는 구현 약속 2건.** ① 에이전트 응답 껍데기와 가벼운/중간 모델의 타임아웃·재시도 규칙 ② 연결 유지 신호 주기와 스트리밍 도중 재연결 시 이어붙이기 규칙. 응답 속도 결정이 무거운 모델의 15초 타임아웃만 확정했으므로 나머지는 비어 있다. → 실험 도구를 만들면서 최소한만 정하고, 제품 규격은 M1 착수 시 정한다.
3. **원가 수치의 계산 전제가 결정과 어긋난다.** 원가 문서는 「최근 대화 20턴」을 넣고 1인 1시간당 $0.30을 계산했는데, 확정된 규칙은 「초기값 10턴」이다. 10턴 기준으로 다시 계산한 문서가 하나도 없다. **이 숫자가 프로젝트를 멈출 수 있는 조건($1)의 입력값이다.** → 실험 실측으로 대체한다. **덧붙여 원가 문서 §1의 계산은 「캐시 읽기 14,000토큰」이 지배 항목인데, 지금 쓰는 제공자(OpenAI 호환)에는 `cache_control`을 전달할 방법이 없다** — 실측은 캐시 0% 상태일 수 있으므로 §1의 캐시 전제와 직접 비교되지 않는다.
4. **D-31이 「접지 않는다」고 적은 것이 애초에 주입되지 않는다.** 관계 장부(D-11)와 저장소 색인이 안전장치 절에는 「접지 않는 것」으로 명시되어 있는데 **매 턴 넣는 네 가지 안에는 둘 다 없다.** 캐스트 이름·상태값은 매 턴 들어가지만 「그 인물과 무슨 일이 있었는지」는 최근 10턴 밖으로 나가면 사라진다 — 3~4시간 세션에서 10턴은 20~30분이다. → M1-13.
5. **D-17이 정한 에이전트 넷 중 `context_summarizer`가 없다.** 그래서 D-31의 「턴당 토큰 예산 상한 + 오래된 대화 접기」가 미구현이고 지금은 그냥 10턴 자르기다. **M0 원가 실측은 「압축 없는 상태」의 값**임을 Phase 6이 알아야 한다. → M1-12.
6. **D-21의 진행 규칙 세 가지 중 ①만 구현되어 H2의 관측 지표가 죽었다.** 실패 3회마다 정확히 한 칸이 도므로 `failure_to_clock_ratio`가 항상 3.0이다. → 제안 D-60 + M1-09.

### D21과 D-47이 이번 실험의 시간 예산 안에서 양립하지 않는다

D21 규격은 「시계 하나 = 4~6칸 / **8~15시간**」이고 이번 실험은 2세션 합쳐 **6~8시간**이다.
즉 규격대로면 4칸을 다 돌지 못한다. 그런데 D-47은 「파국까지 도달하면 그것이 세션의 자연스러운
종료점」이라고 정했다. 게다가 실제 페이싱을 정하는 것은 이야기가 아니라 **주사위**다 —
파국까지 필요한 판정 횟수가 능력치에 따라 29~72회로 갈리고, 2세션 예상 판정 수(약 40회)가
정확히 그 경계선에 있다.

→ **D-61로 해소했다 (승인 2026-08-05).** 실험의 목표 범위를 「첫 에피소드(1~2칸)」로 잡고,
파국 도달을 목표나 성공 조건으로 삼지 않는다. 시계 데이터는 4칸 그대로 둔다 — 칸수를 2로
줄이면 파국 서사가 2칸에서 터져 이야기 자체가 바뀐다. 종료 판정 규칙은
`docs/experiment/hypothesis-scoring-rules.md` §9에 있다: 파국 도달 여부와 무관하게 예정
시간에 끝내고, 중간에서 끝난 것을 실패로 세지 않으며, EXP-03의 「완주 대 흐지부지」는 시계
도달 칸이 아니라 사람이 끝까지 있었는지로 판정한다.

부수 효과로 D21 표의 「1,000팀 $2,500~6,000」의 **입력값이 처음으로 존재한다** — 집계에
시계 칸별 누적 스냅샷(`episodes`)이 생겨 「1~2칸에 몇 시간·몇 토큰이 들었나」가 세션 합계에서
갈라진다.

---

## Constraints

- **실행 환경**: Python 백엔드 + TypeScript 프런트엔드 — 원본 문서가 정한 것이 아니라 이번 정리 과정에서 프로젝트 수준으로 새로 고른 것이다
- **아키텍처(되돌릴 수 없음)**: 모든 사건을 순서대로 기록하고 현재 상태는 그 기록에서 만든다 — 제자리 수정 방식으로 먼저 짜면 재접속 동기화·다시보기·비동기 참여·관전자 합류가 전부 나중에 불가능해진다. **M0에서 확정해야 하는 유일한 되돌릴 수 없는 결정**
- **아키텍처 불변 규칙 3개**: ① 규칙 계산은 시간을 모른다(타이머·접속 상태 개념 금지) ② 세션마다 쓰기 주체는 하나뿐이다 ③ 플레이 모드는 코드 분기가 아니라 설정값이다(새 모드 추가 = 표에 열 하나)
- **LLM 경계**: 주사위와 판정 계산에 AI가 끼어드는 지점을 만들지 않는다. AI는 닫힌 목록에서 고르는 일과 서술만 한다. 수치는 못 바꾼다
- **프롬프트 캐싱**: 조립 순서를 안정성 순서(영구 고정 → 세션 고정 → 턴마다 변함)로 짠다. 나중에 할 최적화가 아니라 처음부터 지켜야 하는 설계 제약이다 — 캐싱 유무가 원가를 3.7배 가른다
- **기억 주입**: 매 턴 넣는 것은 네 가지로 고정(현재 장면에 등장한 대상 / 내 캐릭터 상태 / 위협 시계 상태 / 최근 대화 10턴). AI가 저장소 전체를 훑는 경로는 만들지 않는다
- **응답 속도**: 문장 입력 → 행동 확인 표시 0.5초, 확인 → 서사 첫 글자 2초. 완결까지는 목표 없음. 첫 글자 5초 초과 시 진행 표시, 15초 초과 시 판정 결과를 먼저 내보내고 서사를 뒤이어 붙인다
- **모델**: M0은 최상급 모델 고정 — 여기서 재는 것은 원가가 아니라 **품질 상한선**이다. 최상급으로도 재미없으면 제품이 성립하지 않아 거기서 판단이 끝난다. 단 원가가 생존 가능한 범위인지는 답해야 한다
- **모델 교체 가능성**: 에이전트별로 모델·제공자를 설정값으로 만든다. 지금 넣으면 공짜고 나중에 넣으면 재작성이다
- **한국어**: 대부분의 모델 벤치마크가 영어 기준이라 참고가 안 된다. 한국어 서사 품질은 별도로 측정한다
- **M0 범위선**: 룰북 데이터의 필드 단위 규격 · 효과 원자 연산 최종 목록 · 적 상태값 묶음의 구체 형태 · 화면 배치와 컴포넌트는 **전부 M0 밖**이다

---

## Key Decisions

<decisions status="locked" authority="docs/GPTRPG-M0-decisions.md" count="33">

**D1~D33은 전부 잠금 결정이다.** `docs/GPTRPG-M0-decisions.md`가 단일 권위이며 전문은 그 문서와 `.planning/intel/decisions.md`에 있다. 아래는 한 줄 요약이다. 뒤집힌 항목은 지우지 않고 취소선 + 사유로 남긴다는 것이 이 프로젝트의 규약이다.

| # | 결정 | 상태 |
|---|---|---|
| D1 | 플레이 모델 = 실시간 동기 파티 먼저. 비동기·솔로는 같은 엔진 위에 나중에 | 잠금 |
| D2 | "세션"을 1급 개념에서 강등 — 캠페인이 진짜 게임, 세션은 접속 구간일 뿐 | 잠금 |
| D3 | 코어 = 사건 기록 + 세션당 단일 쓰기 주체 + 진행 정책 설정값. 되돌릴 수 없는 결정 | 잠금 |
| D4 | 첫 타깃 룰북 = 던전월드 계열. D&D 5e는 M4로 이동 | 잠금 |
| D5 | 판정 방식은 코드, 룰북은 데이터. 룰북별 전용 어댑터 폐기 | 잠금 |
| D6 | 판정 방식 6종 로드맵 — 2d6 등급식·d100 롤언더가 M0, d20이 M1 | 잠금 |
| D7 | 효과 표현(원자 연산 30~50개) 도입 + 자동화율 지표 | 잠금 |
| D8 | 시나리오는 룰북에 종속되지 않는 독립 계층 | 잠금 |
| D9 | 시나리오 형식 4종 — 위협 시계가 M0, 노드 그래프·클루 웹이 M1, 테이블 생성이 M2 | 잠금 |
| D10 | 조율은 외부 메신저에서 끝난다 — 발언권 락·순서 강제·짧은 타이머 폐기 | 잠금 |
| D11 | 이어하기(리캡)를 1급 기능으로 승격. 검증 기준 = "3주 뒤에 읽고 이어할 수 있는가" | 잠금 |
| D12 | 목표 재정의 — 완벽 방어가 아니라 투명성. 틀렸을 때 알아채고 고칠 수 있으면 된다 | 잠금 |
| D13 | 양보 불가 = 결과의 무게 / 플레이어 주체성 / 불확실성 / 공동 창작. 룰 정합성은 대체 가능, 전술적 깊이는 M4 | 잠금 |
| D14 | 주사위는 순수 코드 — AI 관여 지점 없음. 증명 가능한 무작위성은 제품 약속으로 유지 | 잠금 |
| D15 | 이탈 행동의 대가는 두 층 — 서사적 대가는 막지 않고 세계가 반작용, 사회적 침해는 하드 블록 | 잠금 |
| D16 | 재량 판정 계층 = 닫힌 목록 분류 + 플레이어 확인. 신뢰도 임계값 폐기 | 잠금 |
| D17 | 에이전트 10개 → 4개로 축소. 메시지 버스는 인프로세스 함수 호출로 충분 | 잠금 |
| D18 | M0은 최상급 모델 고정 — 품질 상한선을 잰다. 이후 상황별 이원화 | 잠금 |
| D19 | 에이전트별 모델 분리 + 제공자 추상화 계층을 M0에 넣는다. 자체 호스팅은 M0 결정 아님 | 잠금 |
| D20 | 등급제 + 소프트 상한 & 파티/솔로 분리. 무료 티어에 시간 카운터를 만들지 않는다 | 잠금 |
| D21 | 위협 시계 = 나쁜 일 목록 + 순서. 진행 규칙 3가지 병용(실패 카운터 / 조건 / AI 선택). 4~6칸 8~15시간 | 잠금 |
| D22 | 캐릭터 만들기 = 7가지 동작의 조합 + 애착 장치 6개. 30초 완성품 경로는 폐기 | 잠금 |
| D23 | 안전 장치 4개 층. 정지 카드의 세 원칙(익명·설명 요구 없음·즉시)은 절대. 1~3층 M1, 4층 M2 | 잠금 |
| D24 | M1은 친구 초대 링크만. 공개방 매칭은 M2 이후. AI가 남의 캐릭터를 조종하지 않는다 | 잠금 |
| D25 | 캐릭터 라이프사이클은 유형 목록으로 — 유형은 코드, 어느 유형인지는 룰북이 선언 | 잠금 |
| D26 | 룰북 원문을 버리지 않는다 — 원문 + 구조화 데이터 + 연결. 점진적 구조화 | 잠금 |
| D27 | 룰북 파일 목차 8항목 확정. 판정 방식 2개로 인터페이스 검증 통과 | 잠금 |
| D28 | 자유 텍스트 해석은 정확도가 아니라 **마찰**로 측정한다. 정답 데이터 300개 | 잠금 |
| D29 | 라이선스는 플랫폼 정책이며 법적 결론이 아니다. 유저 룰북 수익화는 자작 오리지널 한정 허용 | 잠금 |
| D30 | **가설 6개 + 멈출 조건 3등급 + 실험 설계 + 최소 도구 범위.** 이 로드맵의 뼈대 | 잠금 |
| D31 | 매 턴 넣는 것은 네 가지로 고정. 저장소 상시 주입 폐기 | 잠금 |
| D32 | 적과 NPC의 숫자는 룰북이 선언한다. 플랫폼 코드에 체력·피해 같은 개념을 넣지 않는다 | 잠금 |
| D33 | 응답 속도 목표 두 지점(0.5초 / 2초) + 초과 시 행동. "전체 3초" 폐기 | 잠금 |

</decisions>

<decisions status="open" authority="이번 정리 과정">

원본 문서가 정하지 않았고 이번 정리에서 새로 정한 것.

| 결정 | 사유 | 출처 | 상태 |
|---|---|---|---|
| 실행 환경 = Python 백엔드 + TypeScript 프런트엔드 | 사용자가 직접 선택. 브라우저 안에서 규칙 코어를 직접 돌리는 시나리오는 포기했다 | **어떤 원본 문서에도 없다.** 문서가 정한 것이 아니라 프로젝트 수준의 새 결정이다 | 확정 (Phase 1, D-02) |

</decisions>

<decisions status="locked" authority="docs/experiment/hypothesis-scoring-rules.md">

**D30(잠금)의 적용 범위를 좁히는 결정 2건 + 실험 범위 재설정 1건 — 사용자 승인 2026-08-05.**

D30을 뒤집는 것이 아니라 **적용 범위를 제한하는** 것이므로 취소선을 쓰지 않는다. 문턱값(95% /
30% / $1 / 3회)은 한 글자도 바꾸지 않았다 — 바뀐 것은 **무엇을 그 문턱에 넣는가**다.

| # | 결정 | 근거 | 그래서 바뀐 것 |
|---|---|---|---|
| **D-59** | 이번 두 세션으로 **H1(재미)의 킬 판정을 내리지 않는다.** 탐색으로 격하하고 조건을 갖춘 재실험에서 판정한다 | 교란 셋이 서로 다른 방향으로 작동한다 — 간격 1~3일(D-57 이탈) / 무료 티어 모델(D-58, D18 취지 미충족) / 오너가 참가자 겸 관찰자라 **실질 표본 3명**(D-55·D-56의 귀결, 어디에도 적혀 있지 않던 사실). 결과가 어느 쪽으로 나와도 D30의 규칙을 적용할 수 없다 | **Phase 6 게이트가 「통과」를 낼 수 없다** — M1 착수 전에 재실험이 들어간다. 로드맵 게이트 표에 「유보」 행이 생겼다. 판정 조건 넷은 채점 규칙 §2 |
| **D-60** | **H2의 판정 근거를 `failure_to_clock_ratio`에서 관찰 기록으로 옮긴다** | D-21의 진행 규칙 세 가지 중 ①실패 누적만 구현되어 비율이 항상 문턱값(3)에 고정된다 — 정보량이 0이다. D-21이 「①이 관측 지표를 공짜로 만든다」고 적은 것은 ③이 함께 있을 때의 이야기였다 | MEAS-03의 자동 집계는 사실 기록으로만 남고 판정 입력에서 빠진다. 시계 진행마다 「이어졌다/억지였다」를 그 자리에서 적는다. ②③ 구현은 M1-09 |
| **D-61** | **실험의 목표 범위를 시계 4칸 전체가 아니라 D20·D21이 확정한 상품 단위 「첫 에피소드(1~2칸)」로 잡는다.** 시계 데이터 자체는 4칸 그대로 두고, 파국 도달을 목표나 성공 조건으로 삼지 않는다 | D21 규격(4칸 = 8~15시간)과 이번 시간 예산(6~8시간)이 안 맞고 D-47(파국 도달이 종료점)과도 어긋난다. 게다가 페이싱을 정하는 것이 이야기가 아니라 주사위다 — 파국까지 필요한 판정이 능력치에 따라 29~72회로 갈려 2세션 예상치(약 40회)가 경계선이다. 1~2칸으로 잡으면 문서 자신이 확정한 상품·과금 단위와 일치하고, 「시계가 남아 있는 상태로 끝나는 것이 구매 동기」라는 주장을 실험으로 확인하게 된다 | 시계 칸수·시나리오 데이터는 **안 바뀐다**(4칸 유지). 바뀐 것은 ① 세션 종료 규칙(파국까지 밀어붙이지 않는다) ② EXP-03의 완주 판정이 시계 도달 칸과 무관해진다 ③ 집계에 시계 칸별 누적 스냅샷(`episodes`)이 생겨 「무료로 주는 1~2칸이 얼마짜리인가」가 실측으로 나온다 — D21 표의 「1,000팀 $2,500~6,000」의 입력값이 이제 존재한다 |

</decisions>

<decisions status="locked" authority="v1.1 마일스톤 착수 — 사용자 승인 2026-08-05">

**세션1(2026-08-04) 실전 결과에 따른 방향 전환 1건 + 재개 결정 1건.**

| # | 결정 | 근거 | 그래서 바뀐 것 |
|---|---|---|---|
| **D-62** | **킬 크리테리아 실험을 보류하고 프로토타입 완성으로 방향을 바꾼다.** EXP-01~04와 HYP-01~06을 로드맵에서 내리고, Phase 5·6(세션2 진행·가설 판정 게이트)을 실행하지 않는다. 자동 계측(MEAS)은 코드에 그대로 두고 **판정 입력이 아니라 관찰값**으로 강등한다 | 세션1이 실제로 답한 것은 「재미있나」가 아니라 「도구가 망가져서 잴 수가 없다」였다. 네 명이 한 사람으로 뭉뚱그려지고, 진행자가 캐릭터를 이탈하고, 지시문 전체가 서사로 새는 상태에서 나온 어떤 반응도 AI 진행의 품질에 대한 근거가 아니다. D-59가 이미 「이번 세션으로 H1을 판정할 수 없다」고 인정했으므로, 도구를 고치지 않은 채 세션2를 돌리는 것은 오염된 표본을 하나 더 만드는 일이다 | **로드맵에서 「멈춤 판정」이 사라진다.** 폐기가 아니라 보류다 — 프로토타입이 완성되면 조건을 갖춘 실험을 다시 설계해 넣는다. **원가 상한($1/인·시간)은 여전히 답해야 하는 숫자**이며, 계측 코드가 살아 있으므로 프로토타입을 돌릴 때마다 저절로 쌓인다 |
| **D-63** | **M1-09 · M1-12 · M1-13을 M1에서 v1.1로 당긴다** | 셋 다 「세션이 끝까지 돌아가는가」에 직접 걸린다. M1-13(관계 장부)과 M1-12(문맥 압축기)가 없으면 3~4시간 세션의 후반부에 AI가 앞을 기억하지 못하고 — 최근 10턴은 20~30분 분량이다 — M1-09가 없으면 위협 시계가 실패 3회에만 반응해 이야기와 무관하게 기계적으로 돈다. 「돌아가는 프로토타입」의 정의에 이 셋이 포함된다 | M1 범위가 그만큼 줄어든다. M1은 초대 링크·d20·시나리오 형식·룰북 업로드·안전 장치 1~3층 중심으로 남는다 |

**재논의 예고 — v1.1의 첫 단계는 코드가 아니라 결정이다.** 잠금 결정을 코드가 조용히
우회하는 경로를 만들지 않는다는 것이 이 프로젝트의 규약이므로, 세 가지를 먼저 다시 연다.

| 결정 | 왜 다시 여는가 |
|---|---|
| **D-20** 캐릭터 시트는 4칸, 쓰기 경로 없음 | C4(능력치가 판정에 안 들어가고 캐릭터 상태가 안 변함)를 고치려면 이 결정을 우회할 수밖에 없다 |
| **D-31** 매 턴 넣는 것은 네 가지로 고정 | 관계 기록을 다섯 번째 칸으로 열지, 기존 칸에 흡수시킬지가 정해져야 기억 유지 코드를 짤 수 있다 |
| **D-17** 에이전트 10개 → 4개 | 원 설계는 서브에이전트 10개가 각자 상황·룰북·아이템을 판단해 진행자에게 넘기는 구조였고, D-17이 응답 속도·원가를 근거로 4개로 줄였다(코드에는 2개). **문제는 축소의 대가로 하기로 한 정형화가 거의 비어 있다는 것이다** — 효과 표현(D7)은 코드에 흔적 0건, 시나리오 형식은 플러그인이 아니라 특정 시나리오 하나가 상수로 박혀 있고, 룰북은 파일이 아니라 파이썬 모듈이다. 판정 커널 2종만 설계대로 왔다. 「줄인 몫을 누가 받았는가」에 지금 답이 없다 |

**효과 표현(D7)의 첫 조각은 v1.1에서 시작한다.** 원자 연산 30~50개 목록 전체를 만드는 것은
이번 범위가 아니지만, 「판정 결과가 자원을 얼마나 바꾸는가」를 룰북이 선언하는 형식이
곧 그 원자 연산의 첫 두 칸이다 — 지금 아무렇게나 정하면 나중에 재작성이고, 자랄 모양으로
정하면 공짜다 (RULE-09).

</decisions>

---

## Evolution

**각 단계가 끝날 때마다**
1. 무효화된 요구사항 → Out of Scope로 사유와 함께 이동
2. 검증된 요구사항 → Validated로 단계 번호와 함께 이동
3. 새로 나온 요구사항 → Active에 추가
4. 새 결정 → Key Decisions에 추가 (D1~D33은 잠금이므로 뒤집을 때 취소선 + 사유)
5. "What This Is"가 현실과 어긋나면 갱신

**v1.1이 끝나면**
- 이 문서의 「지금 어디에 있나」 절을 다시 쓴다
- 프로토타입을 실제로 돌려 나온 원가 실측을 Business Context에 반영한다
- **보류한 실험(EXP·HYP)을 다시 꺼내 재설계할지 결정한다** — 보류는 폐기가 아니다

---
*Last updated: 2026-08-05 — 마일스톤 v1.1(돌아가는 프로토타입) 착수. 세션1(2026-08-04) 실전 결과로 킬 크리테리아 실험을 보류하고(D-62) 코어 완성으로 방향 전환. M1-09·M1-12·M1-13을 v1.1로 당김(D-63). C4 대응을 위한 D-20 재논의가 v1.1의 첫 단계*
