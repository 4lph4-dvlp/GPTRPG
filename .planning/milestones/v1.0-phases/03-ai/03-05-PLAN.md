---
phase: 03-ai
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gptrpg/agents/providers/openrouter_provider.py
  - tests/test_providers.py
autonomous: false
requirements: [RIG-01, RIG-03]
gap_closure: true
gap_ids: [G-03-1]

user_setup:
  - service: openrouter
    why: "이 계획의 사람 확인 단계가 실제 키로 모델 목록이 돌아오는지를 눈으로 본다 — 합성 이중체로는 헤더 인코딩 실패가 재현되지 않는다(SDK 클라이언트 생성자를 가짜로 바꾸면 httpx 인코딩 자체가 일어나지 않는다)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "openrouter.ai -> Keys (사용자가 UAT 1번 시험에서 이미 쓴 키 그대로)"

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "OpenRouter로 나가는 모든 HTTP 요청이 헤더 인코딩 오류 없이 나간다 — 모델 목록 조회뿐 아니라 분류(RIG-01)·서사(RIG-03) 호출도 같은 클라이언트를 타므로 함께 살아난다"
    - "실제 OPENROUTER_API_KEY로 `agents select`를 돌리면 모델 목록이 번호와 함께 화면에 뜬다 — `모델 목록 조회 실패` 줄이 나오지 않는다"
    - "귀속 헤더 두 칸(HTTP-Referer/X-Title)이 여전히 붙어 나간다 — 헤더를 지우는 방식으로 고치지 않았다"
    - "어떤 제공자 어댑터든 클라이언트에 헤더를 실어 보내면, 그 헤더의 키와 값이 ASCII로 인코딩 가능한지 자동 시험이 지킨다 — 여섯 번째 어댑터가 같은 실수를 되풀이하면 시험이 잡는다"
  artifacts:
    - src/gptrpg/agents/providers/openrouter_provider.py
    - tests/test_providers.py
  key_links:
    - "`_ATTRIBUTION_HEADERS` -> `OpenAIProvider(default_headers=...)` -> `openai.OpenAI(default_headers=...)` -> httpx 클라이언트의 **모든** 요청 — 이 사슬이 클라이언트 수준이라 헤더 하나가 깨지면 그 제공자의 모든 호출이 죽는다. 모델 목록만의 문제가 아니다"
    - "UAT 1번 시험의 실제 키 재실행 -> RESEARCH.md 가정 A4(모델 목록이 실제로 돌아온다)와 Open Question 1(귀속 헤더가 필수인가)의 실증 — 자동 시험은 이 둘 중 어느 것도 증명하지 못한다"
---

<objective>
OpenRouter 어댑터가 귀속 헤더에 한글 문자열을 실어 보내 그 제공자로 가는 **모든** 요청이 `UnicodeEncodeError`로 죽던 것을 고친다. UAT 1번 시험의 blocker(G-03-1)를 닫는다.

Purpose: HTTP 헤더 값은 ASCII만 허용되고, 이 헤더는 요청 하나가 아니라 `openai.OpenAI` 클라이언트 생성 시점에 `default_headers`로 박혀 그 제공자의 모든 요청에 붙는다. 즉 지금 상태의 OpenRouter는 모델 목록만 못 부르는 게 아니라 분류(RIG-01)도 서사(RIG-03)도 한 번도 성공할 수 없다 — 다섯 제공자 중 하나가 통째로 죽어 있다. 자동 시험 307개가 전부 통과하면서도 이걸 못 잡은 이유는 시험이 SDK 클라이언트 생성자를 가짜로 바꿔치기해 httpx의 인코딩 단계 자체를 건너뛰기 때문이다 — 그래서 이 계획은 코드 수정 + 인코딩 가능성 자체를 단언하는 시험 + 실제 키 재실행 셋을 함께 낸다.

Output: ASCII 귀속 헤더, 헤더 인코딩 가능성 회귀 시험, 실제 키로 확인된 모델 목록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-ai/03-UAT.md
@.planning/phases/03-ai/03-02-SUMMARY.md
@src/gptrpg/agents/providers/openrouter_provider.py
@src/gptrpg/agents/providers/openai_provider.py
@tests/test_providers.py
</context>

<gap_coverage>
G-03-1의 `missing` 두 항목이 어느 과업에 들어갔는지:

| missing 항목 | 담당 |
|---|---|
| X-Title 값을 ASCII로 교체하거나 RFC 8187 percent-encoding으로 인코딩 | Task 1 (ASCII 교체를 고름 — 아래 이유) |
| 회귀 테스트: 헤더 값이 ASCII로 인코딩 가능함을 보장하는 단위 테스트 추가 | Task 1 |
| *(gap의 truth 자체 — "실제 키로 정상 동작한다")* | Task 2 (사람 확인) |

**두 선택지 중 ASCII 교체를 고른 이유:** RFC 8187 percent-encoding은 `Content-Disposition`의 `filename*`처럼 **별표 접미사 확장 필드**를 받는 자리에서만 서버가 되돌려 해석한다. `X-Title`은 OpenRouter가 자기 대시보드·앱 순위에 그대로 찍는 평범한 자유 문자열 헤더라 확장 필드 문법을 해석하지 않는다 — percent-encoding해서 보내면 요청은 통과하지만 그쪽 화면에 `%EC%8B%A4%ED%97%98...`이 글자 그대로 박힌다. 사람이 읽을 앱 이름 자리에 깨진 문자열을 남기는 것이므로 ASCII 문자열로 바꾸는 쪽이 맞다.
</gap_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 귀속 헤더를 ASCII로 바꾸고, 헤더 인코딩 가능성 자체를 시험으로 못 박는다</name>
  <files>src/gptrpg/agents/providers/openrouter_provider.py, tests/test_providers.py</files>
  <read_first>
    - `src/gptrpg/agents/providers/openrouter_provider.py` — `_ATTRIBUTION_HEADERS` 상수와 그 위 도크스트링(귀속 헤더를 왜 항상 붙이는지에 대한 방어적 결정이 적혀 있다)
    - `src/gptrpg/agents/providers/openai_provider.py` 47~65번째 줄 — `default_headers`가 `openai.OpenAI` 생성자에 그대로 넘어가 클라이언트 수준에 박히는 자리
    - `tests/test_providers.py`의 `_FakeOpenAIClient`, `_no_network` 픽스처, `test_openrouter_sends_attribution_headers`, `test_openai_compatible_clients_disable_sdk_level_retries` — 새 시험이 그대로 물려받을 기존 관례(특히 `getattr(provider, "_delegate", provider)._client`로 위임 어댑터와 직접 어댑터를 함께 다루는 모양)
  </read_first>
  <behavior>
    - 시험 1: `_ATTRIBUTION_HEADERS`의 모든 키와 모든 값이 `.encode("ascii")`를 예외 없이 통과한다. 지금은 `X-Title` 값에서 `UnicodeEncodeError('ascii' codec can't encode characters in position 10-11)`가 난다 — 고치기 전에는 반드시 실패해야 하는 시험이다.
    - 시험 2: 어댑터가 **실제로 클라이언트에 넘긴** 헤더 사전(모듈 상수를 다시 읽는 것이 아니라 `OpenRouterProvider(...)._delegate._client.default_headers`)의 모든 키·값이 ASCII로 인코딩된다. 상수만 고치고 넘기는 경로가 따로 생겨도 이 시험이 잡는다.
    - 시험 3: 다섯 어댑터 전부에 대해, 클라이언트가 헤더 사전을 갖고 있으면(없는 어댑터는 건너뛴다) 그 키·값이 ASCII로 인코딩된다. `PROVIDER_FACTORIES`를 돌면서 `getattr(provider, "_delegate", provider)`로 클라이언트를 찾고 `default_headers`가 `None`이 아닐 때만 단언한다 — 여섯 번째 어댑터가 헤더를 붙이면 자동으로 이 그물에 걸린다.
    - 시험 4: 귀속 헤더 두 칸의 이름(`HTTP-Referer`, `X-Title`)이 여전히 존재하고 값이 빈 문자열이 아니다 — 헤더를 지우는 방식의 "수정"에 대한 회귀 방지. 기존 `test_openrouter_sends_attribution_headers`를 이 단언까지 하도록 넓히면 된다.
  </behavior>
  <action>
    `_ATTRIBUTION_HEADERS`의 `X-Title` 값을 ASCII 문자열로 바꾼다. 값은 이 도구를 가리키는 고정된 영문 이름 하나로 정한다(예: 도구 이름 + 마일스톤 표시). `HTTP-Referer` 값은 이미 ASCII라 손대지 않는다. 헤더 자체를 지우거나 조건부로 붙이는 식으로 바꾸지 않는다 — 이 헤더를 항상 붙이는 것은 조사 단계 미해결 질문에 대한 의도된 방어적 선택이고(파일 도크스트링에 근거가 적혀 있다), 이번 gap은 "붙일지 말지"가 아니라 "어떤 바이트로 붙일지"의 문제다.

    `_ATTRIBUTION_HEADERS` 바로 아래 설명 문장에 **왜 ASCII여야 하는지**를 한 줄로 남긴다: HTTP 헤더 값은 ASCII만 허용되고, 이 사전은 요청 하나가 아니라 `openai.OpenAI` 클라이언트에 `default_headers`로 박혀 이 제공자의 모든 요청에 붙으므로, 여기에 비-ASCII 한 글자가 들어가면 모델 목록·분류·서사가 한꺼번에 죽는다. 사용자에게 보일 한글 이름이 필요하면 헤더가 아닌 다른 자리(화면 출력)에 두어야 한다.

    파일 맨 위 도크스트링의 귀속 헤더 문단에도 이번에 실측된 사실을 덧붙인다 — 03-UAT 1번 시험에서 실제 키로 부르자 `UnicodeEncodeError`가 났고, 원인은 값의 한글이었으며, 이 파일의 시험 이중체(`_FakeOpenAIClient`)는 httpx의 인코딩 단계를 거치지 않아 이 실패를 재현하지 못했다는 것. 다음에 이 파일을 읽는 사람이 "시험이 통과하니 헤더는 안전하다"고 오해하지 않게 하는 것이 목적이다.

    `tests/test_providers.py`에 위 `<behavior>`의 네 시험을 넣는다. 시험 3은 `PROVIDER_FACTORIES`를 돌되 `_no_network` 픽스처가 이미 다섯 SDK 생성자를 전부 가로채고 있으므로 추가 가짜를 만들 필요가 없다. 인코딩 실패를 사람이 읽을 수 있게 하기 위해, 단언이 깨졌을 때 어느 제공자의 어느 헤더 이름인지가 메시지에 나오도록 한다.

    ASCII 검사에 `str.isascii()`가 아니라 `.encode("ascii")`를 쓴다 — 실패했을 때 어느 위치의 어떤 글자가 문제인지가 예외 메시지에 그대로 나와서, UAT에 보고된 오류 문장과 같은 모양으로 읽힌다.
  </action>
  <verify>
    <automated>cd /home/alpha-pi/GPTRPG &amp;&amp; uv run pytest tests/test_providers.py -q &amp;&amp; uv run ruff check src tests &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    `_ATTRIBUTION_HEADERS`의 모든 키·값이 ASCII로 인코딩되고, 귀속 헤더 두 칸은 여전히 붙어 나가며, 다섯 어댑터 어느 것이든 헤더를 실어 보내면 ASCII 여부를 시험이 검사한다. 307개 기존 시험이 그대로 통과한다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실제 OpenRouter 키로 모델 목록이 돌아오는지 사람이 확인한다 (UAT 1번 재실행)</name>
  <precondition>`OPENROUTER_API_KEY` 환경 변수에 실제 OpenRouter 키가 들어 있다 — UAT 1번 시험에서 쓴 그 키 그대로. 비어 있으면 `agents select`의 제공자 목록에 `openrouter`가 아예 나타나지 않으므로 이 확인을 진행할 수 없다.</precondition>
  <what-built>
    OpenRouter 귀속 헤더의 `X-Title` 값을 ASCII 문자열로 바꿨다. 헤더 값이 ASCII로 인코딩 가능한지 자동 시험이 지킨다. 다만 자동 시험은 SDK 클라이언트 생성자를 가짜로 바꿔치기하므로 httpx의 실제 인코딩 단계를 타지 않는다 — 그래서 UAT 1번 시험을 실제 키로 한 번 더 돌려야 이 gap이 진짜로 닫힌다.
  </what-built>
  <how-to-verify>
    1. `OPENROUTER_API_KEY`가 설정돼 있는지 확인한다.
    2. 저장소 최상위에서 실행:
       `uv run gptrpg agents select --role action_classifier --config /tmp/openrouter-check.json`
       (`--config`로 버릴 임시 경로를 주는 이유: 지금 쓰는 NIM 설정 파일 `.gptrpg/agents.json`을 덮어쓰지 않기 위해서다. 이 확인은 설정을 바꾸는 것이 목적이 아니다.)
    3. 제공자 목록이 뜨면 `openrouter` 옆의 번호를 입력한다.
    4. **기대하는 결과:** 모델 이름이 번호와 함께 줄줄이 뜬다. `오류: 'openrouter' 모델 목록 조회 실패 — ...` 줄이 나오지 않는다.
    5. 아무 모델이나 하나 골라 번호를 입력하면 `저장됨: /tmp/openrouter-check.json`이 찍히고 끝난다.
    6. 확인이 끝나면 임시 파일을 지운다: `rm -f /tmp/openrouter-check.json`

    **다르게 실패하면 그 한 줄을 그대로 알려 주세요.** 특히 `401`/`403`(키 문제 — 코드 문제가 아님)과 그 외 오류를 구분해야 한다. 인코딩 오류 문구(`ascii codec can't encode`)가 다시 나오면 수정이 덜 된 것이다.
  </how-to-verify>
  <resume-signal>모델 목록이 떴으면 "approved". 다른 결과가 나오면 화면에 찍힌 오류 한 줄을 그대로 붙여 주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이 도구 → OpenRouter (제3자 API) | 요청 헤더·프롬프트가 우리 프로세스를 떠나 외부 사업자에게 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information disclosure | `_ATTRIBUTION_HEADERS` | low | accept | 귀속 헤더는 이 도구를 가리키는 **의도된 공개 식별자**다(저장소 주소 + 앱 이름). API 키·세션 식별자·플레이어 문장은 들어가지 않으며, 새 값도 고정 문자열이라 호출마다 달라지지 않는다 — 사용자·세션을 식별하는 정보가 늘어나지 않는다 |
| T-03-05-02 | Information disclosure | `_select_one_role`의 오류 출력 (`main.py` 186~189번째 줄) | low | mitigate | 이번 사람 확인이 실패했을 때 화면에 찍히는 예외 문장을 그대로 붙여 넣게 되어 있으므로, 붙여 넣기 전에 키 문자열이 섞여 있지 않은지 본다. 기존 관례(`MissingApiKey` 메시지에 키 값을 넣지 않는다, `test_missing_api_key_message_never_contains_key_value`)를 이 계획에서 약화시키지 않는다 |
| T-03-05-SC | Tampering | npm/pip/cargo installs | low | accept | 이 계획은 새 패키지를 하나도 설치하지 않는다 — 기존 `openai` SDK 경유 호출만 고친다. 공급망 확인 대상 없음 |
</threat_model>

<verification>
- `uv run pytest tests/test_providers.py -q` — 새 인코딩 시험 4개 포함 통과
- `uv run pytest -q` — 기존 307개가 하나도 깨지지 않는다
- `uv run ruff check src tests` / `uv run lint-imports` — 통과
- 사람 확인: 실제 키로 `agents select`의 OpenRouter 모델 목록이 화면에 뜬다
</verification>

<success_criteria>
- G-03-1이 닫힌다: 실제 OPENROUTER_API_KEY로 모델 목록이 오류 없이 조회된다
- 귀속 헤더 두 칸이 살아 있고 값은 전부 ASCII다
- 헤더 인코딩 회귀 시험이 다섯 어댑터 전부를 그물로 덮는다
- SUMMARY에 RESEARCH.md 가정 A4(모델 목록이 실제로 돌아온다)의 실증 결과와, Open Question 1(귀속 헤더가 필수인지)에 대해 이번 실행에서 알아낸 것을 한 줄로 남긴다
</success_criteria>

<output>
Create `.planning/phases/03-ai/03-05-SUMMARY.md` when done
</output>
