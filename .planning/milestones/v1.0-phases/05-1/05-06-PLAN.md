---
phase: 05-1
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - docs/experiment/session-2-log.md
  - docs/experiment/experiment-results.md
autonomous: false
requirements: [EXP-02, EXP-03, MEAS-05]
user_setup:
  - service: openrouter
    why: "2세션 3~4시간 동안 행동 분류기·AI 진행자를 돌린다 (D-58)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "저장소 최상위 `.env.local`"
    dashboard_config:
      - task: "요금제·한도 정책이 1주 사이에 바뀌지 않았는지 2세션 직전에 한 번 더 확인 (스냅샷은 2026-08-03 기준)"
        location: "openrouter.ai/docs/api-reference/limits"
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "1세션으로부터 정확히 1주 뒤에 2세션이 진행되었다 (EXP-03, D-57)"
    - "2세션에 몇 명이 왔는지가 기록되었다 — 이것이 H1(재미)의 가장 중요한 입력값이다 (EXP-03)"
    - "세션을 완주했는지 흐지부지됐는지가 관찰자의 판단과 그 근거 한 줄로 기록되었다 (EXP-03, D-53)"
    - "참가자들이 손으로 쓴 리캡만 읽고 2세션을 이어 시작할 수 있었는지가 기록되었다 — 추가 설명이 필요했다면 무엇이 빠졌는지까지 (MEAS-05)"
    - "Phase 6이 여섯 가설을 채점할 때 열어 볼 한 장(`experiment-results.md`)이 존재하고, 두 세션의 숫자와 집계 파일 경로가 전부 그 안에서 가리켜진다"
    - "무료 티어 모델을 썼다는 캐비어트가 결과 문서에 남아 있다 — Phase 6이 H1 판정에 유보를 붙일 수 있다 (D-58)"
    - statement: "`session-1-log.md`와 `session-2-log.md`의 세션 날짜 차이가 정확히 7일이다"
      verification: backstop
    - statement: "`experiment-results.md`가 여섯 가설 각각(H1~H6)에 대해 이번 실험이 확보한 입력값이 무엇인지를 한 줄씩 가리킨다 — 판정은 하지 않고 재료만 모은다"
      verification: backstop
  artifacts:
    - docs/experiment/session-2-log.md
    - docs/experiment/experiment-results.md
  key_links:
    - "`docs/experiment/session-1-recap.md`(05-05) → 2세션 시작 → `session-2-log.md`의 MEAS-05 판정 칸 — 리캡이 실제로 작동했는지가 여기서만 드러난다"
    - "`session-1-log.md` + `session-2-log.md` + 집계 JSON 두 벌 → `experiment-results.md` → Phase 6 게이트 — 실험이 만든 모든 숫자가 한 장으로 모이는 지점"
    - "`docs/experiment/session-prep.md`의 두 세션 날짜(05-04) → 실제 두 세션 날짜 — 1주 간격이 계획대로 지켜졌는지 대조된다"
  prohibitions:
    - statement: "2세션 불참자의 사유를 관찰자가 대신 해석해 결과를 보정하지 않는다 — 「바빠서 못 온 것이지 재미없어서가 아니다」 같은 해석은 기록에 사유로만 남기고, 참석 인원 숫자 자체는 그대로 둔다. H1이 재는 것은 「일주일 뒤에 다시 왔는가」라는 행동이다"
      status: resolved
      verification: judgment
    - statement: "`experiment-results.md`에서 가설을 판정하지 않는다 — 이 문서는 재료를 모으는 곳이고 판정은 Phase 6의 일이다. 여기서 「H1 통과」 같은 결론을 먼저 써 두면 Phase 6이 그 결론을 확인하는 절차로 전락한다"
      status: resolved
      verification: judgment
    - statement: "세션 기록과 결과 문서에 참가자의 실명·연락처를 적지 않는다 — 가명 또는 캐릭터 이름만 쓴다"
      status: resolved
      verification: test
---

<objective>
1세션으로부터 정확히 1주 뒤에 2세션을 돌린다. 몇 명이 왔는지, 손으로 쓴 리캡만 읽고 이어 시작할 수 있었는지, 세션을 완주했는지 흐지부지됐는지를 기록하고, 두 세션이 만든 모든 숫자를 Phase 6이 열어 볼 한 장으로 모은다.

Purpose: **「2세션에 몇 명이 왔는가」가 H1의 답이다.** 이 실험 전체가 그 한 숫자를 얻기 위해 설계됐고, H1은 프로젝트를 멈출 수 있는 두 조건 중 하나다. 1주 간격을 줄이면 그 숫자의 의미가 사라진다. 그리고 리캡만으로 이어 시작할 수 있었는지는 오직 이 순간에만 관찰할 수 있다 — 지나가면 다시 못 잰다.

Output: `docs/experiment/session-2-log.md`(채워진 2세션 관찰 기록), `docs/experiment/experiment-results.md`(Phase 6 입력 한 장).

**이 계획은 1세션으로부터 정확히 1주 뒤에야 시작될 수 있다.** 체크포인트에서 일주일 넘게 멈춰 있는 것이 정상이며, 그것이 이 실험의 설계다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-1/05-CONTEXT.md
@.planning/phases/05-1/05-05-SUMMARY.md
</context>

## Artifacts this phase produces (이 계획 몫)

| 종류 | 이름 | 위치 |
|---|---|---|
| 새 문서 | 2세션 관찰 기록 | `docs/experiment/session-2-log.md` |
| 새 문서 | 실험 결과 한 장 (Phase 6 입력) | `docs/experiment/experiment-results.md` |
| 채움 | `session-1-recap.md`의 「2세션 시작 시 확인할 것」 절 | `docs/experiment/session-1-recap.md` (05-05이 만든 빈 칸을 채운다) |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 정확히 1주 뒤에 2세션을 진행한다 (3~4시간)</name>
  <precondition>`docs/experiment/session-1-log.md`와 `docs/experiment/session-1-recap.md`가 존재하고, 오늘이 `session-prep.md`에 적힌 2세션 날짜다(1세션으로부터 7일 뒤). 날짜가 아직 안 됐으면 여기서 기다린다 — 당겨서 진행하지 않는다.</precondition>
  <read_first>
    - `docs/experiment/session-1-recap.md` — 참가자에게 보낸 리캡 전문. 2세션 시작 때 이것을 읽어 준다
    - `docs/experiment/session-1-log.md` — 1세션이 어디서 끝났는지, 시계가 몇 칸이었는지
    - `docs/experiment/session-prep.md` — 확정된 2세션 날짜와 참가자 넷
    - `docs/experiment/observation-log-template.md` — 이번에도 같은 양식을 복사해 쓴다
    - `README.md` 「실험 당일 실행 절차」 — 서버 기동과 링크
  </read_first>
  <what-built>
    1세션이 끝났고, 그 상황을 사람이 손으로 쓴 3~5줄 리캡이 참가자 전원에게 공유되었다. 코드도 준비물도 1세션과 똑같은 상태다 — 이 계획이 새로 만든 것은 없다. 이번에 재는 것은 오직 하나, **일주일이 지난 뒤에 사람들이 다시 오는가**이다. 그리고 왔다면, 리캡만 읽고 이야기를 이어 붙일 수 있는가이다.
  </what-built>
  <action>
    사람에게 2세션 진행을 요청하고, 끝난 뒤 아래 「알려 줄 것」 항목을 받아 적는다. 1세션과 같은 절차이므로 여기서 다시 쓰지 말고 준비된 문서를 가리키되, **이번 세션에만 있는 두 관찰**(리캡만으로 이어 시작했는가 / 몇 명이 왔는가)을 놓치지 않게 앞에 세운다.
  </action>
  <how-to-verify>
    1. **세션 시작 (MEAS-05 판정 순간 — 이때만 관찰할 수 있다)**
       - `session-1-recap.md`의 리캡 3~5줄을 그대로 읽어 준다. **그 밖의 설명을 먼저 덧붙이지 않는다.**
       - 읽은 뒤 참가자들이 바로 이어서 플레이를 시작하는지, 아니면 「그래서 그때 뭐였지?」를 묻는지 본다. 물었다면 **무엇을 물었는지** 그대로 적는다 — 그것이 리캡에서 빠진 것이다.
       - 참석 인원을 적는다. 못 온 사람이 있으면 사유를 들은 그대로 적는다 (사유를 해석하지 말고 들은 문장으로).
    2. **세션 중 (3~4시간)** — 1세션과 같다. `observation-log-template.md`를 복사해 태그 넷(`몰입`/`마찰`/`억지`/`사고`)으로 틈틈이 적는다. 진행자 노릇을 하지 않고, 했다면 개입 자기 점검 칸에 적는다.
    3. **세션 종료 후** — 집계를 확인하고 파일 경로를 적는다. 위협 시계가 몇 칸까지 갔는지, 파국에 도달했는지 적는다.
    4. **완주 판정(EXP-03, D-53)** — 참석 인원 수와 끝까지 있었는지를 보고 완주인지 흐지부지인지 판단한다. 사전에 정한 기준 시간은 없다. **판단 근거 한 줄을 반드시 함께 남긴다.**
    5. 아래를 그대로 알려 준다:
       - 세션 날짜, 시작 시각, 종료 시각
       - **참석 인원 (넷 중 몇 명)**, 못 온 사람의 사유를 들은 그대로
       - 리캡만 읽고 이어 시작했는가 / 추가 설명이 필요했는가 / 필요했다면 무엇을 물었는가
       - 참가자별 끝까지 있었는지
       - 관찰 기록에 적은 것 전부 (태그 + 시각 + 한 줄)
       - 개입 자기 점검 — 했는가, 했다면 무엇을
       - 위협 시계가 몇 칸까지 갔는지, 파국에 도달했는지
       - 완주 판정과 판단 근거 한 줄
       - 집계 조회 출력과 집계 파일 경로
       - 세션 중 막힌 것 (요청 한도 거부, 스트림 정지, 파싱 실패 등)
  </how-to-verify>
  <acceptance_criteria>
    - 2세션이 1세션으로부터 정확히 7일 뒤에 진행되었다 — 아니라면 실제 간격과 그렇게 된 사유가 확보되었다
    - 참석 인원이 「넷 중 몇 명」으로 확보되었다 (H1의 핵심 입력값)
    - 리캡만으로 이어 시작했는지에 대한 답이 예/아니오와 (아니오라면) 참가자가 실제로 물은 내용으로 확보되었다 (MEAS-05)
    - 완주/흐지부지 판정과 판단 근거 한 줄이 확보되었다 (EXP-03)
    - 못 온 사람의 사유가 들은 그대로 확보되었다 — 관찰자의 해석이 아니라 참가자의 말
    - 개입 여부에 대한 답이 확보되었다
    - 집계 파일 경로가 확보되었다
    - 확보된 정보 어디에도 참가자 실명·연락처가 들어 있지 않다
  </acceptance_criteria>
  <resume-signal>2세션이 끝나면 위 「알려 줄 것」 열 항목을 알려 주세요. 2세션이 아예 성사되지 않았다면 그것도 결과입니다 — 몇 명이 못 온다고 했는지와 그 사유를 들은 그대로 알려 주세요.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 2세션 기록과 실험 결과 한 장을 남긴다</name>
  <files>docs/experiment/session-2-log.md, docs/experiment/experiment-results.md, docs/experiment/session-1-recap.md</files>
  <read_first>
    - Task 1에서 사람이 준 답 전부
    - `docs/experiment/observation-log-template.md` — 칸 구성을 그대로 따른다
    - `docs/experiment/session-1-log.md` — 두 세션을 나란히 놓을 때의 형식 기준
    - `docs/experiment/session-1-recap.md` — 「2세션 시작 시 확인할 것」 절의 빈 칸을 채운다
    - `docs/experiment/session-prep.md` — 드라이런 결과, 한도 대응 결정, D-58 캐비어트
    - `.planning/ROADMAP.md` Phase 6 절과 「게이트」 표 — 이번 실험의 숫자가 어떻게 쓰이는지. 결과 문서는 그 표가 필요로 하는 것을 전부 담아야 한다
    - `.planning/REQUIREMENTS.md` HYP-01~HYP-06 — 각 가설의 판정 기준
  </read_first>
  <action>
    받은 답을 파일로 남기고, 두 세션이 만든 것을 한 장으로 모은다. **사람이 말한 문장은 요약하거나 다듬지 않는다** — 특히 못 온 사유와 참가자가 물은 내용은 들은 그대로 옮긴다.

    `docs/experiment/session-2-log.md`: `observation-log-template.md`의 칸 구성을 그대로 따라 채운다. 1세션 로그와 같은 모양이어야 두 세션을 나란히 읽을 수 있다. 세션 메타, 참가자 표(이번엔 **참석 여부** 열이 핵심), 관찰 표, 개입 자기 점검, 세션 마감(시계 도달 칸·파국 도달 여부·완주 판정과 근거·집계 파일 경로). 못 온 사람이 있으면 참가자 표의 그 행에 사유를 들은 그대로 적는다. 답을 못 받은 칸은 「미확보」로 남긴다.

    `docs/experiment/session-1-recap.md`의 `## 2세션 시작 시 확인할 것` 절을 채운다 — 리캡만 읽고 이어 시작했는지, 추가 설명이 필요했다면 참가자가 무엇을 물었는지, 그래서 리캡에서 무엇이 빠졌던 것인지 한 줄. 이 절이 MEAS-05의 실제 판정 근거다. 위의 `## 리캡 본문` 절은 한 글자도 건드리지 않는다.

    `docs/experiment/experiment-results.md`를 새로 쓴다. Phase 6이 게이트 판정을 할 때 열어 볼 한 장이다. **여기서 가설을 판정하지 않는다** — 재료만 모으고 판정은 Phase 6의 일이다. 담을 것:
    - **두 세션 요약 표** — 세션 번호 / 날짜 / 간격(일) / 시작·종료 시각 / 참석 인원 / 완주 판정 / 시계 도달 칸 / 집계 JSON 경로
    - **가설별 입력값 목록** — H1~H6 각각에 대해 「이번 실험이 확보한 것」과 「그것이 어느 파일 어디에 있는지」를 한 줄씩. 예를 들어 H1은 2세션 참석 인원과 완주 판정, H5는 두 집계 JSON의 토큰 소모량과 턴 수, H2는 실패 횟수 대비 시계 진행 횟수와 `억지` 태그 관찰, H4는 사건 기록의 플레이어 원문·제안·확정 여부와 `마찰` 태그 관찰, H6은 비경험자 2명의 캐릭터 만들기 소요 시간과 완료 여부. 확보하지 못한 것이 있으면 「미확보」와 그 사유를 적는다.
    - **애착 질문 답 네 개** (1세션 로그에서 옮겨 온 원문 그대로)
    - **캐비어트** — ① 무료 티어 모델을 썼으므로(D-58) H1 결과가 나빴을 때 「AI 진행자 개념의 한계」인지 「모델이 약해서」인지 구분되지 않는다는 유보. H5는 이 유보의 영향을 받지 않는다(토큰 소모량은 진짜 숫자이므로 공개 단가를 곱해 추정하면 된다) ② 관찰자가 참가자이기도 했다는 것(D-56) ③ 개입이 있었다면 그 내용 ④ 참가자가 서로 아는 사이라 「낯선 사람과의 플레이」는 이 실험이 답하지 않는다는 것 ⑤ 세션 중 도구 문제(요청 한도·스트림 정지)가 있었다면 그것이 「흐지부지」 판정에 얼마나 섞였는지
    - **원가 재계산에 필요한 것** — STATE.md의 미해결 항목 1번(원가 문서의 $0.30이 「최근 대화 20턴」 전제인데 확정 규칙은 10턴이라 다시 계산해야 한다)이 Phase 6에 걸려 있다. 이번 실측 토큰 수와 턴 수가 그 재계산의 입력값이라는 것을 한 줄로 가리킨다
  </action>
  <verify>
    <automated>test -f docs/experiment/session-2-log.md && test -f docs/experiment/experiment-results.md && uv run python -c "import re, datetime, pathlib; ds=[datetime.date.fromisoformat(re.search(r'\d{4}-\d{2}-\d{2}', pathlib.Path(f).read_text(encoding='utf-8')).group()) for f in ('docs/experiment/session-1-log.md','docs/experiment/session-2-log.md')]; gap=(ds[1]-ds[0]).days; print('세션 간격', gap, '일'); assert gap == 7, '1주 간격이 아니다'"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/experiment/session-2-log.md`와 `docs/experiment/experiment-results.md`가 둘 다 존재한다
    - 두 세션 로그에서 처음 나오는 날짜의 차이가 정확히 7일이다 — 위 자동 검사가 이를 단언한다. 실제 간격이 7일이 아니었다면 자동 검사가 실패하며, 그 경우 실제 간격과 사유를 결과 문서에 적고 이 기준을 사람이 명시적으로 완화한 기록을 남긴다
    - `session-2-log.md`의 참가자 표에 참석 여부 열이 있고 네 행이 전부 채워져 있다
    - 완주/흐지부지 판정과 판단 근거 한 줄이 들어 있다
    - `session-1-recap.md`의 `## 2세션 시작 시 확인할 것` 절이 채워졌고, `## 리캡 본문` 절의 줄 수는 그대로 3~5줄이다
    - `experiment-results.md`에 `H1`, `H2`, `H3`, `H4`, `H5`, `H6` 여섯 문자열이 전부 나타나고 각각에 대해 확보한 입력값과 그 위치가 한 줄씩 적혀 있다
    - `experiment-results.md`에 두 집계 JSON 파일 경로가 전부 적혀 있고, 그 경로에 파일이 실제로 존재한다
    - `experiment-results.md`에 D-58 캐비어트가 적혀 있고, H5는 그 영향을 받지 않는다는 진술이 함께 있다
    - `experiment-results.md`에 가설 판정 결론(통과/실패)이 적혀 있지 않다 — 판정은 Phase 6의 일이다
  </acceptance_criteria>
  <done>Phase 6이 `docs/experiment/experiment-results.md` 한 장을 열면 여섯 가설을 채점할 재료가 어디에 있는지 전부 알 수 있고, 어떤 유보를 붙여야 하는지도 함께 나와 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 참가자의 불참 사유(사람의 말) → 결과 문서 | 여기서 해석이 섞이면 H1의 답이 조용히 좋아진다. 이 단계에서 가장 실질적인 위협이다 |
| 세션 기록 → 저장소 | 실명이 넘어가면 이력에서 지우기 어렵다 |
| 결과 문서 → Phase 6 판정 | 재료 문서가 결론을 미리 쓰면 게이트가 형식적 확인 절차로 전락한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Tampering | 불참 사유의 사후 해석 | high | mitigate | Task 1의 수락 기준이 사유를 「들은 그대로」 받도록 못박고, Task 2가 해석 없이 옮긴다. 참석 인원 숫자 자체는 어떤 사유가 있어도 보정하지 않는다 — H1이 재는 것은 행동이다 |
| T-05-22 | Tampering | `experiment-results.md`가 판정을 미리 씀 | high | mitigate | 수락 기준에 「판정 결론이 적혀 있지 않다」를 명시적으로 넣는다. 이 문서는 재료를 모으는 곳이고 게이트는 Phase 6이다 |
| T-05-23 | Information Disclosure | `session-2-log.md`, `experiment-results.md` | high | mitigate | 가명·캐릭터 이름만 쓴다. Task 1 수락 기준과 Task 2 작성 지시 양쪽에 실명 부재 확인이 들어 있다 |
| T-05-24 | Repudiation | 1주 간격 위반이 기록되지 않음 | medium | mitigate | Task 2의 자동 검사가 두 로그의 날짜 차이를 계산해 7일이 아니면 실패한다. 완화하려면 사람이 명시적으로 사유를 남겨야 한다 |
| T-05-25 | Denial of Service | 2세션 도중 요청 한도·스트림 정지 | medium | transfer | 05-04의 결정과 드라이런이 대응을 정했다. 이 계획은 발생 사실을 기록하고, 결과 문서의 캐비어트 ⑤가 그것이 「흐지부지」 판정에 섞인 정도를 Phase 6에 알린다 |
| T-05-SC | Tampering | 패키지 설치 | low | accept | 이 계획에 패키지 설치 명령이 없다 |

**ASVS level 1 / block on high.** high 셋(T-05-21·T-05-22·T-05-23)은 전부 수락 기준과 작성 지시로 완화된다 — 이 단계의 위협은 코드 취약점이 아니라 **측정 무결성**이다.
</threat_model>

<verification>
- 두 세션 로그의 날짜 차이가 정확히 7일임이 자동 검사로 확인된다
- `experiment-results.md`가 존재하고 여섯 가설 각각의 입력값 위치를 가리킨다
- 두 집계 JSON 파일이 문서가 가리키는 경로에 실제로 존재한다
- `uv run pytest -q` 전체 통과 (회귀 확인)
</verification>

<success_criteria>
- EXP-03이 닫힌다 — 1세션 → 정확히 1주 → 2세션이 실제로 진행되었고, 참석 인원과 완주 여부가 기록되었다
- MEAS-05가 닫힌다 — 손으로 쓴 리캡만으로 2세션을 이어 시작할 수 있었는지가 관찰되어 기록되었다
- EXP-02가 닫힌다 — 참가자 넷이 모였고, 우리가 진행자 노릇을 했는지 여부가 두 세션 모두 기록되었다
- Phase 6이 게이트 판정을 시작할 수 있다 — 재료가 한 장에 모였고 유보 조건도 함께 적혀 있다
</success_criteria>

<output>
Create `.planning/phases/05-1/05-06-SUMMARY.md` when done
</output>
